**Verdict: PLAN-NEEDS-MINOR**

The root cause is correct. In [forwarding/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/forwarding/mod.rs:1524), tunnel resolution computes `outer`, then returns a new resolution with `egress_ifindex = endpoint.logical_ifindex`, `tx_ifindex = outer.tx_ifindex`, and `next_hop = outer.next_hop` at lines 1547-1557. `outer.egress_ifindex` is not carried.

The MissingNeighbor arm really keys recovery by `decision.resolution.egress_ifindex`: `neg_key` at [poll_descriptor/mod.rs:2375](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2375), resolved-wins lookup at 2388-2395, resolver enqueue iface lookup at 2450-2458, and kernel probe iface lookup at 2520-2527. `trigger_kernel_arp_probe` binds with `SO_BINDTODEVICE` in [neighbor.rs:46](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/neighbor.rs:46), so binding to the GRE logical iface explains “no ARP on the outer wire.”

Neighbor keying is also as the plan says: neighbors are keyed by `(ifindex, IpAddr)` in [types/forwarding.rs:35](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/types/forwarding.rs:35), `lookup_neighbor_entry` uses that ifindex at [forwarding/mod.rs:1560](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/forwarding/mod.rs:1560), and VLAN transmit split happens later in [session_glue/mod.rs:52](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/session_glue/mod.rs:52) by setting `tx_ifindex = egress.bind_ifindex`. So `outer.egress_ifindex`, not `outer.tx_ifindex`, is the right neighbor key.

The packet does not die before MissingNeighbor side effects on the cold miss path. A cold outer lookup returns `MissingNeighbor`, enters the arm at [poll_descriptor/mod.rs:2204](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2204), fires the recovery side effects, then later the trailing slow-path call reaches the R-C tunnel gate at [slow_path.rs:231](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/tx/dispatch/slow_path.rs:231). R-E remains intact because pending-neigh admission is gated on `tunnel_endpoint_id == 0` at [poll_descriptor/mod.rs:2780](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/poll_descriptor/mod.rs:2780).

Minor fixes before implementation:

- The “~15 literal sites / sentinel avoids touching all constructors” claim is wrong. Adding a Rust struct field requires updating every `ForwardingResolution { ... }` literal unless the code is refactored to constructors/default update syntax. Sentinel fallback is still safe, but it does not remove compile churn.
- Use an accessor like `resolution.neighbor_ifindex()` with `neigh_ifindex > 0 ? neigh_ifindex : egress_ifindex`; `<=0` fallback is safer than only `==0`.
- HA sync should explicitly keep `neigh_ifindex` local-only. `SessionSyncRequest` currently carries `egress_ifindex`, `tx_ifindex`, `tunnel_endpoint_id`, VLAN, hop, and MAC, but no neighbor ifindex in Rust [control.rs:692](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/protocol/control.rs:692) or Go [protocol.go:1664](/home/ps/git/bpfrx/.claude/worktrees/1912-research/pkg/dataplane/userspace/protocol.go:1664). Imported synced sessions are re-resolved on worker upsert at [upsert_synced.rs:39](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/session_glue/commands/upsert_synced.rs:39) and peer-synced hits re-resolve at [session_glue/mod.rs:1021](/home/ps/git/bpfrx/.claude/worktrees/1912-research/userspace-dp/src/afxdp/session_glue/mod.rs:1021). Preserve that.
- Add a regression test that tunnel-marked `MissingNeighbor` probes/resolver-keys on the outer L3 ifindex while still not buffering the frame.

No PLAN-KILL: the root cause is real, Option A is the right shape, and R-C/R-E plaintext-leak protections remain preserved.


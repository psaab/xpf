Codex Task started in the background as task-mqalegbz-xh6amz. Check /codex:status task-mqalegbz-xh6amz for progress.
CODEX_ID=task-mqalegbz-xh6amz
PLAN-NEEDS-REVISION

The r4 blocker is closed for the immediate same-apply stanza-to-list move: A.5 now adds `tc.RIListMember` as an unbind veto, populated with the same `LinuxIfName` + `.0` strip and forwarding-instance skip used by step 0a ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:347), [daemon_apply.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/daemon/daemon_apply.go:216)). The `vrf-` identity lookup is also correct against `BindInterfaceToVRF` ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:365), [vrf.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/routing/vrf.go:126)), and transient `LinkByName` / `LinkSetNoMaster` retention is explicitly folded ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:373)).

Q1: RIListMember population is complete relative to what 0a binds, if implemented literally as specified. `compileRoutingInstances` only appends `interface` stanzas into `ri.Interfaces` ([compiler_routing.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/config/compiler_routing.go:293)), `RoutingInstanceConfig.Interfaces` is the field 0a consumes ([types_routing.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/config/types_routing.go:339)), and 0a skips `instance-type forwarding` and normalizes with `LinuxIfName` plus `.0` trimming ([daemon_apply.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/daemon/daemon_apply.go:218)). Do not “improve” this with `ResolveKernelIfName`: per-unit tunnel names use `uN` ([compiler_interfaces.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/config/compiler_interfaces.go:226)), while 0a would bind the literal `.N` form, so exact 0a parity is the right rule.

Q2: new defect in the list-veto lapse rule. Plan v5 says the `RI-list veto` clears `appliedRI` ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:373)) and test 6 pins “stanza→list move ... entry cleared” ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:541)). Counterexample: commit 1 has tunnel stanza RI `A`, so `appliedRI[gr]=A`; commit 2 moves same VRF to `routing-instances A interface gr-0/0/0.0`, vetoes unbind, and clears the claim; commit 3 removes the RI-list membership. Step 0a has only a bind loop and no unbind pass ([daemon_apply.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/daemon/daemon_apply.go:216)), and tunnel apply only binds on nonempty `tc.RoutingInstance` ([tunnel.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/routing/tunnel.go:183)). With `appliedRI` cleared, there is no remaining path to `LinkSetNoMaster`, so the tunnel stays slaved to VRF `A` after both stanza and list config are gone.

Required revision: make `RIListMember` a veto only, not a claim-lapse event. Retain `appliedRI` on list veto, or add explicit applied-list ownership with symmetric unbind on removed list membership. The minimal fix is to remove list-veto from the clear set and change test 6 to pin retention plus a follow-up “stanza→list→removed” unbind case.

Codex session ID: 019ebab0-0e4f-7db1-8c30-497060cffed4
Resume in Codex: codex resume 019ebab0-0e4f-7db1-8c30-497060cffed4

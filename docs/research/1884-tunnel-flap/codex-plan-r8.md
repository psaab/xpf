Codex Task started in the background as task-mqan0vrp-wpr5br. Check /codex:status task-mqan0vrp-wpr5br for progress.
CODEX_ID=task-mqan0vrp-wpr5br
verdict — PLAN-NEEDS-REVISION

The two r7 findings are closed as stated: stanza claims now update only after successful `BindInterfaceToVRF`, with failure retaining the prior claim ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:407), test pinned at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:616)). MTU text/test sync is also closed: `tc.MTU > 0` reconciles on create/reuse, while `tc.MTU == 0` leaves owned reuse alone ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:270), [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:587), [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:626)).

Q1: I found one remaining stale-master sequence in the overlap case where `tc.RoutingInstance != ""` and `tc.RIListMember != ""` are both present. The plan only transfers list claims in the `stanza empty ∧ RIListMember != ""` branch ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:415)), while the stanza branch on bind failure retains the previous claim ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:407)). Current config paths appear to allow both knobs: tunnel stanza RI is compiled separately from routing-instance interface lists ([compiler_interfaces.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/config/compiler_interfaces.go:189), [compiler_routing.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/config/compiler_routing.go:295)), and validation only warns for unknown RI interfaces, not overlap ([compiler.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/config/compiler.go:915)).

Counterexample: commit 1 stanza-binds A, claim=A. Commit 2 config has stanza B plus RI-list C. Step 0a binds C before ApplyTunnels ([daemon_apply.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/daemon/daemon_apply.go:216)); then stanza bind to B fails, which is a real path through VRF lookup/LinkSetMaster ([vrf.go](/home/ps/git/bpfrx/.claude/worktrees/1884-research/pkg/routing/vrf.go:129)). Plan retains claim=A, while kernel master is C. Commit 3 removes stanza and list. The identity check sees claim A != master C, clears without unbind ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:437)), and 0a has no unbind leg ([plan.md](/home/ps/git/bpfrx/.claude/worktrees/1884-research/docs/research/1884-tunnel-flap/plan.md:427)), so C is stranded.

Q2: No separate MTU defect found. The required revision is to define the both-present precedence: either reject/document mutual exclusion, or in the stanza-bind-failure path observe/transfer the list master when `RIListMember` is nonempty and current master equals `vrf-<RIListMember>`, with a test for stanza A → stanza B + list C where B bind fails → all removed unbinds C.

Codex session ID: 019ebad9-a4f3-7502-a071-fc5078796103
Resume in Codex: codex resume 019ebad9-a4f3-7502-a071-fc5078796103

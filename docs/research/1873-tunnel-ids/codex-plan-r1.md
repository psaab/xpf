**Findings**

MAJOR 1. Path A’s collision probing preserves a deterministic wrong-tunnel case. The repo proves ID is the runtime identity: [tunnels.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/tunnels.go:42) has `"sort.Strings(names)"`; [tunnels.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/tunnels.go:92) assigns `"ID: nextID"` today; Rust installs by ID at [tunnels.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/forwarding_build/tunnels.rs:72): `"state.tunnel_endpoints.insert("`; and live sessions re-resolve by ID at [session_glue/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/session_glue/mod.rs:90): `"if decision.resolution.tunnel_endpoint_id != 0"`.

Worked trace using the plan’s FNV fold: `wg1408.0` and `wg78.0` both fold to `824`. Sorted assignment gives `wg1408.0 -> 824`, `wg78.0 -> 825`. Remove `wg1408.0`; `wg78.0` now gets `824`. Existing `wg1408.0` sessions still store `824`; [forwarding/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/forwarding/mod.rs:1508) does `"state.tunnel_endpoints.get(&tunnel_endpoint_id)"`, so they now resolve to `wg78.0`. WG then encrypts to that endpoint via [frame/wg.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/frame/wg.rs:52) `"forwarding.tunnel_endpoints.get(&id)?"` and [frame/wg.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/frame/wg.rs:98) `"engine.try_encap(&endpoint.wg_peer_pubkey"`. GRE has the same ID lookup at [gre.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/gre.rs:306): `"forwarding.tunnel_endpoints"` / [gre.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/gre.rs:308) `".get(&decision.resolution.tunnel_endpoint_id)?"`. Required change: collisions cannot be log-and-probe for a security fix. Make collisions fail closed, or add a session purge/name-owner check for removed/remapped IDs.

MAJOR 2. The “same config => same ids” invariant is overstated. Snapshot eligibility depends on live interface inventory, not only config. [tunnels.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/tunnels.go:20) filters `"iface.Name == \"\" || iface.Ifindex <= 0"`; [tunnels.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/tunnels.go:53) drops non-WG tunnels with missing source/destination; [tunnels.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/tunnels.go:56) requires `"iface, ok := ifaceByName[ifName]"`. With the collision pair above, one HA node temporarily missing `wg1408.0` assigns `wg78.0 -> 824`; the other assigns `wg78.0 -> 825`. Required change: state the invariant as “same config plus same eligible endpoint set,” and add an eligibility-gate collision test.

MAJOR 3. The live validation does not yet pin the security contract. It checks WG engine survival, not stale-ID absence/misroute. Also the proposed “TWO WG tunnels” validation conflicts with current single-port shim plumbing unless specified carefully: [maps_sync.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/maps_sync.go:1541) says `"snapshotWgListenPort returns the WireGuard listen port"` and [maps_sync.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/dataplane/userspace/maps_sync.go:1542) says `"S2a supports a single WG tunnel"`; each WG control thread binds its own socket at [wg_control.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/coordinator/wg_control.rs:94), `"bind_wg_socket(listen_port)"`, and exits on bind failure at [wg_control.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/coordinator/wg_control.rs:100), `"wg_bind_listen_port:{listen_port}:{err}"`. Required change: add direct stale-session tests for removed ID drop, collision/remap prevention, HA sync ID agreement, and a live recipe that proves both tunnels are actually active.

**Other Checks**

Consumer map: mostly complete, but section 2 underplays route/boolean consumers. Routes carry IDs through [interfaces.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/forwarding_build/interfaces.rs:81) `"tunnel_endpoint_id"` and [fib.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/forwarding_build/fib.rs:174) `"tunnel_endpoint_by_ifindex"`. I found no dense/small array-by-ID consumer; core state uses `FastMap<u16, TunnelEndpoint>` at [forwarding.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/types/forwarding.rs:23).

Absent-ID fail-safe: yes for actual encap. Missing ID returns NoRoute at [forwarding/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/forwarding/mod.rs:1508), and the encap builders use `?`, so absent IDs drop, not plaintext. The cached fallback keeps the nonzero tunnel ID, so [frame/mod.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/afxdp/frame/mod.rs:264) still enters tunnel encap.

Paths B/C/D: C and standalone D are fairly killed. B is not killed fairly as “same outcome”: it is correctness-superior because names are injective, but it is a real wire break. The code proves the current wire is fixed u16: [codec.rs](/home/ps/git/bpfrx/.claude/worktrees/1873-research/userspace-dp/src/event_stream/codec.rs:208) says `"[16:18] TunnelEndpointID u16 LE"` and [sync_protocol.go](/home/ps/git/bpfrx/.claude/worktrees/1873-research/pkg/cluster/sync_protocol.go:154) writes `"val.FibGen"` as u16.

Required changes:
1. Replace collision probing with fail-closed collision handling, or add verified session purge/name-owner protection for every removed/remapped ID.
2. Rewrite the determinism invariant around the eligible endpoint set and add collision plus eligibility-gate tests.
3. Expand tests/live validation to cover stale-session drop, collision/remap prevention, HA agreement during config timing windows, and a realistic two-tunnel WG setup.
4. Reword Path B as semantically superior but too costly/wire-breaking, not “same outcome.”

PLAN-NEEDS-REVISION (with numbered required changes)

Codex session ID: 019eb904-003e-7323-8f7b-3f4753442621
Resume in Codex: codex resume 019eb904-003e-7323-8f7b-3f4753442621

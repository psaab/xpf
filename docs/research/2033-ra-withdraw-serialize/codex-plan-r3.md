VERDICT: NEEDS-REVISION

1. MAJOR - `graceful-upgrades-hard` is broken by `stop()` closing the conn unconditionally. In r3, graceful can store `modeGraceful`, but a racing hard `stop()` still does `conn.Close()` before join at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:335). That can close the socket before the owner emits the goodbye at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:311). The residual is not “sub-µs”; it includes any hard-stop path that closes before a graceful caller arrives or before the owner writes. T7 also still says hard winning first can “correctly” skip goodbye at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:686), contradicting I13. Cleaner fix: arbitrate effective shutdown mode before any close, and make hard pre-close conditional on effective mode still being hard; otherwise join-then-close.

2. MAJOR - I13 is not accurately verified against source. `daemon_apply.go` RA Apply/Clear is under `applySem`, but only for non-cluster configs: [daemon_apply.go](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/pkg/daemon/daemon_apply.go:1011). The `daemon_ha.go:911` and `:1055` Apply calls cited by r3 are not under `applySem`; they are reached from VRRP/reconcile/direct VIP paths such as [daemon_ha.go](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/pkg/daemon/daemon_ha.go:418), [daemon_ha.go](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/pkg/daemon/daemon_ha.go:670), and [daemon_ha_vip.go](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/pkg/daemon/daemon_ha_vip.go:165). Demotion clear is also unguarded at [daemon_ha.go](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/pkg/daemon/daemon_ha.go:448) and [daemon_ha_vip.go](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/pkg/daemon/daemon_ha_vip.go:191). Revise the race model and tests around actual VRRP Apply/Withdraw/Clear concurrency, not “daemon_ha Apply under applySem.”

3. MAJOR - r2 WithdrawOnce/Apply is only partially fixed. The good part: r3 correctly identifies that blocking under `m.mu` deadlocks, because WithdrawOnce must reacquire `m.mu` to release the claim at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:494). But option (b), “skip this pass and rely on reconcile,” at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:504) violates the r2 requirement that Apply wait/retry and not drop the MASTER Apply. Kill option (b); specify defer + second pass only, with claim release via `defer` and bounded standalone goodbye open/write behavior.

4. MODERATE - graceful close-after-join has an unbounded wait hole. r3 preserves hard close-before-join because a `WriteTo` can be stuck at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:330), but graceful waits for owner exit before close at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:345). If the owner is blocked in a normal `WriteTo` or goodbye `WriteTo`, graceful withdraw can hang. `mdlayher/ndp.Conn` supports `SetWriteDeadline`; use bounded writes or a timed graceful fallback.

5. MINOR - r2 invariant wording is mostly fixed, but one shorthand is unsound. The correct test is “no lifetime>0 seq greater than the first goodbye,” which T1 states at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:629). The earlier shorthand “last goodbye > every normal RA” at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/agent-a2ba3a465d97da6a7/docs/research/2033-ra-withdraw-serialize/plan.md:220) would miss a normal RA interleaved between goodbye packets. Fix the shorthand.

R2 status: #1 resolved with the shorthand nit; #2 not fully resolved because option (b) still permits skip; #3 only works for pure hard or pure graceful, but fails under the new hard-vs-graceful race handling.

Codex session ID: 019ee275-84c7-7311-b135-c3c9d329d116
Resume in Codex: codex resume 019ee275-84c7-7311-b135-c3c9d329d116

# Codex plan review — round 16 — #6749 armed-state plan v8.11 (c381b621a44f)

**Reviewer:** Codex (hostile; companion session 019fcaa9-fa07-7ce3-b114-47683c8fcf59; prompt /tmp/codex-6749-r16-prompt.txt). Raw output: /tmp/codex-6749-r16.out. The verdict arrived after the prior session ended; this doc is the of-record transcription (the r16 issue comment already counted it: 13 BLOCKER + 2 MAJOR + 1 MINOR — the raw output numbers 17 findings; #1 is the meta-finding over the disposition table, so 13 content BLOCKERs #2-#12/#14/#17 + MAJOR #13/#15 + MINOR #16; re-counted here as 14 BLOCKER rows including the meta row, matching the raw output's own tags).

**Verdict: DEMAND-REVISION** (14 BLOCKER + 2 MAJOR + 1 MINOR per the raw output's own severity tags).

---

Reviewed the exact `c381b621a44f` blob. Findings (verbatim structure; full text in /tmp/codex-6749-r16.out):

1. [BLOCKER] The §1 disposition table is materially false — every r15 row re-derived: f2 OPEN (B transported as revision A; burned revisions reusable), f3 PARTIAL (migration failure + allocator ordering absent; transport an unbound scalar), f4 OPEN (seed after legacy stamping; one counter retained), f5 OPEN (socket "proof" nonexistent; same-revision stale content passes both legs), f6 OPEN (no typed wire error; response/poll ordering unpinned), f7 PARTIAL (tests still permit precheck bypass; Option B defeats divergence), f8 OPEN (token ABA; stale post-supersession send; missing outcome), f9 OPEN (normative API omits nextWake/Validate; validation not atomic with quiescence), f10 OPEN (restore can fail/timeout with no owner; selective MAC-safe rebind impossible), f11 PARTIAL/MAJOR (no-timeout FIFO directionally sound but budget/tests contradict), f12 OPEN (no causal map/helper state), f13 OPEN/MAJOR (tests encode obsolete/impossible behavior), f14 CLOSED only for the named fixture (MINOR overstatement), f15 OPEN (new severity-High unbounded states).
2. [BLOCKER] Option B destroys the config-identity invariant before any crash — `ActiveConfigRevision()` returning last-confirmed A for in-memory B transports the pair (B,A); suppression does not engage; the helper accepts B under A's identity; A-owned debts match B; the persistence retry has no dataplane observer; the claimed crash non-reuse counter is the per-process archive counter (store.go:233-245, reseeded store_persist.go:516-579; manual ArchiveConfig bumps it, store_persist.go:761-798). Option B needs an allocation journal or B must remain unapplied until durable.
3. [BLOCKER] Legacy migration has no failure policy and no complete allocator ordering — failed migration write enters `loadOtherError` (warn-only, daemon_run_bringup.go:277-305); allocator lacks `max(current, durable high-water)+1`; the Load-time expired-confirm recovery (store_persist.go:110-114 → 171-228) can double-assign 1 unless migration seeds first. Promotion census: Commit/CommitConfirmed/SyncApply/PromoteRollback/Load-migration/expired-window recovery. Min-reader: one central envelope check (envelope.go:217-255) suffices but the writer must raise supported-reader above 1 (envelope.go:111-127).
4. [BLOCKER] `SetActiveRevision` + `ApplyConfig` is not an atomic pair transport — the setter is absent from the normative API inventory; applySem does not bind a manager-global scalar to the `*config.Config` argument (Compile builds outside m.mu, manager_compile.go:177-228; overlapping Compiles documented, maps_sync.go:1266-1270; direct Compile public, cli/apply.go:196-200). Apply-path audit table: DHCP/feed must REPLACE the captured cfg with the reread config; a single `ActivePair()` getter required (background persistence retry takes s.mu without applySem). The robust transport is a token-bound `(cfg, commitRevision, deferIntent)` reservation or a paired Apply input.
5. [BLOCKER] R2 startup seeding occurs too late for the current Compile ordering — Compile stamps Generation/FIB at manager_compile.go:200-217, pings at :324-325 (process.go:18-29,116-125); a surviving helper's first snapshot arrives pre-stamped with reset legacy values and the rollback guard (snapshot.rs:33-105) refuses it; FIB read directly from the map (manager_generation.go:10-22). The committed plan retains one status-poll-seeded counter contradicting the split.
6. [BLOCKER] Socket ownership is not rebase proof, and downward rebase creates a new rollback window — connection-per-request (process_control.go:129-169), unlimited accept sequence (lifecycle.rs:169-179,384-400), no session/nonce/lease/peer identity; a stale same-UID manager can assert the same ping Boolean; lowering both revisions admits already-queued old requests. Rebase requires an authenticated incarnation/nonce with exclusive queue draining, or a helper respawn.
7. [BLOCKER] Freshly minted stale content escapes both suppression and the dual fence — same-revision stale content passes (T1 builds outside m.mu, T2 publishes, T1 mints fresh and sends); every auxiliary producer must validate a current reservation/snapshot token in the common send primitive; the plan's "stale same-commit clone is helper-refused" is impossible with a fresh mint; its stale-A test credits the wrong leg.
8. [BLOCKER] `note_commit_revision` has no typed refusal and no pinned note-first observation path — ControlResponse has only OK/error-string/Status (protocol.go:86-90; control.rs:932-945); Go discards the whole response on OK=false (process_control.go:163-169,219-230); every ordinary handler response carries Status (handlers/mod.rs:260-267), so OK=false+Status cannot identify a CAS refusal — a wire `error_code` is required; note acceptance/echo must process before divergence in one common lineage-observation routine used by responses and polls.
9. [BLOCKER] The re-sync's normative test still permits the exact precheck bypass it claims to close — the test says the drain invokes `ApplyConfig`, never asserts precheck/debt recreation; a bare `d.dp.ApplyConfig` greens it; Option B makes both sides appear at A so neither divergence direction fires.
10. [BLOCKER] The tokened compile reservation has an ABA state resurrection and an incomplete outcome set — T1-finished reservation resurrected by T2's restore (captured booleans insufficient; needs a predecessor-token state machine or serialized Compile); T1 can publish after accepted T2 (token validity checked only at Finish); same-revision T2 has no commit-level supersession; exits not fully classifiable (pending-XSK staged-but-not-published has no outcome; send-failure UNKNOWN vs definite rejection collapsed by process_control.go:106-169); stale API still specifies un-tokened ClearCompileReservation; direct CLI Compile has no reservation.
11. [BLOCKER] Claim validation is not atomic with quiescence, and balanced unwind has no restore owner — §6 omits nextWake/Validate; two incompatible contention rules; operator can bump the token between Validate's release and Prepare's reacquire; Prepare/Notify block on m.mu while recovery owns applySem (process_linkcycle.go:145-162,191-224) defeating try-lock; stop_workers timeout is UNKNOWN (stop_workers.rs:7-30 stops every worker before replying); the abandoned batch's daemon-side finalizer must unconditionally restore after every possibly-landed quiesce before releasing applySem; NotifyLinkCycle is void and swallows failures (:184-224); restore needs a typed result + retry/restart policy; operator-unregister is not config-member removal (manager_status.go:132-179).
12. [BLOCKER] Batch MAC revalidation has no executable safe rebind — rebind clears/reconciles every registered binding globally (rebind.rs:41-71), no member hold set; programRethMAC on mismatch executes another DOWN→MAC→UP cycle (daemon_reth.go:238-270) requiring a renewed settle; the plan must choose repair-before-rebind with renewed settle or a selective helper-side hold; its own test expects the wrong-MAC member's slots physically bound, contradicting the normative prohibition.
13. [MAJOR] No-timeout FIFO directionally sound but the committed fairness model is internally inconsistent — Claim-after-applySem ordering must be normative; the 10/30s acquisition budget contradicts no-timeout; §9 retains the 150s context + position-losing retries; the 60s Warn must stop/reset on acquisition.
14. [BLOCKER] `FabricSyncStateOK()` is a name, not a coherence protocol — no map-commit generation, dirty-before-send transition, accepted-map generation, exact echoed payload proof, or helper-incarnation reset; "no debt" can be true in four unsafe states; `update_fabrics` carries no publication revision (protocol.go:55-84) so the clear ordering is undefined; the stated identity `(name,parent,vlans,queues)` is source-wrong (FabricSnapshot has no VLAN field, protocol.go:315-333; the planner key is planning.rs:160-171).
15. [MAJOR] The v8.11 chain tests can green with the blockers intact — enumerated missing/false-positive cases (migration failure, legacy+expired-confirm 1→2, downgrade refusal, Option-B crash-reuse, DHCP/feed reread-compile assertions, stale-A leg attribution, ping seed ordering, note error-code discrimination, re-sync via revised applyConfigLocked, reservation outcomes/ABA, Claim normative API, stop-workers timeout-but-landed, restore/ctrl failure injection, fairness warning lifecycle, mid-quiesce MAC binding, fabric dirty window/echo/incarnation).
16. [MINOR] Pass-1 budget is a valid fixture bound, not a production ceiling — RethToPhysical iterates the configured map (types.go:62-95); RETH RG IDs extend to 155 (compiler_validate_strict_reth_vrrp.go:17-22); ≈19s/≈70s are baselines not bounds; "env recovery ≤1 poll + RPC" excludes m.mu delay (a legal control RPC can consume ~67s).
17. [BLOCKER] Severity-High residuals remain unbounded, including v8.11-introduced hazards — B aliased to A permanently; crash reuse of an undurable revision; unauthenticated downward rebase; overlapping Compile publishing stale same-revision content; token ABA resurrection; timeout-landed quiescence without restore; restore failure with every worker stopped and no owner; global rebind enabling a wrong-MAC member; readiness true without causal proof. Several reproduce the issue's entire-dataplane-disabled state while all gates believe they are settled.

DEMAND-REVISION

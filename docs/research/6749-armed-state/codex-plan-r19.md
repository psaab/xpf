# Codex plan review — round 19 — #6749 armed-state plan v8.14 (ef735f529)

**Reviewer:** Codex (hostile; `codex exec -C <worktree> -s read-only`, background bash `b9uiq5t7f`; prompt /tmp/codex-6749-r19-prompt.txt). Raw output: /tmp/codex-6749-r19.out. Reviewed the exact `ef735f529` blob.

**Verdict: DEMAND-REVISION** (11 BLOCKER + 4 MAJOR; f7 note verb CLOSED core, f12 late cutoff CLOSED, prior f15 Warn lifecycle remains CLOSED).

---

1. [BLOCKER] §1 r18 disposition table materially false — re-derived: f3/f5/f8/f9/f10/f11 OPEN; f2/f4/f6/f14 PARTIAL; f7/f12 CLOSED; AGY f1/f3/f4/f5 and SMR18-1..5/9/10 remain open through the same mappings; AGY f6 only partial (compiled.Warnings aliases active store state); AGY f2/f9 (note path), AGY f7/SMR18-8 (cutoff), AGY f8/SMR18-6 (live timer) are the clean sub-closures.
2. [BLOCKER] The pair-specific storage primitive is feasible, but the entry gate and the FRR contract cannot coexist: `writeTreeMarked` (db.go:431-461) is implementable with the required pins (only active-pair writes advance durableRevision; volatile advance only after WriteFileDurable returns; restart seeds from the file; a crash after the file lands but before the RAM assignment is conservative). But an ENTRY return at applyConfigLocked necessarily skips FRR (applyRoutingRules is deeper in the same call (daemon_apply.go:314-335, daemon_apply_routing.go:189-226)), directly contradicting "FRR itself follows the commit" and test (a). The plan must choose full gating or an explicit partial-apply/control-plane pipeline.
3. [BLOCKER] Blanket auxiliary suppression creates an unbounded stale-authorization FAIL-OPEN: an A-policy schedule closing during the gate window must still publish the inactive bit, but m.exposureGateActive suppresses the scheduler indefinitely if storage never recovers — the scheduler documents that a missed closing publish leaves the old permit live (manager_compile.go:575-610) — NOT fail-closed. And if gated B is superseded by durable C exposing through its ordinary flow, nothing clears the flag until a redundant drain succeeds; the LinkController additions don't even include a set/clear method. The flag needs pair/token qualification, clearing on ANY observed acceptance of the current active pair, and a safety-critical exception for publications derived from the last exposed pair.
4. [BLOCKER] The HA deferred-outcome transport is not implementable from the named APIs: item.gen exists only inside SessionSync.configApplyLoop (sync_conn_config.go:325-395); OnConfigReceived receives only text and returns only error (sync.go:339-347); the daemon wiring forwards only text (daemon_ha_sync.go:909-913) — the daemon cannot record the claimed HA generation or later settle the fence/CF/high-water. Async direct settlement is also unsafe (recordAppliedConfigGen is deliberately non-CAS, ordered-loop-owned (:265-307)) — settlement must be marshalled through the ordered consumer with an explicit typed outcome and token; the plan defines neither that API nor what happens to applyingConfigGen during ExposurePending.
5. [BLOCKER] The deferred success tails lose security history, omit rollback, and have no retryable settlement state: latest-wins loses A→B session invalidation (A exposed → gated B tightens → gated C promoted → C's debt retains only B→C context → C exposes → A→B invalidation missed, an A-authorized session stays alive; daemon_apply_commit.go:256-269 classifies partial local invalidation as a stale-authorization gap and :405-416 omitted peer invalidation as a permanent security fail-open) — the debt must retain the LAST EXPOSED configuration or conservatively merge every skipped invalidation; the wrapper census omits commit-confirmed auto-rollback (:629-723 — it invalidates sessions and pushes the rollback even if gated); tail-failure ownership is undefined (a crash after exposure but before the tails leaves no owner; a monolithic debt would repeatedly reapply and duplicate completed tails — needs phased ownership); the warning surface is ALIASED (the store assigns s.compiled = compiled and returns the same pointer (store_commit.go:224-324) — appending the transient note mutates active in-memory state, so later applies can log the supposedly nonpersistent warning after recovery — use a response copy or a separate result note).
6. [MAJOR] `appliedRevision` alone does not make every applied marker pair-safe: production callers are daemon_apply.go:70, daemon_apply_commit.go:285/:438 (ActiveDigest)/:475 (MarkAppliedDigest), and the sole ActiveApplied consumer daemon_ha_sync.go:563; current store APIs are text-only (store.go:781-849); §6 adds only a field and comparison — it does not replace ActiveDigest/MarkAppliedDigest with a captured pair or make MarkActiveApplied accept the revision; because promotions may interpose without applySem, an A apply can finish after gated B was promoted and a current-active re-read can falsely stamp B. With a captured-pair stamp the equal-text case is sound (and HA sync carries only text, so the shortcut cannot compare revisions). The primary's periodic push marker (daemon_ha_sync.go:474-497) needs an explicit current-pair-exposed check. sync_conn_config.go:350-395 must settle through f4's ordered mechanism.
7. [BLOCKER] Delayed invalidation contradicts latestBuiltHash and can abandon every viable build (= AGY r19 f1 = SMR19-1): T1 finishes H1; T2 finishes H2 and records it; T1 is refused (H1≠H2); T2 then fails after validation but before send — neither publishes; the inverse completion order (slower T1 overwrites the scalar after T2's H2) is also wrong; no {buildSeq, hash} record or accepted-build high-water exists. And `m.currentPair` adds another ownerless path: R2's promotion makes T1/R1 noncurrent even if R2's build never reaches a send — the ordinary failure posture only promises an identical recommit/feed retry (daemon_apply_dataplane.go:146-158), not an autonomous latest-wins full-apply owner.
8. [BLOCKER] Pre-side-effect freshness validation has no implementable build graph: Compile deletes XDP pins and invokes CompileUserspaceShim BEFORE constructing the snapshot (manager_compile.go:177-214); the snapshot consumes result.NATCounterIDs, so the semantic hash is unavailable before that compile; CompileUserspaceShim itself cleans pins, compiles, attaches XDP, and updates bookkeeping (loader.go:169-208). Required: a specified side-effect-free preparation/compiler phase producing the complete snapshot and NAT IDs, followed by validation, followed by a separately committed mutation phase. Test (d)'s shape cannot validate the proposed mechanics.
9. [BLOCKER] The reservation fold is algebraically wrong: base false; T1 intent true captures false; T2 intent false captures T1's speculative true; BOTH fail pre-publish → oldest-first T1's prior false, head-last T2's prior true → resurrects a deferred epoch that never published. A captured speculative prior cannot be replayed as an absolute value; it must be reduced through predecessor outcomes. Also: no rule for the head finishing while a predecessor remains OPEN (only head Finish folds; a later non-head Finish merely records — no operation necessarily triggers the final fold or clears compileInFlight); PENDING-XSK STAGED is explicitly nonterminal, so a "7×7 outcome matrix" containing it is incoherent; and the matrix is never actually written.
10. [BLOCKER] The mutation lease is contradictory, cross-layer unimplementable, and not transactionally safe: central text says every m.mu acquisition while holding applySem blocks, but §6 still makes PrepareLinkCycleChecked and per-mutation validation try-lock/RetryLater, and tests at plan.md:6005-6009/:6057-6061 explicitly require the stale behavior. The manager owns m.mu; the daemon owns programRethMAC — no enumerated LinkController method can hold the former while executing the latter, and a callback invoked under m.mu conflicts with the no-synchronous-manager→daemon-under-m.mu rule. Per-syscall leasing is insufficient anyway: programRethMAC may execute DOWN, MAC, then UP (daemon_reth.go:238-270) — cancellation after DOWN but before MAC/UP makes the next token check abandon the member, leaving it administratively down with its debt cancelled. The lease must cover the whole member transaction or provide a mandatory token-independent unwind. The "µs-ms" bound is unsupported (up to 4N synchronous RTM_NEWLINK operations with no hard deadline; the budget prices neither whole-transaction N×latency nor per-syscall waits).
11. [BLOCKER] Restore debt still has the wrong intent and no serialization contract: missing-process recovery explicitly runs StartCompile(false) plus a precheck, contradicting the universal rule that StartCompile receives the precheck's rethMACPending result — if MAC work is required, this recreates nondeferred worker startup on stale MACs. Ownership is inconsistent (daemon-side at plan.md:3036-3045, manager-state inventory at :4543-4590). The drain never normatively acquires applySem — without it, the full NotifyLinkCycle or missing-process applyConfigLocked overlaps a MAC batch (the reservation chain does not serialize daemon netlink and restore phases); with it, the restore is a legitimate userspace-bounded FIFO owner and must call the LOCKED apply directly, not the wrapper (daemon_apply.go:49-86). Test (h) asserts neither ownership nor the precheck-derived intent.
12. [BLOCKER] The canonical fabric pair has mutually exclusive semantics and hashes the wrong object: after update_fabrics(P,g) fails, the retained pair IS (P,g) (the mutation was accepted), so the next full carrier carries (P,g) — NOT the simultaneously claimed (P0,g0); producing P0/g0 requires distinct current/helper-accepted/debt-recorded pair storage the single retained projection does not define. The built snapshot already contains Fabrics (builder.go:78-105) and snapshotContentHash hashes that section (:156-178) — replacing the whole slice at publish entry means the validated hash does not describe the bytes sent; no recompute/exclusion rule is specified. And the sample can be causally false: FabricFwdInfo already carries the authoritative map PeerMAC/LocalMAC (types.go:797-804), populated by the daemon before the map mutation (daemon_ha_fabric.go:717-731) — re-resolving peer MAC from an independently refreshed manager cache can bind generation g to a helper payload different from the map; independent LinkByName/address/sysfs reads under m.mu also do not form an atomic kernel sample and introduce unbudgeted lock latency.
13. [MINOR] The note verb closes f7's core defect, but its old-helper narrative is unreachable: old Rust ignores additive unknown fields (no deny_unknown_fields (control.rs:107-139)) then rejects the unknown request TYPE with an untyped response (handlers/mod.rs:124-267); a compliant manager never sends the verb (the capability bit already fails closed); if forced, empty error_code must take byte-identical legacy handling, not CAS-refusal classification.
14. [MAJOR] Tests (a)–(j) can still green over the architecture holes: (a) injects HA generation into a fake debt and misses the production transport/A→B→C invalidation composition/auto-rollback/tail failure/scheduler fail-open/clear-on-other-acceptance; (b) contradicts (a)'s FRR assertion; (c) adequate; (d) misses completed-H2/post-validation failure, out-of-order completion, pair-change-with-no-successor, and the required pure compiler split; (e) not capability-gated old-helper/untyped behavior; (f) no both-prepublish-fail expectation or head-finishes-first rule and its "matrix" includes nonterminal PENDING; (g) can mock an atomic method while production netlink remains outside the lock; misses partial DOWN/MAC/UP abandonment and N×latency; (h) omits applySem, supersession/lifecycle rules, and precheck-derived intent; (i) requires the contradictory P0/g0 carrier and never asserts a hash over the replaced wire Fabrics or map-authoritative MACs; (j) tests Warn cadence, not FIFO correctness or the new unbounded safety failures.
15. [MAJOR] The severity-High hazard budget omits the authorization-affecting unbounded cases (exposure suppression preserving an expired scheduler permit; latest-wins loss of A→B session invalidation; exposure accepted but deferred tails failing ownerless; pair/hash invalidation abandoning every viable build; a cancelled per-syscall lease leaving a member administratively down) and most v8.14-introduced hazards (the blanket exposure flag, typed-tail settlement gap, scalar latest-built hash, pre-side-effect validation fiction, head-last fold error, manager-lock netlink lease, publish-leg fabric replacement/hash split, canonical cache-vs-map MAC split); the risk table still calls performance and architectural mismatch LOW/LOW-MED and §11's budget omits these classes. Q1 remains complete (no eighteenth unowned producer).

DEMAND-REVISION

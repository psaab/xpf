# Codex plan review — round 17 — #6749 armed-state plan v8.12 (08c78677f)

**Reviewer:** Codex (hostile; `codex exec -C <worktree> -s read-only`, background bash `bpji20ck2`; prompt /tmp/codex-6749-r17-prompt.txt). Raw output: /tmp/codex-6749-r17.out. Reviewed the exact `08c78677f2187b8e2bf17e3357c2166b84e8cea4` blob; the ledger-only commit b635a1db7 excluded.

**Verdict: DEMAND-REVISION** (13 BLOCKER + 1 MAJOR + 1 MINOR across 15 numbered findings; #1 is the disposition meta-finding, #13 MAJOR, #15 MINOR).

---

1. [BLOCKER] §1 r16 disposition table materially false — re-derived: f2 OPEN (no exposed-pair state or re-exposure owner), f3 OPEN (migration retry has the same missing exposure transition), f4 OPEN (normative/API text still uses separate revision getters; live-MAC race remains), f5 OPEN (zero-event boot retry has no owner), f6 CLOSED (respawn is the right answer), f7 OPEN (no build/token linearization), f8 OPEN (incomplete wire/API + producer/consumer contract), f9 CLOSED narrowly (test forbids bare d.dp.ApplyConfig; revision transport open under f4), f10 OPEN (stated algorithm still reproduces the ABA), f11 OPEN (incompatible APIs + ownerless restore modes), f12 OPEN (late-arrival event + exposure bound false), f13 OPEN (a legal owner path contains an unbounded wait), f14 OPEN (no atomic writer transaction or support proof), f15 OPEN/MAJOR (tests green), f16 CLOSED (fixture scope), f17 OPEN (new unbounded states). Also: the plan records "13 BLOCKER" for Codex r16 while the of-record recount is 14 tagged blockers including the meta finding.
2. [BLOCKER] Exposure gate has neither representable state nor a re-exposure owner (= AGY f1 = SMR17-1, deepened): today SyncApply replaces the sole in-memory pair before persistence and returns B on write failure (store.go:681/:738); the plan defines no separately retained durable pair, exposed revision, or atomic A→B exposure transition; persistRetryLoop (store_persist.go:389-465) has no daemon callback/manager channel; HA depth: applying durable A reports success → `syncAndApply` stamps B's captured digest as applied (daemon_apply_commit.go:426/:464) and a later equal-text B push skips (daemon_ha_sync.go:474/:549) — the standby is marked converged while running A; migration failure has the same hole. Required: distinct accepted pair vs exposed/durable pair + a latest-wins durability-exposure debt queued after s.mu, drained under applySem through the full revised precheck path; HA applied markers keyed to the exposed pair.
3. [BLOCKER] Paired transport still has three competing revision authorities + the live-MAC leg publishes the wrong pair without its precheck (= AGY f4 = SMR17-3, sharpened): the re-sync text still cites the DELETED SetActiveRevision-under-applySem mechanism (plan.md:2712-2733); `m.pendingCommitRevision` sourced from a separate ActiveConfigRevision() getter (plan.md:3535); ApplyResult likewise (plan.md:3621); no `applyConfigLocked(ctx,cfg,commitRevision)` signature defined; boot reads the pair before the wrapper acquires applySem (daemon_apply.go:49). The live-MAC second leg's ActivePair() re-read can obtain B when B's PERSISTENCE TRANSITION (s.mu-only retry, store_persist.go:402) interposes — not just an operator commit; the second leg must reuse the outer transaction's original pair, and B's durability notification must queue behind the outer transaction as a separate full B apply.
4. [BLOCKER] Ping-first improved, but the promised zero-event boot retry does not exist: production boot calls d.applyConfig once (daemon_run_bringup.go:516); the wrapper only logs and returns on failure (daemon_apply.go:49-58); the status republisher requires a staged lastSnapshot (process_status.go:10-17) and the status loop starts only after the successful Compile tail (manager_compile.go:400-414). Lock answer: ping under applySem+m.mu on daemon paths, m.mu only on direct; 3s base deadline (process_control.go:33-56) — implementable. Missing: the retry owner + incarnation-scoped seeded state.
5. [BLOCKER] Freshness token has no valid build linearization (= AGY f2 = SMR17-2, deepened): mint-after-build → lock order ≠ build order; mint-before-build → every build-affecting input must be captured atomically with the token (scheduler/overlay inputs are captured at different times today); whole-build-under-m.mu → build helpers self-acquire (bumpGeneration, manager_generation.go:33) and expensive BPF work lands under the status/control lock. The send-time (config,revision) check cannot catch the same-commit feed/overlay case (same pair). Plus: auxiliary publishers already hold m.mu while sending — the common primitive needs explicit locked/unlocked entry points or deadlocks; `snapshot_token` is absent from §6 + canaries; the semantic hash (builder.go:156-178) must exclude `snapshot_token` or dedup dies and note-CAS is ineffective (stamping after hashing reopens the race).
6. [BLOCKER] `error_code` has no complete producer/consumer contract: §6 omits ControlResponse.error_code and still says "response shapes unchanged" (plan.md:3555-3561); producer census: stale_completion (rebind, ordering sound), diverged_fabric (update_fabrics), epoch_rollback + publication_rollback (apply_snapshot guards), note_cas_refusal (no concrete dispatcher/API entry added), not_seeded is MANAGER-LOCAL (cannot be a Rust ControlResponse), the snapshot_token refusal has no code listed; consumer: a typed response must survive only when error_code != "", invoke the common lineage observer, select the reservation/debt outcome — copying status for ALL failures would change existing behavior (Rust attaches status to ordinary failed responses, handlers/mod.rs:260-267).
7. [BLOCKER] The predecessor-chain algorithm still reproduces the exact ABA it claims to eliminate: T2's captured prior IS T1's {defer=true,inFlight=true}; T1's Finish while T2 is head is a no-op (never recorded); T2 fails pre-publish and restores its own prior {true,true} — resurrecting T1's inFlight although T1 returned. Non-head outcomes must be RECORDED and head popping must skip/replay completed nodes deterministically. API impossible as written: main text says StartCompile returns a token, §6 declares StartCompile(rethMACPending bool) with no return (plan.md:3579-3599); manager state lists no head/registry/ID counter/finished-node state; panic handling undefined (pre-write / possibly-landed / post-acceptance need three dispositions); PENDING-XSK STAGED ownerless after helper failure (= AGY f5 = SMR17-5; no token stored for the deferred leg; the status loop logs poll failures without respawning, process_status.go:169-245).
8. [BLOCKER] `PrepareLinkCycleChecked` has two incompatible lock/API designs (= SMR17-9): §6 retains the split ValidateClaimToken + PrepareLinkCycle() error (plan.md:3607/:3627) against the merged main-text API; the operator-bump window closes only if the method try-acquires m.mu, validates, KEEPS m.mu across ctrl-disable + stop_workers RPC, and releases only after quiescence has definitely begun or its outcome is UNKNOWN; RetryLater is safe only before any ctrl/worker mutation — a post-quiescence try-lock skip must first route through the mandatory restore finalizer; the phase boundary is unpinned.
9. [BLOCKER] Typed restore debt not executable for ordinary recovery or respawn: the rebind-error→#5134 mapping fails because the #5134 debt self-clears when the last snapshot is already non-deferred (manager_worker_arm_5134.go:50-54) — an ordinary recovery rebind failure leaves workers stopped with a debt that clears itself; missing-process→respawn yields a zeroed helper and rebinding an empty helper cannot restore forwarding — a daemon-owned revised full apply must follow respawn; ctrl-error→retry names no owner/cadence/escalation/terminal posture; §6 carries neither the restore-debt state nor a typed NotifyLinkCycle result. (The timeout-but-landed unconditional restore itself is correct: stop_workers clears volatile fields (stop_workers.rs:7) and rebind reconciles every registered binding (rebind.rs:41-71).)
10. [BLOCKER] The late-arrival promise uses a nonexistent event source and understates exposure by an unbounded queue delay (= AGY f7 = SMR17-7, deepened): the plan promises an "event-fired first-fire attempt" while itself stating no passive link-event machinery exists (plan.md:2414; the netlink listener emits SNMP traps only, daemon_flow.go:725); even with a new hook, the current batch's global rebind binds the late member with the factory MAC, the attempt queues FIFO behind all owners, and a second whole-dataplane quiesce follows — wrong-MAC duration = queue delay + RPCs + programming + settle + second rebind, an unbudgeted outage class.
11. [BLOCKER] The no-timeout FIFO progress proof is false because an apply owner can wait forever: helper teardown is not bounded — Rust worker stop performs blocking thread joins (worker_manager.rs:141) and `stopLocked` performs an unconditional `<-done` after Kill() (process.go:231-244); a D-state/unreaped helper has no deadline; Compile invokes this while the daemon owns applySem (daemon_apply_dataplane.go:137, manager_compile.go:228). The 60s Warn provides visibility, not progress.
12. [BLOCKER] `map_generation` supplies ordering, not atomic mutation identity or semantic coherence (= AGY f3 = SMR17-4, deepened + the fresh-boot trigger ANSWERED): fabric map writers are fab0/fab1 set+clear (daemon_ha_fabric.go:738/:771/:944/:969), direct key writes (maps_fabric.go:16-33), and public legacy-adapter methods that bypass manager state (legacy_dataplane.go:346); the controller writes the BPF shim OUTSIDE m.mu then asks the manager to build/send (controllers.go:68/:112) — no single sample/mint/payload transaction; a counter does not prove semantic correspondence (map written with peer MAC X at g; neighbor resolution changes to Y before the payload samples; helper accepts Y tagged g → accepted==current while map-X/helper-Y diverge) — minting must be tied to a captured mutation projection that ALSO constructs the helper payload; new-helper-zero vs old-helper-zero needs a capability bit/nonzero incarnation seed/protocol bump; a full snapshot must advance accepted even when fabric content is equal (ordering unpinned); §6 + canaries omit both fields; the fabric test still asserts payload-hash ordering (plan.md:4381) despite generation-replaces-hashing. The first-proof trigger EXISTS: startClusterComms starts the fab0/fab1 population goroutines (daemon_ha_sync.go:1223-1242) — so HA takeover is correctly blocked until proof IF the proof is distinguishable and causal.
13. [MAJOR] The v8.12 chain tests can green without closing the architecture (= SMR17-6): per-test false-green shapes enumerated (migration/exposure never asserts retry-alone exposes; paired transport omits revision flow through revised applyConfigLocked/boot under-lock/persistence-between-legs; ping/respawn never exercises the zero-event boot retry or post-respawn paired replay; freshness never proves atomic input capture/hash exclusion/wire presence/all-five routing; typed errors omit per-producer/typed-only preservation/untyped-unchanged; reservation misses the finished-T1-prior case/panic phases/deferred-token/helper death; Prepare/restore mocks an atomic method and omits the split §6 API/operator bump/post-quiescence skip/ordinary rebind failure/fresh-helper replay/ctrl cadence; repair/FIFO omits the absent event source/queue-time-from-event/current-rebind exposure/second quiesce/unbounded stopLocked; causal fabric omits concurrent writers/projection TOCTOU/adapter bypass/old-helper-zero/idempotent advance/first-startup proof); the canary list omits snapshot_token/error_code/map_generation/accepted_fabric_map_generation (plan.md:4437).
14. [BLOCKER] Severity-High residual budget still unbounded: durable B never exposed; HA standby permanently marked converged while running A; zero-event boot apply lost on not_seeded; B published by the live-MAC second leg without B's precheck; reservation inFlight=true forever; restore rebind failure whose #5134 debt self-clears; respawned fresh helper with no paired replay; FIFO behind unbounded stopLocked; late MAC return with no event source/arbitrary delay; readiness stuck false or falsely true. Q1 survives the sixteenth enumeration (no new producer); the v8.12 machinery does not.
15. [MINOR] The 60-second queue-Warn lifecycle is internally inconsistent (= AGY f8 = SMR17-8): "Warn every 60s while waiting" vs "edge-reset per queue episode" vs the test's one-fire-then-stop; define whether RetryLater starts a new episode and choose one-shot edge vs periodic reporting.

DEMAND-REVISION

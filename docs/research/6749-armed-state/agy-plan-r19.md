# AGY plan review — round 19 — #6749 armed-state plan v8.14 (ef735f529)

**Reviewer:** AGY (hostile, zero-tool-call inline-evidence constraint; prompt /tmp/agy-6749-r19-prompt.txt assembled at 116,959 bytes). Raw output: /tmp/agy-6749-r19.out. Background bash `b4u6mmwvz` (direct `agy --print-timeout 9m --print`).

**Verdict: DEMAND-REVISION** (3 BLOCKER + 3 MAJOR + 1 MINOR + 1 NIT; f3 NOT-VERIFIED on SMR re-derivation — see below).

---

1. **[BLOCKER] Delayed-invalidation contradiction: T2's pre-publish failure overwrites `m.latestBuiltHash`, abandoning BOTH builds** (§5-C epoch contract, §9(d)): T1 records H1; T2 records H2 over it; T2 fails pre-publish (never accepted); T1's publish-leg check (H1 != H2) abandons T1 anyway — no build can publish, and T1's SUPERSEDED report would be false. (= SMR19-1, which independently found the leg incoherent with the canonical fabrics replacement; the shared fix is the leg's DELETION — the pair-current + accepted-successor legs + the token backstop cover every case, including this both-abandoned one.)
2. **[BLOCKER] `m.exposureGateActive` uncleared on subsequent non-gated commits** (§5-C (vi), §6, §9(a)): storage recovers, operator commits C2 (write succeeds, R_c2 ≤ durableRevision), C2 applies directly with outcome Exposed, C2 SUPERSEDES exposure debt D1 (cancelled, never drains) — and only the drain clears the flag, so it stays set forever with C2 fully active. (= SMR19-3(iii), sharper: the non-gated successful apply must ALSO clear the flag.)
3. **[BLOCKER] Reservation fold algebra overwrites accepted intent with stale captured prior** (§5-C, §6 matrix, §9(f)): traced as T1 ACCEPTED (defer=true) then T2 PRE-PUBLISH FAILURE restoring T2's prior (claimed false) → final defer=false while the helper runs true. **SMR re-derivation: NOT-VERIFIED** — T2 starts while T1 is in-flight, so T2's captured prior IS T1's current state `(true, true)`, not `(false, false)`; the fold yields defer=true, coherent with the helper. The trace's step 2 ("T2 captures prior deferWorkers = false") misreads the capture semantics. (The chain order is StartCompile order; the older node's state at the newer node's start includes the older node's intent.)
4. **[MAJOR] `MarkActiveApplied` call path subject to a concurrent `store.Commit` TOCTOU race** (daemon_apply.go:49-60; §5-C (iv), §6): `MarkActiveApplied()` takes no parameters and reads the store's current active revision; a concurrent `store.Commit(R3)` during R2's apply records R3 applied though only R2 was exposed. Required: `MarkActiveApplied(digest, revision)` carrying the pair actually exposed. (New, verified against the inline evidence.)
5. **[MAJOR] Uncomputed fabric replacement at publish-leg entry invalidates the semantic hash** (§5-C (i), §6 item 11, §9(i)): the hash is computed at build end over the built fabrics; the publish leg replaces the section from the canonical pair without recomputing — the wire content diverges from `m.latestBuiltHash`. (= SMR19-1; resolved by the hash-machinery deletion.)
6. **[MINOR] Netlink mutation-lease hold latency assumption (µs-ms) invalid for link-cycle operations** (daemon_reth.go:238-270): setDown/setHardwareAddr/setUp invoke synchronous driver state machines (50-500ms per interface on mlx5/i40e-class drivers); m.mu across each syscall stalls all m.mu readers for seconds on a multi-member batch. (SMR r19's narrowed form: the lease covers the DECISION (the re-read) only — the syscall runs unlocked; a cancellation landing mid-syscall is wasted-but-harmless work (no operator MAC verb exists; the operator's own reconcile owns the member's end state), and the next mutation's re-read gates the decision.)
7. **[NIT] `requestDetailedLocked` typed error API risks standard-idiom caller drops** (process_control.go:145-170; §6 item 12): a populated response beside a non-nil error requires callers to check `resp.error_code` before `if err != nil` — any un-updated or future caller following the standard idiom drops the code. Adopted: a custom error type encapsulating `error_code` (and the decoded response) enforced via `errors.As` — no caller-discipline trap.

Evidence wishes (informational): store.go/store_commit.go (MarkActiveApplied/ActivePair/durableRevision mutex invariants); manager_compile.go:177-350 (publish-leg ordering); daemon_apply_commit.go:170-260 (ApplyOutcome handling, warnings population, flag clearing).

DEMAND-REVISION

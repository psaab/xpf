Reviewed the exact `3e388fde8139ac2b4102bd2601f956a570391b83` blob via `git show`, not the later worktree copy. Source claims were checked against that commit. No files modified.

1. **BLOCKER — the r5 disposition table is materially false.** The table is at `plan.md@3e388fde8:78-93`.

| r5 row | Round-6 audit |
|---|---|
| B2 | **OPEN/PARTIAL.** Pending-only E2 fixes automatic re-registration of retained operator records. It does not fix C2’s wire collision, and planner candidate elision can delete the operator record entirely. |
| B3 | **CLOSED narrowly.** Wholesale restoration preserves A’s identities and claims. Its “real volatile state” assertion introduces a separate slot-alias problem; finding 3. |
| B4 | **OPEN, fix is unsafe.** Replan-only `update_fabrics` can publish new logical bindings while workers remain bound to old physical interfaces; finding 4. |
| B5 | **CLOSED only on the post-teardown failure leg.** The same live-sysfs/empty-vector sink is reachable through the new `update_fabrics` replan; finding 5. |
| B6 | **PARTIAL.** The apply→MAC window closes, but clearing before asynchronous completion opens a quiescence/completion race; finding 6. |
| B7 | **OPEN.** Several pending states are non-actionable by plain rebind, failed tagged completion is not recoverable, and permanent failures churn the full worker set; finding 7. |
| B8 | **OPEN.** Rust latch consumption is insufficient; Go retains and republishes the old latch, snapshot generation is the wrong epoch, and MAC failure has no durable recovery; finding 8. |
| M9 | **OPEN.** Items 12–19 remain false-green-capable; finding 9. |
| M10 | **CLOSED narrowly.** The edge-trigger requirement and repeated-tick test are adequate. |
| AGY minor-1 / SMR N1 | **CLOSED** with M10. |
| AGY nit-2 / SMR N2 | **CLOSED.** The requested documentation is present. |
| SMR N3 | **CLOSED narrowly.** Item 14(iv) adds the idempotent re-arm pin, but that does not repair production retry ownership. |

2. **BLOCKER — B2’s C2 semantics remain unimplementable, and E2 cannot preserve claims that the planner deletes.** V7 says the tri-state is “unchanged from v6” and retains generic C2 at `plan.md@3e388fde8:476-491`, while declaring only two additive wire fields—neither identifies the operator verb—at `plan.md@3e388fde8:700-711`. V6’s inherited rule was `register → none`, `disarm → operator`, but both operations serialize identically as `(registered=true, armed=false)` in [control.go:104](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/control.go:104). Go transmits only the two booleans in [protocol_binding.go:11](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/protocol_binding.go:11); Rust receives only them in [control.rs:959](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/protocol/control.rs:959) and applies them without provenance in [binding.rs:27](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/handlers/binding.rs:27) and [queue.rs:29](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/handlers/queue.rs:29). “Set the state before reconcile” cannot distinguish identical requests.

The plan must either add an operation discriminator or explicitly change the ownership contract so register and disarm intentionally have the same state. Item 9’s code-order test cannot prove the currently promised distinction.

Worse, “operator records are never planner-restored” only governs retained candidates. A fabric with an invalid parent is omitted before identity carry in [planning.rs:462](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:462); a regular interface with zero/unreadable queue count is likewise omitted at [planning.rs:452](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:452). The operator record therefore disappears without a global fan-out. When the candidate returns, S5 creates it as `pending`, allowing later automatic arming—directly contradicting `plan.md@3e388fde8:775-783`.

Q1 narrowly passes: assuming every proposed S/C transition is implemented exactly, I found no additional terminal producer of precisely `Registered && !Armed && none`. That enumeration is insufficient, however: record deletion and the stale-physical `Registered && Armed && none` state in finding 4 both bypass D entirely.

3. **MAJOR — B3’s plain restoration is directionally correct, but its volatile-state claim is false.** Capturing `existing_bindings` before apply at [snapshot.rs:158](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/handlers/snapshot.rs:158) and restoring it wholesale correctly preserves accepted-A claims. The #4952 and #5143 product pins require failure reporting, no persistence, prior snapshot/generation retention, and the typed failure stage; see [server/tests.rs:1156](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/tests.rs:1156) and [server/tests.rs:1493](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/tests.rs:1493). They do not require retained B.

The literal B-vector assertion belongs to #6140’s full-apply leg proof, explicitly documented at [server/tests.rs:1330](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/tests.rs:1330) and asserted at [server/tests.rs:1475](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/tests.rs:1475). Changing it to restored A is valid only if another observable continues to prove the full-apply leg.

The new hole is partial spawn. B’s live states and physical identities are installed by numeric slot before spawning at [bringup.rs:273](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/bringup.rs:273), and already-launched B workers deliberately survive a later spawn failure at [bringup.rs:172](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/bringup.rs:172), pinned by [coordinator/tests.rs:4151](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/tests.rs:4151). After restoring A, `refresh_bindings` looks up volatile state by slot only at [refresh_bindings.rs:25](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/refresh_bindings.rs:25). It can therefore copy B’s socket, heartbeat, and `ready` state onto an unrelated A identity. S4 keeps forwarding fail-closed, but the status is not “real” and is a new v7 reporting alias. Require an identity match before copying volatile state, or stop/clear B survivors; item 16 must use a multi-worker partial spawn plus A/B reshuffle.

4. **BLOCKER — B4’s replan-only `update_fabrics` creates a wrong-physical binding state while reporting enabled.** The proposed handler updates fabrics, replans, then merely refreshes status (`plan.md@3e388fde8:534-546`). Fabric snapshots contain the live parent name and ifindex produced in [fabric.go:38](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/fabric.go:38), and the plan key includes those values in [planning.rs:160](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:160). R3 nevertheless carries control state by `(interface, queue_id)`.

Trace: `fab0` keeps the same parent name but receives a new ifindex. Replan publishes that new ifindex while carrying `registered=true, armed=true, none`. No pending slot exists. Actual worker identities change only during reconcile at [bringup.rs:273](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/bringup.rs:273), which `update_fabrics` does not call. Slot-only refresh copies the old worker’s ready state onto the new logical binding. Rust declares enabled solely from registered/armed at [status.rs:267](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/status.rs:267), and Go programs the reported new ifindex to that old slot at [maps_sync.go:689](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/maps_sync.go:689). Packets are then directed toward an XSK bound to the old physical device while status can say enabled and ready.

Removal-only, contraction, and same-name/new-ifindex changes may produce no pending record, so the new retry never repairs them. This is a new v7 hazard in the exact issue class. Every binding-plan projection change must either reconcile atomically or first gate the entire non-operator vector unarmed/pending and schedule a guaranteed reconcile—including removal/empty-vector cases.

5. **BLOCKER — B5’s live-sysfs race has been relocated into `update_fabrics`, and Q5 understates its cadence.** `update_fabrics` compares the entire `FabricSnapshot`, not the binding projection; that structure includes peer data, MACs, and `up` in [snapshot.rs:445](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/protocol/snapshot.rs:445). Any such change invokes the proposed full replan. Replanning reevaluates every regular interface whose snapshot queue count is zero through sysfs at [planning.rs:575](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:575); an unreadable directory returns zero at [planning.rs:605](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:605), omits that candidate, and can install `[]` at [planning.rs:492](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:492).

After sysfs recovers, plain rebind still does not replan. An identical apply computes both keys against the now-recovered environment at [snapshot.rs:163](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/handlers/snapshot.rs:163), while the same-plan deficit exits immediately for zero runnable bindings at [planning.rs:36](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/planning.rs:36). The permanent empty-vector sink therefore remains.

Q5’s “every 30s” premise is also wrong: fabric refresh is both periodic and netlink-event-driven at [daemon_ha_fabric.go:243](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/daemon/daemon_ha_fabric.go:243) and [daemon_ha_fabric.go:1039](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/daemon/daemon_ha_fabric.go:1039). Replan only when the binding projection changes; persist telemetry-only changes without replanning. Once geometry changes correctly trigger reconciliation, oscillation also needs rate/backoff treatment.

6. **BLOCKER — B6 closes the old pre-MAC window but opens a completion/quiescence race.** V7 clears defer just before `NotifyLinkCycle`/reapply (`plan.md@3e388fde8:551-563`). `NotifyLinkCycle` then deliberately sleeps one second before acquiring the manager lock and issuing rebind because mlx5 queues are not reusable immediately at [process_linkcycle.go:184](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/process_linkcycle.go:184).

During that second, `m.deferWorkers` is false. The status loop may send a desired global arm—which is an explicit authorized exit—or the new plain pending retry. The global arm can recreate workers before the mandatory quiescence ends, recreating the EBUSY failure this sleep exists to prevent. Item 989’s “tagged completion already expected” negative cannot be implemented from the proposed predicate because no completion-in-flight state exists at `plan.md@3e388fde8:600-608`.

Keep an explicit completion epoch/in-flight gate through the sleep and dispatch, and gate both desired-arm sync and pending retry on it. Clearing a boolean before calling an asynchronous completion owner does not “span completion.”

7. **BLOCKER — B7’s pending retry is neither complete nor operationally acceptable.**

- A failed tagged completion deliberately leaves stored defer true and slots pending (`plan.md@3e388fde8:913-914`). The retry sends an untagged rebind (`plan.md@3e388fde8:600-618`), which is not authorized to consume that latch or converge pending slots. Current `NotifyLinkCycle` warns once and returns on failure at [process_linkcycle.go:219](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/process_linkcycle.go:219). This becomes permanent teardown/rebind churn with no successful exit. Completion debt itself must be retried as tagged and epoch-scoped.

- S1/S2 explicitly create `registered=false, pending`, but worker planning ignores unregistered bindings at [bringup.rs:273](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/bringup.rs:273). A plain rebind does not replan and therefore cannot heal a missing interface, yet `anyBinding(pending)` retries it forever.

- The predicate checks desired state, not actual `ForwardingArmed`. The #6165 protocol gate can leave desired true while actual remains false at [manager_ha.go:601](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_ha.go:601); Rust only runs AF_XDP when actual forwarding is armed at [status.rs:414](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/helpers/status.rs:414). Such retries can only stop workers, never converge.

- Every reconcile tears down the full worker set at [reconcile/mod.rs:330](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/mod.rs:330), incurs a 500-ms mlx5 quiescence when workers existed at [teardown.rs:54](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/teardown.rs:54), and can wait ten seconds for readiness at [bringup.rs:30](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/afxdp/coordinator/reconcile/bringup.rs:30). The status loop holds `m.mu` across its RPCs at [process_status.go:162](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/process_status.go:162). Fixed five-second full-set churn for a permanent queue failure is not acceptable, even when ctrl is already fail-closed.

- The first-Compile fix remains underspecified. `apply_snapshot` and status application can fail at [manager_compile.go:350](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_compile.go:350) and [manager_compile.go:369](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_compile.go:369), before today’s loop creation at [manager_compile.go:411](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_compile.go:411). Merely moving it before desired-sync still leaves those paths orphaned.

Use an actionable predicate (`registered`, valid ifindex, actual armed, helper latch clear, no completion in flight), exponential capped backoff with jitter and failure-fingerprint reset, and topology/config/link events as reset triggers. Unregistered pending should wait for a replan-producing event rather than cycling workers.

8. **BLOCKER — B8 lacks an authoritative defer epoch and durable MAC completion.**

- **Go re-latches Rust after successful completion.** Deferred Compile stores `snap.DeferWorkers=true` in `m.lastSnapshot` at [manager_compile.go:330](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_compile.go:330). `SetDeferWorkers(false)` changes only the separate manager boolean at [manager.go:485](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager.go:485), and successful `NotifyLinkCycle` never clears the cached snapshot field at [process_linkcycle.go:219](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/process_linkcycle.go:219). Route and scheduler republishes clone that snapshot wholesale at [manager_overlay.go:188](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_overlay.go:188) and [manager_compile.go:575](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_compile.go:575), setting Rust’s latch true again. Latch consumption must update the Go authority too, or defer must be represented by a dedicated epoch rather than persisted in ordinary snapshots.

- **Snapshot generation is the wrong debt scope.** A FIB-only update increments `lastSnapshot.Generation` at [manager_generation.go:69](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_generation.go:69), and resolved-fabric persistence does likewise at [manager_ha.go:208](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/manager_ha.go:208). These are not newer MAC/config plans, yet the proposed equality at `plan.md@3e388fde8:583-588` would discard valid #5134 debt. Scope it to a dedicated plan/defer/MAC epoch and define how same-plan publishers carry it.

- **Q3 is a real production sink.** The plan simultaneously leaves the current deferred cleanup unchanged and claims a MAC failure leaves defer set (`plan.md@3e388fde8:557-571`), but current cleanup clears whenever active at [daemon_apply_dataplane.go:71](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/daemon/daemon_apply_dataplane.go:71). No transfer of ownership is specified. More importantly, `programRethMAC` can successfully set the new MAC and then fail `setUp`, returning `(true, error)` at [daemon_reth.go:257](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/daemon/daemon_reth.go:257). A later attempt sees the desired MAC already installed and returns without retrying link-up at [daemon_reth.go:244](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/daemon/daemon_reth.go:244). Thus even “the next commit/event” need not recover it. The daemon needs durable, epoch-scoped MAC/link-up debt with autonomous backoff and explicit phase revalidation.

- **Q4’s narrow Rust ordering is acceptable; the cross-process protocol is not.** The server starts with `snapshot: None` at [lifecycle.rs:313](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/lifecycle.rs:313), and the state file is explicitly best-effort observability, not restoration, at [handlers/mod.rs:296](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/userspace-dp/src/server/handlers/mod.rs:296). Clearing the latch under the handler lock before refresh and one persist is sufficient locally; a crash kills those workers and Go reapplies. The actual atomicity gap is the stale Go latch above, plus uncertain acknowledgements: a small Go control request has a three-second deadline at [process_control.go:33](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/process_control.go:33), while Rust worker readiness can take ten seconds. Completion must therefore be idempotently retryable.

9. **MAJOR — M9 remains false-green-capable.**

- Item 12 adequately pins happy-path expansion.
- Item 13 does not prove autonomous recovery after failed tagged completion, the clear→`NotifyLinkCycle` sleep interval, Go shadow-latch republishing, or recovery from the “MAC set, link-up failed” state.
- Item 14 manually supplies successful retries; it does not exercise unregistered pending, actual-forwarding-false, permanent failure/backoff, or all first-Compile error exits.
- Item 15’s restored-A claim case is good, but it misses claim deletion through fabric candidate omission.
- Item 16 correctly prohibits failure-path replanning, but misses sysfs failure during `update_fabrics` and the partial-B-worker/restored-A slot alias.
- Item 17 tests only a genuinely newer plan-changing commit. It can green while FIB, neighbor, fabric, overlay, or scheduler generation changes wrongly discard live debt.
- Item 18 proves the same-plan deficit predicate, not production ownership or scheduling.
- Item 19 tests only `[] → fab0` and unchanged input. It misses removal/contraction, same-name/new-ifindex, queue-count change, invalid-parent/operator-claim loss, and telemetry-only MAC/up changes.
- The Go retry tests at `plan.md@3e388fde8:989-993` omit actionable registration/ifindex, actual forwarding, failed tagged latch, completion-in-flight, permanent failure progression/reset, and backoff.
- Item 9’s C2 order pin cannot distinguish wire-identical register and disarm operations.

The principal new v7 hazards versus master are the stale-physical `update_fabrics` publication, sysfs-driven empty-vector installation outside failure recovery, restored-A/partial-B volatile aliasing, unbounded full-set retry churn, and Go re-latching a successfully consumed Rust defer state.

10. **NIT — M10 itself is genuinely resolved, but its scope should remain separate from retry observability.** The plan specifies false→true and recovery edges at `plan.md@3e388fde8:626-632` and a repeated-tick test at `plan.md@3e388fde8:997-1000`; that is sufficient for the existing one-second #6165 warning currently emitted at [process_status.go:240](/home/ps/git/kimi-xpf/.claude/worktrees/6749-research-armed-state/pkg/dataplane/userspace/process_status.go:240). The new pending/completion retry machinery still needs its own attempt counter, backoff state, failure fingerprint, and edge/rate-limited diagnostics.

DEMAND-REVISION

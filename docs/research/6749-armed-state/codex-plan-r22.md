# Codex plan review — round 22 — #6749 armed-state plan v8.17 (aca354bba)

**Reviewer:** Codex (hostile; `codex exec -C <worktree> -s read-only`, background bash `bymt66avd`; prompt /tmp/codex-6749-r22-prompt.txt). Raw output: /tmp/codex-6749-r22.out. Reviewed the exact `aca354bba` plan blob and same-commit source, read-only. "The original slot-arming diagnosis remains intact; the v8.17 control-plane architecture does not."

**Verdict: DEMAND-REVISION** (13 BLOCKER + 1 MAJOR).

---

1. [BLOCKER] §1 r21 disposition table materially false — f2 remains open; f3/f4 only partially corrected and contradicted elsewhere; f5-f12 open; f13 right direction, no complete transaction or test; f14 plainly open. Declaring SMR21-7 "superseded" by closeout debt does not define safe failure or supersession semantics.
2. [BLOCKER] The promotion-serialization invariant is false at startup and does not cover every promoted outcome: plain Commit (daemon_apply_commit.go:172-175, promotion at 194-218, apply at 222) and CommitConfirmed (:527-575; `ConfirmCommit` itself is not a promotion (store_commit.go:729-759)) are serialized; **SyncApply holds applySem and promotes at :331-355 but deliberately returns WITHOUT applying for topology or identity changes (:381-402)** — so "every promotion WITH its apply" is false; PromoteRollback's nil rollback target takes bootstrap teardown instead of the normal apply (:629-697); expired-confirm boot recovery and planned legacy revision migration run inside `Load` under only s.mu (store_persist.go:21-24/:171-228) — ordinarily startup-exclusive; `bootstrapFromFile` is another startup promotion outside applySem (daemon_apply_commit.go:14-63); standalone CLI/store fallbacks sit outside the daemon invariant (cli_config.go:301-343). **The fatal startup edge: the rollback executor is registered BEFORE Load (daemon_run.go:130-136); Load re-arms the timer (store_persist.go:231-253); later startup phases run WITHOUT applySem (daemon_run.go:157-172); naming reads the active config independently (daemon_run_naming.go:42-90); manager construction (daemon_run_bringup.go:161-208); dataplane selection/apply at :418-520 — a near-expiry timer can roll B back to A mid-startup, producing B-derived naming with A-derived managers/dataplane.** And: **GO-LOCAL directly invokes revised applyConfigLocked — it would replay a SyncApply promotion that was deliberately REFUSED by the topology/identity guards, bypassing those restart-only checks.** (The persistence-retry claim is sound: it never changes s.active, holds s.mu across the rewrite (store_persist.go:389-465), and the disk envelope is atomically replaced (db.go:431-461) — a drain observes before-or-after durability, never a torn pair.)
3. [BLOCKER] The stale f3/f4 model was not actually removed: plan.md:4295-4305 still says operator promotion and persistence retry can interpose, immediately before lines 4306-4324 say no promotion can interpose; the history still mandates re-read/abort at lines 800-805; test (b) requires abort after interposition at :6857-6864, then requires interposition to be impossible and the leg always complete at :6865-6875, then constructs nondurable B between the legs at :6876-6880 — these requirements cannot coexist and can direct implementation back to r21's indefinite workerless outage.
4. [BLOCKER] The tightening-only closeout has no implementable A-live model or safe persistent-debt semantics: "A-live" must mean the last EXPOSED configuration, not the compiled candidate or store-active predecessor (current `oldActive` is captured from the store (daemon_apply_commit.go:111-118); after gated B followed by C, that value is B although A remains exposed). The existing owners accept WHOLE desired configurations: SNMP starts/stops listeners and replaces all authorization (daemon_snmp_reconcile.go:375-423); web reconciliation changes endpoints before auth and consumes a complete desired API config (management.go:189-249); **web binds are NOT "independent of interface state" — `resolveAPIBinds` resolves configured interface addresses (daemon_run_servers.go:507-539)**; host-auth passes one whole config through every owner (daemon_apply_hostauth.go:53-79). The set-difference narration does not define the necessary synthetic projection or targeted removal APIs — mixed changes (remove A's listener, add B's listener, retain a credential on only one endpoint) cannot safely call these whole-config reconcilers. The debt has no pair key, captured projection, recomputation rule, or supersession fold — if B's removal fails and C later reauthorizes that access, replaying stale B debt removes something C keeps; blindly discarding B can lose a still-required revocation — the retry must RECOMPUTE from actual live authorization to the latest desired state. The failure transition is also missing: if closeout failure does not prevent B exposure, B can run with stale authorization; if it does prevent exposure, closeout adds another unbounded whole-forwarding-plane gate — the plan specifies neither. The operator-session strand is an intentional monotonic-revocation consequence (B may revoke the channel used to submit B), but that posture, the loss of the synchronous note/response, and an out-of-band recovery channel must be explicit. Finally, lines 4490-4493 defer networkd, services while plan.md:4542-4547 still classifies them as FOLLOW.
5. [BLOCKER] `beginFirstExposure` and the completion cursor have no cross-layer lifecycle: acceptance occurs inside Compile (manager_compile.go:350-365) AND in the status loop (pending-XSK, process_status.go:18-37/:120-139); daemon wrappers own invalidation/tails after ApplyConfig returns — §6 adds only a three-valued ApplyOutcome and generic debt context; `ApplyResult` gains only commit revision (plan.md:5938-5942) — no prior pair, ledger ID, cursor, or acceptance event (current ConfigSink/ApplyResult: apply.go:37-40/:97-117). `oldActive` equals the manager prior only when the previous store-active pair was fully exposed; it differs after gated B then direct C — once the uniform exposed-base rule exists, `oldActive` is redundant and unsafe unless explicitly retired. The single `{pair, phaseCursor, completionState}` record cannot represent incomplete B tails PLUS a subsequently accepted C without a per-ledger chain or an explicit fold. The "crash leaves the tail debt" claim is false for daemon-local memory. Finally, an `appliedRevision` sidecar is not the prior configuration: a crash after C acceptance/sidecar persistence but before A→C invalidation leaves no A config and no cursor, and invalidators no-op when old config is nil (daemon_policy_invalidate.go:60-65) — unless the full prior config and cursor are durably recoverable, startup must clear all sessions whenever completion is unprovable, INCLUDING sidecar-present cases.
6. [BLOCKER] A finalized pending-XSK reservation can still publish late, so f8 is not closed (= AGY r22 f2 = SMR22-1, sharper): T1 stages; same-pair T2 starts and finalizes T1; T2 FAILS before acceptance; T1's deferred leg fires — T1 remains pair-current, there is NO accepted newer build, and accepted-token fencing admits T1 — it publishes using a terminal reservation whose later Finish is a no-op. The primitive must verify that the reservation node is still OPEN, or OVERLAP must atomically cancel the staged publisher. Test (f) never exercises this trace.
7. [BLOCKER] The GO-LOCAL live-registration discriminator refers to state that is neither defined nor lifecycle-bounded: current `OnXSKBound` is an unkeyed, one-shot fabric-IPVLAN callback (manager.go:290-293, maps_sync.go:451-456, daemon_apply_interfaces.go:98-109) — it has no pair/token identity or unregister/ack operation. **The actual pending snapshot publisher is `syncSnapshotLocked` (process_status.go:10-140).** The plan defines no registry, lease, timeout, helper-death transition, OVERLAP clear, superseding-promotion clear, or completion acknowledgement; saying "a LEAKED registration … has no live registration" assumes the missing liveness oracle — a lingering entry suppresses GO-LOCAL forever; premature removal attacks a healthy staged owner.
8. [BLOCKER] The accepted-token fence remains normatively contradictory and lacks its seed field: plan.md:4976-4983 still requires newest-SEEN fencing, rejected tokens advancing the fence, and retry reminting, while plan.md:5035-5058 requires newest-ACCEPTED fencing, rejected tokens not advancing, and immutable exact-equal retries — both cannot ship. The accepted-only model is correct (within one Go incarnation, a new build lower than a rejected build cannot occur because buildSeq is monotonic; cross-incarnation authority is handled by reset). But §6 only adds request-side snapshot_token while seeding requires an accepted-token ping/status echo — neither the protocol inventory nor canaries name `accepted_snapshot_token`. Tests also omit rejected-high followed by exact-equal/lower viable retry and restart seeding from accepted rather than seen.
9. [BLOCKER] Fence ownership and settlement exactly-once cannot be implemented on the stated scalar: the plan gives the drain a MAX-CAS ownership token but leaves the loop's begin/end pair untouched — current loop operations are unconditional `Store(gen)` and `Store(0)` (sync_conn_config.go:289-309) — so a loop begin can lower a drain-owned fence, and any loop end can erase it. This requires separate owner slots/a token registry with an effective maximum, or tokenization of every owner — not MAX-CAS by only one writer. Settlement prose does not define settlement-ID allocation, dedup retention/GC, duplicate re-ack, stale-discard acknowledgement, or release on every terminal path; current ordered items contain only `{gen, text}` (sync.go:686-691). Retain-until-processing-ack would correctly make a same-process landed-but-unprocessed item repost, assuming the scheduler and consumer restart survive — the plan defines neither consumer restart nor that state; a process crash loses the channel, cursor, and tail debt together, so the claimed startup replay cannot presently reconstruct the settlement.
10. [BLOCKER] The gate's clear-on-transfer posture lacks the promised retry re-quiescence and current-plan restore: the intended posture is coherent only if each retry becomes a new transaction (reassert the gate, stop any workers an intervening operator verb spawned, perform restore, and rebind the current plan including the operator change) — the plan says this happens but its APIs do not provide it: `PrepareLinkCycle` performs `stop_workers` (process_linkcycle.go:145-163); `NotifyLinkCycle` merely disables, waits, rebinds, and reconciles (:184-224); planned `PrepareLinkCycleChecked` is MAC-claim-token-bound, while restore debt has a different token and is specified to retry `NotifyLinkCycle` — no restore-authorized quiesce operation exists. Similarly, cancellation-insensitive link-down recording is required before token disposition, but §6 still exposes only `ReportMACDebtAttempt`, whose stale results are discarded wholesale — the plan must specify a separate unconditional link-observation transition.
11. [BLOCKER] Runtime-scoped tombstone expiry contradicts the canonical fabric-pair invariant and can resurrect a down path indefinitely: the plan first requires every carrier — including full snapshots — to use the retained map-derived canonical pair verbatim, then says the next full apply discards the tombstone and re-adds any configured fabric even if the operational map remains deliberately zero. Current clear sets `fabricPopulated=false`, after which further clear attempts no-op (daemon_ha_fabric.go:762-778). An unrelated full apply can therefore reintroduce the Rust `FabricLink` while the BPF map remains disabled and no further tombstone is guaranteed: Rust's full build installs configured fabric snapshots (fib.rs:233-264), and when EVERY installed fabric reports down, redirect deliberately falls back to one (fabric.rs:446-464) — A BLACKHOLE. This is not proven benign oscillation: **the tombstone must persist until a successful nonzero map transaction, or the full apply must atomically re-establish operational map state before re-adding the helper link.**
12. [MAJOR] The outbound-marker ordering direction is right, but there is no complete send transaction: current `QueueConfig` accepts raw text and allocates generation before taking `writeMu` (sync_conn_config.go:230-243) and has no store or callback from which to rederive ActivePair; the daemon claims its marker before calling it (daemon_ha_sync.go:474-497). If delayed A is rederived to B under the send lock, the marker must record B, not the originally captured A. The plan must also define whether active-but-unexposed B is suppressed. A structured transaction returning `{queued, sentPair, sentDigest}` and a lock-order rule are needed; no §9 test performs periodic-capture A → B promotion/send → delayed A lock acquisition, despite f13 claiming test closure.
13. [BLOCKER] The v8.17 test plan is both contradictory and false-greenable: (a) misses prior/ledger transport, concurrent fence owners, settlement duplicate/ack/crash/reconnect, and multi-ledger cursor state; (b) simultaneously requires abort, no possible interposition, and an interposed B, and also requires no SNMP/web before the gate and then requires SNMP/web closeout before the gate; (c) misses rejected-token non-poisoning, accepted-echo seeding, and exact-equal retry; (d) requires no T1 XDP/pin/shim/bootstrap side effect after "T1 builds," although Compile mutates those before publication (manager_compile.go:177-226/:279-350), and misses registration lifetime; (e) adequate; (f) misses the late T1 staged send after OVERLAP without an accepted successor; (g) misses the separate link-observation transition and operator-change/current-plan retry; (h) misses retry re-quiescence and exposure-ledger completion; (i) tests only incremental tombstone acceptance, not full build, same-plan merge, selective dual-fabric deletion, continued-down reapply, or recovery; (j) explicitly leaves FIFO correctness "docs only" and tests no settlement dedup, acknowledgement, fence ownership, or crash replay.
14. [BLOCKER] The hazard budget omits the v8.17 hazards it claims to include (the disposition says "v8.16/17 classes" but the budget explicitly stops at "v8.13-v8.16"): missing unbounded/high-severity cases — recovered-timer promotion during startup and mixed boot state; SyncApply restart-only promotion later live-applied by GO-LOCAL; persistent closeout failure, stale closeout revocation, and deliberate loss of the submitting management channel; acceptance/cursor transport loss and sidecar-present crash with stale-authorized sessions; a dead registration suppressing GO-LOCAL forever; post-OVERLAP stale staged publication; settlement/fence ownership loss across consumer or process crash; operator-versus-restore-retry collision; tombstone expiry resurrecting a down fabric; stale outbound config regression. Persistent storage failure and kernel D-state can be acceptable severity-High residuals because they are fail-closed, explicitly owned, and Warn-visible; silent stale authorization, unsafe live application of restart-only topology, stale publication, ownerless suppression, and fabric blackholing are not. Relative to v8.16, the new closeout projection/debt, accepted-exposure cursor, accepted-token echo, registration discriminator, clear-on-transfer retry, and tombstone-expiry rules introduce new failure classes; the recovered-timer race already exists on master, but v8.17 newly makes a global correctness proof depend on the false assertion that it cannot occur.

DEMAND-REVISION

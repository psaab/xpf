# AGY plan review — round 22 — #6749 armed-state plan v8.17 (aca354bba)

**Reviewer:** AGY (hostile, zero-tool-call inline-evidence constraint; prompt /tmp/agy-6749-r22-prompt.txt assembled at 126,197 bytes). Raw output: /tmp/agy-6749-r22.out. Background bash `bvchwhnoy` (direct `agy --print-timeout 9m --print`).

**Verdict: DEMAND-REVISION** (3 BLOCKER + 2 MAJOR + 1 MINOR).

---

1. **[BLOCKER] `accepted_snapshot_token` is missing from `StatusSnapshot`, breaking the startup seed** (§6 items 4/10/11; §5-C Delta 6): §6 item 10 additively extends only `ConfigSnapshot` and `ControlRequest` with `snapshot_token: u64`, omitting any `accepted_snapshot_token` field from `StatusSnapshot` (in contrast to item 11's `accepted_fabric_map_generation`) — so after a daemon restart Go cannot learn the helper's accepted token, defaults `m.latestBuildSeq` to 0, and the helper permanently rejects its applies with `stale_snapshot_token`. (Resolution: §6 gains `StatusSnapshot.accepted_snapshot_token: u64` (serde default 0; the seed's source) + the protocol canary.)
2. **[BLOCKER] The `OnXSKBound` callback is neither revoked nor checked for OVERLAP status before the helper send** (Delta 5/7/8; §6 StartCompile/FinishCompileReservation): a newer commit T2 finalizes T1's staged reservation as OVERLAP, but the plan does not mandate cancelling the registered callback, nor does the callback verify its token is un-overlapped before issuing ApplyConfig — the leg fires with T1's staged payload and overwrites T2's accepted configuration, re-enabling `DeferWorkers=true`. (= SMR22-1, independently derived; the shared fix: the OVERLAP finalization CANCELS the staged leg's registration, and the leg checks its token's liveness BEFORE publishing (an OVERLAP-finalized token is dead — the leg discards the staged object and skips); the registration's lifetime is bounded by {OnXSKBound firing with the liveness check, OVERLAP finalization, helper death, an explicit stage timeout → the GO-LOCAL re-drive}.)
3. **[BLOCKER] `ApplyResult` does not transport `ledgerID` or `priorPair` from `beginFirstExposure`** (Delta 3; §5-C (ii); §6 ApplyResult): §6's explicit `ApplyResult` specification adds only `commitRevision`, omitting `ledgerID` and `priorPair` — so the daemon wrapper has no path to receive the captured values from Compile's atomic acceptance and must resort to un-synchronized post-hoc reads after `m.mu` is released. (= SMR22-2's transport half; resolution: `ApplyResult` gains `{priorPair, ledgerID, firstExposure}`; the transition runs manager-side at acceptance (the same `m.mu` section that advances `m.acceptedCommitRevision`); the wrapper's `oldActive` parameter is REPLACED by the prior on the invalidation path (one source of truth — they differ exactly post-gate).)
4. **[MAJOR] The `QueueConfig` re-derivation under the write lock lacks the exposure-gate validation** (Delta 11; §6; sync_conn_config.go:230-252): re-deriving a stale capture from `ActivePair()` could push a promoted-but-GATED config (R_b > durableRevision) to the HA standby — violating the invariant that unexposed configs must never be pushed. (Resolution: the revalidation includes the exposure check (R ≤ durableRevision) — a gated pair is never pushed (the push holds; the peer's state never leads the primary's exposed state; the peer's own exposure debt converges its local config independently).)
5. **[MAJOR] Cancellation-insensitive link recording strands deleted members in `linkOnlyRecovery`** (Delta 9; §5-C (g); §9 test 20(g)): a member removed from the binding plan whose link-down failure was recorded before token disposition stays in `linkOnlyRecovery` indefinitely, repeatedly attempting to bring UP an interface that no longer exists in the desired config. (Resolution: the member-removal rule sweeps ALL THREE collections (standing), AND the cancellation-insensitive recording skips a member already removed (the recording checks the desired membership at record time).)
6. **[MINOR] Contradictory tombstone behavior during the full snapshot build** (Delta 10; §5-C (i)): the text says "a tombstoned fabric is OMITTED from the built set" and then asserts the full build "re-adds CONFIGURED fabrics" — omitting preserves the removal, re-adding reverses it. (Resolution: the full build is CONFIG-AUTHORITATIVE — it builds from the CONFIG's fabric set (not from the canonical pair's tombstone state): a tombstoned-but-configured fabric IS built (the runtime-scoped tombstone dies with the apply); a tombstone whose fabric was REMOVED from the config is absent by construction. The "OMITTED from the built set" phrasing is corrected.)

Evidence wishes (informational): daemon_apply.go:160-220 (closeout hook placement vs the gate check); manager_compile.go:150-260 (chain linkage + OnXSKBound registration/revocation); sync_conn_config.go:230-252 (write-lock holds + exposure validation); daemon_apply_commit.go:95-118/:245-270 (ApplyResult → beginFirstExposure → invalidation).

DEMAND-REVISION

# AGY plan review — round 17 — #6749 armed-state plan v8.12 (08c78677f)

**Reviewer:** AGY (hostile, zero-tool-call inline-evidence constraint; prompt /tmp/agy-6749-r17-prompt.txt assembled at 119,259 bytes: boilerplate + transport-trimmed plan + evidence excerpts store.go:681-730, db.go:431-461, manager_compile.go:177-232, process_linkcycle.go:184-230, stop_workers.rs:1-32). Raw output: /tmp/agy-6749-r17.out. Background bash `bi99ym7sf` (direct `agy --print-timeout 9m --print`).

**Verdict: DEMAND-REVISION** (4 BLOCKER + 3 MAJOR + 1 MINOR + 1 NIT).

---

1. **[BLOCKER] Exposure gate lacks a re-exposure trigger** (§5-C Option-B exposure gate; store.go:681-730, db.go:431-461): when the background `writeTreeMarked` retry eventually lands the pair, nothing in configstore notifies the dataplane — a transient write failure silently defers a committed config's dataplane exposure until an unrelated future commit; HA `SyncApply` diverges the nodes' dataplanes indefinitely. (= SMR17-1, independently derived and source-verified: `persistRetryLoop` (store_persist.go:389-465) has no observer.)
2. **[BLOCKER] `snapshot_token` post-build minting admits concurrent stale-content re-ordering** (§5-C freshness token; manager_compile.go:177-232): two concurrent Compiles build outside `m.mu`; lock acquisition order (not build order) assigns tokens, so T1's older content can carry the newer token; moving the build inside `m.mu` would hold the lock across heavy compilation. (= SMR17-2 in its pair-vs-content form: the build-current validation compares only the (config, revision) pair, which same-commit reshapes share.)
3. **[BLOCKER] Fresh boot trapped in `FabricSyncStateOK() == false`** (§5-C (i) causal readiness; daemon_ha.go:774-783): a node booting with fabric configs on disk and no new BPF map mutations during bringup sends no fabric RPC, so no echo, so the coherence proof never exists and HA takeover readiness stays false indefinitely. (SMR17-4 sharpens this to the manager-re-init desync: `map_generation` has no seed rule, unlike `publication_rev`.)
4. **[BLOCKER] Mandatory live-MAC re-apply with an interposed commit arms un-programmed RETH MACs** (§5-C (b) paired transport; daemon_apply_dataplane.go:466-489): the re-apply's `ActivePair()` re-read returns an interposed C2 mid-MAC-program; C2 is compiled and published non-deferred WITHOUT C2's RETH MAC precheck. (= SMR17-3; source-verified — today `reapplyAfterDeferredMAC(cfg)` re-applies the captured cfg.)
5. **[MAJOR] Failed deferred-publish leg leaks `m.compileInFlight = true`** (§5-C reservation + PENDING-XSK STAGED): the staged reservation stays open; a failed/absent deferred-publish leg wedges inFlight, and the (v) latch echo (skips while compileInFlight) freezes permanently. (= SMR17-5.)
6. **[MAJOR] Synchronous ping at top of Compile under `m.mu` induces ~67s lock monopolies** (§5-C seed order; manager_compile.go:177-232): a control RPC at Compile top under `m.mu` blocks all concurrent polls/reads for up to the deadline. (SMR17-10 corrects the deadline class: the ping is a small request at the 3s `controlBaseDeadline` (process_control.go:34-41), not the 67-120s class — a pin, not a redesign.)
7. **[MAJOR] Repair-before-rebind late-arrival exposure exceeds the ≤ ~1s claim** (§5-C debt execution): the event-fired attempt queues FIFO behind applySem holds (up to ~67s each), so the wrong-MAC member forwards for ~1s PLUS the queueing delay. (= SMR17-7: text honesty; the no-priority posture (AGY r16 f8) already owns the latency class.)
8. **[MINOR] `RetryLater` re-queue resets the 60s queued-state Warn timer** (§5-C fairness): sustained `m.mu` contention keeps the Warn dark. (= SMR17-8: key the edge on first-queue time.)
9. **[NIT] Evidence wishes:** store.go/store_persist.go background-retry notification hooks; daemon_apply_dataplane.go:450-500 re-apply binding; rebind.rs error-code mutation logic; daemon_ha_fabric.go:730-980 call sites + mint locations. (Informational; the f1/f4 claims were independently source-verified by SMR this round.)

DEMAND-REVISION

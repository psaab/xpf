# AGY plan review — round 21 — #6749 armed-state plan v8.16 (0ef942686)

**Reviewer:** AGY (hostile, zero-tool-call inline-evidence constraint; prompt /tmp/agy-6749-r21-prompt.txt assembled at 126,318 bytes). Raw output: /tmp/agy-6749-r21.out. Background bash `bhnz048u7` (direct `agy --print-timeout 9m --print`).

**Verdict: DEMAND-REVISION** (2 BLOCKER + 2 MAJOR).

---

1. **[BLOCKER] The flow-level second-leg abort strands the helper with `DeferWorkers=true` when the interposed commit is itself exposure-gated** (§5-C (i), §9 (b); daemon_apply_dataplane.go:466-495): A's leg 1 publishes the workerless deferred snapshot; B is promoted but exposure-gated (R_b > durableRevision); A's leg 2 aborts on interposition (SUPERSEDED-BY-NEWER-COMMIT); B's own apply returns ExposurePending at entry and publishes nothing — the helper stays at A's `DeferWorkers=true` (workers disarmed) until B's storage lands: an unbounded dataplane outage under storage lag, introduced by the v8.16 abort rule itself. (Resolution (folds into v8.17): the second leg checks the interposed pair's EXPOSABILITY — exposable (R_b ≤ durableRevision) → abort (B's flow owns); GATED → COMPLETE A's bring-up (A remains the enforced config until B's exposure drain replaces it) — the v8.14 outer-pair rule was correct for exactly this case and the v8.16 abort must be conditional on the successor's exposability.)
2. **[BLOCKER] The unqualified GO-LOCAL rule prematurely publishes pending-XSK staged configs with `DeferWorkers=false`** (§5-C re-sync, Delta 10): the staged window (B promoted, StartCompile(true), staged with DeferWorkers=true, acceptedCommitRevision=A) fires the rule on the next poll tick; the drain's applyConfigLocked calls StartCompile(false) and publishes B with DeferWorkers=false BEFORE the XSK bind — EBUSY / socket bind errors, dataplane down. (= SMR21-1, independently derived; the shared fix is the live-registration discriminator: the rule fires on `active > accepted` AND **no live deferred-publish registration exists for the active pair** — the staged window is owned by its `OnXSKBound` registration, and a leaked registration (no live owner) lets the rule fire and OVERLAP-finalize the orphan (the AGY r20 f2 closure survives).)
3. **[MAJOR] Permanent operator lockout from binding/queue verbs during recovery backoff intervals via uncleared `m.linkCycleActive`** (§5-C debt execution, §6, Delta 8): the restore debt retries at 5/10/30/60s with no terminal cap; the gate holds through every backoff interval, hard-locking the operator's administrative verbs for an unbounded window during a persistent control-socket outage. (= SMR21-2: the gate holds from the quiescence through the program phase (or abandonment) and CLEARS at the transaction's restore completion — the restore debt's retries are post-window and do not hold it.)
4. **[MAJOR] The exposure-gate entry placement conflicts with the management-auth closeout set and the bootstrap-exit line** (§5-C (i), Delta 1; daemon_apply.go:160-220): line 192's bootstrap exit (interface renaming, IP forwarding enablement, dataplane arming) sits BETWEEN the management-auth mutations (SNMP at 167, web-management at 200) — so a gate check at ENTRY skips the closeout (violating it), and a gate check after 200 runs the bootstrap exit (violating the gate's invariant). (Resolution: the flow is re-ordered — the closeout set (SNMP + web-management + host-auth, all management-plane) runs FIRST, THEN the gate check, THEN the bootstrap exit + host/VRF/dataplane mutations (forwarding-plane); the web-management reconciliation moves ahead of the bootstrap exit (it is management-plane and independent of interface state).)

Evidence wishes (informational): daemon_apply_commit.go:240-300/:620-725 (drain loop + auto-rollback gating); manager_compile.go:340-400 + manager_worker_arm_5134.go:50-64 (outcome reduction, node-local stamping, clone interaction); binding.rs:29-47 + fabric.rs:373-437 (busy-error handling + tombstone drop).

DEMAND-REVISION

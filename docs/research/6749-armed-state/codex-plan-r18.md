# Codex plan review — round 18 — #6749 armed-state plan v8.13 (92fb722e1)

**Reviewer:** Codex (hostile; `codex exec -C <worktree> -s read-only`, background bash `bknj3ap8h`; prompt /tmp/codex-6749-r18-prompt.txt). Raw output: /tmp/codex-6749-r18.out. Reviewed the exact `92fb722e1` blob via `git show`; later worktree edits excluded.

**Verdict: DEMAND-REVISION** (11 BLOCKER + 3 MAJOR; f15 Warn lifecycle genuinely CLOSED).

---

1. [BLOCKER] §1 r17 disposition table materially false — re-derived: f2/f4/f5/f6/f7/f9/f10/f12/f14 OPEN, f3/f8/f11/f13 PARTIAL, f15 CLOSED narrowly (first-queue timestamp + periodic reporting coherent).
2. [BLOCKER] "Exposed pair" is not representable by the applied marker: the existing state is only `appliedDigest string` comparing config TEXT (store.go:278/:781/:803) — a same-text promotion with a new commit_revision inherits `ActiveApplied()==true` from its predecessor even when the new pair was gated; "stamp nothing" does not clear the inherited equality, and `handleConfigSync` can take its equal-text shortcut (daemon_ha_sync.go:549). §6 adds no applied revision/pair API. (The claimed staged-ahead suppression is also impossible: gating happens before Compile, so no staging, so `pending > accepted` cannot hold as claimed — v8.13's own (v) text was wrong.)
3. [BLOCKER] No typed accepted-versus-exposed outcome through the apply and HA stacks: a gated apply returning nil is treated by every wrapper as having RUN — boot stamps `MarkActiveApplied` (daemon_apply.go:49), local commit clears sessions/pushes peer/stamps applied (daemon_apply_commit.go:245), peer sync sets `armedActive=true` and runs its success tail (:479), `configApplyLoop` clears CF health and advances `lastAppliedConfigGen` (sync_conn_config.go:350/:385) — which controls manual failover readiness (sync.go:231, sync_bulk.go:235) and stale-session refusal (sync_conn_gen.go:398). A standby still enforcing A reports B applied, transfer-ready, and healthy; the later exposure drain has no HA `item.gen` to settle them. Returning an error instead violates the promised success. The primary has no remote exposure ACK (`QueueConfig` only writes (sync_conn_config.go:230); the daemon claims its marker before sending (daemon_ha_sync.go:474)). The operator warning is unplumbed (REST/gRPC/CLI copy only `compiled.Warnings` (api/config.go:600, grpcapi/server_config.go:275, cli/cli_config.go:277); appending the async debt to persistent config warnings would leave stale warnings after recovery). Required: a typed `ApplyOutcome{Ran, Exposed, ExposurePending}` plus deferred success tails plus an HA-generation settlement path.
4. [BLOCKER] The two gate implementations are not equivalent, and the debt has no bounded wake: the global `PersistDegraded()` getter gates DURABLE A's mandatory second leg when nondurable B was promoted between A's legs (the plan expressly allows the interposition; the second leg arms A's workerless first publication (daemon_apply_dataplane.go:466)) — the dataplane stays down until B is durable, indefinitely on persistent storage failure. **Gating must be pair-specific** (durableRevision: a pair is exposable iff its revision ≤ the store's durable high-water). The locus is ambiguous ("inside applyConfigLocked before compiling" comes after that function's SNMP/web-management/bootstrap/kernel/VRF mutations (daemon_apply.go:127/:167/:200/:246)) — must be at function ENTRY. Polling on an unspecified "wake cadence" supplies neither an always-live timer nor nextWake; the store sleeps 1/2/4/8/16/32/60s and exits silently — commit→exposure is `persistence recovery (unbounded until storage recovers) + poll + FIFO/apply`, contradicting "never indefinite".
5. [BLOCKER] `buildSeq` invalidates predecessors before a viable successor exists and fences only the final send: T1 is abandoned as soon as T2 captures; T2 can then fail in shim compilation or snapshot construction before sending anything (manager_compile.go:177/:200/:214) — T1 invalid, T2 published nothing, no debt owns the accepted config; the plan specifies neither T1's report nor its reservation outcome. Compile also performs side effects before send-time validation (XDP/pin work and shim compilation at :177-226, classifier/bootstrap map mutations at :279-350; the deferred path confirms those maps are already ahead before its later send (process_status.go:103)) — "T1 never reaches the helper" does not prevent stale Go/BPF side effects. §6 declares no manager "current (config,revision) pair" field to compare against. Required: predecessor invalidation DELAYED until the successor's observed acceptance (or a latest-wins rebuild debt), validation at the publish leg's ENTRY before any side effect, and the current-pair field.
6. [BLOCKER] Capture order still does not define content freshness (= AGY f4 = SMR18-2): Compile reads scheduler state, route/feed overlays, FIB generation, NAT IDs, and cold-path mask at separate times (manager_compile.go:192-225); the builder later samples routes, fabrics, interfaces, and neighbors (builder.go:41-105), including live netlink fabric state (fabric.go:34/:102) — "feed/overlay state refs" neither enumerates these nor requires immutable deep captures. T1(seq1) can lazily read newer Y after T2(seq2) publishes older X and be discarded merely for capturing first, with no build/event guaranteed for Y. Also: `snapshot_token` lacks incarnation handoff — the helper refuses older tokens per incarnation, but a manager re-init creates a fresh low `m.latestBuildSeq` while the surviving helper's stored token is higher — refused indefinitely. Seed the token from the ping echo (or require respawn).
7. [MAJOR] The error-code response side is feasible (Go already decodes the body before checking OK (process_control.go:148/:163)), but the note request verb is internally contradictory: §6 adds `ControlRequest.note_commit_revision` and demands a named dispatcher entry, yet its normative control-verb list says signatures unchanged and omits the verb; today both request structs lack the field (protocol.go:55, control.rs:108) and Rust falls through to unknown request type (handlers/mod.rs:124/:255). §6 must enumerate the new request type, the match arm/module, and the end-to-end typed-return API (the decoded response returned with the typed error and `requestLocked` observing its status — today it returns immediately on error (:219)).
8. [BLOCKER] The recorded-outcome reservation chain has no valid fold algebra (= AGY f5 = SMR18-3, deeper): head-first replay fixes (T1 ACCEPTED, T2 PRE-PUBLISH FAILURE) and breaks (T1 PRE-PUBLISH FAILURE, T2 ACCEPTED); UNKNOWN, PRE-SEND, PENDING-XSK, tail failure, OVERLAP, incomplete gaps, and three-node chains have no reduction rule; test (f) exercises only the favorable first ordering, not the 7×7 matrix.
9. [BLOCKER] Checked quiescence still has a read-to-syscall cancellation race and reintroduces starvation: the token re-read unlocks, `SetBindingState` then acquires m.mu, cancels the debt, bumps the token (manager_status.go:157), and the daemon executes `setHardwareAddr`/`setDown`/`setUp` anyway (daemon_reth.go:248) — a stale Report cannot undo the physical mutation; the syscall needs a MUTATION LEASE (re-read + syscall under one m.mu hold) or operator mutation serializing through applySem. And `RetryLater` releases applySem, losing the FIFO position the progress proof rests on — repeated finite status-loop m.mu holds (process_status.go:152) can fail every try-lock indefinitely. Stale split-API prose survives at plan.md:2884.
10. [BLOCKER] Restore debt remains non-executable: daemon-side at plan.md:2850 but "manager-side, daemon-driven" at :4297 (contradictory placement); §6 supplies no record/Claim/Report/clear/deadline API analogous to the MAC debt; "ctrl error → retry" does not choose between a bare ctrl map write (unsafe — safe enable occurs only after binding/map reconciliation in `applyHelperStatusLocked` (maps_sync.go:689/:809)), all of NotifyLinkCycle, rebind, or a paired full apply; during the unresolved debt workers may be bound while ctrl=0 — a total fail-closed outage with only an edge Warn; test (h) can green by manually recording and replaying a mock debt.
11. [BLOCKER] `map_generation` can falsely certify map/helper coherence: a failed `update_fabrics` retains old `lastSnapshot.Fabrics=P0` on failure (manager_ha.go:175); route/scheduler/#5134 clones carry P0 but, under the plan, the CURRENT g; the helper accepts P0,g and unconditionally echoes g (the v8.13 idempotent-advance rule), making accepted==current true for map-P/helper-P0. A concurrently prebuilt full Compile has the same problem (Fabrics built before m.mu (builder.go:78, manager_compile.go:214/:228)). "Build payload from FabricFwdInfo" is also incomplete (that sample has two ifindexes and two MACs; FabricSnapshot additionally needs names, overlay identity, queues, peer address, Up (protocol.go:315) via independent link/neighbor reads (fabric.go:34/:102)) — holding m.mu does not turn two observations into one. Required: a canonical (payload, generation) pair retained per projection; EVERY carrier (update_fabrics AND full snapshots) uses the payload bound to its own content's generation; test (i) omits the failed-send/interposed-full-carrier race.
12. [MAJOR] Late-arrival cutoff and latency remain wrong (= AGY f7 = SMR18-8, deeper): Claim returns currently-due items only — a non-due member cannot be "absorbed at re-Claim" merely because its link returned; membership is fixed at re-Claim, not at the end of the program phase; a return after re-Claim but before rebind is outside the absorbed set and can be globally rebound with its factory MAC; no passive event source exists (daemon_flow.go:725). The "5s initial backoff" claim is false for a member already at the 60s floor; the honest interval is its own current deadline + FIFO delay + the transaction.
13. [MAJOR] Tests (a)–(j) can green with the architecture still broken: (a) needs the real scheduler timer, the same-text/new-revision marker, HA applied-gen/CF/readiness, local-primary and rollback tails; (b) the global getter suppressing durable A's mandatory second leg; (c) the concrete scheduler API and autonomous bounded wake; (d) T2 failure/no successor, lazy-input inverse ordering, pre-send side effects, surviving-helper token seed; (e) the request-type inventory and dispatcher-to-Go typed-response path; (f) inverse ordering, 7×7 outcomes, three-node folds; (g) cancellation after reread but before syscall, repeated RetryLater starvation; (h) the real cross-owner drain, clear predicate, exact ctrl retry, ctrl=0 outage; (i) failed update_fabrics followed by a stale full carrier; (j) progress (asserts Warn visibility while starvation remains possible).
14. [BLOCKER] The severity-High hazard budget cannot be signed off: it omits indefinite disk-gate exposure, post-durability no-wake exposure, false HA readiness, abandoned-build suppression, RetryLater starvation, restore ctrl=0, and warm late-member blackholes; the claimed systemd "outer bound" is not a bound — `stopLocked` waits unconditionally after SIGKILL (process.go:231) while `TimeoutStopSec`/`Restart` cannot terminate an uninterruptible D-state task before the kernel wait returns. New v8.13 hazards vs v8.12/master: text-only exposed-pair false positive, no-successor build abandonment + pre-send side effects, surviving-helper token wedge, non-commutative reservation replay, post-reread mutation race, RetryLater starvation, full-carrier false map proof. Q1 survives the seventeenth enumeration (no new activation-state producer).

DEMAND-REVISION

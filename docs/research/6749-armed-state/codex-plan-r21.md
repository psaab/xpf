# Codex plan review — round 21 — #6749 armed-state plan v8.16 (0ef942686)

**Reviewer:** Codex (hostile; `codex exec -C <worktree> -s read-only`, background bash `bpar7yyf5`; prompt /tmp/codex-6749-r21-prompt.txt). Raw output: /tmp/codex-6749-r21.out. Reviewed the exact `0ef942686` blob.

**Verdict: DEMAND-REVISION** (12 BLOCKER + 2 MAJOR; the narrow prior closures remain valid: suppression-flag deletion, old-helper legacy behavior, identity-vs-telemetry separation, the drain's 5/10/30/60 backoff; Q1 remains complete).

---

1. [BLOCKER] §1 r20 disposition table materially false — every Codex row re-derived OPEN (f2-f13); the table also reuses "Codex f3" for pair linearization while the adjacent history calls f3 the already-closed suppression-flag deletion.
2. [BLOCKER] The security FOLLOW set is neither directionally safe nor a coherent partition of `applyConfigLocked`: the v8.16 text says {networkd, services} follows too (contradicting "exactly {dataplane, FRR, routing, fabric} defers"); SNMP is not merely community revocation (starts/stops UDP/161, adds users/communities/trap targets (daemon_apply.go:167-198)); web-management does listener bind/port/TLS/auth replacement (:200-213); bootstrap exit/naming/IP forwarding/dataplane arming are forwarding mutations (:215-236); VRFs/management-VRF routes/interfaces/fabric IPVLANs/dataplane/routing/route-leak all affect packet paths (:246-335); "services" includes neighbor resolution, RA, IPsec, DHCP (daemon_apply_routing.go:13-22); the tail mixes VRRP/forwarding with DNS/NTP/syslog/credentials (daemon_apply_tail.go:43-208). FOLLOW mutations DEPEND on deferred state: networkd consumes `ApplyResult.ManagedInterfaces` (exists only after d.dp.ApplyConfig (daemon_apply_dataplane.go:137-165, 218-245)); web bind resolution reads current kernel interface addresses (daemon_cluster_bind.go:44-81); host-inbound is kernel packet enforcement, and its B-config view combined with live A interfaces can DELETE A's protection when B removes/moves an interface (daemon_nft.go:344-365/:419-462, zones_host_inbound.go:84-105, interfaces.go:181-236). The RELAXING direction is plainly unsafe (enabling SNMP, adding an RW community/API credential/root key, expanding an HTTP listener, adding a host-inbound permit grants B access while A remains exposed) — FOLLOW must be limited to PROVEN MONOTONIC TIGHTENING over the union of A-live and B-desired reachability; additions/listener expansions/relaxations must DEFER; and closeout failures need a persistent debt (SNMP/web currently retry only on a later commit (daemon_snmp_reconcile.go:394-410, daemon_apply.go:210-213); the existing host-auth closeout may time out and abandon an owner, explicitly safe only during daemon shutdown (daemon_apply_hostauth.go:34-79, 158-186)).
3. [BLOCKER] The flow-level pair check is either unreachable under the source locking model or racy under the plan's model: current pair-changing promotions are serialized WITH apply under applySem (local commit (daemon_apply_commit.go:129-175), HA sync (:326-355), commit-confirmed (:525-531), auto-rollback (:629-645)); the persistence retry advances durability under s.mu but performs no new ActivePair promotion — so "an operator promotion needs no applySem" is source-false and the interposition tests are unreachable without changing the locking invariant. If v8.16 does permit such promotion, its two reads still do not linearize it (the window spans d.dp.ApplyConfig, manager Compile, host/XDP/pin/bootstrap-map mutations, socket I/O, and observed acceptance); and the manager check compares against `m.currentPair`, a cache updated only by ActivePair reads — a promotion after A's daemon read does not update it, so A still compares against cached A and publishes. Either retain applySem as the actual exposure lease, or add a store-backed promotion-generation invalidation/lease checked at publication.
4. [BLOCKER] The new "abort the second leg on any interposed pair" rule recreates the exact indefinite whole-dataplane outage (= AGY r21 f1): durable A's first apply publishes DeferWorkers=true; nondurable B becomes ActivePair; A's mandatory live-MAC second leg sees B and aborts; B's queued flow reaches the durability gate and skips its dataplane leg; permanent storage failure means B never replaces the snapshot — the source explicitly says the second apply is what starts workers and that losing it leaves the workerless snapshot replaying forever (daemon_apply_dataplane.go:466-489); B's own DeferWorkers stamp cannot clear anything because B never reaches Compile; the #5134 debt is recorded only when the second reapply is attempted and fails, not when the plan reports it success-equivalent and skips it. This directly falsifies "the dataplane keeps running the last durable config" and the hazard claim. (Tests (b) and (d) also require contradictory behavior.)
5. [BLOCKER] The drain cannot safely raise the HA generation fence using the existing fence state: the current contract is deliberately loop-owned (recordAppliedConfigGen is non-CAS and loop-only; beginConfigApply/endConfigApply are unconditional atomic stores (sync_conn_config.go:275-309)); the loop raises the fence before the callback, advances the high-water, then clears it (:325-395); session admission folds max(fence, high-water) (sync_conn_gen.go:398-432). v8.16 lets the daemon drain raise it out of loop at acceptance without an ownership token, max-CAS operation, or conditional release — a B drain can lower a concurrently raised C fence, and one owner's unconditional endConfigApply can clear another's. The callback is still text→error (sync.go:339-347, daemon_ha_sync.go:909-913); the plan names ApplyOutcome but does not define the typed callback/loop transition that transports item.gen into ExposurePending. Settlement also lacks exactly-once semantics (retaining debt after a successful nonblocking post permits duplicates; clearing at post permits loss; the monotonic high-water prevents a second NUMERIC advance but does not make fence release, CF-health success, or ledger phase completion idempotent) and has no peer-incarnation identity (bulk/reconnect resets both high-water and fence to zero (sync_conn_gen.go:324-367) — a delayed old-incarnation settlement can advance the new incarnation past its legitimate generations). Required: `(peer incarnation, gen, pair, settlement ID)`, loop-side dedup/acknowledgement, and a fence token only its owner can release.
6. [BLOCKER] `lastExposedPair` advances too early to serve as the old invalidation base: helper acceptance occurs inside Compile before it returns (manager_compile.go:350-365); the wrapper invalidates afterward (daemon_apply_commit.go:245-270) — so advancing last-exposed at the same observed-acceptance points changes A→B into B→B even in the no-gate common case. The useful invariant is only: before B acceptance, `oldActive == lastExposed == A` when no prior gate exists; acceptance destroys that equality unless the transition atomically returns the previous pair — the API must be equivalent to `beginFirstExposure(B) -> {priorPair, firstExposure, ledgerID}`, with the wrapper carrying that immutable prior pair through completion. Boot also needs an unknown-base policy: a process-local empty lastExposedPair is safe on a genuinely cold helper with no sessions, but NOT when a manager/daemon restarts over a surviving A helper with disk active B — the invalidators explicitly no-op on a nil old configuration (daemon_policy_invalidate.go:60-65, 193-195), so EMPTY→B can leave A-authorized sessions alive; persist/echo the exposed base or conservatively clear all sessions when the base is unknown.
7. [BLOCKER] The "completion ledger" has no state capable of distinguishing first exposure, completed replay, and incomplete same-pair replay: lastExposedPair cannot be the predicate because it advances at acceptance — before tails complete; a same-pair apply after a session-clear failure is simultaneously "already exposed" and "not complete". Re-sync currently clears on observed acceptance/publication — valid only if it atomically transfers ownership to a separately keyed tail debt before clearing; required state is at least `{pair, phase cursor, completion state}` distinct from lastExposedPair; acceptance clears re-sync only after installing that record; tail success advances the completion cursor; a pure divergence replay of a completed pair skips tails; an incomplete replay resumes its phase. "A crash between phases leaves the tail debt" is also false for daemon-local in-memory debt (the crash destroys it) — boot must persist the phase cursor or rerun idempotent tails conservatively. Restore's first-exposure and f10's claimed closure inherit this defect.
8. [BLOCKER] The node-local defer stamp is not transported to Compile: the plan returns a reservation token from StartCompile but the revised transport remains only (config, revision) through ConfigSink.ApplyConfig and Manager.Compile (apply.go:37-40, manager.go:348-357) — under the documented overlapping Compile model, Compile cannot identify "its own reservation node" through a mutable head or process-global value. The pending-XSK path is worse: it stores and returns at manager_compile.go:272-313, BEFORE the current defer stamp at :330-332 — the staged object must carry both the immutable token and its already-resolved DeferWorkers value; the later OnXSKBound leg cannot consult whatever node happens to be head then. Auxiliary clones also need explicit semantics: route/scheduler clones must preserve the cached snapshot's defer value; #5134 currently forces DeferWorkers=false as a generation-scoped completion action (manager_worker_arm_5134.go:50-64) — it may do so only after proving it owns completion for that exact staged generation; "no reservation means default false" is unsafe for a clone of a deferred snapshot. Pass the reservation token as an argument; deleting "intent as a Compile argument" removed the required association.
9. [BLOCKER] The unqualified GO-local rule attacks a legitimate pending-XSK owner (= AGY r21 f2 = SMR21-1): `ActivePair.revision > acceptedCommitRevision` is intentionally stable in several states (boot N/0 (should drive); legacy 0/0 (quiet); nondurable B/A (the exposure debt owns); pending-XSK B/A (intentionally staged, reservation token owned by the deferred-publish leg); abandoned/failed B/A (GO-local should drive)) — v8.16 fires "PERIOD" while pending-XSK explicitly remains an open reservation until OnXSKBound; the drain's new StartCompile then OVERLAP-finalizes a healthy staged owner, races its deferred leg, and can repeatedly replace the staged token. The old generic compileInFlight suppression was circular for leaked nodes, but deleting all suppression is not the fix: suppress only while a valid staged-XSK token has a live completion owner; clear that exception on OnXSKBound, helper death, explicit stage timeout, or a genuine superseding promotion.
10. [BLOCKER] "Newest seen" makes rejected snapshots an ordering authority and can poison future valid publication: the helper advances its fence even for a rejected token, while Go advances acceptedBuildSeq only on acceptance and seeds a restart from newest accepted — after manager restart, helper seen=100, accepted=5 causes Go to seed from 5 and mint refused tokens 6…100 (an unbounded retry-count delay, not benign restart behavior); classifying T1's stale_snapshot_token as success-equivalent is therefore false (no accepted successor exists); on a same-revision auxiliary update where active==accepted, GO-local does not necessarily supply another owner (the existing route-leak republish even documents that it has no dirty-retry owner (daemon_apply.go:328-335)). There is also a scalar contradiction: `snapshot_token == buildSeq` is immutable per build, yet every wire retry remints it. Use separate build-order and attempt-order identities, or have the helper fence only on ACCEPTED tokens and echo the actual fence used for restart seeding.
11. [BLOCKER] The quiescence gate can lock operators out forever, and the failed-UP record is still lost on cancellation (= AGY r21 f3 = SMR21-2): `linkCycleActive` is set on Valid and "cleared by the restore", while restore retries have no terminal cap and may leave ctrl=0 forever — binding/queue verbs are refused throughout arbitrarily long backoffs and forever on a deterministic controller failure, the very verbs an operator may need for recovery. The gate should cover each physical quiescent transaction, then clear when synchronous restore succeeds or work is transferred to restore debt; each retry can reassert it for its bounded transaction; if safety truly requires continuous exclusion, the plan needs a defined operator-abandon/override path. The independent linkOnlyRecovery assertion is also not implemented by the stated result API: `ReportMACDebtAttempt` discards stale-token results wholesale, so a cancellation racing programRethMAC's final-UP failure (daemon_reth.go:257-270) can discard the only observation that the member is down — record link-down recovery through a cancellation-insensitive path before token disposition, or separate link-state results from MAC-debt authorization.
12. [MAJOR] The fabric tombstone is not defined across the actual three refresh/build paths: incremental `update_fabrics` (handlers/mod.rs:144-174, refresh_fabric_links then stores the supplied snapshots); full snapshot build (`populate_fabrics`, forwarding_build/fib.rs:233-264); same-plan/full runtime refresh (snapshot_refresh.rs:247-269, 380-396 — preserves and merges old resolved fabrics). Current incremental refresh preserves an all-unresolved set (snapshot_refresh.rs:83-163), and the shared resolver treats zero ifindex as a skip rather than deletion (fabric.rs:373-403). Merely adding `removed` to the payload does not say which of these paths consumes it, nor how `forwarding.fabrics`, `ha.fabrics`, the RuntimeView, skip diagnostics, and the persisted snapshot are updated atomically. Selective deletion also needs a stable key: the runtime FabricLink contains only parent/overlay ifindexes, address, MACs, and carrier — NOT the configured fabric name (forwarding.rs:848-860) — a tombstone whose parent ifindex is zero cannot identify one of two fabrics without an old-snapshot lookup or an added identity. Lifetime is unspecified (does a full config apply retain the tombstone, re-add the configured fabric immediately, or re-add only after a new nonzero map mutation?). Tests need clear→incremental refresh→full/same-plan apply→periodic refresh→environment recovery, plus selective dual-fabric removal and config removal.
13. [BLOCKER] Atomic marker capture still does not order the captured config with the outbound generation: the current periodic path reads A, claims its marker, and later calls `QueueConfig` (daemon_ha_sync.go:462-497); capturing (text,digest,revision,exposed) atomically prevents a torn tuple but not: periodic captures A; B promotes and sends first; delayed periodic A acquires QueueConfig afterward and receives the higher wire generation; the peer applies stale A after B; the marker remains at B, so the periodic reconciler believes B is already pushed. `QueueConfig` assigns its generation at send time under a separate write lock (sync_conn_config.go:230-252) — the captured store revision must be ordered into one revision-aware outbound queue, or revalidated against ActivePair immediately before generation allocation/write.
14. [MAJOR] The tests and hazard budget can green while all of the above remains: (a) misses concurrent fence raisers, conditional release, duplicate/full-channel settlement, reconnect-generation reset, and first-exposure vs incomplete replay; (b) explicitly requires both aborting and running A's second leg and does not test relaxing security changes or A-live/B-derived host-firewall views; (c) tests helper respawn, but not seen>accepted restart poisoning; (d) asserts no T1 host side effect after "T1 builds," although Compile's side effects precede publication, and omits cached-pair authority and sent-but-rejected successors; (e) locally adequate; (f) checks terminal fold values, not token transport, per-snapshot wire stamps, staged-XSK ownership, or auxiliary clone semantics; (g) checks busy refusal and final-UP ownership, but not gate clearing on debt transfer or cancellation before stale-result discard; (h) misses restore first-exposure ledger completion and gate lifetime; (i) tests one incremental tombstone, not full/same-plan/selective/persisted/recovery paths; (j) leaves FIFO correctness "docs only" and tests neither settlement deduplication nor fence ownership. The cumulative budget omits the v8.16-created unbounded cases: workerless durable A behind permanently nondurable B; security closeout failure or early authorization expansion; out-of-loop fence corruption and old-incarnation settlement; pending-XSK reservation churn; rejected-token catch-up; indefinite operator lockout; tombstone re-add oscillation; and stale outbound config regression. Q1 remains complete against current source (no additional writer of `Registered && !Armed && activation_state==none`, assuming tombstone handling does not reconstruct bindings ad hoc).

DEMAND-REVISION

# Codex plan review — round 20 — #6749 armed-state plan v8.15 (132309631)

**Reviewer:** Codex (hostile; `codex exec -C <worktree> -s read-only`, background bash `b5mn725fe`; prompt /tmp/codex-6749-r20-prompt.txt). Raw output: /tmp/codex-6749-r20.out. Reviewed the exact committed blob at `132309631`; uncommitted v8.16 worktree text excluded.

**Verdict: DEMAND-REVISION** (10 BLOCKER + 3 MAJOR; f3 (suppression flag deletion) narrowly CLOSED; f13 (old-helper note) CLOSED; AGY f7 + SMR19-3/4/7 clean; Q1 remains complete).

---

1. [BLOCKER] §1 r19 disposition table materially false — re-derived: f2/f4/f5/f7/f8/f9/f10(lease)/f14/f15 OPEN; f6/f11/f12 PARTIAL; f3/f13 CLOSED; AGY f1/f5 inherit f7, f3 inherits f9, f4 inherits partial f6, f6 inherits f10; SMR19-1/2/5/6 inherit f7/f6/f5/f10-f11.
2. [BLOCKER] FULL GATING indefinitely defers security-critical revocation: the gate precedes SNMP, web management, bootstrap, and all host/dataplane work (daemon_apply.go:167-213, 293-307) and reports the nondurable promotion as success-with-note — during permanent storage failure a committed tightening leaves old SNMP communities, web credentials, and host authorization live forever; source deliberately performs these revocations early and calls leaving permissive host authorization after promotion a monotonic-revocation violation. Test (b) locks in the unsafe ordering. The plan must either reject/rollback the promotion or define a security-closeout path. (The `commitOverlay`/networkd-value loss is a non-issue — the drain recomputes them (daemon_apply.go:289, 320, 335).)
3. [BLOCKER] The entry gate does not linearize the pair for the whole apply: pair-current validation occurs only inside the userspace publish leg — after SNMP/web/VRF/interface mutations (daemon_apply.go:167-357) — so an interposed promotion is either detected only after A's mutations ran (no outer-pipeline abort defined, and ordinary dataplane failure then continues through A's FRR/routing/services) or not detected at all; tests (b) (durable A's second leg must run after B promotes) and (d) (a promotion invalidates T1) contradict. The plan needs ONE flow-level exposure ordering rule, not an entry durability check plus a late manager-only freshness test.
4. [BLOCKER] The ordered-loop settlement has neither a viable ingress handshake nor a safe session fence: `gen` lives only inside configApplyLoop; OnConfigReceived receives only text and returns nil/error (sync.go:339-347, daemon_ha_sync.go:909-913) — nil advances the generation immediately, error invokes ordinary apply-failure/CF behavior; nothing transports item.gen into the daemon debt or represents ExposurePending to the loop. Worse: after C is physically exposed, v8.15 clears A-era sessions while leaving applyingConfigGen at A until the queued settlement runs — an A-stamped session arriving after the clear but before settlement is admitted and survives C; existing code raises the new-generation fence BEFORE the clear and retains it until the high-water advances (sync_conn_gen.go:398-432) specifically to prevent that race. FIFO does not repair it: a later config item can sit ahead of settlement and wait for applySem while the drain holds it (deadlock), and the finite (64) channel can lose it (sync.go:847-857, sync_conn_read.go:315-331) — no tail-debt callback, repost rule, stale-settlement rule, or owned context form is defined.
5. [BLOCKER] "Last exposed" is narration, not a state machine: no lastExposedPair inventory, immutable config owner, writer census (boot/background apply, local commit, HA apply, auto-rollback, exposure drain, GO-local re-sync, restore, second-leg first exposure), or atomic consume/update API; existing applied markers cannot substitute (exposure can succeed while a later nonfatal tail fails). Concrete missed transition: A exposed; B promoted but gated; then C promotes durably and takes the ordinary path — C's wrapper captured B as oldActive, so it invalidates B→C, not A→C; an A-authorized session survives permanently (daemon_apply_commit.go:95-118, 245-270, daemon_policy_invalidate.go:242-280). The phased peer-push failure is also unobservable (QueueConfig returns void, silently no-ops without a connection, handles write failure internally (sync_conn_config.go:230-252, daemon_ha_sync.go:355-378)).
6. [BLOCKER] GO-local re-sync can expose a committed config while permanently losing all wrapper tails: the re-sync invokes only applyConfigLocked; on acceptance it has no retained A base, syncPeer intent, HA generation, digest, or phase cursor, yet its accepted-revision condition can clear the debt — B then forwards while A-authorized sessions and stale peer/marker state remain. Eventual success must complete the original exposure transaction, not merely the locked apply; restore has the same distinction (replaying genuinely already-exposed A needs no re-stamp, but a first real exposure must enter the same completion ledger).
7. [BLOCKER] The reservation fold can publish the wrong reservation's defer intent: StartCompile(T1=true) writes shared m.deferWorkers=true; overlapping T2=false overwrites it; T1 remains viable if T2 fails — yet T1's snapshot stamps from the mutable global, not T1's immutable node intent (manager_compile.go:330-350); the helper can accept T1 with defer_workers=false and start workers before T1's MAC work, and the later fold restoring Go to T1's true cannot undo that unsafe wire publication. The missing transport is structural: the token is returned but revised ApplyConfig/Compile carries only (cfg, commitRevision) — no goroutine-local association can identify T1 under documented overlap (apply.go:37-40, manager.go:348-357). The old reduction also remains normative at plan.md:3671-3674, contradicting the corrected one. The chain-root value is well-defined and head-pop works if every node finishes; it does nothing for a node that never reaches Finish.
8. [BLOCKER] The claimed side-effect-free preparation phase is source-false: the userspace shim .o is build-time embedded (loader_userspace_shim.go:1-12) — phase 2 does not compile a new .o; CompileUserspaceShim calls CompileConfig, which directly creates VLANs, reconciles addresses, changes MTUs, toggles RX-VLAN/ethtool/rings, changes links UP/DOWN, and bumps the real FIB-generation map (compiler_iface.go:446-510, 586-650, 679-717; compiler.go:298-304; maps_fabric.go:78-95) — a stale build can mutate host networking before validation rejects it. Separately, "NO ACCEPTED NEWER BUILD" has no defined state (no acceptedBuildSeq; the ping echo alternates between helper "newest seen" and "newest accepted" — they differ when a higher-token snapshot is sent but rejected). Validation also needs an incarnation check or mandatory re-ping if the helper respawns between phase 1 and phase 3; test (c) covers only a Compile begun after respawn.
9. [BLOCKER] Member-boundary checks do not preserve physical quiescence or failed-link ownership: after PrepareLinkCycleChecked stops workers and releases m.mu, an operator registration verb can acquire m.mu and invoke Rust reconciliation (registration changes tear down and bring workers back up (binding.rs:27-51, status.rs:373-411, reconcile/mod.rs:330-398)) — Go can then run the unlocked DOWN→MAC→UP transaction on that same member with live XSK/UMEM, the exact condition PrepareLinkCycle exists to prevent (manager_status.go:157-179, process_linkcycle.go:136-164). "The member always completes" also assumes away errors: programRethMAC can fail its final UP, or ignore failure of its best-effort UP after a MAC-set error (daemon_reth.go:257-270); if cancellation concurrently removes the debt, stale results are discarded wholesale and the claimed-slot rebind merely recreates XSKs — it does not restore admin-UP; the member can remain down with no owner. Finally, active old text still demands per-mutation validation/leases and try-lock-or-skip while holding applySem (plan.md:3138-3171, 6157-6181, 6452-6466, 6514-6518), contradicting the new blocking member-boundary contract.
10. [MAJOR] Marker and restore closure is only partial: the periodic push reads active config, formatted text, and its claimed marker SEPARATELY (daemon_ha_sync.go:462-497) — a promotion can interpose after the proposed current-pair-exposed check; it needs ONE store-atomic captured (text, digest, revision, exposed) value through enqueue. Auto-rollback receives a fresh local revision and directly applies, clears, and pushes, but never stamps that rollback pair applied — absent from the advertised four-call-site marker census (daemon_apply_commit.go:697-723). Restore ownership is still contradictory (declared wholly daemon-side while appearing under "Go manager state" with manager-shaped Record/Claim/Report APIs); a replay that first exposes current state must use the common completion ledger.
11. [BLOCKER] Canonical fabric equality is not a valid coherence proof for zero entries: when the daemon clears a map entry to Ifindex=0, an all-unresolved helper refresh PRESERVES the previous resolved working set (fabric.rs:373-397, 406-437; snapshot_refresh.rs:83-112) — the helper can acknowledge generation g, the debt can clear, and HA readiness can pass while the BPF map is disabled but Rust still retains the old fabric. (The normal path constructs each complete FabricFwdInfo before one map call (daemon_ha_fabric.go:717-738, 929-944) — no per-struct half-population race; the unresolved guard genuinely rejects missing parent/MAC values.)
12. [MAJOR] Tests (a)–(j) still admit false-green implementations: (a) fakes the settlement injection and misses the ingress handshake/pre-clear fence/direct-durable C/post-acceptance drain errors/peer-push observability; (b) contradicts (d) and misses management-auth deferral + the full-flow stale-pair race; (c) misses respawn between ping and validation; (d) misses actual compiler netlink/FIB mutations, accepted-token state, sent-but-rejected successors, and reservation-bound wire intent; (e) adequate; (f) checks terminal fold state, not each accepted snapshot's token-bound intent, and misses a never-finishing final node; (g) misses operator-triggered worker restart and UP-failure-plus-cancel; (h) covers semaphore and intent but not owner placement or first-exposure completion; (i) misses zero-map plus stale helper preservation; (j) docs-only fairness cannot test settlement FIFO, full-drain failure, leaked reservations, or unbounded rtnetlink latency.
13. [MAJOR] The hazard budget is demonstrably stale: Q7 remains the old v8.8 list and the risk table still rates performance LOW and architectural mismatch LOW-MED, omitting the indefinite management-auth revocation delay, mid-flow pair hybrids, the stale-session settlement window and FIFO loss/deadlock, direct-C loss of the last-exposed invalidation base, re-sync/restore exposure without wrapper completion, wrong reservation intent on the wire, operator rebind during quiescence and cancellation-plus-UP failure, a leaked reservation suppressing GO-local re-sync, and the exposure-drain-accepted-but-later-tail-failure owner (route-leak republish currently has no dirty-retry owner (daemon_apply.go:331-357)). These are new to v8.15's proposed mechanics, not merely master's original #6749 defect.

DEMAND-REVISION

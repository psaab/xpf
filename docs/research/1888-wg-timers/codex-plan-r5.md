F3: RESOLVED. v7 now defines `timer_pass(&self, now_ns, endpoint_known)` and explicitly says the engine cannot infer learned endpoints itself in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1888-research/docs/research/1888-wg-timers/plan.md:469). The §5.2 loop computes `endpoint_known = effective_endpoint.is_some()`, passes it into `engine.timer_pass(now, endpoint_known)`, keeps `timer_pass` read-only, and assigns skip/fail mutation to `engine.pace_keepalive_skip(now)` in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1888-research/docs/research/1888-wg-timers/plan.md:509). `t8_last_attempt_ns` is also present in the Peer additions at [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1888-research/docs/research/1888-wg-timers/plan.md:350).

New finding, MAJOR: the v7 H1/H2 success-path cleanup can erase a valid post-handshake T7 arm. The loop sketch keeps socket burst + TUN burst before the timer/attempt arm in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1888-research/docs/research/1888-wg-timers/plan.md:498); success is only detected by `attempt.drive` in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1888-research/docs/research/1888-wg-timers/plan.md:513); and both success/give-up paths clear `t7_arm` in [plan.md](/home/ps/git/bpfrx/.claude/worktrees/1888-research/docs/research/1888-wg-timers/plan.md:621). Concrete trace: msg2 installs fresh S2 during socket burst, queued TUN egress immediately sends non-empty data on S2 and arms T7, then the same iteration’s success cleanup clears that fresh arm. If no later data is sent, F1’s “sent data, heard nothing, reinit at armed+15s” is lost.

Post-give-up semantics alone are fine: clearing `t7_arm` on give-up preserves “only data after the boundary re-arms.” The break is success-boundary ordering. Upstream does handshake-complete timer cleanup directly in the receive path before later data-send timer arming; see local wireguard-go [receive.go](/home/ps/go/pkg/mod/golang.zx2c4.com/wireguard@v0.0.0-20260522210424-ecfc5a8d5446/device/receive.go:420) and [timers.go](/home/ps/go/pkg/mod/golang.zx2c4.com/wireguard@v0.0.0-20260522210424-ecfc5a8d5446/device/timers.go:137), and Linux `timers.c` has the same immediate handshake-complete/data-sent split: https://git.zx2c4.com/wireguard-linux/tree/drivers/net/wireguard/timers.c#n384

PLAN-NEEDS-REVISION — F3 is resolved, but v7 still has a real success-boundary T7 loss: data sent on the freshly installed session before `attempt.drive` runs can be cleared by the success cleanup. Move success boundary cleanup to the authenticated completion path before any egress, or make cleanup conditional/timestamped so post-completion data remains armed.
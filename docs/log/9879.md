# #9879 — destination-NAT `off` exemption loses to a NARROWER later translate

- **Timestamp**: 2026-09-15
- **Action**: Lane opened at base `4eedd01d7`. STEP-0 verified the defect is
  PRESENT on base, not already-fixed: (1) a broad `off` (destination-only)
  plus a later translate (same destination + `destination-port 80`) in one
  rule-set commits clean with 0 warnings — no gate fires; (2) dataplane
  lookup for the "exempted" TCP/80 flow returns `Some(translate)`.
  Mechanism confirmed at `destination.rs:707-779` (cross-tier `.or_else`
  chain), `:787-789` (`Some(Exempt) | None => return None`), `:842-848`
  (MOST-SPECIFIC-WINS comment), `protocol/nat.rs:311-326` (`off` variant),
  `docs/vsrx-gaps.md:119` (implicit ordering); the strict NAT validator has
  only static-/0 and NAT64 shadow detection, no off gate. STEP-0
  scaffolding removed after the final cells subsumed it. SMALL-tier brief
  posted to the issue as comment 5678872121.
- **File(s)**: none (measurement; scaffolding removed)

- **Timestamp**: 2026-09-15
- **Action**: Implemented the visibility fix (dataplane tier order
  untouched — HPC constraint). New shared predicate
  `DNATOffShadowReason` (`pkg/config/nat_off_shadow_9879.go`) detects a
  broad `off` before a narrower later translate in one rule-set
  (literals-only, no `match application`; uncertain shapes skip rather
  than fire; tier model mirrors the builder incl. #6462 TCP+UDP
  expansion and #3449 range-keying, with equal-tier/prefix ties
  correctly no-firing). New strict gate `validateDNATOffShadowStrict`
  rejects on commit, warns on lenient load/peer-sync, wired last in
  `runUniformGatesNAT`. Show surface annotates the shadowed `off` with
  a PARTIALLY SHADOWED warning (not NOT INSTALLED — the entry installs
  and still exempts the remainder). Reject is safe: every off-first
  intent re-expresses as translate-first, which agrees under both Junos
  first-match and xpf specificity. Documented the ordering + gate in
  `docs/vsrx-gaps.md`.
- **File(s)**: `pkg/config/nat_off_shadow_9879.go` (new),
  `pkg/config/compiler_validate_strict_nat.go`,
  `pkg/config/compiler_uniformgates_nat.go`, `pkg/natshow/natshow.go`,
  `pkg/natshow/dest.go`, `docs/vsrx-gaps.md`

- **Timestamp**: 2026-09-15
- **Action**: Verification. New cells: Go gate rejects x4 tiers
  (port/proto/prefix/longer-prefix) + lenient-warn + 9 clean controls +
  cross-rule-set skip + 6 predicate tier cases
  (`compiler_nat_off_shadow_9879_test.go`); natshow annotation on/off
  (`nat_off_shadow_9879_test.go`); Rust most-specific-wins pins for the
  port and protocol tiers (`tests_destination.rs`, prefix tier already
  pinned as the #9159 premise, translate-vs-translate control already
  exists as `dnat_exact_port_beats_wildcard`). Full suites:
  `go test ./pkg/config/` ok (201s), `./pkg/natshow/` +
  `./pkg/dataplane/userspace/` ok, `cargo test nat::` 511 passed /
  0 failed. Named tests 5x green both languages. `gofmt -l pkg/`
  clean, `go vet` clean on both touched Go packages. No existing
  fixture trips the new gate.
- **File(s)**: `pkg/config/compiler_nat_off_shadow_9879_test.go` (new),
  `pkg/natshow/nat_off_shadow_9879_test.go` (new),
  `userspace-dp/src/nat/tests_destination.rs`

- **Timestamp**: 2026-09-15
- **Action**: Coordination: file overlap with Eng9877 (PR #10079) confirmed
  via hub; parent sequenced 9877-first with a rebase of this lane onto
  post-9877 master before PR. Deconfliction: validator appended at end
  of `compiler_validate_strict_nat.go`, gate block placed last in
  `runUniformGatesNAT` (away from 9877's #8430-adjacent hunk), shared
  predicate in a NEW file (no edit to `nat_exclusion_reason.go`),
  additive-only annotation hunk in `natshow/dest.go`.
- **File(s)**: none (process)

- **Timestamp**: 2026-09-15
- **Action**: Parent-review fold (GPT NEEDS-MAJOR + SPARK NEEDS-MAJOR,
  convergent on tier precedence). (1) Rewrote the predicate around the TRUE
  probe order — destClass (host > prefix) > port (exact > wild) > proto
  (pinned > ANY) > prefixLen — via a sound witness search: fires only on a
  concrete (dst, proto, port, src) flow verified against EVERY emitted entry
  of both rules with the translate best strictly outranking the exemption
  best. Fixes the false rejection (/24 exact-port off vs /26 wildcard-port
  translate; tr-range vs off-exact) and the missed shadow (pinned-prefix off
  vs any-protocol host translate). (2) Multi-address exemptions evaluate by
  best entry, never weakest cell; added the prefix+host control plus Rust
  loss/hold proofs. (3) Translate (and off) candidates now filter through
  DestinationNATRuleExcludedReason — unknown #9877 leaves and undefined /
  invalid pools yield no TRANSLATED claim; fixed unit fixtures to define
  pools. (4) Documented LAST-local ordering (last in NAT segment, wins over
  later-domain errors by design) + pin test with a Filter trip. (5) Added
  the SPARK-§6 prefix-prefix port-mismatch control. Verification: full
  pkg/config + natshow + userspace suites ok, cargo nat:: 515 passed,
  named 5x green both languages, gofmt/vet clean.
- **File(s)**: `pkg/config/nat_off_shadow_9879.go`,
  `pkg/config/compiler_nat_off_shadow_9879_test.go`,
  `pkg/config/compiler_uniformgates_nat.go` (comment),
  `userspace-dp/src/nat/tests_destination.rs`

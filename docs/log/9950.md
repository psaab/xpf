# #9950 fragments cohort (F-035/F-036/F-053) — lane log

Base: `a126bc209` (fix/9950-fragments). Members verified via `gh issue view 9950` (no comments).

## STEP-0 RED (base, `tests_9950.rs`)
- `f035_overlap_fragments_denied_both_orders_9950`: overlap forwards 1 vs 0.
- `f036_dnat_reply_nonfirst_translated_on_wire_9950`: wire src 10.0.61.102 vs 172.16.80.8.
- `f053_pool_snat_reply_nonfirst_translated_on_wire_9950`: wire dst 172.16.80.100 vs 10.0.61.100.
Wire asserted via `rewrite_forwarded_frame_in_place` + `wire_ipv4_addrs_9950` (VLAN-aware).

## Plan reviews (2 hostile, before implementing)
- `PlanReviewA`: PLAN-FIXABLE, 11 must-fix (flow-cache bypass for both fixes, table placement, DoS/failure-direction,
  coalesce+overflow, v6 proto drop, cache-hit install, hit-tail post-gate owner-only, after-screens, post-decap,
  forward-hit cell, reply-reorder follow-up).
- `PlanReviewB`: PLAN-FIXABLE, 15 must-fix (3 P0: cache bypass past first datagram, undecided overflow, contradictory hook).
- Adjudication: `docs/log/9950-adjudication.md` (all accepted; #6122 reply-miss residual install-only + tracked follow-up;
  VLAN/fabric covered-by-construction, see addendum).

## Fix
- `fragment_overlap/`: receiver-key tracker (v4 keeps proto, v6 drops it), 16x64, 2s idle/10s absolute, inline 8 ranges,
  coalesce, fail-closed overflow, global counters only.
- `inspect.rs`: `ExtChainFragment.header_offset` (shared #6435 walker, no third parser).
- `nat64.rs`: `frag_overlap` Arc-threaded across reloads (same lifecycle as `frag_assoc`).
- `poll_descriptor/mod.rs`: overlap hook post-screen+IPsec pre-cache post-decap (fragments skip cache seed+lookup);
  session-hit tail install (post-gate, owner-only, NAT64 gated v6, raw override captured).
- `frag_assoc.rs`: doc surgery (POST-COMMIT sites, same-tuple replies, `reverse: None` by design for same-family).
- Miss-site residual comment + this log (reply-reorder windows: DNAT-without-SNAT discloses; SNAT-covered drops via
  probe; distinct-pool drops via MissingNeighbor).

## GREEN (after fix)
- `afxdp::tests_9950`: 5 passed (f035 both orders + benign adjacent, f036 wire src, f053 wire dst,
  f053 second-datagram-post-cache, forward-hit-existing-flow).
- `fragment_overlap::tests`: 8 passed (adjacent, overlap both orders, retransmit, empty, 44-frag coalesce,
  9-disjoint overflow, v4 clamp/atomic, v6 proto-dropped + walker).
- Full suites: `cargo test --bin xpf-userspace-dp afxdp::` + `screen::` + `fragment_` (count=1) + named 5x (see PR evidence).

## Residuals (tracked follow-up, not silent)
- Reply tail-before-first (+TTL/evict/commit/HA/LAG windows) still misses #6122 (forward-rules-only) and forwards.
  In-order (µs, realistic) fixed + wire-asserted. Session-gated reverse arms need new L3 index (unbounded here).
- Batched counter plumbing deferred (globals + drops suffice).
- End-to-end v6 overlap via harness deferred (unit covers parsing+table).

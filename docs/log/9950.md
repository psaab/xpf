# #9950 fragments cohort (F-035/F-036/F-053) — lane log

Base: `a126bc209` (fix/9950-fragments). Members verified via `gh issue view 9950` full body (no comments).
PR: #10128 (parent review: dual NEEDS-MAJOR — folded below, per item).

## STEP-0 RED (base)
- `f035_overlap_fragments_denied_both_orders_9950`: overlap forwards 1 vs 0.
- `f036_dnat_reply_nonfirst_translated_on_wire_9950`: wire src 10.0.61.102 vs 172.16.80.8.
- `f053_pool_snat_reply_nonfirst_translated_on_wire_9950`: wire dst 172.16.80.100 vs 10.0.61.100.
Wire asserted via `rewrite_forwarded_frame_in_place` + VLAN-aware parse (NOT the assoc table).

## Plan reviews + adjudication
- 2 hostile reviews pre-implementation: both PLAN-FIXABLE (A: 11 must-fix, B: 15/3 P0).
- Adjudication: `docs/log/9950-adjudication.md` (+ implementation addendum). `9950-plan.md` retained
  pre-review draft, marked superseded.

## Fix (see PR body for the file list)
- `fragment_overlap/`: receiver-key + routing-domain tracker, 16×64, 16 inline coalesced ranges,
  30s idle/60s absolute, all-full conditions fail-closed (overlap/empty/overflow/shard-full).
- Check/record SPLIT: pure `check_overlap` pre-hook (post-screen/pre-IPsec/pre-cache/post-decap,
  tri-state parse — Unreadable skips table AND cache); `check_and_record` post-commit at the single
  common TX site (pre-key re-check closes the cross-worker race + records admission; translated-key
  re-check for post-NAT identity). Refused traffic never plants ranges. Fragments never seed/consult
  the flow-cache (ident-less 5-tuple key).
- Session-hit tail installs (post-gate, owner-only, NAT64 v6-gated) for forward hits + replies.
- Shared `#6435` walker extended with `header_offset` (no third parser); instance rides `Nat64State`.

## Parent-review fold (per item)
1. Shard-full fail-closed: evict-oldest REMOVED — full shard drops + `FRAG_OVERLAP_SHARD_FULL_DROPPED`
   (unit: 65th same-shard datagram drops, anchor ranges still overlap-detect). Flood-time availability
   loss accepted as the security-control trade.
2. TTL 30s idle / 60s absolute: covers Linux `ipfrag_time` (30s); memory cap-bounded (~200KB), TTL only
   sets residue squat. Justified in module docs.
3. Hook moved pre-IPsec (post-screen): IKE first fragments plant ranges. Residuals documented in code:
   SYN-cookie-challenge `continue`s pre-hook; ESP/AH outer recorded, inner unknowable.
4. Routing domain IN the key (flow-stamped or identical ingress SSOT); record-before-admission amplifier
   CLOSED by moving record post-commit (check-only early) instead of merely documenting it — refused
   cross-domain tails no longer poison same-domain controls (proven by the unmodified #5798 refusal legs).
   #10132 scope accordingly narrowed to NAT64-reply assoc + v6 hit-tail e2e (see issue comment).
5. Post-translation check at common TX (same-family re-address + NAT64 v6→v4 truncation collisions unit-
   proven: distinct pre-keys share one post-key and drop). Pure port-PAT skips on key equality (else
   self-overlap). v4→v6 needs no check: zero-extended ident + injective addrs ⇒ post-collision implies
   pre-collision (nat64.rs:2759/3860 cited in docs).
6. Follow-ups FILED (quoted in code + log + PR): #10130 reply-reorder discriminator, #10131 batched
   counters, #10132 NAT64-reply/v6-e2e/record-split. Pool residual corrected honestly: MissingNeighbor
   buffers/retries (not terminal); with a pool neighbor the fragment forwards untranslated to the pool
   (misdelivery, demonstrated by the F-053 fixture — no disclosure).
7. Cap 8→16 with memory analysis; benign 17-frag even/odd interleave unit cell (peak 9 ≤ 16) + e2e
   benign-reversed case. Sparse-17 overflow still fails closed.
8. Tri-state `OverlapParse`: Unreadable (truncated/version-mismatch) skips table AND cache; proven
   non-fragments flow on. Version nibble verified against wire (closes the stamp-trust remnant).
9. v6 overlap-pair txn cell added (both orders + benign). NAT64-v6 hit-tail e2e → #10132 (quoted).
10. Overflow/overlap drops leave the live entry byte-identical (no MRU move, no refresh) — merge-into-
    scratch-then-commit; strict-overlap returns before any mutation.
11. Docs: ident-wrap + TTL + amplifier + v6-bytes analyses in module docs; `9950-plan.md` superseded;
    `docs/feature-gaps.md:360` + Force-Reassembly row updated; `FEATURES.md` row corrected + new
    `fragment_overlap/` row. Fixtures wire-valid (24B MF=1 firsts, adjacent tails); F-035 armed with a
    `syn-fin` screen order test (first passes, tail skips screens yet drops on overlap).
- Declined with cause: v6-offset advisory claiming `&0xFFF8` yields units — it yields BYTES (units occupy
  bits 15..3; `>>3*8` is the identity). Verified against screen predicates + RFC layout; clarifying comment
  added in code.

## GREEN (after fix)
- `afxdp::tests_9950`: 7 passed (f035 v4 both-orders+benign×2 with counter attribution, f035 armed-screen
  order, f036 wire, f053 wire, f053 second-datagram-post-cache, forward-hit, f035 v6 both-orders+benign).
- `fragment_overlap::tests`: 15 passed (adjacent, overlap×2, retransmit, empty, 44-coalesce, reorder-17,
  overflow-17, shard-full, domain-split, v4+v6 parse tristate, post-key same-family + NAT64, check-purity ×2).
- Full relevant suites count=1: 4098 passed, 0 failed (afxdp/screen/fragment, threads=1). Named 22 cells 5× green.
- Fixture geometry (no contract change): NAT64 tails 0x0008→0x0018 and third-fragment 0x0009/0x0010→0x0019/0x0030 —
  the old offsets genuinely overlapped the 20B firsts (0..20 vs 8..28); assoc lookup is offset-blind so authority
  assertions are preserved while the new control no longer (correctly) drops them. v6 pkt_len kept full-frame (#6883).
- fmt: owned-files only (no wholesale churn); clippy (stable; pinned 1.98.1 lacks the component): clean for owned files.

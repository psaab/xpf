# #9626: the cold-prime obligation is discharged by a local write and is an unversioned boolean

Lane: lane-ha2. Worktree `/var/tmp/wt-9626-cold-prime-debt`, branch
`fix/9626-cold-prime-owed-until-acked`, from origin/master `538a89100`.

- **Timestamp**: 2026-09-11 05:38 PDT
- **Action**: Re-located the sites at `538a89100`. They match the plan drafted at `7c494b5ca`.
  - Arms: `sync_conn.go` (the #9618 incarnation switch; installConn's
    `wasDisconnected || supersededCurrent || epochReboot`).
  - Success discharges: `handleNewConnection`, the survivor re-drive and the sweep's owed re-drive.
    Each ran `needColdPrime.Store(false)` on `doBulkSync() == nil`.
  - Record and ack: `bulkSyncWindow` stores `pendingBulkAckEpoch` before BulkEnd, and the BulkAck
    handler clears it on an exact epoch match.
  - The daemon's bulk-prime retry defers while an ack is pending and younger than a function-local
    `retryWhileAckPendingAfter = 35s`.
- **File(s)**: none

- **Timestamp**: 2026-09-11 05:44 PDT
- **Action**: Measured item 1 RED on the unmodified master tree, with an uncommitted probe cell.
  - installConn armed the debt, and `syncSweep`'s owed re-drive completed a bulk (pending epoch 1).
  - With no BulkAck delivered, `needColdPrime` was already false.
  - The probe was then moved out of the worktree.
  - Item 2 (the race) is argued, not executed on master: master's flag cannot represent the
    ordering deterministically. The fix's cells and the mutation matrix bind it instead.
- **File(s)**: none (scratch probe)

- **Timestamp**: 2026-09-11 05:49 PDT
- **Action**: Fix, applied by a checked patch script (every anchor exactly once, or nothing
  written). The first run refused on one stale doc anchor, and no file was written.
  - `armColdPrimeLocked` bumps `coldPrimeGen` and sets the flag. Both arms use it.
  - `bulkSyncWindow` takes `coldPrimeOwedGen()` (under `s.mu`) when the bulk starts, and stores
    it in `pendingBulkOwed` immediately before `pendingBulkAckEpoch`.
  - The BulkAck handler, after the epoch match, loads the stamp, clears the pending state and
    calls `dischargeColdPrime(stamp)`. That clears the flag under `s.mu` only when the stamp equals
    `coldPrimeGen`.
  - The three success discharges are removed. All six clears of the pending epoch now go through
    `clearPendingBulkAck`, which also drops the stamp.
  - The sweep's owed re-drive gains `!s.coldPrimeAckAwaited()`: skip while the bulk stamped with
    the current debt is younger than `BulkAckPendingRetryAfter`.
  - `BulkAckPendingRetryAfter = 35s` is exported from pkg/cluster, and the daemon's retry loop
    reads it instead of its local literal.
  - gofmt and vet are clean on pkg/cluster and pkg/daemon.
- **File(s)**: `pkg/cluster/sync.go`, `pkg/cluster/sync_bulk.go`, `pkg/cluster/sync_conn.go`,
  `pkg/cluster/sync_conn_read.go`, `pkg/cluster/sync_conn_sweep.go`,
  `pkg/daemon/daemon_ha_sync.go`

- **Timestamp**: 2026-09-11 05:51 PDT
- **Action**: The cold-prime suites were run against the change. Exactly the three tests that
  encoded write-success discharge failed, and they were updated to the new contract:
  - `TestBootIDRebootColdPrimeIsDrivenToTheReplacement_9618`: three sweeps still send exactly one
    bulk, the debt stays owed, and the replacement's BulkAck discharges it.
  - `TestColdPrimeOwedWhenRuntimeWiredAfterConnect` (#82): one bulk, a pending ack and a still-owed
    debt, then the ack clears it. The standby replay is unchanged.
  - `TestOwedColdPrimeRedrivesOnSurvivorDespitePriorIncarnationAck_5718`: waits for the re-drive to
    record its pending epoch and delivers that ack on the survivor. Its fixture armed the debt with
    a bare `Store(true)`, which leaves the generation 0, so it now arms through
    `armColdPrimeLocked` as production does.
- **File(s)**: `pkg/cluster/sync_bootid_reboot_coldprime_9618_test.go`,
  `pkg/cluster/sync_cold_prime_late_runtime_82_test.go`, `pkg/cluster/sync_test.go`

- **Timestamp**: 2026-09-11 05:54 PDT
- **Action**: New cells in `pkg/cluster/sync_cold_prime_ack_9626_test.go`.
  - Owed until the peer acks. An ack for another epoch does not discharge; the matching one does.
  - An ack cannot discharge a debt armed after its bulk started (a real installConn supersession).
    The next re-drive's ack does discharge it.
  - An arm DURING the bulk, inside the store walk between BulkStart and the pending record, is not
    paid by that bulk. This binds the start-time stamp; a record-time stamp would pass every other
    cell.
  - The sweep does not re-bulk while the ack for this debt is young. It re-drives past
    `BulkAckPendingRetryAfter`, and the replaced bulk's late ack is ignored.
  - A bulk sent while nothing was owed (stamp 0) discharges nothing.
  - The cold-prime suites (#4962, #5718, #9618, #9174, #82, BulkAck, #9508, #6910, #6031) pass under
    `-race`.
- **File(s)**: `pkg/cluster/sync_cold_prime_ack_9626_test.go`

- **Timestamp**: 2026-09-11 05:56 PDT
- **Action**: Docs, `pkg/cluster/README.md`.
  - The #4962 bullet and the #9618 paragraph no longer say "consumed when a bulk succeeds".
  - A new paragraph, "The PEER discharges the obligation", covers the mechanism and the bound.
  - Item 3 is documented as an accepted limitation with its owner. A cold prime carries only the
    RGs this node is primary for (`ShouldSyncZone`, `primaryOwnerRGIDs`). After a reboot the
    survivor holds every RG once heartbeat loss is declared (manual-failover pins are cleared
    then), so the window stays open only if a table-losing peer reconnects before loss is
    declared.
  - Closing that window belongs to the owner-RG export path in pkg/daemon.
- **File(s)**: `pkg/cluster/README.md`

## Gates on 3cfc88ceb

- **Mutation matrix** (`mut9626.py`, a detached copy of 3cfc88ceb, `go test -json -race`, scored by the
  expected test's fail event). The baseline passed exactly the five cells; 7/7 KILLED,
  and the tree was restored clean:
    - P1-write-discharges: TestOwedColdPrimeStaysOwedUntilThePeerAcks_9626
    - P2-ack-ignores-generation: TestAnAckCannotDischargeADebtArmedAfterItsBulkStarted_9626
    - P3-ack-discharges-nothing: TestOwedColdPrimeStaysOwedUntilThePeerAcks_9626
    - P4-arm-no-generation: TestAnAckCannotDischargeADebtArmedAfterItsBulkStarted_9626
    - P5-stamp-at-record: TestAnArmDuringTheBulkIsNotPaidByThatBulk_9626
    - P6-no-storm-gate: TestSweepDoesNotRebulkWhileTheAckForThisDebtIsYoung_9626
    - P7-storm-ignores-age: TestSweepDoesNotRebulkWhileTheAckForThisDebtIsYoung_9626
- **`go test -race ./pkg/cluster`** on 3cfc88ceb: exit 0, no data race.
- **make test** on 3cfc88ceb: exit 0. Go leg 78 packages ok, no FAIL. Rust leg 11
  `test result:` lines, 5877 passed, 0 failed. No disk-full. It ran with `GOFLAGS=-p=4 CARGO_BUILD_JOBS=4`, to keep peak memory down on the shared host.
- **make test-failover** on local merge `7fcfa9a2e` (origin/master `11696bb28` + #9626 `3cfc88ceb`),
  deployed under the cluster lock with CoS re-applied.
  - The post-deploy reassert verified node0 primary for every RG.
  - 22 passed, 0 failed; iperf3 **20.9000 Gbps**.
  - Ledger row `0f17197b6da96f63.json`: verdict PASS, exe_check MATCH, build_git_sha = the merge.

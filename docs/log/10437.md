# 10437 / 10438 — canonical NAT view rendering

- **Timestamp**: 2026-09-19
- **Action**: route REST and gRPC NAT views through the shared `pkg/natshow` match/action renderers; add the additive source-match field to REST/gRPC source-NAT records.
- **Files**: `pkg/api/nat.go`, `pkg/api/types.go`, `pkg/api/nat_view_10437_10438_test.go`, `pkg/grpcapi/server_nat.go`, `pkg/grpcapi/xpfv1/xpf.pb.go`, `pkg/grpcapi/nat_view_10437_10438_test.go`, `proto/xpf/v1/xpf.proto`.

## RED then GREEN

### Cell 1 — destination-NAT complete match (#10437)

**RED** (handlers reverted to the pre-fix implementations):

```text
go test -count=1 -run TestRESTNATDestCarriesCompleteMatch_10437 ./pkg/api
--- FAIL: TestRESTNATDestCarriesCompleteMatch_10437
    REST reports dst_addr "203.0.113.10/32" where pkg/natshow renders
    "203.0.113.10/32 203.0.113.11/32"
    REST reports dst_addr "" where pkg/natshow renders "web-net"
FAIL

go test -count=1 -run TestGetNATDestinationCarriesCompleteMatch_10437 ./pkg/grpcapi
--- FAIL: TestGetNATDestinationCarriesCompleteMatch_10437
    gRPC reports dst_addr "203.0.113.10/32" where pkg/natshow renders
    "203.0.113.10/32 203.0.113.11/32"
    gRPC reports dst_addr "" where pkg/natshow renders "web-net"
FAIL
```

The probe was run by temporarily reverting only the two destination-handler
rendering hunks; the worktree was restored before the GREEN run. Translation
and not-installed control assertions remained intact.

**GREEN**:

```text
go test -count=1 -run 'TestRESTNAT(DestCarriesCompleteMatch|SourceActionAndMatch)_104(37|38)' ./pkg/api
ok   github.com/psaab/xpf/pkg/api  0.024s

go test -count=1 -run 'TestGetNAT(DestinationCarriesCompleteMatch|SourceActionAndMatch)_104(37|38)' ./pkg/grpcapi
ok   github.com/psaab/xpf/pkg/grpcapi  0.013s
```

Both handlers now return `natshow.RuleMatchDestination(rule)`, preserving plural CIDRs, named address-book members, translation action, and not-installed status.

### Cell 2 — source-NAT action and source match (#10438)

**RED** (handlers reverted to the pre-fix implementations):

```text
go test -count=1 -run TestRESTNATSourceActionAndMatch_10438 ./pkg/api
--- FAIL: TestRESTNATSourceActionAndMatch_10438
    REST reports type "" for rule "r-off", want "off"
    REST source row for rule "r-off" carries no source_match
    REST source row for rule "r-multi" carries no source_match
    REST source row for rule "r-book" carries no source_match
    REST reports type "" for rule "r-none", want "none"
    REST source row for rule "r-none" carries no source_match
FAIL

go test -count=1 -run TestGetNATSourceActionAndMatch_10438 ./pkg/grpcapi
--- FAIL: TestGetNATSourceActionAndMatch_10438
    gRPC reports type "" for rule "r-off", want "off"
    gRPC reports source_match "" for rule "r-off"
    gRPC reports source_match "" for rule "r-multi"
    gRPC reports source_match "" for rule "r-book"
    gRPC reports type "" for rule "r-none", want "none"
    gRPC reports source_match "" for rule "r-none"
FAIL
```

The first draft of this test used an incorrect exactly-one-row assumption for
a three-rule fixture; that harness-only failure was corrected by selecting
each rule's stable row before capturing the RED output above. The RED probes
temporarily reverted only the source-handler hunks and restored the worktree.
The generated source-match field remained present so the captured failures
isolate the pre-fix handler behavior.

**GREEN**:

```text
go test -count=1 -run 'TestRESTNAT(DestCarriesCompleteMatch|SourceActionAndMatch)_104(37|38)' ./pkg/api
ok   github.com/psaab/xpf/pkg/api  0.024s

go test -count=1 -run 'TestGetNAT(DestinationCarriesCompleteMatch|SourceActionAndMatch)_104(37|38)' ./pkg/grpcapi
ok   github.com/psaab/xpf/pkg/grpcapi  0.013s
```

The REST and gRPC regressions cover `off`, `interface`, `pool`, and actionless `none`, plus scalar/plural source addresses and named address-book members. Pool rows retain the structured contract (`type: pool`, `pool: <name>`) while the action classification is sourced from `natshow.SourceRuleAction`.

## Scoped suite proof

```text
go test -count=1 ./pkg/api
ok   github.com/psaab/xpf/pkg/api  54.731s

go test -count=1 ./pkg/grpcapi
ok   github.com/psaab/xpf/pkg/grpcapi  20.508s
```

The proto binding was regenerated with the repository's `protoc-gen-go-grpc v1.6.1`; the unrelated generated gRPC stub remained unchanged.

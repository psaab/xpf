# #9622 — a routing instance may not take the management VRF's name

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified muse-spark-008 H5 live at `81efb7aad` (strict compile accepts `routing-instances mgmt` at the report's R1 tip and at master; the daemon's hardcoded `mgmtVRFName` path unchanged since the report base). Enumerated the daemon's hardcoded VRF names: exactly one (`mgmt`); routing-instance table ids hash into [100000, 999999] and cannot reach table 999. Filed #9622 from #8865.
  - **File(s)**: none (probe not committed)
- **Timestamp**: 2026-09-10
  - **Action**: Added `config.ManagementVRFInstanceName` as the one definition of the name, `IsReservedRoutingInstanceName`, and `validateReservedRoutingInstanceNamesAST` (strict reject / lenient warn) on a new shared `routingInstanceNameUnionAST`, which the #3855 table-id gate now also uses. `compileRoutingInstances` quarantines a reserved-name instance on the lenient path; `lenientReservedRoutingInstanceName` added to the tolerant opts. The daemon reads the constant and skips a reserved instance in VRF planning and member binding.
  - **File(s)**: `pkg/config/routinginstance_reserved_name_9622.go`, `pkg/config/routinginstanceid.go`, `pkg/config/compiler.go`, `pkg/config/compiler_opts.go`, `pkg/config/compiler_routing.go`, `pkg/daemon/daemon_apply_interfaces.go`
- **Timestamp**: 2026-09-10
  - **Action**: Cells: strict reject for a top-level, an applied-group and a `${node}`-group instance; lenient quarantine with a warning and the unreserved sibling kept; `mgmt1`, `vrf-mgmt`, `MGMT`, `oob-mgmt` still commit. Documented the gate.
  - **File(s)**: `pkg/config/routinginstance_reserved_name_9622_test.go`, `docs/config-schema.md`, `docs/log/9622.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 1 returned MERGE NO (2 MAJOR, 3 MINOR). Applied:
    - MAJOR: the shared name scan (`collectRoutingInstanceNamesAST`) skipped the brace-elided leaf instance that `compileRoutingInstances` builds (#8787). It now mirrors the compiler, so both the reserved-name and `#3855` table-id gates see a packed instance.
    - MAJOR: the reservation is xpf-specific, not Junos parity (Junos reserves `mgmt_junos` under `system management-instance`, which xpf does not implement). This is now stated in the gate's doc and in `docs/config-schema.md`.
    - MINOR: the table-id gate leaves reserved names out of its union, matching the runtime order (reserved quarantine before the collision pass). The reserved gate is strict-only, so a lenient load reports the quarantine once.
    - MINOR: added `ManagementVRFDeviceName` and `ManagementVRFTableID`; the `vrf-mgmt` / `999` literals in the daemon, dataplane networkd compiler and routing FIB importer read them.
    - MINOR: new cells cover hierarchical and brace-elided input, `CompileConfigForNode*`, the collision ordering, packed-leaf collisions and `CheckText`; the docs no longer claim coverage the tests do not have.
  - **File(s)**: `pkg/config/routinginstanceid.go`, `pkg/config/routinginstance_reserved_name_9622.go`, `pkg/config/compiler.go`, `pkg/config/compiler_opts.go`, `pkg/config/compiler_routing.go`, `pkg/config/routinginstance_reserved_name_9622_test.go`, `pkg/configstore/check_reserved_ri_9622_test.go`, `pkg/daemon/daemon_apply_interfaces.go`, `pkg/daemon/daemon_flow.go`, `pkg/daemon/daemon_run.go`, `pkg/daemon/daemon_run_servers.go`, `pkg/daemon/daemon_ha_comms_wiring.go`, `pkg/dataplane/compiler_iface.go`, `pkg/routing/fibimport.go`, `docs/config-schema.md`, `docs/log/9622.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 2 verified all five round-1 dispositions and raised one MAJOR and one NIT.
    - MAJOR: a packed instance in a group nothing applies now collides with an active instance. Measured at `bdc238675` (master) and at the branch head:
      - A braced instance in an unapplied group is already refused on master, and master's lenient warning names the active instance as quarantined.
      - A packed one in the same position is accepted on master and refused on the branch.
      - Two packed top-level instances that fold to one table pass the strict gate on master, and the runtime then quarantines one of them with a warning. The branch refuses them.
    - Disposition: the over-approximation is #3855's pre-expansion view, which predates this branch and already applies to the braced spelling; filed as #9657. Narrowing the scan back would reopen the strict bypass in the third case, so the widening stays, and the scan comment and schema doc now say so.
    - NIT: two stale comments still pointed table 999 at pkg/daemon; fixed.
    - No logic changed since `fa453fc7c`, so the round-2 mutation matrix (7 mutants, 9 cells, all killed) still applies.
  - **File(s)**: `pkg/config/routinginstanceid.go`, `pkg/config/routinginstance_reserved_name_9622_test.go`, `pkg/routing/fibimport.go`, `docs/config-schema.md`, `docs/log/9622.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 3 returned MERGE YES with one NIT. Master does not silently quarantine the packed collision: the strict gate accepts it, and the runtime then quarantines one instance with a warning that the CLI and API surface. The scan comment, the schema doc and this log now say that. The previous commit's message used the inaccurate wording.
  - **File(s)**: `pkg/config/routinginstanceid.go`, `docs/config-schema.md`, `docs/log/9622.md`

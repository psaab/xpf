# #9895 — fail closed reverse NAT fallback across routing domains

- **Timestamp**: 2026-09-17
  - **Action**: Read issue #9895 in full and verified the reported mechanism
    against the current `origin/master` base `e7cfba758` after rebasing. The
    local 1:N and shared 1:1 reverse-NAT paths index replies with a
    domain-zero reverse key, so an unqualified pass-2 candidate could admit a
    tuple-valid reply from a different non-zero routing domain.
  - **File(s)**: `userspace-dp/src/session/lookup.rs`,
    `userspace-dp/src/afxdp/shared_ops.rs`

- **Timestamp**: 2026-09-17
  - **Action**: Controlled pre-fix RED after rebasing onto the current base.
    Reverting both guards while leaving the regression cells and production
    wrapper unchanged made each exact refusal cell fail: direct
    `session::reverse_domain_9895_tests::cross_domain_reply_is_refused_9895`
    → **0 passed, 1 failed, 6284 filtered**; local zone-aware
    `afxdp::session_glue::tests::zone_matching_cross_domain_reply_is_refused_9895`
    → **0 passed, 1 failed, 6284 filtered**; populated shared-map
    `afxdp::session_glue::tests::zone_matching_cross_domain_shared_reply_is_refused_9895`
    → **0 passed, 1 failed, 6284 filtered**. The failures prove the
    tuple-valid, zone-matching tenant-B reply was admitted before the guards.
  - **File(s)**: `userspace-dp/src/session/lookup.rs`,
    `userspace-dp/src/afxdp/shared_ops.rs`,
    `userspace-dp/src/session/reverse_domain_9895_tests.rs`,
    `userspace-dp/src/afxdp/session_glue/tests.rs`

- **Timestamp**: 2026-09-17
  - **Action**: Added fail-on-revert regression coverage. The direct session
    module pins cross-domain refusal, same-domain preference, tenant-forward /
    domain-0 reply fallback, and domain-0-forward / tenant-reply fallback.
    The session-glue cells drive the production `ReverseIngress::Zone` wrapper
    once through the local table and once through a populated shared NAT map;
    both assert the fixture egress zone so a wrong-zone rejection cannot mask
    the result. The shared cell also pins a legitimate non-zero-forward /
    domain-0-reply fallback.
  - **File(s)**: `userspace-dp/src/session/reverse_domain_9895_tests.rs`,
    `userspace-dp/src/session/mod.rs`,
    `userspace-dp/src/afxdp/session_glue/tests.rs`

- **Timestamp**: 2026-09-17
  - **Action**: Changed local pass 2 to skip a validating candidate when the
    reply domain and candidate domain are both non-zero; the preceding
    same-domain check still admits equality. Applied the same mixed-zero-only
    rule to the shared NAT map's exact-then-domain-zero probe, checking the
    stored forward entry before cloning it. Shared publication still retains
    both the preserving reverse key and the canonical domain-zero key.
  - **File(s)**: `userspace-dp/src/session/lookup.rs:282-370`,
    `userspace-dp/src/afxdp/shared_ops.rs:632-666`

- **Timestamp**: 2026-09-17
  - **Action**: Updated the routing-domain contracts to describe the guarded
    behavior: reverse fallback is valid only when either endpoint is domain 0;
    two different non-zero domains fail closed. Updated both the `SessionKey`
    field documentation and AF_XDP forwarding README, including the shared
    NAT-map second probe.
  - **File(s)**: `userspace-dp/src/session/key.rs:109-155`,
    `userspace-dp/src/afxdp/forwarding/README.md:323-343`

- **Timestamp**: 2026-09-17
  - **Action**: RED-to-GREEN proof on the final implementation. The six issue
    cells passed: `CARGO_TARGET_DIR=/tmp/cargo-9895 cargo test 9895 --
    --test-threads=1` → **6 passed, 0 failed, 6416 filtered**. The direct
    module passed **4**, the local zone cell passed **1**, and the populated
    shared zone cell passed **1**.
  - **File(s)**: `userspace-dp/src/session/reverse_domain_9895_tests.rs`,
    `userspace-dp/src/afxdp/session_glue/tests.rs`

- **Timestamp**: 2026-09-17
  - **Action**: Audited all sibling lookup-shaped paths. The local
    `find_forward_nat_match` and shared `lookup_shared_forward_nat_match` are
    the only reverse-match candidate fallbacks; both now enforce the same
    mixed-zero-only rule. `lookup_forward_nat_across_scopes_inner` only
    delegates local then shared. `lookup_shared_session` and
    `lookup_shared_forward_wire_match` are exact-key lookups, and the
    translated-alias / forward-wire local paths are exact-key validated
    lookups with no domain-zero fallback. A repository search found no other
    `map.get(&probe)` or reverse-match candidate probe requiring this guard.
  - **File(s)**: `userspace-dp/src/session/lookup.rs`,
    `userspace-dp/src/afxdp/shared_ops.rs`

- **Timestamp**: 2026-09-17
  - **Action**: Affected Rust suites are green, single-threaded as required by
    this crate's session tests: `CARGO_TARGET_DIR=/tmp/cargo-9895 cargo test
    session:: -- --test-threads=1` → **308 passed, 0 failed, 6114 filtered**;
    `CARGO_TARGET_DIR=/tmp/cargo-9895 cargo test
    afxdp::session_glue::tests:: -- --test-threads=1` → **149 passed, 0
    failed, 1 ignored, 6272 filtered**.
  - **File(s)**: `userspace-dp/src/session/*`,
    `userspace-dp/src/afxdp/session_glue/tests.rs`

- **Timestamp**: 2026-09-17
  - **Action**: Existing `vrf_session_identity_doc_guard` remains **1 passed,
    1 failed**. The failure is the pre-existing stale install-string assertion
    at `userspace-dp/tests/vrf_session_identity_doc_guard.rs:253` expecting
    unconditional `remove_entry`; this issue does not change install
    replacement semantics. The routing-domain source assertions run before
    that stale assertion and remain satisfied.
  - **File(s)**: `userspace-dp/tests/vrf_session_identity_doc_guard.rs`

# #9813: a fabric overlay re-created outside an apply is left outside vrf-mgmt

- **Timestamp**: 2026-09-11 10:58 PDT
- **Action**: Triaged the issue read-only on master `8a0919262`, then prototyped the fix locally,
  waiting on the lead's confirmation of the pick and scope.
  - Confirmed: `ensureFabricIPVLAN` creates the IPVLAN with only a name, a parent and L2 mode. The #6791
    `reassertFabricIPVLANOnce` re-creates a missing overlay through `fabricEnsureFn`, and
    `missingFabricOverlays` never looks at `MasterIndex`. The management-VRF set is bound only by apply
    steps 0b and 2.7. Session sync binds its sockets to the VRF device (`pkg/cluster/sync_conn.go`).
  - Apply order: `applyFabricIPVLAN` runs before step 2.7, so an overlay created inline is bound by the
    re-bind. The deferred `OnXSKBound` callback can run after it, and so can the re-assert loop.
  - Open PR #9851 (`fix/9819-vrf-miss-terminator`) changes `pkg/routing` only; this change uses the
    existing `BindInterfaceToVRF` API and touches no `pkg/routing` file.
- **File(s)**: none (read-only)

- **Timestamp**: 2026-09-11 10:58 PDT
- **Action**: Implemented the fabric/management half.
  - `fabric_overlay_vrf_9813.go`: `fabricOverlayOutsideMgmtVRF` checks whether a name is in the
    published management-VRF set, exists, and has a master that is not `vrf-mgmt`.
    `bindFabricOverlayToMgmtVRF` binds through the routing manager. `fabricOverlaysOutsideMgmtVRF`
    and `rebindFabricOverlaysToMgmtVRF` handle every configured overlay.
  - The re-assert pass binds after its ensure step, and its cheap gate counts an overlay outside the VRF.
  - The deferred `OnXSKBound` callback is now `createDeferredFabricOverlays`, which binds each overlay it
    creates.
  - Cells: a re-created overlay is bound; an overlay that lost its master is bound without being
    re-created; an enslaved overlay is left alone; nothing is bound outside the published set; the
    deferred creation binds; an AST pin checks that the callback is the binding creator; a kernel cell
    in a private netns covers both the re-create and an out-of-band nomaster.
  - Base evidence: a temporary probe, using base symbols only, ran in a detached worktree at master
    `30c30ab6a` (which includes #9851) under `unshare -rn`. The re-assert loop re-created fab0 with
    master index 0 while vrf-mgmt was index 6: the defect as filed. The probe was not committed.
  - README: `pkg/daemon/README.md`, next to the #6791 paragraph.
  - Not covered here: tenant routing-instance members re-created outside an apply. Their scope is with
    the lead.
- **File(s)**: pkg/daemon/fabric_overlay_vrf_9813.go, pkg/daemon/fabric_overlay_vrf_9813_test.go,
  pkg/daemon/daemon_fabric_reassert.go, pkg/daemon/daemon_apply_interfaces.go, pkg/daemon/README.md,
  docs/log/9813.md

## Gates on 4735be0d8

- **Mutation matrix** (`mut9813.py`, a detached copy of 4735be0d8, `unshare -rn go test -json -race`, so the
  kernel cell runs; scored by the expected test's fail event). The baseline passed exactly the 7 cells, the
  kernel cell included; 8/8 KILLED, and the tree was restored clean:
    - M1-gate-ignores-unbound-overlays: TestFabricReassertRebindsAnOverlayThatLostItsMaster_9813
    - M2-pass-does-not-rebind: TestFabricReassertBindsARecreatedOverlayToTheManagementVRF_9813
    - M3-deferred-creation-not-bound: TestDeferredFabricOverlayIsBoundWhenCreated_9813
    - M4-callback-is-not-the-binding-creator: TestApplyFabricIPVLANDefersThroughTheBindingCreator_9813
    - M5-binds-outside-the-published-set: TestFabricReassertBindsOnlyWhatTheApplyPutInTheManagementVRF_9813
    - M6-master-not-checked: TestFabricReassertLeavesAnEnslavedOverlayAlone_9813
    - M7-vrf-device-name-disagrees-with-routing: TestReassertBindsTheFabricOverlayInTheKernel_9813
    - M8-binds-the-wrong-instance: TestFabricReassertBindsARecreatedOverlayToTheManagementVRF_9813
- **`go test -race ./pkg/daemon`**, skipping only `TestIPsecSASyncHotEnableStartsAndStops8967` (#9844), on
  4735be0d8: exit 0, no data race. That test's race is pre-existing: it reproduces on master, and both full
  race runs of #9766/#9767 raced only there.
- **make test** on 4735be0d8: exit 0. Go leg 78 packages ok, no FAIL. Rust leg 22
  `test result:` lines, 8373 passed, 0 failed. No disk-full. It ran with `GOFLAGS=-p=4 CARGO_BUILD_JOBS=4`, to keep peak memory down on the shared host.
- **make test-failover** on local merge `0a6442b60` (origin/master `d68856bf5` + `4735be0d8`),
  deployed under the cluster lock with CoS re-applied.
  - The post-deploy reassert verified node0 primary for every RG.
  - 22 passed, 0 failed; iperf3 **21.0000 Gbps**.
  - Ledger row `9eca535fcfd08ae7.json`: verdict PASS, exe_check MATCH, build_git_sha = the merge.
- **Earlier failover gates on this head.** Both are superseded by the gate above.
  - `f0b552a28`, master `30c30ab6a` + `4735be0d8`: 22 passed, 0 failed, but iperf3 **13.6 Gbps**, below the campaign's 17.1 floor. Ledger `f6b1ad22fde7ace7`. The run was otherwise normal: iperf3 survived the fw0 crash, the rejoin and the manual failback.
  - `c8a551c88`, master `d68856bf5` + `4735be0d8`: the host's low-memory stop killed this re-gate during phase 1 of the rolling deploy, before any test ran. It had pushed binaries and config to fw1 and not yet restarted it.
  - The gate recorded above re-ran in the foreground on the same master. Its rolling deploy restored both nodes, and it read 21.0 Gbps. The 13.6 Gbps reading did not reproduce, so it is recorded here and not filed. The campaign files only when two master runs fall below 17.1.

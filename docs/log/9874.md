# #9874 — authored-but-empty source-NAT `match` ships as unconstrained catch-all on the tolerant path

- **Timestamp**: 2026-09-12
- **Action**: Filed from deep-review finding F-003 (slice E04-04), research-validated
  (three-way adversarial gate: Codex / AGY / Claude SMR all MATERIAL High).
  Mechanism: `natMatchIsConstrained` + the `matchAuthored &&
  !natMatchIsConstrained` tests (`nat_match_unconstrained_8430.go`), strict
  error vs tolerant warning (`compiler_uniformgates_nat.go:112-121`), and the
  Rust consumer (`source/mod.rs:1287`, empty match set means match-any). #8430
  owns the strict reject; no open owner for the tolerant-path ship. Severity
  High: a boot or peer-sync installs a catch-all translator after only a
  warning, breaking return-path identity.
- **File(s)**: none (filing)

- **Timestamp**: 2026-09-15
- **Action**: Lane opened at base `d3e959067`. STEP-0 verified the defect is
  PRESENT on base, not already-fixed: strict rejects the empty-match SNAT rule
  via the #8430 gate; tolerant (`CompileConfigLenient`) accepts with warning
  only and `buildSourceNATSnapshots` ships rule R1 with
  `source=[] dest=[] ports=[] apps=[]` and no marker; Rust installs it with
  `source_constrained=false` and `nets_match_v4(false, [], ip)` returns true —
  executed end-to-end (unmarked empty snapshot translates an arbitrary flow).
  DNAT is already fail-closed for this shape (Go builder skips when `destAddrs`
  is empty, `nat_destination.go:167-169`) but silently. STEP-0 scaffolding
  removed after the final cells subsumed it. Brief posted to the issue as
  comment 5676392732.
- **File(s)**: none (measurement; scaffolding removed)

- **Timestamp**: 2026-09-15
- **Action**: Plan review: Codex APPROVE-WITH-NOTES + AGY APPROVE-WITH-NOTES
  (GLM CLI absent on this host, so AGY served as second reviewer). Both endorse
  claim-and-drop, exported+`json:"-"`, DNAT warned-skip-only, and the show-label
  shapes. Adopted deltas: bump the snapshot protocol to v19 (v9/v10/v11
  doctrine — an old helper ignoring the marker installs exactly the catch-all
  this closes, so #9173's STANDS test answers no); gate the allocator pending
  on `!lenient_match_dropped` (no bitmaps / #6812 budget for a rule that can
  never mint); extend `DestinationNATRuleExcludedReason` before the `off`
  exemption; poison-note precedence over pool-disarm and terminal-action notes
  in `show`. Rejected: `builderExcluded` harness edit and extending the #7473
  SNAT composition. Verified the only production `SourceNatLookup` consumer
  drops on `Unavailable`, one `SourceNatRule` construction site, and
  `exception_reason()` as the only exhaustive match on the failure enum.
  Adjudication posted as comment 5676722875. Incident: AGY's headless run
  needed `--dangerously-skip-permissions` to produce output and applied edits
  despite `--mode plan`; all foreign edits were reverted and verified — the
  implementation is the lane's own.
- **File(s)**: none (review)

- **Timestamp**: 2026-09-15
- **Action**: Implementation (tolerant-path only; strict unchanged). Typed
  marker `NATRule.LenientMatchDropped bool` with `json:"-"` (#9246 precedent —
  #4406 goldens and `ConfigFingerprint` stable), set at the end of per-rule
  parse in `compileNATSource` / `compileNATDestination` as
  `rule.matchAuthored && !natMatchIsConstrained(rule.Match)`, the exact #8430
  predicate. SNAT wire field `lenient_match_dropped` (omitempty /
  serde-default); Go SNAT builder copies it with a `slog.Warn`; Go DNAT builder
  warned-skips marked rules (disposition unchanged — already closed — now loud,
  no DNAT wire change since no entry is emitted). Rust source table:
  `lenient_match_dropped` on `SourceNatRule` + snapshot, parse assigns it with
  an `eprintln!`, and the match loop returns
  `Unavailable(AuthoredMatchEmpty)` (`source_nat_authored_match_empty`) AFTER
  `matches()` but BEFORE the `off` short-circuit — a poisoned `off` rule drops
  instead of exempting everything. Claim-and-drop, no fall-through (forwarding
  untranslated is the #5688 leak shape). Show: source `ADMITTED BY TOLERANT
  LOAD` note (#7640 style) naming the drop consequence, destination
  `NOT INSTALLED` note (#6534 style) via the predicate's marker clause;
  healthy output byte-identical. Sweep: `Rule.LenientMatchDropped` column
  registered fixture-constant (blind spot, NOT production-constant).
- **File(s)**: pkg/config/types_security.go,
  pkg/config/compiler_nat_source.go, pkg/config/compiler_nat_destination.go,
  pkg/config/nat_exclusion_reason.go,
  pkg/config/nat_empty_match_poison_9874_test.go,
  pkg/config/nat_source_axis_sweep_6812_test.go,
  pkg/dataplane/userspace/nat_source.go,
  pkg/dataplane/userspace/nat_destination.go,
  pkg/dataplane/userspace/protocol_nat.go,
  pkg/dataplane/userspace/nat_empty_match_poison_9874_test.go,
  pkg/dataplane/userspace/nat_source_axis_sweep_6812_test.go,
  pkg/natshow/natshow.go, pkg/natshow/source.go, pkg/natshow/dest.go,
  pkg/natshow/nat_empty_match_poison_9874_test.go, userspace-dp/src/nat/mod.rs,
  userspace-dp/src/nat/source/mod.rs, userspace-dp/src/nat/source/match_rules.rs,
  userspace-dp/src/nat/source/failure.rs,
  userspace-dp/src/nat/tests_empty_match_poison_9874.rs,
  userspace-dp/src/protocol/nat.rs

- **Timestamp**: 2026-09-15
- **Action**: Snapshot protocol v18 -> v19 (4-file bump per the v18 template,
  plus pins): the typed diagnostic is `json:"-"` but the transmitted wire
  field moves the shape digest, and the bump is REQUIRED (not STANDS) because
  an old helper ignoring the marker keeps installing the catch-all — the old
  behavior IS the defect. Moved the #9054 pin 18 -> 19 and regened the
  `protocol_wire_v1.json` fixture (the regen fixed the `wire_invariant`
  default-specimen failure the field initially caused). Fixed a self-inflicted
  green-breaker along the way: the `allocator_key()` closure reformat had
  duplicated the #9428 parity guard's must-replace-once anchor.
  MERGE-SEQUENCING NOTE: v19 is ALSO claimed by lane 9875b
  (FromUnrepresentable). Parent ruling: first-merged PR takes 19, the other
  rebases to 20 + re-gates. This lane continues against 19; do NOT renumber
  here.
- **File(s)**: pkg/dataplane/userspace/protocol.go,
  userspace-dp/src/protocol/control.rs,
  pkg/dataplane/userspace/snapshot_shape_version_8892_test.go,
  pkg/dataplane/userspace/secure_tunnel_protocol_6691_test.go,
  pkg/dataplane/userspace/learned_route_cap_blackhole_9054_test.go,
  userspace-dp/tests/fixtures/protocol_wire_v1.json

- **Timestamp**: 2026-09-15
- **Action**: Validation (resume agent, first-hand on the final tree). Flake
  check — every named 9874 cell 5x, all green: `go test -run 9874 -count=5`
  in pkg/config (9 cells), pkg/dataplane/userspace (5 cells), pkg/natshow (4
  cells); `cargo test 9874` x5, lib `6 passed; 0 failed` every iteration. Full
  `cargo test --no-fail-fast`: 6038 passed / 6 ignored across all 11 targets
  with exactly ONE failure — `cos_literal_guard_7689`
  (NeighborSnapshot 29 exhaustive literals vs ceiling 26), PROVEN PRE-EXISTING
  (identical count at base, zero file overlap with this diff) and already
  filed as #10070 — not chased. Full Go suites for the three touched packages,
  first-hand: pkg/config ok (208s), pkg/dataplane/userspace ok (57s),
  pkg/natshow ok. Untouched-consumer suites (configstore/cli/api/grpcapi)
  green per the implementation handoff on identical Go code (this resume adds
  no Go change). `go vet` + `gofmt` clean on touched files. Scrub review found
  one self-inflicted doc mangling in the wip — two comment lines dropped from
  `PoolPeerAddressOverlap`'s doc when the new variant landed beside it, leaving
  "config that reaches this state..." dangling — restored verbatim from base;
  every other deleted line in the diff audited intentional (pins, golden,
  deliberate restructures). Wip squashed into six shippable commits; the PR
  carries (Closes #9874), unmerged per lane contract.
- **File(s)**: none (validation)

- **Timestamp**: 2026-09-15
- **Action**: Parent PR review (GPT NEEDS-MAJOR + GLM READY): GPT's 3 findings
  stood mechanistic and unrefuted, folded as review-response commits on top of
  the opened PR #10074. (1) HIGH — poisoning left a default allocator reused
  as real state: the previous-generation collection keyed every pool-mode rule
  via `drain_allocator_key`, so a poisoned rule published its never-built
  `PortAllocator::default` (max_tracked_flows == 0) under the live pool key;
  a subsequently healthy rule reused it and failed every mint as
  AllocatorExhausted (correcting the match did NOT restore SNAT), and on a
  shared pool the placeholder could displace a healthy sibling's allocator
  (first-wins or_insert). Fix: skip poisoned NON-DRAINING rules at collection
  (they can hold only the default — no pending, no 1c carry), while poisoned
  DRAINING rules still contribute their carried live allocator (#7717).
  Covered by poisoned→healthy recovery (fresh build + e2e translate),
  poisoned-first shared-pool refreshes (gen2/gen3 still translate, 0 rebuilds),
  and poisoned+draining retention (live count survives, new flows still drop
  as AuthoredMatchEmpty). (2) MEDIUM — Go still charged pools Rust excludes:
  the #6812 charges walk never filtered by LenientMatchDropped, so
  poisoned-only pools consumed first-fit budget and could mark a later healthy
  pool aggregate_over_budget, which Rust honors. Fix: only a HEALTHY reference
  charges, at its first healthy reference (no seen-mark on poisoned refs);
  no-op on strict (#8430 rejects first). Covered by a charges order pin
  ([poolB poolA], poolC absent) and a compiler→snapshot regression (1024
  poisoned-only pools + healthy good). (3) LOW — the no-allocator test asserted
  `allocator_key()` only, blind to pending-guard removal; added the #6812 F3
  constructor-count leg (healthy twin builds exactly 1). Every new test
  RED-checked by reverting its fix (Rust: recovery build 0, sibling mint
  failure, pending-guard build 2; Go: charges [poolA poolB poolC], good
  aggregate_over_budget). Coordination: lane 9877 (partial match loss) reuses
  this mechanism's pattern with a sibling marker; merge protocol agreed —
  whoever lands second adds the show-precedence yield (their NOT-INSTALLED
  over my ADMITTED for skipped rules) and the walk merge is a clean OR of two
  separate ifs. Re-gates after the fold, first-hand: `cargo test
  --no-fail-fast` 6041 passed / 6 ignored with the ONLY failure the
  pre-existing cos_literal_guard_7689 (#10070); `go test` pkg/config ok
  (195s), pkg/dataplane/userspace ok (53s), pkg/natshow ok; named 9874 cells
  5x green (Go 10+6+4, Rust 9/9 every iteration); gofmt/vet clean; new Rust
  test code rustfmt-clean (mod.rs:349 drift is the intentional #9428-anchor
  avoidance, ambient drift elsewhere left untouched).
- **File(s)**: userspace-dp/src/nat/source/mod.rs,
  userspace-dp/src/nat/tests_empty_match_poison_9874.rs,
  pkg/config/compiler_validate_strict_nat.go,
  pkg/config/nat_empty_match_poison_9874_test.go,
  pkg/dataplane/userspace/nat_empty_match_poison_9874_test.go

# #9490 — config: four multi-value leaves accept and compile undeclared trailing tokens

- **Timestamp**: 2026-09-10
  - **Action**: Established each leaf's real accepted set before gating it.
    - **Login-class permissions:** transcribed from Juniper's "User Access
      Privileges" permission flags table (42 flags). xpf's `super-user` alias
      is kept, because mapJunosPermissions has always mapped it to PermAll.
    - **Community members:** read from FRR stable/10.6 bgpd/bgp_community.c
      (community_gettoken, community_valid). A standard community is one or
      more words, each a well-known name from a fixed 14-name set or ASN:VALUE
      with exactly one `:` and both parts 0..65535. `internet`, the Junos
      `no-export-subconfed`, large communities and extended communities are
      rejected.
    - **Address-set members:** the runtime resolver reads Addresses then
      AddressSets on the compiled global book, after zone-local folding.
    - **Blast-radius census:** tests use only valid permission flags and
      ASN:VALUE / `no-export` community members.
  - **File(s)**: none (research)
- **Timestamp**: 2026-09-10
  - **Action**: Gates:
    - The permissions leaf is a typed multi leaf with ValidateLoginPermission,
      so validateMultiValueLeaf checks every token in every spelling.
    - ValidCommunityMember, the SSOT shared by the strict gate and pkg/frr's
      render belt, now requires a non-regex member to be an FRR standard
      community literal. The doc comments that had deferred this "to whoever
      has FRR's grammar in hand" now cite the source, and the strict error no
      longer claims every member renders as an expanded list.
    - New validateAddressSetMembersDefinedStrict: every member of every set
      must name an address or address-set. It is strict on commit, warns via
      lenientAddressSetMembersDefined on load / peer-sync, and runs AFTER the
      #3149 policy-side gate, so a set a policy uses keeps that gate's
      policy-naming message.
  - **Action**: Two existing cells spoke:
    - The #3149 cell wanted the policy-side message. Ordering the new gate after
      it keeps that message.
    - The #4097 render cell pinned `bgp community-list standard evilc permit
      65000:100  router bgp 65000`. FRR's parser rejects that line too, since
      `router` is no community. The definition is now omitted, which is
      strictly stronger, and the no-injected-line assertion is kept.
  - **File(s)**: pkg/config/login_permission_flags_9490.go,
    pkg/config/schema_system.go, pkg/config/community_member.go,
    pkg/config/compiler_validate_strict_routing.go,
    pkg/config/compiler_validate_strict_addrset_members_9490.go,
    pkg/config/compiler_uniformgates_sampling_appset.go,
    pkg/config/compiler_opts.go, pkg/frr/policy_injection_4097_test.go,
    docs/system-login.md
- **Timestamp**: 2026-09-10
  - **Action**: Cells:
    - pkg/configstore TestMultiValueLeavesRefuseUndeclaredTokens9490 covers
      every issue row through CheckText, refused and naming the bogus token. It
      also covers the acceptance rows: bracketed, flat-run and repeated-line
      valid spellings for each leaf must commit.
    - pkg/config TestNonRegexCommunityMemberIsAnFRRLiteral9490 tables FRR's
      literal grammar, with regex controls.
    - TestAddressSetMemberGateIsLenientOnLoad9490: strict refuses, lenient warns
      and boots.
  - **File(s)**: pkg/configstore/multivalue_leaf_validators_9490_test.go,
    pkg/config/community_literal_9490_test.go, docs/log/9490.md
- **Timestamp**: 2026-09-10
  - **Action**: Suites: pkg/configstore, pkg/frr, pkg/cli, cmd/cli and
    pkg/grpcapi are green. pkg/config was green except the #9206 multi-leaf
    absorption census, which counted 37 absorbing sites (26 distinct) against a
    record of 39 (27). This change caused the drop: `system login class
    permissions`, now a typed multi leaf with a validator, is refused at the
    schema walk at both of its positions, top-level and the `groups` rehost.
    Community members and address-set members are refused at compile, so they
    correctly remain in the count. The record is tightened to 37/26 with that
    attribution.
  - **File(s)**: pkg/config/multi_leaf_absorption_population_9206_test.go, docs/log/9490.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation matrix at code head `2b21268aa`, scored by `go test -json`
    test NAME across pkg/config and pkg/configstore. Each mutant was applied
    under an exact-count anchor assertion, proven by a non-empty `git diff`,
    and restored with `git checkout HEAD --`. NOOP was clean, and 13 of 13
    were KILLED by their target:
    - P1: permissions unvalidated.
    - P2: permissions untyped, which leaves its validator inert.
    - P3: permission accepts anything.
    - C1: literal check removed.
    - C2: 16-bit bound widened.
    - C3: large community admitted.
    - C4: `internet` admitted.
    - C5: single-word members only.
    - A1: address-set gate not called.
    - A2: defined() accepts everything.
    - A3: nested address-set members unchecked.
    - A4: lenient flag ignored.
    - A5: lenient flag unset on the tolerant path.
  - **File(s)**: docs/log/9490.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    6248d4d70. That tree is origin/master 631066ed5 merged with 2 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9490, #9561.
    - Go leg: 80 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9490.md

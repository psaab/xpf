# #9561 — config: brace-elided snmp v3 usm spellings compile to zero SNMPv3 users

- **Timestamp**: 2026-09-10
  - **Action**: Measured the AST before changing anything. The three elided
    spellings put part of the path on v3's own Keys: `[v3 usm]`,
    `[v3 usm local-engine]` and `[v3 usm local-engine user u1]`. compileSNMPv3
    read only an 8-key packed line or a real `usm` child, so all three compiled
    to zero users on the strict and tolerant paths with no warning. The same
    probe ran `packedBodyChildren(v3, schemaForPath("snmp", "v3"))` on each: it
    rebuilds every elided spelling into exactly the braced
    `usm > local-engine > user u1 > body` chain, and returns a braced v3's
    children unchanged.
  - **Action**: compileSNMPv3 looks `usm` up through that shared #6818/#7653
    expander instead of FindChild. The existing user reader handles the rebuilt
    chain, so there is no second code path.
  - **Action**: Cell TestElidedUSMSpellingsCompileTheUser9561 runs the three
    elided spellings with the braced and #7653 spellings as same-run controls,
    on strict and lenient. It pins the absolute result (user u1, sha256/aes128
    and both passwords), because equivalence alone passes when everything is
    broken. The #7653, #9155-R02, #6822, #9414 and #8859/#8895 cells pass, and so
    does the full pkg/config suite.
  - **File(s)**: pkg/config/compiler_system.go,
    pkg/config/snmpv3_elided_usm_9561_test.go, docs/log/9561.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation matrix at code head `49d56e6cf`, scored by `go test -json`
    test NAME. Each mutant was applied under an exact-one-anchor assertion,
    proven by a non-empty `git diff`, and restored with `git checkout HEAD --`.
    NOOP was clean, and 3 of 3 were KILLED by
    TestElidedUSMSpellingsCompileTheUser9561:
    - M1: back to FindChild("usm").
    - M2: the wrong schema path (`snmp` instead of `snmp v3`), so nothing
      expands.
    - M3: matching the wrong child name.
  - **File(s)**: docs/log/9561.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    6248d4d70. That tree is origin/master 631066ed5 merged with 2 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9490, #9561.
    - Go leg: 80 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9561.md

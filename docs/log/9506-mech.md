# #9506 P-MECH mechanism phase (deny-only V1 transport)

## Scope

Mechanism phase only: D1b quarantine + nft guard, durable quarantine
identity, and the Rust D11 worker/verdict bridge, with the proof ledger
below. Policy/permit behavior (S9.5), counter follow-ups, and later slices
are out of scope for this log.

Commits on `fix/9506-mech` (oldest first, additive, no restack):

| Commit | Area |
| --- | --- |
| `e5252ef4a` | Go deny-only transport hardening (suppression, 26B tail, code10) |
| `a02639396` | Rust P-MECH V1 transport foundations + strict 26B tail + outcome10 |
| `f8cf097c8` | Go closed-world admission-reason validation |
| `2fb8d4453` | Go terminal would-permit suppression coverage |
| `f562cfd5d` | Go admission reason/boolean consistency |
| `0c6647373` | Rust transport reclamation and view preservation |
| `4e01cb956` | Rust worker verdict transport join |
| `b720dbc4b` | D1b quarantine + nft guard |
| `964a3d910` | Persist IPsec quarantine identity |
| `957b37f4c` | Rust D11 verdict bridge (8 files, +520/-43) |

`957b37f4c` routes V1 submits carrying the 26B P-MECH tail into the
authoritative RuntimeView + `IpsecInnerQueue` ingress, adjudicates on
workers, and joins verdicts to Go-facing completions (Deny /
WouldPermit-10, never a q0 write).

## Focused proofs (post-commit reruns, `957b37f4c`)

- `cargo test -p xpf-userspace-dp ipsec_inner_queue`: **18 pass, 0 fail**.
- `cargo test -p xpf-userspace-dp ipsec_inner_verdict_bridge`: **2 pass, 0 fail**.
- `cargo test -p xpf-userspace-dp reinject_9506`: **66 pass, 0 fail**.
- `cargo test -p xpf-userspace-dp --no-run`: compile OK (warnings only).
- Go focused regression (`AdmissionReasonConsistency`,
  `SubmitBatchPMechTailTwoFrames`, `ExtendedCompletionOutcomes`):
  **all PASS**.
- `CARGO_HOME=/tmp/cargo-9506mech` throughout. No test or fixture was
  modified to obtain these results.

## Attested run attempt-1: VOID (wrong binary)

- **Command**: `./test/incus/with-cluster.sh '9506 mech' -- ./test/incus/t12-g2-9506.sh`
- **Tree**: clean `957b37f4c56e33a697ae090a89e04d69bb947f3a`, verified frozen
  before the run (no staged/unstaged/untracked entries).
- **Result**: exit 2; `T12_G2_SUMMARY cells=30 pass=0 fail=0 void=30`.
- **Attestation**: `exe_check=UNAVAILABLE`, `local_exe_sha=unknown`,
  `fw0_exe_sha=fw1_exe_sha=cf654836…45bae`. The deployed pair is
  self-consistent but attests nothing about `957b37f4c`.
- **Ruling**: the VOID measures the wrong binary, so attempt-1 was never
  the attested run. No budget burned. No rerun-and-tune; redeploy +
  single rerun separately authorized with pre-run exe-sha verification.
- **Archive**: `/var/tmp/xpf-t12-g2-9506-1789902213`.
- **Full ledger**: harness output `artifact://53483` (3,517 lines).

VOID reasons split into two classes (counts from the ledger):

- `missing-precondition:exe-attestation-UNAVAILABLE-requires-MATCH`
  (bypass, recreate, conformance, refusal, rotation, ownership, bind
  scope cells).
- `measurement-incomplete:*` (exact armed-pinhole/provenance sets,
  order/permit observers, VRF/workload observers, queue-buffer
  observers).

This is explicitly NOT the fixtures-missing VOID: the ledger records
`fixture_ready=1` and
`fix9506: converge stn=2/2 sa=1/1 (want 1) queues=8/8 (want 8) nft=1/1`,
plus `T12_G2_PRECONDITIONS` all ones.

Observed-not-attributed traffic notes (no claim attaches: the binary
under test is unattested):

- t12 pre→post per node (symmetric fw0/fw1): `consumed 0→4`,
  `adjudicated 0→4`, `reinjected 0→3`, `written 0→3`, `uncertain 0→1`,
  `late_completions 0→1`, `timeouts/stale/cancelled/refused 0`,
  `delivered 0→4`.
- v4 G2 cells mirror the t12 counts; v6 G2 cells show zeros
  (`provenance_samples=0`, all counters 0).
- `late_completions=1` and v6-G2 zeros are carried as open ledger
  notes for the valid run (explain even if green).

## Attested run attempt-2: VOID (fixture missing; executable MATCH)

- **Pre-run deployment gate**: `XPF_DEPLOY_FAST=1 CARGO_HOME=/tmp/cargo-9506mech
  make cluster-deploy`; local and both running remote executables were
  `d5e18a0b4e3060ca234afab8ed7c0e358a0b37f693124d116d34039a86edda63`.
- **Command**: `./test/incus/with-cluster.sh '9506 mech' -- ./test/incus/t12-g2-9506.sh`.
- **Attestation**: `git_sha=957b37f4c56e33a697ae090a89e04d69bb947f3a`,
  `local_exe_sha=fw0_exe_sha=fw1_exe_sha=d5e18a0b4e3060ca234afab8ed7c0e358a0b37f693124d116d34039a86edda63`,
  `exe_check=MATCH`, `exe_scope=both`.
- **Result**: exit 2;
  `T12_G2_SUMMARY cells=30 pass=0 fail=0 void=30 exe_check=MATCH`;
  archive `/var/tmp/xpf-t12-g2-9506-1789904125`.
- **Exact fixture failure**: `error: rpc error: code =
  FailedPrecondition desc = local redundancy group 2 not ready for explicit
  failover ready=false ready_since=<zero> reasons=[userspace helper not
  enabled userspace forwarding unsupported userspace forwarding not armed
  userspace XSK liveness not proven]`. The fixture setup then restored state
  and deleted `loss:xpf-ipsec-peer-9506`.
- **Exact preconditions**:
  `T12_G2_PRECONDITIONS stn=0 stn_fw0=0 stn_fw1=0 xfrm_sa=0
  xfrm_sa_fw0=0 xfrm_sa_fw1=0 divert_table=0 fence_table=1
  q0_mark_surface=0 ruleset_fw0_readable=1 ruleset_fw1_readable=1
  complete_fixture=0`. Thus this is a fixtures-missing VOID, not a mechanism
  verdict; userspace was not armed and no live stN/XFRM-SA packet workload
  was measured.
- **Witness readbacks**: pre-fixture fw0/fw1 both reported
  `daemon_active=1`, `socket_ready=1`, `reinject_sockets=2`,
  `s5_available=0`, `actor_active=0`, empty `run_id`, `generation=0`,
  `permit_state=UNKNOWN`, `permit_epoch=0`, and all packet/completion
  counters zero, with
  `reason=product-observer-unavailable:s5_reinject-status-absent`.
  Listener overhead was `fw0=41 fw1=39`, with two reinject listeners each.
  The observer readback was structural `fence=0 divert=0 order=0`, with
  both provenance joins unavailable.
- **Ledger**: `/var/tmp/xpf-t12-g2-9506-1789904125/cells.tsv` has 30 rows:
  13 `fixture-setup-failed`, 8 `measurement-incomplete`, 8
  `missing-precondition`, and 1 `product-observer-unavailable`; all verdicts
  are VOID. Restore/residue remained clean:
  `T12_G2_RESTORE fw0_config_cmp=1 fw1_config_cmp=1 fw0_residue_clear=1
  fw1_residue_clear=1 residue_probe_error=0`. Full harness ledger/output is
  preserved at `artifact://53629`.
- **Ruling**: attempt-2 is the one valid executable attestation (`MATCH`) but
  is VOID because the live fixture was missing. The attestation budget is
  exhausted; no further live runs or fixture repair were attempted.

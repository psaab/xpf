# #9571 — duplicate-policy fold turning a deny into a permit

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified at origin/master `a475b3ea4` with a throwaway
    probe (removed). Channel `CompileConfigLenient` + snapshot mirror +
    `policymatch.Match`: deny-first/permit-second (zone-pair and global), the
    /25 union, and deny + permit-fragment each compiled to ONE permit policy
    with no poison, and Match permitted 10.0.1.5 in the union case. Strict
    `CompileConfig` and `configstore.CheckText` rejected every duplicate.
    Found the premise that "keep them as ordered policies" restores
    first-match is stale: the helper refuses two same-named rules in one
    context (`DuplicateRuleId`), and the #8752 fixture (permit then deny
    fragment) HAS conflicting actions, so neither issue fix option could
    be applied as written.
  - **File(s)**: none (probe deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Fold asks each statement's action by calling
    `compilePolicy`; a name merged into a permit after a deny/reject
    statement is reported, warned, and marked `LenientContentDropped` after
    the compile at both entry points. #9023 block sites pass `askNone9571`.
    Corrected the #3473 "first-match enforcement is still correct" comments
    and recorded the new cause on the flag.
  - **File(s)**: pkg/config/dup_instance_merge_8752.go,
    pkg/config/dup_block_merge_9023.go, pkg/config/compiler.go,
    pkg/config/compiler_uniformgates_policy.go, pkg/config/compiler_opts.go,
    pkg/config/compiler_security_policy.go, pkg/config/types_security.go,
    pkg/config/README.md
- **Timestamp**: 2026-09-10
  - **Action**: Tests in pkg/config (both lenient entry points, 6 widening
    rows, 8 accepting controls), the userspace wire, and the simulator.
  - **File(s)**: pkg/config/dup_policy_fold_widening_9571_test.go,
    pkg/dataplane/userspace/dup_policy_fold_widening_9571_test.go,
    pkg/policymatch/fold_widening_9571_test.go

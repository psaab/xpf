# #9527 — feeds: two direction-blind rationale statements about the hold-interval drop-to-empty

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified the chain at `ff4171851` before rewording:
    - recordFailure clears prefixes and hasSnapshot past the hold;
    - SnapshotForBindings omits a binding with an unready feed, and its comment
      names the hold-interval drop;
    - policies_lower.go returns false for a declared-but-omitted binding, so
      buildOneRuleSnapshot emits `__unsupported_address__`;
    - userspace-dp policy.rs preflight returns `UnrepresentableAddress`, and the
      previous-good snapshot is retained (fresh boot: default-deny).

    A referenced binding therefore never presents an empty set to enforcement.
  - **Action**: Corrected pkg/feeds/README.md. The drop fires onUpdate but does
    not make enforcement see an empty set. For a referenced binding, the whole
    snapshot is rejected, and the last-good prefixes stay enforced in either
    direction (denylist keeps denying, allowlist keeps permitting), for later
    commits too. Only the live show surface goes empty.
  - **Action**: Corrected the feeds.go onUpdate comment. "fail-CLOSED" and
    "strictly safer" were direction-blind, and the comment now states both
    directions. It no longer claims anything about what onUpdate returns; it
    names the manager's reject warning and ProcessStatus reason (#3261) as the
    signals. The issue's item 3, a per-binding fail mode, is a design change
    that is not taken here; the README says it is not implemented.
    Documentation/comment only; `go vet ./pkg/feeds/` is clean.
  - **File(s)**: pkg/feeds/README.md, pkg/feeds/feeds.go, docs/log/9527.md
- **Timestamp**: 2026-09-10
  - **Action**: Filed #9689 for the issue's optional item 3, a per-binding
    fail mode, so closing this documentation fix does not drop the design
    question. #9689 also records the operator-visible consequence the corrected
    text implies: while a referenced binding is unresolved, every later
    snapshot is rejected, so unrelated commits are not enforced until the feed
    recovers or the reference is removed.
  - **File(s)**: docs/log/9527.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9527.md

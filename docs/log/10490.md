# #10490 — showaudit name discovery misses quarantined-state builders

- **Timestamp**: 2026-09-22
  - **Action**: drove the issue from the plan rounds through implementation. The
    quarantine verdict was already the runtime SSOT, but its
    `QuarantinedZoneNames` spelling was outside the six predicate suffixes that
    `pkg/showaudit` discovers. The userspace builders could therefore remove a
    zone while the gate had no family row or renderer census.
  - **Evidence**: the pre-cutover non-test census was 6 direct calls in 5
    files: 3 userspace builders plus the CLI reverse map, daemon capture, and
    policymatch consumers. The gate-visible population was the 3 builder calls
    in 2 files. The second collection census found 55 functions in 34 files
    before the two existing completion exemptions, 53 functions in 32 files
    after those exemptions, and 52 initial unannotated entries after the
    permanent canary.
  - **Base**: implementation was rebased from `b71c52d60` onto
    `f4bae0a5a` (#10487). The v4 plan is `00afcf64d`.
  - **File(s)**: `docs/pr/10490-showaudit-discovery/plan.md`

- **Timestamp**: 2026-09-21 (plan round 1)
  - **Action**: committed `631e8a7c7` (`docs: add showaudit discovery plan
    (#10490)`) with the initial wrapper, family-row, census, and successor
    design.
  - **Review**: round-1 reviewers returned PLAN-NEEDS-MINOR and
    PLAN-NEEDS-MAJOR. The follow-up required the `ZoneIDs` collection, exact
    helper names, complete renderer census, structured-surface ownership, and
    #10490-first ordering.
  - **File(s)**: `docs/pr/10490-showaudit-discovery/plan.md`

- **Timestamp**: 2026-09-22 (plan rounds 2–4)
  - **Action**: rebased plan commit `e0a2a7926` recorded the exact 55/34 union,
    sorted 52-entry skeleton, `Successor: "#10489"`, and one permanent
    `showZonesDisplay` canary. Delta findings were folded in
    `8d62e1f99`: the B-A1 gate-blind SSOT hatch, the A3-(b) `(name, cfg)`
    renderer helper choice, and the metric omission rule. Parent applied the
    v4 cosmetic status-label correction in `00afcf64d`.
  - **Review**: Delta-3 confirmed the substantive fold as PLAN-READY. The
    two-lane contract remains #10490 first, with #10489 open until it drains
    the non-nil census; `GetZones` and `zonesHandler` stay initial Unannotated
    entries and receive the agreed exact no-wire exemptions in #10489.
  - **File(s)**: `docs/pr/10490-showaudit-discovery/plan.md`

- **Timestamp**: 2026-09-22 (implementation)
  - **Action**: committed `0ddaebe25` (`config: add zone quarantine reason
    helpers (#10490)`). Added `config.ZoneQuarantineExclusions(names)` as
    the accepted builder predicate spelling and
    `config.ZoneQuarantineExcludedReason(name, cfg)` as the shared renderer
    reason over the existing sorted-first collision SSOT. Tests cover the
    quarantined name, survivor, ordinary name, and nil config.
  - **Action**: committed `338bcdbc1` (`showaudit: register zone family row
    and canary (#10490)`). Switched exactly the 3 userspace builder calls,
    registered the `Security.Zones` plus `ZoneIDs` family with the sorted exact
    52-entry census and successor, and added the permanent local CLI canary.
    The canary adds an operator-visible `Quarantine:` line without changing
    existing status or gauge wording.
  - **Fold**: the registry and all three alias calls land in one gate-green
    commit; separating them would leave either an unregistered predicate or a
    stale row. Existing `QuarantinedZoneNames` consumers outside the builder
    remain unchanged. No wire schema or Rust behavior changed.
  - **File(s)**: `pkg/config/zoneid.go`, `pkg/config/zoneid_test.go`,
    `pkg/dataplane/userspace/zones.go`,
    `pkg/dataplane/userspace/zones_quarantine.go`,
    `pkg/showaudit/surface_gate_6534_test.go`,
    `pkg/cli/cli_show_security_zones.go`,
    `pkg/cli/cli_show_security_zones_quarantine_10490_test.go`

- **Validation**:
  - `GOCACHE=/dev/shm/gocache-10490 GOTMPDIR=/dev/shm go test ./pkg/config -count=1` — PASS.
  - `GOCACHE=/dev/shm/gocache-10490 GOTMPDIR=/dev/shm go test ./pkg/showaudit -count=1` — PASS; the `security_zone` census reports 53 render functions, 1 annotated canary, and 52 unannotated successor entries.
  - `GOCACHE=/dev/shm/gocache-10490 GOTMPDIR=/dev/shm go test ./pkg/cli -run TestShowZonesDisplayReportsQuarantinedZone10490 -count=1` — PASS.
  - `GOCACHE=/dev/shm/gocache-10490 GOTMPDIR=/dev/shm go test ./pkg/dataplane/userspace/... -count=1` — PASS.
  - `GOCACHE=/dev/shm/gocache-10490 GOTMPDIR=/dev/shm go test ./pkg/showaudit/... ./pkg/config/... ./pkg/cli/... ./pkg/dataplane/... ./pkg/grpcapi/... ./pkg/api/... -count=1` — PASS.
  - Firsthand builder RED-on-revert: temporarily reverted all 3 alias calls; `TestEveryBuilderDropPredicateIsRegistered6534` failed because no userspace file called `ZoneQuarantineExclusions`. Restoring all 3 made the test pass.
  - Firsthand canary RED-on-revert: temporarily removed the canary reason call; `TestSurfaceAnnotationCensusIsExact6534/security_zone` failed with all 53 render functions unannotated and named `pkg/cli/cli_show_security_zones.go:showZonesDisplay`. Restoring the call made the test pass.
  - Landing census after cutover: 4 old-name calls in 4 non-test files (3 non-builders plus the wrapper body), 3 builder-package-qualified alias calls in 2 userspace files, and 4 production alias calls in 3 files when the config reason helper is included. The registry and plan lists each contain 52 entries with no missing, extra, or duplicate key; only the registry list is lexicographically sorted.
  - `git diff --check` — PASS.

- **Outcome**: implementation branch `fix/10490-showaudit-discovery` is
  ready for the PR with `Closes #10490`; #10489 remains the live successor
  until its renderer census drains to nil.

# #9564 — scripts/mutate.sh: attribute a kill to the failure MESSAGE, not only the test name

- **Timestamp**: 2026-09-10
  - **Action**: Added mutation_target_output_has and mutation_verdict_for_message
    to scripts/mutate-lib.sh.
    - The expected text is matched against the jq-decoded `Output` events of the
      target test and its `Target/...` subtests only, so JSON escaping of `"` and
      `\` is handled, and a sibling test's message cannot satisfy the cell.
    - The decoded output is captured before matching: the selftest runs under
      pipefail, and `jq | grep -q` fails with SIGPIPE exactly when grep matches.
    - Verdicts: KILLED when the target's output contains the text; otherwise
      KILLED-WRONG-MSG. A non-KILLED verdict, and an empty column, are returned
      byte-identical.
  - **Action**: scripts/mutate.sh reads an optional 6th TAB column (`expect`)
    after the #8231 `target`. It refines an attributed go verdict with it,
    notes that the column is ignored without a target or -json stream, prints
    why a KILLED-WRONG-MSG cell failed, and counts `killed_wrong_msg` in the
    summary. The header documents both optional columns.
  - **Action**: scripts/mutate-selftest.sh gains the issue's five acceptance
    rows plus two refinement controls, over one fixture JSON stream:
    1. the target fails with the expected message: KILLED;
    2. a different message: KILLED-WRONG-MSG;
    3. the message only in another test's output: KILLED-WRONG-MSG;
    4. an empty column, where a non-KILLED verdict and a VOID stay unchanged
       (rows 4b and 4c are those two controls);
    5. a quoted message inside a subtest matches through JSON escaping.

    `make test-mutate-lib` is green. docs/engineering-style.md documents the
    column next to the #8231 5th column.
  - **File(s)**: scripts/mutate-lib.sh, scripts/mutate.sh,
    scripts/mutate-selftest.sh, docs/engineering-style.md, docs/log/9564.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation check of the new lib functions at 0eec8acb4, run
    against scripts/mutate-selftest.sh. The no-op run is CLEAN. Each mutant
    is scored KILLED only if the selftest exits non-zero AND the acceptance
    row that the mutant targets is among the FAIL lines.
    - L1, message ignored (`if mutation_target_output_has ...` becomes
      `if true`): KILLED. Row 2 fails.
    - L2, not scoped to the target (`select(.Test == $t ...)` becomes
      `select(true)`): KILLED. Row 3 fails.
    - L3, raw grep in place of the decoded match: KILLED. Row 5 fails.
  - **File(s)**: docs/log/9564.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9564.md

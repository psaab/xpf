# #9831 log

## A group's leaf zone is dropped by any inline leaf zone

- **Timestamp**: 2026-09-11
- **Action**: Measured at master `ed313e4c9` and again at `36dfa8e5a`, listing
  every compiled zone.
  - A group `security-zone zga;` plus an inline `security-zone trust;` compiled
    `[trust]`.
  - A group `security-zone [ zga zgb ];` plus an inline `security-zone trust;`
    compiled `[trust]`.
  - A group leaf `zga;` plus an inline braced `trust { tcp-rst; }` compiled
    `[trust zga]`.
  - A group braced `zga { tcp-rst; }` plus an inline leaf `trust;` compiled
    `[trust zga]`.
  - Strict commit accepted every row.
- **File(s)**: none (scratch probe)

- **Timestamp**: 2026-09-11
- **Action**: Cause. In the leaf branch of `mergeNodes`, `leafListPeer(*dst, key)`
  returns the first leaf, or single-key node, whose `Keys[0]` equals the
  keyword. An inline leaf naming a different zone therefore took the
  inline-wins override, and the group's zone was skipped.
- **Action**: Fix, general to named instances. A named instance is a container
  whose schema declares instance args (`groupNamedInstanceSchema9831`). For
  one, the inline peer is the node naming the same instance, keyword plus
  instance keys (`sameInstancePeer9831`).
  - A different instance is adopted.
  - A same-instance peer keeps the existing #7648 and override path.
  - A group list `security-zone [ zga zgb ];` is now adopted, and the #9656
    zone-statement check then refuses it on strict.
- **File(s)**: `pkg/config/group_merge_named_instance_9801.go` (new),
  `pkg/config/ast_groups.go`, `pkg/config/README.md`, `docs/config-schema.md`

- **Timestamp**: 2026-09-11
- **Action**: Tests, shared with #9801.
  `TestGroupLeafInstanceKeepsItsIdentity9831` covers:
  - a zone beside another zone's inline leaf;
  - the same zone with an inline braced body, where inline still wins;
  - a syslog host beside another host's inline leaf, at the AST level.

  `TestGroupMergeOfLeafZonesAtCommit9801` adds the configstore cells for the
  group zone and for the refused group list.
- **Validation so far**: see docs/log/9801.md.
- **File(s)**: `pkg/config/group_merge_named_instance_9801_9831_test.go`,
  `pkg/configstore/group_merge_named_instance_commit_9801_9831_test.go`

- **Timestamp**: 2026-09-11
- **Action**: A self-probe before review found a regression in the first
  identity rule (0195e18b4).
  - A group `security-zone [ zga zgb ];` beside an inline
    `security-zone zga { tcp-rst; }` committed there with zgb dropped.
    Master ad2ba883a refused it. The identity match found the braced zga, #7648
    had no body to expand, and the inline-wins override dropped the extra key.
  - A mistyped `security-zone trust scren edge;` beside `trust { }` behaved the
    same way.
- **Action**: Measured at ad2ba883a, written inline, to decide the rule.
  - Duplicate zone statements compile as one zone:
    `trust; trust apply-macro M;` commits, and so does `trust; trust;`.
  - #9656 refuses `zga; [ zga zgb ];` and `trust; trust scren edge;`.
  - `system syslog { host 10.0.0.1; host 10.0.0.1 any any; }` compiles two
    destinations for 10.0.0.1.
  - A group `host 10.0.0.1 any any;` beside an inline braced
    `host 10.0.0.1 { }` compiled two destinations. Beside an inline leaf
    `host 10.0.0.1;` it compiled one destination, without the group's
    facilities.
- **Action**: Fix. `sameInstancePeer9831` handles a group leaf that carries
  keys past the instance it names.
  - Against a braced peer, it merges through #7648 when that expands the
    statement; otherwise the leaf is adopted, as master did.
  - Against a leaf zone, the leaf is adopted, so #9656 judges it as inline
    text. This also fixes a list beside its own first member,
    `[ zga zgb ];` beside `zga;`, which dropped zgb silently at master.
  - Against any other leaf instance, the override stands, because adoption
    would compile a second instance (the syslog measurement).
  - `underZones9801` is shared with the #9801 wildcard scope.
- **Action**: Tests.
  - `TestGroupZoneStatementTailIsJudgedAsInline9831`: four refusals plus an
    `apply-macro` control.
  - `TestGroupPackedInstanceKeepsOneInstance9831`: one syslog destination
    beside a braced host, with the group's facilities, and beside a leaf host.
  - Controls, at ad2ba883a: the leaf-tail rows and the braced syslog row fail,
    and the braced-tail rows pass because master refuses them. At 0195e18b4:
    all four tail rows fail, and both syslog rows pass.
- **Residual (pre-existing)**: a group's packed `host 10.0.0.1 any any;`
  beside an inline leaf `host 10.0.0.1;` compiles the host without the group's
  facilities, as at master. Filed as #9855. Duplicate host statements compiling
  two destinations is filed as #9854; it is the #8436 class, at a site that
  census skips (`dupConservationSkipped8436`).
- **Validation**: `go test ./pkg/config/ ./pkg/configstore/` at the fixed tree
  ran 10306 tests with 0 failures.
- **File(s)**: `pkg/config/group_merge_named_instance_9801.go`,
  `pkg/config/ast_groups.go`,
  `pkg/config/group_merge_named_instance_9801_9831_test.go`,
  `pkg/config/README.md`, `docs/config-schema.md`

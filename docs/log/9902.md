# #9902: five pool-and-screen hardening residuals

## Brief + plan reviews (FULL tier: Codex + polite-2nd, 4 rounds)
- Brief DRAFT v1 → v2 (adjudicated) → v3 (F-026 v3) → v4 (F-026
  instance-identity). Round-1: Codex PLAN-NEEDS-MAJOR / polite NEEDS-MINOR.
  Round-2: F-023/F-098 NEEDS-MINOR-may-proceed, F-026 NEEDS-MAJOR (selector,
  freshness-publication, continuity), F-096/F-097 READY.
- Round-3 on F-026 v3: Codex R1/R2/R4 PASS, R3 FAIL with 2 genuine blockers
  (helper-restart token replay; reconcile-not-exclusive rebuild funnel) +
  polite READY. v4 replaced the (gen, reconcile_calls) proxy with
  (ProcGen, AllocatorID) instance identity.
- Round-4 on F-026 v4: Codex PLAN-READY + polite PLAN-READY (both
  verified: exactly 2 Shared constructors, Clone-over-Arc reuse,
  fetch_add-only counters, procGen spawn coverage; one nonblocking
  failed-reconcile wording correction adopted).
- Screen pact with Eng9901/Eng9901b: serialized screen/tests.rs
  (tail-only appends), messaged before rebase; no conflicts either way.

## Adjudicated corrections (all adopted)
- C1: exhaustion_total counts allocator fullness, never config-error/drift
  refusals ("allocator-REPORTED exhaustion events").
- C4: poisoned-rule (#9874) default-zero rows break first-wins → V3.1
  constructed-first selection (MaxTrackedFlows>0, tie→first, never sum);
  also fixes UsedPorts selection.
- F-023: SEPARATE active bucket (65536/s provisional policy, scope
  per-worker per-zone), prefilter-first, Invalid-indistinguishability
  documented; M23.3 prefilter-budget test.
- F-026: seq bumped INSIDE setLastStatusLocked (publication point, not
  post-request); baseline pruned incl. baseline-only records; clear =
  "no recently observed exhaustion"; NEXT_ALLOCATOR_ID starts at 1 (0 =
  legacy/unknown).
- F-097: doc says "import-time reservation release, adopted records
  survive"; guard prevents accidental untracked use (not a
  first-worker-frees fix); hot-path warning retained.
- F-098: no global CAS bound (1 load/occupied-word + 1 CAS uncontended);
  edge masks; honest concurrency (skipped-word races); test-only attempt
  accounting in the helper.

## Change
- F-096: ErrSessionWalkCancelled latched where a callback refuses a row
  (scan error wins ties); noteSessionScanError renders the partial-tally
  caveat; zero renderer churn. Cells: cancel-after-nonzero/v6/precedence
  + #7315 sentinel asserts + healthy goldens.
- F-097: release_synced_source_nat_allocation_untracked (rollback=false);
  session_import + tunnel_purge migrated (exhaustive); #[cfg(test)] on
  release_source_nat_allocation; 3 rustdoc links retargeted.
- F-098: AddressOccupancy::claim_in_block (edge masks, local-copy forward
  progress, both det arms); #9130 suite green.
- F-023: syn_cookie_active_ack_counter bucket + active-arm
  prefilter-then-limiter, over-limit → Invalid; standby byte-identical.
- F-026: (ProcGen, AllocatorID) exhaustion alarm — Rust id mint/echo
  (snapshot → status → wire), Go wire decode, constructed-first
  projection, lastStatusSeq freshness, monitor matrix (silent rebase on
  identity change; raise/refresh on true delta; 3-clean clear), dual
  prune sets, deterministic clear-then-mark + commit warning, render +
  daemon/CLI/gRPC wiring, README.
- STEP-0 RED pre-fix (each): wire-fixture missing-key; Go exhaustion
  matrix (no alarm existed); 65th-ACK validates; nil-walk renders
  authoritative; named entry E0425; naive-loop bound tests. Scratch
  proofs: allocator_id:0 echo-break reds 4/6 lifecycle cells (2 equality
  cells then hardened with nonzero pins); F-098 naive restore reds both
  bound tests; F-097 scratch-misuse E0425.

## Gates
- Rust: nat 529, screen 295, syn_cookie 99, ha 90, wire fixture green;
  non-test cargo check 0 errors; 9902-filter 18/18 5x. protocol:: 99/100
  — 1 PRE-EXISTING base failure (NeighborSnapshot literal ceiling 26 vs
  29; zero lane mentions; fails on pure master too).
- Go: natpoolalarm, natshow, userspace, daemon, config, cli, grpcapi
  green; named 5x per area; gofmt/vet clean. userspace post-rebase shows
  1 PRE-EXISTING master failure (TestContractSnapshotGolden9821, #9899
  drifted #9821's golden; fails on pure fcfdafcb5 too).
- In-band refusal → no publish/no bump is structural (poll loop applies
  only on err==nil; bump lives in the single publication funnel).

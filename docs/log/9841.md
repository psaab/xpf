# #9841 — interface MTU apply cannot read/write is only logged, commit reports success

- **Date**: 2026-09-14. Worktree `.claude/worktrees/9841-mtu-unconverged`,
  branch `fix/9841-mtu-unconverged`, base `origin/master 9603f2a5f`.
- **Defects** (STEP-0-confirmed present at base): `compiler_iface.go`
  phys setup silently skips the MTU write when the link lookup fails (the
  `nlErr == nil` guard has no log line of its own) and warn-and-continues
  on `LinkSetMTU` refusal; same shape in the #9757 VLAN-child path. The
  netdev stays at its old MTU while the apply returns success, and only
  the journal carries the warn lines — no commit warning, no show field.
  Body L9 STALE: #9761 is CLOSED and planner-only; the jumbo-child
  mechanism is kernel refusal per OPEN #9845.
- **Policy**: (b) keep applying + retry + surface. Fail-closed (a) is
  unsafe here: the child is written before the parent in the same
  `mapZoneInterface` call, so on a valid raise-both commit the kernel
  refusal is a NORMAL transient converging on the next commit — rejecting
  would strand it (deterministic in-call order retries identically), with
  a mirrored hazard lowering a parent below a live child. Severity LOW,
  no policy bypass; (b)'s explicit policy: persistent lookup/permission
  failures produce successful-but-warned commits; warnings advisory.
- **Design**: `MTUUnconverged{Name, ConfigRef, WantMTU, LiveMTU, Grade,
  Detail, ExpectIfindex, ExpectParentIfindex}` records keyed
  `(Name, ConfigRef)`; graded reasons (`lookup-failed`,
  `config-impossible`, `parent-write-pending`, `write-failed`,
  `reset-target-unresolved`, `blocked-by-child-live`); one
  identity-validated uncached retry per lookup site (retry-mismatch
  evicts in BOTH helpers — fetch memoizes before validation);
  fresh-verify-before-record + clear-on-LinkSetMTU-nil (cached-equality
  skip never clears, #8119); per-apply carry via
  `ApplyResult.UnconvergedMTUs` (Clone-based, sorted choke, no
  json/proto tags, arm-proof-ignored). Commit warnings sync ONLY on
  operator-commit applies (daemon wrapper at `applyAndSyncCommitted` +
  CLI `commitApply` both paths) iff the apply succeeded AND the
  last-apply generation advanced; background applies never write. Show:
  ConfigRef-exact-first/kernel-fallback matching, one fresh observation
  driving display + suppress/annotate, filter-aware leftover section, no
  row consumed twice. #9845 owns `markHostMutated` + ordering; terse /
  detail / extensive variants and proto changes out of scope.
- **Plan review**: brief + 2 hostile rounds × Codex + GLM-5.3, raw
  outputs in `docs/pr/9841/reviews/` (uncommitted). R1 both
  APPROVE-WITH-CHANGES: sustained policy (b), redesigned warning
  ownership by construction (sole-writer, generation guard, no mutex)
  and ConfigRef show matching. R2 (delta-only) both
  APPROVE-WITH-CHANGES, architecture holds: (Name, ConfigRef) keys,
  exclusive per-row claim, leftover visibility
  `HasPrefix(ConfigRef) OR HasPrefix(Name, LinuxIfName(filter))`,
  identity-validated suppression, ownership/aliasing/generation pins,
  grading precedence, `-1`-never-renders. Budget 2/2 consumed;
  implemented without further rounds.
- **Commits** (5 code, each green): `2f34ecbe0` dataplane record +
  graded retry; `07c02a779` daemon commit-warning sync;
  `0f506bf3a` show annotations (local CLI + grpcapi text twin, CLI
  commit sync, `LastApplyGenerationOf`, helper cells, #1451 canary +
  docs-table update for the 4 new `pkg/dataplane` importers);
  `e8634a89a` dataplane review fixes (retry eviction, grading
  precedence, authored ConfigRef, converged-repeat clear);
  `eb9a122b5` response-copy projection (daemon wrapper + CLI
  `commitApply` return the response object; RETH member fallback in
  both renderers).
- **Post-PR review** (GPT NEEDS-MAJOR 5 findings + GLM NEEDS-MINOR,
  all folded): GPT-1 warning race — the sole-writer story was false
  (snapshot retains the applied pointer; status/control readers observe
  it under the userspace mutex), fixed by projecting onto a
  response-only copy (`WithMTUWarningsForResponse9841`; wrapper and
  `commitApply` return the response object, applied input never
  mutated); GPT-2 by-name eviction removed `attrs.Name` instead of the
  requested key (wrong-named stranger stayed cached), fixed to evict
  precisely the populated keys both directions + false comment
  corrected; GPT-3 ConfigRef rebuilt via Itoa (aliases like `reth0.080`
  missed their row), fixed by threading the authored `ifaceRef`
  through + resolving the RETH fallback through the member in both
  renderers; GPT-4 fresh-verified convergence on a repeat left the
  earlier record stale, fixed by clearing the composite key (cached
  skips still never clear); GPT-5 grading judged live-fit before
  known-plan impossibility (contradicting its own precedence doc),
  fixed by evaluating impossibility first + pin flipped; GLM-F2 CLI
  hybrid safety comment overstated (same retained-pointer race),
  replaced with the copy contract (commits listed above). New pins:
  aliased-serialization race hammer (proven to trip write-through),
  wrong-name squatter row + subsequent-access cell (both proven
  pre-fix-red), repeat-converged clear (proven pre-fix-red),
  authored-ref + member-fallback cells in both renderers,
  response-copy identity/aliasing cells.
- **Verification**: new cells green — dataplane +3
  (ObserveLive/AnnotateRow/RenderLeftovers), CLI +7 (lo-phys annotate,
  converged-suppress, lo.50 unknown, leftover + error-path, reth +
  member-filter, identity-mismatch-kept, commitApply legacy+hybrid ×
  fire/stale arms), grpcapi +3 (twin annotate, twin leftover, commit
  marker-survival); full `pkg/dataplane` (21s) + `pkg/daemon` (87s) +
  `pkg/cli` (7s) + `pkg/grpcapi` (15s) suites green; 5× named runs
  green in all 4 packages plus 5× `-race` hammer green; vet + gofmt
  clean. No cluster (seam-driven cells + loopback; no live cells).
  Post-review-fold re-gate: dataplane +8 (squatter row, eviction,
  repeat-clear, phys clear/keep, authored ref, response copy, grade
  pin flip), daemon hammer reworked + aliased hammer added, CLI +3
  (alias, member-fallback, runCommit response), grpcapi +2 (alias,
  member-fallback twins); full suites green again (dp 30s, daemon
  93s, cli 7s, grpcapi 15s); 5× named + 5× `-race` green all 4
  packages; new pins mutation-proven (aliased hammer, squatter row +
  eviction cell, repeat-clear, authored ref all fail pre-fix).
- **Residuals** (documented, not denied): failed-syscall-with-converged-
  end-state stays journal-only; write→clear TOCTOU vs an external
  reverter uncloseable without read-back; rename-between-apply-and-show
  surfaces as leftover; structured `GetInterfaces` carries no MTU
  (text-only contract); #9845 owns the in-call order + host-mutation
  mark.

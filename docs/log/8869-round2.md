# 8869 — GEMINI-049 round-two adjudication

Research record only. No production code changed in this round.

- **Checked HEAD:** `43e7038a1` (`origin/master`)
- **Report base:** `b0f3aba21`
- **Source read:** exact report body `/tmp/gemini-review-049.md` (6,841 lines), structured copy `/tmp/research-work.wbojXp0Wtq/f049.json`, all 29 comments on #8869, prior `docs/log/8869.md`, and the five plan files on `origin/research/8869-gemini-049`.
- **Prior state:** 17 of 56 High body findings adjudicated; 39 were not reached.
- **Round limit:** 15 findings, reached; no mechanical dismissals.
- **Order:** unchanged #011 first; then wire/dataplane correctness (`006,013,014,015,024,025,026,030,096`); then control plane (`033,034,035,047,049`).

## Verdict table

| Finding | Base validity at `b0f3aba21` | Current mechanism/consequence at `43e7038a1` | Disposition | Filing |
|---|---|---|---|---|
| 011 MQFQ stale tail/vtime | INVALID: pop-side max, drain reset, and frontier re-anchor were already present | Mechanism and consequence refuted; `pop.rs:180,251-252,268-276`, `accounting.rs:61-67,119-129`, in-tree ordering/rollback cells | Wrong when written | None |
| 006 SYN-cookie ACK RST | Valid code description | `tcp.rs:502-515` still emits `seq=ack`, `ack=seq+1`, `RST|ACK`; protocol discrepancy confirmed, Linux stall/drop consequence unresolved without peer capture | Live but unresolved impact | None pending packet proof |
| 013 IPv4 UDP zero checksum | Helper-level omission valid | Helper lacks guard (`checksum.rs:394-421`), but every live UDP caller guards (`frame/mod.rs:1341-1354,1728-1730`; `_words:479-481`); production wire consequence not reached | Not reached for reported wire defect | None |
| 014 CoS lease false sharing | Valid layout claim | `SharedCoSLeaseState` fields share one line (`lease.rs:74-79`); updates are watermark/drain-loop gated, not per packet; impact unmeasured | Performance candidate; impact unresolved | None pending many-core measurement |
| 015 DNAT steering mutex | Valid registry shape | Global mutex/vector exists (`checksum.rs:280-282`), but only session install/close beside BPF syscalls (`:406,525,553,600`); claimed fast-path/CPS consequence refuted | Not a High dataplane defect | None |
| 024 NAT64 fragment ID truncation | Code description true; defect contract invalid | Low-16 copy is required by RFC 7915 §5.1; casts at `nat64.rs:2483-2492,3282-3290,3858-3863` are conformant; post-translation overlap guard is the relevant defense | Wrong contract; consequence refuted | None |
| 025 port recycle/TIME_WAIT | No timestamp/hold mechanism confirmed | FIFO ring has no elapsed-time gate (`allocator.rs:804-805,1042-1189`), but RFC 5382/6888 permit immediate reuse in tracked/filtering cases; tuple collision not proven | Deferred, not dismissed | None pending churn/peer proof |
| 026 address-only/PAT collision | Valid | Shared allocator key omits `no_translation` (`nat/source/mod.rs:398-405`); address-only owns no PAT bit (`allocator.rs:2516-2525,3722-3729`); same-allocator peer guard skips it (`overlap.rs:156-163`); simultaneous duplicate reverse tuple is fail-open | Confirmed live structural defect | [#10190](https://github.com/psaab/xpf/issues/10190) |
| 030 embedded ICMP type/checksum | Invalid at base: base already mapped Type 8→128 (`b0f3aba21:userspace-dp/src/nat64.rs:3281-3285`) | HEAD retains type mapping; quoted inner checksum is intentionally informational and outer checksum is recomputed | Wrong when written; consequence refuted | None |
| 096 NAT64 total length narrowing | Valid | Outer/non-first `20+l4_len as u16` remains (`nat64.rs:2517-2518,3858-3859`); embedded arm is bounded by `MAX_EMBEDDED_LEN=1300`; valid IPv6 payload 65516..65535 would wrap an illegal IPv4 header when an unusually large ingress MTU/GRO buffer delivers it | Confirmed, constrained reachability | [#10191](https://github.com/psaab/xpf/issues/10191) |
| 033 family compound-key elision | Valid | Fixed by compound-key descent/scope chain (`compact_normalize_8662.go:86-110,139-140,239-240`; `compact_normalize_scope.go:605-612`) | Fixed since | None |
| 034 DNAT port range | Wrong at base: strict gate already iterated `DestinationPorts` and rejected `<1`/`>65535` (`b0f3aba21:pkg/config/compiler_validate_strict_nat.go:413-424`) | Parser still form-only by design; gate and downstream fail-closed builders prevent wrap | Wrong when written | None |
| 035 GRE key/TTL narrowing | Valid at base | Shared bounded parser and both interface/unit gates (`compiler_interfaces.go:27-64,270-298,424-443`) prevent wrap; `fcfdafcb5` | Fixed since | None |
| 047 confirm tombstone durability | Valid double-failure residual at base: tombstone write is best-effort, then delete fails; retry re-drives delete only | Same narrow residual remains (`store_commit.go:673-750` at base; `:792-815,845-864` at HEAD); ordinary delete failure is tombstoned, simultaneous write+delete failure can still resurrect | Known residual; duplicate | Existing #5915/#8565/#6739 chain; no new filing |
| 049 SNMP EngineID hash (Medium) | Hash omission true; stale-key consequence invalid | EngineID immutable per agent lifetime (`snmp/agent.go:377-393,413-418,517-527`); keys re-localize against same ID (`snmp/v3.go:57-73,105-133`) | Medium consequence refuted; not in High denominator | None |

## Filings

Only two rows met the filing bar after mechanism/consequence separation and
existing-issue deduplication:

1. **#10190** — same-allocator address-only/PAT reverse-tuple collision
   (GEMINI-049-026). Distinct from #5269/#5341's address-only-only cases and
   #8115's cross-allocator guard.
2. **#10191** — NAT64 IPv6-to-IPv4 total-length narrowing above 65515 bytes
   (GEMINI-049-096). The embedded quote arm is bounded; outer/non-first arms
   remain structurally unsafe for an unusually large ingress MTU/GRO buffer.

No issue was filed for rows whose operational consequence remained unresolved
(#006, #014, #025), or whose current call graph refuted the advertised impact
(#013, #015, #024, #030, #033, #034, #035, #049). #047 is a live but
narrowly conditioned residual already recorded by #5915/#8565/#6739, not a
fixed-since row and not a duplicate filing.

## Counts and next start

- **This round:** 15 rows (**14 High + 1 Medium**).
- **Cumulative:** **31 of 56 High** body findings adjudicated; **25 High not reached**; one Medium also adjudicated.
- **Mechanical dismissals:** 0.
- **Next start:** `GEMINI-049-055`, then `GEMINI-049-057`, continuing the
  consequence-ordered control-plane queue. The remainder is not clean by
  default; unresolved rows retain their measurement conditions.

## Validation

Documentation-only validation: exact source reads, base-vs-HEAD code reads,
issue duplicate searches, and `git diff --check`. No production test suite is
applicable because this round changes no code.

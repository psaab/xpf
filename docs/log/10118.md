# Issue #10118: WireGuard interop reached gate

- **Change**: Added `make test-wg-interop`, wrapping `test/incus/wg-interop.sh all` in `harness-result.sh` with the shared loss-userspace-cluster environment and `smoke-cells` adapter.
- **Census**: Removed `test/incus/wg-interop.sh` from `test/incus/HARNESSES.unreached` (shrink-only). Added the still-unmeasured ledger-coverage declaration for `test-wg-interop`; it must be removed after the first measured PASS/FAIL row.
- **Envelope pin**: `wg-interop.sh` now emits the canonical `<n> passed, <n> failed` summary. `harness-result-selftest.sh` includes it in the declared/discovered adapter census and Makefile mapping (13 gates total, 5 `ha-smoke` + 8 `smoke-cells`).

## RED -> GREEN proof

- **RED — unwired selftest**: Before adding the summary and Makefile recipe, `bash test/incus/harness-result-selftest.sh` reported `162 passed, 4 failed`: `wg-interop.sh has 0 summary echo lines`, only `12 of 13` declared gates covered, the discovered set differed from the declaration, and `wg-interop` had `no-makefile-wrap`.
- **RED — census**: With the final `HARNESSES.unreached` shrink retained but the recipe's `wg-interop.sh all` invocation removed in a temporary Makefile copy, `sh scripts/harness-census.sh` reported `UNREACHED and undeclared ... test/incus/wg-interop.sh`, `25 reached, 31 declared unreached`, and exited 1.
- **GREEN — envelope selftest**: `make test-harness-ledger-lib` completed the full harness-result selftest with `165 passed, 0 failed`; its adapter census reported all 13 declared gates and the discovered set equal to the declaration.
- **GREEN — reachability**: `make harness-census` passed: `57 runnable harnesses -- 26 reached, 31 declared unreached`.
- **GREEN — census selftest**: `make test-harness-census-lib` passed all `27` cells (`passed=27 failed=0`), including the real-tree reachability cell.
- **GREEN — ledger coverage**: `make harness-coverage` passed: `ledger-coverage: 7 of 22 wrapped gates measured`; `test-wg-interop` is explicitly reported `ZERO ROWS ... [declared: still unreached]` until a live measured run is recorded.
- **GREEN — dry-run wiring**: `make -n test-wg-interop` rendered:

  ```text
  BPFRX_CLUSTER_ENV=test/incus/loss-userspace-cluster.env ./test/incus/harness-result.sh run \
      --gate test-wg-interop --adapter smoke-cells --env loss-userspace-cluster --cluster \
      -- ./test/incus/wg-interop.sh all
  ```

- **Syntax/whitespace**: `bash -n test/incus/harness-result-selftest.sh test/incus/wg-interop.sh` and `git diff --check` passed.
- **Hermetic helper smoke**: A throwaway source of the helper prologue exercised `pass`, `warn`, `fail`, and `summary`: success exited 0 with `WireGuard interop: 1 passed, 0 failed`; failure exited 1 with `WireGuard interop: 1 passed, 1 failed`; `WARN: advisory` was present.

- **GREEN — full ledger module**: `python3 -m unittest discover -s test/incus -p ledger_compare_test.py` ran `75` tests and passed.

## Live evidence status

- **Attestation**: Read-only `inc exec` showed fw0 and fw1 both running `/var/lib/xpf/versions/working-15440-ga36ba20ac-dirty/xpfd` with SHA-256 `81fd76ae464ea31c64bb6fb4b673128b9230d7afe8e9cf992bc250002f74542c`. That executable was copied read-only to a temporary local path for the wrapper's build-executable attestation.
- **Live preflight**: `make test-wg-interop` was run with that exact temporary executable and a temporary ledger. The harness-result envelope recorded `verdict=FAIL`, `cells_passed=0`, `cells_failed=1`, `exe_check=MATCH`, and both node readbacks matched the same SHA. The harness stopped in P0 before provisioning the peer because the deployed build identity was `g3a1c5b113`, while this branch is `beb14e8725eb97404a7e5cfdc30ee3dad600a416`; the command printed `deployed build ... != this branch ...` and GNU Make exited 2.
- **Postcondition**: Read-only checks after the refusal found no `xpf-wg-peer` row and no `wg0`/`wg1` device on either firewall.
- **Missing live evidence**: No peer was provisioned and no independent-kernel handshake, transport, rekey, or green ledger row exists yet. A clean run requires deploying/building this branch on both loss nodes, then rerunning `make test-wg-interop`; after its first measured PASS/FAIL row is intentionally accepted, remove the `test-wg-interop` declaration from `test/incus/LEDGER_COVERAGE.unreached`.

# #9792 part 1 — the #9156 leaf-run gate's ten skipped rows

- **Timestamp**: 2026-09-11
- **Action**: give the #9156 gate fixtures for the 10 containers it could not compare, record each verdict, and keep part 2 open.
- **File(s)**: `pkg/config/leaf_run_fixtures_9792_test.go` (new), `pkg/config/leaf_run_differential_gate_9156_test.go`, `pkg/config/leaf_run_gate_helpers_9156_test.go`, `docs/log/9792.md`.

## Why each row was skipped (measured)

A throwaway probe compiled each skipped row's separate-lines oracle and logged the rejection:

| row | rejection |
|---|---|
| `class-of-service fairness rss-expectation interface <i> queue <q>` | `expected queue 0..255` (instance name placeholder) |
| `firewall three-color-policer <p> single-rate` / `two-rate` | `requires positive committed-information-rate` (placeholder rates) |
| `security policies from-zone ... policy <p>` / `global policy <p>` | `references undefined scheduler "xpfaaa"` |
| `services ip-monitoring ... preferred-route route <r>` (and the routing-instance form) | `match rpm-probe is required` |
| `services rpm probe <p> test <t>` | `target is required` |
| `system [dataplane-type -> domain-name]` | `unknown dataplane-type "xpfaaa"` |
| `system login class <c> [allow-commands -> allow-commands-regexps]` | no synthesizable value (a regex) |

## Change

- `leaf_run_fixtures_9792_test.go` holds a `leafRunFixture9156` for each row: typed instance names, accepted values, context, #8690's `preambleFor` for schedulers, and one combined `services` block for ip-monitoring. A policy split across a preamble and the site's own block compiled with no match, and the routing-instance form also needs its instance defined.
- The gate applies a fixture on the braced arm, as the flat-set arm's base text, and on the strict check. `gateCompileFlatSet9156` takes the base text and `strictAdmitsLeafRun9156` takes the run text. The zero fixture renders the text the gate built before, so rows without one are unchanged.
- **Guards:**
  - `TestLeafRunFixturesNameEnumeratedSites9792`: a fixture must name an enumerated site;
  - `checkLeafRunFixtureOutcomes9792`, called by the gate: a fixture row back in a skip bucket fails, and so does a recorded one-line refusal (`runRefused`) that stops being true, in either direction.

## Verdicts

The gate now reports `106 containers enumerated; 104 COMPARED (80 agree, 24 differ); 2 skipped`; before, it compared 96 and skipped 10.

- **Agree (3):** rss-expectation, rpm probe test, login class.
- **Differ (5), all `{lenient-only}`:**
  - both `scheduler-name` rows;
  - both ip-monitoring `preferred-metric` rows;
  - `system dataplane-type`.
  All five are `{braced+flat}`. The strict walk refuses the one-line spelling, so none is reachable by a commit. Added to `leafRunKnownDiffer9156`.
- **Refused on one line (2):** both three-color-policer rows. With valid rates the oracle compiles, but the one-line run drops `committed-information-rate` and the policer is refused. That is loud, not silent, and it is recorded in `runRefused`.
- **Operator-reachable: none**, so part 1 needs no product fix.

## Part 2, still open

24 lenient-only rows: the original 19 plus the 5 above. Each is to be fixed, or accepted with a measured reason. Not in this change.

## Validation

- `go test ./pkg/config/`: 9327 passed, 0 failed.
- gofmt and go vet are clean, and the refactoraudit gate is green.
- **Mutation matrix:** 3/3 killed (below).

## Mutation matrix

Run at 2e035fc40 by `mut9792.py`: `go test -json -run 'LeafRun|9156|9792'` over pkg/config, scored by `Action=="fail"` on the named top-level test. The no-op run was CLEAN, and each mutant was restored with `git checkout HEAD --`.

| mutant | verdict | target |
|---|---|---|
| M1-fixture-lookup-removed | KILLED | `TestLeafRunDifferentialGate9156` |
| M2-flat-arm-ignores-fixture | KILLED | `TestLeafRunDifferentialGate9156` |
| M3-render-ignores-preamble | KILLED | `TestLeafRunDifferentialGate9156` |

Not bound by a cell: the strict check's use of the fixture. Every fixture row is lenient-only with or without its preamble, so no row can show the difference.
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    63b6e3481. That tree is origin/master 0be3e6e81 merged with 5 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9687, #9785, #9792, #9793, #9807.
    - Go leg: 78 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5877 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9792.md

# #9792 part 1 — the #9156 leaf-run gate's ten skipped rows

- **Timestamp**: 2026-09-11
- **Action**: give the #9156 gate fixtures for the 10 containers it could not compare, record each verdict, and keep part 2 open.
- **File(s)**: `pkg/config/leaf_run_fixtures_9792_test.go` (new), `pkg/config/leaf_run_differential_gate_9156_test.go`, `pkg/config/leaf_run_gate_helpers_9156_test.go`, `docs/log/9792.md`.

## Why each row was skipped (measured)

A throwaway probe compiled each skipped row's separate-lines oracle and logged the rejection:

| row | rejection |
|---|---|
| `class-of-service fairness rss-expectation interface <i> queue <q>` | `expected queue 0..255` (instance name placeholder) |
| `firewall three-color-policer <p> single-rate` / `two-rate` | `requires positive committed-information-rate` (placeholder rates) |
| `security policies from-zone ... policy <p>` / `global policy <p>` | `references undefined scheduler "xpfaaa"` |
| `services ip-monitoring ... preferred-route route <r>` (and the routing-instance form) | `match rpm-probe is required` |
| `services rpm probe <p> test <t>` | `target is required` |
| `system [dataplane-type -> domain-name]` | `unknown dataplane-type "xpfaaa"` |
| `system login class <c> [allow-commands -> allow-commands-regexps]` | no synthesizable value (a regex) |

## Change

- `leaf_run_fixtures_9792_test.go` holds a `leafRunFixture9156` for each row: typed instance names, accepted values, context, #8690's `preambleFor` for schedulers, and one combined `services` block for ip-monitoring. A policy split across a preamble and the site's own block compiled with no match, and the routing-instance form also needs its instance defined.
- The gate applies a fixture on the braced arm, as the flat-set arm's base text, and on the strict check. `gateCompileFlatSet9156` takes the base text and `strictAdmitsLeafRun9156` takes the run text. The zero fixture renders the text the gate built before, so rows without one are unchanged.
- **Guards:**
  - `TestLeafRunFixturesNameEnumeratedSites9792`: a fixture must name an enumerated site;
  - `checkLeafRunFixtureOutcomes9792`, called by the gate: a fixture row back in a skip bucket fails, and so does a recorded one-line refusal (`runRefused`) that stops being true, in either direction.

## Verdicts

The gate now reports `106 containers enumerated; 104 COMPARED (80 agree, 24 differ); 2 skipped`; before, it compared 96 and skipped 10.

- **Agree (3):** rss-expectation, rpm probe test, login class.
- **Differ (5), all `{lenient-only}`:**
  - both `scheduler-name` rows;
  - both ip-monitoring `preferred-metric` rows;
  - `system dataplane-type`.
  All five are `{braced+flat}`. The strict walk refuses the one-line spelling, so none is reachable by a commit. Added to `leafRunKnownDiffer9156`.
- **Refused on one line (2):** both three-color-policer rows. With valid rates the oracle compiles, but the one-line run drops `committed-information-rate` and the policer is refused. That is loud, not silent, and it is recorded in `runRefused`.
- **Operator-reachable: none**, so part 1 needs no product fix.

## Part 2, still open

24 lenient-only rows: the original 19 plus the 5 above. Each is to be fixed, or accepted with a measured reason. Not in this change.

## Validation

- `go test ./pkg/config/`: 9327 passed, 0 failed.
- gofmt and go vet are clean, and the refactoraudit gate is green.
- **Mutation matrix:** 3/3 killed (below).

## Mutation matrix

Run at 2e035fc40 by `mut9792.py`: `go test -json -run 'LeafRun|9156|9792'` over pkg/config, scored by `Action=="fail"` on the named top-level test. The no-op run was CLEAN, and each mutant was restored with `git checkout HEAD --`.

| mutant | verdict | target |
|---|---|---|
| M1-fixture-lookup-removed | KILLED | `TestLeafRunDifferentialGate9156` |
| M2-flat-arm-ignores-fixture | KILLED | `TestLeafRunDifferentialGate9156` |
| M3-render-ignores-preamble | KILLED | `TestLeafRunDifferentialGate9156` |

Not bound by a cell: the strict check's use of the fixture. Every fixture row is lenient-only with or without its preamble, so no row can show the difference.
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    63b6e3481. That tree is origin/master 0be3e6e81 merged with 5 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9687, #9785, #9792, #9793, #9807.
    - Go leg: 78 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5877 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9792.md

## Part 2: the 24 lenient-only rows

- **Timestamp**: 2026-09-11T18:40Z
- **Action**: Applied #9235's lenient-path remedy at the compile reader of every
  container on #9792's list. The remedy is `hoistAndSplitRun8939`, reached
  through `expandRun9235` / `expandRunChildren9235`, and each container is
  resolved out of `setSchema`. The gate went from 24 differ to 0:
  - 8 security rows: the screen tcp syn-flood and limit-session readers, the
    security-zone reader, both policy rows, nat64 rule-set, dynamic-address
    feed-server, and flow traceoptions packet-filter.
  - 16 more rows: bridge-domain, the class-of-service interface body, the
    sampling flow-server for inet and inet6, the interface
    [bandwidth -> description] reader, gigether-options,
    aggregated-ether-options, the tunnel wireguard loop and its peer loop, the
    BGP neighbor merge for the default instance and for routing instances, the
    routing-instance body, both ip-monitoring preferred-route rows, the system
    body, and the NTP server modifiers.
  - Each row left `leafRunKnownDiffer9156` in this change, because the gate's
    ratchet requires it.
- **Action**: The whole-body expansion was wrong for the routing-instance
  reader, and it was measured.
  - It hoisted the `description` at the end of the flat-set chain
    `protocols > bgp > group g > authentication-key k > description d` up to
    the instance.
  - That added two NEW flat-set rows for routing-instance BGP group and
    neighbor. With that one expansion reverted, the gate read 104 agree and 1
    differ.
  - The #9234 bound did not stop it, because the leaf directly above the
    `description`, `authentication-key`, does not declare it.
  - `expandLeafHeadedRuns9792` expands only runs whose head is one of the
    container's own leaves. The routing-instance, system and interface readers
    use it, since all three hold deep subtrees. The gate then read 105 agree
    and 0 differ.
- **Action**: Named containers (`interfaces <if>`, `routing-instances <ri>`,
  `bridge-domains <bd>`) declare their body as a wildcard, so each resolver
  passes a placeholder name.
  - The first resolvers stepped one level too high:
    `("interfaces", "tunnel", "wireguard")` resolved to nil.
  - `TestFlatRunResidueSchemasResolve9792` now asserts a DECLARED child rather
    than `resolveSchemaChild`, which a wildcard answers for any keyword.
- **File(s)**:
  - `pkg/config/flat_run_residue_9792.go` (new: resolvers and the leaf-headed
    expander) and `pkg/config/flat_run_residue_9792_test.go` (new).
  - The readers in `compiler_security_screen.go`,
    `compiler_security_zones.go`, `compiler_security_policy.go`,
    `compiler_nat_source.go`, `compiler_security_flow.go`,
    `compiler_services.go`, `compiler_class_of_service.go`,
    `compiler_interfaces.go`, `compiler_bgp_neighbor_merge_9192.go`,
    `compiler_routing.go` and `compiler_system.go`.
  - The register in `leaf_run_differential_gate_9156_test.go`.
- **Docs**: `pkg/config/README.md` has no flat-run or leaf-run section. The
  contract lives beside the code: the gate file's register comment, and the
  #9235 and #9792 residue files, which name the channel, the remedy and the
  bound. So no README change is needed.
- **Action**: Guarded every #9792 expansion (`expandResolvingRuns9792` / `expandResolvingRun9792`, `packedOrNestedLeafRun9792`).
  - An unguarded `hoistAndSplitRun8939` broke six existing cells. The two
    system-config cells lost `processes { utmd disable; }`: `processes`
    declares no schema children and was taken for a nested run. Two screen
    cells failed: an unknown trailing token was hoisted, changing #3332's
    diagnostic and #8321's single unknown-leaf record. The routing-instance
    BGP `description` was hoisted too. And three NTP spellings dropped on the
    #2419 gate.
  - A child now expands only when its head is a declared, value-bearing,
    single-valued leaf, and every statement in its run is a declared leaf of
    the container. `TestPackedOrNestedLeafRunGuard9792` drives each shape.
- **Action**: `TestLeafRunRegisterIsNotVacuous9156` asserted that the register
  is non-empty. That premise was removed deliberately, as its failure text
  prescribes, because every row is fixed.
  - The "consulted" property now uses a fabricated registered row for the
    duration of the cell.
  - `TestLeafRunDifferentialGateDiscriminates9156` still proves the gate finds
    a differing container.
- **Action**: Regenerated `testdata/flat_set_chain_losers_8939.txt`
  (`UPDATE_8939=1`). The loser list is unchanged. walked went 109 -> 111 and
  unmeasured 78 -> 76.
  - Measured per pair against the merge base, the two pairs are
    `security dynamic-address feed-server <s> hold-interval 86400 hostname
    <h>`, with and without `update-interval`. Their packed compile used to fail
    validation, because the chain lost `hostname`; it now reads it.
  - The schema gate still refuses that spelling at commit, so commit
    acceptance is unchanged.
  - The security-zone `[description -> screen]` pairs stay unmeasured. Their
    packed compile now errors on the undefined screen, exactly as the split
    form already did.

## Validation (part 2)

- `go test ./pkg/config/ ./pkg/configstore/`: 10317 passed, 0 failed. `TestLeafRunDifferentialGate9156` passed with 105 compared, 105 agree, 0 differ.
- gofmt, go vet and the refactoraudit gate are clean.
- **Mutation matrix:** 26/26 killed (below).

## Mutation matrix

Run by `mut9792.py`: `go test -json` over pkg/config on the gate, resolve, register and guard cells, scored by `Action=="fail"` on each mutant's named top-level test. Both no-op runs were CLEAN, and each mutant was restored with `git checkout HEAD --`.

The first run at b2b06e767 killed 24 of 26. M22 (the guard accepts a valueless head) and M23 (the guard accepts a container, wildcard or multi head) ESCAPED. Every real-schema row of `TestPackedOrNestedLeafRunGuard9792` that rejects a head shape was also rejected by a later check: a valueless real head is a container too, and a real container head has an undeclared tail. So deleting either shape arm changed no verdict. `guardSchema9792` adds synthetic rows that give each shape a declared tail, plus a positive control row with the same tail that must be accepted. M22 and M23 were re-run at 1357de897 and both were KILLED. The suite count above is from b2b06e767, before those rows; the integration `make test` covers the final head.

| mutant | verdict | target | run at |
|---|---|---|---|
| M1-syn-flood-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M2-limit-session-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M3-security-zone-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M4-policy-leaves-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M5-nat64-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M6-feed-server-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M7-packet-filter-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M8-bridge-domain-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M9-cos-interface-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M10-flow-server-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M11-interface-leaves-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M12-gigether-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M13-aggregated-ether-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M14-wireguard-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M15-wireguard-peer-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M16-bgp-neighbor-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M17-routing-instance-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M18-preferred-route-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M19-system-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M20-ntp-server-unexpanded | KILLED | `TestLeafRunDifferentialGate9156` | b2b06e767 |
| M21-security-zone-resolver-nil | KILLED | `TestFlatRunResidueSchemasResolve9792` | b2b06e767 |
| M22-guard-accepts-valueless-head | KILLED | `TestPackedOrNestedLeafRunGuard9792` | 1357de897 |
| M23-guard-accepts-container-head | KILLED | `TestPackedOrNestedLeafRunGuard9792` | 1357de897 |
| M24-guard-splits-undeclared-packed-tail | KILLED | `TestPackedOrNestedLeafRunGuard9792` | b2b06e767 |
| M25-guard-ignores-nested-tail | KILLED | `TestPackedOrNestedLeafRunGuard9792` | b2b06e767 |
| M26-guard-checks-only-first-level | KILLED | `TestPackedOrNestedLeafRunGuard9792` | b2b06e767 |

## Integration batch 22

- `make test` on the batch 22 integration tree failed one cell: `pkg/grpcapi` `TestShowTextGolden`, topic dynamic-address.
- **Cause.** The fixture configures `security dynamic-address feed-server office url http://example.com/feed feed-name blocklist`, but the golden recorded the show output without `Feed name: blocklist`, i.e. with the configured value lost. Now that the feed-server reader is expanded, the value is read and shown.
- **Fix.** Regenerated the golden with `UPDATE_SHOW_GOLDEN=1`. The JSON diff is one line and adds exactly `Feed name: blocklist`. `go test ./pkg/grpcapi/` passes.
- **Why the chain missed it.** `pkg/grpcapi` was not in this change's chain suites (`pkg/config`, `pkg/configstore`); only the integration run covers it.
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    766615de4. That tree is origin/master bfc7a15e2 merged with 2 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9446, #9792.
    - Go leg: 78 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 8373 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9792.md

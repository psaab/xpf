# #9913 — bound flow-export collector DNS and dial

- **Timestamp**: 2026-09-17
- **Action**: Reproduced the defect on unmodified `origin/master` before
  changing source. A test-double resolver slept for 6 s while
  `dialCollectors` ran through the existing `resolveUDPAddr` seam; the
  `TestStep0SlowResolveUnbounded9913` guard failed after 3.00 s with
  `dialCollectors blocked >3s on a 6s slow resolve — unbounded`.
- **File(s)**: `pkg/flowexport/transport.go`

- **Timestamp**: 2026-09-17
- **Action**: A daemon-level throwaway run against the unbounded mutant used a
  custom `net.DefaultResolver`. The first `reconcileV9Exporter` entered the
  blocked resolver; a second reconcile could not finish during a 100 ms wait,
  then both completed after DNS release. This proves the slow resolver parks
  `flowReconMu`, not merely that a lower-level helper blocks. The throwaway
  test was deleted after the run.
- **File(s)**: temporary `pkg/daemon/step0_flowexport_9913_throwaway_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Added a fresh 5 s context for each collector build. A configured
  source address is resolved with `net.Resolver.LookupIPAddr(ctx, ...)` so the
  concrete `LocalAddr` is bounded; the destination hostname is passed
  unchanged to `net.Dialer.DialContext`, preserving the standard resolver's
  multi-address fallback. Literal source parsing stays on
  `net.ResolveUDPAddr`'s fast path. Existing fatal construction errors,
  source binding, and partial-connection cleanup remain unchanged.
- **File(s)**: `pkg/flowexport/transport.go`

- **Timestamp**: 2026-09-17
- **Action**: Tested the final cells against a local unbounded mutant
  (`context.WithTimeout` replaced by `context.WithCancel`) before restoring
  the production line. The two slow cells failed after 1.00 s while the two
  normal-resolve controls passed. After restoration the full targeted set was
  **7 passed, 0 failed**. This is the RED-then-GREEN proof for the bound.
- **File(s)**: `pkg/flowexport/collector_dial_bound_9913_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Added fail-on-revert cells with a test-only 50 ms bound: a slow
  resolver and a slow dial both return `context.DeadlineExceeded` within the
  bound, while literal and `localhost` normal resolves still open and close
  one collector. The existing resolve/dial teardown cells remain covered.
  The targeted race run was **7 passed, 0 failed**.
- **File(s)**: `pkg/flowexport/collector_dial_bound_9913_test.go`,
  `pkg/flowexport/transport_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Affected package suite is green: **170 tests passed, 0 failed**
  (`go test ./pkg/flowexport -count=1`).
- **File(s)**: `pkg/flowexport/*`

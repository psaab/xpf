# #10488 — preserve rename ancestry in the touched modularity gate

- **Timestamp**: 2026-09-21
  - **Action**: drove the issue from the reported false crossing through an adversarial-ready implementation plan. The live producer was verified to use destination-only `git diff --name-only` rows and to look up only the destination at the merge base, so a pure rename was measured as a new file.
  - **Evidence**: the fresh Git control emitted `R100` with old and new paths while the existing probe emitted `- 1701 destination.go`; the producer/consumer row protocol and threshold decision did not need to change.
  - **Plan**: v2 added the exact NUL record contract, `-M90%` boundary, A/C/M/R/T/U/X/B table, staged and untracked semantics, blob-availability fallback, conflict and typechange probes, 22 real-repository fixture cases, and the zero-touch decision for `audit_touched_test.go`. Both delta review lanes dual-confirmed PLAN-READY. The plan was rebased onto merged #10487 commit `f4bae0a5a` before implementation.
  - **File(s)**: docs/pr/10488-rename-crossings/plan.md

- **Timestamp**: 2026-09-21 (implementation)
  - **Action**: committed `a3fd7224e` (`audit: preserve rename ancestry in touched probe (#10488)`). The producer now captures checked NUL-delimited status and untracked streams, pairs `R<score>` source/destination paths, uses `-M90%` with explicit rename detection, preserves the existing three-column output, stages rows before publishing, and fails closed on malformed or undetermined streams.
  - **Action**: committed `8bb7400c9` (`audit: cover rename ancestry regressions (#10488)`) with real fixture-repo coverage for pure/staged/edited/committed renames, threshold crossings, similarity boundaries, copies, consumed-source recreation, directory moves, typechanges, conflicts, whitespace, producer failures, and missing blobs.
  - **Action**: committed `dc991a9ae` (`docs: document rename-aware audit changed set (#10488)`) updating the touched-file gate's operator contract without changing #10487's accepted-entry liveness gate.
  - **Fold**: no Go consumer, threshold, acknowledgement, accepted-entry, runtime, or public API changes. `pkg/refactoraudit/audit_touched_test.go` remains untouched.
  - **File(s)**: scripts/refactoring-audit-touched.sh, pkg/refactoraudit/audit_jobs_test.go, docs/refactoring-audit.md

- **Validation**:
  - `GOCACHE=/dev/shm/gocache-10488 GOTMPDIR=/dev/shm go test -count=1 ./pkg/refactoraudit/ -run '^(TestTouchedRename10488|TestTouchedSetIsLocalToTheBranch|TestTouchedSetSeesUncommittedAndUntrackedGrowth|TestBranchTouchingNothingAuditedIsSilent|TestUndeterminableBaseFailsLoudly|TestThresholdCrossingCases|TestAcceptedCrossingIsTheOnlyEscape)$'` — PASS.
  - `GOCACHE=/dev/shm/gocache-10488 GOTMPDIR=/dev/shm go test -count=5 ./pkg/refactoraudit/ -run '^TestTouchedRename10488$'` — PASS.
  - `GOCACHE=/dev/shm/gocache-10488 GOTMPDIR=/dev/shm go test -count=1 ./pkg/refactoraudit/` — the package has no new red; it fails at the pre-existing #6937 calibration `TestStructMetricIsTypesNotFields6937` (`CompileResult` 32 fields / 21 types versus its 20-type floor), matching the baseline failure set.
  - Firsthand red-on-revert: in a temporary Git fixture, the origin producer emitted `- 1701 pkg/rename_new.go`; restoring the implementation emitted `1701 1701 pkg/rename_new.go` and both commands exited 0. The temporary fixture was removed.
  - `bash -n scripts/refactoring-audit-touched.sh`, `bash scripts/refactoring-audit-touched.sh`, and `git diff --check` — PASS.

- **Outcome**: implementation branch `fix/10488-rename-crossings`; final log commit and PR publication are the remaining delivery steps for parent review.

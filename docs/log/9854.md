# #9854 — two `system syslog host <addr>` statements compiled two destinations

- **Timestamp**: 2026-09-14
- **Action**: Reproduced all three issue rows at the lane base via
  `config.CompileConfig`, plus the flat-set counterparts:

  | configuration | compiled `System.Syslog.Hosts` |
  |---|---|
  | `system { syslog { host 10.0.0.1; host 10.0.0.1; } }` | 2 destinations, both no facilities |
  | `system { syslog { host 10.0.0.1; host 10.0.0.1 any any; } }` | 2 destinations: empty + `any/any` |
  | group `host 10.0.0.1 any any;` + inline braced `host 10.0.0.1 { }` | 2 destinations |
  | flat `set ... any warning` + `set ... daemon info` | 1 host, both pairs (merges) |

  Strict commit accepted every row. Mechanism:
  `pkg/config/compiler_system.go` built one `SyslogHostConfig` per AST node
  and appended unconditionally, while the runtime builds one `SyslogClient`
  per entry (`pkg/daemon/daemon_system.go:1312`) — a message matching both
  went to the same address twice. The #8436 census skips this site
  (`dupConservationSkipped8436`), so the divergence was hand-measured, as
  the issue says. Fix route chosen: find-or-create on the address (Junos
  merge semantics), mirroring the zone merge (#4818) and the BGP neighbor
  merge (#9192) — not refusal, because flat-set accepts-and-merges and the
  hierarchical spelling must conserve with it.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-14
- **Action**: Implemented the coalescing: per-address find-or-create with
  facilities appended in config order, scalar modifiers last-wins, and
  `allow-duplicates` OR. Guarded the `source-address` read so a valueless
  repeat cannot wipe a recorded address. Added
  `pkg/config/syslog_dup_host_9854_test.go` (issue rows, merge order,
  scalar last-wins, allow-duplicates, distinct-address and single-host
  controls, hier==flat conservation). Verified RED-on-revert (5 of 7 fail
  with the fix stashed; the 2 controls pass on master by design) and ran
  the syslog regression subset plus `go vet` clean. Pushed and opened
  PR #10048.
- **File(s)**: pkg/config/compiler_system.go,
  pkg/config/syslog_dup_host_9854_test.go

- **Timestamp**: 2026-09-14
- **Action**: Named the adjacent same-class gap the review asked about and
  scoped it out: duplicate `syslog` STANZAS still reset —
  `case "syslog": sys.Syslog = &SystemSyslogConfig{}` reallocates per
  stanza, so sibling `syslog {}` blocks and twin top-level `system {}`
  blocks both compile only the LAST stanza's hosts (measured post-fix:
  `hosts=[10.0.0.2]`, first stanza wiped), and `mergeDuplicateBlocks9023`
  has no `syslog` site. A fix needs `sys.Syslog` find-or-create plus a
  cross-stanza host index and `file`/`user` coverage — beyond the issue,
  which names duplicate `host` statements. Recorded in the PR body. Full
  `go test ./pkg/config/ ./pkg/daemon/` green (185s / 87s).
- **File(s)**: none (PR body)

- **Timestamp**: 2026-09-14
- **Action**: Folded the parent review (core defect: group-wins scalar
  inversion). Expansion appends adopted group nodes AFTER inline nodes, so
  plain last-wins let a group's `port`/`source-address` beat the inline
  block's explicit value — measured: group `port 999` + inline `port 514`
  merged to 999. The compile now expands TAGGED on all three funnels
  (`ExpandGroupsTagged`, new `ExpandGroupsWithVarsTagged`), and the
  coalescing reads per-instance provenance: inline always records (last
  inline wins), inherited records only first (first group wins) —
  preserving Junos apply-groups precedence and the ast_groups.go:385-386
  typed-merge rule. Tags ride the `cloneForExpansion` copy and no reader
  except the coalescing (plus `| display inheritance` on its own clone)
  branches on them. Same review round: corrected the flat-set immunity
  claim (a bare `host X` leaf plus a body container ARE two nodes,
  ast_edit.go:508-553 vs :720-738) with a both-orders control; added the
  valueless-repeat guard test (strict rejects the shape, so it compiles
  without the gate); covered allow-duplicates flag-second; deduped exact
  facility pairs (flat `set ... any any` twice is one pair; both hier
  shapes now are too). Guard test mutation-checked (guard removed -> RED).
  Named tests 5x, full suites, `go vet` clean.
- **File(s)**: pkg/config/compiler.go, pkg/config/ast_groups.go,
  pkg/config/compiler_system.go, pkg/config/syslog_dup_host_9854_test.go

- **Timestamp**: 2026-09-14
- **Action**: Chased the group-vs-group shape matrix to a RED boundary and
  stopped there deliberately. Same-shape groups resolve correctly (packed +
  packed drops the later at expansion via peer-override; braced + braced
  merges with the first winning). Mixed shapes split: G1 braced + G2 packed
  merges to G1's value (first wins, pinned — RED without the
  inherited-first rule), but the mirror G1 packed + G2 braced merges to
  G2's value, because the braced later-group body lands INSIDE the inline
  container ahead of the packed earlier-group sibling — position inverts
  application order, and no position-based rule recovers it. Fixing the
  mirror needs ordinal provenance, which `InheritedFrom` does not carry;
  the outcome is defined but positional, documented in the `#9854` code
  comment, and deliberately unpinned. Also observed while verifying (NOT
  touched): a packed GROUP host leaf beside ANY inline packed host leaf is
  dropped whole at expansion (`leafListPeer` matches any same-keyword
  leaf, even a different address) — measured: group `host 10.0.0.1` plus
  inline packed `host 10.0.0.2` compiles only .2. Pre-existing,
  master-identical, expansion-side — own lane.
- **File(s)**: pkg/config/compiler_system.go (comment),
  pkg/config/syslog_dup_host_9854_test.go (FirstGroupWins subtests)

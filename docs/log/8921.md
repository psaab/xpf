# #8921 -- multi-site elision admissions (items 1 and 2)

- **Timestamp**: 2026-09-10
  - **Action**: Measured every recorded (pair, site) cell of the #8921 registry
    against the #2419 census and the #8690 scope-guard logic with throwaway
    probes (removed). 533 cells: 393 compiled-equivalent, 139 unruled (91 value
    not observable, 44 outside the census shape, 4 fixture limits), 1 divergent
    at `interfaces unit @ interfaces/*`. (The first draft of this entry said 140
    unruled and 5 fixture limits; the probe's unruled list had included the
    divergent cell. Corrected after Codex review.)
  - **File(s)**: none (probes deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Measured the structural property instead: normalized elided tree
    vs braced tree per cell. Keys-only: 532/533 identical, the exception being
    the wildcard-slot row where the fold never fires. Every field: 492/533
    differed -- the fold dropped KeysQuoted/KeysBracketed. Confirmed a
    behavioural consequence with positive controls: elided
    `group g1 export A "export";` and `api-auth api-key AAA "api-key";` refused
    at strict commit by the #9027 gate while the braced spellings commit.
  - **File(s)**: none (probes deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Fixed the fold and splitBracedPackedChildren8886 to split both
    per-key masks with the keys; stopped the ratchet walk recording pairs at a
    wildcard slot; regenerated the registry (one row removed); added
    multisite_adjudication_8921_test.go (structural adjudication of every cell
    with a per-cell negative control, census cross-check, #9027 consequence
    cells, splitter cell).
  - **File(s)**: pkg/config/compact_normalize_8662.go,
    pkg/config/multisite_admission_ratchet_8921_test.go,
    pkg/config/multisite_adjudication_8921_test.go,
    pkg/config/testdata/multisite_admissions_8921.txt
- **Timestamp**: 2026-09-10
  - **Action**: Recorded the item 1 decision (stay keyword-keyed) and the
    adjudication in the config-schema doc; this log.
  - **File(s)**: docs/config-schema.md, docs/log/8921.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex hostile review returned MERGE NO. Measured each finding:
    (1) the oracle normalized the braced tree too -- changed to the braced tree
    as parsed, required to be a fixed point; the "every value" claim narrowed.
    (2) CONFIRMED: commit-check expanded groups before normalizing; a packed
    group `neighbor hold-time 2` failed to meet an inline braced override and
    was refused while the braced group body validated clean. Fixed by
    normalizing before ExpandGroups in schemaValidateExpandedTreeForNode.
    (3) CONFIRMED pre-existing: `proposals [ P "proposal-set" ]` split into a
    bogus `proposal-set;` in both spellings. splitPackedStatements8768 now
    declines a boundary on a quoted token or inside a bracket run.
    (4) group leaf-list union dropping the quote: NOT reproduced; its control
    showed a group-inherited bare self-repeat escaping the #9027 gate once an
    inline leaf exists -- filed as #9627. (5) instance named like an admitted
    keyword: reachable, and folds to the braced tree; comment corrected and a
    cell added. (6) #8690 arm overstated in docs: corrected. (7) census floor
    and splitter offset/bracket cells added. (8) log arithmetic corrected.
  - **File(s)**: pkg/config/compact_normalize_8662.go,
    pkg/config/multisite_adjudication_8921_test.go,
    pkg/config/multisite_admission_ratchet_8921_test.go,
    pkg/configstore/store.go,
    pkg/configstore/group_expand_compact_order_8921_test.go,
    pkg/configstore/README.md, docs/config-schema.md, docs/log/8921.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 2 returned MERGE NO. Measured: (1) CONFIRMED -- the
    round-2 commit-check fix normalized BEFORE stripping inactive nodes; an
    inline compact neighbor beside an `inactive:` sibling made the fold decline
    and the override failed again. Reordered to strip -> normalize -> expand,
    with the stripped normalized tree as defsSource; added the cell.
    (2) CONFIRMED -- the authored-value split decline only changed the tolerant
    compile: #8437 refuses `proposals [ P "proposal-set" ]` at commit on master
    and on the branch. (3) CONFIRMED REGRESSION -- the decline broke
    `pre-shared-key ascii-text "s" "mode" aggressive;`, which master splits and
    accepts. Withdrew the decline (kept mask propagation on split statements)
    and filed #9635 with both constraints. (4) test comment "EVERY value"
    narrowed. Note: the first draft of these comments cited a PREDICTED issue
    number (9634) because the issue was filed in parallel with the edit; the
    real number is 9635 and the comments were corrected before commit.
  - **File(s)**: pkg/config/compact_normalize_8662.go,
    pkg/config/multisite_adjudication_8921_test.go,
    pkg/configstore/store.go,
    pkg/configstore/group_expand_compact_order_8921_test.go,
    pkg/configstore/README.md, docs/config-schema.md, docs/log/8921.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 3 verified every round-2 disposition (strip before
    normalize correct; decline withdrawal correct; no definition loss from the
    stripped defsSource) and returned MERGE NO on one Medium finding: the
    ordering test's fixtures lacked peer-as, so the strict compiler refused
    them and the commit-check-only cell proved no parity. Fixtures now carry a
    group-level peer-as and every override case asserts commit-check AND
    CompileConfig accept; the control still asserts the inherited hold-time is
    refused. Round-3 mutation matrix: M1, M2 (rerun buildably after a VOID),
    M3, M4, M5, M9, M10 all KILLED.
  - **File(s)**: pkg/configstore/group_expand_compact_order_8921_test.go,
    docs/log/8921.md

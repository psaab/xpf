# #9878: closed-world arm on `security zones` + `security policies`

## STEP-0 repro (base 7a10fd749)
- New test `TestClosedWorldSecurity9878_TypoRejected` FAILS on base:
  `policie`/`frum-zone`/`mach`/`interfces`/`system-servces` all commit
  clean (SchemaValidate err nil). Not ALREADY_FIXED.

## Narrow vs wide (verified, parent verdict A)
- Arming ONLY zones+policies cannot reject `policie`: sibling-level
  unknown under open `security`, never descends into either subtree.
- Trial-armed `security` itself: all 5 typo cells green, BUT
  TestShippedHAConfigsCheckCleanOnBothNodes_9619 reds on
  `security flow tcp-mss all-tcp` (compiled but unmodeled; tcp-mss is an
  intentionally-opaque value grammar per #1979). Reverted to narrow.
- `policie` dropped with documented gap; security-level arm filed as
  #10078 (quoted in test comment + PR body).

## Change
- `pkg/config/schema_security.go`: `closedWorld: true` on `zones` and
  `policies`. `from-zone` inherits (childClosed propagation); no flag.
- Tests (`pkg/config/schema_closedworld_security_9878_test.go`):
  TypoRejected (4 cells naming token + closed-world), AcceptsValid
  (policy/zone/global/default-policy/wildcard-iface/hi-t shapes),
  LenientDoesNotBrick (#1960), ShippedConfigsStillCommit (10/10 + control).
- Ratchets updated in-change with measurements:
  - #9091: blind 144→140, armed 23→27 (4 containers moved lists).
  - #9206: 37→57 / 26→36. Every new site measured with garbage values:
    9 refused at strict compile (undefined address/app/zone validators,
    validateHostInboundTokensStrict, address-set member check), 1 reaches
    (zone-book `address` commits like the global-book rows — known #9490
    class, not new).

## Gates
- Named 7 tests x5: green. Full `pkg/config` + `pkg/configstore`: green.
- `gofmt` clean, `go vet` clean.

## Fold-2: parent review (GPT + SPARK convergent NEEDS-MAJOR)
- **(1) False-refusal inventory**: AcceptsValid extended to policy-rematch/extensive, default-policy-log (single+bracket), scheduler-name (both scopes), excluded matches, global from-zone/to-zone, deny-log-count collapsed, zone description/tcp-rst/screen/address-book/nested sets, bare interfaces. Two fixture bugs caught by the gate itself (multi-word description, valueless `then deny log` — both pre-existing strict behavior, not gate regressions). Hierarchical typo cells added (flat-only was unproven); hierarchical `policie` ACCEPTS in both serializations → converted to known-open tripwire.
- **(2) Sweep proof**: hardened — read/parse/validate failures Errorf, `checked != len(files)` fatals, 10/10 pinned; positive control rewritten through the temp-file + NewParser path.
- **(3) 9206 census corrected**: walk appended single xpfinst per args>=1, collapsing from-zone identity — control rejected at the mid position for the wrong reason, 3 zone-pair match sites silently dropped. Walk now carries every arg slot + midKeyword at its slot. True count 60/39 (not 57/36). Re-derived zone-address: `address <name> <prefix>` is DEFINITION (garbage name commits by design); correct probe is trailing-after-prefix (#3332) → STRICT REJECT → 0 reach across all 13.
- **(4) midKeyword hole closed (spark-F1)**: `to-zon` silently accepted flat (compiler reads Keys[1],Keys[3]) and silently dropped hierarchical (FindChild miss). Validated at both walk sites (only from-zone uses midKeyword — blast radius 1). Cells RED-pre-fix proven via patch roundtrip (no stash).
- **(5) Leaf-completeness audit**: paragraphs on both flags + docs/config-schema.md closure entry.
- **(6) Minors**: policie tripwire (rip-9206 shape); #10078 scope broadened by issue comment (alg/ssh/policy-stats/dynamic-address/nat/log/flow/screen/ike/ipsec/global-book audit); Lenient cell reworded to its exact binding + new configstore Store.Load ingress cell (6924 pattern).

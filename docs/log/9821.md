# #9821 — declared dotted interface as RI member: declared-first split in every member consumer + pair-atomic Rust merge

Lane: worktree `9821-declared-dotted`, branch `fix/9821-declared-dotted`.
Base at implementation: `origin/master 8a60f4c7d`; final rebase onto
`origin/master d3e959067` (#9637, 1 commit) before PR.
Method: plan-first with hostile review (both pi models, `--thinking max`),
7 plan rounds to a parent-ordered HARD STOP, then 16 shippable commits with
red-green cells + 5x flakes + affected suites per commit. No cluster use
(plan substitute: executable Go→Rust contract golden + fake-link cells).
Parent owns final PR review and merge.

- **Timestamp**: 2026-09-14
- **Action**: STEP-0 present-verification at `b4e78ea28`. All seven
  defect sites confirmed live: dot allowed in `ValidateInterfaceName`;
  declared-first only in `resolveKernelIfNameWith`; warning cuts at dot;
  strict unitref cuts without declared check; raw-base lookup in
  `logicalUnitDeviceKeyForRef`; `InterfaceUnitRefKeys` literal-only for
  dotted; Rust last-nonzero domain vs last-wins name merge. Not
  ALREADY_FIXED. Read full issue + research record (COHORT, Low, 3-way
  gate). Wrote `docs/pr/9821/plan.md` (11 sections, v1).

- **Timestamp**: 2026-09-14
- **Action**: Rounds 1–3 (both models, max-3 cap). R1: GLM NEEDS-MAJOR /
  Codex KILL. R2: GLM NEEDS-MAJOR / Codex KILL. R3 (v3): GLM PLAN-READY
  (+4 folds M1–M4) / Codex NEEDS-MAJOR (F1–F6). SPLIT AT CAP → BLOCKED per
  the both-must-agree gate, no implementation. Posted plan v3 + review
  history to #9821 as comments. Derived UNREVIEWED `v4-delta.md`
  (D1–D12: effective-key gate, structural pools, override/role/classifier
  follow-through, version bump, FRR type, folds).

- **Timestamp**: 2026-09-15
- **Action**: R4 (parent-authorized, post-rebase to `18f5f9a78`): GLM READY
  (+3 folds) / Codex NEEDS-MAJOR (R4-1–R4-7). SPLIT → lane STOPPED per
  parent rule (agree→implement; split→STOP + verbatim + gap). Posted r4
  verdicts + gap synthesis. Parent sustained Codex; derived v4 tranche 2
  (D13–D18: shim resolver, override stamp, alias agreement, pool alias
  policy, exclusion proofs, wire-atomic sequence) + GLM folds.

- **Timestamp**: 2026-09-15
- **Action**: R5 (scoped D13–D18): SPLIT again (GLM READY+F1–F5 / Codex
  NEEDS-MAJOR R5-1–R5-5). Parent sustained R5-1/R5-2/R5-4/R5-5. Derived
  `v5-delta.md` (D19 stamp construction, D20 bare aliases, D21
  SameInterface reword, D22 WG index, D23 stable_iface migrate, D24
  inventories/assignment).

- **Timestamp**: 2026-09-15
- **Action**: R6 (scoped v5): GLM NEEDS-MINOR / Codex NEEDS-MAJOR
  (R6-1/R6-2 sustained as design completions, rest folds). Derived
  `v6-micro-delta.md` (D25 stable_iface tuple contract, D26 WG identity
  keys, D27 folds) under parent HARD STOP (r7 is the LAST review).

- **Timestamp**: 2026-09-15
- **Action**: R7 (scoped D25–D27, inventory-CLOSED): GLM NEEDS-MINOR /
  Codex NEEDS-MAJOR (R7-1–R7-5). Adjudicated per mechanism (Codex governs
  where it traced failing pairs GLM did not): R7-1 kind-tracking,
  R7-2 mixed-version proof+pins (fence declined with evidence), R7-3
  lookup order, R7-4 stamp-presence fallback (simplified),
  R7-5/GLM-F1 converged. Folded as v6 appendix constraints. Posted r7
  record to #9821. IMPLEMENTING under HARD STOP (no r8).

- **Timestamp**: 2026-09-14/15
- **Action**: Implemented commits 1–5 + 9 on the pre-rebase base (split
  helper; RI fan-down/units/gates/store; zone keys/mirrors + host-inbound
  follow-through with golden regen; resolver unit arm with agreement
  extension; #23 dead-last collision gate with 9 cells; Rust pair-atomic
  merge with 2 cells). Each: red-green via scratch worktree (no stash per
  standing rule), 5x flakes, affected suites green.

- **Timestamp**: 2026-09-15
- **Action**: Scrub + rebase (parent GO). Dropped 3 pushed process-only WIP
  checkpoints (docs/pr-only, all preserved on disk untracked) — 7/7
  replayed conflict-free. Rebasing onto `origin/master 8a60f4c7d` (19
  ahead): CLEAN, 7/7 replayed, ZERO conflicts. Re-verified green
  (pkg/config 190s, userspace 53s, forwarding_build 184). Parent directed
  rebase-before-remaining-work; implementing 6/7/8/10/11 against current
  sites (re-anchored every cite — several v3 paths had drifted, e.g.
  `contested.rs` → `interfaces.rs`). CORRECTION (review fold): the re-anchor
  note that once stood here claiming `host_inbound_classify.go` "gone" was
  wrong — the file lives at `pkg/dataplane/userspace/`, was searched in the
  wrong package, and its D6 migration was missed until parent review caught
  it (folded as its own commit with red-green cells).

- **Timestamp**: 2026-09-15
- **Action**: Fanned three independent slices to subagents (shared
  worktree, disjoint files, no-commit rule): WG index (D22/D26/F-D26),
  STAMP follow-through (D19/D20/F-D19/R6-3/R6-4 audit + gaps), FRR render
  (#15v3/D7/cell 11). All three landed with red-green + 5x + suites;
  reviewed each diff and committed separately. Notable slice findings,
  folded: FRR production construction lives outside render files (wired
  both assemblers + construction pin); STAMP fixed a real child-only raw
  alias gap (union post-pass); WG surfaced a DEFINED-gate strict tension,
  correctly resolved per plan (strict zone still rejects dotted members
  — pinned; WG cells ride lenient/struct shapes).

- **Timestamp**: 2026-09-15
- **Action**: Implemented commit 6 (daemon bind 6a + pools/DHCP 6b: #5,
  #16 authored-singular/structural-plural with the NO-NEW-BIND killer pin,
  D2 owner-stamped pools, D16 shared 4-level resolver, #6/#7). Red-green
  per commit; pre-6a baseline reproduces the dead member for every pool
  cell. Deliberate structural consequence pinned: vlan-id addressing
  (`B.100` for unit 10) misses — units are addressed by unit number,
  aligning pools with the member-units consumers.

- **Timestamp**: 2026-09-15
- **Action**: Implemented commit 7 in two parts. 7b (shim): D13
  `resolveInterfaceRef` migration with pure table + fake-link
  zone/SNAT/GRE/MTU/Disable cells via existing seams, R6-5 SNAT seam vars.
  7c (advisories + folds): D17 RG/unresolved/MTU/trunk swaps; D23/D25/F-R71
  kind-tracking stable_iface sender + declared install short-circuit with
  an extracted testable receiver; F-MIXED never-worse tables both
  directions via legacy oracles; F-EVIDENCE fixture pins.

- **Timestamp**: 2026-09-15
- **Action**: Implemented commit 10 (WIRE-ATOMIC, D18): `is_unit`
  structural bit — Go field (unconditional emit) + 3 emit sites + 2
  classifiers, Rust `Option<bool>` + classifier rewrite, version bump both
  took 18, #10074 took 19, #10075 took 20 — see the rebase entries), shape
  const+digest (verified delta is exactly the new field), fixture regen
  `+ "is_unit": null`), 6691/9054 pin moves, helper refusal cell, cell-17
  Go + Rust. One suite catch during landing (9054 literal pin — moved per
  its own convention).

- **Timestamp**: 2026-09-15
- **Action**: Implemented commit 11 (contract golden, cell 13 + 13b): Go
  producer (fixture + stubbed links + normalized time → committed bytes,
  UPDATE_GOLDEN=1) with independently-asserted pins + `is_unit` lockstep
  (7037 helpers); Rust consumer (include_str → build → pair/structural/
  control asserts from golden-derived values); 13b bind⊆golden-rows belt
  check + FRR operand belt re-validation. Every golden row reviewed
  against the spec (pair atomic, structural identity, controls mirror).
  Red-on-master reproduces the issue verbatim (base RA/unit-0 empty).

- **Timestamp**: 2026-09-15
- **Action**: Adjacent observations (pre-existing, out of cohort scope,
  noted for parent triage, not fixed): (1) combined single-line
  `unit N vlan-id V family inet address A` silently drops the address at
  parse level even undotted (split lines work; 6722 fixtures use combined
  lines but never assert addresses); (2) strict zone DEFINED still
  rejects dotted members at first-dot (plan-pinned; WG advisory serves
  lenient/struct shapes); (3) `snapshotInterfaceNameMatches` config/
  runtime divergence for dotted families (plan §10 follow-up pointer).

- **Timestamp**: 2026-09-15
- **Action**: Final validation: full `go test ./...` (72 pkgs ok; 3
  failures all reproduce on clean master — docsref dangling citations
  for 9587/9604/9824, refactoraudit 6937 calibration, Rust
  cos-literal-guard — pre-existing, not lane regressions) + `cargo test
  --release --bins` (5896 passed, same 1 pre-existing guard failure).
  Moved the contract test module to `forwarding_build/tests.rs` (its
  natural module-level home) to hold `interfaces.rs` under the 1500
  WATCH floor the lane crossed (1204→1531→1422).

- **Timestamp**: 2026-09-15
- **Action**: Final rebase onto `origin/master d3e959067` (1 commit, #9637
  host-inbound). One conflict: BOTH lanes bumped the snapshot wire to 18
  — the v8 rule moves the merged wire past both, so the lane renumbered
  to 19 (Go + Rust consts and notes, 6691/9054/shape pins, v18-helper
  refusal cell, golden regen whose diff is exactly the version field).
  Re-verified affected suites green (config/daemon/dataplane/userspace/
  frr/cli + forwarding_build/protocol). Pushed with lease, opened PR
  (`Closes #9821`) with the test table. STOP without merging — parent
  owns review + merge.

- **Timestamp**: 2026-09-15
- **Action**: Post-PR re-bump to v20 (parent instruction): #10074 merged
  first and took v19 (`SourceNATRuleSnapshot.LenientMatchDropped`). Per
  the v8 rule the lane rebased onto `origin/master 7a10fd749` and
  renumbered (Go + Rust consts and notes, 6691/9054/shape pins with the
  recomputed merged digest, v19-helper refusal cell, golden regen whose
  diff is exactly the version field). Rebase conflicts confined to the
  wire commit's 5 version files; fixture auto-merged with both lines.
  (Push deferred: parent review verdicts arrived first — see next entry;
  single push after folds + full re-gate.)

- **Timestamp**: 2026-09-15
- **Action**: Parent review fold (GPT NEEDS-MAJOR ×6 + GLM NEEDS-MINOR
  ×2 + 1 observation), all folded as own commits with red-green cells:
  (1) #23 requires generated-key string equality (padded spellings no
  longer refuse); (2) Rust merge rewritten to the settled member-claimed
  guard (v2 §5.3 pseudocode verbatim — membership survival, not recency;
  clearing test rewritten, not patched); (3) D6 classify migration (the
  file was never gone — wrong-package search); (4) MTU gate walk migrated
  (strict-reachability proven: DEFINED admits via declared `p`, so GPT-4
  governs over GLM's diagnostic-only rating); (5) stable sender unifies
  on input spelling (slash convergence + slash-approx); (6) stamp
  bare-alias first-wins aligned with runtime #18. GLM-1 dangling cite
  fixed to the two real cells; GLM-2 resolved by the (4) migration.
  Observation recorded (no change): query/display cfg-free
  canonicalization + `nat_iface_pool_overlap` first-dot stay listed
  follow-ups (v3 §10 discipline).

- **Timestamp**: 2026-09-15
- **Action**: Post-review re-bump to v21 (parent instruction): #10075
  merged first and took v20 (`FirewallTermSnapshot.FromUnrepresentable`).
  Per the v8 rule the lane rebased onto `origin/master 4eedd01d7` and
  renumbered (Go + Rust consts and notes, 6691/9054/shape pins with the
  recomputed merged digest — delta verified exactly the `is_unit` line,
  v20-helper refusal cell, golden regen whose diff is exactly the version
  field, squashed into the contract commit via fixup). Rebase conflicts
  confined to the wire commit's 5 version files; fixture auto-merged with
  both lines. Re-gated affected suites, pushed with lease, updated the PR
  body.

- **Timestamp**: 2026-09-15
- **Action**: Parent review delta fold-2 (GPT NEEDS-MAJOR + SPARK
  NEEDS-MINOR on the v20-verified tree), rebased onto `origin/master
  2456a1fcb` (#9877), full re-gate + red-proofs re-run post-rebase:
  (1) GPT-1 bare-alias first-wins REMAINS LATER — genuine: the bare-direct
  branch merged onto alias-occupied canonicals (signed-zero `p.-0` sorts
  before `p.0`; unit-alias onto declared bare). Now every bare stamp
  write first-wins regardless of occupant provenance, exactly as the
  runtime #18 bare arm; 2 agreement cells, both RED pre-fix.
  (2) GPT-2 saturated Atoi — genuine: the MTU walk zeroed every Atoi
  error while the resolver retains saturation on overflow. Now
  assign-then-ignore exactly as `resolveInterfaceRef`; overflow cell
  RED pre-fix. The blank identifier tripped the #6940 textual census,
  so the error is named + checked-deliberately-uncorrected (own
  commit; gate green). (3) SPARK-F1 "2 RED + 3 pins" — REFUTED: every
  durable claim (merge commit, PR body, log, code FAIL-ON-REVERT
  headers) says exactly 1 RED + 3 preservation pins, and the
  scratch-worktree matrix measured exactly that on byte-identical
  merge code; red-matrix note committed above the cells. (4) SPARK-F2
  bare-Literal comment — ALREADY FOLDED inside (1): the false
  "always same-identity" sentence died in the fix commit and the
  exact both-declared + `p.0.001` scenario pin landed there,
  RED-verified. Procedure note: two parallel red-proof swaps raced
  on one file and contaminated a run; re-ran serially from git HEAD
  (both RED confirmed). Gates: `pkg/config` + `pkg/dataplane` +
  `pkg/dataplane/userspace` ok; cargo release 5917 pass with the
  single `cos_literal_guard_7689` failure reproduced on pristine
  master (pre-existing, lane adds zero NeighborSnapshot literals).

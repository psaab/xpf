# #9503 — cos: a three-color policer `then loss-priority` commits clean on all four channels and then disarms ALL forwarding

- **Timestamp**: 2026-09-10
  - **Action**: The issue's three coordinated edits, posture "apply,
    meter-only, warn". The issue rejects reject-at-commit (the #1960 no-brick
    line, and #8445's own text recommends marking alone).
    1. Go capability gate (`userspaceSupportsThreeColorPolicers`,
       pkg/dataplane/userspace/capabilities.go). A color-blind policer with
       `then loss-priority <level>` no longer sets
       `ForwardingSupported=false`, which had disarmed every binding. The
       colour-blind guard is unchanged.
    2. Rust shape check (`snapshot_three_color_shape_supported`,
       userspace-dp/src/filter/compiler.rs) admits `loss-priority <level>`, so
       `treatments_from_then_action` gives its existing no-drop default.
       Without this edit, step 1 alone would fail the referencing terms closed
       and drop every packet on them.
    3. A commit advisory (`validateThreeColorPolicerMarkingWarnings`,
       registered after the #2507 filter loss-priority warning) says the
       policer meters but neither drops nor marks.
  - **Action**: Refinement over the issue's "drop the ThenAction clause".
    Both sides admit exactly `""`, `discard` and `loss-priority <level>`
    (`threeColorThenActionSupported` in Go, the Rust shape check), so an
    action neither knows still refuses. That is a drift guard, and it keeps
    the two sides in agreement. The disarm reason text drops "and then
    discard", which is no longer true; the test literals that quote it were
    updated.
  - **Action**: A prior decision was reversed deliberately. Two existing
    cells pinned loss-priority as unsupported, and both are the behaviour this
    issue reverses. In each, the loss-priority row became an unknown-action
    (`frobnicate`) row, so "an unsupported action refuses" keeps a real case:
    - Go TestDeriveUserspaceCapabilitiesRejectsUnsupportedThreeColorPolicerActions;
    - Rust unsupported_three_color_snapshots_fail_closed_in_rust_compiler.
  - **Action**: The #8445 error text already describes marking alone as
    METERS ONLY, and is now true for three-color policers too. The
    docs/feature-gaps.md Three-Color Policer row records the meter-only
    posture and the pre-#9503 disarm.
  - **Action**: Cells.
    - pkg/dataplane/userspace/three_color_loss_priority_9503_test.go follows
      compile, then buildSnapshot, then the helper-echoed
      `lastStatus.Capabilities`, to the FINAL `desiredForwardingArmedLocked`.
      Rows:
      - loss-priority, color-blind: armed;
      - the discard control: armed;
      - color-aware with loss-priority: disarmed;
      - color-aware with discard: disarmed.
      A drift-guard table for `threeColorThenActionSupported` is added.
    - pkg/config: strict compile accepts, with exactly one meter-only warning
      naming the policer; discard does not warn.
    - pkg/configstore: CheckText accepts, and the warning reaches the commit
      channel.
    - Rust three_color_loss_priority_meters_without_dropping_9503: the same
      two packets as the default-discard cell; the RED one is forwarded,
      counted red, and 0 drops.
  - **File(s)**: pkg/dataplane/userspace/capabilities.go,
    pkg/dataplane/userspace/manager_capabilities_test.go,
    pkg/dataplane/userspace/manager_ha_test.go,
    pkg/dataplane/userspace/three_color_default_4535_test.go,
    pkg/dataplane/userspace/format/status_test.go,
    pkg/dataplane/userspace/three_color_loss_priority_9503_test.go,
    pkg/config/compiler_validate_warn_firewall.go,
    pkg/config/compiler_validate_warn.go,
    pkg/config/three_color_marking_warning_9503_test.go,
    pkg/configstore/three_color_loss_priority_checktext_9503_test.go,
    userspace-dp/src/filter/compiler.rs, userspace-dp/src/filter/tests.rs,
    docs/feature-gaps.md, docs/log/9503.md
- **Timestamp**: 2026-09-10
  - **Action**: Go mutation matrix at 1013f343e, scored by `go test -json`
    test name over `-run '9503|ThreeColor|Capabilit'` in pkg/config,
    pkg/configstore and pkg/dataplane/userspace. NOOP clean (53 tests ran);
    5 of 5 mutants KILLED:
    - B1, gate reverted (loss-priority not admitted): killed by
      TestThreeColorLossPriorityKeepsForwardingArmed9503/then_loss-priority_high,_color-blind.
    - B2, any action admitted: killed by TestThreeColorUnknownActionStillRefuses9503.
    - B3, colour-blind guard dropped: killed by
      TestThreeColorLossPriorityKeepsForwardingArmed9503/color-aware_then_loss-priority.
    - B4, advisory not registered: killed by
      TestThreeColorMarkingActionWarnsMeterOnly9503.
    - B5, advisory fires on discard: killed by
      TestThreeColorMarkingActionWarnsMeterOnly9503.
  - **Action**: Rust mutation matrix on `snapshot_three_color_shape_supported`,
    scored by the failing `cargo test` name over the `three_color` filter
    cells; 2 of 2 KILLED:
    - R1, loss-priority not admitted: killed by
      three_color_loss_priority_meters_without_dropping_9503.
    - R2, any action admitted: killed by
      unsupported_three_color_snapshots_fail_closed_in_rust_compiler.
  - **Action**: Before the commit, the full pkg/config (205.8 s),
    pkg/configstore (36.8 s), pkg/dataplane/userspace (54.0 s) and its format
    subpackage were green, vet was clean, and the Rust three-color cells passed
    16/16.
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    84f4c4f1c. That tree is origin/master 2c29dc14f merged with 6 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9603, #9340, #9512, #9695, #9697, #9503.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5831 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9503.md

# #9415 — operator-facing text asserting a posture the code does not have

- **Timestamp**: 2026-09-11
- **Action**: Correct five stale texts (cohort members 1-5, member 5 from the issue comment)
- **File(s)**:
  - `pkg/dataplane/userspace/screens.go`: per-zone lines of the unresolved and inert `show security screen` blocks
  - `pkg/api/metrics_descriptors_global.go`: `xpf_screen_inert_profile_zones` HELP
  - `pkg/config/compiler_validate_strict_screen.go`: undefined screen-profile commit error and its doc comment
  - `pkg/config/compiler_validate_strict_policy.go`: the four undefined-zone literals (zone-pair from/to, global match from/to)
  - `docs/syn-cookie-flood-protection.md`: cookie-reply classification paragraph
  - `docs/feature-gaps.md`: IPsec SA Synchronization row, Done -> Partial
  - tests: `pkg/dataplane/userspace/screens_text_posture_9415_test.go`, `pkg/config/operator_text_posture_9415_test.go`, `pkg/api/metrics_screen_inert_help_9415_test.go`, `pkg/cluster/ipsec_sa_sync_gap_row_9415_test.go`

## Re-verified at origin/master 580d959e0

1. The per-zone lines at `screens.go:284-285` and `:670-671` still said "no screen checks are enforced for this zone", and the Disposition two lines below still described the substituted conservative default. The Rust `missing_profile_verdict` (`userspace-dp/src/screen/unresolved.rs`) evaluates both kinds against `ScreenProfile::conservative_default()`. The inert HELP said "enforces nothing". The unresolved HELP carries only the Disposition and was already true.
2. The strict error still ended "silently runs with NO screen protection (the dataplane fails open ...)". Its comment still quoted the removed `return ScreenVerdict::Pass` line.
3. `docs/syn-cookie-flood-protection.md` (now line 458) still said replies "intentionally bypass output filters, CoS classification, DSCP rewrite, and mirroring". `cookie_reply.rs:100-120` calls `classify_generated_reply`, counts `syn_cookie_output_filter_drops`, and carries `cos_queue_id` and `dscp_rewrite`; only `mirror_clone: false` is bypassed.
4. `docs/feature-gaps.md:655` still read "Done" for a row whose mechanism is connection-name sync plus re-initiation.
5. All four literals in `validatePolicyZoneReferencesStrict` still said "silently never matched". The Go mirror (`policies_reject_zone_9410.go`) already describes the whole-snapshot refusal.

No test pinned any stale literal, other than #9570's negative check that "silently never matched" does not appear for `junos-global`. That check is unaffected.

## Wording

- Screen per-zone lines: "none of its configured checks are in effect; the substituted conservative default below applies".
- Screen commit error: names the #7168 substituted default (malformed-packet checks, no flood/scan/session-limit thresholds) instead of fail-open.
- Policy literals: "on a tolerant load the userspace helper refuses the WHOLE policy snapshot for an unresolvable zone (#3402), so none of the config's policies is enforced". The zone name and the `define ... in the same commit` hint are kept.
- Syn-cookie paragraph: replies are classified since #2238, only mirroring is bypassed, and the legacy eBPF XDP_TX posture must not be restored.
- IPsec SA row: Partial, naming the per-tunnel IKE renegotiation window on every RG failover (planned or not) and the dependence on the remote peer accepting a fresh IKE.

## Golden baseline

`TestCompileGolden4406` (the #4406 output-preservation harness) went red, because the lenient `strict-violations` cases carry the zone-pair from-zone literal as a downgraded warning. Its per-case "CONFIG changed" list named 18 cases. That list is not a count of changes: once the file differs anywhere, every non-null `Config` compares unequal, because a baseline `RawMessage` read back from the indented file is never byte-equal to the compact fresh marshal. A structural JSON diff of actual against baseline found exactly one distinct change, the new warning text, at `strict-violations/lenient/{generic,node0,node1}.config.Warnings[4]`. The baseline was regenerated with `GOLDEN_4406_UPDATE=1`, and the file diff is those three lines only. The golden passes on pristine master `580d959e0` and at this head.

## Validation

- Broad run (`pkg/config pkg/api pkg/dataplane/userspace pkg/cli pkg/grpcapi pkg/cluster pkg/policymatch`) at da62f291e: 16650 passed, 1 failed (the golden above, now regenerated).
- Mutation matrix at da62f291e, scored by `go test -json` test name. Each cell restores a stale text; NOOP was clean in all four packages. 8/8 KILLED:
  - T1 unresolved per-zone line, T2 inert per-zone line (`TestScreenStatusBlocksDoNotClaimNoChecks_9415`);
  - T3 inert HELP;
  - T4 screen commit error;
  - T5 zone-pair from-zone literal, T6 global to-zone literal (their named subtests);
  - T7 syn-cookie paragraph;
  - T8 IPsec row back to Done.
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    ddb0df0d5. That tree is origin/master 580d959e0 merged with 6 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9619, #9415, #9478, #9601, #9365, #9685.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5838 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9415.md

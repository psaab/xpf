# #9325 -- five xpf-deploy defects (cohort)

- **Timestamp**: 2026-09-10
  - **Action**: Baseline `make selftest` before any change: passed=165
    skipped=0 failed=0, SELFTEST-EXIT=0.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Items 1 and 3, plus the incus extra.
    - A missing or unrunnable virsh now reads UNKNOWN, not ABSENT: virsh is a
      client, so a restricted PATH says nothing about libvirtd.
    - Both libvirt teardowns re-probe after destroy and undefine, and unlink
      only on an affirmative ABSENT. undefine of a running domain succeeds and
      leaves it running as a transient domain, and a failed destroy is also
      what a stopped domain returns.
    - _incus_state gives incus the same three states. Absence is read from
      incus's own wording, measured on this host for instance, image and
      network ("Instance not found", "Image ... not found", "Network
      interface ... not found").
    - destroy_incus removes the day-0 drive only after an affirmative ABSENT,
      re-probing after delete. _incus_exists keeps its non-destructive
      callers' behaviour.
  - **File(s)**: scripts/deploy/xpf-deploy.py
- **Timestamp**: 2026-09-10
  - **Action**: Item 2. `_qcow2_backing_file` now raises _ProbeIndeterminate
    when qemu-img cannot be run, instead of returning "no backing". The
    measured table is in the docstring, in docs/distribution.md, and in the
    replaced cell: only "golden present + a sibling overlay" changes, from
    proceed to refused.
  - **File(s)**: scripts/deploy/xpf-deploy.py, docs/distribution.md
- **Timestamp**: 2026-09-10
  - **Action**: Item 5, and the asymmetric-cleanup extra.
    - The non-privileged arm creates its temp with mkstemp in the images
      directory and writes it through that descriptor, so no path is resolved
      after O_EXCL creation. It refuses to rename a temp whose inode changed,
      and it removes the temp on any exception, an interruption included.
    - The sudo arm uses a random temp name and removes its root-owned temp when
      install or mv fails.
    - The issue's strace refutes a sudo `install` symlink primitive, so none is
      claimed.
  - **File(s)**: scripts/deploy/xpf-deploy.py
- **Timestamp**: 2026-09-10
  - **Action**: Item 4.
    - `fetch` verifies the signed SHA256SUMS, and if it lists the provenance
      sidecar, fetches the sidecar and reads `validated` from VERIFIED bytes,
      before the image is downloaded.
    - `false` refuses unless --allow-unvalidated is passed. A listed sidecar
      the mirror withholds is refused regardless of the override. A release
      that lists no sidecar, or a sidecar without the field, warns.
    - `image-roll` reads the field from the manifest it already verifies, with
      the same override.
    - Existing fetch fixtures list no sidecar, so they take the warn path
      unchanged.
  - **File(s)**: scripts/deploy/xpf-deploy.py, docs/distribution.md,
    docs/image-validation.md, docs/in-place-upgrade.md
- **Timestamp**: 2026-09-10
  - **Action**: Tests.
    - scripts/deploy/test_xpf_deploy_cohort_9325.py: 29 tests, 17 defect cells
      and 12 controls.
    - Inverted, with the refuted rationale replaced:
      test_qemu_img_absent_is_indeterminate_9325 (disk) and
      test_missing_virsh_is_unknown_9325 (robustness).
    - Changed: test_interrupted_replace_leaves_the_old_golden_intact injects at
      copyfileobj, the new copy primitive, and now also asserts no stray temp.
    - Negative control, the cohort file run against HEAD's scripts/ tree: all
      17 defect cells red, none green. 7 of 12 controls green. The other 5
      drive API or messages that do not exist in the old source
      (_incus_state; the fetch provenance lines).
    - Every scripts/deploy/test_xpf_deploy_*.py passes.
  - **File(s)**: scripts/deploy/test_xpf_deploy_cohort_9325.py,
    scripts/deploy/test_xpf_deploy_disk.py,
    scripts/deploy/test_xpf_deploy_robustness.py
- **Timestamp**: 2026-09-10
  - **Action**: The libvirt connection-URI extra is split out as #9669. The
    obvious fix, pinning qemu:///system, would itself read an existing
    session-scope domain as absent and unlink its disk, so the fix needs a
    design choice.
  - **File(s)**: docs/deploy-quickstart.md (the teardown refusal is documented)
- **Timestamp**: 2026-09-10
  - **Action**: `make selftest` on commit 7ee2431da: passed=166 skipped=0
    failed=0, SELFTEST-EXIT=0. That is the baseline of 165 plus the new cohort
    file.
  - **File(s)**: docs/log/9325.md

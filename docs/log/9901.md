# #9901 cohort log — userspace-dp packet identity (lane Eng9901)

Branch `fix/9901-packet-identity`, worktree
`.claude/worktrees/9901-packet-identity`, base `1fe6fa0aa`.
STATUS WHEN WRAPPED (budget): 3/5 implementing members COMMITTED + green;
F-073 PLAN-KILLED with evidence; F-074/F-077 fully designed + adjudicated,
NOT implemented. No PR opened. Resumption plan at the bottom.

## STEP-0 (all verified live on base 1fe6fa0aa — file:line proof)

| Member | Verdict | Proof |
|---|---|---|
| F-010 | LIVE, FIXED | `fragment_assoc/mod.rs:623-624` re-stamped `deadline_ns`/hit; no `created_ns`; `FRAG_TTL_NS`=2s (`:168`); fences present (`:601-604` gen, `:619-622` owner-RG). |
| F-072 | LIVE, FIXED | `screen/extract.rs:342` own `0..8` walk vs `walk_ipv6_ext_chain` (`inspect.rs:156-288`); agreement by one Dest-Opt test (`screen/tests.rs:6194`) only. |
| F-073 | LIVE residual, PLAN-KILLED (wire vacuous) + NEEDS_DESIGN (auth) | V1a/V1b/V2 gates present (`fabric.rs:243-360`); residual documented (`:234-236`); `routing_table` inert (shim `:1019`, Default, guard test `:2861`). |
| F-074 | LIVE, DESIGNED | `mtu==0→Forward` documented fail-open (`icmp_ptb.rs:105-108,123-124`); no unknown-MTU counter (grep clean); per-zone-only buckets (`icmp_ratelimit.rs:28-33`). MTU SSOT already landed (#2300/#2331/#2684/#2845). |
| F-075 | LIVE, FIXED | Shim `parse_ipv4` (`lib.rs:1541-1604`) never read `iph[2..4]`; fail → `drop_degraded_transit` (always DROP). |
| F-077 | LIVE, DESIGNED | Quote bounds follow declared len (`parse.rs:112-128,245-259`); no per-session error limit; outer unauthenticated. |

## Commits (all green at commit time)

- `86fb26875` F-010: `created_ns` + 10s absolute bound in both prune sites
  (`frag_entry_live`), `FRAG_MAX_LIFETIME_EVICTIONS` + full Rust status
  vertical + fixture regen. Tests: continuous-stream repro (RED pre-fix),
  reinstall-restart, reclaim-frees-slots (RED pre-fix). 5x green;
  suites: frag 244, fabric_zone 15, nat64 185+33.
- `e7ed89d2a` F-072: shared `IPV6_GENERIC_EXT_HEADERS` const (predicate
  delegates), extractor guards on it + full-set `debug_assert`; new
  `screen/tests_9901_walker_parity.rs` (256 values × 2 shapes × every
  truncation; R1 exact fail-closed, R2 TCP resolution; source-pin).
  Mutation check (drop 60 from guard) REDS both sweeps. 5x green;
  screen 295, frame 456.
- `e953344bf` F-075: `userspace-xdp/src/ipv4_len_gate.rs` (pure, host+
  BPF); shim refuses `total<ihl`, scalar-bounds L4 reads by declared end
  (reads stay on real `data_end` — verifier rejects scalar substitution,
  measured); first frags tuple-tolerant w/ sentinel punt; extractor gate;
  `screen/tests_9901_len_gate.rs` parity sweep + call-site pin; `.o` +
  manifest regenerated. Verifier PASS 567607 insns (+1623, headroom 43.24%).
  Gate-removal mutation REDS sweep. 5x green; screen 297, frame 456, shim_
  pins 31. Fixture fix: minimal-header test now sets total=20 (total 0 is
  malformed, not minimal).

## F-073 PLAN-KILL (parent ACCEPTED, no overrule)

SSOT wiring is VACUOUS: derived domain is a pure function of keyed inputs
(direct: one domain per logical ifindex, and logical ifindex is in the key,
so different VRFs already differ pre-fix and the repro is unsatisfiable;
fabric: unambiguous zones imply one instance, ambiguous resolve 0 for all
VRFs per `interfaces.rs:778-780`, plus encoded/unencoded-halves split risk).
True separation needs origin identity on the wire → protocol change →
parent-filed follow-up #10105 (quote in PR body). "Remove" declined:
zero-value churn, and `meta.routing_table` is the natural reserved seat for
#10105. NO code change; docs stay as-is (inertness honestly documented).

## F-074 design (adjudicated, NOT implemented)

1. Unknown-MTU counter: `EGRESS_MTU_UNKNOWN_FORWARD_TOTAL` static in
   `icmp_ptb.rs` (`pub(in crate::afxdp)`); bump in `compute_forwarded_egress_ptb`
   (`tx/dispatch/mod.rs`, inside `if let Some(l3)`) when `mtu == 0`
   (predicate: mtu==0 AND l3 readable; excludes unparseable-frame arm).
   Rust vertical: status.rs getter + helpers + protocol/status.rs field +
   lifecycle literal + fixture regen (mirror F-010 files). Dispatch-level
   repro in `tx/dispatch/tests/ptb.rs` using `large_udp_v4_frame`,
   `forwarding_for_ptb`, `run_ptb_dispatch_with_forwarding` (see file head):
   unknown-tunnel-kind decision → forwarded + counter delta 1; severing reds.
2. ZoneLimiter (per-source-fair, hierarchical, lock-free, bounded) in
   `icmp_ratelimit.rs`: `{ aggregate: TokenBucket, src_tags: [AtomicU64; 64],
   src_slots: [TokenBucket; 64], overflow: TokenBucket }`.
   Slot = `(seed ^ addr_mix) % 64`, seed = `crate::hot_hash_seed::hot_path_hash_seed()`;
   tag claim = CAS, NO bucket reset on takeover (colliders share sustained
   rate; tag is hint-only). Gate order source-FIRST then aggregate (#5567:
   source-deny consumes nothing shared). Budgets: per-source 100/s + burst
   100 (fixed consts); zone 1000/1000; overflow 100/100 for None src.
   Fallback: per-reason `LazyLock<ZoneLimiter>` statics (precedent:
   `nat/source/match_rules.rs`). `ForwardingState` maps →
   `FastMap<u16, Arc<ZoneLimiter>>`; populate in `forwarding_build/zones.rs`;
   `generated_error_bucket` returns limiter; `reset_bucket_for_test` resets
   all sub-buckets (mirror TAT store); arrival helper for TE tests.
   Signatures: `allow_generated_error_zoned[_at](…, from_zone_id,
   src: Option<IpAddr>, …)`, same for reject wrappers. Call sites key on
   META addrs (NOT frame bytes — PTB frame is post-NAT): TE `icmp.rs:320`
   → `Some(flow.src_ip)`; reject `reject_reply.rs:421` → `Some(flow.src_ip)`;
   PTB `dispatch/mod.rs:332` → `meta.l3_addrs().map(src)`.
   Tests: fairness (A drains src budget, B passes), aggregate preserved
   (5 fresh sources vs zone burst 4 → 5th denied), boundedness/collision
   sharing, fallback preserved; update ~15 existing call sites + fixtures.
   Memory: ~1.5KB/zone eager (document wire-max ~100MB accepted; realistic KBs).
   Docs: update `docs/generated-reply-rate-limit.md`.

## F-077 design (adjudicated, NOT implemented)

1. Floor: `MIN_QUOTED_L4_BYTES = 8` in `parse_embedded_v4/v6`, TCP/UDP/
   ICMP(v6) arms ONLY (where bytes are read; GRE/else unchanged), required
   within BOTH declared end and captured bytes, gated on outer-atomic
   (thread `outer_atomic: bool` from callers reading outer frag_off;
   outer fragments keep pre-fix adequacy). Floor-refusal counter.
   Repro: 4-byte TCP quote in atomic outer parses pre-fix, refused post-fix.
2. Per-session GCRA (NOT 1s min-interval — breaks mtr 30/s + traceroute
   multi-quoter same-id sessions): burst 64 / rate 64/s in ONE u64 TAT
   (`SessionEntry.last_icmp_error_tat`, init 0 = full by construction).
   `SessionTable::note_icmp_error_delivered(key, now) -> bool`; thread keys
   from caller-held values (`fwd.key`/locals/`ResolvedSessionLookup.key` —
   NEVER widen `SessionLookup`); shared/peer matches via bounded side table
   INSIDE `SessionTable` (`SessionKey→TAT`, cap 1024, stalest-eviction, no
   signature churn). Insert after successful match in `nat_match_v4/v6` +
   `nat64_match`. Node-local scope (document; entry not on HA wire).
   Suppression counter. Repros: mtr-burst (30 same-session <1s all pass),
   flood (1000 → ~64 + counter), PTB-pair, same-router.
   Honest residual: single-shot forgery unstoppable without ICMP auth.

## Metrics batch (ONE final Go commit for all 4 counters)

Rust verticals land per-member (additive: serde `default`, no
`DisallowUnknownFields` on Go decode, coverage test is Describe⊇Collect
only — verified). Go commit adds: `protocol_status.go` fields +
`pkg/api/metrics_descriptors_userspace_drops.go` (or fitting emitter)
descriptors + emitter wiring + `metrics_test.go` rows + emitter-count bumps.
Counters: `frag_max_lifetime_evictions_total` (Rust done, F-010),
`egress_mtu_unknown_forward_total`, `embedded_quote_subminimal_refused_total`,
`embedded_error_per_session_suppressed_total` (names per repo idiom).
Go files: coordinate with Eng9899 first (owns Go config/appid; metric files
unaffected per their report, but re-check at the time).

## Notes for resumer

- Reviews: hostile + polite plan reviews ran; adjudication merged (KILLs:
  F-073 wire vacuous — upheld; F-077 as-specified — redesigned, see above).
- `origin/master` moved +1 (`5f75d012a`, Go-only, no conflict): rebase at scrub.
- Pre-existing base failure (NOT this lane, verified on clean `1fe6fa0aa`
  scratch worktree): `additive_wire_exhaustive_literals_only_ever_decrease_7689`
  (NeighborSnapshot +3 over ceiling). Do not chase; note in PR.
- Peers: Eng9902 (screen/ shared; message before ANY screen/ edit — protocol
  works, they ack fast); Eng9900b (main_tests.rs committed hunks — rebase
  around if touched); Eng9637d/Eng9899 Go-only, no overlap.
- BPF rebuild: `make generate-userspace-xdp` (nightly-2026-05-23, verifier
  gate); commit regenerated `.o` + manifest with shim changes. Verifier
  lesson: NEVER pass a computed scalar as `data_end` — scalar-gate first,
  read on the real end.
- Remaining: implement F-074 → F-077 (repro-RED → fix → 5x → suites, one
  commit each) → Go metrics commit → rebase → scrub → PR (`Closes #9901`,
  quote #10105 for F-073, test table) → Copilot review → STOP.

# #9683: `incus exec` in a stdin-fed loop swallows the loop's input

- **Timestamp**: 2026-09-11T04:40Z
- **Action**: diagnosis. The combined #9673 + #9675 gate failed the post-deploy primary
  reassert three times in a row, the last on plain master 906705ca7.
  - Both nodes' journals: the reassert issued `failover reset` and the transfer request for
    rg=0 only. No rg=1 or rg=2 command reached either daemon.
  - The transfer is healthy. `request chassis cluster failover redundancy-group 1 node 0` by
    hand returned "transfer committed", and RG1 was on node0 within 10 s.
  - The parser is not the cause: `deploy_rolling_rg_ids` prints `0 1 2` from the captured
    status.
  - Loop repro on fw0 with a read-only `cli -c "show version"`: 1 of 3 iterations as
    shipped, 3 of 3 with stdin detached, in 3/3 trials each.
  - `xpf_cluster_build_probe` as shipped printed fw0 only. The same loop with
    `incus exec -n` printed fw0 and fw1.
  - Sweep: every `while read` loop under test/incus and scripts whose stdin is redirected
    and whose body calls incus without detaching stdin. It found three loops: the
    deploy-lib transfer loop, the deploy-lib pin-clear loop, and the
    cluster-build-identity probe. Positive control: it flagged the two known deploy-lib
    loops.
- **Action**: filed #9683. Worktree `/var/tmp/wt-9683-incus-exec-stdin` on origin/master
  ff4171851, branch `fix/9683-incus-exec-stdin`.
- **Action**: change.
  - `incus exec -n` at the three sites, each with a comment naming the failure.
  - `deploy-lib-selftest.sh`: the `incus()` mock parses leading flags and drains stdin
    unless `-n` is given, like the real client. The drain reads only input already
    present, and never a terminal, so it cannot block.
    - The existing per-RG sequence cell (`test_reassert_issues_reset_transfer_reset_per_rg`)
      and the peer-pin count cell (`test_reassert_clears_the_peer_manual_pin`) now see a
      missing `-n`.
    - The peer-pin cell's call-log patterns accept `exec -n <inst>`, so the count stays
      keyed on the call rather than on its flag spelling.
  - `cluster-build-identity-selftest.sh`: the mock script drains stdin the same way. New
    case 2b changes the SECOND node's binary and asserts that the probe samples both nodes
    and that the diff reports CHANGED. Case 2 changes node0, which a first-node-only probe
    still sees.
  - `docs/engineering-style.md`: a gotcha bullet under the shared-cluster protocol.
  - **File(s)**: `test/incus/deploy-lib.sh`, `test/incus/deploy-lib-selftest.sh`,
    `test/incus/cluster-build-identity.sh`, `test/incus/cluster-build-identity-selftest.sh`,
    `docs/engineering-style.md`, `docs/log/9683.md`
- **Timestamp**: 2026-09-11T06:20Z
- **Action**: gates on fa06d0425.
  - Mutation matrix (`mut9683.py`, a detached copy of fa06d0425). Each mutant removes one `-n`.
    The baseline passed, and 5/5 were KILLED, each by its named cell:
    - M1, the peer reset: the peer-pin count cell (peer resets=1, want 2).
    - M2, the node0 reset, and M3, the transfer: the per-RG sequence cell ("missing
      transfer/reset for: rg1"), plus the count cell.
    - M4, the pin clear: the sequence cell (no pin-clearing reset after the rg1 transfer), plus
      the count cell (node0 resets=3, want 4).
    - M5, the build probe: identity case 2b (sampled 1 of 2).
  - Self-tests on fa06d0425: deploy-lib 57 passed, 0 failed; build-identity ALL 10 CASES PASS;
    `make harness-census` exit 0; `make test-cluster-lock-lib` exit 0; `make selftest` 166 passed, 0 skipped, 0 failed.
  - The first `make selftest` run failed one leg, `test/incus/fairness_multi_sample_test.py`
    (`test_fairness_harness_preserves_raw_sample_artifacts`: its `cos-flows.tsv` held only the
    header). It ran concurrently with the mutation matrix. That file passed on its own on this head
    and on master ff4171851 (44 tests OK each). The re-run of `make selftest` on fa06d0425 passed.
    The leg touches nothing this change edits.
  - Cluster gate: ONE local merge `4ad6fb1bb` of origin/master `2c29dc14f`, this branch
    `fa06d0425`, PR #9673 `ed335688a` and PR #9675 `433a4623f`. Deployed under the cluster lock,
    with CoS re-applied.
    - Post-deploy reassert: "Re-asserted node0 primary for all redundancy groups (post-deploy,
      verified; fallback regime)".
    - Both journals show the reassert's resets for rg 0, 1 and 2: node0 at 06:06:55-57, and the
      peer's at 06:08:31-33 on fw1's clock. The three failing deploys showed rg=0 only.
    - `make test-failover`: 22 passed, 0 failed; iperf3
      20.8000 Gbps. Ledger row `18f6162cfd19d9d5.json`: verdict PASS, exe_check MATCH, build_git_sha = the
      merge.

# #9506 S3 spike — fence + divert coexistence proof

**Verdict: PROVEN-spike; S4 handoff required.** Kernel placement and fail-closed
coexistence proven on Linux 7.0.13. Daemon lifecycle machinery for the T12
permit/overlay/conntrack ACK cells does not exist; those cells are S4-owned
and unclaimed here.

## Measured evidence

- **Bridge step 0:** `ip link set st99 master br0` → `RTNETLINK answers: Invalid
  argument` (exit 2); `st99` unenslaved; control veth enslaves fine. Degraded
  branch taken per plan conditional; no bridge-xfrmi receipt claimed.
- **Priority map:** `xpf_ipsec_divert` forward/input in inet+bridge at `-175`;
  `xpf_transit_barrier` forward at `0`. `nft -j` shows no same-priority tie.
- **Leave-alone proof:** routed UDP without capture undelivered; inet fence
  counter `1 packet / 39 bytes`.
- **NFQUEUE ACCEPT proof:** real XFRM-decrypted packet reached queue `1101`;
  listener ACCEPT; packet undelivered; inet fence `2 packets / 80 bytes`.
  ACCEPT resumes traversal, does not bypass.
- **Bridge control proof:** bridged veth traffic undelivered; bridge fence
  `2 packets / 168 bytes` (fence leg only; xfrmi receipt unclaimed).
- **Host-bound proof:** real XFRM-decrypted host-bound packet reached queue
  `1102`; post-ACCEPT undelivered locally (capture before local input,
  fail-closed).
- **VRF premise:** `st99`→VRF: queue `1102` zero, master-name input counter
  `1 packet / 38 bytes`; forward fence advanced, queue `1101` zero.
  Confirms l3mdev substitution. Kernel-hook evidence, not daemon ACK.

## T12 mapping

Proven: unique queue provenance; `-175` before fence-0; no bypass;
ACCEPT-resumes-to-fence; leave-alone fenced; explicit bridge-degraded
branch; unattributed flush errors not bridge-classified; VRF premise.
S4-owned (machinery absent): install-failure publication/rollback; ACK
timeout + ambiguous post-commit; staged success/mixed-OPEN exclusion;
VRF publication/overlay/conntrack ACK; clear/retire + stuck-DOWN cells.

## Shipped layer

- `pkg/nftables/ipsec_divert.go`: inet/bridge installer, 4 provenance
  classes, priority guard, bridge preflight, combined flush, inet-only
  degraded branch, idempotent removal (genuine failures fatal).
- `pkg/nftables/ipsec_divert_test.go`: renderer/provenance, live netns
  dual-family install/readback, degraded classifier, flush-failure test.
- `pkg/nftables/netlink_installer.go` + daemon test seams: Installer surface.
- `pkg/config` advisory wording per r6 §7.1/§7.2 (this log's companion).

## Verification

`go test ./pkg/nftables ./pkg/daemon ./pkg/config` — passed.
`go test ./pkg/nftables -run '^TestIpsecDivert' -count=1` — passed.

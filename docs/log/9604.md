# #9604 — Reverse-triggered zone-policy re-derivation

Worktree `/home/ps/git/pi-xpf/.claude/worktrees/9604-reverse-rederivation`,
branch `fix/9604-reverse-rederivation`, base `origin/master 7ef226474`.
Full plan + raw reviews live on disk at `docs/pr/9604/` (uncommitted);
this file is the shipped history.

- **Timestamp**: 2026-09-13
- **Action**: Plan v1 (DRAFT): judge a stale REVERSE hit by its FORWARD
  companion's tuple and zones under the same generation stamp, revoke both
  halves on non-permit. STEP-0 verified still present at this base — GATE 1
  returns None for every reverse hit, no other site re-derives for a reverse
  hit. NOT ALREADY_FIXED. Scope framed honestly: security-posture fix,
  steady-state cost zero (cold path only), PLAN-KILL acceptable.
- **File(s)**: docs/pr/9604/plan.md (uncommitted)

- **Timestamp**: 2026-09-13
- **Action**: Round-1 hostile plan review. Codex PLAN-NEEDS-MAJOR, two
  blockers: (1) `is_peer_synced()` is the wrong ingress-provenance boundary
  (excludes SharedPromote) — use an explicit trusted-local-identity
  classification mirroring `arrived_on_the_admitting_interface`, disposition
  for every origin; (2) NAT64 protocol handling — judged protocol must come
  from the forward key, ICMP gate after companion resolution. Plus: genuine
  lone-reverse + foreign-reverse cells, mandatory SNAT/DNAT/NAT64 cells,
  hit-only stamping preferred. GLM PLAN-NEEDS-MINOR (B1–B5): §5.3 elision
  mechanism false (degenerate miss key, not companion==canonical); ICMP gate
  must read the forward entry's protocol; routing-domain-asymmetric decline
  + stored-vs-reresolved to-zone belong in §10; SNAT pair-revoke cell
  mandatory. Plan v2 folds all of it.
- **File(s)**: docs/pr/9604/plan.md, docs/pr/9604/reviews/codex-r1.txt,
  docs/pr/9604/reviews/glm-r1.txt (uncommitted)

- **Timestamp**: 2026-09-13
- **Action**: Round-2 hostile plan review. Codex PLAN-NEEDS-MAJOR: provenance
  table + protocol design corrected, but conditional dual-stamping still
  lacks equivalence (fabric-arrived forward packets judge recorded zones;
  zone-based ownership admits other-interface arrivals) — hit-only stamping
  required; matrix still leaves translated-src, src-port, DNAT-port,
  SharedPromote-live-resolution, and cache-eviction shapes unpinned.
  GLM PLAN-NEEDS-MINOR (R2-1–R2-3): wrong-pairing is shape-dependent
  (combined SNAT+DNAT DOES elide — state both shapes); SharedPromote cause
  is a fail-open decline, not wrong-zone resolution (ifindex never synced,
  #6928); zone-membership-move cell missing (recorded-from-zone-everywhere
  shape). Plan v3 folds all of it: hit-only stamping, FromZoneSource enum,
  both pairing shapes, corrected promote narrative, zone-move detector,
  loud declines.
- **File(s)**: docs/pr/9604/plan.md, docs/pr/9604/reviews/codex-r2.txt,
  docs/pr/9604/reviews/glm-r2.txt (uncommitted)

- **Timestamp**: 2026-09-13
- **Action**: Round-3 (final) plan review. Codex PLAN-NEEDS-MINOR:
  architectural blocker closed by construction; three bounded test-contract
  corrections (source-port control, zero-identity pair, concrete
  cache/allocator proof + stamp-state assertions + G→G+1 zone-move form) —
  "No fourth design round is necessary." GLM PLAN-READY: every prior
  failure now stated correctly, hit-only strictly safer, matrix covers
  every verdict-relevant wrong shape. Plan v4 folds Codex's corrections
  with no re-dispatch per reviewer instruction. Review CLOSED, proceeding
  to implementation.
- **File(s)**: docs/pr/9604/plan.md, docs/pr/9604/reviews/codex-r3.txt,
  docs/pr/9604/reviews/glm-r3.txt (uncommitted)

- **Timestamp**: 2026-09-13
- **Action**: Implemented the core: stale reverse hit resolves the forward
  companion via `reverse_session_key` with the reverse entry's own nat,
  re-asks zone policy for the forward tuple/protocol/zones through the
  shared cold judgment; non-permit revokes both halves through the existing
  pair teardown. Reverse pair never evaluated (structural); from-zone
  live-resolved for locally-authored origins, recorded for peer/fabric;
  ICMP gate on the judged family; hit-only stamping; revocation carries the
  judged triple; loud declines for degenerate-key/reverse-as-forward.
- **File(s)**:
  userspace-dp/src/afxdp/poll_descriptor/mod.rs,
  userspace-dp/src/afxdp/poll_descriptor/policy_revalidation.rs,
  userspace-dp/src/afxdp/poll_descriptor/session_hit_authority.rs,
  userspace-dp/src/session/README.md,
  userspace-dp/src/session/install.rs, userspace-dp/src/session/mod.rs

- **Timestamp**: 2026-09-13
- **Action**: Fail-on-revert matrix through the real poll body: 27
  detectors + 34 controls. First cut 25 of 27 green; the two reds diagnosed
  fixture-side and closed with no production-code change — SNAT
  pool-release (allocator state is per-build, invisible across snapshot
  rebuilds by construction; cell pins key/nat pairing instead, release
  covered from both hit directions by the #5622 cell) and the cache cell
  (restructured around gen-7 pinned txn, gen-0 installs, reverse-slot
  seeding; both-tuple redrive proves no descriptor outlives the session).
  Final: 27/27 + 34/34 green, existing cells unmodified.
- **File(s)**:
  userspace-dp/src/afxdp/tests_policy_revocation_8356.rs

- **Timestamp**: 2026-09-14
- **Action**: §12 fold — full `cargo test --release` reds exactly one cell
  outside the matrix:
  `fabric_punt_seed_admits_the_peers_return_7770` (two-sided causality;
  judgment input measured at (from lan, to 0, egress_ifindex 21)). The punt
  seed's stored egress is the fabric TRANSPORT, so live to-zone hits the
  unknown sentinel → default-deny → revoke; admission adjudicated the seed
  on the pre-redirect egress zone and recorded it. Fix: judge the recorded
  zone whenever the judged entry's disposition is FabricRedirect
  (`to_zone_override`), on both paths — the forward-hit form is the same
  latent bug. Recorded 0 evaluates as the live 0 it replaces. #7770 is the
  fail-on-revert guard; no new cell; matrix stays 27 + 34.
- **File(s)**:
  userspace-dp/src/afxdp/poll_descriptor/policy_revalidation.rs

- **Timestamp**: 2026-09-14
- **Action**: §13 fold — parent review GPT NEEDS-MAJOR (HIGH): the
  from-zone table keys on origin alone, but fabric-admitted flows install
  as ForwardFlow with the peer's ORIGINAL zone and identity (0, 0), so
  every fabric-admitted reverse-only flow declined via LiveIfindex{0} and
  kept its stale verdict. Fix: key the locally-authored arm on
  !fabric_ingress, fall fabric provenance into RecordedZone (mirrors the
  forward fabric gate); HARD CONSTRAINT wording tightened. Three new
  production-admitted cells (deny + reject detectors, permit preservation
  control), all red with the override removed — matrix 27 → 29 detectors,
  controls 34 → 35. GLM F1: now-false "re-derivation declines reverse packets"
  comments updated.
- **File(s)**:
  userspace-dp/src/afxdp/poll_descriptor/policy_revalidation.rs,
  userspace-dp/src/afxdp/tests_policy_revocation_8356.rs,
  pkg/daemon/daemon_policy_invalidate.go,
  userspace-dp/src/afxdp/session_glue/README.md

- **Timestamp**: 2026-09-14
- **Action**: History scrub per parent-approved spec: 9 commits (4 plan
  + core + 2 test + 2 fold) regrouped into 4 shippable commits
  (companion judgment / regression cells / FabricRedirect zone /
  fabric-ingress override) + this log. `docs/pr/9604/` (plan + 6 raw
  reviews) stays on disk uncommitted. Verified each regrouped tree
  byte-identical to its source commit on all shippable paths.
- **File(s)**: docs/log/9604.md

# #9590 — the go vet gate in make test-go was scoped to pkg/flowexport while the tree is vet-clean

## 2026-09-10 — Action: re-verify at base, widen the gate, control it

- **File(s):** `Makefile` (the `test-go` vet line and its comment), `docs/log/9590.md` (new).
- **The spec.** The issue body; it has no comments.
- **Base:** origin/master 7b6da7bde.
- **The change.**
  - `$(GO) vet ./pkg/flowexport/...` becomes `$(GO) vet ./...`.
  - The stale justification is deleted. It named two diagnostics, in `cmd/cli` and
    `pkg/cli`, that no longer exist.
  - In its place, a short comment says the gate is tree-wide because the tree is
    clean, and that any exception must be a named, reasoned carve-out rather than a
    quiet re-narrowing.
- **Positive control, bound to each Makefile's own expanded recipe line**
  (`make -n -o test-race-dp test-go`), not to a retyped command:
  - The widened Makefile expands to `go vet ./...`, and the base Makefile to
    `go vet ./pkg/flowexport/...`.
  - Clean tree, widened line: rc 0, 0 diagnostic lines.
  - A planted `pkg/wgkey/zz_vet_control_9590.go` passes a struct holding an
    `atomic.Uint64` by value: the #2224 class, outside `pkg/flowexport`. It builds (rc 0).
  - **Widened line with the plant: rc 1**, naming it: `vetControl9590Copy passes lock
    by value: ... contains sync/atomic.Uint64 contains sync/atomic.noCopy`.
  - **Base (narrow) line with the plant: rc 0**, blind to it.
  - The plant was removed, and the tree was back to the Makefile change only.
- **Blast radius: every unmerged fix branch.** There were no open PRs, so these are the
  lane's own branches. Each head ran `go vet ./...` in a throwaway detached worktree:
  - #9582 at 369a16d33: rc 0, 0 diagnostic lines;
  - #9509 at 853dd82d1: rc 0, 0 diagnostic lines;
  - #9508 at 391097b70: rc 0, 0 diagnostic lines;
  - #9551 at c7776639f (parked): rc 0, 0 diagnostic lines;
  - #9507: skipped, because it had already landed (its merged head is part of master).

  No open branch carries a vet diagnostic, so the widened gate does not red a lane
  mid-landing.
- **make test** on the committed head: rc 0 at e8ce430b6, counted from the log:
  - 77 Go packages `ok`, 0 `FAIL` lines, 0 panics;
  - 11 Rust `test result:` lines: 5791 passed, 0 failed, 6 ignored.
  - **The widened recipe line ran inside it.** `go vet ./...` is echoed at log line 14,
    and the next lines are the Makefile's own echoed comments. Vet printed nothing, and
    the recipe went on to the test legs.
- **No mutation matrix, and no cluster gate.**
  - The change is one recipe line. Its mutant is the base Makefile's narrow line, and
    the positive control above ran exactly that pair.
  - It touches no cluster, VRRP, session-sync or failover code, so `make test-failover`
    does not apply.
  - The landing rule's C2 (HA-path delta since the cluster gate) therefore has nothing to
    bind. The pre-push check is given this branch's rebase base as its cluster base, so
    C2 is vacuous by construction, and C1 and C3 do the work. C3 includes `go vet ./...`
    on master alone and on the merged tree.

## 2026-09-10 — Action: landing-rule verdict (recorded after the check)

- **File(s):** `docs/log/9590.md`. This amend changes only this log, so the verdict still binds the code.
- **Verdict, verbatim:**

```
T_test=7b6da7bde T_cluster=4363e0168 HEAD=b365f2498 origin/master=4363e0168 matrix_head=b365f2498
master_delta (T_test..origin/master): 23 files; mine (own change, from merge-base 4363e0168): 2 files
mutated files changed since the matrix head: none
C1 intersection (since T_test): empty
C2 HA-path files changed since the cluster gate: none
C3: merged tree must build with no NEW diagnostics (differential against master's vet output)
master alone: go vet rc=0 (0 output lines), cargo check rc=0
merged: go build rc=0; NEW vet diagnostics: none; cargo check rc=0; NEW cargo errors: none
Rust control: injected break on the merged tree: cargo rc=101 (must be non-0), error names the symbol: 2 -> PASS
Go control: build rc=0 (must be 0); NEW vet diagnostics: 1, in the control file: 1 -> PASS
merge-check worktree and target removed: yes
DECISION: LAND by merge commit without re-gating (C1, C2, C3 hold; both controls refused as they must)
```

# #10487 — refactoring-audit accepted entries outlive their floors

- **Timestamp**: 2026-09-21
  - **Action**: drove the issue from research through implementation. The
    accepted-file census at `origin/master` `b71c52d60` found 12 entries:
    2 dead (16.7%), 10 live, and 0 missing paths. The dead entries were
    `pkg/api/metrics_userspace.go` at 529 LOC against [REFACTOR] 2000 and
    `pkg/vrrp/instance.go` at 825 LOC against [WATCH] 1500.
  - **Evidence**: `isAccepted` matched path+tier only; the touched gate had
    no tree-global existence or LOC re-check. `audit_loc` was verified as
    `wc -l`, and `audit_is_audited_path` as pattern-only, so deleted paths
    could also remain accepted without a file.
  - **STEP-0**: fetched `origin/master` (still `b71c52d6093f23a7d9c9cca12d00cb40b3dffa8d`); actual `gh pr list` searches for merged #10487, merged refaudit-floor terms, and open #10487 returned no matches.
  - **File(s)**: docs/pr/10487-refaudit-floors/plan.md

- **Timestamp**: 2026-09-21 (plan round 1)
  - **Action**: committed the initial adversarial-ready plan as
    `8763e6d3c` (`refaudit: plan accepted-entry floor recheck (#10487)`).
    The design selected a tree-global, fail-closed floor check plus pruning;
    no production code was proposed.
  - **Review**: round-1 reviewer A returned PLAN-NEEDS-MINOR and reviewer B
    returned PLAN-NEEDS-MAJOR. The design premise was confirmed, but the test
    plan needed committed re-add/deleted-path/metric-parity pins, the no-CI
    wiring needed correction, and the accepted-file/style wording needed a
    same-PR prune rule.
  - **File(s)**: docs/pr/10487-refaudit-floors/plan.md

- **Timestamp**: 2026-09-21 (plan round 2)
  - **Action**: folded every round-1 finding into
    `d4d3ae3d3` (`refaudit: close plan review findings (#10487)`). The plan
    resolved the source questions: `audit_is_audited_path` is pattern-only;
    `audit_loc` counts newline bytes; Go parity uses `bytes.Count`; missing
    paths fail closed; the committed tests cover re-add, rename/delete, and
    newline/CRLF/empty fixtures; enforcement is `make test-go -count=1`, not
    CI.
  - **Review**: both delta reviewers dual-confirmed PLAN-READY with zero
    regressions and empty residuals. The minimum live margin remained 41 LOC
    (`maps_sync.go` 2041 vs its 2000 floor).
  - **File(s)**: docs/pr/10487-refaudit-floors/plan.md

- **Timestamp**: 2026-09-21 (implementation)
  - **Action**: committed `528359549` (`refaudit: prune dead accepted entries
    (#10487)`) and removed the two stale accepted lines. Committed
    `91656c8c7` (`refaudit: add live accepted-entry checks (#10487)`) with
    `TestAcceptedEntriesAreLive`, explicit missing/read failures, tier-floor
    and demotion guidance, a committed re-add negative control, deleted/
    renamed-path coverage, and Go-vs-`classify.sh loc` parity fixtures.
  - **Action**: committed `90dc001b0` (`refaudit: document accepted-entry
    lifecycle (#10487)`) updating the accepted-file header, the two-gates
    documentation, engineering-style guidance, and the plan's manual
    enforcement residual. The docs now say to prune/demote in the same PR;
    they also state that no CI exists and a skipped `make test-go` can leave
    master red until the next developer's run.
  - **Action**: committed `1dc0f589b` (`refaudit: pin accepted-entry
    boundaries (#10487)`) to pin WATCH 1499/1500/1501, REFACTOR
    1999/2000/2001, WATCH above 2000, and unknown-tier fail-closed cases.
  - **Fold**: no production runtime code changed. The implementation is
    test-only plus accepted-entry/doc policy, and the review-plan findings
    were folded before implementation.
  - **File(s)**: docs/refactoring-audit-accepted.txt,
    pkg/refactoraudit/accepted_live_test.go, docs/refactoring-audit.md,
    docs/engineering-style.md, docs/pr/10487-refaudit-floors/plan.md

- **Validation**:
  - `GOCACHE=/dev/shm/gocache-10487 GOTMPDIR=/dev/shm go test -count=5 ./pkg/refactoraudit -run 'TestAcceptedEntriesAreLive|TestAcceptedEntryFloorBoundaries|TestAcceptedEntryReaddFails|TestAcceptedDeletedPathFailsClosed|TestGoLocMatchesAuditLoc'` — PASS.
  - `GOCACHE=/dev/shm/gocache-10487 GOTMPDIR=/dev/shm go test -count=1 ./pkg/refactoraudit/...` — the package has no new red; it fails at the pre-existing #6937 calibration `TestStructMetricIsTypesNotFields6937` (`CompileResult` 32 fields / 21 types versus its 20-type floor), the same base failure recorded during plan review.
  - Firsthand red-on-revert: temporarily re-added a [WATCH] `pkg/vrrp/instance.go` entry and ran `go test -count=1 ./pkg/refactoraudit -run '^TestAcceptedEntriesAreLive$'`; it failed naming 825 LOC below the 1500 floor. The accepted file was restored and the worktree is clean.
  - `git diff --check` — PASS.

- **Outcome**: implementation branch `fix/10487-refaudit-floors`; PR is opened after this final history commit with `Closes #10487`.

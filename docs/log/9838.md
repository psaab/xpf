# #9838 log

## A leaf-spelled interface or routing instance compiles nothing

- **Timestamp**: 2026-09-14
- **Action**: STEP-0, measured present at base `9603f2a5f` through
  `config.CompileConfig` (strict) and `config.CompileConfigLenient`,
  checking whether the compiled JSON contains the instance name
  (throwaway probe, deleted after use):
  - `interfaces { ge-0/0/0; }`: strict accepts, instance absent (bug).
  - `interfaces { ge-0/0/0 { } }`: strict accepts, instance present.
  - `routing-instances { ri1; }`: strict accepts, instance absent (bug).
  - `routing-instances { ri1 { } }`: strict accepts, instance present.
  - `security-zone trust;` / `trust { }`: both compile (control).
- **Action**: Cause. `compileInterfaces` skips every `IsLeaf` child
  (`compiler_interfaces.go:31`); `compileRoutingInstances` skips
  single-key leaves (`compiler_routing.go:561`); zones compile leaves
  via `namedInstances` exactly like their braced spelling. Hence the
  asymmetry. Not ALREADY_FIXED.
- **File(s)**: none (scratch probe)

- **Timestamp**: 2026-09-14
- **Action**: Tier SMALL, direction strict-refuse (acceptance allows
  either). Compiling the leaf would mint a new compiled shape and need
  wildcard-group parity per #9801 plus keyword guards against phantom
  interfaces from `apply-macro` / `traceoptions` leaves; refusing keeps
  every compiled shape unchanged and zones byte-identical.
- **Action**: 10-line brief posted to #9838 as a comment; no review
  rounds per SMALL tier.
- **File(s)**: none

- **Timestamp**: 2026-09-14
- **Action**: Fix. `validateBareLeafInstance9838` runs at the
  `runPreWalkGates` tail and refuses a single-key `IsLeaf` child of any
  top-level `interfaces` / `routing-instances` root, except known
  keywords (`nonInterfaceIfKeyword`, apply statements). Lenient
  downgrades to a warning via `lenientBareLeafInstance9838` (#1960).
  Scope is the bare leaf only: elided routing instances normalize
  pre-validation (#9620) and packed tails belong to #2419.
- **File(s)**: `pkg/config/compiler_bare_leaf_instance_9838.go` (new),
  `pkg/config/compiler_prewalk.go`, `pkg/config/compiler_opts.go`

- **Timestamp**: 2026-09-14
- **Action**: Tests. Config-level matrix (leaf vs braced x
  interface/RI, second-root rows, zone control, wildcard-group
  variants with body-carriage checks, keyword non-fire guards,
  lenient warn + absent checks); configstore `CheckText` matrix
  mirroring the issue's measurement. The #8787 `bare name is not an
  instance` subtest pinned the old contract (strict accepts, zero
  instances); updated to the #9838 contract (strict refuses, lenient
  still compiles zero instances) — the anti-phantom intent is
  preserved on the accepting path.
- **File(s)**:
  `pkg/config/compiler_bare_leaf_instance_9838_test.go` (new),
  `pkg/configstore/bare_leaf_instance_commit_9838_test.go` (new),
  `pkg/config/routing_instance_packed_8787_test.go`

- **Timestamp**: 2026-09-14
- **Action**: Validation. New cells: 6 config + 1 configstore, all
  pass; `-run '9838|TestPackedRoutingInstanceSurvives8787' -count=5`
  green. `go test ./pkg/config/ ./pkg/configstore/` green after the
  #8787 contract update (one failure before it, that subtest only).
  Full `go test ./...`: green except a pre-existing `docsref`
  `TestNoNewDanglingDocCitations` failure on lane 9604's citation
  (reproduces on the pristine base with this lane stashed; untouched
  by this change). No cluster run: strict-gate change with no new
  compiled shape and no dataplane touch.
- **File(s)**: none

- **Timestamp**: 2026-09-14
- **Action**: Parent review: GPT NEEDS-MAJOR (real, folded), GLM
  READY-advisory (notes 1 + 3 folded). The gate falsely refused BRACED
  input through hierarchical LoadMerge: FormatSet renders an empty braced
  container as a terminal `set` line (#9126), SetPath rebuilt it as
  `IsLeaf:true`, and the gate refused a spelling the operator never wrote.
  Reproduced pre-fix via throwaway probe (all four refused): braced
  `ge-0/0/0 { }` merge, braced `ri1 { }` merge, no-op `ge-0/0/0 { }` merge
  into a configured instance (leaf twin: terminal replay deduped leaves
  only), and hier-leaf merge.
- **File(s)**: none (scratch probe, deleted)

- **Timestamp**: 2026-09-14
- **Action**: Fix. SetPath builds a CONTAINER (reusing an existing
  same-key one, so the no-op merge is a no-op) when a flat path ends AT
  a named-instance slot: a schema wildcard with children and no value
  args (`ast_edit.go`, terminal branch). Junos `set` creates the empty
  container; it has no leaf at a container position. Scoped to wildcard
  slots on purpose: declared value carriers (`flow-server <addr>`,
  `next-hop <gw>`, `route <dst>`, `area <id>`, `filter F`,
  apply statements) stay terminal leaves, exactly as today.
- **Action**: Consequence, pinned and documented rather than hidden: the
  merge ingress NORMALIZES a hier leaf to the empty container (flat `set`
  cannot express leaf-at-container-position), so it commits there — the
  issue's first disjunct, and what Junos `load merge` does — while
  CheckText / LoadOverride still refuse the same text (second disjunct).
  A never-matching leaf twin is left for the gate: it means the bare
  spelling was actually authored. `set interfaces traceoptions` bare now
  rebuilds as the container the non-terminal and hier-braced spellings
  already build: no novel downstream shape (name-validity contract #6834
  unchanged). Deferrals stand: brace-elided instances stay with #2419,
  Junos-parity direction accepted as-is.
- **File(s)**: `pkg/config/ast_edit.go`

- **Timestamp**: 2026-09-14
- **Action**: Fold tests. SetPath cells (wildcard terminals build
  containers incl. quoted; reuse/no-op; declared terminals stay leaves
  incl. the ECMP next-hop carrier; flat terminals compile like braced);
  quoted-name + trailing-comment gate cells with quoted-braced controls
  (GLM note 3); LoadMerge controls for BOTH instance types (braced
  commits, no-op into configured commits, leaf normalizes — pinned);
  Store.Load + SyncApply no-brick halves with the CommitCheck over-reach
  guard, 7145 pattern (GLM note 1).
- **File(s)**: `pkg/config/setpath_wildcard_terminal_9838_test.go` (new),
  `pkg/config/compiler_bare_leaf_instance_9838_test.go`,
  `pkg/configstore/loadmerge_bare_leaf_instance_9838_test.go` (new),
  `pkg/configstore/bare_leaf_instance_no_brick_9838_test.go` (new)

- **Timestamp**: 2026-09-14
- **Action**: Validation (fold). 11 config + 10 configstore 9838 cells
  pass; named `-run
  '9838|TestPackedRoutingInstanceSurvives8787|TestEmptyContainerRoundTripsThroughSetView9126'`
  `-count=5` green. `go test ./pkg/config/ ./pkg/configstore/` green
  (config ~180s, configstore ~36s). Full `go test ./...`: green except
  the same pre-existing `docsref` 9604 failure (verified on the pristine
  base before the fold). No cluster run (unchanged rationale).
- **File(s)**: none

- **Timestamp**: 2026-09-14
- **Action**: Parent review delta: GPT NEEDS-MAJOR (real, folded), GLM
  READY. The wildcard-terminal dedup matched containers only, so replay
  onto a legitimate bodyless leaf twinned it: `groups { G; }` plus an
  empty merge refused as a duplicate group (#5180), and an ip-monitoring
  address leaf plus its twin compiled two targets whose weights the
  runtime sums independently. Reproduced pre-fix via throwaway probe
  (leaf=1 container=1 for groups G, monitor 10.0.1.1, and iface
  ge-0/0/0 alike).
- **File(s)**: none (scratch probe, deleted)

- **Timestamp**: 2026-09-14
- **Action**: Fix. Terminal replay at an instance slot is now
  IDENTITY-PRESERVING: a same-key node in either shape is the same
  statement (flat `set` cannot spell the difference), so it is reused
  and nothing changes. Container creation is scoped to the #9838
  subjects (top-level `interfaces` / `routing-instances`,
  pointer-compared, so group-contained fragments qualify and zone-member
  / RA stanzas do not); every other slot keeps the terminal leaf it
  always built. A bare #9838 leaf survives the replay alone and still
  refuses — the rule preserves refusal evidence, it never converts or
  deletes the leaf.
- **File(s)**: `pkg/config/ast_edit.go`

- **Timestamp**: 2026-09-14
- **Action**: Delta tests. SetPath identity cells (non-subject terminals
  stay leaves; replay onto legitimate leaf / configured container /
  bare #9838 leaf each leaves exactly the one node, and the #9838 leaf
  still refuses); mixed-ingress store controls (empty-group and
  configured-group merges stay single with the body intact; monitor
  merge leaves the compiled targets byte-identical; bare-leaf override
  plus empty merge still refuses both stanzas). Also folded: the
  LoadOverride-side refusal cells from the freeze round.
- **File(s)**: `pkg/config/setpath_wildcard_terminal_9838_test.go`,
  `pkg/configstore/loadmerge_bare_leaf_instance_9838_test.go`
- **Action**: Validation (delta-2). 13 config + 12 configstore 9838
  cells pass; named `-run
  '9838|TestPackedRoutingInstanceSurvives8787|TestEmptyContainerRoundTripsThroughSetView9126'`
  `-count=5` green. `go test ./pkg/config/ ./pkg/configstore/` green
  (config ~168s, configstore ~33s). Full `go test ./...`: green except
  the same pre-existing `docsref` 9604 failure (verified on the pristine
  base before the first fold). No cluster run (unchanged rationale).
- **File(s)**: none

- **Timestamp**: 2026-09-14
- **Action**: Parent review delta2: GPT NEEDS-MAJOR P2 (real, folded),
  GLM READY. Reachability FIRST: P2 claims a no-op replay twins across
  disjoint top-level roots. Verdict REACHABLE — the parser appends
  without merging (`parser.go:345-349`, pinned by the 5675 two-root
  precondition), and a raw LoadOverride splices that shape straight
  into the candidate. Reproduced pre-fix via throwaway probe: empty
  merge of a root-2 target refused as `duplicate interface "ge-0/0/0"`
  (#5180), and the RI analogue committed ri1 TWICE.
- **File(s)**: none (scratch probe, deleted)

- **Timestamp**: 2026-09-14
- **Action**: Fix. Before creating the container, a top-level terminal
  (`len(path)==2`, so deeper paths never consult top-level roots)
  resolves identity across every same-named root in either shape; a
  match reuses without touching — no authored-leaf conversion or
  deletion, ever (`ast_edit.go`, #9838-subject branch).
- **File(s)**: `pkg/config/ast_edit.go`

- **Timestamp**: 2026-09-14
- **Action**: Delta-3 tests. SetPath cross-root cell (asserts the
  two-root precondition loudly, 5675 pattern; replay leaves exactly the
  one configured node across roots, both stanzas); store
  override→empty-merge controls for later-root targets in BOTH stanzas
  (commit OK, unit/type intact, RI count exactly one).
- **File(s)**: `pkg/config/setpath_wildcard_terminal_9838_test.go`,
  `pkg/configstore/loadmerge_bare_leaf_instance_9838_test.go`

- **Timestamp**: 2026-09-14
- **Action**: Validation (delta-3). 14 config + 13 configstore 9838
  cells pass; named `-run
  '9838|TestPackedRoutingInstanceSurvives8787|TestEmptyContainerRoundTripsThroughSetView9126'`
  `-count=5` green. `go test ./pkg/config/ ./pkg/configstore/` green
  (config ~174s, configstore ~35s). Full `go test ./...`: green except
  the same pre-existing `docsref` 9604 failure (verified on the pristine
  base before the first fold). No cluster run (unchanged rationale).
- **File(s)**: none

# #10146 — promote the stale #8341 failover pin

Worktree `fix/10146-pin`, based on `origin/master`.

- **Timestamp**: 2026-09-18
- **Issue review**: Read the full open #10146 body and all comments before editing. The issue has no comments. It records that the pool-mode TCP flow already connects after the role change, so the `known_gap 8341` cell fails every run with the promotion message.
- **Change**: Promoted the post-role-change pool-mode TCP data-path check in `test/incus/test-failover.sh` to a normal assertion. The PASS arm requires the positive `iperf Done` result marker; every other result is a FAIL and includes the last five log lines. Removed the now-unused `known_gap` helper, counters, and summary tally. The canonical failover summary remains one `passed, ... failed` line, now counting the promoted assertion.

## Live verification

- **Lock discipline**: Ran under the #1875 exclusive cell:
  `XPF_CLUSTER_LOCK_TIMEOUT=300 ./test/incus/with-cluster.sh "#10146 positive pool assertion" -- make test-failover`
- **Ledger**: `test/results/ledger.d/16e9c5a84be28bbc.json` (removed after recording evidence; generated shards are not source changes).
- **Result**: `test-failover` **PASS**, `23 passed, 0 failed`, `exe_check=MATCH` on both firewalls, running executable SHA `1d28b748017f3042baa6f63e7466e3ad3079a6158afcdd34f85f089b34775d7b`, and 18.6000 Gbps throughput.
- The promoted cell printed: `PASS  a pool-mode TCP flow connects after the role change (data path, not just allocation)`.

## Hermetic verification

- `bash -n test/incus/test-failover.sh`: PASS.
- `git diff --check`: PASS.
- `bash test/incus/iperf-throughput-selftest.sh`: **24 passed**.
- `bash test/incus/harness-result-selftest.sh`: **181 passed, 0 failed**.
- `bash test/incus/cluster-cell-selftest.sh`: **32 cases PASS**; the static half confirms `test-failover.sh` takes the #1875 lock before mutation.
- `sh scripts/harness-census.sh`: **57 runnable harnesses, 26 reached, 31 declared unreached; OK**.
- `sh scripts/selftest-census.sh`: **34 self-tests invoked, 58 Python files guarded; OK**.
- `sh scripts/run-selftests.sh`: **218 passed, 0 skipped, 2 failed**. The failures are the pre-existing `scripts/go-skip-census.sh` floor drift and `ledger-compare-all` stale/undetermined baseline declarations; this change does not alter Go skip sites or ledger baselines. The changed script's standalone shellcheck warning is also pre-existing (`SC2034` for the existing unused `for i` loop variable; `origin/master` reports the same warning).

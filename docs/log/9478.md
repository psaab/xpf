# #9478 — adjudicate the stranded #9065 positional-ladder work

- **Timestamp**: 2026-09-11
- **Action**: Adjudicate the preserved patch and census cell; land a verified value-slot binding census
- **File(s)**: `cmd/cli/value_slot_binding_census_9478_test.go` (new), `docs/log/9478.md`

## Inputs

`/var/tmp/xpf-preserved-untracked/9065-positional-ladder/` (base `75195f44a`):
- `tracked.patch`: 4 files, +159/-15;
- `positional_ladder_class_9065_test.go`: 396 lines, 7 tests.

## Q2 — is the production diff distinct from what #9065 closed? No; discard it.

- It touches the same three #9065 members:
  - the `show security flow session zone` name;
  - the `show interfaces` modifier overwriting the selector;
  - the `show security nat ... pool` selector.
- All three are fixed on master by PR #9345 (`4103b7a5d`), through different mechanisms (`resolveSessionZone`, `cmdtree.SplitModifiersAt`, pool passthrough). Master carries its own cells: `selector_type_and_scope_9065_test.go`, `selector_survives_modifier_9065_test.go` and `pkg/cmdtree/modifier_split_9065_test.go`.
- `git apply --check` fails on all four files at `580d959e0`.
- **The patch is a mid-mutation-matrix snapshot, not a fix.** `showFlowSession` contains `_ = p.zoneName // MUTANT9065: resolve dropped`, an applied mutant that discards the zone resolve.

## Q1 — does the census still measure something true at master? The gate does; its dispositions did not.

Only the census class gate (`TestEveryOperationalValueSlotIsAdjudicated9065`, declared set == discovered set) was run against master `580d959e0`. It PASSES: the tree has 44 value slots and the cell declared exactly those. Its other six tests do not compile, because they call helpers the landed fix does not have (`showInterfacesRequestFromArgs`, `selectorAfter`).

Every declared disposition was then read at its `cmd/cli` site. The gate only checks set equality, so none of these claims was ever tested. Most exempt entries were wrong:
- **"local-only or forwarded verbatim", but keyword-bound in a typed request:** `clear security flow session interface|source-nat-pool|zone`, `monitor security packet-drop from-zone|interface`, `ping routing-instance`, `show security flow session source-nat-pool`.
- **"local-only", but bound by a shared parser or a fixed position:** the `request chassis cluster data-plane userspace *` and `request chassis cluster failover *` slots (`dpuserspace.Parse*`, `clusterfailover.ParseCommand`), `request dhcp renew`, `monitor interface`.
- **"RPC carries no selector", but bound:** both `deterministic-nat ... pool` slots (`natPoolArg` into `GetNATDeterministicRequest.Pool`).
- **Correct as local-only / refused:** `monitor traffic interface` and `monitor security flow filter interface`, which the remote CLI refuses outright, and `show security policies from-zone to-zone policy`, which `validatePolicyZoneSelectors` rejects as an unrecognized token.

No slot was found silently dropping a value. The wrong labels were all on the benign side; none hid a live drop.

## Q3 — does closing #9065 need correcting? No.

The stranded production diff is superseded, not a remainder. The census was the uncaptured half, and it lands here with corrected facts.

## What landed

`TestEveryOperationalValueSlotHasARemoteBinding_9478` keeps the set-equality gate (fixture first, undeclared, dead). Each of the 44 entries records the binding mechanism (keyword, shared parser, tree modifier split, fixed position, forwarded to the daemon, refused by the remote CLI) and the site it was read at. A value slot added to `pkg/cmdtree` reds the gate until someone reads the remote-CLI path and records it.

The `fixed position` entries (`show route table|instance|protocol`, `show class-of-service scheduler-map`, `show firewall filter`, `show security screen ids-option|statistics zone`, `request dhcp renew`, `monitor interface`, `deterministic-nat ... pool`) are correct for today's grammar. They are recorded as that shape, not certified safe against a future modifier.

## Mutation cells (head 18e0ead1c)

`TestEveryOperationalValueSlotHasARemoteBinding_9478`: NOOP PASS. Each cell below restored the file from HEAD afterwards.
- M1 drop the `show security zones` entry (an undeclared slot): FAIL (killed)
- M2 add a dead entry `show bogus retired slot`: FAIL (killed)
- M3 blind the discovery predicate (`false && ...`, fixture-first): FAIL (killed)
- M4 blank one entry's site: FAIL (killed)
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    ddb0df0d5. That tree is origin/master 580d959e0 merged with 6 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9619, #9415, #9478, #9601, #9365, #9685.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5838 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9478.md

# #9528 — flowless ICMP-error arms skipped the interface input filter and the PBR drop

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified at 48a5177be / d1266ee79, since the issue's line
    numbers are from 098294d28. In the flowless `else` arm of
    `poll_binding_process_descriptor` the order was:
    - the #6472 NAT64 arm (`continue` on Queued and Dropped);
    - the #7359 input filter;
    - the #5690 same-family arm (`continue`);
    - the #3291 flowless enforcement, whose PBR verdict is the only
      `ingress_route_table_override` call on this path.

    So the NAT64 arm skipped both gates and the #5690 arm skipped PBR, as
    claimed. Controls: the association-hit arm evaluates both (#5798, #6835),
    and no document exempts either arm. The issue calls the loop not drivable
    in-crate, but `tests_embedded_poll_filter.rs` already drives
    `poll_binding_process_descriptor` with real frames, so the cells here are
    behavioural, not source-order.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Hoisted, not duplicated. The flowless arm now builds the L3
    context, runs the non-PBR input filter, runs the PBR verdict (`Drop`
    recycles), and only then tries the #6472 and #5690 arms. The flowless
    transit enforcement consumes the same `route_table_override`. The move is
    pure code motion inside one block, plus comments.
    - Collision check: no open PR touches poll_descriptor. wt-9582 edits
      `mod.rs` hunks at 3107 and 6063, far from this block.
    - A direct Python truncate-write of `mod.rs` failed with EBADF
      (touch/append worked). The edit went through a scratch copy and `cp`
      instead.
  - **File(s)**: userspace-dp/src/afxdp/poll_descriptor/mod.rs
- **Timestamp**: 2026-09-10
  - **Action**: Cells `*_9528` appended to `tests_embedded_poll_filter.rs`,
    each with a no-filter control in the same harness:
    - NAT64 plus an input `discard`: dropped, counted once.
    - NAT64 plus a PBR `discard`: dropped, counted once.
    - #5690 plus a PBR `discard`: dropped, counted once.
    - An accepting term and a PBR steer on both arms: still delivered, counted
      once.
    - A flowless non-first fragment with a PBR steer: evaluated once.

    The meta carries `flow_src_addr` / `flow_dst_addr`; without them the L3
    context is None and every gate is skipped (the #7359 warning).
  - **File(s)**: userspace-dp/src/afxdp/tests_embedded_poll_filter.rs
- **Timestamp**: 2026-09-10
  - **Action**: Docs: a new bullet in the afxdp README; the NAT64 row in
    feature-coverage now states the order.
  - **File(s)**: userspace-dp/src/afxdp/README.md, docs/feature-coverage.md,
    docs/log/9528.md
- **Timestamp**: 2026-09-10
  - **Action**: The first full `make test` at f4ae5b751 was red on the Rust
    leg: `cos_literal_guard_7689::additive_wire_exhaustive_literals_only_ever_decrease_7689`
    reported `NeighborSnapshot: 27 exhaustive literals, ceiling 26 (+1)`.
    Attributed before acting: the +1 is this change's own exhaustive
    `NeighborSnapshot` literal in the same-family harness, not another lane's
    merge, and the guard passed in #9526's `make test` on the preceding master.
    Rewrote the literal with `..Default::default()`, as the guard prescribes,
    instead of raising the ceiling.
  - **File(s)**: userspace-dp/src/afxdp/tests_embedded_poll_filter.rs
- **Timestamp**: 2026-09-10
  - **Action**: Rust mutation matrix. 6 cells, run at 185f3ecec (and identically at f4ae5b751 before the fixture fix), scored on each named target's `test ... FAILED` line (a build failure, a timeout or nothing collected scores VOID). The unmutated control collected 5/5 and passed. 5/5 mutants KILLED, 0 SURVIVED, 0 VOID, and the tree was clean after the run. The mutants: the #6472 arm inserted back ahead of the input filter; the #5690 arm inserted back ahead of the PBR verdict; the hoisted PBR Drop ignored; the PBR verdict evaluated twice; the hoisted input-filter deny skipped. The three insertions keep their anchor, so their occurrence count reads 1->1 and the kill proves they applied; the other two were confirmed by count.
  - **File(s)**: none (scratch matrix)
- **Timestamp**: 2026-09-10
  - **Action**: `make test` (Go + Rust) at 185f3ecec: 77 Go packages ok, 0 FAIL; Rust 5786 passed, 0 failed. The first run, at f4ae5b751, was red on the #7689 ratchet (NeighborSnapshot exhaustive literals 27 > 26). The +1 was this change's own new fixture literal, so it was rewritten with ..Default::default() rather than raising the ceiling.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Landing rule applied: master MOVED since the tested base, twice. It first moved d1266ee79 -> 042effebb (the #9526 merge); that merged tree compiled (go build ./... ok; go vet ./... differential 0 master / 0 merged / 0 new; cargo check --tests --benches ok). Master then moved again to fe86a51c8 (the #9599 merge) before the push, which the pre-push guard caught. The check was re-run against fe86a51c8: master_delta ∩ mine EMPTY, no mutated file changed on master, and the merged tree compiled again with the same results. Merged with --match-head-commit. The commit was then amended to add these log entries only (no code change).
  - **File(s)**: docs/log/9528.md

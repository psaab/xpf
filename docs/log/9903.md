# #9903: four permissive-direction gates

## Brief + plan reviews (MEDIUM tier)
- Brief at docs/pr/9903/brief.md (untracked). 2 hostile plan reviews:
  codex-plan-r1 (OBJECTION F-126/127/129 premises, AGREE F-128),
  glm-plan-r1 (AGREE F-126/128, OBJECTION F-127, AGREE F-129 w/ binding
  corrections). All fix directions survived; adjudicated corrections below.
- No overlap with Eng9901's Rust set (confirmed both directions).

## Adjudicated corrections (all adopted)
- F-126: jump-not-DROP; whole-generation-refusal tradeoff documented;
  parity = no-silent-truncation (no kernel claims); text :2093 is
  addresses, not ifnames.
- F-127: brief's "As4 yields zeroes" WITHDRAWN — verified PANIC via probe
  (v6-in-v4), ::mapped never-match (v4-in-v6). Predicate pinned to
  Is6()==f.v6 mirroring filterFamilyAddrs.
- F-129: "unauthenticated" → authorization fail-open; NO trailing-slash
  normalization (flag shapes instead); deny scoped to exact /api/v1 or
  /api/v1/ prefix on the raw path.

## Change
- F-126: length gate in iifnameMatch (len>15 → p.fail); text keeps
  verbatim. Cells: overlong/mixed/boundary + daemon text-verbatim parity.
- F-127: p.fail on unparseable/wrong-family in pfxAddrMatch; emit/installer
  propagate (fail-closed). Cells: garbage x5 shapes, wrong-family x5
  (panic + never-match directions), well-formed control.
- F-128: revoked consulted before herr; doc corrected. Cells: ctx-Err
  handler demote → deterministic PermissionDenied (RED pre-fix: Canceled),
  handler-only preservation, 5x repetition.
- F-129: unknown /api/v1 reads 403; census sees Handle/methodless/slash
  shapes and flags non-plain ones; live-mux shape census. Cells:
  middleware-direct deny/pass-through, synthetic census fixtures, live
  shape gate.
- STEP-0: every member RED pre-fix (roundtrips via patch save/restore, no
  stash; wrong-family panics pre-fix as predicted).

## Gates
- Full pkg/nftables + grpcapi + api + daemon: green.
- Named 5x per area: green. gofmt/vet clean.

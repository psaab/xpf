# #10129 — frr: narrowed-chain suffix-safe aliases shipped; non-suffix migration expansion remains owed

- **Timestamp**: 2026-09-17
  - **Cohort**: F-008 (narrowed BGP chain renders permit-terminated surviving
    subset, warn-only). Base a36b6fb2f (`origin/master`), worktree
    10129-frr-narrow, branch `fix/10129-frr-narrow`. Follows #9947 (F-007
    shipped; F-008 split here).
  - **STEP-0 RED (base, since removed)**:
    TestNarrowedSuffixSafeFailsClosed10129 FAILED — narrowed [ACCEPTER,GHOST]
    attached ACCEPTER permit-terminated and ApplyFull returned nil.
    Raw RED transcript (base 609433efb):
    `--- FAIL: TestNarrowedSuffixSafeFailsClosed10129 (0.00s)` —
    "RED: narrowed suffix-safe [ACCEPTER,GHOST] applied cleanly; want
    render-rejection failing the apply closed (#10129)".
    The witness (permit-terminated surviving subset) is kept as green baseline
    pins in the shipped file; the rejection assertion is gone (killed below).
  - **Plan reviews (2 hostile, pre-implementation)**:
    HostilePlanA → REJECT-plan KILL (suffix-only reject incoherent — the #8363
    suffix rule constrains deny SYNTHESIS, not rejection, so ghost-first/middle
    still install the hole; strict already rejects every narrowed candidate at
    store.Commit so commit loudness is unreachable and 100% of firings are
    lenient log-and-continue with warm last-good narrowed persisting; cold boot
    with no prior managed section leaves FRR without managed peers/routes
    (outage, not a filter); any reject wedges the
    ipmon failover actuator plus sync/rollback/feed divergence; fires on
    already-deny-terminated/quarantined survivors; weak render still manufactured
    below the ApplyFull guard).
    HostilePlanB → RESCOPE (no #1960 brick, but six background outages hide
    behind it: actuator aborts failover dirty-forever, cold-boot
    missing-managed-section (outage, not a filter) with logs+gauges only,
    SyncApply promoted-but-unapplied divergence both nodes,
    rollback #9811 debt loop, feed publication debt; record-before-reject makes
    gauges describe the rejected-not-installed config while non-suffix sites lose
    log lines; docs-only decision without fleet data ships rollout before
    measurement). Smallest safe alternative offered: typed sentinel +
    per-consumer branches + cold-boot last-good gate + warn parity + measured
    fleet split — a cross-subsystem project, not a lane cutover.
  - **Scope result**: the render-rejection and deny behaviors are not changed in
    this measurement lane. The existing warn-only path remains the behavior
    under test, while these cells pin the per-shape reachability owed before
    any future deny. The migration choice, rollout, and operator comms remain
    open at #10129; this log does not adjudicate them. The attached-map
    baselines below provide evidence for that later decision.
    Settled and pinned here: suffix-safe splits three ways — populated
    fall-through survivors (suffix deny WOULD change permit→deny: outage risk),
    empty survivors (member deny flips permit-all→deny-all: max change, stays IN
    the denominator per #9947), terminating-default survivors (suffix member deny
    NOT EMITTED — break drops it) and match-all-final-term survivors (suffix deny
    emitted but SHADOWED — FRR first-match never reaches it): both deny-inert,
    no outage and no fix. Survivor shape decides deny REACHABILITY, ghost
    position decides the SUFFIX predicate — sizing must never conflate them.
    Single-alias PRIMITIVE pinned (direct renderRouteMapForPolicy calls,
    unattached — not attachment-site behavior; the shared-map re-render check is
    vacuous for the pure function, and attached alias behavior is still owed).
    VRF == global; nil PolicyOptions is emptied-owned (zero narrowed sites, no
    double-count); quarantined survivors warn but already attach the bounded
    deny (future deny redundant, future reject would false-positive).
  - **Migration options (owed — NO decision recorded here)**: the warn-first
    vs immediate-deny choice, rollout shape, and operator comms are explicitly
    OUT of scope for this lane and remain owed at #10129. Options on the table,
    none selected: (a) warn-first release — keep warn-only one release to
    measure the fleet split, then deny; (b) immediate deny for suffix-safe
    shapes with operator comms; (c) per-shape rollout — fall-through survivors
    (permit→deny behavior change) + empty survivors (permit-all→deny-all flip,
    #9947) first with comms, since deny-inert member-synthesis shapes
    (terminating-default: deny not emitted; match-all-final-term: deny shadowed)
    need no rollout — but any standalone alias must preserve explicit defaults
    via policyTrailingAction(..., nil), never a blind "deny" (which flips an
    explicit-accept survivor). Comms everywhere: define the missing
    policy-statements or remove them from the chain (the existing warn text).
    Telemetry facts on this tree: `xpf_frr_policy_chains_narrowed` and
    `xpf_frr_policy_chains_narrowed_deny_safe` are declared in
    `pkg/api/metrics_descriptors_controlplane.go`, emitted by the collector in
    `pkg/api/metrics.go:1329-1335`, and wired to the FRR manager sets by
    `pkg/daemon/daemon_run_servers.go:619-629`. The source sets are produced by
    `warnNarrowedChains` / `narrowedDescriptors` in
    `pkg/frr/policy_chain_narrowing_warn_8363.go`; the gauges carry NO
    survivor-body dimension, so the empty-survivor and deny-inert shares are
    NOT observable today — the decision needs either a config-correlated census
    (join gauge sites with PolicyOptions bodies) or new instrumentation
    (per-site survivor-shape labels at the narrow detector plus render-side
    shape classification in renderComposedRouteMap, `pkg/frr/policy_render.go`).
  - **File(s)**: pkg/frr/policy_chain_narrowed_shapes_10129_test.go (14 cells,
    incl. an ApplyFull-nil kept pin guarding the KILL; measurement only; no
    production code touched).
  - **Verification (this tree, base a36b6fb2f, branch fix/10129-frr-narrow,
    2026-09-17)**: all 14 cells green (`go test ./pkg/frr -run 10129 -count=1`
    → 14 `--- PASS`); TestEmptySurvivorSynthesizedDenyIsReachable9947 green
    (permit-all→deny-all flip still holds); 8363/suffix cells green; full
    `go test ./pkg/frr/... -count=1` green. Docs-only diff; no production
    code touched.
  - **Scope note**: warn-only, zero behavior change — no outage, no brick, no
    fail-open, no actuator wedge possible. #10129 stays OPEN for the deny (or
    risk-accepted alternative); #9947's owed F-008 pre-deny measurement landed
    via #10137 (measurement only; Refs, not Closes). This follow-up neutralizes
    the log's decision language into owed options per parent scope; it records
    no migration decision.

## Phase 1 progress — mechanism ready, behavior still WARN-ONLY (historical snapshot, 2026-09-18)

- **Mechanism status**: the narrowed-chain deny alias is implemented with a
  reserved `-xpf-narrowed-xpf-chain` name, standalone and composed render
  primitives, deduplication across global/VRF sites, and fail-closed collision
  checks for operator policies, composed chains, emptied-chain deny, and
  distinct surviving chains. It is **not activated in production**.
- **Activation boundary**: `Manager.narrowedAliasesEnabled10129` defaults false
  in `New` and is unexported; only same-package test fixtures can enable it.
  The ordinary attachment path therefore remains WARN-ONLY: narrowed chains
  render their surviving subset, retain the existing permit-terminated
  behavior, and emit the existing warning. The test-mode end-to-end cell
  enables the field explicitly and verifies the alias attachment plus
  deny-terminated definition.
- **Telemetry**: added
  `xpf_frr_policy_chains_narrowed_shape{shape=...}` with fixed
  `empty`, `fall-through`, `match-all`, `quarantined`,
  `terminating-default`, and `unknown` series. Counts come from the same
  narrowed population as the existing gauges; the shape classifier keeps
  survivor-body reachability distinct from the `GhostsAreSuffix` position
  discriminator.
- **Corpus evidence**: `git ls-files '*.conf' | wc -l` returned **20**.
  Re-running
  `grep -rEi 'router bgp|route-map|policy-statement|policy-options|neighbor.*(route-map|import|export)|protocols bgp|import-policy|export-policy' --include='*.conf' .`
  returned **zero matches**. The broader YAML/JSON scan found no BGP policy
  fixture; remaining `bgp` mentions were historical docs/index material, not
  deployable policy-chain corpus. The tracked `examples/deploy/ha-pair.conf`
  and `examples/deploy/standalone.conf` were included in the 20-file scan.
- **RED→GREEN evidence**: reverting the protocol attachment wiring made
  `TestNarrowedAliasManagedSectionEndToEndTestMode10129` fail because the
  alias was not attached; restoring it passed. Replacing the shape classifier
  with an `unknown` stub made
  `TestNarrowedSurvivorShapeClassifies10129`,
  `TestNarrowedShapeGaugeCountsPopulation10129`, and
  `TestNarrowedQuarantinedShapeIsMeasured10129` fail; restoring the classifier
  made all three pass. Stashing the shape descriptor/collector wiring made
  `TestFRRNarrowedPolicyChainShapeGauge10129` fail with
  `missing narrowed metric family: ... shape=false`; restoring the wiring made
  the API telemetry cell pass.
- **Safety gate still owed**: do not flip the default until a fleet census
  records narrowed counts by survivor shape (including empty survivors), the
  migration/rollout and operator communication plan is approved, and the
  load-time/HA-sync/rollback consequences are addressed. The current phase
  records mechanism readiness and measurement only; it does not close #10129.
- **Telemetry lifecycle correction**: `Clear`/`ApplyFull(nil)` now resets the
  narrowed survivor set on every clear attempt. A degraded clear can schedule
  a later full-diff retry that removes the section, so retaining the old set
  would leave telemetry permanently stale; the failed-clear cell therefore
  expects empty telemetry even when the clear returns an error.
  `TestNarrowedTelemetryClearsAfterApplyFullNil10129` and
  `TestNarrowedTelemetryClearsAfterFailedClear10129` pin both transitions.
- **Suite note**: `go test ./pkg/frr -count=1` passed and the full
  `go test ./pkg/frr ./pkg/api ./pkg/daemon -count=1` run passed `pkg/frr`
  and `pkg/api` but hit the pre-existing
  `TestCommittedDigestStableAcrossMTUSync9841` failure in `pkg/daemon`
  (`SyncApply: sync config parse error` from the test's
  `system host-name mtu-test` fixture). Parent verification reproduced the
  same failure on clean control `HEAD`; no daemon test/helper file is changed
  by this phase.

## Phase 2 — production suffix-safe alias (2026-09-18)

- **Disposition**: #10129 had no assignee when checked and is covered by the
  #9947 cohort PR. The production fix is intentionally suffix-only, matching
  the settled acceptance: fall-through and empty survivors attach a private
  deny-terminated alias; ghost-first/middle survivors keep their surviving
  members and do not receive an inserted terminator; terminating-default and
  match-all-final-term survivors stay on their explicitly terminating shared
  map. The latter shapes are deny-inert, so changing their attachment would
  be a behavior change without closing a permit fall-through.
- **Owning boundaries**: `Manager.ApplyFull` runs the narrowed alias collision
  belt on every render path (including tolerant load, peer-sync, and rollback)
  before writing; `buildManagedSection` emits aliases beside ordinary maps;
  `generateProtocols` swaps only eligible attachment references. Alias
  definitions are private and deduplicated across global/VRF sites, so shared
  standalone/composed maps remain permit-terminated for intact attachments.
- **Rollout decision**: suffix-only is the migration control for this PR:
  deny only suffix fall-through and empty survivors, while retaining
  non-suffix/ghost-first/middle and deny-inert behavior exactly. Existing
  narrowed warnings, shape gauges, and operator guidance to define or remove
  missing names remain unchanged. The fleet census/migration gate recorded in
  Phase 1 remains owed only for any future non-suffix expansion, which is
  outside this PR's scope.
- **Collision safety**: operator-policy, composed-chain, emptied-deny, and
  distinct-survivor alias collisions compare final `frrName` outputs and return
  an apply error before the managed section is written. This keeps FRR's
  last-good state rather than allowing same-name route-map merges, including a
  tolerant invalid raw policy name that sanitizes to an alias's final name.
- **RED cells** (pre-flip, base of this worktree):
  `go test ./pkg/frr -run 'TestNarrowedProduction' -count=1 -v` failed the
  seven behavior-flip cells: single, composed, empty-survivor, VRF, export,
  rendered-name collision, and warning-parity. The deny-inert control was
  already green before the flip because it must remain on the shared map.
- **GREEN cells** (post-flip):
  `go test ./pkg/frr -run 'TestNarrowedProduction' -count=1 -v` passed all
  eight production cells, including
  `TestNarrowedProductionDenyInertShapesRemainShared10129`; the broader
  `go test ./pkg/frr -run 'TestNarrowed|TestNarrowing|TestEmptySurvivor|TestTerminating|TestMatchAll' -count=1 -v`
  passed all 44 matching top-level tests, including shared-map invariants,
  ghost-first/middle preservation, explicit-default preservation, VRF/global
  dedupe, and empty-survivor deny-all.
- **Full FRR proof**: `go test ./pkg/frr/... -count=1` passed after the
  production flip. The previous phase's API/daemon note remains unchanged:
  `TestCommittedDigestStableAcrossMTUSync9841` is a known BASELINE-RUST
  failure outside this lane.

- **Runtime probe**: under the shared lock, `incus exec loss:xpf-userspace-fw0 -- vtysh -c 'show version'` reported FRRouting 10.6.0. The same live target's `vtysh -c 'show bgp ipv4 unicast summary'` reported `bgpd is not running`, so this environment cannot provide a live BGP route-map prefix accept/deny measurement. No live route-evaluation claim is made; the production cells assert emitted route-map attachment/sequence behavior and the full Go FRR suite is the available scoped proof.

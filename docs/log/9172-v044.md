# #9172 V044 -- packed `routing-instance destination;` binds the literal keyword

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified at master 3226ad857 with a probe: packed bare
    `tunnel { routing-instance destination; }` passed SchemaValidate and
    compiled (strict and lenient) to RoutingInstance="destination"; the braced
    bare spelling compiled to "" and was refused by SchemaValidate ("declares a
    value and none was given"), as was the flat set form.
  - **File(s)**: none (probe deleted)
- **Timestamp**: 2026-09-10
  - **Action**: packedTunnelRoutingInstance8936 now returns (name, packed) and
    recognises the two-key bare shape, so both tunnel call sites stop falling
    back to nodeVal (which returned the keyword). The tunnel routing-instance
    schema node opts into packedTail, so the packed bare spelling gets the same
    commit refusal as the braced one. Regression test covers both tunnel sites,
    strict and lenient compile, and SchemaValidate, with braced controls.
  - **File(s)**: pkg/config/compiler_interfaces.go,
    pkg/config/schema_interfaces.go,
    pkg/config/tunnel_routing_instance_packed_9172_test.go,
    docs/log/9172-v044.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation matrix at f5c077e5f, scored by test name and subtest:
    M1 (bare packed shape not recognised), M2 (packedTail opt-in removed),
    M3a / M3b (either tunnel call site ignores the bare shape) -- all KILLED
    on TestPackedBareTunnelRoutingInstanceBindsNothing9172's packed-bare
    subtests; the #8936 spellings cell stayed green throughout.
  - **File(s)**: docs/log/9172-v044.md

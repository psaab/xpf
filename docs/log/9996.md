# #9996 — natpoolalarm CLEARED line dropped the clearing sample

- **Timestamp**: 2026-09-17
- **Action**: Read the issue and traced `evaluate()` through the utilization
  state machine. Threshold-driven clears computed a fresh `pct` but called
  `clear()` without it; `clear()` consequently emitted the held `st.pct`.
  Config-disabled and nil-config paths use `clearAll()` and have no fresh
  utilization sample.
- **File(s)**: `pkg/natpoolalarm/natpoolalarm.go`,
  `pkg/natpoolalarm/natpoolalarm_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Added a regression cell that raises at 85%, silently holds and
  refreshes to 95%, then clears at a fresh 65% sample. Added a second cell that
  verifies a nil-config `clearAll()` remains an unsampled clear and retains the
  held 85% value without fabricating a sample.
- **File(s)**: `pkg/natpoolalarm/natpoolalarm_clear_sample_9996_test.go`

- **Timestamp**: 2026-09-17
- **Action**: RED proof before the implementation. The targeted regression run
  failed as expected:
  `TestThresholdClearCarriesClearingSample9996` received
  `utilization=95%` in the CLEARED line while the clearing sample was 65%.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-17
- **Action**: Passed an optional fresh sample pointer through `evaluate()` to
  `clear()`. The threshold-driven path passes `&pct`; deterministic
  unmeasurable, eligibility-prune, and `clearAll()` paths pass nil. `clear()`
  uses the fresh sample only when present and otherwise retains the held value.
  Updated module documentation to state the sampled-versus-unsampled clear
  distinction.
- **File(s)**: `pkg/natpoolalarm/natpoolalarm.go`,
  `pkg/natpoolalarm/README.md`

- **Timestamp**: 2026-09-17
- **Action**: GREEN proof. The targeted regression run passed:
  `go test ./pkg/natpoolalarm -run
  'Test(ThresholdClearCarriesClearingSample9996|UnsampledClearKeepsHeldPct9996)$'
  -count=1`. The full package suite also passed:
  `go test ./pkg/natpoolalarm -count=1`.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-17
- **Action**: Committed implementation, regression coverage, and module
  documentation in `43e64de68` (`fix natpool alarm clear sample`).
- **File(s)**: `pkg/natpoolalarm/natpoolalarm.go`,
  `pkg/natpoolalarm/natpoolalarm_clear_sample_9996_test.go`,
  `pkg/natpoolalarm/README.md`

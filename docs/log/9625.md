# #9625 — configstore: the #9014 arm-write retry runs once

- **Timestamp**: 2026-09-10
  - **Action**: Added `&& !s.confirmArmDegraded` to persistRetryLoop's bottom
    exit check, so it uses the same debt set as the top check. Before, a retry
    that failed a second time with no other debt owed returned with
    confirmArmDegraded still true. Health stayed degraded for the rest of the
    commit-confirmed window, and nothing was left to write the crash-recovery
    record.
  - **Action**: Cell TestArmWriteRetryOutlivesASecondFailure9625. A counting
    seam on confirm.json writes requires the arm plus at least three retries
    while the fault persists; before the fix the count stopped at 2. After the
    fault clears, the cell requires health to heal, the record to be written,
    and the loop to end with no debt. The #9014 superseded / resolved cells
    still cover the arms that clear the debt without writing.
  - **File(s)**: pkg/configstore/store_persist.go,
    pkg/configstore/confirm_arm_retry_second_failure_9625_test.go, docs/log/9625.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation pair at code head `e3b12c1e8`, scored by `go test -json`
    test NAME. The mutant was applied under an exact-one-anchor assertion,
    proven by a non-empty `git diff`, and restored with `git checkout HEAD --`.
    NOOP was clean. M1 restores the old bottom exit, without the
    confirmArmDegraded term, and is KILLED by
    TestArmWriteRetryOutlivesASecondFailure9625.
  - **File(s)**: docs/log/9625.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9625.md

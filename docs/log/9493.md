# #9493 — frr: six operand classes render into the managed frr.conf with no validator and no belt

- **Timestamp**: 2026-09-10
  - **Action**: Census before adding commit gates, which have repo-wide blast
    radius. 809 tracked config-like files and the Go test literals carry no
    out-of-range `cost`/`multihop`, no spaced `local-address`/`virtual-link`/`net`,
    and no whitespace in a policy, prefix-list, community or as-path name.
    SchemaValidate is called only from configstore's commit-check path, so a new
    schema validator cannot brick a persisted config on the tolerant load path.
  - **File(s)**: none (research)
- **Timestamp**: 2026-09-10
  - **Action**: Commit-time validators, attached per leaf in schema_routing.go:
    - BGP group/neighbor `local-address`: ValidateIPAddress;
    - `multihop`: ValidateInteger(1, 255);
    - OSPF/OSPFv3 interface `cost`: ValidateInteger(1, 65535);
    - `virtual-link` neighbor (key): an IPv4 router ID;
    - `transit-area`: ValidateOSPFArea;
    - IS-IS `net`: ValidateISISNET, i.e. hex octets in dot groups, 8 to 20
      octets;
    - policy-statement, term, prefix-list and community names:
      ValidateFRRObjectName (one token);
    - as-path name: position 0 only, because the regex in position 1 may
      legitimately contain spaces.
  - **Action**: The first run showed local-address, transit-area, net and the
    prefix-list name still committing. A schema `validator:` runs only on a
    TYPED leaf, so those leaves now carry a `valueType`, and the prefix-list
    name, whose block children are prefixes, uses a keyValidator instead.
  - **File(s)**: pkg/config/schema_validators_frr_operand_9493.go,
    pkg/config/schema_routing.go
- **Timestamp**: 2026-09-10
  - **Action**: Render belts for the tolerant path:
    - `update-source`, `virtual-link` and `net`: omitted with a warning when
      malformed;
    - `cost` and `multihop`: clamped to 65535 / 255, since omission would change
      routing to FRR's defaults;
    - every route-map, prefix-list, community-list and as-path name, at 16
      definition and reference sites: rendered through `frrName`, which leaves a
      safe name unchanged and aliases an unsafe one to a single token plus a
      hash. Omission is not safe for names: dropping
      `neighbor X route-map NAME out` removes the outbound filter.
  - **Action**: Updated TestGeneratePolicyOptions_SetClauseAndPrefixListSanitized_4482.
    It pinned the older belt's output for three NAME slots, `match community`,
    `match as-path` and `set comm-list`: the injected payload collapsed onto one
    line with spaces. That is still a line FRR rejects, because it splits into
    extra arguments. The cell's property, no injected second line, is kept, and
    the three slots now assert the single-token alias.
  - **File(s)**: pkg/frr/operand_belt_9493.go, pkg/frr/protocols_render.go,
    pkg/frr/policy_render.go, pkg/frr/redistribute.go,
    pkg/frr/prefix_list_render.go, pkg/frr/policy_setclause_injection_4482_test.go,
    pkg/frr/README.md
- **Timestamp**: 2026-09-10
  - **Action**: Cells:
    - pkg/configstore TestFRROperandsAreValidatedAtCommit9493: through CheckText,
      every bad row is refused naming its leaf, and each row's good value commits,
      so the refusals cannot come from a fixture gap;
    - pkg/frr TestFRRNameIsOneTokenAndStable9493: identity, determinism, and no
      `a b` / `a_b` collision;
    - TestScalarOperandBelts9493: omit and clamp rows with valid controls;
    - TestEveryRenderedObjectNameIsOneTokenAndResolves9493: a behavioural census
      over definitions and references, with a positive control per reference
      shape and a safe-name control.
  - **File(s)**: pkg/configstore/frr_operand_commit_gate_9493_test.go,
    pkg/frr/operand_belt_9493_test.go, docs/log/9493.md
- **Timestamp**: 2026-09-10
  - **Action**: The full pkg/config suite was green except the #9156 leaf-run
    ratchet, which reported two recorded containers that no longer differ: the
    OSPFv3 interface `cost -> dead-interval` one-line fold, top-level and under
    routing-instances. This branch caused it. Typing `cost` makes the strict walk
    and the lenient compiler agree on that spelling, and master still records
    both entries as differing. The two entries are removed from the ratchet's
    record, which tightens it; no other entry moved. The #8852 and #9206
    inert-validator ratchets flagged the untyped-leaf attachments on the first
    run, and are green now that those leaves are typed.
  - **File(s)**: pkg/config/leaf_run_differential_gate_9156_test.go, docs/log/9493.md
- **Timestamp**: 2026-09-10
  - **Action**: The first full matrix KILLED 18 of 19. R8 survived: dropping
    the hash suffix from frrName's alias. The collision cell compared
    `a b` with `a_b` and `a.b`, which are SAFE names returned unchanged, so
    they differ with or without a hash. The collision the hash prevents is
    between two UNSAFE names with one sanitized fragment. Added
    `frrName("a b") != frrName("a\tb")` and re-ran the full matrix on the
    amended head.
  - **File(s)**: pkg/frr/operand_belt_9493_test.go, docs/log/9493.md
- **Timestamp**: 2026-09-10
  - **Action**: Full mutation matrix re-run at code head `5bea50529`, with the R8
    collision row added. NOOP was clean, and 19 of 19 mutants were KILLED by
    their target test (the G1-G8 and R1-R11 set listed above). R8 is now killed
    by TestFRRNameIsOneTokenAndStable9493's two-unsafe-names collision
    assertion.
  - **File(s)**: docs/log/9493.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9493.md

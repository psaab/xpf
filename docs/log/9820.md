# #9820 — frr: one family/area predicate for statics, backup-router, OSPF area

- **Date**: 2026-09-14. Worktree `.claude/worktrees/9820-frr-family`,
  branch `fix/9820-frr-family`, base `origin/master b4e78ea28`.
- **Defects** (all STEP-0-confirmed present at base): SYN-FRR-02 — a v6
  static with a v4 NH commits clean and renders `ipv6 route <v6dst>
  <v4nh> [ifname]` (interface-slot fill, normally inactive; with an
  interface ordinarily parse-fatal); SYN-FRR-04 — validator (v6) / belt
  (v4) / emission (v6) disagree on `::ffff:a.b.c.d`; SYN-FRR-05 —
  `ValidateOSPFArea` accepts mapped literals and the tolerant path
  renders malformed areas raw on stanza/interface/OSPFv3 lines.
- **Fix**: shared `config.FRRAddrFamily`/`FRRAddrIsMapped` (mapped ⇒ v6
  *literal* classification); one-directional static commit gate + lenient
  flag + per-NH render belt (v4-via-v6 kept); backup-router mapped-NH
  refusal + predicate swap in gate/belt/emission with minimal texts
  (`frrOperandIsV6` deleted; B3 destination flips toward gate agreement);
  area mapped + padding arms (padding = intentional tightening, quoted
  padding is ordinary-ingress-authorable) + two-check belt on all three
  line sites; per-direction rewrites of the contradicted #2891/#8597
  mechanism claims in comments + READMEs.
- **Plan review**: 11-section plan + 3 hostile rounds × Codex
  (`openai-codex/gpt-6-astra`) + GLM (`zai/glm-5.3`), raw outputs in
  `docs/pr/9820/reviews/` (uncommitted). R1 GLM MINOR / Codex MAJOR; R2
  both MAJOR (truncated-v2 artifact + B2/B3/B4 substance); R3 GLM MINOR
  ("fold and implement") / Codex MAJOR (text contradictions + package /
  cell corrections, architecture + all Qs approved). No agreement in
  budget, zero architectural dissent in 6 reviews; parent authorized
  implement-on-v3.y (call A), all r3 findings folded (several in
  reviewer-supplied exact language). Grammar pinned first-hand from FRR
  stable/10.6 source; OSPFv3 `interface area` confirmed absent on 10.6
  (pre-existing, parent-filed as #10037 — OSPFv3 controls byte-identical,
  belt still correct). Plan + verdicts posted to #9820 as comments.
- **Commits** (5, each green): predicate; static gate; static belt +
  overlay inheritance; backup-router unification + doc rewrites; area
  arms + three-path belt tests.
- **Verification**: new cells green in all 3 packages (incl. B3 flip
  behavior, mapped spellings ×3, boundary/padded, precedence,
  BFD-preservation, non-vacuity); full `pkg/config` (211s) + `pkg/frr` +
  `pkg/configstore` (34s) suites green; 5× named runs 15/15 green; vet +
  gofmt clean. No cluster (compile/render-deterministic; both r3 agreed).
- **Residuals** (documented in plan §10, owned by parent): v4-via-v6
  Rust-side gateway-less inertness; bare-interface-name NH silent drop;
  `staticRouteRendersFIB` presence approximation; #10037 OSPFv3 emission.

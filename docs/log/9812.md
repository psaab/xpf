# #9812 — validation cohort: forcing leg, census fold-in, separation deny

Three assurance gaps, one test-infrastructure change, no product defect.
Base `609433efb`; members SYN-VAL-03, SYN-VAL-08, SYN-VAL-04.

## STEP-0: RED on base (all reproduced in this worktree before any edit)

VAL-03 (9420 cell skips silently, no forcing leg):

- `grep 9420 Makefile scripts/run-selftests.sh` → zero hits. The #7796 cell by
  contrast has `test/routing/selftest-rule-dscp_7796.sh` + a Makefile target +
  a `run-selftests.sh` leg.
- Tools hidden from PATH: `go test -run TestNextTableIngressScopeOnRealKernel_9420`
  prints `--- SKIP: ... rules_9420_test.go:200: unshare not available`, package
  `ok`, rc=0 — a skip indistinguishable from a pass.
- Same + `XPF_REQUIRE_NETNS=1` → STILL skips at :200. The env touched only arm 3
  (`:231`); the `unshare`/`ip` LookPath arms ignored it.
- Correction to the issue's "appears only" (review S4): the env is READ in four
  test files (`rules_9420_test.go:231`, `vrf_miss_terminator_9819_test.go:346`,
  `host_inbound_ingress_9637_test.go:125`,
  `host_inbound_reinject_verdict_9637_test.go:53`) — but SET nowhere.

VAL-08 (go-skip census in no aggregate, already red):

- `bash scripts/go-skip-census.sh` → rc=1: total 318→365, priv-absent 26→40,
  other 281→314 (priv-present 10 and unparsed 1 unchanged). Slope while
  unwired: 318 (floors) → 330 (issue pin) → 342 (`b2c0c4c1b`) → 365 (this
  base; 366 before the parent-review comment fix, which excluded one
  comment-prose mention from the count).
- `run-selftests.sh` names it only in the `SH_SCRIPTS` syntax list and via its
  self-test; no `run_bash|run_shell scripts/go-skip-census.sh`. The Makefile
  target is a leaf.

VAL-04 (no reached gate observes routing separation in the deny direction):

- `ping_vrf_aware` returns 0 when the probe answers in the default table OR any
  VRF; zero must-not-arrive assertions under `test/incus/`.
- The §3 matrix mentions none of vrf/rib-group/next-table/PBR (each `grep -c` 0).
- `test-wire-properties.sh` covers PMTUD + IPv6 transit only; `test-fbf-steering.sh`
  is an unreached GATE.
- #9819 landed after this issue was filed, so VAL-04's "hermetic netns cells for
  VRF-miss termination" half now EXISTS (`TestVRFMissTerminatorOnRealKernel9819`,
  baseline reproduces before the fix rows) — but runs in NO aggregate
  (`grep 9819 Makefile scripts/run-selftests.sh` → zero; hidden-tools run →
  `--- SKIP ... :338`, `ok`, rc=0). A VRF-miss escape reds nowhere.

## Plan reviews and adjudication (before implementation)

Two hostile plan reviews: scope (APPROVE-WITH-CHANGES, 4 findings) and mechanism
(APPROVE-WITH-CHANGES, 10 findings). Adjudication: accept S1–S4, M1–M8, M10;
partial M9 (placement + `sh` recipe yes, `.PHONY` no — the sibling 7796 target is
not phony-listed either, and touching its line is another issue's scope; the
omission is observed-not-owned). Nothing fully rejected. Review-driven changes:

- S1: the owed LIVE wire harness is tracked in follow-up #10136 (filed before
  implementing), cited in the §3 row and here — not just in this log.
- S2: the banking table below carries per-cause WHY, not just per-file WHERE;
  re-bank-not-remove is a deliberate scope call (removal = un-skipping 48 sites
  across ~15 owners' issues).
- M1/M2: the leg pins both kernel cells BY NAME (anchored `^=== RUN   <full>$`
  + per-name `--- SKIP` absence + rc==0); the header states the predicate runs
  all 12 cells (5×9420 + 7×9819) and the 10 fakes are intentionally included.
- M3: the leg probes `ip` → 77 (with the forcing env, a missing `ip` would
  otherwise FAIL instead of SKIP).
- M4: nested-`unshare` proof gates landing (leg run below), since the 7796
  precedent (in-process netns) does not cover nesting.
- M5: the python3 guard is POSIX with a sh-compat comment (Makefile leaf and
  census self-test invoke the script via `sh` despite its bash shebang).
- M6: the census self-test also probes python3 → 77 (after the exists-check, so
  a missing script still FAILs); otherwise a python3-less host goes RED.
- M7/M8: `#!/bin/sh`, `GO=${GO:-go}`, column-0 `run_shell` adjacent to the 7796
  leg; Skip/Skipf lines byte-identical, env note in Fatalf only (a `NETNS`
  substring in a Skip message would flip buckets — verified unchanged below).

## Change

- NEW `test/routing/selftest-routing-kernel_9812.sh`: 7796-pattern forcing leg
  (probe go/unshare/ip/netns → 77; else `XPF_REQUIRE_NETNS=1 unshare -rn go test
  -count=1 -v -run '9420|9819' ./pkg/routing/` + by-name guard). Registered in
  §4 of `run-selftests.sh` and as `make test-routing-kernel-lib`.
- `rules_9420_test.go` arms 1–2 and the 9819 tool loop honor `XPF_REQUIRE_NETNS`
  (env set → Fatalf). Skip sites/messages byte-identical → census unchanged.
  The 9420 oracle additionally requires all 8 observations present and a
  recognized `unreachable` negative (GPT-1): missing/empty/invalid-probe
  observations FAIL instead of satisfying the deny.
- `run_bash scripts/go-skip-census.sh` in the census section of `run-selftests.sh`
  (after go-buildtag, OUTSIDE §4 so the selftest-census odd-set stays exact;
  `run_bash` per #8153). In-script python3 → 77 guard; stale header counts
  refreshed (26→40, 36→50, 318→365 with the fold-in noted). The classifier is
  comment-aware (GPT-3): `//` and `/* */` prose never counts, strings match
  first so a URL cannot hide a real call.
- Floors re-banked to measured-at-landing: 365 / 40 / 10 / 314 / 1 (table below).
- The leg probes `bash` and ships a hermetic probe self-test
  (`selftest-routing-probes_9812.sh`, six cells incl. a rotted-predicate guard
  control), registered in §4 and in `make test-routing-kernel-lib` (GPT-4).
- §3 matrix: Routing-Instance Separation (Deny) row (`wire_routing_separation`,
  §2 floors + near-miss control, designed for the #9531 `wire_deny_verdict`
  core — no new verdict code; the live gate in #10136 will call it). No
  `--gate` recipe, so LEDGER_COVERAGE/HARNESSES declarations are N/A; no
  census reads the matrix.
  No duplicate netns cells: the 9819 terminator + 9420 F1b (deny scored against
  the F1a positive control in the same run) ARE the hermetic deny cells.
- Docs: `testing.md` states what `make selftest` runs (self-tests + named
  censuses + ledger legs) with refreshed counts; one-line accuracy touches in
  `docs/log/9420.md` and `docs/rib-group-route-leaking.md`.
## Banking table: the 47 unbanked sites, per cause (S2)

Diffed HEAD census `--list` against the floor-setting commit `8cb4c806b` with
HEAD's comment-aware classifier (old tree reproduces the floors exactly:
318/26/10/281/1 — it held no comment-prose match — so the attribution is
apples-to-apples). No file decreased — pure growth while the guard was unwired.

Group A — real-kernel/netns privilege-gated cells, +20 (+14 priv-absent, +6 other).
WHY: new kernel-semantics coverage since #9052, each skipping without a netns —
the same silent-skip shape this change forces for its NAMED cells. Forcing the
rest belongs to their owners.

| File | +n | Buckets | Owning change |
|---|---|---|---|
| pkg/routing/rules_9420_test.go | 3 | 2 priv-absent + 1 other | #9420 kernel measurement (this cohort's subject) |
| pkg/routing/vrf_miss_terminator_9819_test.go | 2 | 1 + 1 | #9819 terminator kernel cell (this cohort's deny cell) |
| pkg/daemon/fabric_overlay_vrf_9813_test.go | 4 | priv-absent | #9813 VRF overlay netns cells |
| pkg/daemon/ri_member_vrf_reassert_9813_test.go | 2 | priv-absent | #9813 |
| pkg/daemon/ri_bare_member_vlan_children_9754_test.go | 3 | priv-absent | #9754 VRF/vlan netns cells |
| pkg/daemon/host_inbound_ingress_9637_test.go | 3 | 1 priv-absent + 2 other | #9637 netns cells (already read XPF_REQUIRE_NETNS) |
| pkg/daemon/host_inbound_reinject_verdict_9637_test.go | 3 | 1 + 2 | #9637 |

Group B — tool/env/fixture-gated cells, +27 (all other). WHY: per-file skips for
absent tools, fixtures or environment states, each named by its file:

9770 `steering_spawn_clear` +9; 9841 MTU trio (`compiler_iface_mtu_unconverged` +3,
`grpcapi/show_interfaces_mtu` +3, `cli_show_interfaces_mtu` +1); `proxyarp_test`
+1 (positive-control pneigh entry install — the old NeighList site was renamed
to NeighProxyList, net priv-absent unchanged); `pci_function_domain_9458` +2,
`trap_defer_v1_9917` +2; singletons `heartbeat_restart_owed_9751`,
`syslog_commit_dial_9326`, `apply_snapshot_identity_9520`,
`monitor_cfg_refresh_9144`, `linklocal_reth_zone_9137`, `closed_owner_8276_9506`.

No Group C: unparsed is ±0. The apparent +1 (`memlockcensus/census_8371_test.go:185`)
is comment prose — ``// t.Skipf(...) }`` inside a doc comment — which the old
classifier counted textually. The comment-aware matcher (GPT-3) excludes it, so
unparsed stays 1: the real `t.Skip()` in `parser_error_cap_5827_test.go:174`.
Per-file diff old-vs-new classifier confirms exactly that one site removed,
nothing added, no other file moved.

Bucket check: A(14 priv-absent + 6 other) + B(27 other) = 14 + 0 + 33 = 47. ✓

Sequencing note: this bank is measured at this branch's head. A lane landing
first with new `t.Skip` sites (e.g. Eng9810's new routing tests) shifts the
floors — re-measure at rebase; this change itself adds zero skip sites.

## Verification (red-green)

- Forcing arms: hidden-tools + env → FAIL naming the tool
  (`rules_9420_test.go:201`, `vrf_miss_terminator_9819_test.go:339`); no env →
  SKIP `ok` (unchanged).
- Oracle (GPT-1): hardened cell green; scratch missing-key mutant (deleted F1B
  echo) FAILs "missing or empty observation F1B_SCOPED_VRFC — refusing to score
  a negative against nothing"; scratch invalid-observation mutant (static
  "Invalid argument") FAILs "must be unreachable ... an unrecognized
  observation satisfies nothing". Both restored, script verified intact.
- Leg: `sh test/routing/selftest-routing-kernel_9812.sh` → PASS, all 12 cells
  RUN+PASS incl. both kernel cells (nested `unshare` proven, M4); fake-unshare,
  `ip`-less and `bash`-less PATHs → 77 (M3, GPT-4). Guard expressions
  unit-proven: rotted predicate → RUN-absence fires; SKIP line matches, PASS
  line does not. `selftest-routing-probes_9812.sh` pins all four probes plus
  the guard hermetically, 6/6 in 0.5s; deleting the bash probe (scratch)
  makes its cell fail with the exact GPT-4 false-FAIL (`failed to execute
  bash`, exit 127 → leg FAIL instead of SKIP).
- Census: `bash`/`sh` → OK rc=0 at 365/40/10/314/1 (Go edits move no bucket,
  M8; comment fix removes exactly the prose site, verified per-file); self-test
  17/17 (empty sweep now asserts status + GOOD NEWS, GPT-2; gamma prose cell,
  GPT-3, proven load-bearing: old matcher counts 8 on the fixture and reds);
  python3-less → 77 for both script and self-test (M6).
- Aggregate red-green: full `make selftest` with mutant floors (total 364) →
  `FAIL: scripts/go-skip-census.sh`, rc=1; without → census leg PASS. Both
  routing legs PASS in the aggregate (214 passed, 1 pre-existing FAIL).
- Deny mutant (scratch, restored, tree verified clean): neuter
  `setVRFMissTerminator` install → 9819 kernel cell FAILs with six escape rows
  (misses escape via dev xmain — v4 `via 10.20.0.254`, v6 `via
  2001:db8:20::fe` — want `unreachable`) + `0 terminators, want 1`.
- Suites: `go test -count=1 ./pkg/routing/` ok; named kernel cells 5× green;
  `gofmt` clean; `go vet ./pkg/routing/` clean; `make test-routing-kernel-lib`
  PASS; `make go-skip-census` OK.
- Full `make selftest`: all owned legs PASS; the single other FAIL is
  pre-existing on base and untouched by this change — `shellcheck
  test/incus/harness-result.sh` SC2194 (v0.11.0 flags the intentional constant
  slug-set in the `case`), owned elsewhere.

## Parent review round (PR #10139: GPT NEEDS-MAJOR 4, spark NEEDS-MINOR 3)

All six findings folded, no rejections:

- GPT-1 (deny oracle accepts missing): the 9420 oracle now requires all 8
  observations present/non-empty and a recognized `unreachable` negative for
  A, B3 and F1b; positive controls (B1/B2/F1a/F1c) untouched. The shell
  helper's discarded probe status is contained Go-side: any invalid-probe
  output fails the `terminated` check.
- GPT-2 (empty sweep asserts no status): cell 6 restores complete floors and
  asserts rc≠0 + `0 skip call sites` + GOOD NEWS.
- GPT-3/SPARK-F1 (comment prose counted + bank arithmetic): classifier strips
  `//` and `/* */` string-aware; gamma cell pins both directions. Group
  subtotals corrected (A=20, B=27, no Group C; 47 total). The pushed C1 commit
  message carries the old numbers — superseded by the review-fix commit.
- GPT-4 (missing bash false-FAILs): leg probes `bash` → 77, pinned by the
  six-cell hermetic probe self-test (probe deletion proven to fail its cell).
- SPARK-F2: verdict-core wording corrected to designed-for/will-call.
- SPARK-F3: mutant escape now names both families.

## What remains owed

- #10136: the live `wire_routing_separation` cluster gate (#9531 pattern).
- Forcing legs for the group-A kernel cells owned elsewhere (9637/9813/9754).

# #9532 — config/nat: validateStaticNATThenTargetStrict's doc comment claims the dataplane fails closed

- **Timestamp**: 2026-09-10
  - **Action**: Re-derived the tolerant-path behaviour at `b4397e0e0` before
    rewording. The chain:
    - StaticNATRuleExcludedReason drops only `then static-nat inet` and
      out-of-range ports, not an empty target;
    - buildStaticNATSnapshots therefore emits the rule with InternalIP "";
    - static_nat.rs parse_nat_prefix fails on it, records a parse error and
      `continue`s.

    So the rule is skipped, and the packet forwards untranslated if policy and
    routing allow it. This is what the function's own error string and the
    sibling validateStaticNATInetTargetStrict already say.
  - **Action**: Rewrote the doc comment to state that chain, and to reserve
    "fail closed" for the strict reject, as the sibling gate does. The two other
    "the dataplane then fails closed" sentences in this package, both on the NAT
    `match ...-address-name` gate, describe a different mechanism: an
    unresolved reference makes the rule match NOTHING. They are left as they
    are. Comment-only change; no behaviour or test change, per the issue's
    acceptance criterion.
  - **File(s)**: pkg/config/compiler_validate_strict_nat.go, docs/log/9532.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9532.md

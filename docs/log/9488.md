# #9488: re-measure the number the re-steering kill rests on

- **Timestamp**: 2026-09-11 10:56:49–11:07:05 UTC
  - **Action**: Re-ran #1748 R1 (manual exact-5-tuple ntuple re-pin of established flows) at master `e1cebdf8b`, with an unsteered control. It ran as one `with-cluster.sh` lock cell on `loss:xpf-userspace-fw0`: `make cluster-deploy`, then `apply-cos-config.sh`.
    - **Pre-state:**
      - fw0 held the LAN VIP 10.0.61.1;
      - `ge-0-0-1` had `Total 0 rules`, `ntuple-filters: off`, 6 combined queues;
      - `show configuration class-of-service | match equal` was empty, so equal-flow-enforcement was not configured.
    - **Push:** `iperf3 -c 172.16.80.200 -p 5210 -P 12 -t 70 -i 1 -J --forceflush` from cluster-userspace-host (10.0.61.102), iperf 3.20.
      - Port 5210 maps to `iperf-24g`, queue 10, `24g exact`, which is `shared_exact`.
    - **re-pinned:** at t≈14 s the cell enumerated the client's established sockets (`ss -Htn state established "( dport = :5210 )"`, 13 of them: 12 data plus control). It then ran `ethtool -K ge-0-0-1 ntuple on`, followed by one `ethtool -N ge-0-0-1 flow-type tcp4 src-ip 10.0.61.102 dst-ip 172.16.80.200 src-port <p> dst-port 5210 action <i mod 6>` per socket, in port order.
    - **unsteered:** the same push with no rules.
    - **Order:** r1 unsteered then re-pinned; r2 re-pinned then unsteered; r3 unsteered then re-pinned.
    - **Metrics:**
      - `/metrics` was scraped on fw0 once a second for `xpf_fairness_cstruct` and `xpf_userspace_cos_active_flow_count` (queue 10).
      - `show chassis cluster data-plane fairness` was captured before each run and at t≈30 s.
    - **Restore, verified on the running NIC** after each re-pinned run and at the end: `Total 0 rules`, `ntuple-filters: off`. iperf3 exited 0 on all six runs.
  - **File(s)**: none (measurement only)

- **Timestamp**: 2026-09-11 11:08 UTC
  - **Action**: Reduced the runs. Per-flow CoV is `population_cov` (`test/incus/fairness_cov.py`) over the per-stream mean of the 1 s iperf3 intervals starting in each window. The flow vector and Cstruct come from the last scrape in the window. Every run is listed below; none was dropped.
    - The flow vector lists only workers that export a queue-10 flow count, so a 5-entry vector means one worker had none.
    - t10–15 counts can include the idle iperf3 control connection (13).
    - Cstruct recomputed from each vector (per-flow share `1/(active workers × flows on its worker)`, then `population_cov`) matches the exported value: `[1,2,3,4,3]` 0.507, `[5,2,1,3,2]` 0.563, `[3,1,1,4,1,2]` 0.601. The same formula gives R1's `[2,2,1,1,4,2]` 0.500.

    | run | window | per-flow CoV | aggregate | queue-10 flows per worker | Cstruct |
    |---|---|---:|---:|---|---:|
    | r1 unsteered | t10–15 | 50.6% | 22.23 G | [1,2,3,4,3] | 0.507 |
    | r1 unsteered | t45–50 | 49.0% | 22.05 G | [1,2,3,3,3] | 0.447 |
    | r1 unsteered | t55–60 | 50.7% | 22.32 G | [1,2,3,3,3] | 0.447 |
    | r1 unsteered | t65–70 | 55.4% | 22.18 G | [1,2,3,3,3] | 0.447 |
    | r1 re-pinned | t10–15 | 44.3% | 21.62 G | [5,2,1,3,2] | 0.563 |
    | r1 re-pinned | t45–50 | 6.7% | 22.68 G | [2,2,2,2,2,2] | 0.000 |
    | r1 re-pinned | t55–60 | 9.0% | 22.76 G | [2,2,2,2,2,2] | 0.000 |
    | r1 re-pinned | t65–70 | 7.6% | 22.71 G | [2,2,2,2,2,2] | 0.000 |
    | r2 re-pinned | t10–15 | 25.1% | 22.63 G | [3,2,2,2,2,1] | 0.333 |
    | r2 re-pinned | t45–50 | 8.2% | 22.78 G | [2,2,2,2,2,2] | 0.000 |
    | r2 re-pinned | t55–60 | 9.2% | 22.66 G | [2,2,2,2,2,2] | 0.000 |
    | r2 re-pinned | t65–70 | 8.6% | 22.73 G | [2,2,2,2,2,2] | 0.000 |
    | r2 unsteered | t10–15 | 51.2% | 22.71 G | [3,1,1,5,1,2] | 0.676 |
    | r2 unsteered | t45–50 | 51.2% | 22.74 G | [3,1,1,4,1,2] | 0.601 |
    | r2 unsteered | t55–60 | 46.7% | 22.72 G | [3,1,1,4,1,2] | 0.601 |
    | r2 unsteered | t65–70 | 46.2% | 22.57 G | [3,1,1,4,1,2] | 0.601 |
    | r3 unsteered | t10–15 | 41.5% | 22.70 G | [4,1,2,1,2,3] | 0.542 |
    | r3 unsteered | t45–50 | 41.7% | 22.71 G | [3,1,2,1,2,3] | 0.471 |
    | r3 unsteered | t55–60 | 41.5% | 22.60 G | [3,1,2,1,2,3] | 0.471 |
    | r3 unsteered | t65–70 | 42.7% | 22.59 G | [3,1,2,1,2,3] | 0.471 |
    | r3 re-pinned | t10–15 | 35.9% | 22.47 G | [3,1,3,2,1,2] | 0.471 |
    | r3 re-pinned | t45–50 | 10.0% | 22.80 G | [2,2,2,2,2,2] | 0.000 |
    | r3 re-pinned | t55–60 | 13.7% | 22.71 G | [2,2,2,2,2,2] | 0.000 |
    | r3 re-pinned | t65–70 | 13.9% | 22.64 G | [2,2,2,2,2,2] | 0.000 |

    - **Retransmits (sender, whole run):** unsteered 2 / 3 / 0; re-pinned 2682 / 2854 / 4771.
      - For the re-pinned runs, the 1 s intervals starting in t13–20 hold 2681 / 2851 / 4770 of those. The rest of each run holds 1 / 3 / 1.
      - Per second, all but those 1 / 3 / 1 are in t14–16: r1 t15 1001, t16 1680; r2 t14 39, t15 59, t16 2753; r3 t15 1733, t16 3037. That is while the 13 rules were going in; the cell logged `rules added` about 3 s after `client sockets` in each run.
    - **Lowest 1 s aggregate in t10–25:** re-pinned 19.76 G (t14) / 21.38 G (t15) / 21.95 G (t16); unsteered 21.71 / 22.25 / 22.30 G.
    - No stream was below 100 Mb/s in any 1 s interval from t13 to t25, in any run.
  - **File(s)**: none

- **Timestamp**: 2026-09-11 11:18 UTC
  - **Action**: Filed #9778 (LOW; architecture question, not a defect). It asks whether moving an established flow is safe under AF_XDP per-queue UMEM ownership, given #1748's barriered ownership-transfer protocol, with this measurement attached.
    - This follows acceptance 3's "it does" branch. It does not ask to reopen #1748 or #1751.
    - #9778's body was corrected at 11:27 to carry the premise finding below.
  - **File(s)**: none

- **Timestamp**: 2026-09-11 11:25 UTC
  - **Action**: Checked the issue's premise before writing it into the doc. The premise does not hold.
    - #1203's final comment attributed its 49–55% to the flow-fair MQFQ path being gated off for `shared_exact` (citing `cos/queue_ops/pop.rs:112` and `types/cos.rs:432-434`) and to 1024 flow buckets. At #1203's PR head `979482dff` (2026-05-05; merge-base `38881341c`):
      - `cos/admission.rs:495` is `queue.flow_fair = queue.exact;`.
      - `types/cos.rs:103` is `COS_FLOW_FAIR_BUCKETS: usize = 4096` (1024 → 4096 landed in `891f07525`, #1076, 2026-05-01).
      - `pop.rs:112` is the vtime update, with no `shared_exact` gate.
      - `types/cos.rs:432-434` is a doc comment still saying `flow_fair = queue.exact && !shared_exact`. That comment was stale; `0b10d25f3` (#1210, 2026-05-06) scrubbed it.
      - The only live `flow_fair && !shared_exact` is the ECN mark arm selection at `admission.rs:337`.
    - Positive control: the same `git grep` probes find the gate and the old constant where they existed:
      - `565c05b50` `tx.rs:4825` `queue.flow_fair = queue.exact && !queue_fast.shared_exact;`
      - `2bff7a22a` `types.rs:606` `COS_FLOW_FAIR_BUCKETS: usize = 1024`
    - The gate left the code in `2a20cc8a0` (#785 Phase 3, 2026-04-19). `git log -S` on the two forms of the line names that commit as removing one and adding the other. The "post-#914" wording in the `admission.rs` comment is later commentary, not the change.
    - Consequence: #1203's number was not hollowed by a later fix; the reason its controller stayed at 49–55% is unestablished. R1 (`ecdc16f2e`) and HEAD both ran with the same flow-fair and bucket facts as #1203.
  - **File(s)**: none

- **Timestamp**: 2026-09-11 11:30 UTC
  - **Action**: Updated the docs.
    - `docs/fairness-regimes.md`:
      - Renamed § "Why hardware steering does NOT beat the floor" to "Hardware steering and the floor". Nothing linked to the old anchor (`git grep` over the tree, `_Log.md` excluded).
      - The intro now separates static rule sets (#1649, still no) from re-placing established flows (does beat the floor at HEAD) and safety (#9778).
      - The negative-dependence bullet no longer states the move is forbidden by physics. It cites #1765's verdict as standing until #9778 answers.
      - The theorem paragraph is scoped to the Monte-Carlo half.
      - New "Re-placement measured at HEAD (#9488)" with the summary table and how to read it: beats `Cstruct + ε`; residual above Gate 2's ε, but not a Gate 2 verdict; the retransmit burst; why #1203's number is no longer the citation.
      - New paragraph stating R1's 16.8% → 3.8% and how to read it.
      - The #1203 entry in the "three attempts" list is now a bare pointer to that section.
    - `docs/per-5-tuple/state.md` row 4 (#840 / #1203): bare pointer to the same section.
  - **File(s)**: `docs/fairness-regimes.md`, `docs/per-5-tuple/state.md`, `docs/log/9488.md`
  - **Not measured:**
    - the Gate 2 harness method on the re-pinned state;
    - reverse-direction (`-R`) re-pin;
    - HA peer rule mirroring;
    - session teardown/GC after a move;
    - incremental (one-flow) moves.

- **Timestamp**: 2026-09-11 ~12:00 UTC
  - **Action**: Codex round 1 on `6421200b7` returned MERGE NO with six findings. All six were changed in `docs/fairness-regimes.md`:
    - **HIGH, the R1 reading.** "16.8 → 3.8 understates what placement does at HEAD" compared runs taken under different conditions.
      - The doc now says R1 used the same recipe on different code, at a lower aggregate (16.2 G against about 22.7 G). Its smaller delta cannot size the effect at HEAD.
      - Codex's premise that R1 ran with CoS off is not supported by the record. The 16 / 23 Gb/s CoS-on / CoS-off lines it cited come from a separate profiling A/B, and R1's 16.24 G matches that era's CoS-on figure. The doc makes neither claim.
    - **MED, retransmits.** "In the second of the move", "at most 2" and "dipped for one second" were wrong or unsupported. The doc now says:
      - all but 1 / 3 / 1 of the retransmits fall in the 1 s intervals t14–16 (per-second counts above);
      - at most 3 fall in the rest of each run;
      - only the lowest 1 s aggregate is quoted.
    - **MED, the section-opening claim** that the dataplane "locks each flow to the worker that processes its RSS-hashed RX queue". The doc now separates the queue-to-socket binding, which is fixed, from which queue a flow lands on: RSS by default, movable by an ntuple rule.
    - **MED, the `Cstruct + 0.05` range 0.383–0.613.** It used pre-re-pin draws whose exported flow count can include the idle control connection.
      - The comparison is now against the unsteered runs' t45–70 `Cstruct + 0.05` (0.497–0.651), and the early windows' Cstruct is not quoted.
      - The R1 paragraph's "no window more than 16.4 pp below" became 13.9 pp, over the t45–70 windows only.
    - **LOW:** "each re-pinned run ended at 6.7–13.9%" became "every post-re-pin window (t45–70) sat at 6.7–13.9%".
    - **LOW:** "no static rule set beats the RSS floor" is now scoped to uncoordinated ephemeral source ports, matching #1649.
    - The same retransmit wording was corrected in #9778's body, the commit message and the PR body.
  - **File(s)**: `docs/fairness-regimes.md`, `docs/log/9488.md`

- **Timestamp**: 2026-09-11 ~12:10 UTC
  - **Action**: Codex round 2 on `9ff1c9620` returned MERGE NO with three findings. All three are changed:
    - **MED, `docs/per-5-tuple/state.md` row 4.** The pointer left the row's false reason standing ("kernel pins flow→queue at bind"). The row now quotes that as the recorded reason and says what the kernel actually pins: a socket to its RX queue. An ntuple rule moves the flow, and safety is open in #9778.
    - **MED, "each re-pinned run looked like an unsteered one before its re-pin (t10–15)".** The t10–15 window is not before the re-pin, because the rules started going in around t14 and r2 already had retransmits at t14. The clause is gone. The t10–15 rows in the table above are data only, and #9778's table header now says the re-pin starts about t14.
    - **LOW, this log.** "All of them are in t14–16" contradicted the 1 / 3 / 1 remainder beside it. It now says "all but those 1 / 3 / 1".
  - **File(s)**: `docs/fairness-regimes.md`, `docs/per-5-tuple/state.md`, `docs/log/9488.md`

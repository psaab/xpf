# 10016 — trap send failures were absent from `trapsDropped`

- **Timestamp**: 2026-09-17
- **Action**: counted every worker sender error in `trapsDropped`, preserving the exact `accepted == delivered + trapsDropped` accounting; documented that only a nil sender result is delivered.
- **File(s)**: pkg/snmp/traps.go, pkg/snmp/trap_sendfail_10016_test.go, pkg/snmp/README.md
- **RED→GREEN**: pre-fix `TestTrapSendFailure_10016_CountsFailedSends` failed (`trapsDropped = 0`, want 5); after the fix both #10016 regressions passed, including mixed success/failure exactness.
- **Validation**: targeted send-failure + shutdown-drain accounting tests passed; same set passed under `go test -race`; full `go test ./pkg/snmp -count=1` passed with 312 tests.

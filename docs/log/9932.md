# #9932 — the DHCP-client heads did not fold under a brace-elided family

- **Timestamp**: 2026-09-12
- **Action**: Re-confirmed live at `origin/master 371e2b594` before working it —
  the finding was filed during #9620 and master had moved.

  | spelling | strict | compiled |
  |---|---|---|
  | `family inet { dhcp; }` | OK | `DHCP=true` |
  | `family inet dhcp;` | OK | **`DHCP=false`** |
  | `unit 0 family inet dhcp;` | OK | **`DHCP=false`** |

  Then found the same gap on `inet6`. The bare `dhcpv6` keyword the compiler
  reads is declared NOWHERE in the schema (pair reach 0), so the fold cannot
  route it; the properly declared `dhcpv6-client` has the identical defect and
  is what this change fixes. The undeclared-but-compiled `dhcpv6` alias is an
  #8787-class observation, recorded not fixed.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-12
- **Action**: Diagnosed why #9620 H11's boundary walk did not reach it. The walk
  splits a RUN after a container head; `family inet dhcp` is a single statement,
  so the splitter had nothing to split. The blocker was the ADMISSION: only
  `inet address`, `inet6 address`, `filter input` and `filter output` were
  admitted, so the fold declined and the whole run stayed on the family node.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-12
- **Action**: Admission evidence, measured in the order this pass demands rather
  than asserted:

  | pair | schema reach | elided == empty stanza |
  |---|---|---|
  | `(inet, dhcp)` | **1** (`interfaces * unit family inet`) | yes |
  | `(inet6, dhcpv6-client)` | **1** (`interfaces * unit family inet6`) | yes |
  | `(inet6, dhcp)` / `(inet6, dhcpv6)` | **0** — not declared at all | n/a |

  The last row is why the inet6 fix is on `dhcpv6-client` and not on the
  keyword the compiler happens to read.
- **File(s)**: pkg/config/compact_normalize_scope.go

- **Timestamp**: 2026-09-12
- **Action**: Four registers demanded rows, each read off its own instrument:

  - **#8763** `famOnlyCases`: both pairs measure **`recovery`** — the fold
    delivers exactly the braced result, not merely "something changed".
  - **#8768**: fixture statements for the two heads, plus `DHCP`/`DHCPv6` added
    to both family readers so the comparison can see the value at all.
  - **#8852**: both registered blind. Measured the shape rather than assuming
    it: `dhcp` is args:0 with 4 children and no wildcard, `dhcpv6-client`
    args:0 with 6 — the same shape as `firewall family` and `unit family`, so
    the census emits no site and arm 2 adjudicates nothing. A boundary, not a
    clearance.
  - **#9446**: ten spelling-bound notes. This is the one worth reading: the
    admission changed **no verdict**. It moved the REASON one level deeper — the
    pass now clears the outer pair, descends, and is refused by the sub-option
    pair instead, which is why those sites still drop while their pair list
    GREW. The new signatures were taken from the instrument's own output rather
    than typed, and the shared explanation was written once after reading the
    shape, as that test's message requires.
- **File(s)**: pkg/config/compoundkey_traversal_landing_8763_test.go,
  pkg/config/packed_optin_guard_8768_test.go,
  pkg/config/compact_scope_blind_pairs_8852_test.go,
  pkg/config/testdata/compact_block_permanent_exclusions_8690.txt

- **Timestamp**: 2026-09-12
- **Action**: Scope decision, recorded because the alternative was silent. The
  sub-option CHAIN is left unadmitted: `family inet dhcp lease-time 3600;` still
  loses the lease-time. It is measured and ready — all four `dhcp` children and
  all six `dhcpv6-client` children reach exactly one schema path and every one is
  empty-equivalent — but completing both chains is ten more pairs each owing rows
  in four registers.

  Left half-admitted silently it would be the "chain-incomplete" shape #8763
  warns reads as coverage, so it is pinned instead:
  `TestDhcpSubOptionChainIsStillUnadmitted9932` fails if the chain ever folds,
  which forces the register notes to be re-measured with it.
- **File(s)**: pkg/config/dhcp_client_fold_9932_test.go

- **Timestamp**: 2026-09-12
- **Action**: Mutation matrix at `ebb55fc63`, detached worktree. Baseline clean
  at 10 tests. **4 of 4 killed, 0 VOID, 0 escaped.**

  | mutant | killed by |
  |---|---|
  | T1 `inet dhcp` not admitted | inet, family brace elided |
  | T2 `inet6 dhcpv6-client` not admitted | inet6, family brace elided |
  | T3 neither admitted | inet, unit AND family elided |
  | T4 the sub-option chain admitted too | the unadmitted-chain pin |

  T4 is the one that matters for the scope decision: it proves the pin can
  actually detect the chain folding, so the boundary is falsifiable rather than
  a comment.
- **File(s)**: none (scratch harness)

- **Timestamp**: 2026-09-12
- **Action**: Positive controls. All five fold rows fail at
  `origin/master 371e2b594`. The flat-set control PASSES there — it was already
  correct, which is the fact that bounds the exposure to configuration text, so
  a cell holds it rather than the issue's prose alone.

  `pkg/configstore` was run beside `pkg/config` from the first measurement.
- **File(s)**: none (measurement)

# #9977 — the DHCP-client sub-option chains lost their value on a fully elided run

- **Timestamp**: 2026-09-12
- **Action**: Re-confirmed live at `origin/master 82f647173` FIRST, because
  #9620 H9's reader pop had landed since this was filed and might have subsumed
  it. It did not:

  | spelling | compiled |
  |---|---|
  | `family inet { dhcp { lease-time 3600; } }` | `DHCP=true LeaseTime=3600` |
  | `family inet dhcp lease-time 3600;` | `DHCP=true`, **LeaseTime lost** |
  | `family inet6 { dhcpv6-client { client-type stateful; } }` | `DHCPv6=true ClientType=stateful` |
  | `family inet6 dhcpv6-client client-type stateful;` | `DHCPv6=true`, **ClientType lost** |
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-12
- **Action**: Dumped the normalized tree before choosing a route, and it changed
  the route. #9932's fold already places `dhcp` under `family inet`; what
  remains is that the `dhcp` node carries `lease-time 3600` as a PACKED TAIL:

  ```
  elided:  family inet  >  dhcp lease-time 3600
  braced:  family inet  >  dhcp  >  lease-time 3600
  ```

  And `compiler_interfaces.go` used **no `packedBody` at all** — so this is a
  READER gap, not an admission gap.

  I filed this issue pre-measured for the ADMISSION route (ten pairs, each
  reach-1 and empty-equivalent). That route was viable and is now not taken: ten
  pairs each owe rows in the #8763, #8768, #8852 and #9446 registers, to reach
  what two `packedBody` calls reach. Same trade as #9620 H9, and the reason is
  the same — a reader change alters no admission, so those four censuses see
  nothing new.
- **File(s)**: pkg/config/compiler_interfaces.go

- **Timestamp**: 2026-09-12
- **Action**: Register bookkeeping, and the first item is the satisfying one.

  - **The #9932 pin fired exactly as designed.**
    `TestDhcpSubOptionChainIsStillUnadmitted9932` asserted the fully elided run
    DIFFERS from braced — true when written, false now. **REPLACED, not
    deleted**: its other half ("still not admitted") is still true and still
    worth holding, so the replacement asserts both and that they are compatible.
  - **#2419 inventory**: 7 sites now compile equivalently, and the test's own
    message is the rule — *"a stale inventory is an allowlist that hides the next
    regression"* — so those lines are removed.
  - **#8690 register**: its header states its scope as *"the classification of
    every line still in compact_block_divergences_2419.txt"*, so the 7
    classifications go with the 7 inventory lines.
  - **THREE sites stay**: `prefix-delegating`'s two leaves and
    `update-router-advertisement interface` are one level deeper than the node
    this read expands, so they still diverge at their own leaf. Register lines
    kept, signatures re-measured from the instrument's own output rather than
    typed.
- **File(s)**: pkg/config/dhcp_client_fold_9932_test.go,
  pkg/config/testdata/compact_block_divergences_2419.txt,
  pkg/config/testdata/compact_block_permanent_exclusions_8690.txt

- **Timestamp**: 2026-09-12
- **Action**: Mutation matrix at `0c91b07a9`, detached worktree. Baseline clean.
  **4 of 4 killed, 0 VOID, 0 escaped.**

  | mutant | killed by |
  |---|---|
  | V1 no `packedBody` on the inet read | inet dhcp lease-time |
  | V2 no `packedBody` on the inet6 read | inet6 dhcpv6-client client-type |
  | V3 a WRONG schema path | inet dhcp lease-time |
  | V4 the chain admitted after all | the replacement pin's half (a) |

  V3 is the one worth keeping: a wrong path makes `packedBody` a silent no-op —
  it still compiles and still returns a node — so the failure shape is
  indistinguishable from not calling it. V4 proves half (a) can fail, which is
  what stops it being decoration.
- **File(s)**: none (scratch harness)

- **Timestamp**: 2026-09-12
- **Action**: Positive controls. The three sub-option rows fail at
  `origin/master 82f647173`; the bare-head control passes there, since #9932
  fixed it.
- **File(s)**: none (measurement)

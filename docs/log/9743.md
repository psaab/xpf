# #9743 — `display inheritance` expanded groups on the un-normalized tree

- **Timestamp**: 2026-09-12
- **Action**: Reproduced at `origin/master 766da5332`, one configuration written
  two ways:

  | group body | display shows `description inherited` | display shows `instance-type forwarding` | compiled |
  |---|---|---|---|
  | elided (`blue instance-type forwarding description inherited;`) | **no** | **no** | both present |
  | braced (`blue { instance-type forwarding; description inherited; }`) | yes | yes | both present |

  Both spellings commit identically; only the display differed.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-12
- **Action**: Fixed by normalizing the expansion clone in `FormatInheritance`
  AND `FormatPathInheritance` before `ExpandGroupsTagged`, which is what both
  compile cores do. After the change the two spellings' displays are
  BYTE-IDENTICAL, for the whole-config and the path entry point alike, so the
  cells claim equality rather than presence: they are one configuration written
  two ways, and any rendering difference is the defect.

  A separate cell holds the display against the COMPILED config, because
  matching displays alone would be satisfied by breaking both in the same way.
- **File(s)**: pkg/config/ast_format.go,
  pkg/config/inheritance_display_normalizes_9743_test.go

- **Timestamp**: 2026-09-12
- **Action**: Mutation matrix at `b31344249`, detached worktree. Baseline clean
  at 9 tests. **4 of 4 killed, 0 VOID, 0 escaped.**

  | mutant | killed by |
  |---|---|
  | P1 no normalize in FormatInheritance | the two spellings display alike |
  | P2 no normalize in FormatPathInheritance | the path entry point |
  | P3 normalize AFTER expanding | the two spellings display alike |
  | P4 normalize the CALLER's tree too | the no-mutation cell |

  P4 escaped on the first run, and it is the useful one. Every other cell parses
  a fresh tree and renders once, so none of them can tell a read-only render
  from one that also rewrites the caller's tree. `display inheritance` is a
  read-only operation on an operator's candidate and normalizing is a rewrite,
  so that contract now has its own cell.

  Same shape as the #9635 and #9736 matrices: a surviving mutant pointed at a
  missing cell, not at working code.
- **File(s)**: pkg/config/inheritance_display_normalizes_9743_test.go

- **Timestamp**: 2026-09-12
- **Action**: Positive controls. The two equality cells and the elided row of the
  agreement cell fail at `origin/master 766da5332`; the braced row and the
  no-groups control pass there and here.

  `pkg/configstore` was run beside `pkg/config` from the FIRST measurement, not
  at the validation stage — on #9736 a false reject reached the commit-gate
  package and nothing earlier in that lane would have shown it.
- **File(s)**: none (measurement)

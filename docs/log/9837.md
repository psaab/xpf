# #9837 log

## Tagged-unit family MTU above the interface MTU commits clean, kernel refuses

- **Timestamp**: 2026-09-14
- **Action**: STEP-0 at base `9603f2a5f`, worktree
  `.claude/worktrees/9837-vlan-mtu-gate`, branch `fix/9837-vlan-mtu-gate`.
  Confirmed present, not ALREADY_FIXED:
  - the issue repro (vlan-tagging `ge-0-0-2`, `mtu 1400`, unit 50 vlan-id 50,
    `family inet mtu 9000`) commits clean via a throwaway
    `TestStep0VlanMtuGateAbsent9837` (since removed): `ifc.MTU=1400
    unit.MTU=9000 vlanID=50`;
  - `unit.MTU` is the lower of the two family values in the conventional
    inet-first order [QUALIFIED below: the value is ORDER-dependent — the
    inet arm overwrites, the inet6 arm takes min — see the review-round entry]
    (`pkg/config/compiler_interfaces.go`: inet arm sets, inet6 arm takes min);
  - the parent MTU is planned by `planPhysDesired`
    (`pkg/dataplane/compiler_iface_desired.go`), the child MTU is written by
    `applyVLANSubInterfaceMTU9757`, which only `slog.Warn`s on refusal;
  - no commit-time comparison exists (`grep` for MTU exceed/unit-vs-interface
    gates finds only the type comment and the #9727 advisory).
- **File(s)**: none (throwaway probe, removed)

- **Timestamp**: 2026-09-14
- **Action**: Self-tiered SMALL: the issue fully specifies the fix (strict
  reject when a tagged unit's family MTU exceeds the interface-level `mtu`
  with both set; untagged and no-interface-mtu cases unchanged), and the
  strict-gate + lenient-downgrade + end-of-phase wiring shape is the
  established #9424 pattern. Posted the 10-line brief as an issue comment
  (no review rounds per SMALL tier):
  https://github.com/psaab/xpf/issues/9837#issuecomment-5671781917
- **File(s)**: none (issue comment)

- **Timestamp**: 2026-09-14
- **Action**: Fix. New `validateVlanUnitMTUStrict` rejects
  `ifc.MTU > 0 && unit.VlanID > 0 && unit.MTU > ifc.MTU`, naming the
  interface, unit, and both values. The predicate keys on `unit.VlanID > 0`
  — exactly what `mapZoneInterface` branches on (`resolveInterfaceRef`
  reads `unit.VlanID` before the per-unit-tunnel arm, so a tunnel unit
  carrying a vlan-id takes the VLAN child path too) — rather than the
  `vlan-tagging` flag, which the dataplane never consults. Interfaces and
  units are visited sorted for a stable first error. Lenient downgrade via
  new `lenientVlanUnitMTU` (#1960 no-brick; the runtime warning already
  covers the leniently-loaded case). Wired after the address-list gate in
  `runUniformGatesInterfaceAddr`, still at the END of the P6b phase, so no
  earlier first-error slot changes. No dataplane change: the runtime
  warning stays as the backstop for the no-parent-mtu case the commit
  cannot see.
- **File(s)**: `pkg/config/compiler_validate_strict_vlan_mtu_9837.go` (new),
  `pkg/config/compiler_opts.go`, `pkg/config/compiler_uniformgates_interface_addr.go`

- **Timestamp**: 2026-09-14
- **Action**: Cells (`pkg/config/compiler_validate_strict_vlan_mtu_9837_test.go`):
  - `TestVlanUnitMTUAboveParentRejected9837`: the issue repro, refused,
    naming `ge-0-0-2`, `unit 50`, `9000`, `1400`, `#9837`;
  - `TestVlanUnitMTUHierarchicalRejected9837`: the braced spelling refused;
  - `TestVlanUnitMTUControlsAccepted9837`: untagged-above, tagged-without-
    parent-mtu, equal-MTU, below-MTU, and inet-9000/inet6-1300 (effective
    min below parent in the conventional order; that row has since moved to
    the both-orders test) all still commit;
  - `TestVlanUnitMTUInet6OnlyRejected9837`: inet6-only offender refused;
  - `TestVlanUnitMTULenientWarns9837`: tolerant path warns `#9837`;
  - `TestVlanUnitMTUFirstErrorStable9837`: two offenders name `ge-0-0-2
    unit 50`.
- **Validation**: `TestVlanUnitMTU*9837` 6/6 pass, stable over `-count=5`;
  full `go test ./pkg/config/ -count=1` green (184s); full
  `go test ./pkg/dataplane/ -count=1` green (21s). No cluster run: no
  dataplane, VRRP, session-sync or failover code changed. The only other
  in-repo `family inet mtu` fixtures are untagged unit-0 (tunnel/group
  tests), outside the gate predicate.
- **File(s)**: `pkg/config/compiler_validate_strict_vlan_mtu_9837_test.go`

- **Timestamp**: 2026-09-14
- **Action**: PR opened: https://github.com/psaab/xpf/pull/10042
  (`Closes #9837`, test table). Copilot reviewer requested twice, both
  refused, attempt requirement recorded as met: `gh pr edit --add-reviewer
  Copilot` → `GraphQL: Could not resolve user with login 'copilot'`;
  REST `requested_reviewers` with `copilot-pull-request-reviewer` → `422
  Reviews may only be requested from collaborators`. No merge (parent owns
  final review).
- **Action**: Weighed late advisories suggesting per-family (inet vs inet6)
  naming in the error. Declined as extra scope: the issue asks to "name
  both values" (family MTU + interface MTU), which the message does, and
  the compiled unit MTU [was: "effective (min)"; QUALIFIED below as
  order-dependent] is the value the kernel actually refuses.
  Per-family naming would need shadow InterfaceUnit fields
  (fingerprint-adjacent weight, cf. the `json:"-"` on MalformedAddresses)
  or an AST walk inside a typed-config gate (against the #4406 phase split
  the same advisory directs). Predicate, typed-config placement, and the
  #1960 lenient downgrade already match the advisory's primary directive.

- **Timestamp**: 2026-09-14
- **Action**: Parent review: GLM READY + GPT NEEDS-MAJOR, adjudicated that
  the gate is CORRECT (rejects exactly what the runtime would write and the
  kernel would refuse) but the family-min comments overclaimed: the inet arm
  OVERWRITES unit.MTU unconditionally while the inet6 arm takes the MIN, so
  the compiled value is order-dependent (pre-existing quirk, parent files it
  separately; the runtime shares it, so out of scope here). Measured with a
  throwaway probe (since removed): flat-set inet-first compiles 1300 and
  strict accepts; inet6-first compiles 9000 and strict rejects — gate fires
  exactly on the compiled runtime value in both orders.
- **Action**: Micro-fold, per item. (1) Gate comment
  (`compiler_validate_strict_vlan_mtu_9837.go:31`) and test comments now
  describe the order-dependent behavior (conventional order = min) instead
  of an unconditional min. (2) New
  `TestVlanUnitMTUFamilyOrderMatchesRuntime9837` pins gate≡runtime in both
  orders (accept leg asserts compiled 1300; reject leg asserts compiled 9000
  read back through the lenient path); the old single-order min row left the
  controls table to avoid double-pinning. (3) `TestVlanUnitMTULenientWarns9837`
  now asserts `9000`, `1400`, and `#9837` all appear in the downgraded warning.
- **Validation**: `TestVlanUnitMTU*9837` 7/7 pass, stable over `-count=5`;
  full `go test ./pkg/config/ -count=1` green (183s); full
  `go test ./pkg/dataplane/ -count=1` green (20s).
- **File(s)**: `pkg/config/compiler_validate_strict_vlan_mtu_9837.go`,
  `pkg/config/compiler_validate_strict_vlan_mtu_9837_test.go`

- **Timestamp**: 2026-09-14
- **Action**: Parent review delta: GLM READY + GPT NEEDS-MAJOR. GPT's finding
  is REAL and blocking: the v1 gate compared against ifc.MTU, but the planner
  selects an untagged sibling's HIGHER unit MTU as the effective parent
  (`compiler_iface_desired.go:151-156`, pinned by
  `compiler_iface_tagged_parent_mtu_9761_test.go:71-93`). Shape ifc 1400 +
  untagged unit0 1500 + tagged unit50 1500 is runtime-fine (parent runs 1500)
  but v1 refused it against 1400 — a NEW false refusal vs base.
- **Action**: Fix. The gate now computes the planner's effective parent per
  interface: the lowest-numbered REFERENCED untagged non-tunnel sibling with
  an MTU wins, else the interface level. Referenced = named by a
  security-zone ref, parsed as resolveInterfaceRef parses (Cut on the first
  ".", Atoi-or-zero with the error checked per #6940, all zones — the planner
  does not skip mgmt/control); the tagged side stays shape-based per the
  issue. Sibling-source rejections name child, effective parent, source unit,
  and interface MTU. Measured gate≡planner with a throwaway dataplane probe
  (since removed): tagged 1500 → parent plan 1500 = child (gate accepts);
  tagged 1600 → parent plan 1500 < child (gate rejects).
- **Action**: Cells: new `TestVlanUnitMTUUntaggedSiblingOverride9837`
  (override accepts the runtime-fine shape; override raises the bar and still
  rejects above it naming effective 1500; unreferenced sibling does not count;
  lowest sibling number wins). First full-suite run after the fix tripped the
  #6940 census (`n, _ := strconv.Atoi`); rewritten to check the error with
  identical Atoi-or-zero semantics, and the guard is green.
- **Action**: Branch-wide sweep for unconditional-min statements: qualified
  in place at STEP-0 (`unit.MTU is the lower ... in the conventional inet-first
  order`), the cells entry (old min row noted as moved to the both-orders
  test), and the advisory entry ("effective (min)" → compiled unit MTU). Gate
  and test comments were already order-qualified; PR body likewise.
- **Action**: Weighed two further advisories urging per-family (reject-when-
  either-exceeds) semantics. Declined: that fires on runtime-fine configs
  (conventional-order inet 9000 + inet6 1300 under 1400 compiles 1300, kernel
  happy) — the exact NEW false refusal class this delta forbids — and it
  conflates the separately-filed overwrite quirk into this gate.
- **Validation**: `TestVlanUnitMTU*9837` 8/8 pass, stable over `-count=5`;
  full `go test ./pkg/config/ -count=1` green (184s); full
  `go test ./pkg/dataplane/ -count=1` green (21s). No cluster run: no
  dataplane/VRRP/failover code changed.
- **File(s)**: `pkg/config/compiler_validate_strict_vlan_mtu_9837.go`,
  `pkg/config/compiler_validate_strict_vlan_mtu_9837_test.go`

- **Timestamp**: 2026-09-15
- **Action**: Advisory-driven residual: the `ifc.MTU <= 0 → skip` guard ran
  before the sibling scan, so no-interface-mtu + referenced untagged unit0
  1400 + tagged unit50 9000 slipped the gate while the planner runs the
  parent at 1400 and the kernel refuses 9000. Proved with a throwaway probe
  (since removed): strict accepted. The issue's "cannot see the parent MTU"
  premise does not hold when a referenced sibling pins it, and the delta's
  "same effective parent the planner would select" mandate covers it — fixed
  rather than deferred. The sibling scan now runs first; the skip fires only
  when the EFFECTIVE parent is unknown (parentMTU <= 0). Sibling-source
  message gains a "no interface mtu" variant; the no-check bullet is refined
  to "no interface mtu AND no referenced sibling override".
- **Action**: Cells: two subtests in
  `TestVlanUnitMTUUntaggedSiblingOverride9837` (sibling pins parent with no
  interface mtu: rejects 9000 naming effective 1400, accepts 1300). The old
  no-parent-mtu control (no sibling) still accepts — the truly-unknown case
  stays a runtime warning per the issue.
- **Action**: Other advisories weighed. "Truncated delta" is stale (the full
  finding arrived and was folded). "Don't skip on any sibling" is satisfied:
  the gate never skips on siblings, it computes the effective parent
  (lowest-numbered wins, pinned). #6940 and the doc-duplication points were
  already fixed in the previous round (verified: guard green, phrase occurs
  once). The permanent dataplane-side gate≡planner sync cell is disclosed as
  an optional hardening idea, not implemented: each side is pinned
  independently (#9761 for the planner, the sibling cells for the gate) and
  agreement was measured twice with throwaway probes.
- **Validation**: `TestVlanUnitMTU*9837` 8/8 pass (6 sibling subtests), stable
  over `-count=5`; full `go test ./pkg/config/ -count=1` green (168s); full
  `go test ./pkg/dataplane/ -count=1` green (21s). No cluster run: no
  dataplane/VRRP/failover code changed.
- **File(s)**: `pkg/config/compiler_validate_strict_vlan_mtu_9837.go`,
  `pkg/config/compiler_validate_strict_vlan_mtu_9837_test.go`

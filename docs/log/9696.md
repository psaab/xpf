# #9696 — Go neighbor-replace ACK check could not recognise a #6034 fence

- **Date**: 2026-09-14. Worktree `.claude/worktrees/9696-fence-predicate`,
  branch `fix/9696-fence-predicate`, base `origin/master 14f680b3f`.
- **Defect**: Rust fences `generation != 0 && generation <= applied` and ACKs
  its applied generation (`coordinator/mod.rs:712-716`), so every fence ACKs
  `>= gen`; both Go senders only treated `ack != 0 && ack < gen` as fenced
  (`manager_neighbor.go:126`, `manager_generation.go:102`). An `ack > gen`
  fence fell through to the success path: `lastSnapshot.Neighbors` written,
  index rebuilt, diff gone quiet, helper keeping the old table.
- **Reachability (traced, both plan reviewers independently re-derived)**:
  structurally CLOSED in-tree, not live. First `lastSnapshot` exposure is
  always the seeded post-success Compile path; the `pendingXSKStartup`
  deferral requires a prior seeded publish; first-publish failure returns
  unexposed; one `Boot()` Manager per daemon; counter never reset. Landed
  as HARDENING: predicate completion against protocol-legal inputs the
  helper can emit (and the 9684 suite already injects) + doc repair +
  pinning. Exact-match residual (`gen == applied` fenced yet ACKed `== gen`,
  indistinguishable in Go) documented; owned follow-up filed as #10035.
- **Fix**: both senders use `ack != 0 && ack != gen` as the fence arm —
  retain retry debt (no write/rebuild/resolve/mark) and unconditionally
  `seedNeighborReplaceGenerationLocked(ack)` so the retry is strictly
  higher. `RegenerateNeighborSnapshot` returns early; `BumpFIBGeneration`
  falls through to the FIB bump (#1844 contract). Success (`== gen`) and
  legacy (`0`) paths byte-identical. Comments corrected at both senders,
  the retained resolve gates, `protocol_status.go:108-118`,
  `partial_update_outcome_9684.go`, the 9684 test premise, the envelope
  test wording, and `afxdp/README.md:1204-1207`.
- **Plan review**: 3 hostile rounds × Codex (`openai-codex/gpt-6-astra`) +
  GLM (`zai/glm-5.3`), raw outputs in `docs/pr/9696/reviews/` (uncommitted).
  R1 Codex NEEDS-MAJOR / GLM NEEDS-MINOR; R2 Codex NEEDS-MAJOR (text
  repairs) / GLM NEEDS-MINOR; R3 both PLAN-READY. No KILL at any round.
  Substance posted to the issue; process files kept off the branch.
- **Verification**: new cells in
  `neighbor_replace_fence_above_gen_9696_test.go` (F1 fence retains debt +
  seeds at both senders; F2 retry carries 101, lands, resolves; F3
  lower-ACK defensive no-decrease; C1/C2 exact + legacy-zero controls; B1
  Bump still sent incl. injected-error surfacing; R1 Regen bookkeeping
  untouched). Pre-fix check (senders stashed): fence cells FAIL with the
  recorded-as-applied symptom (`cached neighbors = []`); post-fix all
  pass. Full `pkg/dataplane/userspace` suite green; `-race` on the new
  cells green; 5/5 named runs green. No cluster smoke (fault-injection
  determines the predicate; no dataplane surface).

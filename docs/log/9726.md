# #9726: userspace-dp library provenance

- **Timestamp**: 2026-09-11 16:57 UTC
  - **Action**: Researched how the helper links libxdp and libbpf.
    - The build host has libxdp 1.6.3 and libbpf 1.7.0. `Cargo.lock` pins `libbpf-sys` 1.6.3+v1.6.3, which vendors and statically builds libbpf 1.6.
    - `build.rs` links `static=xdp` and a bare `static=bpf` with no search path of its own. `.cargo/config.toml` adds `--allow-multiple-definition`, which would silence duplicate libbpf symbols.
    - A presence probe on a binary is meaningless, because unreferenced functions are garbage-collected, so the link decision had to come from the linker.
  - **File(s)**: none

- **Timestamp**: 2026-09-11 16:57 UTC
  - **Action**: Ran a link trace: `cargo rustc --release --bin xpf-userspace-dp -- -C link-arg=-Wl,--trace -C link-arg=-Wl,-Map=...`.
    - All 477 libbpf archive members come from `target/release/build/libbpf-sys-*/out/libbpf.a` (vendored 1.6). None come from the host's `libbpf.a`.
    - The 10 libxdp members come from the host's `/usr/lib/x86_64-linux-gnu/libxdp.a` (1.6.3).
    - So the helper pairs a libxdp compiled against libbpf 1.7 headers with libbpf 1.6 code, and nothing recorded it.
      - *Correction (Codex round 1, finding 5): not established. pkg-config's libbpf 1.7 is only the build host's current libbpf; it does not identify the headers the prebuilt `libxdp.a` was compiled with.*
  - **File(s)**: none

- **Timestamp**: 2026-09-11 16:57 UTC
  - **Action**: Implemented the record, the range check and the flag canary.
    - `build.rs` exports `XPF_LINKED_LIBXDP_VERSION` (pkg-config), `XPF_LINKED_LIBBPF_VERSION` (from `libbpf-sys`'s `DEP_BPF_INCLUDE`/`bpf/libbpf_version.h`) and `XPF_BUILD_HOST_LIBBPF_VERSION` (pkg-config).
    - A libxdp outside `>= 1.6.3, < 1.7` fails the build, naming the version. A host libbpf whose major.minor differs from the vendored one produces a `cargo:warning`; that is today's state (1.7 vs 1.6), so it warns rather than fails.
    - The bare `static=bpf` is kept, with a comment on why it resolves to the vendored archive.
    - `csrc/xsk_bridge.c` pins `XSK_LIBXDP_FLAGS__INHIBIT_PROG_LOAD == 1` with a `_Static_assert` and exports the value. `src/xsk_ffi.rs` passes a named constant instead of the literal `1`.
    - `ProcessStatus` gains `linked_libxdp_version`, `linked_libbpf_version` and `build_host_libbpf_version` in Rust (`serde(default)`) and Go (`omitempty`), set in `server/lifecycle.rs`. The helper logs the three versions once at startup.
    - **Cells** (`src/xsk_ffi_tests.rs`): `libxdp_inhibit_prog_load_flag_matches_the_header_9726` and `build_records_the_linked_library_versions_9726`.
    - **Docs**: `userspace-dp/README.md`, "Linked library provenance".
  - **File(s)**: `userspace-dp/{build.rs, csrc/xsk_bridge.c, src/xsk_ffi.rs, src/xsk_ffi_tests.rs, src/protocol/status.rs, src/server/lifecycle.rs, README.md}`, `pkg/dataplane/userspace/protocol_status.go`, `docs/log/9726.md`

- **Timestamp**: 2026-09-11 17:22 UTC
  - **Action**: Verified the implementation, then closed two gaps before review.
    - **Verified** on the uncommitted tree at base `b44818428`. `cargo test --release --bin xpf-userspace-dp 9726` compiled from `/var/tmp/wt-9726` and ran both cells, which passed. `go test ./pkg/dataplane/userspace/ ./pkg/api/` passes.
    - **A stale record after an in-place upgrade.** `build.rs` re-ran only when `xsk.h`, `xsk_bridge.c` or `PKG_CONFIG_PATH` changed. A libxdp upgrade that left the header alone would relink the new `libxdp.a` on the next build for any other reason, still under the old recorded version.
      - **Fix.** `build.rs` now also tracks the installed `libxdp.pc`, `libxdp.a` and `libbpf.pc`, each only if it exists; Cargo would rerun the build script on every build for a path that does not.
    - **`PKG_CONFIG` override.** `build.rs` runs the command `PKG_CONFIG` names, as the `pkg-config` crate does, and re-runs when it changes. That is how the out-of-range failure is demonstrated.
    - **The build dependency is declared.** `debian/control` now has `libxdp-dev (>= 1.6.3), libxdp-dev (<< 1.7)`. `make deb` runs `dpkg-buildpackage -us -uc -b` without `-d`, and `debian/rules` builds the helper (`override_dh_auto_build`), so `dpkg-checkbuilddeps` refuses a host outside the range before the build.
  - **File(s)**: `userspace-dp/{build.rs, README.md}`, `debian/control`, `docs/log/9726.md`

- **Timestamp**: 2026-09-11 18:50 UTC
  - **Action**: Codex round 1 returned MERGE NO with 3 HIGH and 4 MED findings. Fixed all seven; the change is uncommitted for review.
    - **HIGH 1: the recorded libxdp version need not describe the linked archive.** `build.rs` read only pkg-config's version, but compiled the bridge against a hard-coded `/usr/include` and let the linker resolve `static=xdp` along its default search path. A `PKG_CONFIG_PATH` naming another libxdp recorded one version and linked another.
      - **Fix.** The bridge compiles against libxdp's pkg-config `includedir`, and `<includedir>/xdp/xsk.h` is tracked. `check_linked_libxdp_archive` runs the C compiler (`cc::Build::get_compiler`) with `-print-search-dirs` and takes the first `libraries: =` directory holding `libxdp.a`. It fails the build, naming both paths, unless that archive canonicalizes to pkg-config's `<libdir>/libxdp.a`. No `rustc-link-search` was added, because an extra `-L` could shadow the vendored `libbpf.a`.
    - **HIGH 2: stale wire fixture.** The three new `ProcessStatus` fields serialize, so `wire_invariant_default_specimens` failed against `tests/fixtures/protocol_wire_v1.json`.
      - **Fix.** Regenerated with `XPF_PROTOCOL_WIRE_REGEN=1`. The diff is exactly `build_host_libbpf_version`, `linked_libbpf_version` and `linked_libxdp_version`, each `""`.
    - **HIGH 3: the re-run was mtime-only.** Cargo's `rerun-if-changed` compares mtimes, so a `libxdp.a` replaced by a file with an older mtime was missed. The 17:22 entry's fix, and the README's claim that an upgrade "cannot relink a new `libxdp.a` under the old recorded version", overstated what it covers.
      - **Fix.** `build.rs` re-runs when `XPF_LINKED_LIBS_STAMP` changes. The Makefile exports it for `build-userspace-dp`, `build-userspace-dp-debug-log` and `test-rust` as the first 32 hex digits of a sha256 over `libxdp.a`, `libxdp.pc` and `libbpf.pc`. `make deb` is covered too, because `debian/rules` calls `build-userspace-dp`. The README and comments now say a raw `cargo build` relies on mtimes alone.
    - **MED 4: libbpf was not bounded.** `libbpf-sys = "1.6"` admitted any later 1.x, nothing checked the libbpf family, and only major.minor was recorded.
      - **Fix.** `libbpf-sys = "~1.6"`; `Cargo.lock` is unchanged (`1.6.3+v1.6.3`). `build.rs` reads the locked `libbpf-sys` version and re-runs when `Cargo.lock` changes. It fails unless the vendored header is 1.6 and the locked libbpf major.minor matches it, and records the full `1.6.3`.
    - **MED 5: host libbpf overclaim.** pkg-config's libbpf is only the build host's current libbpf; it does not identify the headers the prebuilt `libxdp.a` was compiled with. The build warning, `build.rs` comments, the Rust and Go field docs, the startup log line, the README and the 16:57 link-trace entry claimed it did.
      - **Fix.** Each now says "the build host's libbpf (pkg-config)" and claims nothing about what libxdp was compiled against. The 16:57 line is kept and marked as corrected rather than rewritten.
    - **MED 6: the flag canary did not observe production.** It compared a Rust constant with the header, so `create_xsk_binding_impl` passing 0 left it green.
      - **Fix.** `csrc/xsk_bridge.c` fills every socket config through one static helper, `bridge_fill_socket_config`, which sets `libxdp_flags = XSK_LIBXDP_FLAGS__INHIBIT_PROG_LOAD` and `xdp_flags = 0`. Both create functions lost their `libxdp_flags` and `xdp_flags` parameters, and the Rust constant is gone. `bridge_xsk_socket_config_libxdp_flags` returns the flags of a config that helper filled. `socket_config_carries_libxdp_inhibit_prog_load_9726` replaces `libxdp_inhibit_prog_load_flag_matches_the_header_9726` and asserts those flags are non-zero and equal to the header's value.
    - **MED 7: the provenance cell checked only format.**
      - **Fix.** The checks moved to `build_support/libversions.rs`, which uses no process or env. `build.rs` includes it, and `src/main.rs` includes it again under `cfg(test)`. `src/build_libversions_tests.rs` replaces `build_records_the_linked_library_versions_9726` with five cells:
        - `check_libxdp_version`: 1.6.2 and 1.7.0 fail, naming the version; 1.6.3 and 1.6.9 pass; "1.6" is below the range; "1.x" is unparsable.
        - `check_libbpf_family`: 1.6 headers with `1.6.3+v1.6.3` record `1.6.3`; 1.7 headers fail; a 1.7 lock fails.
        - `locked_package_version` and `header_define`, on inline texts.
        - The recorded versions pass the checks, and the recorded libbpf equals the locked version.
    - **Verified** (debug profile, base `99ca99a01` plus this uncommitted change):
      - `wire_invariant_default_specimens` failed before regeneration and passed after it without the variable.
      - A `PKG_CONFIG` wrapper naming a copy of `libxdp.a` in another directory fails the build script with the two-path message. One reporting libxdp 1.7.0 fails with the range message.
      - `make -p` shows the stamp defined for all three targets. With a stub `CARGO`, it expands in the `build-userspace-dp-debug-log` and `test-rust` recipes to 32 hex digits equal to an independent `sha256sum`, and it is absent from `make build-ctl`.
      - `gofmt -l pkg/dataplane/userspace/` is empty and `go vet ./pkg/dataplane/userspace/` passes.
      - `cargo test --bin xpf-userspace-dp 9726 -- --test-threads=1`, compiled from `/var/tmp/wt-9726/userspace-dp` on the final tree: 6 passed (`check_libxdp_version_admits_only_the_validated_range_9726`, `check_libbpf_family_requires_1_6_in_the_headers_and_the_lock_9726`, `locked_package_version_reads_only_the_named_package_9726`, `header_define_reads_only_a_numeric_define_9726`, `recorded_library_versions_pass_the_build_checks_9726`, `socket_config_carries_libxdp_inhibit_prog_load_9726`). No compiler warning points at a changed file. `wire_invariant_default_specimens` passes on the same tree.
      - **Mutation.** The C fill's `libxdp_flags` was set to 0 and `build.rs` made to record `{major}.{minor}`, the two pre-fix behaviours. The same run then failed exactly two cells: `socket_config_carries_libxdp_inhibit_prog_load_9726` ("lets libxdp load its own XDP program") and `recorded_library_versions_pass_the_build_checks_9726` (`"1.6"` against `"1.6.3"`). The four pure cells passed. Both files were restored and checked against the `git diff` saved before mutating; the run above is on the restored tree.
      - `rustfmt --edition 2021 --check` in file mode prints nothing for `build.rs`, `build_support/libversions.rs`, `src/build_libversions_tests.rs` and `src/protocol/status.rs`. For `src/xsk_ffi.rs` (with `src/xsk_ffi_tests.rs`) and `src/server/lifecycle.rs` its output is identical to HEAD's, so this change adds no hunk. `src/main.rs` cannot be checked with `--edition 2021`, here or at HEAD: the crate is edition 2024 and uses let chains.
    - **Limits.** The canary observes the shared fill helper, not each create function's call to it. The archive check reads the compiler's search path and assumes rustc's own `-L` directories (the sysroot and build-script OUT_DIRs) hold no `libxdp.a`; today they do not.
  - **File(s)**: `Makefile`, `userspace-dp/{Cargo.toml, build.rs, build_support/libversions.rs, csrc/xsk_bridge.c, src/main.rs, src/build_libversions_tests.rs, src/xsk_ffi.rs, src/xsk_ffi_tests.rs, src/protocol/status.rs, src/server/lifecycle.rs, tests/fixtures/protocol_wire_v1.json, README.md}`, `pkg/dataplane/userspace/protocol_status.go`, `docs/log/9726.md`

- **Timestamp**: 2026-09-11 19:32 UTC
  - **Action**: Codex round 2 returned MERGE NO with two HIGH and two MED findings. Fixed all four; the change is uncommitted for review.
    - **HIGH 1: the archive check predicted the link from the compiler's default search path, not from the real rustc link.** RUSTFLAGS `-L` or `-C link-arg=-L...`, a build script's `rustc-link-search`, or a configured linker can change the archive the linker takes.
      - **Fix (a).** `build_support/libversions.rs` gains `link_search_overrides(encoded_rustflags)`. It splits `CARGO_ENCODED_RUSTFLAGS` on U+001F and returns each offending flag as written, a flag and its separate value joined by a space:
        - `-L`/`-L<path>` and an `@file` of rustc flags;
        - a `-C`/`--codegen`/`-Z` `link-arg`, `link-args` or `pre-link-arg[s]` value holding a `-L`, `--library-path`, `--sysroot`, `-T`/`--script` or `@` word, directly or in a `-Wl,` list, or a driver `-B`/`-specs` word;
        - a `-Wl,` list token holding one;
        - `-C linker=` and `-C linker-flavor=`, which replace the linker whose search path the check reads.
      - `build.rs` reads `CARGO_ENCODED_RUSTFLAGS` (lossily, so a non-UTF-8 flag is not read as none), re-runs when it or `RUSTFLAGS` changes, and panics naming the flags. The repo's only rustflags, `-C link-arg=-Wl,--allow-multiple-definition` (`.cargo/config.toml`), pass; there is no `userspace-dp/.cargo` and no `~/.cargo/config.toml`.
      - **Fix (b).** `linker_library_dirs` runs `-print-search-dirs` with `RUSTC_LINKER` when Cargo sets it, otherwise with the cc compiler. A configured linker that prints no `libraries: =` line panics: its search path cannot be verified. This repo configures no linker. `build.rs` also re-runs on `CARGO_TARGET_<TRIPLE>_LINKER` and `LIBRARY_PATH` (the driver lists the latter).
      - **Fix (c).** The build fails if `libbpf-sys`'s `OUT_DIR`, the parent of `DEP_BPF_INCLUDE`, holds a `libxdp.a`. The comment and README state that this crate's cc output directory holds only `libxsk_bridge.a` and `ring`'s `OUT_DIR` only its `ring_core` archives; neither is checked.
      - The archive checks now run before the C compile.
      - **Cell (d).** `link_search_overrides_flags_search_path_and_linker_flags_only_9726`: the repo's flags in both spellings, no flags, `-C opt-level=3`, `-lxdp`, `-Wl,--gc-sections`, `-Wl,-Bstatic` and `-fuse-ld=lld` give none. 28 forms are each reported as written, alone and between the repo's flag and `-C opt-level=3`. Two overrides in one string are both reported.
    - **HIGH 2: the version bound ran too late.** It ran after the C compile and the archive check, so a libxdp 1.7 host could fail in the compile, or with "no libxdp.a", without the build naming the version.
      - **Fix.** `main` reads `--modversion libxdp` and runs `check_libxdp_version` first, before the RUSTFLAGS check, the pkg-config directories, the archive check and `cc::Build`. It passes the version to `record_library_versions`, which no longer reads pkg-config for it.
    - **MED 3: the Makefile stamp was fragile.** A `PKG_CONFIG` path with spaces was word-split, and a missing pkg-config or a failed read became the sha256 of empty input.
      - **Fix.** `userspace-dp/build_support/linked-libs-stamp.sh` (POSIX `sh`, `set -eu`) runs `"${PKG_CONFIG:-pkg-config}"` quoted for libxdp's `libdir` and `pcfiledir` and libbpf's `pcfiledir`. It fails with a message if a query fails, a value is empty, or `libxdp.a`, `libxdp.pc` or `libbpf.pc` is not a readable file. Readability is checked before hashing, because dash has no `pipefail`, and the digest's shape is checked after. It prints the first 32 hex digits. The target-specific `$(shell ...)` export is gone. All five cargo lines in `build-userspace-dp`, `build-userspace-dp-debug-log` and `test-rust` are prefixed with `stamp=$$(sh userspace-dp/build_support/linked-libs-stamp.sh) && XPF_LINKED_LIBS_STAMP=$$stamp`, so a stamp failure fails the recipe line.
    - **MED 4: the canary did not observe the config passed to libxdp.** It read a config through the same fill, not the one either create function passed.
      - **Fix.** `csrc/xsk_bridge.c` calls libxdp through two static function pointers, typed with `__typeof__` from libxdp's declarations and defaulting to `xsk_socket__create` and `xsk_socket__create_shared`. The test-only seam `bridge_xsk_capture_socket_create_for_test(int)` points them at captures that record `config->libxdp_flags` and return `-EINVAL` without reading another argument, or back at libxdp's functions; both reset the record. `bridge_xsk_captured_libxdp_flags` reads it. `bridge_xsk_socket_config_libxdp_flags` is removed, and `src/xsk_ffi.rs` declares the seam.
      - `socket_create_passes_inhibit_prog_load_to_libxdp_9726` replaces `socket_config_carries_libxdp_inhibit_prog_load_9726`. It captures `bridge_xsk_socket_create_private` and `bridge_xsk_socket_create_shared` in turn, with null pointers and a dummy ifname, and disables capture before any assertion. It then asserts each returned `-EINVAL` and passed flags that are non-zero and equal to `bridge_xsk_libxdp_inhibit_prog_load_flag()`.
      - **Caught during implementation.** The first build added the seam but left both create functions calling libxdp directly. GCC dropped the write-only pointer stores, and the cell failed with rc -14 (`-EFAULT` from the real `xsk_socket__create`) against -22. After routing both calls through the pointers, `objdump` of the test binary shows each create function calling `*bridge_socket_create` / `*bridge_socket_create_shared`.
    - **Verified** (debug profile, base `a45691950` plus this uncommitted change):
      - `rustfmt --edition 2024 --check` in file mode: zero hunks for `build.rs`, `build_support/libversions.rs` and `src/build_libversions_tests.rs` (zero at HEAD too). `src/xsk_ffi.rs`, which walks `src/xsk_ffi_tests.rs`, has 14 hunks whose bodies are identical to HEAD's 14 in `/var/tmp/wt-9726-review`.
      - `cargo test --bin xpf-userspace-dp 9726 -- --test-threads=1`, compiled from `/var/tmp/wt-9726/userspace-dp`, passed 7: the five pure cells from round 1, `link_search_overrides_flags_search_path_and_linker_flags_only_9726` and `socket_create_passes_inhibit_prog_load_to_libxdp_9726`. No warning names a changed file or the C bridge. `wire_invariant_default_specimens` passes.
      - **Mutation.** With `libxdp_flags = 0` in `bridge_fill_socket_config`, only `socket_create_passes_inhibit_prog_load_to_libxdp_9726` failed ("the private create lets libxdp load its own XDP program", left 0). The file was restored; its sha256 and `git diff` matched those saved before mutating, and the 7 cells passed again on the restored tree.
      - A fake `PKG_CONFIG` reporting libxdp 1.7.0, with a nonexistent `includedir`, made `cargo check --bin xpf-userspace-dp` panic at `build.rs:17` naming 1.7.0. Its call log holds only `--modversion libxdp`, and the build-script output has no cc lines, so it failed before the compile.
      - `RUSTFLAGS="-L /tmp/xpf-9726-alt" cargo check --bin xpf-userspace-dp` panicked at `build.rs:87` naming `-L /tmp/xpf-9726-alt`, with no cc lines. A plain `cargo check` then passed.
      - The stamp script is shellcheck-clean. It prints 32 hex digits equal to an independent `sha256sum`, and the same through a copy of pkgconf at a path containing a space. With `PKG_CONFIG=/nonexistent`, a missing `libbpf.pc` or an empty `libdir` it exits 1 with a message.
      - `make -n` shows the prefix on all five cargo lines of the three targets. A real `make build-userspace-dp-debug-log PKG_CONFIG=/nonexistent` stops with Error 1 before cargo. With a stub `CARGO`, the stamp reaches cargo's environment.
      - No Go file changed.
    - **Limits.**
      - A dependency other than `libbpf-sys` that put a `libxdp.a` in the `OUT_DIR` it adds to the search path would not be seen.
      - The RUSTFLAGS matcher lists known forms; it is not an allowlist of link arguments. A `-C linker=` is refused rather than followed.
      - The seam swaps unsynchronized pointers and relies on the single-threaded suite (#8291).
  - **File(s)**: `Makefile`, `userspace-dp/{build.rs, build_support/libversions.rs, build_support/linked-libs-stamp.sh (new), csrc/xsk_bridge.c, src/build_libversions_tests.rs, src/xsk_ffi.rs, src/xsk_ffi_tests.rs, README.md}`, `docs/log/9726.md`

- **Timestamp**: 2026-09-12 04:29 UTC
  - **Action**: The range proof at `dc5479c25` passed all five cells. Codex round 3 returned MERGE NO with three HIGH and one MED; all four are fixed here.
    - **HIGH 1: a `libxdp.a` in a `LIBBPF_SYS_LIBRARY_PATH` directory was never checked.** libbpf-sys emits `rustc-link-search=native=` for every non-empty `:`-separated entry of `LIBBPF_SYS_LIBRARY_PATH_<TARGET>`, the underscored target spelling AND the bare name — its build.rs loops over all three rather than taking the first — and rustc puts those ahead of the linker's own directories.
      - **Fix.** `libbpf_sys_link_search_dirs` (build.rs) enumerates libbpf-sys's OUT_DIR plus every directory those three names give, each tagged with the mechanism that added it, and the archive check fails if any holds a `libxdp.a`. The three names are also `rerun-if-env-changed`.
      - The reader is the pure `libversions::libbpf_sys_search_dirs`, with a cell over: nothing set, one spelling with several entries, all three set (order preserved), empty entries and an empty value, and a relative entry.
    - **HIGH 2: the check asked the cc crate's compiler, which honours `CC`.** rustc's link step does not, so with an SDK `CC` and a matching `PKG_CONFIG_PATH` the check approved the SDK's archive while rustc linked through plain `cc`. **Fix.** `linker_library_dirs` runs `RUSTC_LINKER` when Cargo sets it and otherwise literal `cc` from PATH, rustc's default linker for this target. The cc crate still selects the compiler for the C bridge, which is what `CC` is for.
    - **HIGH 3: the stamp ran before cargo and never named the version.** An out-of-range libxdp whose package ships no `libxdp.a` failed with "not a readable file" — the ordering defect round 2 fixed, reintroduced one layer out. **Fix.** `linked-libs-stamp.sh` queries `--modversion libxdp` before any file probe and names it in every failure; the range decision stays in `build.rs`.
    - **MED 4: a `cat ... | sha256sum` pipeline hid a read failure.** dash has no `pipefail`, so only sha256sum's status survived and partial input produced a well-formed digest that passed the shape check. **Fix.** One `sha256sum` over all three operands (it fails if any cannot be read), then hash that manifest. The readability precheck is kept for its message, not as the guard.
  - **File(s)**: `userspace-dp/{build.rs, build_support/libversions.rs, build_support/linked-libs-stamp.sh, src/build_libversions_tests.rs, README.md}`, `docs/log/9726.md`

- **Timestamp**: 2026-09-12 04:45 UTC
  - **Action**: The range proof at `f5468a386` passed all seven cells. Codex round 4 returned MERGE NO with two HIGH findings that are ONE root cause, and its linker audit confirmed round 4's `cc`-not-`CC` conclusion (adding that rustc then passes `-B<sysroot>/gcc-ld -fuse-ld=lld`, a directory that holds no `libxdp.a` here).
    - **The root cause: check-then-link.** build.rs VALIDATES an archive and the LINK happens later. Cargo decides whether to re-run a build script by mtime, so a libxdp downgraded to a copy carrying an older mtime is linked against a cached script output that recorded the previous version; a package replacement can also land while cargo runs. Both findings are that window, and no amount of search-path inspection closes it, because the inspection happens at the wrong time.
    - **Fix: link what we validated.** `snapshot_libxdp_archive` copies pkg-config's `<libdir>/libxdp.a` into `OUT_DIR/xpf-linked-libxdp/` and emits a `rustc-link-search` for that directory alone, so `static=xdp` resolves to this build's own copy. If the script does not re-run, the previous snapshot is reused and the binary, its recorded version and its archive stay one consistent set.
      - *Correction (Codex round 5, findings 1 and 2; fixed in round 6): two claims here are wrong. The snapshot was NOT "taken at the moment the version was read" — the version read and the copy were separate operations with a window between them, so a libxdp upgraded in that window recorded one version and copied another, past the range gate. And "resolves to this build's own copy" held only by SEARCH ORDER: the copy kept the name `libxdp.a` and was linked by basename, so a snapshot deleted from `OUT_DIR` without Cargo re-running the script let `-lxdp` fall through to a system or SDK archive with no error.*
    - **What this DELETES.** `check_linked_libxdp_archive`'s prediction, `linker_library_dirs`, the `-print-search-dirs` parsing, the canonical compare, and with them the entire question of which program rustc invokes as the linker — `RUSTC_LINKER`, `CC`, `-B`/`gcc-ld`, `LIBRARY_PATH`. Rounds 2, 3 and 4 each re-litigated that question; the link no longer has to FIND libxdp, so none of it decides anything. This is the "same root cause in successive rounds means remove the mechanism" case.
    - **What remains, and why.** `refuse_link_search_overrides` (RUSTFLAGS `-L` precedes this crate's own) and `refuse_shadowing_libxdp_archives` (a dependency's `-L` precedes it too, so libbpf-sys's OUT_DIR and its three `LIBBPF_SYS_LIBRARY_PATH` spellings are checked).
      - *Correction (Codex round 5, findings 3 and 4; both deleted in round 6). The ordering claim in the second half is BACKWARDS. The real link command puts this crate's `-L` directories (its `OUT_DIR`, then the snapshot directory) BEFORE `libbpf-sys`'s `OUT_DIR`, `ring`'s and the sysroot, so a `LIBBPF_SYS_LIBRARY_PATH` directory could not shadow the snapshot and the check only refused legitimate SDK directories. The first half was incomplete in the other direction: it read `CARGO_ENCODED_RUSTFLAGS` only, and a `cargo rustc -- -Lnative=...` (observed landing ahead of every build-script `-L`) or a rustc wrapper never appears there.*
    - **The limit, restated honestly.** The linked libxdp IS the archive whose version was recorded, because it is the same file. What the build does not do is adopt a host upgrade before the script re-runs.
  - **File(s)**: `userspace-dp/{build.rs, README.md}`, `docs/log/9726.md`

- **Timestamp**: 2026-09-12 05:15 UTC
  - **Action**: Codex round 5 returned MERGE NO with three HIGH and two MED findings. All five are fixed here by one decision — **give the snapshot a name nothing else supplies** — plus a copy/version ordering fix and a docs correction. The change is uncommitted for review.
    - **The decision: `libxpfxdp.a`, not `libxdp.a`.** The snapshot is copied to `OUT_DIR/xpf-linked-libxdp/libxpfxdp.a` and linked `static=xpfxdp`. Linking it by the BASENAME `libxdp.a` selected it only by search ORDER, and that is what findings 2, 3 and 4 each attacked from a different side. A name no distribution, SDK or dependency `OUT_DIR` ships answers all three at once:
      - **HIGH 2 (the snapshot was linked by basename, and absence was not fatal).** A snapshot deleted from `OUT_DIR` without Cargo re-running the build script let `-lxdp` fall through to a later directory, and the binary reported the cached version of an archive it no longer linked. Now the link fails: `rust-lld: error: unable to find library -lxpfxdp`. Failure is the point — nothing else can answer to that name.
      - **HIGH 3 (the flag check read only `CARGO_ENCODED_RUSTFLAGS`).** A `cargo rustc -- -Lnative=/tmp/old-xdp`, or a rustc wrapper, adds a search path that never appears in that variable. Measured: such a `-L` lands AHEAD of every build-script `-L`, so under the old scheme it would have won. It cannot supply a `libxpfxdp.a`, so it decides nothing.
      - **MED 4 (`refuse_shadowing_libxdp_archives` rested on a reversed ordering claim).** Confirmed on the real link command — this crate's `OUT_DIR` and snapshot directory come BEFORE `libbpf-sys`'s `OUT_DIR`, `ring`'s and the sysroot. The check could only have rejected a legitimate `LIBBPF_SYS_LIBRARY_PATH` SDK directory that was not on the shadowing side at all. Deleted.
      - The emission order `static=xpfxdp` BEFORE `static=bpf` is kept: libxdp's undefined libbpf symbols are resolved by the `-lbpf` that follows. Verified by linking, not by reasoning (below).
      - The snapshot directory is also swept each run, so a snapshot an earlier revision wrote as `libxdp.a` does not linger on a directory that is on the search path. The live `OUT_DIR` in this worktree held exactly that, plus an 8-byte `libxdp.a` decoy from a round-5 experiment.
    - **HIGH 1: the version/copy window.** `check_libxdp_version` ran on a version read BEFORE the copy, so a libxdp upgraded between the two recorded 1.6.3 while copying and linking 1.7.0 — past a gate that had already passed. `snapshot_libxdp_archive` now reads the source's bytes, writes the snapshot FROM those bytes (so the linked archive is that content by construction), reads `--modversion libxdp`, range-checks it, then re-reads the source and fails unless it still holds the copied bytes and reports the same version as the pre-copy read. The early read and range check stay, so an out-of-range host still fails naming the version before the C compile (the round-2 HIGH 2 property).
    - **MED 5: the claims.** The README's "Linked library provenance" and the round-5 log entry described the deleted linker-search inspection, stated the `-L` ordering backwards, listed rerun triggers that no longer exist (`CARGO_TARGET_<TRIPLE>_LINKER`, `LIBRARY_PATH`, `CARGO_ENCODED_RUSTFLAGS`, `RUSTFLAGS`), and said the snapshot was taken "at the moment" the version was read. The README section is rewritten around the unique name, the measured ordering, the exact `cargo:rerun-if-*` list the script emits (plus the `cc` crate's own `CC`/`CFLAGS`/`AR` triggers), and the honest limits: a host upgrade is adopted only when the script re-runs, and the build trusts pkg-config to describe the archive in the `libdir` it names. The round-5 entry above carries inline corrections rather than a rewrite.
    - **What this DELETES.** `refuse_link_search_overrides`, `refuse_shadowing_libxdp_archives` and `libbpf_sys_link_search_dirs` (build.rs); the pure `link_search_overrides`, `option_overrides_link_search`, `link_word_adds_search` and `libbpf_sys_search_dirs` (`build_support/libversions.rs`); and their two cells. Rounds 2, 3, 4 and 5 each found a new hole in that apparatus — an unread flag source, an unenumerated environment spelling, a reversed ordering. The unique name removes the question instead of answering it again. Every version/lock check is untouched.
    - **Verified** (debug profile, base `dcd6805e3` plus this uncommitted change):
      - `cargo build --bin xpf-userspace-dp` succeeds. `cargo rustc --bin xpf-userspace-dp -- -C link-arg=-Wl,-Map=...`: all 10 libxdp member lines name `.../build/xpf-userspace-dp-fd0e8aa659760ea5/out/xpf-linked-libxdp/libxpfxdp.a` (`xsk.o`, `libxdp.o`, the three `.embed.o`), and the map contains ZERO occurrences of the string `libxdp.a` — no system archive is linked. The 1038 libbpf member lines all come from `build/libbpf-sys-*/out/libbpf.a`.
      - `sha256sum` of the snapshot equals `/usr/lib/x86_64-linux-gnu/libxdp.a`.
      - **Absence is fatal.** Deleting `out/xpf-linked-libxdp/libxpfxdp.a` and touching `src/main.rs` (which does not re-run the build script) fails the rebuild: `rust-lld: error: unable to find library -lxpfxdp`, `error: linking with cc failed`, exit 101. Restored from a copy; the following build succeeds.
      - **A stray `-L` decides nothing.** A decoy directory holding a bogus `libxdp.a`, passed as `cargo rustc -- -Lnative=<decoy>`, builds clean and its map still names only the snapshot; the decoy path appears nowhere in the map. `--print link-args` shows that `-L` ahead of every build-script `-L`, which is the ordering finding 3 depends on and finding 4 got backwards.
      - **The two new checks fire, and a positive control separates them from "a wrapper is in use".** A `PKG_CONFIG` wrapper that reports 1.6.3 on its first `--modversion libxdp` and 1.6.4 on its second (both in range, so the range gate cannot catch it) fails the build at `build.rs:185`: "libxdp's pkg-config version changed from 1.6.3 to 1.6.4 while this build snapshotted ...". A second wrapper that leaves the version at 1.6.3 but appends to the archive during that second call fails at `build.rs:175`: "... changed while this build was recording its version (138430 bytes, then 138439) ...". A third wrapper of the same shape that changes nothing builds clean.
      - `cargo test --bin xpf-userspace-dp 9726 -- --test-threads=1`: 6 passed, 0 failed (was 8: `link_search_overrides_flags_search_path_and_linker_flags_only_9726` and `libbpf_sys_search_dirs_reads_every_spelling_in_order_9726` went with the functions they tested). The remaining six are `check_libxdp_version_admits_only_the_validated_range_9726`, `check_libbpf_family_requires_1_6_in_the_headers_and_the_lock_9726`, `locked_package_version_reads_only_the_named_package_9726`, `header_define_reads_only_a_numeric_define_9726`, `recorded_library_versions_pass_the_build_checks_9726` and `socket_create_passes_inhibit_prog_load_to_libxdp_9726`.
      - `rustfmt --edition 2024 --check` in file mode: zero hunks for `build.rs`, `build_support/libversions.rs` and `src/build_libversions_tests.rs`. No build warning names a changed file.
    - **Limits.** The unique name defeats a `-L` that supplies a `libxdp.a`; it does not defeat someone who writes a `libxpfxdp.a` into a directory that precedes `OUT_DIR` on purpose, which is indistinguishable from editing the build script. The two new copy/version checks close a window, they do not make the pair atomic: a source archive that changed and changed back inside it would pass. And no unit cell can assert the link — the evidence for the unique name is the link map and the deletion probe above, not a test.
  - **File(s)**: `userspace-dp/{build.rs, build_support/libversions.rs, src/build_libversions_tests.rs, README.md}`, `docs/log/9726.md`

- **Timestamp**: 2026-09-12 06:05 UTC
  - **Action**: Codex round 6 returned MERGE NO with one HIGH and two MED. The HIGH is the gap in what round 6 built — libxdp was made unshadowable and its SIBLING was left exposed. All three fixed here; the change is uncommitted for review.
    - **HIGH 1: the linked libbpf was still shadowable.** `build.rs` re-emitted `libbpf-sys`'s bare `static=bpf`, which resolves by search ORDER, and this host ships a static **libbpf 1.7.0** in `/usr/lib/x86_64-linux-gnu`. A `cargo rustc --bin xpf-userspace-dp -- -Lnative=/usr/lib/x86_64-linux-gnu` puts that directory ahead of every build-script `-L` (the ordering round 6 measured), so the link took the host's 1.7.0 while `XPF_LINKED_LIBBPF_VERSION` reported the vendored 1.6.3 — #9726's acceptance criterion failing for the other library. Round 6's unique name did nothing for it, and the `refuse_link_search_overrides` round 6 deleted had covered some RUSTFLAGS spellings of it.
      - **Fix.** `snapshot_libbpf_archive` copies `libbpf-sys`'s vendored `libbpf.a` (from `DEP_BPF_INCLUDE`'s parent, which this script already resolves) into the same snapshot directory as **`libxpfbpf.a`**, and `build.rs` emits `static=xpfbpf` BEFORE the bare `static=bpf`. The order is the one libxdp already needed: libxdp's undefined libbpf symbols are resolved by the libbpf that follows it, so the snapshot has to precede the bare name.
      - The directory sweep moved into `prepare_snapshot_dir`, which now keeps both snapshot names and removes everything else.
      - No version/copy window is closed for libbpf, deliberately: the archive and the `libbpf_version.h` whose version is recorded are both outputs of the `libbpf-sys` build script in the SAME cargo invocation, inside a target directory Cargo locks — not files a host package manager can replace mid-build. The asymmetry is stated in the code comment and the README rather than papered over with a check that could not fire.
    - **MED 2: the merge metadata described deleted behaviour.** Corrected on every surface, not the remembered ones: `csrc/xsk_bridge.c`'s header comment (said "Linked against -lxdp"), `userspace-dp/README.md`, the commit message (`scratchpad/9726-commit-msg.txt` — claimed RUSTFLAGS/linker/`LIBRARY_PATH` rerun triggers, the two deleted cells and "8 passed"), and the PR body (`scratchpad/9726-pr-body.md` — same claims, plus the two deleted cells in its test list). `_Log.md` and `docs/issues/pr-history.md` also match `-lxdp`, but both are historical records of `test/xsk-repro`, a different program, and are left alone.
    - **MED 3: "total possession" was overstated.** The README and the `build.rs` comment now say what the unique names actually buy — no ordinary build (system, SDK, dependency `OUT_DIR` or repo path) supplies either name, so ACCIDENTAL substitution is gone — and list what still could: a planted `libxpfxdp.a`/`libxpfbpf.a` on an earlier `-L`, a linker script or explicit archive argument naming one, a custom linker or rustc wrapper, or replacing a snapshot after the build script runs. Each has to be constructed deliberately by someone who could edit `build.rs`; no machinery was added for them.
    - **Confirmed by the reviewer and worth not re-deriving.** (a) The version/copy bound is honest as stated: equality is observational and cannot exclude change-and-change-back; atomicity would need a package-manager lock, a filesystem snapshot, or version metadata inside the archive. (b) The sweep is safe on Cargo's layouts: the directory is this build unit's own, scoped per package/profile/target, Cargo serializes work in a target directory, only immediate non-live entries are removed, a directory fails rather than being recursed into, and a symlink is unlinked rather than followed.
    - **The base moved under this round.** Round 6 was reported uncommitted at `dcd6805e3`; while round 7 was being implemented an external process committed it as **`29ab57795`** — carrying the round-5 commit MESSAGE, i.e. the stale text MED 2 is about (RUSTFLAGS/linker/`LIBRARY_PATH` rerun triggers, the two deleted cells, "8 passed"). The corrected message is in `scratchpad/9726-commit-msg.txt`; **`29ab57795` still needs a reword from that file**, because a scratchpad fix does not reach a commit that already exists. Round 7's own changes are left uncommitted on top.
    - **Verified** (debug profile, base `29ab57795` — round 6 committed — plus this uncommitted change):
      - **The libbpf map, with the host directory forced ahead.** `cargo rustc --bin xpf-userspace-dp -- -Lnative=/usr/lib/x86_64-linux-gnu -C link-arg=-Wl,-Map=...`: **18 distinct libbpf members, every one from `.../out/xpf-linked-libxdp/libxpfbpf.a`** (`bpf.o`, `btf.o`, `btf_iter.o`, `btf_relocate.o`, `elf.o`, `features.o`, `gen_loader.o`, `hashmap.o`, `libbpf.o`, ...), **ZERO from `/usr/lib/x86_64-linux-gnu/libbpf.a`** and zero from any other `libbpf.a` — the trailing bare `-lbpf` extracted nothing. libxdp in the same map: 5 distinct members, all from `libxpfxdp.a`, none from a system `libxdp.a`. The complete archive inventory of that link is `libxsk_bridge.a`, the two snapshots, and the host's `libelf.a`/`libz.a`/`libzstd.a`.
      - **Positive control.** With ONLY the `static=xpfbpf` emission commented out and the same `-Lnative=/usr/lib/x86_64-linux-gnu`, the map takes **17 distinct members from `/usr/lib/x86_64-linux-gnu/libbpf.a`** and zero from the vendored archive — the defect reproduced, and the one line is what prevents it. `build.rs` was restored; its sha256 and the full `git diff` match the copies saved before mutating.
      - Deleting `libxpfbpf.a` from `OUT_DIR` and touching `src/main.rs` fails the rebuild with `rust-lld: error: unable to find library -lxpfbpf`, the same fail-closed behaviour as libxdp. Restored.
      - A plain build (no decoy `-L`) links both snapshots and no `libbpf.a`/`libxdp.a` at all.
      - `cargo test --bin xpf-userspace-dp 9726 -- --test-threads=1`: 6 passed, 0 failed. `rustfmt --edition 2024 --check` in file mode: zero hunks for `build.rs`.
      - Counting note: the first reading of these maps was `grep -c`, which counts LINES (1038 vs 62) and not members — one member appears on several section lines. The numbers above are DISTINCT members.
    - **Limits.** libelf, zlib and zstd are still taken from the host, unrecorded and unpinned; #9726 covers the two libraries whose ABI the dataplane depends on, and the README now says so rather than leaving it implied.
  - **File(s)**: `userspace-dp/{build.rs, csrc/xsk_bridge.c, README.md}`, `docs/log/9726.md`, and the uncommitted merge metadata in the session scratchpad (`9726-commit-msg.txt`, `9726-pr-body.md`)

- **Timestamp**: 2026-09-12 10:57 UTC
  - **Action**: Codex round 7 returned **MERGE YES** — the first clean verdict — and `make test` was green. Recording two harness findings from the range proof, because both are traps any lane can hit and neither is about this change.
    - **`RUSTFLAGS` REPLACES `build.rustflags` from `.cargo/config.toml`; it does not merge.** Two range-proof cells set `RUSTFLAGS="-L <dir>"` to test a hostile search path, and thereby silently dropped this repo's own `-C link-arg=-Wl,--allow-multiple-definition`. The link then failed with `rust-lld: error: duplicate symbol: crc32` — a symbol NEITHER snapshot defines: `nm` puts it in the host's `libelf.a` and `libz.a`, which the helper links as bare `-lelf -lz`, and `.cargo/config.toml` carries that link-arg for exactly this collision and says so. The cell changed two variables while claiming to change one, and the failure named a symbol that reads as a snapshot problem. The contrast is what makes it actionable: `cargo rustc -- -Lnative=...` ADDS a flag and does NOT replace the config rustflags, which is why those cells passed throughout. Fixed by carrying the repo's flags explicitly in the RUSTFLAGS cells.
    - **A link-map cell that does not RELINK writes no map at all**, and every count then reads zero — indistinguishable from "linked nothing", which is what a failure looks like. The cells now `touch src/main.rs` to force the relink and REFUSE an empty map as a broken premise rather than scoring it. Same class as the snapshot-removal cell, which could not hold its premise at all (the build script re-runs on every invocation here and recreates the snapshot) and now says so; the fall-through property is asserted structurally instead — the build links two uniquely named archives and neither name exists anywhere on this filesystem outside a target directory.
    - **The deleted guards were re-checked under the corrected rule** ("a package-wide green under a mutant is evidence about COVERAGE first; delete only what is UNREACHABLE, not merely unreached"). Enumerated rather than reasoned: `find` over `/usr/lib`, `/usr/local/lib`, `/usr/lib64`, `/lib`, the cargo registry, rustup and then the whole filesystem `-xdev` finds neither unique name outside our target directories. With the decoy measurement — a hostile `-L` at the host libdir still links 5 snapshot libxdp members and 18 snapshot libbpf members, zero host members — the deletion rests on unreachability plus a positive control.
  - **File(s)**: `docs/log/9726.md`

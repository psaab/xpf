# #9635 — authored quote/bracket provenance in the packed split

- **Timestamp**: 2026-09-12
- **Action**: Reproduced both witnesses at `origin/master 773146dd4`.

  | spelling | verdict at master |
  |---|---|
  | `security { ike { policy P1 { proposals [ P "proposal-set" ]; } } }` | REFUSED, `proposal-set: missing value` |
  | the same, brace-elided | REFUSED, same |
  | `pre-shared-key ascii-text "s" "mode" aggressive;` | OK, two statements — must stay that way |

  The normalized tree at master shows the split: `proposals P` + `proposal-set`.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-12
- **Action**: The fix, in one change rather than two. `splitPackedStatements8768`
  gained a `valueMask` and extends a `multi` / `valueList` statement over the
  tokens the operator authored as values; the #8437 fusion gate in
  `schema_walk.go` skips the same tokens. Teaching only the splitter was
  measured: the split is then declined and the gate refuses the result, so the
  spelling is unusable either way.

  Both call sites already computed the masks — call site 2 even carried the
  comment "The split DECISION does not read them yet; that ... is issue 9635" —
  so this is plumbing that was left deliberately ready.
- **File(s)**: pkg/config/packed_value_provenance_9635.go,
  pkg/config/compact_normalize_8662.go, pkg/config/schema_walk.go

- **Timestamp**: 2026-09-12
- **Action**: A bug I introduced and then killed with its own cell. At call site
  1 I built the mask from `len(node.Keys)` AFTER the truncation that shortens
  `node.Keys` to the container's identity, so the splitter got an empty mask and
  only the BRACED spelling worked. `M5-mask-offset-wrong` restores that ordering
  and is killed by the brace-elided row alone — the braced row cannot see it.
- **File(s)**: pkg/config/compact_normalize_8662.go

- **Timestamp**: 2026-09-12
- **Action**: Mutation matrix at `4f35a4971`, detached worktree.
  Baseline clean at 16 tests. **7 of 7 killed, 0 VOID, 0 escaped.**

  | mutant | killed by |
  |---|---|
  | M1 splitter ignores provenance | value spells a sibling keyword |
  | M2 gate ignores provenance | the authored spelling stops committing |
  | M3 arity ignored | fixed-arity, two statements |
  | M4 mask always nil | value spells a sibling keyword |
  | M5 mask offset wrong | brace-elided spelling only |
  | M6 gate skips every sibling | non-packed container |
  | M7 run ignores the mask | bare statement after a bracketed list |

  Three cells escaped on the first run and every one was a fault in the CELL:

  - **M3** compared compiled output, and removing the arity rule left the
    compiled output identical — the compiler also reads the packed tail, so the
    whole `pkg/config` suite stayed green with the rule deleted. The issue states
    that acceptance as "keeps committing as two statements", so the statement
    list is now asserted directly.
  - **M7** had no cell at all: in every other fixture the authored tokens run to
    the end of the tail, so an unbounded run gives the same answer.
    `proposals [ P "proposal-set" ] mode aggressive;` is the shape that tells
    them apart.
  - **M6** is the one worth recording. Deleting the gate's provenance clause left
    the ENTIRE package green, which reads as an unfalsifiable clause to remove —
    and I nearly removed it. It is a live guard: every fixture in the package sat
    under a `packedStatements` container, where the splitter separates a bare run
    before the gate sees it, so the gate's answer could not matter. 166
    containers are not `packedStatements` while holding a `multi` leaf with a
    sibling, and under one of them the fused `application-set B` COMMITS without
    the clause and is silently dropped.

    "No test notices" was a fact about the tests, not about the code. A
    package-wide green under a mutant is evidence about COVERAGE first.
- **File(s)**: pkg/config/packed_authored_value_9635_test.go

- **Timestamp**: 2026-09-12
- **Action**: Positive controls. The four fix rows fail at `origin/master
  773146dd4`; the control rows pass there and here. One row was relabelled after
  measuring it at master: a lone quoted value fits inside the leaf's arg span, so
  nothing ever tried to split it — it was never broken, and calling it a
  demonstration of the fix would have been wrong.
- **File(s)**: pkg/config/packed_authored_value_9635_test.go

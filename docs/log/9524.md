# #9524 — address carrying a prefix and an unimplemented value form

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified at origin/master `c7a497c5c` with throwaway probes
    (removed). `config.SchemaValidate`, `config.CompileConfig`,
    `config.CompileConfigLenient` and `configstore.CheckText` all accepted, with
    no warning, a prefix plus `dns-name`, `wildcard-address` or `range-address`,
    in both token orders and both parser shapes. The book held only the prefix
    and the mirror reported nothing. A `deny` on the object under `permit-all`
    let 192.0.2.5 (named by its range-address) and 10.0.0.1 (wildcard) through.
    It narrows and never widens. Control: a sole `dns-name` is strict-rejected
    when referenced (#3149) and poisons the snapshot on the tolerant path.
  - **File(s)**: none (probes deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Recorded unimplemented value forms on `Address`, carried them
    through the zone-local fold, added a strict gate with a lenient downgrade,
    a warn-validator message, and `UsableValue()` read by the policy-only
    representability resolver and the junos-host deny projection. Left
    `policyMatchAddressBookResolves` (shared with the NAT strict gate), static-NAT
    prefix-name resolution, and the legacy `resolveUserspaceAddressBookEntry`
    unchanged. A trial switch of that last one was dropped before commit: the
    #3261 sentinel already replaces a poisoned rule's legacy addresses, so it was
    unobservable and would only have produced an equivalent mutant.
  - **File(s)**: pkg/config/types_security.go,
    pkg/config/compiler_security_addressbook.go,
    pkg/config/address_unimplemented_forms_9524.go,
    pkg/config/compiler_uniformgates_cluster_zone.go, pkg/config/compiler_opts.go,
    pkg/config/compiler_validate_warn.go, pkg/config/junos_host_deny_resolve.go,
    pkg/dataplane/userspace/policies_representable.go, pkg/config/README.md,
    docs/userspace-dataplane-gaps.md
- **Timestamp**: 2026-09-10
  - **Action**: Cells on config (strict/tolerant matrix, controls, junos-host
    resolver), configstore (CheckText), the userspace wire, and the simulator.
  - **File(s)**: pkg/config/address_unimplemented_forms_9524_test.go,
    pkg/configstore/address_unimplemented_forms_9524_test.go,
    pkg/dataplane/userspace/address_unimplemented_forms_9524_test.go,
    pkg/policymatch/address_unimplemented_forms_9524_test.go
- **Timestamp**: 2026-09-10
  - **Action**: Go mutation matrix, 14 cells, scored on `go test -json`
    `Action=="fail"` against each cell's target names. Unmutated control
    collected 6/6 with no failures; 13/13 mutants KILLED (recording severed in
    both shapes, zone-local fold, gate call site, lenient downgrade and its
    constructor flag, `UsableValue`, both reader switches, warn arm, gate
    over-reach on a sole form, gate leak of synthetic zone-local names, flat
    check recording any token); 0 SURVIVED, 0 VOID; every mutation confirmed
    applied by occurrence count; tree clean after the run.
  - **File(s)**: none (scratch matrix)
- **Timestamp**: 2026-09-10
  - **Action**: `make test` (Go + Rust) at 60e8c6929: 77 Go packages ok, 0 FAIL;
    Rust 5748 passed, 0 failed.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Landing rule applied: master MOVED since the tested base
    (c7a497c5c -> c71f9d556) and `master_delta ∩ mine` is EMPTY, and no file a
    mutant mutated changed on master. So the merged tree (60e8c6929 merged with
    c71f9d556) was compiled in a throwaway worktree first: `go build ./...` ok;
    `go vet ./...` differential, line numbers normalised, 0 diagnostics on
    master alone, 0 on the merged tree, 0 new; `cargo check --tests --benches`
    in userspace-dp ok. The commit was then amended to add these log entries
    only (no code change), and it lands via `--merge --match-head-commit`,
    with the merge commit's first parent checked against c71f9d556.
  - **File(s)**: docs/log/9524.md

# #9525 — the tolerant application-spec downgrade installs a different term

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified the issue at e90b9f714 and again at c71f9d556, on
    four channels:
    - `config.CompileConfig` and `configstore.CheckText` (strict);
    - `config.CompileConfigLenient`, followed through the userspace policy wire,
      `dpuserspace.PolicyContentRejectionReasons` and `policymatch.Match` (Go
      plane).

    Both issue fixtures reproduce. icmp `destination-port 80` under a deny never
    fires. A term `icmp-type 999` under a permit admits type 13. Mirror reasons
    are empty for both. `source-port 80` on icmp matches only a sender whose
    Identifier is 80 (Rust `parse_flow_ports` presents the ICMP Identifier as
    `src_port`).
  - **File(s)**: none (probes deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Cohort audit: every arm `lenientApplicationSpecs` downgrades
    (the spec, syntax and structure gates), each measured on the tolerant
    channel with a permit under deny-all or a deny under permit-all.
    Dispositions:
    - ALREADY REFUSED (the #2124 sentinel, one mirror reason): a malformed port
      (invalid, 0, inverted; destination and source), a missing protocol, and
      an unresolvable protocol.
    - REFUSE (fixed here):
      - `destination-port` on icmp, gre or sctp: never matches.
        `protocolIsPortBearing` is tcp/udp only, and the simulator had wrongly
        permitted sctp/80.
      - `source-port` on icmp or icmpv6: matches only the chosen Identifier;
        on gre it never matches.
      - `UnknownICMP`, as a type or a code, direct or in a term: widens to
        every type or code.
      - An icmp field on tcp, or a code without a type: the helper already
        refuses (#3712 `InvalidApplicationIcmpFields`) while the Go mirror
        certified permit or default.
      - A dangling match leaf (destination-port, icmp-type): widens.
      - A conflicting match leaf (destination-port, protocol): a deny
        under-covers.
      - A direct body mixed with terms, including through a nested set: a
        deny under-covers.
    - BENIGN, still installed (the match set is unchanged): a malformed
      `inactivity-timeout`; a dangling `inactivity-timeout` (direct and term);
      a conflicting `inactivity-timeout`; a conflicting `alg`. `alg bogus` is
      not a member at all, because strict accepts it.
    - DEFERRED under a prior decision: `UnknownDirectLeaves`,
      `UnknownTermLeaves` and `UnknownMembers`. A misspelled `destination-poort
      22` permits all TCP; a misspelled set member under-covers a deny.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Documented contract and prior decisions:
    - The `lenientApplicationSpecs` doc (#2142) says a leniently loaded bad app
      "is inert rather than silently mis-matching". That held only for drops the
      expansion could see.
    - `pkg/policymatch/app_unknown_recovery_6524_test.go`
      `TestStrayStatementDoesNotDisarmSiblingLeaves` explicitly decides that a
      stray statement must not disarm a well-formed application. That statement
      is recorded exactly as `UnknownDirectLeaves=["bogus"]`, so every
      unrecognized-statement record stays out of the predicate. Filed #9595 with
      the widening measured and the options.
    - `pkg/policymatch/app_inline_term_icmp_dup_6766_test.go` pinned keep-last
      ENFORCEMENT at the verdict. The first run with the fix reddened exactly
      those two cells. Chasing why: their own header calls the narrowing "the
      thing the gate exists to prevent", and what they protect is keep-last
      over keep-first, which stays bound at the compiled struct in pkg/config.
      So those cells now assert refusal for both values.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Implemented. `config.ApplicationMatchDrops` covers one
    application; `config.ApplicationReferenceMatchDrops` walks the reference as
    the expansion resolves it (application first, then sets through
    `memberIsNestedSet`, depth 3) and adds `MixedDirectTermApps`. Only match
    leaves count, via `applicationMatchLeaves9525`, which with
    `applicationSettingLeaves9525` partitions `valueTakingApplicationLeaves`.
    `expandUserspacePolicyApplications` refuses a reference with any drop, so
    the rule lowers to the #3261 sentinel, and the mirror (which re-runs the
    expansion per application) names it. The strict `source-port` text now
    names the ICMP Identifier, the destination-port text no longer claims ports
    are 0, and the #3373 comment's "TCP/UDP/SCTP" is corrected.
    - Not changed: NAT application terms (`buildSourceNATAppTerms`,
      `appTermFor`) are lowered separately and carry no ICMP constraint at all;
      the app-id catalog only stamps flow app ids for events. No Rust preflight
      arm was added: the Go sentinel reaches the existing refusal, and the issue
      called that arm optional.
  - **File(s)**: pkg/config/application_match_drops_9525.go,
    pkg/dataplane/userspace/capabilities.go,
    pkg/config/compiler_validate_strict_application.go,
    pkg/config/compiler_opts.go
- **Timestamp**: 2026-09-10
  - **Action**: Cells:
    - pkg/config: every refused member (first confirmed strict-rejected),
      naming its own arm; accepted controls (a clean port, a numeric protocol,
      udp ports, icmp and gre with no ports, icmp-type 8, protocol 1, icmpv6
      type and code, a same-value repeat, a clean user set, junos-ping,
      junos-icmp-all) and settings-only controls; the leaf partition;
      predefined applications and sets as a population control; the strict
      text.
    - pkg/configstore: `CheckText` names the Identifier; the portless control
      passes.
    - pkg/dataplane/userspace: the sentinel plus one mirror reason per member;
      the controls' wire compared byte for byte against literals.
    - pkg/policymatch: V3, V4 and the Identifier case report `ContentRejected`;
      icmp-type 8 and the timeout control keep their verdicts.
    - The #6766 verdict cells were rewritten to refusal, and the #6766 config
      test comments synced.
  - **File(s)**: pkg/config/application_match_drops_9525_test.go,
    pkg/configstore/application_match_drops_9525_test.go,
    pkg/dataplane/userspace/application_match_drops_9525_test.go,
    pkg/policymatch/application_match_drops_9525_test.go,
    pkg/policymatch/app_inline_term_icmp_dup_6766_test.go,
    pkg/config/compiler_application_term_icmp_dup_6766_test.go
- **Timestamp**: 2026-09-10
  - **Action**: Docs: a README paragraph, a config-schema #9525 section after
    #3712, the gaps table #3261 row, and the README #6766 sentence (keep-last is
    still compiled, but the policy is refused). Full runs of pkg/config,
    pkg/configstore, pkg/dataplane/userspace and pkg/policymatch are green.
  - **File(s)**: pkg/config/README.md, docs/config-schema.md,
    docs/userspace-dataplane-gaps.md, docs/log/9525.md
- **Timestamp**: 2026-09-10
  - **Action**: Go mutation matrix. 22 cells, scored on go test -json Action==fail against each cell's named targets. The first run's control scored CONTROL-BAD (12/13 collected) and W19 VOID: its run regex 9525|6766|6524 did not match the unnumbered #6524 cell TestStrayStatementDoesNotDisarmSiblingLeaves. Re-run with a regex that names it: the control is CONTROL-OK 13/13 and W19 is KILLED by that cell. Final: 21/21 mutants KILLED, 0 SURVIVED. The mutants sever the expansion call site and every predicate arm; drop the port-bearing and ICMP-family checks (over-reach); break the match-leaf filter, the leaf partition, the mixed and nested-set walks and the set-member check; cross the #6524 boundary (refusing on UnknownDirectLeaves); and revert both strict texts. Every mutation was confirmed applied by occurrence count (W19 is an insertion that reads 1->1; its kill proves it applied).
  - **File(s)**: none (scratch matrix)
- **Timestamp**: 2026-09-10
  - **Action**: `make test` (Go + Rust) at f58ddf4ef, the pre-rebase head: 77 Go packages ok, 0 FAIL; Rust 5775 passed, 0 failed.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Landing rule applied: NON-EMPTY intersection, so rebase and
    re-run what it touches. the landing check against master 48a5177be refused with a non-empty intersection (docs/userspace-dataplane-gaps.md, pkg/config/compiler_opts.go, pkg/config/README.md: the merged #9524 plus #9592). No mutated file changed on master. Rebased, resolving one conflict on the gaps table's #3261 row (#9524's text kept, #9525's appended), then re-ran what the intersection touches: gofmt clean, go build ./... ok, go vet ./... 0 diagnostics, and full pkg/config, pkg/configstore, pkg/dataplane/userspace and pkg/policymatch green (pkg/config 9041, pkg/configstore 735, pkg/dataplane/userspace 2366, pkg/policymatch 371 tests passed; 0 FAIL). Merged with --match-head-commit. The commit was then
    amended to add these log entries only (no code change).
  - **File(s)**: docs/log/9525.md, docs/userspace-dataplane-gaps.md

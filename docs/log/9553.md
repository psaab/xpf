# #9553 — DHCPv6 relay agent (RFC 8415)

- **Timestamp**: 2026-09-16
- **Clean-start remediation**: worktree arrived at `609433efb` with one
  untracked artifact from a prior attempt —
  `pkg/config/dhcp_relay_dhcpv6_9553_test.go` (STEP-0-shaped repro using
  `bracedTree9411`). The initial RED run drove that pre-existing file, so it
  cannot serve as the clean-base STEP-0 cell. Remediation: recorded here,
  deleted the artifact WITHOUT stash (`rm`, no `git stash`), verified
  `git status --porcelain` empty at `609433efb`, then freshly authored the
  repro below and re-ran RED on the clean base.
- **Action**: completed the supported RFC 8415 DHCPv6 relay subset and
  retired the full-stanza #9411 refusal while preserving its scoped remainder
  gate.
- **File(s)**: `pkg/config/{compiler_dhcp_relay_dhcpv6_9411.go,
  compiler_dhcp_relay_dhcpv6_9553.go,compiler_services.go,compiler_prewalk.go,
  schema_routing.go,types_system.go}`, `pkg/dhcprelay/{relay.go,relay6.go}`,
  and their config/runtime tests; stale feature-gap rows and this evidence
  history were updated in the sole docs commit.
- **Provenance verification (Eng9553d, 2026-09-16)**: worktree at `609433efb`
  (`fix/9553-dhcpv6-relay`, `git diff` clean) carried 2 untracked files:
  `docs/log/9553.md` (23:38) and
  `pkg/config/dhcp_relay_dhcpv6_9553_test.go` (23:28). The prior log entry
  claims the test file is a freshly authored repro after `rm` of the original
  artifact (no stash); timestamps are consistent (test 23:28 < log 23:38).
  Verified adoptable: helpers `bracedTree9411`/`mentions9411` exist at base
  (`pkg/config/dhcp_relay_dhcpv6_9411_test.go`), and STEP-0 RED runs clean:
  `go test ./pkg/config -run TestDHCPRelayDHCPv6CompilesToV6Relay9553
  -count=1 -v` FAILs with `#9553 RED: strict CompileConfig refuses the dhcpv6
  relay stanza: forwarding-options dhcp-relay dhcpv6 ... xpf has no DHCPv6
  relay agent ... (#9411)`. ADOPTED as the STEP-0 cell (no re-author needed).
- **Grammar settled (real reference)**: Juniper docs confirm
  `[edit forwarding-options dhcp-relay dhcpv6]` with `server-group <name>
  <ipv6>`, `group <name> { active-server-group <name>; interface <name>; }`,
  global-or-group `active-server-group`, optional `relay-agent-interface-id`
  (juniper.net dhcp-relay-agent-common-configuration-applying.html,
  active-server-group-edit-forwarding-options.html). STEP-0 grammar matches.
  Minimal scope mirrors v4 compile (server-group + group); #9553 also
  implements the documented global-or-group active-server-group fallback.
- **Agent substrate**: `insomniacslk/dhcp/dhcpv6` (in go.mod) has
  `RelayMessage{ HopCount, LinkAddr, PeerAddr }`, `RelayOptions.RelayMessage()`
  / `InterfaceID()`, `NewRelayReplFromRelayForw` — no new dependency.
- **Host-inbound**: client path (ff02::1:2:547) falls to kernel via
  `should_fallback_early` (v6 multicast arm) and past nft unicast-scoped rules
  per #6460 analysis — no dataplane change. Server replies (unicast :547) ride
  conntrack established like v4 giaddr:67 replies (`ctEstablishedRelated` in
  `pkg/nftables/netlink_build.go`). `dhcpv6` system-services token already maps
  to udp 546/547 over ip6 (`pkg/config/host_inbound_tokens.go`). No 67/68
  port-specific gating found in userspace-dp local-delivery path.
- **Config retirement shape**: declaring `dhcpv6` under `dhcp-relay` in
  `schema_routing.go` auto-permits it in the #9552 gate (schema-read set);
  `dhcpRelayV4Node9411` node-selection semantics (skip dhcpv6-elided node for
  v4) stay needed permanently — only the refusal retires, plus new v6 node
  selection in `compileForwardingOptions`.
- **Provenance verification (Eng9553e, 2026-09-16)**: re-verified at `609433efb`
  (`fix/9553-dhcpv6-relay`, `git diff` clean, porcelain exactly the 2 untracked
  files). HEAD == merge-base with origin/master; the 1-behind commit is
  `1b5c976ed frr: pin narrowed per-shape reachability (part of #10129)` —
  unrelated, staying on base per assignment. Issue re-fetched via
  `gh issue view --json` (OPEN, 0 comments): xpf has no DHCPv6 relay agent,
  `pkg/dhcprelay` is v4-only, #9411 refusal must retire deliberately in the
  same change (`validateDHCPRelayDHCPv6AST` + `dhcpRelayV4Node9411`). STEP-0
  RED re-proven on the clean base (no source edits):
  `go test ./pkg/config -run TestDHCPRelayDHCPv6CompilesToV6Relay9553
  -count=1 -v` FAILs with the #9411 refusal naming `pkg/dhcprelay relays
  DHCPv4 only`. ADOPTED both untracked artifacts (no `rm`, no stash).
  Grammar settled against real Juniper docs (not model recollection):
  `forwarding-options dhcp-relay dhcpv6 { server-group <n> <ipv6>;
  active-server-group <n>; group <n> { active-server-group <n>; interface
  <if>; } }` with global-or-group active-server-group
  (juniper.net dhcp-relay-agent-common-configuration-applying.html +
  dhcpv6-relay-agent.html via web_search 2026-09-16). Dep pinned:
  `insomniacslk/dhcp v0.0.0-20251020182700-175e84fbb167` with
  `RelayMessage`/`RelayOptions`/`EncapsulateRelay`/`NewRelayReplFromRelayForw`.

## Final landing (2026-09-17)

- **Action**: completed the supported RFC 8415 DHCPv6 relay subset and retired
  the full-stanza #9411 refusal while preserving a strict remainder gate and
  tolerant no-brick warning path.
- **Implementation**:
  - `pkg/config/compiler_dhcp_relay_dhcpv6_9553.go` keeps DHCPv4 and DHCPv6
    typed state separate and normalizes braced, relay-elided, and fully packed
    parser shapes.
  - `pkg/config/compiler_dhcp_relay_dhcpv6_9411.go` now rejects only
    unsupported/incomplete remainder, including depth-3 children hidden under
    scalar interface and Interface-ID leaves; strict #9553 refusal and tolerant
    warning remain separate.
  - `pkg/config/schema_routing.go` gives DHCPv6 interface values the existing
    `ValueInterfaceName`/`ValidateInterfaceName` guard, rejecting glob-shaped
    names without weakening valid Junos interface spellings.
  - `pkg/dhcprelay/relay6.go` adds multicast client intake on `ff02::1:2`,
    configured UDP/547 upstream sockets, RFC 8415 Relay-Forw/Reply validation,
    mandatory non-empty matching outer Interface-ID, Interface-ID inheritance,
    HA master gating, per-reason `inet6` stats, and warn-once rate limiting.
    Nested downstream Relay-Forw requests and valid nested Relay-Reply responses
    are dropped by default because #9553 exposes no downstream trust knob.
    Startup/link resolution, socket-open failures, one-sided session errors, and
    live link-address/ifindex drift are handled by context-safe rebuilds with
    fresh socket pairs.
  - `pkg/dhcprelay/README.md` documents peer-address handling (including
    global/ULA peers), the nested request/reply trust boundary, default
    100-pps/two-second burst, stats counters, the scalar-only Interface-ID
    boundary (including valueless defaults and explicit group-default
    suppression of family inheritance), all ten rejected Junos modifiers, and
    the portable first-usable-global address policy.
  - The RFC 8415 Relay-Forw builder boundary is eight: hop-7 input becomes
    hop 8, while hop 8 is rejected before creating hop 9. The live client path
    rejects nested Relay-Forw requests before the builder; replies at hop 8
    are accepted while replies above it are discarded.
- **Ratchets**:
  - #8436 duplicate-block conservation: 48 checked, 14 non-conserved, 0
    silent, 94 eligible unprobed and explicitly recorded as skipped.
  - #8929 depth-2 admissions: 26 unadmitted pairs, 0 measured drops.
  - #9091 instance-name blind population: ratchet and instrument checks pass.
  - #8921 multisite registry: 562/562 recorded cells fold; 518/562 reach the
    compiled census with 0 divergence.
  - #7484 spelling coverage: 1212 enumerated, 764 compared, 448 blind;
    unreachable 137/137, advisory 27/27, and value-moves 1/1. The DHCPv6
    scalar-only tolerant safety gate reclassified the family
    `relay-agent-interface-id` row from compared to value-moves and the group
    row from advisory to unreachable; the registry and ceilings were updated
    accordingly. Slot escape sweep/table/coverage pass with 182 sites, 88
    probed, 39 ungated, and 57 hand fixtures.
- **#8939 baseline disposition**: the owned fixture was regenerated under
  `UPDATE_8939=1` after the DHCPv6 schema addition: losers 3->3, walked
  111->111, vacuous 38->38, unmeasured 84->87, and collector reach
  377->379 containers / 141->143 reached. The three loser rows and the
  historical tunnel witness note were not changed. A clean control checkout
  at `95d3a02e5c80` passed `pkg/config` with 10355 pass events and
  `pkg/configstore` with 1061 pass events (status 0), and its focused #8939
  controls emitted no failure. The final rebase base `fc80a4459bd` includes
  sibling #9909 compiler-options/tailgate, userspace, and logging changes.
  The earlier `compiler_opts.go` overlap was reconciled with both #9909 and
  #9553 gate intents retained; no other final-base changes overlap #9553
  paths. The owned fixture, tunnel witness, and unrelated baseline rows remain
  untouched.
- **RED/GREEN proof**:
  - Original clean-base STEP-0 RED at `609433efb` is preserved above:
    `TestDHCPRelayDHCPv6CompilesToV6Relay9553` failed because strict compile
    emitted the #9411 “no DHCPv6 relay agent / DHCPv4 only” refusal.
    Final config boundary cells now pass, including raw fully-elided shape,
    nested scalar-child refusal, valueless Interface-ID defaults, explicit
    group-default suppression of a family scalar, all ten documented modifier
    refusals at both family/group scopes, strict/tolerant behavior, tolerant
    no-brick preservation, and v4 non-overreach.
  - Final DHCPv6 runtime cells cover mandatory Interface-ID (matching,
    absent, and mismatched), family/group precedence including explicit
    group-default behavior, HA, multicast lifecycle, default nested drop,
    rate flood admission/drop counts, the RFC 8415 hop-8 boundary, fresh-pair
    startup/session retries, and address/ifindex rebuild.
- **Validation**:
  - Final full suites: `pkg/config` 10457 pass events, status 0;
    `pkg/configstore` 1057 pass events, status 0; and `pkg/dhcprelay` 139
    pass events, status 0. Final ratchet/boundary selection: 259 pass events,
    status 0.
  - Clean control baseline at `95d3a02e5c80`: `pkg/config` 10355 and
    `pkg/configstore` 1061 pass events, status 0; focused #8939 controls had
    no failure. The final rebase base adds no overlapping inputs.
  - The final stack contains four logical commits (grammar, runtime,
    tests/ratchets, docs), exactly one `docs/log/9553.md` commit, and no
    forbidden package paths.

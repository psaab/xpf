# 9956 — session-scope identity cohort: red-green record + review adjudication

Branch `fix/9956-session-scope`, base `a126bc209`. Three members, each with a
STEP-0 RED cell failing on base and a bounded fix. Plan reviews (2 hostile × 2
rounds) ran BEFORE implementing; both rounds' verdicts and the adjudication are
recorded here (full plan: `docs/pr/9956-session-scope/plan.md`, gitignored).

## STEP-0 RED (base `a126bc209`, all reproduced)

- F-032 `TestQuarantinedInstanceInterfacesNeverInheritDefaultDomain9956` (Go):
  quarantined `ge-0/0/1.0` reads `RoutingDomain = 0`, `RoutingInstance = ""`.
- F-051 `flowless_fragment_bytes_are_charged_to_no_session_9956` (Rust):
  2 fragments forwarded/globally counted, session carries 1 packet.
- F-052 `snat_scope_resolves_the_logical_vlan_unit_9956` (Rust): VID-50 scope
  reports unit-A ifname `"reth0.0"` instead of `"reth0.50"`.

## Plan review + adjudication (summary)

- Round 1: Alpha PLAN-NEEDS-MINOR (ship all three), Beta PLAN-NEEDS-MAJOR
  (F-032 shape). Both verified mechanisms, caller counts, citations (no drift
  at HEAD), and the RED cells.
- Q1 sentinel — Alpha RIGHT, Beta wrong. `routing_domain_wire.rs:51-68` is
  non-identity at 1 (`to_wire(1)=1`, `from_wire(1)=Present(0)`), so sentinel 1
  would alias quarantined sessions under the peer's default domain. Sentinel is
  **2** → Unrecognized → import REFUSED + `SyncedImportUnknownRoutingDomain`
  (fail-closed, `sync_session.rs:82-88`). Stated cost: quarantined sessions are
  node-local; HA failover drops them; the metric grows while quarantined
  traffic flows. Justified: misconfig traffic, alternative is a codec change.
- Q2 route move — Beta RIGHT, Alpha rebutted. Default-mode destination FIB is
  the global `inet.0` regardless of ingress RI, so quarantined traffic forwards
  today; moving connected prefixes to `"<q>.inet.0"` would strand
  same-subnet/inbound traffic while outbound-initiated flows persist via the
  session cache (asymmetric breakage). F-032 is DOMAIN-ONLY (no forEach/route
  change); connected-in-default is a follow-up. Consistency holds by binding
  neither map, as today.
- Convergent adoptions: quarantined-KEY set with `InterfaceUnitRefKeys`
  as-written+fanout expansion (bare refs covered) + survivor-wins by map
  presence; F-051 full 7-stop pipeline (batch/flush/live/snapshot/copy/status/
  reset) + Go mirror + status render + pipeline round-trip cell + tally-adjacent
  placement + frame-length byte basis; F-052 6-signature choke-point threading
  with miss-path Q6 rationale (first vlan unknowable on a miss probe, NOT
  "fragments share ingress" — ECMP can split them) + egress cite.
- Round 2: A2 DELTA-READY (PLAN-READY); B2 DELTA-NEEDS-FIX (2 plan-text items,
  no shape change — failover/metric cost, copy/reset file names). Both folded;
  no PLAN-KILL from any reviewer on any member.

## What shipped (per-member commits, subjects ≤50 + (#9956))

1. `2209ace91` RED cells (3 test files + wiring; RED-by-design, fixes follow).
2. `ae8516c33` F-032: `Config.QuarantinedRoutingInstances` (appended at both
   drop sites) + quarantined-key set + sentinel domain 2 at all 3 row builders
   + contract docs + double-claim/quarantined-bare cells + sentinel-wire
   agreement + sync-refusal cells.
3. `043a28086` F-051: `flowless_forward_packets/bytes` through the 7-stop
   pipeline + tally-adjacent `flow==None` increment + GREEN cell update +
   round-trip + reset companion cells + Go mirror + `Flowless forwards` status
   row + golden. Charge-to-session rejected (ambiguous attribution).
4. `f4b7b4d00` F-052: `ingress_vlan_id` threaded to `nat_scope_ctx_for_flow`
   (6 signatures); logical resolved inside; 3 production callers pass
   `meta.ingress_vlan_id`; untagged logical==physical (byte-identical).
5. `5a15918ec` F-051 guard fallout: layout pins 2208/2336→2224/2352
   (size still 2368) + wire fixture regen (+2 flowless fields only).
6. `0899f18e1` F-032 guard fallout: config golden regen (field null ×18).
7. `bcb74451a` F-032 wire exclusion: `json:"-"` on the quarantine record per
   the MalformedZonePairs precedent; 4406 golden returns to baseline (commit 6
   net-reverted); 8892 digest updated with a v21 STANDS entry (no version bump
   — nothing transmits the field). 9821 verified back to baseline with NO
   regen, confirming the drift was the serialized field.

## Verification (on the record)

- Named cells: 6 Rust 9956 cells 5/5 (`6 passed` × 5); Go quarantine cells +
  status golden 5/5 per package.
- Full Rust `--release`: `6073 passed; 1 failed` — the single failure is
  `additive_wire_exhaustive_literals_only_ever_decrease_7689`
  (NeighborSnapshot 29 vs ceiling 26), proven PRE-EXISTING by running it on a
  clean `a126bc209` worktree (FAILED identically; my diff contains zero
  `NeighborSnapshot` mentions and does not touch the ceiling). Excluded from
  the gate; left for its owner. (Two more first-run failures — layout pins,
  wire fixture — were F-051 fallout, fixed in-commit.)
- Full Go `pkg/config + pkg/dataplane/... count=1`: green, exit 0 (after the
  4406/8892/9821 guard dispositions above; 9821 needed no regen).
- Hygiene: `gofmt` clean on all touched Go files; `go vet` clean
  (config/userspace/format); `rustfmt --check` clean on all touched/new Rust
  files (repo has pre-existing fmt drift elsewhere, untouched); clippy: zero
  findings on new symbols — the 1 error (`umem/mmap.rs`) + warnings are
  pre-existing (error file untouched by this diff; clippy was newly installed
  for the toolchain).
- Must-stay-green guards green: SNAT contract source guard, #7160 band test,
  `routing_domain_wire_band_matches_go_7239`, #5190 copy/zero census.

## For the parent reviewer

- STOP without merging per lane protocol. PR opened from this branch; Copilot
  review requested; all review verdicts above are pre-implementation (plan)
  plus this evidence (code) — no post-implementation hostile code review ran
  in-lane.
- Follow-ups (out of scope, filed or to file): F-032 connected-in-default
  residual; Prometheus plumbing for flowless counters; the pre-existing
  `cos_literal_guard_7689` NeighborSnapshot ceiling breach on master.

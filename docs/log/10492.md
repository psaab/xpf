# #10492 — anchor the debug Rust cargo leg's test selectors

- **Timestamp**: 2026-09-22
  - **Action**: drove the issue from the reviewed plan through the exact
    target-qualified debug-leg implementation. The source census was refreshed
    after rebasing onto `origin/master` `21df4afd8`; Rust `1.98.1` and linked
    library stamp `9691eca02964fdc92f73d70a9828afdd` were used throughout.
  - **Plan rounds**: round 1 returned PLAN-NEEDS-MINOR from one reviewer and
    PLAN-NEEDS-MAJOR from the other. The later v3/v4 delta rounds folded the
    validator, family-tripwire, coordinator, ignored-state, target-normalization,
    fixture, and timing findings; the parent confirmed v4 PLAN-READY before
    implementation.
  - **File(s)**: `docs/pr/10492-cargo-filters/plan.md`

- **Timestamp**: 2026-09-22 (implementation)
  - **Action**: committed `ddc122850` (`fix: add exact debug-leg census
    (#10492)`) with the live validator, target-qualified registries, ignored
    and excluded partitions, hermetic fixtures, and parser/classifier controls.
    Committed `e2d41ee4c` (`fix: gate debug leg with exact census (#10492)`) to
    run the validator before one `--exact` debug argv while preserving the
    compile-only bench and full release legs. Committed `c8166d39b`
    (`test: harden debug-leg census fixtures (#10492)`) to make missing-path
    and same-cardinality substitution cells mutate only the live census and to
    clear shellcheck diagnostics.
  - **Fold**: implementation review moved the checksum and poll-timeout
    coordinator oracles into `debug-leg.tests`; retained the fixture-only
    `wrapping_add` case in `debug-leg.excluded` with a reason; corrected the
    validator's live-vs-registry diff orientation and partition ordering; and
    repaired the fixture mutations before delivery. The resulting registries
    contain `A=2,570` runnable entries, `X=2` ignored entries, and `E=285`
    explicit exclusions.
  - **File(s)**: `Makefile`, `scripts/debug-leg-census.py`,
    `scripts/debug-leg-census-selftest.sh`, `scripts/run-selftests.sh`,
    `userspace-dp/debug-leg.tests`, `userspace-dp/debug-leg.ignored`,
    `userspace-dp/debug-leg.excluded`

- **Validation**:
  - `CARGO_TARGET_DIR=/dev/shm/cargo-10492 TMPDIR=/dev/shm
    XPF_LINKED_LIBS_STAMP=9691eca02964fdc92f73d70a9828afdd python3
    scripts/debug-leg-census.py --manifest userspace-dp/Cargo.toml --cargo
    cargo --toolchain 1.98.1` — PASS: 14 targets, `listed=6,724`,
    `ignored=6`, `F=2,857`, `X=2`, `F∩R=2,855`, `A=2,570`, `E=285`,
    candidate argv `236,137 B`.
  - `bash scripts/debug-leg-census-selftest.sh` — PASS: 12 mutation and
    positive-control cells passed; `bash -n` and `shellcheck` also passed.
  - `make -n test-rust` — PASS: the pinned check/release legs remain present,
    the live validator is before the debug leg, and the debug command retains
    `--bins --tests --test-threads=1 --exact`. A two-path `--exact` Cargo
    reproduction passed 2 tests; Rust 1.98.1 documents multiple positional
    filters, so the 2,570-filter argv is valid.
  - Firsthand allowlist red-on-revert: deleting one `debug-leg.tests` row made
    the validator fail with the named unexpected live path; restoring the row
    made the validator pass again.
  - Firsthand #9499 red-on-revert: temporarily changing
    `parsed.seq.wrapping_add(seg_len)` to `parsed.seq + seg_len` made the exact
    overflow-oracle test fail; restoring the source made the exact test pass.
  - `make selftest` on the clean committed tree — the new shell leg and all
    shell syntax/shellcheck checks passed. The aggregate remains red on six
    unrelated baseline legs: `scripts/dist/test_dist_resign_9920.py`,
    `test/incus/harness-census-selftest.sh`,
    `test/incus/harness-ledger-mutation-selftest.sh`,
    `scripts/harness-census.sh`, `scripts/go-skip-census.sh`, and
    `ledger-compare-all`; summary was 220 passed, 6 failed.
  - `CARGO_TARGET_DIR=/dev/shm/cargo-10492 TMPDIR=/dev/shm make test-rust` —
    the DT-needed prerequisite and compile-only bench check passed. The
    pre-existing release suite stopped Leg 1 at 6,392 passed, 183 failed, and
    6 ignored, so Make did not continue to Leg 2. The exact validator plus
    debug command was then run directly: the `xpf-userspace-dp` target selected
    2,565 entries and reported 2,471 passed and 94 existing dataplane failures
    (0 ignored, 4,017 filtered) before Cargo stopped on that binary; the five
    integration-target entries were therefore not reached in that failed run.
  - `git diff --check` — PASS; the worktree was clean after every temporary
    mutation was restored.

- **Outcome**: implementation branch `fix/10492-cargo-filters`; parent review
  and PR publication remain the next gate. No cluster/incus smoke commands were
  run in this Wave 1 lane.

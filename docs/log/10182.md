# #10182 — restore source-bound collector dial fidelity

- **Timestamp**: 2026-09-17
- **Action**: Reproduced both post-#9913 divergences on current master
  (`900771b13`) before editing. The destination-error cell returned
  `dial collector 192.0.2.99:2055: ...` instead of the prior
  `resolve collector 192.0.2.99:2055: ...`; the source-bound dial cell
  observed the original hostname instead of the resolved IP literal. Both
  cells were RED.
- **File(s)**: `pkg/flowexport/collector_dial_fidelity_10182_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Audited all package callers for matching on either error prefix.
  No caller or test branches on `resolve collector` / `dial collector`; the
  taxonomy is nevertheless restored because it is observable operator/error
  context and the source-bound behavior is required to remain unchanged.
- **File(s)**: `pkg/daemon/`, `pkg/flowexport/`

- **Timestamp**: 2026-09-17
- **Action**: Restored the source-bound split: resolve source and destination
  with the bounded context, preserve the `resolve collector ...` error path,
  and dial the resolved destination. The no-source branch continues to pass
  the original destination to `DialContext`, retaining native DNS candidate
  fallback.
- **File(s)**: `pkg/flowexport/transport.go`,
  `pkg/flowexport/transport_test.go`

- **Timestamp**: 2026-09-17
- **Action**: After the fix, the fidelity cells and existing dial cleanup
  cells were GREEN: **10 passed, 0 failed**. The race run was also **10
  passed, 0 failed**. The two original RED cells now assert taxonomy and
  resolved-address delivery directly.
- **File(s)**: `pkg/flowexport/collector_dial_fidelity_10182_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Full flowexport suite is green: **172 tests passed, 0 failed**
  (`go test ./pkg/flowexport -count=1`).
- **File(s)**: `pkg/flowexport/*`

- **Timestamp**: 2026-09-17
- **Action**: Fail-on-revert proof: replacing the source-bound resolve-then-
  dial block with the former direct `DialContext` call made both fidelity
  tests fail (taxonomy changed to `dial collector`; resolved-address cell
  reached real DNS). Restoring the block returned both tests to GREEN.
- **File(s)**: `pkg/flowexport/transport.go`,
  `pkg/flowexport/collector_dial_fidelity_10182_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Follow-up review identified a dual-stack compatibility gap:
  `LookupIPAddr` may return AAAA before A, while `net.ResolveUDPAddr`'s
  `forResolve` selection prefers the first IPv4 result. Added a deterministic
  hostname-selection cell; the IPv4-preferred subcase was RED before the
  resolver change and the IPv6-only fallback subcase remained GREEN.
- **File(s)**: `pkg/flowexport/collector_dial_fidelity_10182_test.go`

- **Timestamp**: 2026-09-17
- **Action**: The bounded hostname resolver now selects the first IPv4 result
  for plain UDP/UDP4 and the first IPv6 result for UDP6, falling back to the
  first lookup result when the requested family is absent. Literal and empty
  host paths remain unchanged.
- **File(s)**: `pkg/flowexport/transport.go`

- **Timestamp**: 2026-09-17
- **Action**: Hardened the source-bound oracle to assert the exact resolved
  `*net.UDPAddr`, fail fast if the no-source string-dial seam is reached, and
  verify the resolved seam receives a live bounded deadline context. The
  taxonomy oracle now requires an exact `resolve collector ...:` prefix.
- **File(s)**: `pkg/flowexport/collector_dial_fidelity_10182_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Fold verification: final-head flowexport suite **175 passed, 0
  failed**; the race selector covering #9913/#10182 plus cleanup cells
  **12 passed, 0 failed**. The daemon package was not cleanly runnable in
  this environment: the preserved run recorded **2933 passed, 10 failed**.
  All nine event-stream failures reported Unix-socket bind errors under the
  long isolated TMPDIR path; one representative rerun with a short test
  TMPDIR passed. The MTU digest parse-EOF failure reproduced independently.
- **File(s)**: `pkg/flowexport/*`, `pkg/daemon/*`

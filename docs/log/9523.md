# #9523 — an address-book object named `any` hijacks the match-all keyword

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified at origin/master `806a89b5f` with throwaway probes
    (removed). `config.SchemaValidate`, `config.CompileConfig`,
    `config.CompileConfigLenient` and `configstore.CheckText` all accepted an
    address or address-set named `any` with zero warnings. Snapshot:
    `SrcBookIDs=[id] SrcLiterals=[]` versus `SrcLiterals=[any]` for the control.
    Simulator: a telnet probe outside the object's prefix no longer hit the
    `deny any` rule. A zone-local `any` did not capture (the rewrite skips it).
  - **File(s)**: none (probes deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Added `config.IsPolicyAddressWildcardKeyword` and
    `validateReservedAddressNamesStrict` (strict reject, tolerant warn via
    `lenientReservedAddressNames`), wired before the zone-local fold. The
    keyword check now runs first in `classifyPolicyAddresses`, policymatch
    `resolveToken`, and the CLI/gRPC detail resolvers.
  - **File(s)**: pkg/config/policy_address_keywords_9523.go,
    pkg/config/compiler_opts.go, pkg/config/compiler_earlystrict.go,
    pkg/dataplane/userspace/policies_addrbook.go, pkg/policymatch/policymatch.go,
    pkg/cli/app_resolve.go, pkg/cli/cli_show_security_dispatch.go,
    pkg/grpcapi/server_cluster.go
- **Timestamp**: 2026-09-10
  - **Action**: Tests on config, configstore (CheckText), the userspace
    classifier and wire, the simulator (including the name-before-literal
    control), and a Rust cell for the wire the builder now emits. Docs.
  - **File(s)**: pkg/config/policy_address_keywords_9523_test.go,
    pkg/configstore/reserved_address_name_9523_test.go,
    pkg/dataplane/userspace/address_keyword_before_name_9523_test.go,
    pkg/policymatch/address_keyword_before_name_9523_test.go,
    userspace-dp/src/policy_tests.rs, pkg/config/README.md,
    pkg/policymatch/README.md

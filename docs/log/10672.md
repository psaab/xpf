# #10672 — Route ICMP errors quoting replies toward the quoted sender

- **Timestamp**: 2026-09-24
- **Action**: When an embedded ICMP error matches the reverse half as-is, resolve its return path toward the quoted packet's sender using the forward companion's cached resolution, falling back to a fresh route and dynamic-neighbor lookup. The v4/v6 flowless poll cells pin that a server-side Frag-Needed/PTB reaches the server leg rather than returning toward the client.
- **Validation**: Both v4/v6 fail-on-revert cells failed under the old cached-reverse behavior (`target_ifindex` 12 instead of expected 24). The poll-filter module passed 68 tests after implementation; the latest `cargo test --bins` run also executed both cells successfully. The privileged XDP/shim gate passed: `sudo -n env GOCACHE=/tmp/gocache-fix-10672-icmpmisroute make test-shim-run`.
  `cargo test --bins` on base `3a1a2edcd` reported 6,953 passed, 2 failed, and 6 ignored in `xpf-userspace-dp`. Both failures reproduced on a clean `origin/master` snapshot at the same base: `degraded_path_reason_names_agree_with_the_shim_9888` (`stag_drop` missing from the Rust table) and #9894 `flowless_slack_tcp_not_steered_by_port_except_pbr_9894` (`forward` 0, expected 1). No tests or fixtures for either failure were changed.
- **File(s)**: `userspace-dp/src/afxdp/icmp_embed/return_resolution.rs`, `userspace-dp/src/afxdp/icmp_embed/nat_match_v4.rs`, `userspace-dp/src/afxdp/icmp_embed/nat_match_v6.rs`, `userspace-dp/src/afxdp/icmp_embed/mod.rs`, `userspace-dp/src/afxdp/tests_embedded_poll_filter.rs`, `docs/log/10672.md`

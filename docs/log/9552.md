# #9552 — config: forwarding-options dhcp-relay is open-world

- **Timestamp**: 2026-09-10
  - **Action**: Census first, as the issue asks. Every tracked config-like file
    authors only `group` and `server-group` under `forwarding-options
    dhcp-relay`. The Go test literals add only #9411's `dhcpv6` fixtures, which
    #9411 already refuses, and `inactive: dhcpv6`, which is pruned before the
    pre-walk gates run. So a child-keyword gate refuses nothing the tree
    authors.
  - **Action**: New validateDHCPRelayChildTokensAST, following #9323:
    - a pre-walk AST gate scoped to the dhcp-relay level (not closedWorld,
      which would inherit into `group` and `overrides`);
    - the permitted set is read from the schema;
    - statement tokens are read by position, since a packed Keys tail and
      Children are exclusive sources;
    - the fully elided `forwarding-options dhcp-relay <tok>` spelling is
      covered;
    - `dhcpv6` is left to #9411's more specific message, and the meta
      statements are skipped;
    - strict on commit / commit-check, and warned on load / peer-sync via
      lenientDHCPRelayChildTokens (#1960), with one warning per distinct token.
  - **Action**: Cells:
    - refused in braced, brace-elided, flat-set, two-line flat-set (exactly one
      warning) and beside-a-working-relay spellings, strict and lenient;
    - load-bearing: the braced, inline server-group, overrides, apply-macro and
      flat-set DHCPv4 relays still compile to their server-group and group,
      with no #9552 warning;
    - `dhcpv6` keeps #9411's message alone;
    - the CheckText operator-channel cell has a same-run DHCPv4 control;
    - pkg/config and pkg/configstore are green.
  - **File(s)**: pkg/config/compiler_dhcp_relay_children_9552.go,
    pkg/config/compiler_prewalk.go, pkg/config/compiler_opts.go,
    pkg/config/dhcp_relay_children_9552_test.go,
    pkg/configstore/dhcp_relay_children_checktext_9552_test.go, docs/log/9552.md
- **Timestamp**: 2026-09-10
  - **Action**: Ran the 10-mutant matrix at 942fd76ae. Each mutant was scored
    by the `go test -json` failing test NAME against its target. The no-op run
    is CLEAN (3 tests ran, no failures). 10 of 10 mutants were KILLED:
    - D1 accepts everything, D2 always declines, D4 ignores the packed Keys
      tail, D5 does not read Children, D6 ignores lenient, D7 does not dedup,
      D8 leaves the lenient flag unset, D9 does not register the gate. All
      killed by TestDHCPRelayUndeclaredChildIsRefused9552.
    - D3, dhcpv6 not skipped: killed by
      TestDHCPRelayDHCPv6KeepsItsOwnMessage9552.
    - D10, meta keyword not skipped: killed by
      TestDHCPv4RelaySpellingsStillCompile9552.
  - **Action**: Before the commit, the full pkg/config suite (199.8 s) and the
    pkg/configstore suite (37.8 s) were green.
  - **File(s)**: docs/log/9552.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9552.md

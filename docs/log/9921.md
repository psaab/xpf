# #9921 — dist/image env validation + check-then-use cohort

Cohort: F-065 (install.sh URL→deb822 injection), F-066 (build-apt-repo
COMPONENT/ARCH/ORIGIN), F-068 (validate.py/bake.py verify-then-freeze),
F-147 (xpf-day0-config O_NOFOLLOW read). Worktree `fix/9921-dist-env`,
base `origin/master 9b303307a` for STEP-0/implementation, rebased onto
`5e2252918` (#9920 merge) before PR — see rebase entry below.

- **Timestamp**: 2026-09-15
- **Action**: STEP-0 RED on base, one assert-secure cell per member (all exit
  1; logs `/tmp/9921-f0{65,66,68,147}-base.log`, cells `/tmp/9921-repro/`).
  - F-065: dry-run with a newline `XPF_APT_BASE_URL` exits 0 and renders a
    rogue `Signed-By: /tmp/evil9921.gpg` deb822 field ahead of the genuine
    one; shell-metachar URL also exits 0; legit URL accepted (control).
  - F-066: `COMPONENT='../../../../escape…'` writes `binary-amd64/Packages*`
    outside `--out` (exit 0); ARCH needs 6×`..` to leave `--out` (5× escapes
    the dists tree but lands inside `--out`); newline ORIGIN exits 0 and the
    Release carries THREE `Valid-Until:` lines (Origin+Label interpolate, so
    the injection lands twice) ahead of the genuine one; genuine suffix is
    `+0000` while `Date:` echoes `UTC` (apt normalizes).
  - F-068: `Harness` has no `freeze_artifacts`; mocked `incus image import`
    re-opens the LIVE paths; bake has no staging/re-assert helpers.
  - F-147: no `safe_read_medium`; `head -c` follows a symlink to host-side
    bytes and stalls the full timeout on a fifo with no writer.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-15
- **Action**: Two hostile plan reviews BEFORE implementing (both
  APPROVE-WITH-CHANGES; no member misread). Adjudication (A = shell/F-065/
  F-066 focus, B = freeze/F-147 focus; full record in PR body):
  - ADOPTED: exact `*[![:print:]]*` spelling (both P1); single-char denylist
    arms instead of the mega-class (resolves A-P1 heredoc + B-P2 bracket
    hygiene more robustly than suggested); LC_ALL=C save/restore for
    locale-sensitive matches; uppercase-scheme publish parity Q1 (urlsplit
    lowercases — verified); URL/channel hoist above preflight Q2 with a
    failing-preflight proof test; tail hook WITHOUT `exit 0` (A-P1);
    dual-drive parity battery over 24 derived publish edges; COMPONENT/ARCH
    allowlist + ORIGIN ASCII-printable pre-write; Release count==1 AND exact
    epoch equality with `grep -c || true` + explicit GNU-date die; die-message
    asserts on every negative cell; freeze-first-statement in seal+verify
    (before the verify_sig early return) + main + flag-guard idempotence;
    in-memory manifest snapshot + pre-sign assert (live-parse==snapshot AND
    live-rehash==snapshot) inside sign_step — closes the joint live-sums +
    live-artifact tamper both P1s proved; `use Fcntl` (A-P3 wins: Fcntl.pm/.so
    verified in perl-base via dpkg) + strict; perl-absent skip-device Q7 with
    RUNTIME_PACKAGES + debian/control sync pin (file's own sync rule);
    mktemp-after-mount + cleanup audit; node-id two-step rc, cap 16,
    124→warn; helper battery + perl-stub + function-override wiring tests
    (all diverge on base); own-`sh -c` per sourced test; stamp_installer temp
    copies for precedence; main-freeze ordering test via Harness-factory mock
    + copy-spy idempotence; TMPDIR footprint note; deviation docs
    (assert-before-SIGN beats the literal assert-before-write_manifest;
    Valid-Until assert is flat-path-only).
  - MODIFIED: frozen digest map dropped except an info log line (both: no
    consumer); ORIGIN literal-value cell for `'`, `;`, `:` as data (B-P2);
    bake membership pin kept minimal-text (wiring untestable w/o a full
    bake; staging semantics pinned behaviorally).
  - REJECTED with evidence: BSD date parse-fallback (untestable dead code —
    apt-ftparchive implies Debian/GNU date; GNU-parse-or-explicit-die
    instead); `use Errno` (`$!{ELOOP}` probes pass with no imports);
    stage-all-four-sidecars (insufficient alone — the sums file itself needs
    the snapshot; rehash-vs-snapshot covers sidecars); reprepro-path Release
    assert (untestable without a reprepro+GPG harness — documented instead).
  - Reviewer errors corrected: none material; both agreed Q3 method-placement
    (A confirmed the 7347 Recorder calls scenario_qemu without freeze, so
    `__init__` freeze would break it), Q4 work/ staging, Q5 exact epoch, Q8
    single-line sufficiency. Own probes added: perl `$fh` must be declared
    outside the `if` (lexical scope closes the handle — silent 12s), sysread
    length must clamp to `max+1-n` (cap honored exactly), `local x=$(…)` masks
    rc (split form for node-id), `date -d` parses both `+0000`/`UTC` suffixes.
- **File(s)**: `/tmp/plan-9921.md` (review input; not committed)

- **Timestamp**: 2026-09-15
- **Action**: Implemented the adjudicated design.
  - F-065 (`scripts/dist/install.sh`): `validate_apt_url()` shell mirror of
    `publish.validate_apt_url` — (1) `*[![:print:]]*` control/whitespace/DEL
    reject, (2) single-char denylist arms (no bracket quoting subtleties),
    (3) structural https/host[:port]/path allowlist with case-insensitive
    scheme (publish parity); LC_ALL=C save/restore around locale-sensitive
    matches; diagnostics echo a `tr -d '[:cntrl:]'` copy (publish `!r`
    discipline). URL+channel hoisted above `preflight()` (identical refusal
    on every host); `XPF_INSTALL_SOURCE_ONLY` tail hook WITHOUT `exit 0`
    (status propagates). Tests: `test_install_url_validate_9921.py` (9
    cells: good/bad batteries × C/C.UTF-8, dual-drive parity over ~90
    cases, failing-preflight order proof, host-independent e2e refusal,
    stamp_installer precedence, uppercase parity).
  - F-066 (`scripts/dist/build-apt-repo.sh`): COMPONENT/ARCH
    `""|*/*|charset|first-char` allowlist + ORIGIN non-empty single-line
    printable (LC_ALL=C), all next to the SUITE check before any write;
    Release assert is count==1 AND exact epoch equality
    (`grep -c || true`, GNU `date -d` parse or explicit die); reprepro
    asymmetry documented (shared gate covers its distributions file; the
    count assert is flat-path-only). Tests:
    `test_build_apt_repo_inputs_9921.py` (6 cells: traversal/newline
    refusals with message + nothing-written asserts, literal-value
    passthrough, exact Date+VALID_DAYS derivation, default build).
  - F-068 (`scripts/image/validate.py`, `bake.py`): `Harness.freeze_` +
    `artifacts()` (private 0700 staging in `self.work`, basenames preserved,
    live `*_src` aliases for manifest lookup, flag idempotence, digest audit
    log line) called first by `main()` and defensively by
    `assert_image_sealed()`/`verify_signatures()` (before the no-verify
    early return); bake `stage_artifacts_for_gate()` (under `work/`) +
    in-memory `snapshot_manifest_inputs()` + `assert_live_matches_manifest()`
    (live-parse==snapshot AND live-rehash==snapshot) inside `sign_step`;
    manifest hashed over staged bytes via a live→staged mapping (mapping-
    over-list: the #10119 rebase swaps the input list only); gate runs on
    staged paths. `test_bake_base_pin` membership pin updated to the input
    list. Tests: `test_validate_freeze_9921.py` (10) +
    `test_bake_freeze_9921.py` (8, incl. the joint sums+artifact tamper
    cell). Deviation documented: assert-before-SIGN (covers the gate
    window) beats the literal assert-before-write_manifest.
  - F-147 (`scripts/image/xpf-day0-config`): `safe_read_medium()` (perl
    strict/warnings/Fcntl, O_NOFOLLOW|O_NONBLOCK sysopen, fstat `-f` on the
    fd, capped sysread, `timeout 10` backstop; rc 0/10/11/12/13/14/124;
    perl-absent fails closed with no `head` fallback); `try_device`
    restructured (mktemp-after-mount, per-candidate safe reads with
    ENOENT→alias, node-id two-step rc with cap 16, every post-mktemp return
    removes the tmp); header posture comment; `perl-base` pinned in
    RUNTIME_PACKAGES + xpf-appliance Depends (file's sync rule) with sync
    cells in `test_bake_sign_ordering.py`. Tests:
    `test_day0_safe_read_9921.py` (18: helper battery incl. NUL/binary
    exactness, cap truncation, argc/`set -u` safety, perl-absent; try_device
    e2e incl. perl-stub + function-override wiring proofs that diverge on
    base).
- **File(s)**: scripts/dist/install.sh, scripts/dist/build-apt-repo.sh,
  scripts/image/validate.py, scripts/image/bake.py,
  scripts/image/xpf-day0-config, debian/control,
  scripts/dist/test_install_url_validate_9921.py,
  scripts/dist/test_build_apt_repo_inputs_9921.py,
  scripts/image/test_validate_freeze_9921.py,
  scripts/image/test_bake_freeze_9921.py,
  scripts/image/test_day0_safe_read_9921.py,
  scripts/image/test_bake_base_pin.py,
  scripts/image/test_bake_sign_ordering.py

- **Timestamp**: 2026-09-15
- **Action**: Red-green + verification.
  - Repro cells: all 4 RED on base (exit 1, `/tmp/9921-f0{65,66,68,147}-
    base.log`) and GREEN post-fix (exit 0); re-verified RED on a
    materialized base tree (`git archive HEAD`, `REPRO_ROOT=/tmp/9921-
    base`) with the final cell versions.
  - New tests on that base tree: install 9 tests / 47 subfailures,
    repo-inputs 6 / 15 failures, validate-freeze 10 (2F+8E), bake-freeze
    8 (8E), day0 18 / 13 failures — the 5 base-passing day0 cells are the
    documented 3 positive controls + 2 outcome guards (fifo-skip,
    bad-node-id); every security cell diverges.
  - Fixed tree: all 27 scripts/dist + scripts/image suites green in one
    pass (incl. pre-existing 6502/6547/ownership/ledger suites — no
    regressions); the 5 new files (51 cells) green 5×; `test_bake_sign_`
    `ordering` 11 OK incl. the 2 new perl-sync cells;
    `scripts/dist/selftest.sh` 49 passed / 0 failed (incl. §9 H-16
    validate-before-mutate + install dry-run legs).
  - Lint: `shellcheck -S warning` clean on install.sh,
    build-apt-repo.sh, xpf-day0-config; `sh -n`/`bash -n`;
    `py_compile` on all touched/new python files.
  - Footprint note: freeze (validate) + staging (bake) hold ~2× the
    qcow2+metadata in tmp during the gate; `--keep`/`--keep-work` retain
    them. Size TMPDIR for ~3× the artifact set on bake hosts.
  - Merge note: the predicted overlap with sibling PR #10119 (9920 — bake.py
    write_manifest hunk + `test_bake_base_pin` pin) materialized when #9920
    merged as `5e2252918` mid-lane. Rebasing onto it, both conflicts resolved
    by combining intents (SSOT input list + staged mapping; SSOT + staging
    pins) per the pre-agreed mapping-over-list shape — no parent action
    needed. Post-rebase: all 28 dist+image suites green (incl. 9920's 32
    resign cells), selftest 56/0, all 4 repro cells green.
- **File(s)**: none (measurement)

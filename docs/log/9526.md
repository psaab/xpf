# #9526 — the first policy's sessions are never cleared when it is deleted

- **Timestamp**: 2026-09-10
  - **Action**: Re-measured the premise at c71f9d556 / 48a5177be, as the
    coordinator asked, because it cited a zone-0 exit.
    - Go plane, unchanged: both sweep builders still skip `policy_id == 0`
      (`deletedPolicyRuntimeIDs`, `changedPolicyRuntimeIDs`), and the pinning
      cells `TestClearSessionsForDeletedPolicies_FirstPolicyIdZeroNotSwept` and
      its rename twin still pass.
    - Rust plane:
      - The zone-0 decline the issue cites is GONE (#9513, 36bcca321: an
        unzoned interface is now re-judged).
      - GATE 1 (`is_reverse`) and GATE 1b (a type-constrained ICMP permit) still
        decline.
      - `companion_keeps_alive` still re-stamps the forward half from an active
        reverse half.
      - Nothing else clears the session. With directory pathspecs and positive
        controls, `DEFAULT_POLICY_SENTINEL_ID` is used only for attribution; the
        #3395 refresh re-stamps a deleted rule's rows to it but deletes nothing.
    - So the claim still stands for a one-way reverse-sustained flow, on two of
      the issue's three exits. No runtime reproduction was run; the new Rust
      cell is the executable form.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Contract and prior decisions:
    - The id-0 exclusion is a documented, pinned decision, and it is correct:
      host-local, neighbor-seed, fabric, tunnel and older-peer sessions carry 0.
      #3056 closed 2026-06-26, so a pre-#3056 all-zero peer is plausibly still in
      a rolling-upgrade window. Its "idle out" conclusion is what was false.
    - A Go-side discriminator does not exist. `PolicyCounterIdx` is documented as
      MUST-NOT-be-on-the-BPF-map; the capture iterates that map; and the
      positional counter index goes stale after a reorder.
    - The helper's bound rule handle (`SessionMetadata.policy_counter`, with a
      stable `rule_id`) does discriminate: the overloaded-zero installs bind
      `None`.
    - The default-policy handle has the non-empty id `default-policy`, so a naive
      "rule vanished" predicate would sweep every default-permit session. The
      purge keys on the one first-policy rule id instead.
    - No open PR touches session_glue, poll_descriptor, policy.rs, session/ or
      the daemon sweep file. wt-9582 edits session_glue/tests.rs, so the new
      cells are in their own file.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Implemented a helper-side purge scoped to exactly the id the
    daemon must skip:
    - `PolicyState::first_policy_rule_id` (`None` when several rules carry 0)
      and `has_rule_id`.
    - `deleted_first_policy_rule_id(old, new)` and
      `purge_sessions_bound_to_deleted_first_policy`, which tears down through
      `delete_terminal_filtered_session`, the DSCP purge's full-pair path.
    - Worker-loop wiring: compare before `forwarding = new_forwarding;`, purge
      after it.
    - The Go rationale is corrected in both builders; the Go behaviour is
      unchanged.
    - Filed #9596 for the MODIFIED half, which keeps its stable id so the purge
      cannot see it.
  - **File(s)**: userspace-dp/src/policy.rs,
    userspace-dp/src/afxdp/session_glue/mod.rs,
    userspace-dp/src/afxdp/worker/loop_body/mod.rs,
    pkg/daemon/daemon_policy_invalidate.go,
    userspace-dp/src/afxdp/session_glue/README.md
- **Timestamp**: 2026-09-10
  - **Action**: Cells in `deleted_first_policy_purge_9526_tests.rs`:
    - the accessors, including all-zero and empty snapshots;
    - the rule choice: delete and rename fire, while unchanged, a later deletion,
      a reorder and an all-zero old snapshot do not;
    - the purge: both halves of the bound first-policy pair are removed with one
      close delta, while the second policy, unbound id-0 and default-policy pairs
      are kept;
    - the worker-loop wiring, bound on the source.
  - **File(s)**: userspace-dp/src/afxdp/session_glue/deleted_first_policy_purge_9526_tests.rs
- **Timestamp**: 2026-09-10
  - **Action**: Rust mutation matrix. 13 cells, scored on each named target's `test ... FAILED` line (a build failure, a timeout or nothing collected scores VOID). The unmutated control collected 4/4 and passed. 12/12 mutants KILLED, 0 SURVIVED, 0 VOID, each confirmed applied by occurrence count, and the tree was clean after the run. The mutants sever or skew the loop's compare (severed, reading the new state twice) and its purge (guard severed, keyed on an empty id); drop the duplicate-zero guard; key the first rule on id 1; make has_rule_id always false; make the choice ignore the new snapshot; and break the purge filter (reverse entries included, the rule id ignored, unbound sessions matched, no deletion).
  - **File(s)**: none (scratch matrix)
- **Timestamp**: 2026-09-10
  - **Action**: `make test` (Go + Rust) at bde5fe562: 77 Go packages ok, 0 FAIL; Rust 5785 passed, 0 failed.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Landing rule applied: master MOVED since the tested base (48a5177be -> d1266ee79) and master_delta ∩ mine is EMPTY, with no mutated file changed on master. The merged tree was compiled in a throwaway worktree first: go build ./... ok; the go vet ./... differential shows 0 diagnostics on master alone, 0 on the merged tree, 0 new; cargo check --tests --benches ok. Merged with --match-head-commit. The commit was then amended to add these log entries only (no code change).
  - **File(s)**: docs/log/9526.md
- **Timestamp**: 2026-09-10
  - **Action**: Follow-up (coordinator). The worker-loop wiring had been bound
    with a source-text check on the claim "that loop has no unit harness". A
    source-text check proves presence, not reachability, so the claim was
    measured at 3ae82aa49:
    - `worker_loop` has one production caller (coordinator bringup) and no
      test caller, but every argument is constructible in-crate: the
      `WorkerLaunchPlan`, `WorkerControlChannels` and
      `WorkerPublishedTelemetry` constructors, plus
      `WorkerSharedDataplane::from_coord` / `WorkerCoSState::from_coord` from
      `Coordinator::new()`, which the #6241 launch.rs tests already build.
    - With an empty binding plan, setup opens no XSK and every map fd is -1;
      `PollMode::BusyPoll` never blocks; the loop exits on `stop`.
    - `refresh_runtime_view` and the rotation block run at the top level of
      every iteration, not under a bindings-gated branch.
    - `WorkerCommand::UpsertLocal` installs `SyncedSessionEntry::metadata`
      unchanged (`into_session_install`), bound `policy_counter` included.
    - The purge's `delete_terminal_half` calls `remove_shared_session` on the
      coordinator's shared HA maps, which a test can hold.

    The claim was false. Replaced the source-text cell with behavioural cells
    that run the real worker and publish real rotations: delete and rename purge
    both halves and keep the unbound id-0 session; a control rotation that keeps
    the first policy purges nothing, which rules out a missing install and a
    rotation-wide drop as explanations.
  - **File(s)**: userspace-dp/src/afxdp/worker/loop_body/first_policy_purge_rotation_9526_tests.rs,
    userspace-dp/src/afxdp/worker/loop_body/mod.rs,
    userspace-dp/src/afxdp/session_glue/deleted_first_policy_purge_9526_tests.rs,
    userspace-dp/src/afxdp/session_glue/README.md
- **Timestamp**: 2026-09-10
  - **Action**: Follow-up Rust mutation matrix, 8 cells, each scored on its
    named target's `test ... FAILED` line (a build failure, a timeout or
    nothing collected scores VOID). The unmutated control collected 2/2 and
    passed. 7/7 mutants KILLED, 0 SURVIVED, 0 VOID, each confirmed applied by
    occurrence count, and the tree was clean after the run. The mutants, all
    killed by the behavioural cells:
    - in loop_body/mod.rs: the compare severed, the compare reading the new
      state twice, the purge guard severed, the purge keyed on an empty id;
    - in session_glue/mod.rs: the purge deleting nothing, the predicate
      matching unbound sessions, the choice ignoring the new snapshot (killed
      by the control cell).
  - **File(s)**: none (scratch matrix)
- **Timestamp**: 2026-09-10
  - **Action**: `make test` at 076dc7d2e was RED on one Rust cell: the #6592
    runtime-view canary, rule 2. The new test file builds a RuntimeView for
    its own test-local channel. The canary reads each file standalone and
    honours the `test-local` marker only in a file's test half, which it
    derives from top-level `#[cfg(test)]` items; the `#[cfg(test)]` on this
    module's `mod` line lives in loop_body/mod.rs, where the canary cannot see
    it. Fixed as the canary's message prescribes: the `view` fixture carries
    a `#[cfg(test)]` attribute and the construction line carries the marker.
    The canary is unchanged; it over-reports here, and it does not fail open.
    The rotation cells still pass (2/2) and the canary is green (31 passed).
    The matrix result above stands, because the change is an attribute and a
    comment in the test file, which no mutant touched.
  - **File(s)**: userspace-dp/src/afxdp/worker/loop_body/first_policy_purge_rotation_9526_tests.rs
- **Timestamp**: 2026-09-10
  - **Action**: Re-pointed the purge rationale from #9596 to #9604. #9596
    measured that the #8356 re-derivation covers a modified first policy for
    any flow that sends a forward packet; the reverse-only residual is #9604.
  - **File(s)**: pkg/daemon/daemon_policy_invalidate.go,
    userspace-dp/src/afxdp/session_glue/README.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` (Go + Rust) at c9865390e: 77 Go packages ok, 0
    FAIL; Rust 5792 passed, 0 failed.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Landing rule for the follow-up: master MOVED since the tested
    base 3ae82aa49, and kept moving during the landing checks (4363e0168, then
    91277c6aa, then e2cf2f69e). Each time, the guard refused to merge onto a
    master it had not compiled. Every check found master_delta ∩ mine EMPTY
    and no mutated file changed on master, and compiled the merged tree in a
    throwaway worktree: go build ./... ok; go vet ./... differential 0 new
    diagnostics (0 on master, 0 merged); cargo check --tests --benches ok.
    The final check runs immediately before `gh pr merge --merge
    --match-head-commit`, gated on origin/master still being the master it
    compiled, and the merge commit's first parent is verified against that
    master afterwards. This entry was written before the final check, so the
    commit is not amended after it. `make test-failover` was not run for the
    follow-up: it changes no production code (tests, one Go comment and a
    README). The purge itself landed in #9598.
  - **File(s)**: docs/log/9526.md

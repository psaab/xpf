# #9882 — firewall: unknown policer `then` token dropped on every path; lone `forwarding-class` commits as `discard`

- **Timestamp**: 2026-09-15
- **STEP-0 (defect PRESENT on base `2456a1fcb`)**, via real strict `CheckText` + `CompileConfig`/`CompileConfigLenient`:
  - `then { discrad; }` / `then { foo; }` commit CLEAN, `ThenAction="discard"`, `ThenActions=[]`.
  - Lone `then { forwarding-class af11; }` commits CLEAN with `ThenAction="discard"` (`ThenActions=[forwarding-class]`), while the #8445 rejection for `discard+FC` promises lone FC "METERS ONLY" (message verified verbatim).
  - Flat-set `set firewall policer p1 then foo` commits clean strict; lenient boots the typo with zero warnings.
  - Mechanism: `compiler_firewall.go:22-25` (default), `:57-68` (no FC arm, no default arm), `:88-89,164-175` (identical three-color mirror); `schema_cos.go:480-484` (then advertises discard+loss-priority only, open-world); `compiler_validate_strict_filter.go:1960-1973` (#8445 rejects combined-only); `filters.go:804-806` (`ThenAction=="discard"` → `DiscardExcess`).
- **Action**: `UnknownActions []string` on `PolicerConfig` + `ThreeColorPolicerConfig` (mirrors `FirewallFilterTerm.UnknownActions`, #2399); `default:` arm in both `then` loops records `child.Name()`; new `validateFirewallPolicerUnknownActionsStrict` (policers then three-color-policers, sorted; #2399-shaped message) wired into the uniform gates BEFORE the #8445 block (filter precedent: #2399 before #4375); `lenientPolicerUnknownActions` downgrades to warn + keeps `"discard"` (#1960 no-brick).
  - `forwarding-class` arm + schema: new arm writes `ThenAction = "forwarding-class <class>"` (loss-priority arm verbatim) → meter-only; declared under both `then` maps (loss-priority declaration verbatim); no `closedWorld` (compiler-side gate per #2399/#9017).
- **Action**: three-color coordination, the #9503 pattern verbatim: `"forwarding-class "` prefix admitted in `threeColorThenActionSupported` (Go) and `snapshot_three_color_shape_supported` (Rust); `treatments_from_then_action`'s default arm already yields no-drop; the #9503 advisory already fires for any non-discard marking. Single-rate needs no helper change (`build_single_rate_policer_state` reads only `discard_excess`, and its doc comment already names `then forwarding-class` as meter-only).
- **Action**: leaf-form reader in both loops (mirrors `compileFilterThen`'s #2399 leaf path with arg consumption). Found by measurement: the compact fold only fires for declared heads, so packed `then foo;` stayed a leaf and bypassed a children-only gate — verified #2399's filter gate catches its packed twin via the leaf path, so the policer gate must too. Side effect: packed `then discard;` now records `ThenActions=[discard]` (was `[]`), and packed `then discard <marking>;` rejects via #8445 like its braced twin (was a silent fold) — two `compact-blind` #2419 inventory lines deleted.
- **Action**: `hoistAndSplitRun8939` at both then-readers (new `policerThenSchema9882` / `threeColorPolicerThenSchema9882` resolvers, covered by `TestFlatRunResidueSchemasResolve9235`). Required by the #9156 leaf-run differential: the new `[forwarding-class -> loss-priority]` row differed on both axes (one-line run kept only the head). Also closes a strict-path flat-set chain loss (`set … then discard loss-priority high` in one command silently dropped the marking; now the #8445 conflict). No-op on canonical separate-statement shapes (all #8445 cells byte-green).
- **Action**: `policerThenExtras9882` records tokens fused past an action's declared arity as unknown (mirrors `thenActionExtras8971`, including its unknown-head bound). Required by the #2419 spelling differential: the FC/LP rows cleared to all-keep agreement (no registration); the `then discard` row's D-shape nests the second token under the unknown head, so it is registered in `notAValueList` with the verified landing (strict rejects / lenient warns naming the first token on every shape; operator verdict identical for one- and two-token forms).
- **Action**: ratchets, all deliberate with named members:
  - #7484: floor net 741→742 (via 743: policer-FC COMPARED like sibling LP; packed reader moved `policer then discard` flag→COMPARED; then excluded as non-list); flag 224→223; err 43→44 (three-color-FC joins both siblings harness-blind); registry +2 arrivals, 1 reclassification-then-departure.
  - #8880: 449→450 (pair `then forwarding-class`, one key for both paths beside sibling `then loss-priority`); residue measured-benign — packed shapes have braced parity via the leaf-walk (pinned by `TestPolicerThenPackedParity_9882`), which also closes the sibling pair's silent fold; `packedStatements` measured and rejected (splitter stops at undeclared tokens, would still swallow `then discard foo;`'s tail).
  - #8690: `firewall three-color-policer xpfarg then forwarding-class` joins `knownUnexaminable8690` beside its LP sibling (fixture carries no rate block; unmeasurable until #8436, not unfixtured).
  - #8939: regen, unmeasured 82→84 post-rebase (was 76→78 on pre-rebase base; new [discard, FC] pairs' split arms are #8445-rejected — correct gate behavior; loser list byte-identical).
  - #8921: regen, `then forwarding-class` row 4→6 sites post-rebase (was 3→5 pre-#9899; + firewall/policer/then, + firewall/three-color-policer/then), matching the sibling `then loss-priority` row; fold behavior at both new sites measured by the packed-parity cells.
  - #8763 pair moved populations: `then forwarding-class` went family-only → dual-path. Deleted its famOnly case; added it to the dual-path want-set with a retaken family-shape measurement (folds=1, SAME/SAME, braced delivers, reads-but-inert — twin of the LP row).
  - #9156: fixed at the reader (above), no register row needed.
- **Action**: cells.
  - `pkg/configstore/policer_then_unknown_9882_test.go`: strict rejects (typo / unknown / unknown+discard / trailing-after-known / three-color); spelling matrix pinning outcome AND `ThenAction` (discard, lone LP, lone FC→mark, typo→reject/lenient-discard, discard+FC→reject/lenient-discard via the lone-only fence — GPT-1 fold, base-matching in both orders); flat-set strict; unknown-before-conflict order; lenient warn+keep-discard; three-color matrix incl. #9503 advisory presence + conflict-fence twin; packed/braced parity per spelling (+ FC-head #8437 note, O1-pinned); three-color packed; flat-set chain tail; review fold: block-form parity + render/replay/delete lifecycle, valueless-loud (M1), duplicate-accumulate (M2), three-color review shapes; second fold: split-root accumulate (GPT-2), non-leaf tail (GPT-3).
  - `pkg/dataplane/userspace/three_color_forwarding_class_9882_test.go`: single-rate lone-FC `DiscardExcess=false` vs discard `true`; three-color FC armed (reuses #9503 harness) incl. color-aware disarm; shape drift-guard table.
  - `userspace-dp/src/filter/tests.rs::three_color_forwarding_class_meters_without_dropping_9882`: RED forwarded, counted red, zero drops (clone of the #9503 cell).
- **Validation**: `pkg/config` + `pkg/configstore` + `pkg/dataplane/userspace` full suites; Rust `three_color_*` (17), `policer` (26), `unsupported_three_color_snapshots_fail_closed_in_rust_compiler` (drift guard intact); `go vet`; `gofmt`; `cargo fmt --check` clean on touched files. Named 9882 tests 5x.
- **Residuals** (see config-schema.md #9882 section): D-shape second token nested under an unknown head swallowed but commit rejects/warns naming the first (registered); LP/FC values unvalidated; DeletePath refuses block-form leaf delete (pre-existing #8992 guard, pinned); shared hoister still strips block-values at other call sites (none measured — see shield doc).

- **Review fold (parent review: GPT NEEDS-MAJOR + SPARK NEEDS-MINOR, convergent on the hoist).**
  - BLOCKING: `hoistAndSplitRun8939` lifted block-form values (`loss-priority { high; }`) as sibling unknowns — strict rejected a base-accepted spelling while lenient fell back to discard (meter-only → dropped excess; red-proved both halves on HEAD before fixing). Fix: `shieldPolicerThenValues9882` folds leading children into unsatisfied value slots first (positional #9124; masks move with keys #8921; consumed tails/subtrees promoted, never dropped; copy-on-write, idempotent), policer-local by design (shared hoister untouched — no other call site has a measured block-value reader; noted in the helper doc for a future lane). Covered: block-form LP/FC strict+lenient parity cells + render→replay→deletion lifecycle (delete-leaf refusal pinned as pre-existing #8992 behavior; delete-then and Keys-form delete return to default clean).
  - M1 (spark): valueless marking (`then { forwarding-class; }`) recorded as `forwarding-class (missing value)` in UnknownActions; gate renders requires-a-value message (one channel, two presentations). Measured: hierarchical strict already rejected at SchemaValidate arity (pre-existing); flat-set strict + lenient were silent discard — now reject/warn. Cells pin all three paths + three-color twin.
  - M2 (spark): duplicate `then` blocks now iterate `FindChildren` (#3850 mirror); second-block unknown/conflict gated, repetition stays redundancy, terminal last-wins in block order. Cells pin unknown/conflict/redundancy + three-color twin.
  - M3 (spark): added both then-resolvers to `TestFlatRunResidueSchemasResolve9235` (was falsely claimed covered).
  - M4 (spark): `policerThenExtras9882` aligned bound-for-bound with `thenActionExtras8971` (multi/children/wildcard guards + copy instead of alias). No behavioral cell possible (all three heads bare) — defensive alignment, noted here.

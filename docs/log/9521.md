# #9521 — an unsteered WireGuard listen port's plaintext was kernel-forwarded, and the runtime winner could differ from the warned one

- **Timestamp**: 2026-09-10T19:08Z
- **Action**: Re-verified the defect at base `28f0fb93b` in a detached worktree (probe files only, not for merge).
- **File(s)**: `pkg/dataplane/userspace/zz_rv9521_divergence_test.go`, `userspace-dp/src/afxdp/coordinator/wg_control/wg_control_tests.rs`, `userspace-dp/src/afxdp/frame/tests_shim_wg_classify_8274.rs`, `userspace-dp/src/afxdp/coordinator/tests.rs` (all in `/var/tmp/wt-9521-base`)

| Probe | What was run | Base result |
|---|---|---|
| RV1a (Go) | two-port config (wg1:51900 authored first, wg0:51820); live interface rows lack the warned winner `wg0`; `snapshotWgListenPort` | warning names **51820**, dataplane programs **51900** |
| RV1b (Go) | same config through the real `buildSnapshot` (no `wg*` netdev on the host) | warning names **51820**, dataplane programs **0** |
| RV2 (Rust) | `run_wg_control_loop` with an established engine pair, an authenticated type-4 record, a socketpair TUN stand-in | **40 bytes == the inner packet** written to the TUN |
| RV3 (Rust) | the shim's own `wg_worker_claims_record` (path-included) | claimed on a port match, **not claimed** on a mismatch |
| RV4 (Go) | `TestHostInboundAdmitsEveryWireGuardPort9016` | PASS — every configured port admitted |
| RV5 (Rust) | a two-port snapshot through `refresh_runtime_snapshot` | control threads spawned for **both** endpoints `[1, 2]` |

The first RV2 run was VOID: the probe passed a blocking socketpair as the TUN, and the loop hung in its TUN read (production makes the TUN non-blocking before the loop). Saved as `rv9521-rust-base-VOID-hang.log`; re-run with the fd non-blocking.

- **Timestamp**: 2026-09-10T19:08Z
- **Action**: Design decision — fail closed in the helper's control-thread socket, not in nftables, not at commit, not by port-set steering.
- **File(s)**: (analysis)

Candidates and why each was taken or not:

- **Stop admitting unsteered ports in the host-inbound filter.** Rejected. The input chain accepts `ct state established` ahead of the WireGuard admit, so a peer answering a handshake xpf initiated rides conntrack past any per-port rule; a zone admitting all host-inbound services (`hostInboundAllowsAll`) accepts the port anyway; and `applyHostInboundFilter` installs no table at all when no enforceable view, unzoned address or junos-host program exists. The fix would also have to be mirrored into the cold-boot fence and the gap fence, in both renderers.
- **Refuse multi-port at commit.** Rejected by the gate's own recorded decision (#1960 no-brick): a box already running such a config could not commit an unrelated change.
- **Do not spawn a control thread for an unsteered endpoint.** Rejected on measurement of the code path: a listener on an interface-mode source-NAT address is not `is_local_destination` in the shim, so its records go to the worker (`is_interface_nat_destination` → XSK), whose decap matches ANY configured listen port (`wg_endpoint_for_listen_port`), and its handshakes reach the kernel socket through `interface_nat_local_resolution_on_session_miss` → LocalDelivery. That tunnel works and is adjudicated today; refusing the thread would break it.
- **Remove the control thread's TUN write for every endpoint.** Rejected: `docs/log/8274.md` "The residual, stated rather than closed" keeps that write deliberately for the steered port on shim-uncovered ingress and for host-inbound over the tunnel. Not re-litigated.
- **Port-set steering (#1434 Increment 2).** Deferred to #9587, filed here so the deferral has an owner: verifier-gated shim change, v6 line-rate sensitivity, loss cluster.
- **Chosen:** the unsteered endpoint's control thread authenticates a transport record and drops the plaintext (`WgKernelTransport::DropUnsteered`, counted `rx_unsteered_transport_drops`). It is the one place every such record converges, it leaves the steered port and the worker path untouched, and it is fail-closed (a snapshot naming no steered port delivers for no endpoint).

- **Timestamp**: 2026-09-10T19:08Z
- **Action**: Implemented the fix and the single steered-port derivation.
- **File(s)**: `pkg/config/compiler_validate_wireguard_multiport.go` (`SteeredWireGuardListenPort`, gate and warning text), `pkg/dataplane/userspace/protocol.go` (`ConfigSnapshot.WgSteeredListenPort`, ProtocolVersion 13), `pkg/dataplane/userspace/builder.go`, `pkg/dataplane/userspace/maps_sync.go` (`snapshotWgListenPort` reads the field), `pkg/dataplane/userspace/protocol_tunnels.go`, `pkg/dataplane/userspace/format/wireguard.go`, `pkg/api/metrics_userspace.go`, `pkg/daemon/daemon_nft.go` (comment), `userspace-dp/src/protocol/snapshot.rs`, `userspace-dp/src/protocol/control.rs` (status field, CONFIG_SNAPSHOT_PROTOCOL_VERSION 13), `userspace-dp/src/afxdp/types/forwarding.rs`, `userspace-dp/src/afxdp/types/runtime.rs` (`WgKernelTransport`, entry field), `userspace-dp/src/afxdp/forwarding_build/tunnels.rs`, `userspace-dp/src/afxdp/coordinator/tunnel_supervision.rs` (decision at spawn, `kernel_transport_changed` prune arm), `userspace-dp/src/afxdp/coordinator/wg_control/mod.rs` (parameter threading, `#[cfg(test)]` TUN stand-in seam), `userspace-dp/src/afxdp/coordinator/wg_control/dispatch.rs` (the gate), `userspace-dp/src/afxdp/wg/counters.rs`, `userspace-dp/src/afxdp/coordinator/status_wg.rs`

- **Timestamp**: 2026-09-10T19:08Z
- **Action**: Prior decisions that went red, read and re-anchored rather than deleted.
- **File(s)**: see each bullet

- `TestMultiportAdvisoryDoesNotClaimTheTunnelIsDead9016` (`pkg/config`): still forbids "dead" / "no handshake ever" (still false — the tunnel handshakes and sends); now also forbids #9016's "served by the KERNEL path" (true until this change); requires REFUSED / DROPPED / "instead of being written to its wgN TUN".
- `TestOnlyFirstWireGuardListenPortIsSteered9016` (`pkg/dataplane/userspace`): its reversed-rows case pinned the order dependence that WAS the promotion defect; it now requires the reader to ignore rows (reversed, steered row absent, no field → 0).
- `TestWireGuardSteeringAdvisoryNamesTheProgrammedPort_1434`: hand-built the one arrangement (every netdev present) where old and new could not disagree; now uses the real builder and checks all-present / warned-absent / none.
- `TestSnapshotShapeIsPinnedToProtocolVersion8892`, `TestSnapshotProtocolVersionLockstepWithRust`, `TestProtocolVersionMovedWithTheWire9054`, `TestMixedVersionMatrix_6691`, `TestSecureTunnelFieldIsNotIgnorableByAPreV5Helper`: version 12 → 13 with a history entry each. BUMPED, by the v10/v11 test: an old helper ignores the field and keeps delivering the plaintext, which is the defect.
- `TestEmitWireguardTelemetryNeverHandshakedGauge` (55 → 56 series) and `TestEmitWireguardTelemetrySeriesSet`: one new decap drop reason, `unsteered_port`.
- Rationale comments corrected where the premise went false: `wireguard_multiport_warning_1434_test.go` ("permanently, silently down"), `hostinbound_wg_ports_9016_test.go`, `daemon_nft.go` (`emitHostInboundWireGuardAccept`).

- **Timestamp**: 2026-09-10T19:08Z
- **Action**: Filed #9587 (#1434 Increment 2, port-set steering) so the deferral the warning cites has an owner.
- **File(s)**: (GitHub)

- **Timestamp**: 2026-09-10T19:08Z
- **Action**: Docs updated in the same change.
- **File(s)**: `docs/wireguard-interop.md` (multi-tunnel section rewritten; host-inbound runtime nuance), `userspace-dp/src/afxdp/coordinator/README.md` (restart conditions, the drop, the test seam), `userspace-dp/src/afxdp/coordinator/wg_control/mod.rs` (module doc)

Deliberately NOT changed, with reason: `pkg/dataplane/README.md`'s "WireGuard-to-firewall via `wg_steer_to_kernel`" is still true (handshake records); the #5618 plaintext advisory (`compiler_wireguard_plaintext_warn.go`) and the #5582 host-inbound prose describing pre-#8274 transport steering are #9251, which is next and is scoped to them.

# 10395 — promote upgrade advisory cells to permanent regressions

- **Date**: 2026-09-18
- **Base**: `b6326cdd70d80272dcfc6d61d6dc37fa06f73402`
- **Fix**: `71bed89223818e6e392ac0b6b0b41776d04f3870`
- **Files**: `pkg/upgrade/cut_boundary_snapshot_10395_test.go`

## Scope

Issue #10395 promotes the two advisory probes from the Rev10393 delta review to permanent `pkg/upgrade` regression cells. No production code changed.

## RED then GREEN evidence

The RED controls used a detached scratch worktree at `/tmp/10395-red`, copied the permanent test file, and replaced the #10307 production files `pkg/upgrade/cutover.go`, `pkg/upgrade/flip.go`, and `pkg/upgrade/state.go` with their `d3843fc08^` versions. This exercises the pre-fix behavior rather than trusting the issue description.

### Pending-confirm preservation

Command:

```text
go test ./pkg/upgrade -run '^TestRun_CutBoundarySnapshotPreservesPendingConfirm10395$' -count=1 -v
```

The reverted production code failed both independent content assertions:

```text
cut_boundary_snapshot_10395_test.go:59: rollback lost VERIFY commit: got "#xpf-config-envelope v=1\n{\"gen\":\"pre-upgrade\"}", want "#xpf-config-envelope v=1\n{\"gen\":\"committed-during-verify\"}"
cut_boundary_snapshot_10395_test.go:63: pending confirm.json missing after rollback
--- FAIL: TestRun_CutBoundarySnapshotPreservesPendingConfirm10395
```

With the #10307 production fix present, the same cell passed:

```text
--- PASS: TestRun_CutBoundarySnapshotPreservesPendingConfirm10395
ok   github.com/psaab/xpf/pkg/upgrade
```

The cell arms `confirm.json` and writes the commit from `VerifyDataplane` after PREFLIGHT, then forces START failure. It verifies both the commit bytes and pending confirmation bytes survive auto-rollback.

### Stat-failure DB content

Command:

```text
go test ./pkg/upgrade -run '^TestRun_CutBoundarySnapshotFailurePreservesLiveDB10395$' -count=1 -v
```

The reverted production code did not take the cut-boundary snapshot and therefore returned success instead of the injected failure:

```text
cut_boundary_snapshot_10395_test.go:103: Run error = <nil>, want cut-boundary snapshot failure
--- FAIL: TestRun_CutBoundarySnapshotFailurePreservesLiveDB10395
```

With the #10307 production fix present, the same cell passed:

```text
--- PASS: TestRun_CutBoundarySnapshotFailurePreservesLiveDB10395
ok   github.com/psaab/xpf/pkg/upgrade
```

The GREEN cell verifies byte-identical live `active.json` and `confirm.json`, byte-identical PREFLIGHT snapshot `active.json` and `confirm.json`, no `.2.0.0.dbsnap.partial`, a running daemon, current `1.0.0`, and journal state `VERIFIED` after the cut-boundary stat failure.

## Suite evidence

The targeted permanent-cell run passed:

```text
go test ./pkg/upgrade -run 'TestRun_CutBoundarySnapshot(PreservesPendingConfirm|FailurePreservesLiveDB)10395' -count=1 -v
PASS
ok   github.com/psaab/xpf/pkg/upgrade
```

The complete upgrade module passed:

```text
go test ./pkg/upgrade/... -count=1
ok   github.com/psaab/xpf/pkg/upgrade
ok   github.com/psaab/xpf/pkg/upgrade/lock
ok   github.com/psaab/xpf/pkg/upgrade/manifest
ok   github.com/psaab/xpf/pkg/upgrade/runtime
ok   github.com/psaab/xpf/pkg/upgrade/stagedgen
```

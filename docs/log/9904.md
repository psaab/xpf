# #9904 cohort — AF_XDP ring/bind contracts — engineering history

Issue: psaab/xpf#9904 (`[cohort] userspace-dp AF_XDP: three ring/bind
assumptions about kernel and driver behaviour that the tree relies on and
does not record`). Lane branch: `fix/9904-frame-errors`, base
`origin/master` 5f75d012a. Three members (F-069, F-149, F-151), all Low,
all "record-the-contract + add the implied guard", acceptance per member a
unit/doc assertion.

## STEP-0 (RED on base, all three)

- F-069 `umem_readme_cites_fill_alignment_contract_9904`
  (`userspace-dp/tests/fill_alignment_contract_9904.rs`): README lacks any
  `xp_check_aligned` citation → FAILED on base.
- F-149
  `shared_recycle_unknown_slot_preserves_frame_in_single_region_9904`
  (`src/afxdp/tx/dispatch/tests/shared_recycle.rs`): two bindings sharing
  one UMEM, `(99, 0x1000)` unknown slot via
  `apply_shared_recycles_to_bindings` → dropped on base (log: "dropping 1
  shared UMEM recycles for unknown slots (first slot 99 offset 4096)") →
  FAILED on base.
- F-151 `bind_performs_post_bind_identity_verification_9904`
  (`userspace-dp/tests/bind_identity_verification_9904.rs`): `bind.rs`
  contains no post-bind verification → FAILED on base.

## Plan reviews (pre-implementation, both hostile) + adjudication

- Review A (reviewer): PLAN-KILL overall — kill §2 rescue as unsound
  (one-binding trivial pass, departed-region hole, unreached path), retreat
  §1 to doc-only (prime bypass, no drain test, no perf gate), no new
  counter. 12 findings.
- Review B (reviewer): NEEDS-MAJOR — §2 rescue shape right but premise
  unstated, no rescue logging, full Go render bloat; §3 verify failure
  swallowed by stale-flags retry, fail-closed bricks bringup on transient
  renames, source-grep cell vacuous. 8 findings.

Adjudication (verified against source, not by vote):

- F-069 → DOC-ONLY (A wins; issue explicitly allows "either mask ... or
  state why masking unnecessary"). Production keeps relying on the kernel
  aligned-mode mask; the README now cites `xp_check_aligned`, the
  `flags == 0` + 4096 preconditions, and reconciles the harness (which
  masks explicitly) in prose. No code change → A's prime-bypass, drain-test,
  and perf-gate findings evaporate; harness sources untouched per A11.
- F-149 → RESCUE KEPT, with premises verified and every must-fix folded.
  Worker-local provenance verified: `shared_recycles` is a worker-local
  `Vec` (`loop_body/mod.rs:316`), cross-worker inbox carries owned
  `TxRequest` not `Prepared` (`binding_state/mod.rs:762`), bindings never
  mutated after setup (no push/remove in the loop), lookup built once
  (`setup.rs:236`). Given (a) worker-local provenance + (b) intra-worker Rc
  sharing + (c) immutable bindings, single-region rescue is sound — A's P0s
  assume the cross-region provenance its own A3 refutes. Unknown slots are
  unreached in correct code (defensive path), so the rescue is a guard, not
  a live fix — and it stays LOUD: rescued joins `tx_errors` (A7), own
  `..._rescued` subset counter, bounded one-line log per drain (B5).
  Backstop computed once per drain with batched accounting (A8);
  session_glue unified from per-drop to bounded logging (B8/A9); counter cut
  to Rust + Go-wire-additive with NO Go human render/golden (B6/A6).
- F-151 → VERIFY + FRESH-RESOLVE RETRY (B wins on all points). Verify after
  create, before prime; on mismatch drop the socket, fresh-resolve the
  planned ifindex (restoring queue_id), and retry within the existing
  20-attempt budget — transients self-heal, persistent mismatches fail
  closed with planned-vs-observed detail. Fresh `&mut IfInfo` threading
  makes flags retry safe (B1's stale-retry harm evaporates; no terminal
  classification needed). Shared/private recovery now symmetric (B4).
  Residual documented: post-create rename false-positives; delete+recreate
  with same ifindex invisible; bind/rename concurrency assumed from the
  startup storm, not proven. §0 cell tightened from substring to call-site
  pin (B7); helper behavior-tested with injected resolver; wiring
  review-pinned (needs real hardware).

## What shipped

- F-069: `## Fill-alignment contract` section in
  `src/afxdp/umem/README.md` (doc-only, no code).
- F-149: single-region backstop in all three unknown-slot sites
  (`tx/dispatch/shared_recycle.rs` split + all-bindings,
  `session_glue/mod.rs` cancel path) with stated safety premise; new
  `tx_shared_recycle_unknown_slot_rescued` counter (BindingLiveState atomic
  + snapshot + wire structs + From + refresh copy/reset + Go wire fields +
  roundtrip pins; NO Go human render); `tx/README.md` constraint + 
...[truncated 2278 chars]
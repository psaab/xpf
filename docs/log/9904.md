# #9904 cohort — AF_XDP ring/bind contracts — engineering history

Issue: psaab/xpf#9904 (cohort: three ring/bind assumptions about kernel and
driver behaviour). Lane branch `fix/9904-frame-errors`; STEP-0 at
`origin/master` 5f75d012a; rebased onto be80722a0 for the parent-review
fold (clean, no conflicts). PR #10108, STOP without merging.

## STEP-0 (RED on base 5f75d012a, all three)

- F-069 `umem_readme_cites_fill_alignment_contract_9904`: README lacks any
  `xp_check_aligned` citation → FAILED.
- F-149 `..._preserves_frame_in_single_region_9904`: `(99, 0x1000)` unknown
  slot dropped ("dropping 1 shared UMEM recycles for unknown slots") →
  FAILED.
- F-151 `bind_performs_post_bind_identity_verification_9904`: no post-bind
  verification in `bind.rs` → FAILED.

## Plan reviews (pre-implementation, hostile) + adjudication v1

- A: PLAN-KILL (kill rescue as unsound/unreached; doc-only §1; no counter).
- B: NEEDS-MAJOR (§2 premise unstated + Go bloat; §3 stale-flags retry +
  brick risk + vacuous cell).
- Adjudicated: F-069 → doc-only (issue allows it); F-149 rescue kept with
  verified premises (worker-local provenance: local `Vec`, `MpscInbox<TxRequest>`,
  immutable bindings, lookup built once) + loud recovery (`tx_errors` +
  rescued subset + bounded log) + debug-scoped counter (Rust + Go-wire,
  no human render); F-151 verify + in-bind fresh-resolve retry.

## Parent-review fold (adjudication v2)

- GPT-1 (UAF drop order): FIXED. Mismatch teardown now drops device-first
  (socket delete while rings alive), matching `WorkerXskRings` declaration
  order (device, rx, tx) — in-repo evidence, no libxdp source needed. Only
  `DeviceQueue` has `Drop`; User/Rx/Tx hold a raw fd closed by the delete.
  Covered by new hermetic `bind_mismatch_teardown_9904_tests` (null-handle
  fixtures exercise the real ordered-drop path + terminal error).
- GPT-2 (UMEM reuse across in-bind retry): FIXED by removing in-bind retry.
  Re-creating on a UMEM whose owner socket was just deleted reuses owner
  resources across a boundary this tree cannot prove safe (no vendored
  libxdp source for 1.6.3+ evidence). The bind now fails closed on first
  mismatch; recovery is the outer reconcile loop (rebuilds workers
  wholesale: fresh UMEM + fresh `IfInfo` per `bring_up_workers`) — the retry
  boundary that reconstructs owner resources. Transient storms delay
  bringup; they cannot brick it. `&mut IfInfo` threading reverted.
- GPT-3/SPARK-B7 (source-string RED): FIXED. Integration cell rewritten as
  an acknowledged tripwire pinning invocation AFTER create / BEFORE prime
  (positional ordering assert) + routing through the ordered teardown; the
  invoked logic is covered behaviorally (mock-resolver helper test +
  hermetic teardown test). Wiring past that needs hardware (review-pinned).
- GPT-4 (log truncation placeholder): FIXED — this file rewritten complete.
- SPARK minors, all folded: `UMEM_CREATE_FLAGS` const + value pin
  (`umem::create_flags_9904_tests`); kernel bounds-check citation at all
  three rescue pushes (invalid-descs accounting, never OOB DMA); new
  session_glue + split-mixed + single-binding cells; both grep cells
  acknowledged as tripwires; log base SHA fixed; backstop cost noted
  (O(N) ptr compares/once per drain, deliberately not lazy-gated);
  pending_fill growth bounded-by-construction documented (real pool frames,
  per-tick drain).
- README RX-offset overstatement: FIXED — headroom now stated as
  UMEM-256 plus any kernel-added headroom, always intra-chunk.
- Rebase onto be80722a0: done clean; gates re-run (below).

## Validation (final, post-fold)

- Named cells: 10 Rust bin `9904` cells + 2 integration tripwires, each 5x
  GREEN; Go roundtrips 5x GREEN.
- Full `xpf-userspace-dp` bin suite: 5967 passed, 1 failed — the single
  failure is pre-existing on base (`cos_literal_guard_7689` NeighborSnapshot
  ceiling 26 vs 29; raw literal count identical 51 on base and after — none
  in touched files; out of cohort scope, disclosed in PR).
- All 11 integration targets GREEN. Full Go `pkg/dataplane/userspace`
  suite GREEN. gofmt/vet clean; no new Rust warnings/clippy on touched
  lines (tree-wide rustfmt drift is pre-existing toolchain skew).

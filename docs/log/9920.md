# #9920 — dist re-sign laundering + silent loop + unpinned helper toolchain

Cohort: F-063 (re-sign launders a reduced set), F-064 (loop reports success on a red;
OSError traceback), F-148 (helper toolchain unpinned while debian/rules says pinned).
Worktree `fix/9920-dist-resign`, base `origin/master a17a9c67f`.

- **Timestamp**: 2026-09-15
- **Action**: STEP-0 RED on base, one cell per member (`/tmp/repro-9920-F*.sh`,
  base-only evidence — the F-063 repro drives the pre-fix permissive path).
  - F-063: honest 4-file set signed; dropped the `.pkgs` line from the LIVE manifest;
    re-ran the exact recipe derivation (`awk '{print "dist/" $2}'`) + `sign-manifest`:
    EXIT 0, new `.minisig` written, laundered 3-file manifest VERIFIES under the
    rotation key. Second leg: `validated: false` sidecar still re-signs EXIT 0.
  - F-064: two `dist/xpf-*.SHA256SUMS` (first with deleted `.pkgs`, second honest);
    recipe loop verbatim: EXIT 0, only the second `.minisig` exists (half-signed
    tree). Second leg: missing file prints unhandled `Traceback ...
    FileNotFoundError`, no `ERROR:` line.
  - F-148: `find . -name 'rust-toolchain*'` returns exactly
    `userspace-xdp/rust-toolchain.toml`; `userspace-dp/` pin missing;
    `debian/rules:6,37` claims "pinned cargo"; `build-userspace-dp` has no
    toolchain assert; no `rust-version` in `userspace-dp/Cargo.toml`.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-15
- **Action**: Two hostile plan reviews BEFORE implementing (both APPROVE-WITH-CHANGES).
  Adjudication (full record in PR body): the plan's separate `resign` subcommand was
  KILLED — reviewer A proved bake uses the sign LIBRARY, never the CLI, so strictness
  belongs in `sign-manifest` itself (fail-closed default; a permissive documented
  spelling one runbook away would preserve the laundering). Adopted: strict CLI scoped
  to `xpf-*.SHA256SUMS` (only publish-discovered names; deliberate rename-evasion is a
  key-holder actor, out of scope — publish stays fail-closed). Q1 YES narrow
  (bake uses `sign.bake_set_basenames()` SSOT); Q2 NO build.rs assert (both reviewers;
  raw cargo is explicitly second-class); Q3 debian/rules ONLY (siblings become true
  untouched); Q4 YES `1.98.1` (install proven: `rustup toolchain install 1.98.1`
  completed in 13 s); Q5 live-make KILLED (collides with §8k, trap-unsafe) in favor of
  fixture-dir `make -C fixture -f $ROOT/Makefile dist-sign` (§8l), no source-text loop
  assert. Also adopted: cause-needle asserts + single-variable fixtures (B1), POSIX
  + rejection-battery for the new script (B2/A9), minisign-SKIP scoped to signing cells
  (B4), bytes-unchanged atomicity (B6), shared (moved, cycle-free) sidecar parser
  instead of duplication (A6), parse-before-write with content-tamper residual
  documented (A4/A5). Reviewer errors corrected: A10's "bake validates no versions" is
  false (`bake.validate_version`, #5992, at bake.py:145/803) — sign MIRRORS it with a
  parity cell per the established mirror idiom; fixture churn is 3 sites (not ~2).
- **File(s)**: `/tmp/plan-9920.md` (review input; not committed)

- **Timestamp**: 2026-09-15
- **Action**: Implemented the adjudicated design.
  - F-063: `sign.assert_bake_set()` (SSOT set + sibling-dir + mirrored version check +
    `validated`/`base_image_pinned`/`guest_kernel`), enforced by `sign-manifest` for
    bake manifests before any write; `parse_sidecar_fields` moved from publish (single
    caller updated, its test retargeted); bake builds its manifest list from the SSOT
    (same files, same order); `test_bake_base_pin` source pin updated to the SSOT call.
  - F-064: `dist-sign` is `set -e` + per-manifest `|| { echo ... FAILED; fail=1; }` +
    trailing `test -z` (dash-traced for all-honest/first-/last-broken/no-match);
    `_main` maps `OSError` to `ERROR:` + exit 1.
  - F-148: `userspace-dp/rust-toolchain.toml` (`channel = "1.98.1"`),
    `build_support/dp-toolchain.sh` (POSIX strict parse + cargo usability check),
    all five cargo lines in the three helper recipes use `+$$pinned` (explicit —
    cargo never discovers the pin by CWD from the repo root), both `debian/rules`
    blocks name the mechanism, one-line note in `miri-leg.sh` (explicit `+nightly`
    overrides the pin by design).
  - Tests: new `scripts/dist/test_dist_resign_9920.py` (25 cells, `test_*.py` +
    `__main__` runner contract); `selftest.sh` gains `direct_sign` (3 publish-negative
    fixtures that MODEL signed-but-bad sets now bypass the strict CLI via
    sha256sum+minisign primitives) and §8l (real-recipe fixture-dir legs);
    `dp-toolchain.sh` added to `SH_SCRIPTS`.
- **File(s)**: scripts/dist/sign.py, scripts/dist/publish.py,
  scripts/dist/test_publish_provenance.py, scripts/dist/selftest.sh,
  scripts/dist/test_dist_resign_9920.py, scripts/image/bake.py,
  scripts/image/test_bake_base_pin.py, scripts/run-selftests.sh, scripts/miri-leg.sh,
  Makefile, debian/rules, userspace-dp/rust-toolchain.toml,
  userspace-dp/build_support/dp-toolchain.sh

- **Timestamp**: 2026-09-15
- **Action**: Red-green + verification.
  - New file: 25/25 OK post-fix, 5x OK; on base (detached worktree at a17a9c67f):
    12 FAIL + 11 ERROR, with only the 2 honest-path positive controls passing on
    both sides by design (honest-set-signs, non-bake-permissive).
  - Killing mutations executed (each restored after): neuter `validated` leg → its
    cell FAILs; drop `+$$pinned` from one cargo line → recipe cell FAILs; neuter
    pin script → rejection cells FAIL. Tree verified clean after (`git diff --stat`
    back to 10 files + 3 new, 25/25 OK).
  - `scripts/dist/selftest.sh`: 56 passed, 0 failed (incl. new §8l, 7 legs).
  - `make selftest`: passed=192, skipped=0, failed=1 — the single failure is
    `shellcheck test/incus/harness-result.sh`, PROVEN pre-existing (base file
    content fails the same gate; file untouched by this diff, different subsystem).
    Own lint: `shellcheck -S warning` clean on selftest.sh (base-parity; one
    self-introduced SC1125 fixed), dp-toolchain.sh, miri-leg.sh.
  - `make build-userspace-dp`: green under `+1.98.1` (57 s); `make -n` shows the
    pin on all five cargo lines. `cargo fmt --check` delta in
    `benches/prefix_set_lookup.rs` is pre-existing (zero `.rs` files in this diff).
  - Post-fix analogs: honest re-sign exit 0; reduced set exit 1 with the four-file
    needle and no new `.minisig`; empty `dist/` fails with the retained diagnostic;
    missing input prints `ERROR:` + exit 1 with no traceback.
- **File(s)**: none (measurement)

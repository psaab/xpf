# #9892 — `load` and `rollback` bypass the `deny-configuration` regex

Work log for issue #9892. See `docs/log/README.md` for why this lives here and
not in the repository-root `_Log.md`.

- **Timestamp**: 2026-09-12
- **Action**: Landed. `load` and `rollback` are adjudicated against the login
  class's `deny-configuration` regexes on all three surfaces (gRPC, REST, local
  CLI) from ONE evaluator in `pkg/config`; #9633's gRPC-local copy is deleted,
  not left alongside.
- **What the mutation matrix changed about the change**: M8/M9 ESCAPED. A mutant
  that kept the call to the shared evaluator and discarded only its verdict
  (`…; false && err != nil {`) passed every row, because the cross-surface check
  is a CENSUS — it answers "is the evaluator called", never "is its answer
  obeyed", and on the CLI nothing else was asking. Closed by
  `pkg/cli/authz_config_load_cli_9892_test.go`, which drives `handleLoad` and
  `dispatchConfig` against a real `configstore` and asserts the candidate is
  byte-identical after a refusal — a gate that errors after the candidate
  already carries the denied path leaves the operator one `commit` away from the
  outcome it exists to prevent.
- **Second finding, from a MISATTRIB rather than an escape**: deleting
  `"deactivate"` from `loadFlatVerbs` left `TestLoadOfADeniedPathIsRefused9892`
  green despite its row named "deactivate inside a set body". Its fixture rule is
  UNANCHORED, so `deactivate system root-authentication` still contains the
  pattern and the denial fires under both arms. The anchored cell
  (`TestEveryFlatVerbIsTakenVerbatimNotReparsed9892`) is the real owner; the row
  now carries a comment saying what it cannot cover.
- **Validation**: 11 mutants, all KILLED by a named cell, 0 escapes, 0 void.
  Baseline at the landed tree: 12887 collected, 0 failing, `filtered_out=0`.
  `make test-go` green on the merged tree; `LEGS_RUN=go`, `LEGS_SKIPPED=rust`,
  `RUST_DELTA_LINES=0` measured against `origin/master`.
- **File(s)**: `pkg/config/authz_config_load_9892.go`,
  `pkg/config/authz_config_load_9892_test.go`,
  `pkg/config/authz_config_load_surfaces_9892_test.go`,
  `pkg/api/authz_config_regex_9154.go`, `pkg/api/authz.go`,
  `pkg/api/authz_config_route_census_9890_test.go`, `pkg/cli/cli_config.go`,
  `pkg/cli/cli_dispatch.go`, `pkg/cli/authz_config_load_cli_9892_test.go`,
  `pkg/grpcapi/authz_config_rpc_9633.go`, `docs/log/9892.md`

# #9634: resetRecvGen resets three of the four full-set guards

Lane: lane-ha2. Worktree `/var/tmp/wt-9634-fullset-guard-reset`, branch
`fix/9634-reset-every-fullset-guard`, from origin/master `6cf4305ab`.

- **Timestamp**: 2026-09-11 06:05 PDT
- **Action**: Re-read the mechanism at `6cf4305ab`.
  - `resetRecvGen` (`sync_conn_gen.go`) is the peer bulk re-prime reset. Under `recvSeqMu` it reset
    `ipsecRecvSeq`, `dhcpV4RecvSeq` and `dhcpV6RecvSeq`, one line each.
  - `SessionSync` declares a fourth `fullSetSeqGuard`, `persistentNatLeaseRecvSeq` (#8121). It has
    two references in the tree: its declaration, and the `admit` in the `syncMsgPersistentNatLease`
    handler. That handler logs "dropping out-of-order persistent-NAT lease set (stale sequence)".
  - `fullSetSeqGuard.admit` accepts only a strictly higher incarnation, or a higher seq within the
    same incarnation.
  - A rebooted peer's incarnation is a monotonic-clock seed, which restarts lower. Its lease sets
    were therefore refused until its seed passed the dead process's.
- **File(s)**: none

- **Timestamp**: 2026-09-11 06:08 PDT
- **Action**: Fix.
  - `resetRecvGen` resets every guard from one list, `fullSetGuardsLocked()`, which returns the four
    guards. The #5706 comment gains a #9634 note on why it is one list.
  - The `sync.go` field doc names all four guards and the list.
  - `pkg/cluster/README.md`'s full-set ordering bullet now documents the list and the missed guard.
- **File(s)**: `pkg/cluster/sync_conn_gen.go`, `pkg/cluster/sync.go`, `pkg/cluster/README.md`

- **Timestamp**: 2026-09-11 06:09 PDT
- **Action**: Cells in `pkg/cluster/fullset_guard_reset_9634_test.go`.
  - `TestEveryFullSetGuardIsResetOnRePrime_9634` ENUMERATES the `fullSetSeqGuard` fields of
    `SessionSync` by reflection, rather than listing them. It primes each at incarnation 10000 /
    seq 5, checks each refuses 100/1, runs `resetRecvGen`, and requires each to admit 100/1. A
    guard added later is covered whether or not it joins the list. The attribution step fails the
    cell if the enumeration misses `persistentNatLeaseRecvSeq`.
  - `TestFullSetGuardsStillRefuseAReorderWithinAnIncarnation_9634`: within one incarnation, for
    every guard, seq 2 is admitted, seq 1 refused and seq 3 admitted.
  - `TestPersistentNatLeaseSetFromARebootedPeerIsAdmittedAfterRePrime_9634`: end to end through the
    handler. A long-running peer's set is delivered; the lower incarnation is refused before the
    reset (attribution); after `resetRecvGen` it is delivered.
- **File(s)**: `pkg/cluster/fullset_guard_reset_9634_test.go`

## Gates on 5e310ded1

- **Mutation matrix** (`mut9634.py`, a detached copy of 5e310ded1, `go test -json -race`, scored by the
  expected test's fail event). The baseline passed exactly the three cells; 4/4 KILLED, and
  the tree was restored clean:
    - F1-persistent-nat-not-listed: TestPersistentNatLeaseSetFromARebootedPeerIsAdmittedAfterRePrime_9634
    - F2-no-reset-loop: TestEveryFullSetGuardIsResetOnRePrime_9634
    - F3-dhcp-v6-not-listed: TestEveryFullSetGuardIsResetOnRePrime_9634
    - F4-reset-only-first: TestEveryFullSetGuardIsResetOnRePrime_9634
- **`go test -race ./pkg/cluster`** on 5e310ded1: exit 0, no data race.
- **make test** on 5e310ded1: exit 0. Go leg 78 packages ok, no FAIL. Rust leg 11
  `test result:` lines, 5877 passed, 0 failed. No disk-full. It ran with `GOFLAGS=-p=4 CARGO_BUILD_JOBS=4`, to keep peak memory down on the shared host.
- **make test-failover** on local merge `2a2c4d565` (origin/master `125e23931` + #9634 `5e310ded1`),
  deployed under the cluster lock with CoS re-applied.
  - The post-deploy reassert verified node0 primary for every RG.
  - 22 passed, 0 failed; iperf3 **20.7000 Gbps**.
  - Ledger row `58e5669aecf3dfb4.json`: verdict PASS, exe_check MATCH, build_git_sha = the merge.

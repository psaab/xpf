# #9954 userspace-dp TCP MSS in-place TX — lane log

Base: `0f41070767c1fefd56353cac5f4d7ca1ee13aa01` (`git merge-base HEAD origin/master`; current origin/master).
STEP-0 RED pin: `6d3775e11086f2ce8a5aaef650a4c8419921ddf8` (clean assignment base used for the RED reproduction).
PR: #10197 (`https://github.com/psaab/xpf/pull/10197`).

## STEP-0 RED (base)

- `all_tcp_clamps_in_place_v4_syn_9954` (provisional pre-fix name
  `step0_in_place_v4_syn_ignores_all_tcp_master_red`): `1460 != 1300`.
- `all_tcp_clamps_in_place_v6_syn_9954` (provisional pre-fix name
  `step0_in_place_v6_syn_ignores_all_tcp_master_red`): `1460 != 1300`.
- Both cells drove a valid Ethernet + TCP SYN through
  `rewrite_forwarded_frame_in_place` while a `ForwardingState` carried
  `tcp_mss_all_tcp = 1300`; the origin/master rewrite had no MSS input and
  emitted the original MSS.

## Fix

- `frame/mod.rs`: thread the caller-selected `u16` MSS into `rewrite_forwarded_frame_in_place` and run the existing `clamp_tcp_mss_frame` kernel after the in-place family rewrite, so the incremental TCP checksum update sees the final wire tuple. `frame/tcp.rs` now swaps the MSS word for odd option-byte alignment before the incremental delta.
- `frame/tcp_tests.rs`: add single-NOP odd-offset and double-NOP even-offset 1460→1300 cells; both independently recompute the TCP checksum. The odd cell was RED before the parity branch (`24735 != 65535`) and both are GREEN after it.
- `tx/dispatch/mod.rs`, `neighbor_dispatch.rs`, and the cold `poll_descriptor/flow_cache_hit.rs` fallback select the per-packet MSS with the existing `select_tcp_mss` policy before invoking the generic in-place rewrite. The descriptor fast path remains unchanged; current flow-cache eligibility admits only ACK-only TCP/UDP.
- All existing direct rewrite tests pass `0` (unset), preserving their prior behavior. The new v4/v6 in-place SYN cells pass the selected configured value and assert both MSS and TCP checksum; existing frame-build all-tcp v4/v6 cells remain unchanged and pin the copy/build path.

## GREEN (after fix)

- `cargo test --manifest-path userspace-dp/Cargo.toml all_tcp_clamps_in_place`: 2 passed.
- `cargo test --manifest-path userspace-dp/Cargo.toml clamp_tcp_mss_`: 15 passed (odd/even #9954 parity cells plus existing clamp coverage).
- Named build controls `all_tcp_clamps_plain_forwarded_ipv4_syn` and `all_tcp_clamps_plain_forwarded_ipv6_syn` (common filter `all_tcp_clamps_plain_forwarded`): 2 passed.
- `cargo test --manifest-path userspace-dp/Cargo.toml afxdp::frame`: 474 passed.
- `cargo test --manifest-path userspace-dp/Cargo.toml afxdp::tx::dispatch`: 57 passed.
- `cargo test --manifest-path userspace-dp/Cargo.toml afxdp::poll_descriptor`: 141 passed.
- `cargo test --manifest-path userspace-dp/Cargo.toml afxdp::neighbor`: 99 passed, 1 ignored.
- `git diff --check`: clean.

## Tooling note

- `cargo fmt --manifest-path userspace-dp/Cargo.toml --all -- --check` failed on pre-existing workspace formatting drift (617 files reported; no formatter writes applied).

# Issue #10198: DHCPv6 link-local Relay-Forw fallback

- **Timestamp**: 2026-09-18T11:39Z
- **Action**: retained the first usable GUA/ULA in interface-address order and added the first usable link-local address as the RFC 8415 §19.1.1 fallback when no GUA/ULA exists.
- **Action**: permitted direct-client Relay-Forw messages to carry a link-local link-address only when Interface-ID is present; nested Relay-Forw validation remains unchanged.
- **Action**: changed the upstream DHCPv6 socket to an unspecified, route-selected local bind so Relay-Forw link identity is independent of the server-facing source address.
- **Regression**: `TestSelectDHCPv6LinkAddressFallback10198` covers GUA/ULA preference, link-local fallback, and unusable-address rejection. `TestBuildRelayForwardV6LinkLocalRequiresInterfaceID10198` covers the RFC Interface-ID requirement and preserves direct GUA behavior. `TestDHCPV6LinkLocalOnlyRelaysForward10198` covers the supervisor/client path and inspects the emitted Relay-Forw link-address and Interface-ID. `TestDHCPV6UpstreamBindAddrIgnoresLinkLocalIdentity10198` directly pins the unspecified `[::]:547` upstream bind used by the default factory.

## RED -> GREEN proof

- **RED**: before the implementation, `go test -count=1 -run 'TestSelectDHCPv6LinkAddressFallback10198|TestBuildRelayForwardV6LinkLocalRequiresInterfaceID10198|TestDHCPV6LinkLocalOnlyRelaysForward10198' ./pkg/dhcprelay` exited 1. The link-local-only selector returned `no usable global IPv6 link-address`; direct link-local Relay-Forw construction returned `invalid DHCPv6 Relay-Forw link-address`; and the link-local-only live-path cell observed 0 upstream writes instead of 1.
- **GREEN**: after the implementation, the corrected targeted command exited 0 (`ok github.com/psaab/xpf/pkg/dhcprelay 0.013s`). The complete `go test -count=1 ./pkg/dhcprelay` suite exited 0: 121 root tests passed and 1 capability-dependent raw-socket test was skipped.

**Files**: `pkg/dhcprelay/relay6.go`, `pkg/dhcprelay/relay6_linklocal_10198_test.go`, `pkg/dhcprelay/README.md`.

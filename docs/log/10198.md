# Issue #10198: DHCPv6 link-local Relay-Forw fallback

- **Timestamp**: 2026-09-18T11:39Z
- **Action**: retained the first usable GUA/ULA in interface-address order and added the first usable link-local address as the RFC 8415 §19.1.1 fallback when no GUA/ULA exists.
- **Action**: permitted direct-client Relay-Forw messages to carry a link-local link-address only when Interface-ID is present; nested Relay-Forw validation remains unchanged.
- **Action**: kept GUA/ULA upstream sockets on exact-IP binds and added a manager-owned unspecified, route-selected upstream socket for link-local fallback relays. The centralized receive dispatcher routes Relay-Reply packets by Interface-ID, keeping link identity independent of the wire source while avoiding SO_REUSEPORT misdelivery.
- **Regression**: `TestSelectDHCPv6LinkAddressFallback10198` covers GUA/ULA preference, link-local fallback, and unusable-address rejection. `TestBuildRelayForwardV6LinkLocalRequiresInterfaceID10198` covers the RFC Interface-ID requirement and preserves direct GUA behavior. `TestDHCPV6LinkLocalOnlyRelaysForward10198` covers the supervisor/client path and inspects the emitted Relay-Forw link-address and Interface-ID. `TestDHCPV6UpstreamBindAddrSelection10198` deterministically pins exact GUA/ULA output and route-selected link-local/nil/unspecified fallback output. `TestDHCPv6PerInterfaceServerBindDemux10198` exercises distinct real UDP6 GUA/ULA sockets. `TestDHCPv6ExactWildcardCoexistence10198` pins exact-vs-wildcard SO_REUSEPORT delivery with 20 distinct sources. `TestDHCPv6ReplyDispatcherRoutesByInterfaceID10198` exercises a real shared UDP6 receive socket dispatching distinct replies to two Interface-IDs. `TestDHCPv6ReplyDispatcherDisambiguatesDuplicateInterfaceID10198` verifies LinkAddr selection, ambiguous same-ID/same-link rejection, and exactly one parse/empty/unknown dispatcher-drop increment each. `TestDHCPv6ReplyDispatcherStatsSyntheticRow10198` verifies aggregate dispatcher drops appear once in Manager.Stats with two relays. `TestDHCPv6ServerLoopRoutesMismatchedInterfaceID10198` covers GUA receive-path handoff. `TestDHCPv6ServerLoopRoutesSameInterfaceIDDifferentLink10198` covers same-IID mixed-path handoff selected by outer LinkAddr. `TestDHCPv6ClientReplyDispatchRateLimited10198` proves Relay-Replies consume the bounded request budget and excess traffic is rate-dropped. `TestDHCPv6ReplyDispatcherRestartsDeadServer10198` forces a one-sided shared-socket read failure and verifies reopen, existing-target writes, and reply delivery through the replacement socket. `TestDHCPv6ReplyDispatcherCancelsReopenOnRelease10198` verifies that a blocked reopen observes cancellation after the final target unregisters. The factory wiring is reading-verified; no test invokes `defaultDHCPV6ConnFactory` because that requires a live interface and privileged DHCP sockets.

## RED -> GREEN proof

- **RED**: before the implementation, `go test -count=1 -run 'TestSelectDHCPv6LinkAddressFallback10198|TestBuildRelayForwardV6LinkLocalRequiresInterfaceID10198|TestDHCPV6LinkLocalOnlyRelaysForward10198' ./pkg/dhcprelay` exited 1. The link-local-only selector returned `no usable global IPv6 link-address`; direct link-local Relay-Forw construction returned `invalid DHCPv6 Relay-Forw link-address`; and the link-local-only live-path cell observed 0 upstream writes instead of 1.
- **GREEN**: after the implementation, the complete Round-7 targeted command exited 0 (`ok github.com/psaab/xpf/pkg/dhcprelay 0.150s`); the dispatcher/coexistence race suite also exited 0 (`1.170s`). The complete `go test -count=1 ./pkg/dhcprelay` suite exited 0: 131 root tests passed and 1 capability-dependent raw-socket test was skipped.

**Files**: `pkg/dhcprelay/relay.go`, `pkg/dhcprelay/relay6.go`, `pkg/dhcprelay/relay6_linklocal_10198_test.go`, `pkg/dhcprelay/README.md`.

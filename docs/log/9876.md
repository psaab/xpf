# #9876 — the #8480 valueless-`from` gate skipped braced `family { inet { filter } }`, so a valueless prefix-list committed strict-clean as match-ANY

- **Timestamp**: 2026-09-15
  - **Action**: rewrote `validateFirewallFilterValuelessFromStrict` to descend
    `family` nodes through the shared `firewallFamilyMembers9883` extractor
    (the single dual-shape descent also used by `compileFirewall` and the
    #3884/#4296/#9017 gates: `Keys>=2` → `Keys[1]` + self, else one member
    per AF child with the #4827-safe `Name()` read), resolving filter/term
    names through `namedInstances` (the compiler's own dual-shape reader),
    skipping quarantined members (undeclared token or residue — they compile
    to NOTHING, so no terms to check), and keeping the family-less branch and
    document order. The diagnostic now prints the resolved AF token
    (`firewall family inet filter ...`), fixing the F-104 ride-along
    (`firewall family family filter ...`). #8597 stays the generic belt,
    untouched. (First landed with a dedicated `firewallAFNodes` walker;
    deleted on rebase onto #9883, whose extractor subsumes it — same
    traversal plus Residue/nil-safety, with the Keys[0] token read that
    closes the quarantine-bypass the Keys[1] preference had.)
  - **File(s)**:
    `pkg/config/compiler_firewall_valueless_from_8480.go`,
    `pkg/config/firewall_valueless_from_9876_test.go` (new).

  - **STEP-0, measured on base d3e959067**: flat
    `family inet { filter F { term T { from { source-prefix-list; } } } }`
    strict-rejects naming the leaf; the braced-nested
    `family { inet { ... } }` spelling returns gate=nil, `CompileConfig`=nil,
    and compiles `FiltersInet["F"].Terms[0]` with `Action="discard"` and empty
    `SourcePrefixLists` (match-ANY), with `SchemaValidate`=nil (args-0 leaves
    escape the `missingArgs > 0` guard at `schema_walk.go:710-711`). Braced
    `protocol;` is gate-clean but `SchemaValidate`-refused, confirming the
    residual is exactly the two prefix-list leaves. AST proof: flat parses
    `family` as `Keys=["family","inet"]` with a `filter` child; braced parses
    `Keys=["family"]` with an `inet` child, which the old gate
    (`compiler_firewall_valueless_from_8480.go:143-156`) skipped because it
    took `fam.Children` as the filter level. Flat `set` packs
    `["family","inet"]` (`family` is `compoundKey`, `schema_cos.go:514`,
    consumed at `ast_edit.go:500-506`), so the nested shape is
    braced/load-merge only.

  - **Adjacent skips closed by the same walk**: `filter { F { ... } }` and
    `term { T { ... } }` block-name shapes also compiled match-ANY while the
    gate walked past them (it read `filt.Children`/`Keys[1]` directly instead
    of `namedInstances`); both now reject. Packed `from protocol;` statements
    need no gate handling — `normalizeCompactStanzas` (#8662) expands them
    before the gate runs (verified: strict rejects through the real pipeline).

  - **Deliberately out of scope**: the `compiler_interfaces.go` /
    `compiler_services.go` family walks (different subtrees, different AF
    handling). (The `compiler_firewall_family_9017.go` token walk once
    listed here is now the shared extractor itself, via #9883.)

  - **Verification**: the six new 9876 cells failed pre-fix exactly as filed
    (braced accept/reject, flat diagnostic, block names, lenient control;
    the quarantine-skip cell reds with the skip removed — the gate warns
    #8480 for `family inett filter "Q"`, a term the compiler quarantines)
    and pass post-fix; the #8480 cells stay green. Named tests `-count=5` green.
    `go test ./pkg/config/` full suite green (197s); `go test
    ./pkg/configstore/` full suite green (36s); `go vet` and `gofmt` clean.
    End-to-end `configstore.CheckText` (throwaway, not committed) rejects the
    braced discard/accept fixtures and the flat control, each naming the leaf,
    `family inet`, and `#8480`.

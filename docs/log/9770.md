# 9770 — first ctrl-enable flush deletes steering rows the helper's live sessions still use

Worktree `9770-flush-scope`, branch `fix/9770-flush-scope`, base `origin/master 9c5c610cb`.
Lane contract: shippable commits only + this history file; plan + raw reviews uncommitted under
`docs/pr/9770/`; substance posted to the issue as comments.

## STEP-0: defect verified PRESENT at base (not ALREADY_FIXED)

- `flushStaleBPFStateOnCtrlEnableLocked` (`pkg/dataplane/userspace/helper_status_apply.go:325-343`)
  walks `userspace_sessions` with `NextKey` and deletes **every** row, no value predicate.
- Gate exactly as the issue states (`maps_sync.go:540`).
- Two corrections established during investigation (both verified in source, both confirmed by all
  review rounds): (1) the gate re-arms on **every helper departure** (`resetAfterHelperGoneLocked`,
  `process.go:437`), not only daemon startup; (2) the March-era writer is gone — the shim is
  read-only on the map, nothing under `bpf/` references it, Go only deletes — so stale rows are
  exactly previous-incarnation rows in the reused pin (`PinByName` + hitless `Close`).

## Plan evolution (hostile review, `--thinking max`, zai/glm-5.3 + openai-codex/gpt-6-astra)

- **v1** (helper first-bringup selective clear): GLM NEEDS-MINOR, Codex NEEDS-MAJOR (placement not
  quiescent — `publish_runtime` exposes the fd first; replay is not a synchronizer; fate table and
  retention/wording corrections).
- **v2** (Go spawn-time selective clear — review synthesis: quiescence provable at spawn, so
  value-`2 ⟺ stale`): GLM NEEDS-MINOR, Codex NEEDS-MAJOR (retention parity refuted — normal teardown
  reclaims worker rows; selective needs an unfittable finite reclamation policy; test-binding gaps;
  no privileged CI leg exists).
- **v3** (Go spawn-time FULL clear — the loop moved verbatim in time): GLM NEEDS-MINOR (3 doc items),
  Codex NEEDS-MAJOR (blocking finding 1: same-process disarm+re-arm *before* first enable orphans
  rows today's pending flush would remove).
- **v4** (v3 + narrowed contract + explicit-residual request): parent granted ONE targeted r4.
- **r4**: GLM MAJOR + Codex MAJOR, convergent — primary approved by both; §7.6 mechanism verified
  but `PTK` narrowing understated (ordinary same-key replay→upsert co-ownership, not alias flukes);
  §5d helper closer overstated (import-straddle hole) and not minimal (both named a Go-side
  drain+fence closer independently).
- **Parent ruling**: IMPLEMENT v5 — honest residual relabeling + Go-side drain closer + F1/F2 +
  capacity/fate split; reviewers re-verify the closer at PR review. No r5.

Issue comments posted: plan v4 full text, rounds 1–3 record, round-4 record + ruling request, v5
delta (`#issuecomment-5670812056`, `-5670814616`, `-5670952128`, `-5670996336`).

## What shipped (v5)

- `process.go`: `clearStaleSteeringRows` (extracted full NextKey/Delete loop; nil-safe;
  attempts-counted) + `drainAndClearSteeringRowsLocked` (nil-guard → sessionMu → acked drain ping →
  clear; skips on any uncertainty, never fails the primary op) + spawn call site (pre-`cmd.Start`,
  with the XSKMAP clear).
- `helper_status_apply.go`: steering half of the first-enable flush removed; conntrack half
  (keep-synced-by-`Created`, #6816 decode) byte-identical in behavior.
- `manager_status.go`: closer after acked disarm (never on arm/failure). `process_linkcycle.go`:
  closer after acked `stop_workers` (never on failure; void preserves the join-only contract).
- Tests: `steering_spawn_clear_9770_test.go` (7 tests: extracted-fn full+timing+nil; spawn call +
  healthy-reuse/refusal controls; closer ping/failure/unconfigured/nil/fence-hold; disarm call +
  3 controls; link-cycle call + failure control); `manager_ha_test.go` survival rewrite (both
  steering values survive + exact-predicate conntrack assertions on both maps + once-only witness);
  fixture corrected to the real 40-byte/`u8` ABI; 6 memlockcensus rows.

## Mutation matrix (all RED precisely, all reverted)

- M1 (spawn call deleted) → `TestSpawnClearsSteeringRows9770/exiting_child_still_clears` RED
  ("2 steering rows survived the spawn clear"); reuse/refusal controls stay green.
- M2 (steering loop restored in flush) → survival rewrite RED on the REDIRECT assertion.
- M3 (fence lock dropped) → fence-hold subtest RED, siblings green.

## Gates

- `go build ./...`: clean. `go vet` package: clean. `gofmt`: clean.
- Full `pkg/dataplane/userspace` suite unprivileged: `ok` (50.5s).
- Privileged footprint (`sudo go test -run` covering 9770 + link-cycle/disarm/session/supervisor/
  gates suites): 158 PASS, 0 FAIL, 1 SKIP (by-design root skip in 5839). All 7 9770 tests PASS with
  zero SKIP; survival rewrite PASS; `-race` on 9770 tests: PASS, no warnings.
- `TestSnapshotShapeIsPinnedToProtocolVersion8892`: PASS (must NOT fire — no wire change).
  `TestCleanupDecodesThroughTheExportedOffset6816` + sensitivity control: PASS.
  `pkg/memlockcensus`: PASS (6 new rows registered).
- 20k-row clear timing: ~0.5s (pathology bound 120s) — §8 validated, not asserted.
- Rust leg (`make test-rust`, Go-only change): EXIT 0 — release suite 5843 passed / 0 failed
  (+63-bin/8/22 minor suites green), debug frame/nat/session slice 2515 passed / 0 failed.
- FULL privileged suite / `test-memlock-guards`: SIGKILLed at ~31s in `TestFragmentNeverCreatesASession_7494`
  — PRE-EXISTING, proven by base run: pristine `9c5c610cb` (changes stashed, md5-verified restore
  `067c999378279688e6685f476efa3249`) kills identically at 31.029s; the lane's tests never ran
  pre-kill (zero "9770" in the killed log) and the kill file/paths are untouched by this change
  (frag tests use their own BPF collection; no a–f test injects the steering fixture, so all
  spawn/closer clears provably no-op'd). No kernel OOM log for these kills; ancient (23-day-old)
  OOM entries in dmesg are unrelated. Recorded here instead of the full-target green.

## Live (loss userspace cluster)

### (a) Failover baseline — DONE 2026-09-14T22:20–22:23Z (no deploy; pin-56 build as-is)

Lock hold 3 min, carrier 42→42 (no flap). `make test-failover` recorded VOID as a whole
(harness: gate verdict FAIL but exe_check UNAVAILABLE — unattributable; the 20/2 split
below is therefore NOT attributable evidence either). For the record the run showed
20 passed / 2 failed with both fails the known #8297 proxy-ARP shape also seen by sibling
lanes on the same build, plus KNOWN #8341. HONEST SCOPE (PR-review GPT-4): this run did
not exercise the branch build (no code of this lane was deployed — preflight wall), so it
establishes only that the shared cluster was healthy around this lane's work, not branch
validation of any kind. Raw: lane `/tmp/9770-failover.log` (+ harness ledger row VOID).

### (b) Helper-kill exercise — DONE 2026-09-15T03:44–03:58Z (fw1, branch build deployed)

Deployed branch to fw1 only (`make cluster-deploy NODE=1`, rolling cutover clean,
`working-15136-gbc13d3817`, preflight green on pin-56 master). fw1 secondary, all RGs
node0; iperf + ping churn running for mirror traffic. Three kills observed:

- Poison-absence (×4 incl. take-1): nonzero-pad sentinels (values 1+2, unpublishable by
  construction) planted pre-kill; ABSENT in every post-respawn sample including ctrl==0
  samples → spawn clear deleted them pre-enable. Take-3 timeline: 127 stale + 2 sentinels
  intact with dead helper at ctrl==00 → 0 rows at first live-helper ctrl==00 sample.
- Survival (take-5): INCONCLUSIVE as same-key continuity (PR-review GPT-2, sustained).
  Observed (real): realistic pad-0 rows R1/R2 planted on every ctrl==00 sample post-respawn;
  rows=2 at ctrl==00 (×2 samples); R1+R2 present in every dump from the flip second through
  +13s of ctrl==01. The gap: plant exit codes were unrecorded and timestamps are 1s-granular,
  so plant-completion vs actual-flip order is unchecked within ~1s — R1/R2 could have landed
  post-flip with early imports accounting for rows=2. A clean re-run (seeded + verified
  pre-disable, ceased through enable) was deferred as disproportionate: it needs a new lock
  cell + redeploy for a timing-fragile observation whose mechanism is already proven
  deterministically (unit cells drive both values through the real enable function with
  revert-sensitive assertions; static removal leaves no steering mutation on the enable path).
  What the live run DOES establish: spawn-clear pre-enable (×4, no interval issue),
  monotone post-enable regrowth with no drop at any of 4 flips, and R1/R2 persistence
  post-enable. R1/R2 are operator-planted (helper-shaped); the old deletion was
  provenance-blind so the boundary tested is the right one, but the continuity inference
  itself is not proven live.
- Trigger exclusion: sole lock holder (no peer broadcasts in window); no config changes;
  carrier 42 stable; journal shows only per-respawn first-applies (generations 24/31/42/47)
  + one transient HA-inventory arm refusal; no RG events, no failover, no rebind/lo0 changes.
Raw: `/tmp/9770-b{,2,3,4,5}-timeline.log`, bracketing dumps, pre-kill dumps, journal window.

### (c) Closer exercise — DONE same hold (fw1)

`cli -c 'request chassis cluster data-plane userspace forwarding disarm'` acked
(Forwarding armed: false). Daemon log, same second: `reclaimed orphaned steering rows
after full stop, reason=disarm, deleted=158` — HONEST READING (PR-review GPT-4): 158 is
deletion ATTEMPTS (the loop discards per-row errors), not verified reclamation; no
empty-map observation exists because config-driven re-arm beat the dump. What stands:
the closer ran at the right boundary (log ordered after the acked disarm, before the
re-arm RPC in the same second), attempted every pre-disarm row (155 counted + churn
growth), and post-re-arm state repopulated fresh (45 → 109 rows), 6/6 queues + 18/18
bindings armed, ping verified. Per-row verified deletion is covered by unit cells
(exact-count assertions on real maps), not by this live observation. Carrier 42→42;
lock released; fw1 left on branch build (Go-only delta), fw0 untouched.

## Commits

- `2f670a75e` userspace: clear stale steering rows at helper spawn (#9770) — the fix
  (spawn move + drain closer + 2 call sites + flush steering-loop removal + 7 test cells +
  survival rewrite + real-ABI fixture + 6 memlockcensus rows). Originally `a32e49864`,
  rebased onto post-#10032 master (clean, zero conflicts) per parent order.
- `bc13d3817` docs checkpoint (parent-ordered stand-down record; superseded narrative kept
  as history).
- (this finalization commits next, then PR.)

## Resume 2026-09-15 (#10032 merged as 18f5f9a78; tip 2de0b2287 at rebase)

Rebased (clean) onto current master, rebuilt, re-verified: build/vet/fmt clean, unpriv
userspace pkg ok (52.9s), privileged footprint 158 pass/0 fail (1 by-design root skip),
Rust green (5876+2515, 0 fail), 8892/6816/census green. #10032 impact assessed: ctrl
40→56 only, sessions ABI untouched, fixtures size-adaptive (proven green). Force-pushed
with parent GO; re-entered queue; ran (b)(c) above. PR next.
## Checkpoint 2026-09-14 (parent stand-down; lane HOLDING)

WHAT'S DONE: v5 plan (4 hostile rounds + parent-ruled r4, posted to #9770 with verdicts);
fix implemented, committed (`a32e49864`), pushed; all gates green — full userspace pkg
unpriv (50.5s ok), privileged footprint 158 pass/0 fail (7 9770 cells PASS, race-clean),
M1/M2/M3 mutations red precisely, Rust green (5843+2515, 0 fail), 8892/6816/census green;
live (a) failover baseline done 22:20–22:23Z (20 pass, 2× known #8297, VOID harness,
carrier 42→42, lock released, no state changed). Full-sudo-suite SIGKILL proven
pre-existing (base kills identically; lane tests never ran pre-kill).

WHAT'S NEXT (on #10032-merge news): rebase branch onto new master (history rewrite —
needs parent GO + force-with-lease per lane rules), rebuild, re-verify suites green
(fixture ABI + preflight behavior may shift with pin-56 master!), message parent to
re-enter cluster queue for (b) helper-kill 4-part + (c) disarm→dump-empty→re-arm with
rebased build deployed, record results here, amend-or-append log commit, open PR
(`Closes #9770`, honest test table incl. pre-existing-gate notes), add Copilot, STOP.

WHAT I'M WAITING ON: parent broadcast of #10032 merge. Branch HEAD at checkpoint: see
`git log` (fix commit + this log commit, both pushed). Worktree preserved; plan v5 +
raw reviews uncommitted under `docs/pr/9770/`; no code changes pending.

## PR-review fold (parent-adjudicated GPT NEEDS-MAJOR + GLM READY)

- GPT-1 (3 disarm emitters bypass the closer): fenced all three (pre-publish E1,
  desired-state E2 disarm arm, protocol-failure E3) + gated the closer on never-enabled
  (`!initialCtrlCleanupDone`): pre-enable disarms reclaim (today's pending-flush coverage),
  post-enable disarms keep rows (failback recovery need — deleting would trade today's
  lingering-row recovery for permanent kernel bypass on dead-session host-bound flows;
  exact parity since today's consumed gate covers post-enable no better). Per-emitter
  cells: reclaim-pre-enable + keep-post-enable + re-arm-survival (+ E2 arm-never-clears);
  existing disarm/link-cycle tests gained post-enable-keep subtests; closer gained a
  gate subtest (no ping when set). M4 (gate flipped) reds every reclaim/keep pair precisely.
- GPT-2 (R1/R2 unchecked interval): LABELED INCONCLUSIVE (log (b) + PR body). Plant exit
  codes unrecorded + 1s timestamps leave plant-vs-flip order unchecked within ~1s; clean
  re-run deferred as disproportionate (new lock cell + redeploy for timing-fragile
  observation; mechanism proven deterministically by unit cells + static removal).
- GPT-3 (link-cycle lease/goroutine leak): setup now `t.Cleanup(m.releaseLinkCycleLease)`
  per #6871-F1; race ×3 clean.
- GPT-4 (honesty): 158 = deletion attempts (loop discards errors), not verified reclamation;
  failover run VOID as a whole + never exercised the branch build (shared-cluster health
  only). Both corrected in log + PR body.
- Gates re-run post-fold: unpriv pkg ok, privileged footprint 167 pass/0 fail/1 by-design
  skip, Rust green, census/8892/6816 green. Commits below; PR #10061 body updated.

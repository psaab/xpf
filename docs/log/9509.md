# #9509 — becomeMaster discards its rollback error and publishes BACKUP

## 2026-09-10 — Action: re-verify at base (origin/master 48a5177be); establish the node's state

- **File(s):** `docs/log/9509.md` (new),
  `pkg/vrrp/becomemaster_rollback_9509_test.go` (new, acceptance first).
- **The spec.** The issue body; it has no comments. The coordinator asked for the
  node's exact resulting state to be established before rating severity.
- **The harness.** A fake kernel behind the existing seams (`linkByNameFn`,
  `addrAddFn`, `addrDelFn`). Two VIPs; adding VIP 2 fails. Deleting an absent
  address returns `EADDRNOTAVAIL`, as the kernel does, and `removeVIPsLocked`
  treats that as benign.
- **The node's state after the compound fault** (VIP 1 adds, VIP 2 fails, the
  rollback delete fails), all asserted as FIXTURES and all held at base:
  - `becomeMaster` returned false;
  - state is `StateBackup`, and a BACKUP event was published;
  - **VIP 1 is still on the interface.**
- **RED at base, with the intended messages:**
  - `TestBecomeMasterRollbackFailureIsSurfacedAndReconciled_9509`: "becomeMaster
    discarded its rollback failure: vipRemoveFailures = 0, want 1. The node
    published BACKUP while still holding 10.0.61.1/24".
  - `TestStrandedVIPIsRetriedAfterAFailedPromotion_9509`: "after the failed
    promotion no delete was ever attempted again (1 calls). 10.0.61.1/24 stays on
    the BACKUP node for the life of the process". The delete-call count stayed at
    the rollback's one call across seven retry windows after the fault cleared.
    That is permanence MEASURED, which the issue had argued.
- **GREEN at base.**
  - `TestBecomeMasterCleanRollbackFlagsNothing_9509`, the control.
  - `TestRepromotionDuringReconcileKeepsItsVIPs_9509`, a guard for the fix; at base
    no reconcile exists to fence.
  - The #5082 and #5482 neighbours: 7 cells.
- **Severity, as measured.** A node that believes it is BACKUP while holding a VIP
  the peer owns, with no recovery. That is split ARP/ND ownership, the condition
  chassis cluster exists to prevent. The trigger is a compound netlink fault, not
  an attacker path, so the issue's Medium (consequence High × trigger probability)
  stands. The retention it called permanent is now measured, not inferred.

## 2026-09-10 — Action: adjudicate the second site from a census, not by argument

- **All state writes.** `vi.state =` appears once outside tests, inside `setState`,
  so every tenure change bumps `ownerGen` through it.
- **Non-test `setState` callers:**

  | Caller | Can it supersede a `reconcileVIP` holding `vipMu`? |
  |---|---|
  | `becomeMaster` `setState(StateMaster)` | no: `reconcileVIP` acts only while already Master, so this is a no-op, with no bump |
  | `becomeMaster` failure arm `setState(StateBackup)` | no: it runs while holding `vipMu` |
  | run-startup `setState(StateBackup)` (`instance.go`) | no: before the loop; and it removes all VIPs, then surfaces |
  | `becomeBackup` `setState(StateBackup)` | **yes, the only one.** It then blocks on `vipMu`, removes ALL configured VIPs, and surfaces the result |

  Shutdown (`stopCh` while Master) removes VIPs and logs, but never calls
  `setState`, so it cannot reach the superseded arm.
- **Conclusion: one call site.** A failing rollback in `reconcileVIP`'s superseded
  arm is always followed by `becomeBackup`'s full, surfaced removal.
  `TestSupersededReconcileRollbackIsCoveredByBecomeBackup_9509` pins that, with a
  real `becomeBackup` racing a real `reconcileVIP` and every delete failing. The
  site's false comment is still corrected, and its rollback error is now logged.
- **One refinement to the issue's fix direction.** Surface UNCONDITIONALLY, as
  `becomeBackup` does.
  - An earlier divergence's reconcile is superseded by this promotion attempt's
    `setState(StateMaster)`, and exits without clearing `vipDiverged`.
  - A clean rollback has removed every present VIP, because an already-present
    address adds as EEXIST, counts as applied and is rolled back.
  - So clearing the flag is accurate, while leaving it true would cry wolf for the
    process's life.
  - `TestCleanRollbackClearsAStaleDivergenceFlag_9509` pins it, and should be RED at
    base.

## 2026-09-10 — Action: two more cells at base, then the fix

- **File(s):** `pkg/vrrp/becomemaster_rollback_9509_test.go`,
  `pkg/vrrp/instance_transition.go`, `pkg/vrrp/manager.go`.
- **Base run with all six #9509 cells.** 13 executed, 3 failed, each on its #9509
  assertion (the FIXTURES held):
  - `...SurfacedAndReconciled_9509`: `vipRemoveFailures = 0, want 1`.
  - `...RetriedAfterAFailedPromotion_9509`: no delete ever attempted again.
  - `TestCleanRollbackClearsAStaleDivergenceFlag_9509`: "a clean rollback that left
    no VIP on the BACKUP node must clear vipDiverged".
  - GREEN at base: the control; the re-promotion guard;
    `TestSupersededReconcileRollbackIsCoveredByBecomeBackup_9509`; and all 7
    #5082/#5482 neighbours.
  - The adjudication cell being GREEN at base confirms the census by measurement:
    the second site needs no surfacing call of its own.
- **The fix, `becomeMaster`'s fail-closed arm.**
  - `rollbackErr := vi.removeVIPsLocked(res.applied)`, then
    `setState(StateBackup)`, then release `vipMu`.
  - The Error log gains `rollback_err`.
  - Then `vi.surfaceStaleVIP(rollbackErr, "becomeMaster-rollback")`, called
    unconditionally, before `emitEvent`.
  - The order matters. The reconcile captures `ownerGen` when it is scheduled, so
    BACKUP must already be set, or it would fence itself out as superseded. And it
    takes `vipMu`, so the lock must already be released.
  - The false comment, which said the rollback failure "is reported by the Error log
    (applied/failed lists)", is replaced. Those lists describe the ADD.
- **The fix, `reconcileVIP`'s superseded arm.** It stays log-only, for the reason
  the census established.
  - The rollback error is captured and added to the Warn (`rollback_err`).
  - The false comment ("the Warn below reports the rolled-back set") is replaced
    with the coverage argument, and it names the cell that pins it.
- **After the fix:** 13 executed, 0 failed. gofmt clean; `go vet ./pkg/vrrp/` clean.

## 2026-09-10 — Action: docs, and the mutation script

- **File(s):** `pkg/vrrp/README.md`, `docs/critical-patterns.md`,
  `scripts/mutate-9509.py` (new), `docs/log/9509.md`.
- **Docs.** Both living docs that describe VRRP VIP ownership now state the #9509
  behaviour: the MASTER-side rollback is surfaced, why it is ordered after
  `setState(StateBackup)` and after releasing `vipMu`, why it is unconditional, and
  why `ReconcileVIPs`' superseded rollback stays log-only.
  - The README's "Verified role publication" bullet gains a sub-bullet naming the
    adjudication cell.
  - The critical-patterns "Fail-closed VIP ownership (#5082)" bullet gains a
    paragraph.
  - Every other hit for #5482/#5082 was a historical review or the closed
    `_Log.md`, none of them a living doc.
- **Mutation script.** `scripts/mutate-9509.py` is Go-only. It runs the whole
  `pkg/vrrp` package per run, bounded, with at least 50 tests collected and verdicts
  scored by name.

  | Mutant | Change | Must be killed by |
  |---|---|---|
  | V1 | the rollback failure is not surfaced | the acceptance and the permanence cell |
  | V2 | surfaced only on error | the stale-flag cell |
  | V3 | `nil` is surfaced instead of the error | the acceptance |
  | V4 | the call moves before `setState(StateBackup)` | the acceptance and the permanence cell, because the reconcile captures the pre-bump generation and fences itself out |

  All anchors count exactly 1, including both edits of the V4 move.

## 2026-09-10 — Action: mutation matrix on 71e472713

- **File(s):** `docs/log/9509.md` (results only).
- **Run.** Launched with `python3 -u` and watched by a Monitor on its log, so every
  verdict was visible as it landed. The baseline was clean: `go: SURVIVED` over the
  whole `pkg/vrrp` package, with at least 50 tests collected.
- **Result: 4 of 4 KILLED, 0 SURVIVED, 0 VOID.** `matrix rc=0`, and the tree was
  clean after (0 paths). Killers by name:
  - V1 (not surfaced):
    `TestBecomeMasterRollbackFailureIsSurfacedAndReconciled_9509`,
    `TestStrandedVIPIsRetriedAfterAFailedPromotion_9509`.
  - V2 (surfaced only on error): `TestCleanRollbackClearsAStaleDivergenceFlag_9509`.
  - V3 (nil surfaced): `TestBecomeMasterRollbackFailureIsSurfacedAndReconciled_9509`.
  - V4 (surfaced before `setState(StateBackup)`): both cells named for V1. The
    reconcile captured the pre-bump generation, fenced itself out, and never
    removed the VIP, which is the ordering argument measured.
- **No survivor.** Nothing in this change is uncovered, and no mutant needed an
  equivalence argument.

## 2026-09-10 — Action: make test refused a modularity crossing; `reconcileVIP` moves to `instance_vip.go`

- **File(s):** `pkg/vrrp/manager.go`, `pkg/vrrp/instance_vip.go`, `docs/log/9509.md`.
- **The refusal.** `make test` on 9ababfe3d failed in its Go leg:
  `TestTouchedFileCrossedModularityThreshold` reported "pkg/vrrp/manager.go 1499 ->
  1505 LOC, crossed the 1500 LOC [WATCH] floor".
  - The file sat one line under the floor, and the six lines were this change's
    rewritten comment and logged rollback error in `reconcileVIP`.
  - The Go leg's failure stopped `make test`, so its Rust leg did not run.
- **Remedy: a split, not an acceptance, and not trimmed comments to fit.**
  - `reconcileVIP` is a `vrrpInstance` VIP-actuation method that lived in the
    manager's file. It moves, with its doc comment, to `instance_vip.go`, next to
    `addVIPsLocked`, `removeVIPsLocked` and `removeVIPsIfBackup`, which its own
    comments reference.
  - Pure code motion: 55 lines. `manager.go` goes 1505 -> 1449, and
    `instance_vip.go` 302 -> 358.
  - `scripts/refactoring-audit-touched.sh` shows no crossing. `go build`, `go vet`
    and gofmt are clean, and all of `pkg/vrrp` passes, including the adjudication
    cell that calls the moved function. The refactoraudit test passes.
  - No living doc cited the function's file.
- **The matrix still stands.** All four mutants edit `instance_transition.go`, which
  the move does not touch.

## 2026-09-10 — Action: the first cluster gate aborted at CoS on kea's start limit; measured not to be this change

- **File(s):** `docs/log/9509.md` (results only).
- **What happened.** The gate on d035dd83a (lock at 21:05:29Z) deployed cleanly. Its CoS
  commit then ran at xpfd uptime 4 s and failed:
  - the error was `restart kea-dhcp4-server: ... start of the service was attempted too often`;
  - `apply-cos-config.sh` rolled back (verified), and the gate aborted before
    `make test-failover`;
  - I restored CoS under the lock at 21:11:25Z, verified by its transcript.
- **Measured on fw0's journal:**
  - **This deploy's xpfd (pid 1337)** took all three RGs at startup (21:07:14). It logged
    `vrrp: DHCP server apply enqueued (MASTER)` five times in about 200 ms (rg 2 once,
    rg 0 twice, rg 1 twice). systemd restarted kea four times in about 1 s, then hit
    `start-limit-hit` at 21:07:19.
  - **Every other deploy-started xpfd on fw0 that day** logged 0 to 2 enqueues and no
    start-limit hit. That includes the #9582 deploy at 20:50:06Z, which also took all
    three RGs at startup (2 enqueues).
- **Why it is not this change:**
  - This change's one behavioural edit is the `becomeMaster` VIP-rollback branch, and it
    did not run: there was no `VIP actuation failed` line.
  - The `reconcileVIP` move is behaviour-identical. Diffed old against new, the only
    difference is that the rollback error is now captured for the Warn log.
  - Master between the two compared builds' bases (48a5177be..d1266ee79) changed nothing
    under `pkg/daemon`, `pkg/vrrp`, `pkg/cluster` or `pkg/dhcpserver`.
- **A real product defect along the way, filed as #9601.** After the start limit, the one
  converger retry at 21:07:20 failed inside the same window. No further retry followed for
  4 minutes, and kea stayed failed on the primary until the next commit.
- **Gate hardening, outside the repository.** The lane's gate retries its CoS step once,
  after 20 s, only when the transcript names the kea start limit. Any other CoS failure
  still aborts.

## 2026-09-10 — Action: rebased twice for the landing rule

- **File(s):** none in content; the history moved only.
- d035dd83a -> b3e991ee5 onto fe86a51c8, then -> 456250be0 onto 3ae82aa49. #9526's
  `session_glue` and `worker` changes are HA paths, so the gate must run on a current base.
- Both rebases were clean. The delta from the tip is exactly this change's eight files, and
  `go build ./...` passes.
- No mutated file (`pkg/vrrp/instance_transition.go`) changed in master's delta, so the
  4-of-4 matrix stands.

## 2026-09-10 — Action: make test on 456250be0

- **Timestamp**: 2026-09-10T21:30Z to 21:43Z
- **Result, counted from the log:** rc 0; 77 Go packages `ok`, 0 `FAIL` lines, 0 panics;
  11 Rust `test result:` lines, all with 0 failed.

## 2026-09-10 — Action: cluster gate at 853dd82d1; which #9526 case applied

- **File(s):** `docs/log/9509.md` (results only), `test/results/ledger.d/885bb08310ef2c86.json` (the run's ledger,
  committed by convention).
- **Base.** Rebased onto origin/master 0c2db588f (#9507 landed, a `pkg/cluster` HA path),
  giving 853dd82d1. The delta from the tip is exactly this change's eight files.
  `go vet ./...` on this head: rc 0, 0 diagnostic lines (the #9590 branch sweep).
- **Gate, in one lock hold** (21:48:53Z on): `make cluster-deploy` rc 0; CoS re-applied
  and verified on the first attempt; `make test-failover`: 22 passed, 0 failed, rc 0 (ledger `885bb08310ef2c86`: verdict PASS, `build_git_sha` the gated head). Restore:
  fw0 was already primary for every RG (rc 0).
- **#9526, merged without its mandatory failover gate. Which case applied:**
  - **A dedicated post-merge gate ran first.** It covered origin/master 3ae82aa49 from a
    clean worktree: 22 passed, 0 failed, recorded on #9526 at 21:29:43Z.
    `git merge-base --is-ancestor 042effebb 3ae82aa49` holds (042effebb is #9526's merge
    commit).
  - **This gate is a second build carrying #9526:**
    `git merge-base --is-ancestor 042effebb 853dd82d1` also holds, so this
    `make test-failover` ran #9526's purge-on-rotation code during failover as well.
  - Result for #9526: green in both builds that contain it; nothing to attribute. The follow-up on #9526 records this build.

## 2026-09-10 — Action: landing-rule verdict (recorded after the check)

- **File(s):** `docs/log/9509.md`. This amend changes only this log, so the verdict still binds the code.
- **Verdict, verbatim:**

```
T_test=3ae82aa49 T_cluster=0c2db588f HEAD=ce450d63f origin/master=0c2db588f matrix_head=71e472713
master_delta (T_test..origin/master): 12 files; mine (own change, from merge-base 0c2db588f): 9 files
mutated files changed since the matrix head: none
C1 intersection (since T_test): empty
C2 HA-path files changed since the cluster gate: none
C3: merged tree must build with no NEW diagnostics (differential against master's vet output)
master alone: go vet rc=0 (0 output lines), cargo check rc=0
merged: go build rc=0; NEW vet diagnostics: none; cargo check rc=0; NEW cargo errors: none
Rust control: injected break on the merged tree: cargo rc=101 (must be non-0), error names the symbol: 2 -> PASS
Go control: build rc=0 (must be 0); NEW vet diagnostics: 1, in the control file: 1 -> PASS
merge-check worktree and target removed: yes
DECISION: LAND by merge commit without re-gating (C1, C2, C3 hold; both controls refused as they must)
```

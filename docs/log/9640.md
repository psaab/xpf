# #9640: manual failover in effect at peer timeout elects before the confirmed fence

- **Timestamp**: 2026-09-11T02:56Z
- **Action**: created worktree `/var/tmp/wt-9640-fence-order` on origin/master a21c23ac7
  (branch `fix/9640-manual-failover-fence-order`).
- **Action**: read `handlePeerTimeout` (heartbeat_manager.go), `recalcWeight` and
  `electSingleNode` (election.go), `awaitPeerFenceLocked` (fence_confirm_7147.go) and
  `TestConfirmedFenceRunsBeforeElection7147` on master. The issue's mechanism holds:
  - the peer-loss loop clears each `ManualFailover` and calls `recalcWeight`;
  - `recalcWeight` ends in `electSingleNode`, because `peerAlive` is already false;
  - so the election runs BEFORE `awaitPeerFenceLocked`;
  - `electSingleNode`'s #7161 readiness gate is armed only when
    `peerAlive || !peerEverSeen`, so it does not hold the promotion.
- **Action**: probe cell
  `TestManualFailoverAtPeerTimeoutElectsOnlyAfterTheConfirmedFence9640`, which drives the
  real `ManualFailover` API on the #7147 fixture and samples RG0's state inside the
  fence-confirm callback.
  - **File(s)**: `pkg/cluster/fence_manual_failover_9640_test.go`
- **Timestamp**: 2026-09-11T03:07Z
- **Action**: probe RED on unmodified master a21c23ac7: "clearing the manual failover on
  peer loss elected this node primary BEFORE the fence was issued".
- **Action**: measured master's behaviour with a temporary capture cell, before any fix
  and never committed. With a manual failover on RG0 at peer timeout, for policy "",
  disable-rg and disable-rg-confirmed alike:
  - the manual failover emits `[rg0:secondary->secondary-hold]`;
  - peer loss emits `[rg0:secondary-hold->primary]`;
  - the end state is primary, manual failover false, weight 255.
- **Action**: fix. `recalcWeight` is split into `recomputeWeightLocked` (weight
  only) plus the election. `handlePeerTimeout`'s manual-failover-clearing loop uses
  `recomputeWeightLocked`, so the only election after peer loss is the existing
  `electSingleNode` after `awaitPeerFenceLocked`.
  - **File(s)**: `pkg/cluster/election.go`, `pkg/cluster/heartbeat_manager.go`
- **Action**: cells.
  - The probe (RG0 not primary inside the fence wait, primary after, manual cleared).
  - A two-group sibling (a manual failover on RG0 must not promote RG1 before the fence
    either; electSingleNode elects every group).
  - `TestPeerLossWithManualFailoverKeepsTodaysOutcome9640`, pinning every policy to the
    measured master events and end state.
  - Result: pkg/cluster (9640/7147/Fence/PeerTimeout/ManualFailover) 90 passed, 0 failed.
  - The capture cell was deleted.
  - **File(s)**: `pkg/cluster/fence_manual_failover_9640_test.go`, `pkg/cluster/README.md`
- **Timestamp**: 2026-09-11T03:11Z
- **Action**: mutation matrix on 829bc025c (`go test -json`, scored by name plus message):
  3/3 KILLED; tree restored clean.
  - F1: the loop elects via recalcWeight again.
  - F2: the weight helper elects.
  - F3: the manual failover is not cleared.
- **Action**: CORRECTION before the PR. `manualFailoverRestoreWeightLocked` (failover.go)
  already exists for exactly this purpose. It restores the monitor-derived weight after
  a manual failover is cleared, and it deliberately does not elect; `electRG` uses it
  twice. The new `recomputeWeightLocked` duplicated it.
  - The fix now reuses it. `election.go` is back to master byte-for-byte, the peer-loss
    loop calls `manualFailoverRestoreWeightLocked`, and the helper's doc comment names
    the new caller and why an election there would precede the fence.
  - README bullet updated.
  - pkg/cluster (9640/7147/Fence/PeerTimeout/ManualFailover/DualResign/TransferOut)
    all passed.
  - **File(s)**: `pkg/cluster/election.go` (reverted), `pkg/cluster/heartbeat_manager.go`,
    `pkg/cluster/failover.go`, `pkg/cluster/README.md`

## Gates on b0248f525

- **Mutation matrix** (`go test -json`, scored by test NAME plus message): 3/3 KILLED
  (F1 KILLED, F2 KILLED, F3 KILLED). The tree was restored clean.
- **make test** on b0248f525 (own worktree): exit 0. Go leg 77 packages ok, no FAIL. Rust leg:
  11 `test result:` lines, 5821 passed, 0 failed. No disk-full.
- **make test-failover** on local merge `f0f727374` (origin/master `9e63a98e3` + #9640
  `b0248f525`), deployed under the cluster lock with CoS re-applied: 22 passed /
  0 failed, iperf3 **20.7000 Gbps**. Ledger row `eb0c70cee58a09f5.json`: verdict PASS,
  exe_check MATCH, build_git_sha = the merge.

- **Timestamp**: 2026-09-11T08:55Z
- **Action**: landing gate on current master.
  - Local merge `511d0053b` = origin/master `7c494b5ca` + PR #9673 `ed335688a` + PR #9675
    `433a4623f`.
  - Why this run: the earlier cluster PASS was on merge 4ad6fb1bb (base 2c29dc14f). Master then
    took #9713 and #9732, which change routing-instance compile and learned-route import. The HA
    config exercises both.
  - Merged tree: build rc 0, and go vet identical to master's (land-check.sh).
  - `make test` on `81a2a342e` (origin/master 0e950bd5b + both PRs): exit 0.
    - Go: 77 packages ok, 0 FAIL.
    - Rust: 5829 passed, 0 failed.
    - The later master delta to `7c494b5ca` touches none of this branch's files.
  - Deployed under the cluster lock, with CoS re-applied.
    - The post-deploy reassert verified node0 primary for every RG.
    - `make test-failover`: 22 passed, 0 failed; iperf3
      20.7000 Gbps.
    - Ledger row `c57a55bc9b8ff7d0.json` (committed on the #9641 branch): verdict PASS, exe_check MATCH,
      build_git_sha = the merge.

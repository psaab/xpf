# #9584 — duplicate rule identity refused by the helper, unmirrored in Go

- **Timestamp**: 2026-09-10
  - **Action**: Filed from #9571 research, then re-verified at origin/master
    `d7912ae72` with a throwaway probe (removed). Channel
    `CompileConfigLenient`: two security roots, two stanzas for one pair, and
    two global blocks each gave 0 mirror reasons and a normal
    `policymatch.Match` verdict.
  - **File(s)**: none (probe deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Added the rule-identity arm to the content-rejection mirror,
    in the helper's order (identity, then non-zero non-sentinel policy_id).
    Added config-driven, control, hand-built and simulator cells, and a shared
    wire fixture generated by the Go builder that a Rust cell asserts the helper
    refuses. Docs.
  - **File(s)**: pkg/dataplane/userspace/policies_reject_identity_9584.go,
    pkg/dataplane/userspace/policies_reject.go,
    pkg/dataplane/userspace/policies_reject_identity_9584_test.go,
    pkg/policymatch/duplicate_rule_identity_9584_test.go,
    testdata/policy_duplicate_rule_id_9584.json,
    userspace-dp/src/policy_tests.rs, pkg/policymatch/README.md
- **Timestamp**: 2026-09-10
  - **Action**: Landing decision under the coordinator's tested-base rule.
    T = d7912ae72, the base the green `make test` ran on. master_delta =
    `git diff --name-only T..origin/master` = empty (origin/master == T at merge time); mine =
    `git diff --name-only T..HEAD`; intersection = empty. Rule applied:
    empty intersection, landed with --merge as-is, no rebase. The eight mutants mutated only
    pkg/dataplane/userspace/policies_reject.go and the new
    policies_reject_identity_9584.go; neither changed on master, so no mutant
    was re-run. This change touches no config.Config type and no snapshot
    wire field, so the #8892 snapshot golden cannot move. The accepted
    residual is a whole-tree content scanner on the combined tree, which the
    coordinator's post-merge `make test` on master covers.
  - **File(s)**: docs/log/9584.md

# #9618 — the boot-id reboot classifier owes the replacement a cold prime

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified muse-spark-009 DBK-001 at `81efb7aad` with a test-only probe: after a boot-id-first reboot (replacement on the empty alternate slot, BulkStart with a new boot id) the incarnation advanced and the corpse was evicted, but `needColdPrime=false` with `outboundBulkAcked=true`, so no re-prime was owed. The epoch-first control (#9174 V014) armed. Filed #9618 from #8865.
  - **File(s)**: none (probe not committed)
- **Timestamp**: 2026-09-10
  - **Action**: `applyPeerIncarnationSwitchLocked` arms `needColdPrime` when it retires an incarnation on boot-id evidence.
  - **File(s)**: `pkg/cluster/sync_conn.go`
- **Timestamp**: 2026-09-10
  - **Action**: Added the boot-id-first arm cell (with attribution guards that the empty-slot install did not arm and that the switch ran), plus the first-incarnated-prime and same-boot-second-fabric negative controls.
  - **File(s)**: `pkg/cluster/sync_bootid_reboot_coldprime_9618_test.go`
- **Timestamp**: 2026-09-10
  - **Action**: Documented the second arm beside #9174 V014.
  - **File(s)**: `pkg/cluster/README.md`, `docs/log/9618.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex hostile review round 1 returned MERGE NO. Applied: the boot-id switch now also dispatches `OnPeerConnected` (parity with the epoch-first order, which reaches it through `installConn`'s cold-prime arm); the end-to-end cell now carries an established session and asserts it reaches the replacement and that `OnPeerConnected` fired; the same-boot control asserts no dispatch; README states what is not closed. Codex's three consumer-side findings (discharge on local write rather than `BulkAck`, an unversioned latch an older bulk can clear, export of survivor-primary RGs only) are shared by every `needColdPrime` arm and filed as #9626. Its false-arm finding (a delayed dead-incarnation BulkStart) costs one redundant bulk here; the underlying namespace hazard is pre-existing and documented.
  - **File(s)**: `pkg/cluster/sync_conn_read.go`, `pkg/cluster/sync_bootid_reboot_coldprime_9618_test.go`, `pkg/cluster/README.md`, `docs/log/9618.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 2 found `OnPeerConnected` fired twice when both classifiers saw one reboot (epoch first, then boot id), which is harmful: the callback bumps the daemon's sync connection epoch and can race the readiness timer. The boot-id arm and dispatch are now gated on the switch having evicted the corpse (the first classifier to see the reboot). A synchronous `peerConnectedDispatches` counter replaces the sleep-based negative assertion Codex also flagged, and a new cell covers epoch-first then boot-id.
  - **File(s)**: `pkg/cluster/sync_conn.go`, `pkg/cluster/sync_conn_read.go`, `pkg/cluster/sync.go`, `pkg/cluster/sync_bootid_reboot_coldprime_9618_test.go`, `pkg/cluster/README.md`, `docs/log/9618.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 3 refuted the eviction gate with a false negative (the corpse leaves on its own before BulkStart, so the switch evicts nothing and owes nothing) and a false positive (a healthy second fabric installed between the epoch classification and BulkStart is evicted as a corpse; confirmed by execution on master). The dedup cell was also vacuous: after `handleNewConnection` it passed the raw capture conn, which `fabricIdxForConnLocked` never matches, so the switch never ran. Narrowed the change back to its core: the switch arms whenever it applies, no `OnPeerConnected` dispatch, no eviction gate, no dispatch counter. Added the corpse-already-left cell. Filed #9636 for reconciling the two classifiers (dispatch parity, sibling eviction, per-reboot identity).
  - **File(s)**: `pkg/cluster/sync_conn.go`, `pkg/cluster/sync_conn_read.go`, `pkg/cluster/sync.go`, `pkg/cluster/sync_bootid_reboot_coldprime_9618_test.go`, `pkg/cluster/README.md`, `docs/log/9618.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 4 returned MERGE YES with two MINORs, both applied: the README now says "at most one" redundant bulk, and the test cells use a drained pipe so the BulkAck write no longer waits out a 2 s timeout against an unread peer. Round-4 mutation matrix: all seven mutants killed (including the eviction-gate mutant, by the corpse-already-left cell).
  - **File(s)**: `pkg/cluster/sync_bootid_reboot_coldprime_9618_test.go`, `pkg/cluster/README.md`, `docs/log/9618.md`

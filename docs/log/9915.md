# #9915 — cluster/HA sync + event-stream guard cohort (9 members)

- **Base**: `609433efb` (`fix/9915-sync-guards`, worktree `9915-sync-guards`).
- **Issue quirk**: title says "eight", table lists **9 claims** (F-043/044/046/116/117/118/123/124/125);
  table treated as exhaustive (no exclusion in history). Pin-to-base drift: nil for member mechanisms.
- **Method**: STEP-0 named RED cells per member (kept as regression coverage) →
  2 hostile plan reviews + adjudication → implementation → GREEN + full suites
  (`-count=1`) + named 5x + `gofmt`/`go vet`. No `git stash`; no test weakening
  (only adjudicated/named contract updates, each justified).

## Per-member evidence (RED → GREEN)

| Member | RED on base | Fix (files) | GREEN |
|---|---|---|---|
| F-043 node-id bricks keyed sync | `newSyncNoiseState` succeeded for node 2/5/-1/256 (4 errors) | `sync_auth_noise_7163.go:syncNoiseIdentity` rejects ∉{0,1} loud (names ids → `/etc/xpf/node-id`); posture comment in `sync_auth_upgrade.go` (log-and-leave intentional) | PASS + controls 0/1 |
| F-044 gen-guard sizing + visibility | cap 200000 < 5000000; status lines absent; full-of-live skip (base-worktree proven) | ceiling `conntrack.MaxSessions/2` + default 200k + demand growth (per-side caps, tombstone-first, bulk-epoch reclaim) + wired helper capacity (track-current setter, daemon construction + tick) + kernel construction seed (BPF-addressed) + 4 status rows + test decouplings (small injected ceilings) | 7 cells + 9412/F1/skip/9719 rewritten decoupled |
| F-046 telemetry-gap hole | gap → no resync (`SessionSyncResyncs==0`) | dedicated 30s limiter + `TelemetryGapResyncs` + `onFullResync`, advance/dispatch/continue, no drop; superseded `TestEventStreamTelemetryGapDoesNotTriggerResync` deleted (RED cell covers strictly more) | gap PASS + contiguous control PASS, `SessionSyncResyncs` untouched |
| F-116 idle-flap fence abort | flap closes waiter (disconnect err); supersede/evict burn full timeout (base-worktree proven, 2.01s each) | waiter `{ch,conn}` tagging; scoped abort (dropped + not-installed; full = all) at `handleDisconnect` + `installConn` tail + evict tail; lock order mu→fenceAckMu; 3 literal migrations + source-pin update | 4 cells + all 7147/fence siblings GREEN |
| F-117 identity-less lease install | 3 leases received AND held (violating + cross-family) | `validLeaseIdentityForFamily(msgFamily,l)` + pre-store filter + retain-on-empty (#7175 posture) + `DHCPLeasesDroppedNoIdentity` counter (stats/snapshot/literal/status) | callback + held-set + counter + retain asserts GREEN; pure `decode*` untouched |
| F-118 rebase/offset/GC wrap | `rebaseTimestamp(maxUint64,100)==99`; offset survives full disconnect; GC deletes wrapped session | saturating rebase + exact `rebaseSaturates` predicate + install-site counter/log + offset reset at 3 incarnation ends; GC `saturatingDeadline` at all 4 sites (nil at count checks) + `GCStats` field + sweep-log line | unit + install-overflow + control + offset reset/keep + GC wrap + stat-pin (exactly 2) cells GREEN |
| F-123 oversized watermark advance | garbage + `+2` seqs advance watermarks / poison ACK | strict `seq==prevSeq+1` gate on the UNTRUSTED-length path only; trusted within-ceiling drain keeps advance+trim; honest residual comment | 3 cells + 6160/6132/5483 siblings GREEN |
| F-124 slot grammar 256x + pre-validation truncation | 4096/5000/1048575 accepted; slot 70000 builds with truncated port | `parseBindingSlot` → 4096 + docstring; `populateInjectPacketTuple` pre-derivation check + seam-comment rewrite; gRPC handler bound pre-cast; 5449 tables →4095/4096 | 2 RED + all slot/inject/seam siblings GREEN |
| F-125 paused/ackBatch write-only | `EventStreamStatus` lacks both fields (reflection RED) | `Paused` + `EventsSinceAck` (omitempty, sawtooth doc) + `Status()` fills + `pauseMu` store-after-success + reconnect reset | failure/success halves + advance/reset halves GREEN |

## Adjudication (2 hostile reviews, 33 findings, no KILLs)

Scope P0 F-117 filter-before-store accepted (verified store-before-callback);
F-116 supersede/evict abort accepted; F-044 small-cap decoupling + ceiling seam
accepted (no `reviewed-not-executed` coverage); F-123 untrusted-path scoping
accepted; F-046 debounce-only + dedicated counter accepted (shared 2s limiter
rejected: starvation + thundering); F-117/F-118 retain/saturation rules accepted;
F-124 gRPC path accepted (verified bypass); F-125 race fixed via `pauseMu`
(stronger than doc-only). Two PARTIAL: F-118 refuse-the-install rejected
(install-contract change; linger bounded by deletes/reconcile + visible);
F-125 reflection helper kept (no sunset churn). One MOOT: shared-counter
semantics (dedicated counter chosen).
Post-review blockers, all folded in: (1) live-count cap killed → demand growth;
(2) unconditional 25× killed → helper-wired ceiling + kernel construction seed;
(3) heuristic saturation counter replaced by exact shared predicate;
(4) test-only coverage restored to executed call sites via ceiling seam.
- (5) capacity ratchet replaced by track-current (backend replacement honesty).

## F-044 heap budget (measured, not assumed)

- Measured (1M representative keys, `runtime.MemStats` deltas): gen/close-class
  maps 55.9 B/entry; tombstone list node 64 B/entry (+~58 B elems-map entry).
- Formula: effective ceiling = wired helper aggregate `MaxSessions` (logical
  sessions, workers×131072), clamped to [200k default, 5M absolute backstop];
  unwired keeps default (status quo ante, no new headroom); kernel/legacy wired
  once at construction to `conntrack.MaxSessions/2` (BPF-addressed).
- Worst case ≈ 290 B/logical (3 maps + order): 4W/524k ≈ 152 MB, 6W/786k ≈
  228 MB, 16W/2M ≈ 580 MB — only at FULL table, demand-grown (typical far
  less), proportionate to the table's own cost (helper + BPF + UMEM dominate).
- No authoritative RAM spec found in tree (4 GiB image examples only, no
  xpfd MemoryMax); owner precedent (1.5 GiB dismissed on 8–16 GB hardware)
  cited as precedent-not-spec. Merge review is the final blessing gate.

## Residuals (documented, not hidden)

- F-123: real-noncontiguous oversized loops loudly (follow-up: trim-after-N);
  garbage==prevSeq+1 trims one frame (resync-healed).
- F-044: alias-heavy tables past ~50% degrade safely to gen-0; unwired tiny
  profiles hold default + visibility.
- F-046: small holes covered ≤30s + faster backstops (deltas, sweep).
- F-117: legacy short formats still decode structurally (compat kept).
- F-118: corrupt-far-future entries linger to deletes/reconcile (visible via counter).

## Verification (all green; one pre-existing daemon failure)

- Full suites `-count=1` (fresh, post-all-edits): `pkg/cluster` ok (93.4s);
  `pkg/conntrack` ok (0.2s); `pkg/dataplane/userspace` ok (53.6s);
  `pkg/grpcapi` ok (20.3s); `pkg/daemon` = 1 failure in
  `TestCommittedDigestStableAcrossMTUSync9841`, proven IDENTICAL on pristine
  base `609433efb` (base-worktree run) — pre-existing MTU-digest issue,
  unrelated to this cohort (no 9915 file touches MTU paths); everything else green.
- Named cells `-count=5`: `9915` pattern ok in cluster, userspace, conntrack, daemon.
- `gofmt -l` on all 5 trees: empty. `go vet` on all 5 trees: clean.
- Base-RED proofs: initial 13 FAIL + 1 control (pre-edit tree); demand-growth,
  supersede, and evict cells re-proven RED in pristine base worktrees
  (removed after each run).
- Consistency greps per member (call sites/duplicates) clean; diff bounded to
  26 modified + 6 new files (5 RED + this log).

## Fold: parent dual-review (Spark + GPT) onto b33389498

Rebase: the 3 cohort commits onto `origin/master b33389498`; one conflict
(`sync_conn_gen.go`, #9752-adjacent install-table memo vs bounded close-class
wrappers) resolved keeping BOTH intents (wrapper calls + memo/restore blocks,
both families). Pushed head `dc22598c` verified clean at the site — review
MAJOR-1 (`theirs` literal) was a mid-rebase worktree artifact, never pushed.
Artifacts MAJOR-5 (daemon `SetGenGuardSessionCap` wiring — both prod call
sites live) and MINOR-15 (this log) confirmed surviving; RED evidence above
preserved (one cell rewritten behaviorally per MINOR-13, same name).

Per-finding dispositions (reviewer labels → what landed → proof):

| Finding | Disposition | Proof |
|---|---|---|
| MAJOR-2/Medium-2 memo caps | install-table memos take `maxEntries` + skip-bool; all 4 sites grow-then-retry on `sentCap`/`recvCap` | unwired-boundary cell GREEN; raw-ceiling grep 0 |
| MAJOR-3 grow/shrink | clamp-at-read + clamp-down in grow (single pinned `maxCap` read) + `GenCapShrunk` counter/status | shrink cell GREEN (`Stats().GenCapShrunk==1`) |
| MAJOR-4 budget | comment rewritten to honest 10-map math (6 sender + 4 receiver × side cap); per-map=wired is coverage-correct (one family can hold all sessions) | status saturation test GREEN |
| MAJOR-6 GC/churn | GC already saturating (verified, not re-fixed); exact-`MaxUint64` run-GC cell added; sweep excludes corrupt-future `Created` | GC-exact + sweep-skip GREEN |
| MAJOR-7 per-conn offset | kept-conn state cleared AT advance start (before eviction) + both disconnect branches | switch-fallback cell GREEN (asserts replacement path) |
| Medium-3 publish race | `publishClockSyncIfCurrent`: membership check+store atomic under `s.mu`; nil-conn test path preserved | 2 interleaving cells GREEN |
| MAJOR-8 spec/counter | predicate/clamp aligned UP to int64-range spec; installs return landed-`bool`; counter counts landed only | `2^63+1` + stale-no-count GREEN |
| MAJOR-9 premise/TOCTOU | premise verified by arm read (`sendFenceAck` on receiving conn) + behavioral pin; select+register atomic under `s.mu` | 4 fence cells + premise GREEN |
| MAJOR-10 spurious abort | pre-fold scoped aborts RESTORED verbatim + closed-socket reconciliation (replaced socket is closed ⇒ ACK path destroyed ⇒ prompt fail-open equals timeout outcome); own `epochReboot`-gating attempt reverted as wrong | supersede prompt-abort GREEN |
| MAJOR-11 cluster/upgrade | cluster `0..255` rejected loud; identity errors counted centrally; `:539` split (identity vs wrong-role); leave-as-is justified loudly (anti-flap, heals on commit) | bounds + counter GREEN |
| MAJOR-12 legacy/seq | guard split (`newer`/`advanceIfNewer`, `admit` kept); DHCP check→decode→filter→advance-on-apply; atomic advance+store+callback under new `dhcpApplyMu`; NO filter bypass (F-117 untrailered-drops contract forbids it — own bypass attempt reverted) | wedge + ordering (`-race`) GREEN; drops test untouched GREEN |
| Medium-4a escape | 4 fence tests wait observed `syncMsgFence` frames (registration precedes write) | GREEN (escape grep 0) |
| MINOR-14/4b fixtures | per-instance `testClockNow` hook + fixed values (cohort + 9653) | 5x GREEN (live-clock grep 0) |
| MINOR-13 tautology | cap pin rewritten behavioral (setter→ceiling), same name | GREEN |
| MINOR-16 Set(0) | contract reconciled (nonzero tracks, zero retains unknown) + pin | GREEN |
| MINOR-17 nil grow | nil guard + pin | GREEN |
| MINOR-18 grep | abort grep dropped; behavioral pins cited | suite GREEN |

Adjudications: (1) repeated STOP-work advisories weighed against Main's
verbatim twice-confirmed orders (finish rebase, then fold) — proceeded per
Main; advisories self-describe as weigh-don't-obey and no newer Main order
existed. (2) Own `epochReboot`-gated abort reverted on blocker review
(prompt abort correct with or without reboot evidence — closed socket).
(3) Own DHCP filter bypass reverted on blocker review (F-117 contract).
(4) Callback-under-`recvSeqMu` replaced with `dhcpApplyMu` design on blocker
review (public reentrancy deadlock). (5) Kept-conn clear moved to advance
start on blocker review (lock-free read window).

Fold gate (post-all-edits, `-count=1` fresh): `pkg/cluster` ok (93.4s);
`pkg/conntrack` ok (0.2s); `pkg/dataplane/userspace` ok (53.9s);
`pkg/grpcapi` ok (20.2s); `pkg/daemon` ok minus pre-existing
`TestCommittedDigestStableAcrossMTUSync9841` (1-name `-skip`, verified
single-match; pre-existing per base-worktree proof above). Named 9915
`-count=5` ok in cluster/conntrack/userspace/daemon; ordering cell
`-race -count=5` clean; `go vet` 5 pkgs silent; `gofmt -l` empty. No test
weakened at any point: every red outside the Main-ordered lists was fixed in
production code (one stale-numbered test hunk repaired to Main-ordered
intent); every test edit is either a Main-named hardening or a new
regression pin.

Adjudication (6): final blocker (clear evicted/replaced conns' clock state /
gate `clockOffsetFor` by membership) REJECTED with grounding, no code change.
`&authConn` objects are created fresh per connection (sole production site
`wrapSyncConn`, `sync_auth.go:510`; the only other creation is the
`InstallUnauthenticatedConnForTest` seam) and never pooled or reinstalled
(`installConn`'s sole prod caller passes fresh wraps); `clockOffsetFor` has
exactly 2 readers (v4/v6 session installs), each passing the carrying conn.
New-incarnation data therefore arrives only on current-or-fresh conns
(keepIdx cleared at advance, replacements fresh, global cleared), while an
evicted conn's retained offset is readable solely by its own in-flight old
frames — for which the old offset is the CORRECT rebase. Clearing would
convert correct installs into mis-rebased ones (worst case: instant reap of
a live standby copy); gating would add an `s.mu` load to the hot install
path for the same mis-rebase. The publish gate already prevents retired
offsets from going global/new. No new cell: nothing new pinned — the
retired-conn cells already prove the gate. The keepIdx immediate clear
stands untouched (new-boot frames on the re-stamped conn must never see
dead-boot state — the MAJOR-7 bug itself).

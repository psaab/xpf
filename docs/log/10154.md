# #10154 — dataplane canary allowlist and 6928 caller remediation

Worktree `fix/10154-allowlist`, based on `origin/master`.

- **Timestamp**: 2026-09-18
- **Context**: Read the full open #10154 body and all comments. Clean-master reproduction reported two failures: the legacy-import allowlist omitted `pkg/cluster/test_seams.go` and `pkg/daemon/daemon_ha_sync.go`, and the #6928 Cleanup caller walk counted nested checkout copies.
- **Allowlist disposition**: Both imports are legitimate and documented rather than blind ratchet entries. `pkg/cluster/test_seams.go` is an external-package test seam whose generation/journal observers must name the existing legacy `dataplane.SessionKey` wire type. `pkg/daemon/daemon_ha_sync.go` uses the daemon-owned `dataplane.RuntimeDataPlane` interface for sync-guard capacity routing, with the daemon-local cached-status probe and `conntrack.MaxSessions` fallback; it does not add a legacy enforcement path. Added per-importer reasons to `legacyDataplaneImportAllowlist` and the #1451 retirement table.
- **6928 remediation**: `findCleanupCallers` now skips dot-prefixed and underscore-prefixed directories below the supplied root (`path != root`), matching Go package discovery while still traversing a caller-supplied hidden/underscore root. The test was walking `.claude/worktrees/*` nested checkouts and reporting each duplicate `cmd/xpfd/main.go` Cleanup call as a production caller; no production call graph had changed.

## Red → green evidence

- **RED on clean master**: `TestOperatorPackagesOnlyUseDocumentedLegacyDataplaneImports` reported unexpected imports `[pkg/cluster/test_seams.go pkg/daemon/daemon_ha_sync.go]`. `TestCleanupProductionCallersMatchRemediation_6928` reported duplicate Cleanup callers under `.claude/worktrees/*`.
- **GREEN after fix**: Both issue tests passed in one targeted run. The expected Cleanup caller set remains exactly `cmd/xpfd/main.go:main (call)` and `pkg/dataplane/loader.go:var teardownCleanupFn (value)`.

## Verification

- `go test ./pkg/dataplane/ -count=1 -run 'TestOperatorPackagesOnlyUseDocumentedLegacyDataplaneImports|TestCleanupProductionCallersMatchRemediation_6928' -v`: **2 tests PASS**.
- `go test ./pkg/dataplane/ -count=1`: **PASS**.
- `go test ./pkg/dataplane/... -count=1`: **4 packages PASS** (`pkg/dataplane`, `pkg/dataplane/runtime`, `pkg/dataplane/userspace`, `pkg/dataplane/userspace/format`).
- `gofmt -w pkg/dataplane/cleanup_reachability_6928_test.go pkg/dataplane/retirement_boundary_canary_test.go`: **PASS**.
- `git diff --check`: **PASS**.

- **Files**: `pkg/dataplane/retirement_boundary_canary_test.go`, `pkg/dataplane/cleanup_reachability_6928_test.go`, `docs/pr/1373-retire-ebpf-dataplane/README.md`.

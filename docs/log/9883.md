# #9883 — braced filter under unknown family folded into IPv4 pool while #9017 warning said NOTHING

- **Timestamp**: 2026-09-15
- **Action**: STEP-0 on base `origin/master 15be546a1`: defect PRESENT.
  `CompileConfigLenient` of braced `firewall { family inett { filter BAD ... } }`
  yields `inInet=true inInet6=false` WITH the `compiles to NOTHING` warning
  (contradiction); flat-set `set firewall family inett ...` yields
  `inInet=false inInet6=false` with the same warning (message true there —
  confirms the reachable route is the braced one). Strict braced rejects.
  Mechanism: `pkg/config/compiler_firewall.go:245-251`
  (`dests := []map[string]*FirewallFilter{fw.FiltersInet}`, only `inet6`/`any`
  special-cased) + `pkg/config/compiler_firewall_family_9017.go:91-99`
  (lenient warns and CONTINUES into the fold). SMALL-tier brief posted to the
  issue (comment 5680048655); direction: quarantine (fail-safe), not reword —
  rewording cannot make one message true on both routes.
- **File(s)**: none (measurement; throwaway repro, since removed)

- **Timestamp**: 2026-09-15
- **Action**: Quarantined undeclared families out of BOTH pools.
  `compileFirewall` skips any family token outside the schema-declared set,
  read from the same `firewallFamilyPermitted9017()` source the #9017 gate
  enforces (declaring a fourth family permits it in all three places; no
  second list). The #3884 collision gate ignores quarantined families too —
  otherwise its lenient `silently overwrites` warning would contradict the
  token warning on `inett/X` + `inet/X`. The #9017 message text is unchanged:
  the quarantine makes it true on every route. Fail-closed end-to-end: an
  unhooked quarantined filter is inert (as messaged); a hooked one dangles →
  #3296 lenient warns + the snapshot-integrity backstop refuses to publish
  (prior state kept, never degrades to Accept).
- **File(s)**: pkg/config/compiler_firewall.go,
  pkg/config/compiler_firewall_family_9017.go, pkg/config/compiler_prewalk.go
  (comment), pkg/config/firewall_family_unknown_9883_test.go (new)

- **Timestamp**: 2026-09-15
- **Action**: Retired `TestFirewallFilterCrossFamilyNameCollisionNonInetRejected`
  (mpls/any): its premise — "anything except inet6 folds into the same pool" —
  is exactly what the quarantine removes, so the pin is void (strict still
  rejects, now naming the token via the #9017 gate; that scoping is pinned by
  `TestUnknownFamilyEscapesCollisionGate9883` instead). Rewriting it onto
  declared families would duplicate `TestFirewallFilterCrossFamilyNameCollisionRejected`
  (inet+any), which still stands. Header comments updated to the declared-fold
  contract. Full `pkg/config` + `pkg/configstore` suites green; named cells 5x.
- **File(s)**: pkg/config/compiler_firewall_family_collision_3884_test.go

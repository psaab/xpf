# #9562 — config: snmp trap-options children other than source-address commit with zero signal

- **Timestamp**: 2026-09-10
  - **Action**: Measured how `trap-options` reaches the compiler. schemaSNMP
    does not model it, so the top-level SNMP container admits it. A braced
    stanza carries one child per statement, while the brace-elided and flat-set
    spellings pack each statement at the node's Keys[1], one node per line.
    Verified each advisory's claim in pkg/snmp before writing it:
    - each trap socket is a plain `net.DialTimeout("udp")`, so traps use the
      default routing instance;
    - the trap varbinds carry no context OID;
    - the v1 agent-addr is derived per target (#9123), and v2c traps have no
      agent-addr field.
  - **Action**: snmpInertKnobWarnings' `trap-options` arm now emits one advisory
    per statement, read by position in every spelling. The advisory set is the
    complement of the empty compiled set, so `logical-system` or any unlisted
    statement is covered by construction. Wording per statement:
    - `source-address`: text unchanged, pinned by the #4306, #9414 and #8807
      cells;
    - `routing-instance`: default routing instance;
    - `context-oid`: no context varbind;
    - `agent-address`: ignored, because the v1 agent-addr is already the
      per-target source address. A blanket "not implemented" would be false
      here, as the issue warned;
    - anything else: no-op.
  - **Action**: Cells in pkg/config/snmp_trap_options_inert_9562_test.go:
    - each statement in the braced, brace-elided and flat-set spellings, on the
      strict and tolerant paths, asserting the phrase TRUE for it (a prefix
      alone passes against the generic fallback) and no echoed value;
    - one advisory per statement for a three-statement stanza;
    - no trap-options advisory for `trap-group` or `contact` configs.
    - The pkg/config suite is green.
  - **Action**: docs/config-schema.md's inert-knob list now names every
    trap-options statement and what happens to it.
  - **File(s)**: pkg/config/compiler_system.go,
    pkg/config/compiler_snmp_trap_options_9562.go,
    pkg/config/snmp_trap_options_inert_9562_test.go, docs/config-schema.md,
    docs/log/9562.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation matrix at code head `335e20cee`, scored by `go test -json`
    test NAME. Each mutant was applied under an exact-one-anchor assertion,
    proven by a non-empty `git diff`, and restored with `git checkout HEAD --`.
    NOOP was clean, and 7 of 7 were KILLED:
    - T1: packed Keys ignored.
    - T2: braced children ignored.
    - T3: `routing-instance` falls to the generic advisory.
    - T4: `context-oid` falls to the generic advisory.
    - T5: `agent-address` falls to the generic advisory.
    - T6: the trap-options arm emits nothing. Killed by the per-statement cell.
    - T7: the source-address wording changed.

    T3-T5 are the mutants that only the per-statement phrase assertions can see:
    the generic text shares the prefix.
  - **File(s)**: docs/log/9562.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    91b8da13a. That tree is origin/master 0b077cc6a merged with eight pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection:
    #9493, #9532, #9625, #9562, #9527, #9564, #9552, #9572.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5829 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9562.md

# #10004 — failed targeted/batch failover restores the cleared ManualFailover hold

- **Timestamp**: 2026-09-17
- **Action**: Read the full issue body and comments (the issue has no comments). Added fail-on-revert coverage for all three targeted failure arms and all three batch failure arms. Each failure test drives a request after the local hold is cleared and asserts `ManualFailover`, the original `ManualFailoverAt`, and the held state are restored. Added success controls proving a successful targeted or batch transfer still consumes the hold, preflight/send controls proving the existing early-failure promise, ForceSecondary weight-0 and in-flight monitor-debt controls, and reset/overlap controls proving newer operator ownership is not resurrected or overwritten by a stale abort.
- **File(s)**: `pkg/cluster/failover_hold_restore_10004_test.go`

- **Timestamp**: 2026-09-17
- **Action**: Carried a per-request snapshot of the preexisting manual hold through targeted and batch peer-transfer calls. Every abort arm now rolls back its request-matched peer override, removes only that request's grace state, re-runs election when needed, and restores the captured hold and ForceSecondary weight-0 shape unless a newer reset generation or hold already won. Targeted and batch requests reserve their RGs for the full operation using the existing failover serialization guard, preventing overlapping requests from crossing ownership. Successful transfers retain the existing clear/consume behavior; preflight and request-send failures remain before the clear.
- **File(s)**: `pkg/cluster/failover.go`

- **Timestamp**: 2026-09-17
- **Action**: RED then GREEN proof. Before the implementation, `go test ./pkg/cluster -run 'TestRequestPeerFailover.*10004' -count=1` failed all six failure-arm restoration cells with `ManualFailover=false` and a zero timestamp. After the implementation, the same command passed; `go test ./pkg/cluster -run '10004' -count=1` passed all issue cells; targeted and race runs passed; and the complete `pkg/cluster` suite passed.
- **File(s)**: `pkg/cluster/failover_hold_restore_10004_test.go`

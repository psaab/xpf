# #9757 — a deleted unit MTU left the VLAN child at the old value

- **Timestamp**: 2026-09-12
- **Action**: Wrote the acceptance cells FIRST and confirmed them red at
  `origin/master 3f414db4d`, reusing #9761's `applyTaggedParent9761` harness
  (a fake host plus the `linkSetMTUSeam` write recorder) rather than building a
  second one:

  ```
  #9757: ge-0-0-2.80 kept MTU 1400 after the mtu statement was deleted, want the parent's 1500
  #9757: ge-0-0-2.80 reset to 1400, want the parent's 9000
  ```

  The idempotence cell PASSES at master, vacuously — nothing writes there at
  all. Recorded as such rather than counted as a fix cell.
- **File(s)**: pkg/dataplane/compiler_iface_unit_mtu_reset_9757_test.go

- **Timestamp**: 2026-09-12
- **Action**: Fixed by giving the write an else branch: with no unit statement,
  the target is the PARENT link's live MTU, which is what a freshly created VLAN
  child inherits — so the device lands where it would have been had the
  statement never existed, rather than at a number this code invented.
  `family inet6 mtu` compiles into the same `unit.MTU` and is covered by the
  same branch.

  Written only on a real difference. This path runs on every commit, so an
  unconditional write would churn the link and the log; that is asserted by a
  cell rather than assumed.
- **File(s)**: pkg/dataplane/compiler_iface.go, pkg/dataplane/README.md

- **Timestamp**: 2026-09-12
- **Action**: Mutation matrix at `5cdbaab0d`, detached worktree. Baseline clean
  at 3 tests. **4 of 4 killed, 0 VOID, 0 escaped.**

  | mutant | killed by |
  |---|---|
  | S1 no reset branch (the defect) | the reset cell |
  | S2 reset to a constant 1500 | the follows-the-parent cell |
  | S3 the parent wins over a configured unit MTU | the in-apply control |
  | S4 write unconditionally | the idempotence cell |

  S3 is the one the cells were shaped for: a unit WITH a statement sits beside
  one without in the SAME apply, so a fix that reset every child to the parent —
  which passes the headline row — fails a control instead.
- **File(s)**: none (scratch harness)

- **Timestamp**: 2026-09-12
- **Action**: Smoke judgement. `make test-failover` is NOT owed:
  `docs/engineering-style.md` makes it mandatory for "cluster, VRRP,
  session-sync or failover code", and this is the interface-apply path.

  The tempting second reason to run it was to repair the cluster, since this
  defect was found there and left both nodes at 1400. Checked instead of
  assumed — `ip -o link show` on both nodes reads **mtu 1500**, so the stale
  state was already repaired by hand and there is nothing to fix:

  ```
  ge-0-0-2.80@ge-0-0-2: ... mtu 1500 ...   (fw0)
  ge-7-0-2.80@ge-7-0-2: ... mtu 1500 ...   (fw1)
  ```

  A read-only check, no lock taken, nothing deployed.
- **File(s)**: none (measurement)

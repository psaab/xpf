# #9506 S5 q0 capture, provenance, and physical reinject commit

## Scope

S5 wires the userspace capture path end to end: strict IPv4/IPv6 payload
classification, `CaptureOrigin` registration and validation through the
slow-path/fragment pipeline, bounded per-flow handoff and fragment slots,
owner-homogeneous batching, and the Rust q0 full-frame writer with a physical
REINJECT ACK/commit point. The daemon stages queue generations before snapshot
publication, commits the nft diversion only after the matching dataplane
snapshot is fresh, and rolls back staged generations on failure.

The reinject authority announcement is persistent across submit-socket
reconnects. Rust keeps queue-epoch tombstones, transfers installed authority
under the shared target-swap lock, and treats the physical ACK as the commit
point; old-set conntrack ACK and the S4 permit/queue epoch contracts remain
in force.

## Snapshot protocol fence

The S4 authority fields are real transmitted `ConfigSnapshot` fields:
`permit_epoch` and `queue_epochs`. Their addition changed the shape digest
from `c098e2c1f595be54bd617f5ae0b2a2806ef8cfed34ce7cadad9c824d17b5c779` to
`a497626cd7021af40daaf16fac0335547ce677cc5f947a1dfeee688c06fb5b68`.
S5 therefore moved the Go and Rust snapshot protocol from 27 to 28 in
lockstep, refreshed the shape pin and the declared snapshot golden, and named
both fields in the version-history comments. An old helper must not accept a
snapshot while applying stale q0 ownership.

## RED then GREEN evidence

| Cell | RED | GREEN |
| --- | --- | --- |
| Snapshot shape/version lockstep | `go test ./pkg/dataplane/userspace -count=1` caught `TestSnapshotShapeIsPinnedToProtocolVersion8892`: Go/Rust remained at 27 while the computed digest was `a497626c...` instead of the old `c098e2c1...` (`artifact://50369`). | The targeted shape, Go/Rust lockstep, and secure-tunnel compatibility cells passed after the 27→28 bump; the full userspace suite passed below. |
| Declared snapshot golden | The full userspace run caught `TestContractSnapshotGolden9821`: the protocol-v28 snapshot had the same 15,067-byte shape but differed from the version-27 golden (`artifact://50386`). | Regenerated `pkg/dataplane/userspace/testdata/contract-9821-declared-snapshot.json` and the Go producer suite passed; the Rust consumer contract cells passed 3/3, 6,535 filtered (`artifact://50400`). |
| Daemon staged-removal safety | The first daemon suite exposed two no-dataplane/deferred-publish regressions: the nil→nil stage was incorrectly treated as a pending removal, returning `staged divert withheld` (`artifact://50426`). | Kept old→nil removal pending but made nil→nil a no-op. The focused regressions passed, followed by the full daemon suite. |
| Fragment performance M4 | Under concurrent Go/Cargo suite load, `TestMeasureFragments9506` measured IPv4 loaded p99 106,052 ns vs baseline 20,993 ns (5.05x), failing the 5x gate (`artifact://50414`). | In a quiet window, the isolated cell passed: IPv4 22,949→27,324 ns (1.19x) and IPv6 12,412→17,684 ns (1.42x). The failure was CPU contention, not the classifier hot path. |
| q0 authority and physical commit | Rust core/server cells are the RED/GREEN contract for announcement, epoch fencing, target transfer, ACK ordering, and reconnect replay. | Rust core: 51 passed (`artifact://50440`); server: 9 passed (`artifact://50444`); post-bump cargo check: 0 errors (`artifact://50385`). |
| Socket lifecycle safety | The review identified two ambiguity windows: a partial completion frame could desynchronize the persistent stream, and a positive admission with a mismatched lease/origin echo could already have been accepted by Rust. | Completion timeouts now close/reset the stream; admitted echo mismatches cancel by request ID plus full queue-number/epoch scope and enter uncertainty. Derived reinject paths are preflighted for aliases, dot-segment aliases, and regular files; the focused cells pass. |
| Follow-up wire outcome decoding | RED review repro: Rust COMPLETE codes 6–9 were decoded as unknown, poisoning the persistent completion stream and losing the rest of a batch. | GREEN: Go now decodes `Fenced`, `Denied`, `Accepted`, and `WouldReinject`; fenced/denied are terminal DROP statistics and advisory outcomes are uncertain. The extended-outcome pipeline and socket cells pass. |
| Failed capture-stage re-announce | RED review repro: rollback restored the old runtime without invalidating `old.announced`, so the next supervisor tick skipped re-announcement and stale epochs could wedge at `ADMIT_STALE`. | GREEN: restore invalidates the old authority announcement under `authorityMu` before publishing the runtime; the daemon regression and full daemon suite pass. |
| Observer parser and HA symmetry | RED review coverage gap: the hermetic harness selftest did not exercise ruleset JSON parsing or prove that one-node-only `stN`/SA evidence is incomplete. | GREEN: parser fixtures cover exact four-hook, missing-family, missing-list, scalar, malformed, and empty-list inputs; per-node probes require both firewalls. Harness selftest is 17/17. |
| nft observer provisioning | RED live archive: fw1 reported `nft: command not found`; the Incus package lists did not install `nftables`. | GREEN source fix: both Incus setup paths install `nftables`; no live rerun was taken in this follow-up. |

## Affected-suite results

- `go test ./pkg/dataplane/userspace -count=1`: passed, 58.878s after reinject-path preflight checks.
- `go test ./pkg/nfqueue -count=1`: passed, 5.778s with extended outcome decode/resolution cells.
- `go test ./pkg/daemon -count=1`: passed, 91.689s with staged-capture re-announce regression coverage.
- Rust contract consumer: 3 passed, 6,535 filtered (`artifact://50400`).

## Live cells and remainder

The locked isolated-cluster invocation was:

```text
./test/incus/with-cluster.sh '9506 S5 T12/G2' -- ./test/incus/t12-g2-9506.sh
```

The archive is `/var/tmp/xpf-t12-g2-9506-1789825840`. The local `xpfd` and
both deployed `/proc` executables reported
`59ebc242637c10c00bfa19f38c558b76b5c614ee117ce11e8f71496fbd5df414`
(`MATCH/both`) for git `ca5ede686fb4830c721271d1e25f299253292776`.
The observer preconditions were
`stn=0 xfrm_sa=0 divert_table=0 fence_table=0 q0_mark_surface=0
complete_fixture=0`. `fw0-ruleset.json` parsed successfully; the original
`fw1-ruleset.json` observer recorded `nft: command not found`, so the run
could not distinguish an unreadable fw1 observer from an absent table.

The original archive emitted 30 live rows: 12 T12 cells and 18 G2 cells, all
`VOID`. Because this run predates the observer-readability fix, eight rows
carried the generic `env-void` reason and 22 rows carried the old-format
three-item missing-fixture reason
`owned-stN,live-XFRM-SA,xpf_ipsec_divert`. Those rows are retained as the
original run record and are not rewritten in place.
After that run, the harness was corrected to expose per-node
readability/parse bits, require both readable observers in the `has_*` gates,
and use `LIVE_REASON` for every live VOID branch; `./test/incus/t12-g2-9506.sh
--selftest` passes 17/17. The original rows remain immutable evidence and are
not rewritten in place. Teardown of the original run was clean:
`fw0_config_cmp=1 fw1_config_cmp=1 fw0_residue_clear=1
fw1_residue_clear=1 residue_probe_error=0`.

The unprovisioned-fixture statement above is superseded by the completed
fixture lane. The final locked run at
`/var/tmp/xpf-t12-g2-9506-1789844895/` provisioned
`loss:xpf-ipsec-peer-9506`, matching PSK/proposals/traffic selectors, route-based
`stN` on both firewalls, readable nft observers, `nfnetlink_queue` probes, and
real SA/queue/workload measurements for all four shapes at 8/16/32 tunnels.
Every shape converged and tore down cleanly; the final row set is still
`VOID` because exact r6 provenance, listener/reinjection, non-tunnel overhead,
VRF-transition, and pinhole observers are not implemented. See
`docs/log/9506-fixture.md` for the inventory and complete row disposition.
The NAT-T renderer precondition was fixed separately in merged #10474
(`f4048596d`), and all NAT-T cells reached live SAs in the final run.
Verdict for #9506 remains `Refs #9506`; this lane is not Closes-ready while
the 30 observer predicates remain VOID.

The historical inventory below describes the pre-fixture baseline and is
retained as provenance; it is not the current cluster capability. The
dedicated fixture-builder now owns peer endpoint/network, PSK/proposals/traffic
selectors, route-based xfrmi/stN, live SA/divert verification, packet/workload
measurements, and full restore/residue checks. The static cluster setup still
does not embed these VPN declarations; they remain deliberately ephemeral and
lock-scoped to the fixture.

The Rust lifecycle retains a 1 ms `target_sync_thread` poll for this slice. It
samples `slow_path` under the server-state mutex and converges to the current
steady pointer within one poll interval; a transient A→B→A change may be
coalesced, but a superseded intermediate target is never published to
submitters and need not receive authority. Teardown shares the `running`/join
lifecycle. Replacing the poll with event-driven `ArcSwap` publication remains
follow-up cleanup, not an S5 correctness blocker.

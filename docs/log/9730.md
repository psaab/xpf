# #9730 — HA testing docs route validation to diagnostic walkthroughs and mislabel the gates

- **Timestamp**: 2026-09-11
- **Action**: Make the Makefile HA smokes the documented gates, label the long-form walkthroughs diagnostic, and describe the restart gate as what it measures
- **File(s)**:
  - `testing-docs/failover-testing.md`
  - `testing-docs/README.md`
  - `docs/testing-procedures.md`

## Re-verified at origin/master

- `testing-docs/failover-testing.md`:
  - it listed `scripts/userspace-ha-failover-validation.sh` as the "primary failover script for userspace HA continuity";
  - it put `test-failover`, `test-ha-crash` and the three stress smokes under "Legacy eBPF failover scripts" ("Use these when validating the non-userspace cluster");
  - its release-validation order began with the two walkthroughs.
- `testing-docs/README.md:31` said current userspace HA validation "must use" the walkthroughs.
- `docs/testing-procedures.md:145` called the restart gate "zero packet loss during daemon restart (hitless restart)". But `test-restart-connectivity.sh` asserts `<= MAX_LOST_PINGS` (default 2) per restart over `RESTART_CYCLES` (default 3) for the #75 regression.
- In contrast:
  - `test/incus/HARNESSES.unreached:80-81` declares both walkthroughs DIAGNOSTIC, superseded as gates;
  - `docs/engineering-style.md:296` requires `make test-failover` + `make test-ha-crash`;
  - the Makefile smokes default to the loss userspace cluster (`test/incus/loss-userspace-cluster.env`), not an eBPF cluster.

## Change

- **`failover-testing.md`:**
  - scope item 2 names the Makefile gates on the loss userspace cluster;
  - "HA gates (the Makefile smokes)" comes first and says why to run them through `make` (the cluster lock and the ledger row);
  - "Diagnostic walkthroughs (not gates)" follows and cites the census;
  - the two walkthrough scenarios carry a not-a-gate note;
  - "Legacy eBPF Failover Tests" becomes "Makefile HA Smoke Gates", with `make` invocations;
  - the release order starts with the two required gates.
- **`README.md`:** the historical-references row names the gates and labels the walkthroughs diagnostic.
- **`testing-procedures.md`:** the restart gate is described by its actual assertion and scope.

The local `xpf-fw0/xpf-fw1` environment section is left as it is: that cluster really is the legacy regression environment (`CLUSTER_ENV=`).

## Validation

- Docs only.
- `grep -n "Legacy eBPF failover scripts\|hitless restart\|must use \`failover-testing.md\`" testing-docs docs/testing-procedures.md` now matches nothing.
- `make harness-census` is OK.
- No code or harness behaviour changed, and no guard was added: the census already carries the diagnostic/gate classification these docs now repeat.
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    aecc1b61d. That tree is origin/master 80db775c4 merged with 4 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9679, #9693, #9727, #9730.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5869 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9730.md

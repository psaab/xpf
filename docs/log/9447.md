# #9447: two #9327 measurement cells carried a bare `#[ignore]`, so `make selftest` was red

- **Timestamp**: 2026-09-11T00:25Z
- **Action**: Declared both #9327 measurement cells to the ignored-cell census with the `MEASUREMENT:` marker the census requires.
- **File(s)**:
  - `userspace-dp/src/nat/tests_pool.rs` (`recycle_amortization_9327`)
  - `userspace-dp/src/afxdp/session_glue/tests.rs` (`reconcile_cost_9327`)

## Measured before the change (origin/master `6d930698c`, pristine worktree)

`sh scripts/ignored-cell-census.sh` exits 1. Both of its own controls pass, and it names exactly these two cells as undeclared:

```
userspace-dp/src/nat/tests_pool.rs:11271
userspace-dp/src/afxdp/session_glue/tests.rs:9596
ignored-cell census: 7 #[ignore] cell(s) — 5 measurement(s), 0 issue-linked, 2 undeclared
```

## Change

Each bare `#[ignore]` becomes `#[ignore = "MEASUREMENT: #9327 … prints numbers, asserts nothing (run with … --ignored --nocapture)"]`. The text follows the existing measurement cells (`io_uring_write_tests.rs:743`). Each cell's comment already described it as a measurement; only the attribute was missing the declaration. The `#9327` inside the reason does not make the census classify it as issue-linked, because the census reports 0 issue-linked cells after the change.

## After

`sh scripts/ignored-cell-census.sh` exits 0: `7 #[ignore] cell(s) — 7 measurement(s), 0 issue-linked, 0 undeclared`.

No docs change is needed beyond this log. The census script's header already documents the marker, and no module README mentions these cells.

## Validation

- **Per-cell mutation against the census.** The fix was committed first, and each mutant was restored with `sed` rather than `git checkout`.
  - CONTROL (no mutation): rc=0, 0 undeclared.
  - M1, `tests_pool.rs:11271` reverted to a bare `#[ignore]` (occurrences 0 → 1): rc=1, naming exactly `userspace-dp/src/nat/tests_pool.rs:11271`.
  - M2, `session_glue/tests.rs:9596` reverted (0 → 1): rc=1, naming exactly `userspace-dp/src/afxdp/session_glue/tests.rs:9596`.
  - After restore the tree equals HEAD `a28a82bbd`, and a post-restore control gives rc=0.
- **`make selftest`** at `a28a82bbd`: SELFTEST_EXIT=0, passed=165, skipped=0, failed=0. The issue measured 164 / 0 / 1 on master.
- **`make test`** at `a28a82bbd` (base `6d930698c`, TMPDIR/GOTMPDIR unset): MAKETEST_EXIT=0.
  - Go: 77 packages ok, 0 FAIL/panic.
  - Rust: 11 binaries, 5816 passed, 0 failed, 6 ignored.
  - No `make` errors; real ENOSPC hits: 0.

- **Timestamp**: 2026-09-10
  - **Action**: The #3855 routing-instance table-id gate's pre-expansion view now counts only `groups` blocks that an `apply-groups` statement reaches.
    - New `reachableGroupNamesAST` seeds from apply-groups outside the groups stanza at any depth, resolves `${node}` to node0 and node1, and follows applications inside reached group bodies.
    - The lenient warning no longer claims which instance is quarantined, since the union spans both nodes' views; the runtime quarantine names it.
    - The name scan skips `apply-groups`, `apply-groups-except` and `apply-macro` under `routing-instances`. After #9622 widened the scan to two-key leaves, they were counted as instance names.
    - Measured before the fix on master `577831ba2`: the unapplied-group cell and the apply-statement cell were red; the reachable-group and expansion-failure cells were green, as regression guards.
  - **File(s)**: `pkg/config/groups_reachable_9657.go`, `pkg/config/routinginstanceid.go`, `pkg/config/routinginstanceid_unapplied_groups_9657_test.go`, `pkg/config/routinginstance_reserved_name_9622_test.go`, `docs/config-schema.md`, `docs/log/9657.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 1 returned MERGE NO. Its BLOCKER reproduced by execution: skipping `apply-macro` and `apply-groups-except` in the collision scan reopened a strict bypass, because group expansion strips only `apply-groups` and `compileRoutingInstances` compiled the other two as routing instances. At dbe86f41b, `apply-macro M { k v; }` beside a colliding `ri486364` passed strict, compiled to an instance named `apply-macro`, and quarantined `ri486364`. Without any collision the compiler still created a phantom `apply-macro` instance and VRF, and that part is present on master.
    - Fix: one predicate, `isApplyStatementNode` (unquoted `apply-groups`, `apply-groups-except` or `apply-macro`), used by both the compiler and the scan, so they count the same instances. A quoted `"apply-macro"` stays an instance name and still collides.
    - MAJOR (the white-box cell masked the mismatch): new compile-level cell `TestApplyStatementsAreNotRoutingInstances_9657`; the union cell now includes `apply-macro`.
    - MINOR: four comments still said the view unions every groups block; updated.
  - **File(s)**: `pkg/config/routinginstanceid.go`, `pkg/config/compiler_routing.go`, `pkg/config/compiler.go`, `pkg/config/routinginstance_reserved_name_9622.go`, `pkg/config/routinginstanceid_unapplied_groups_9657_test.go`, `docs/config-schema.md`, `docs/log/9657.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 2 returned MERGE NO.
    - Four of its findings came from round 2's quote exemption: a quote does not survive rendering, which an HA peer reparses; expansion strips a quoted `"apply-groups"` by name; the #9323 validator skips by name; and flat-set quote provenance is not stable. `isApplyStatementNode` now goes by name, quoted or not. The quoted cell checks that each keyword's quoted spelling compiles like the unquoted one, before and after a render and reparse.
    - The contextual-reachability MAJOR: expansion walks a group only down to the context of its apply-groups, so a group applied under `system` adds no routing instance. `reachableGroupNamesAST(tree, stanza)` now seeds only at the top level and directly under the stanza. New cell: a group applied under `system`, with a runtime control. `apply-groups-except` stays ignored, and that can only refuse a config, never admit one (documented).
    - The MINOR (completion offers these keywords as instance names): pre-existing schema and completion code; listed as a residual, not changed here.
  - **File(s)**: `pkg/config/groups_reachable_9657.go`, `pkg/config/routinginstanceid.go`, `pkg/config/routinginstanceid_unapplied_groups_9657_test.go`, `docs/config-schema.md`, `docs/log/9657.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 3's partial output named a changed-code issue, and a probe confirmed it. At e141b4553 a routing instance literally named `apply-macro` or `apply-groups-except` (quoted, unquoted or flat set) committed strict-clean and compiled to nothing.
    - A key-count rule was considered and refuted by measurement: SetPath stores a flat `set routing-instances apply-macro M k v` as a one-key `apply-macro` node with child `M k v`, the same shape as an instance under that name.
    - The #9323 routing-instance child validator now reads the instance level through `isApplyStatementNode`. A one-key stanza whose child is a declared routing-instance keyword is refused at strict and warned on lenient. The two-key statement form always commits, including a macro whose key is `interface`.
    - Cells: the quoted spelling now expects the strict refusal and the lenient warning. New `TestApplyStatementSpellingsStillCommit_9657` covers every real spelling (braced, packed, flat macro, flat apply-groups-except, a macro with an interface key) and refuses the flat instance-shaped spelling.
    - Separately, a pre-existing flat-set defect measured on master (flat `set routing-instances apply-groups <g>` inherits nothing) is filed as #9685.
  - **File(s)**: `pkg/config/compiler_routing_children_9323.go`, `pkg/config/routinginstanceid_unapplied_groups_9657_test.go`, `docs/config-schema.md`, `docs/log/9657.md`
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 3 returned MERGE NO with two BLOCKERs on the change.
    - Literal `${node}`: a group literally named `"${node}"`, applied by `apply-groups "${node}"`, lands as is on a compile without node variables. The helper counted only the node0 and node1 resolutions, and both of those expansions fail, so the collision (and a reserved `mgmt`) passed strict. `reachableGroupNamesAST` now counts each reference unresolved as well. New cell `TestTableIDGateCountsALiteralNodeVariableGroup_9657`, with a runtime control.
    - Keyword-named instance: the silent no-op is refused at strict and warned on lenient, as recorded in the previous entry.
    - Codex's MAJOR on a literal `"apply-groups"` instance block is group-expansion behaviour: `runPreWalkGates`, which holds the #9323 validator, runs after expansion (compiler.go ExpandGroupsWithVars at 711, runPreWalkGates at 767). It is outside this change.
    - The completion MINOR stays a residual.
  - **File(s)**: `pkg/config/groups_reachable_9657.go`, `pkg/config/routinginstanceid_unapplied_groups_9657_test.go`, `docs/config-schema.md`, `docs/log/9657.md`

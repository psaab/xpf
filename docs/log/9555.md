# #9555 — a second NAT64 rule-set under the same /96 is shadowed by first-match selection

- **Issue**: #9555, filed by me as an UNMEASURED prediction while writing the
  #9428 guard; measured and retitled before any fix
- **Branch**: `fix/9555-nat64-shadowed-rule-set` (started as
  `measure/9555-nat64-owner-by-name`)
- **Base**: `65541cdd0`, later rebased onto current `origin/master`
- **Worktree**: `/var/tmp/wt-9555`

---

## Timestamp: 2026-09-10 08:55–09:15 — measure first

The coordinator's instruction: measure before anything else, and close NEG if it
does not reproduce.

**Go**: strict `CompileConfig` through the #5144 test's own `natOverlapTree`.
The NAT64 prefix is `64:ff9b::/96` on both rule-sets in every row.

| config | result at `65541cdd0` |
|---|---|
| pools P1, P2 with different names and identical address `100.64.0.7/32` | REJECTED as a #5144 double-mint |
| both rule-sets naming the same pool P1 | accepted |
| disjoint pools P1=`.7`, P2=`.8` | accepted SILENTLY |

**Rust**: `Nat64State` built from two same-prefix snapshots.

```
n_prefixes=2  packet_selects_idx=Some(0)
first_apply  idx0=(100.64.0.7,1024)  idx1=(100.64.0.7,1024)
reload       idx0=(100.64.0.7,1025)  idx1=(100.64.0.7,1026)
```

**My filed mechanism was WRONG.** The body said Rust builds ONE allocator for an
equal `(prefix_bytes, pool_v4)`. Measured: first apply builds TWO independent
allocators, and they share one only after a reload, through first-match
`reuse_allocator`.

**The predicted outcome still held, for a different reason.** Every production
selection is first match on the prefix bytes:
- the packet mint goes `poll_descriptor` → `classify_ipv6_packet` →
  `match_ipv6_dest`, and `allocate_source_for_worker` has one production caller;
- the HA standby reserve narrows through `match_ipv6_dest`.

So the second rule-set is built and never used.

This is NOT a NEG. It has two symptoms: an over-refusal with a false reason, and
a silent dead rule-set when the pools are disjoint. Posted on the issue
(issuecomment-5621852834), and the issue was retitled to the measured defect.

## Timestamp: 2026-09-10 — a prior decision spoke, and it changed the remedy

My issue comment proposed a STRICT duplicate-prefix reject. Before writing it I
read the #5144 cells a reject would red:
- `TestNAT5144NAT64SamePrefixSamePoolAccepted`;
- `TestNAT5144NAT64EquivalentPrefixSpellingAccepted`;
- `TestNAT5144NAT64LeadingZeroMaskSpellingAccepted`.

They accept same-prefix duplicates DELIBERATELY (the #6414 fold), citing "the
Rust nat64.rs canonical-bytes first-match".
- **Their stated premise is wrong:** "resolve to ONE runtime allocator".
- **Their verdict is right:** the second rule-set never mints, so it cannot
  collide.

So a strict reject would overturn a considered decision in order to refuse a
harmless dead rule-set. I also confirmed that warnings reach the operator: the
tail gate appends `validateNAT64PrefixStrict`'s warnings to `cfg.Warnings` in
BOTH modes, and the CLI prints `compiled.Warnings` at commit and check
(`pkg/cli/cli_config.go`).

**Remedy, revised:** align Go's owner model with the dataplane's selection rule,
and make the dead rule-set LOUD rather than rejected.
1. `nat64ShadowedRuleSets` maps each rule-set to the earlier one that shadows it.
2. The #5144 NAT64 owner loop skips shadowed rule-sets, because they never mint.
3. `validateNAT64PrefixStrict` warns, in both modes, that a shadowed rule-set is
   UNREACHABLE, naming the rule-set that shadows it.

The three #5144 cells stay green. Only their rationale comments were corrected,
not their assertions.

## Two traps the helper is built around

- **Config order, not name order.** `compileNAT64` appends rule-sets in
  `namedInstances` (AST) order, and `buildNAT64Snapshots` emits that slice; Rust
  keeps it. The #5144 owner loop sorts by NAME. Choosing "earlier" by name could
  make the rule-set the dataplane never uses the owner, and drop the reachable one
  from the owner set: an UNDER-refusal of a real collision.
- **Conservative on "built".** Rust skips a rule whose prefix address does not
  parse, and skips the WHOLE rule when any pool member fails `parse_pool_v4`
  (#3888). `isNAT64PoolHostAddress` mirrors that parse exactly. A rule-set that
  would be skipped shadows NOTHING, so a later one stays an owner: over-refusal,
  never under-refusal. A missing or empty pool IS built (an empty allocator that
  fails closed), so it still shadows.

## Cells

`pkg/config/compiler_nat64_owner_by_name_9555_test.go` (replacing the
measurement-only file):

| cell | at base `65541cdd0` (fix absent) | with fix |
|---|---|---|
| identical address, two names → accepted + UNREACHABLE warning | **FAIL** (rejected as a double-mint) | pass |
| disjoint pools → accepted + warning | **FAIL** (no warning) | pass |
| same pool → accepted + warning | **FAIL** (no warning) | pass |
| `/96` vs `/096` → still a duplicate + warning | **FAIL** (no warning) | pass |
| CONTROL config-order: Z (pool backs source NAT) before A (sorts first by name) → still REJECTED | pass | pass |
| CONTROL conservative: an earlier rule-set with a `/24` member (skipped by Rust) → the later one stays an owner, its #5144 finding is emitted, and no UNREACHABLE warning | pass | pass |

Base re-verification: I transplanted ONLY this test file onto a detached
worktree at `65541cdd0` and ran `go test -run 9555`. The result was exactly the
four distinguishing FAILs and two control PASSes shown above. The controls pass
in both states by design: they guard the direction a WRONG fix would take (name
order; ignoring "built"), not the defect.

Rust premise, `userspace-dp/src/nat64_tests.rs`:
`nat64_selects_the_first_built_rule_set_for_a_shared_prefix_9555`. It checks
that the first snapshot-order rule-set is selected, and that a rule-set with a
non-host pool member is skipped whole and shadows nothing. If either premise
ever changes, the Go gate's reasoning is stale, and this cell is where that
surfaces.

## Docs

- `docs/config-schema.md`, #5144 section: a #9555 paragraph, and the sentence
  that called equivalent spellings "ONE runtime allocator" corrected.
- `compiler_validate_strict_nat.go`: the #5144 doc bullet for NAT64 owners
  corrected, with the pre-#9555 wording and the measurement that refuted it
  recorded.

## Noted, not in scope

`compiler_validate_strict_nat.go` ~:1325 says a bad NAT64 source-pool entry is
"dropped individually by filter_map(parse_pool_v4)". The current Rust skips the
WHOLE rule (`continue 'rules`, #3888). That is a stale comment in a gate this
change does not touch.

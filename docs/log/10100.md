# #10100 — zeroize interior + rendered-leg symlinks survive

- **Timestamp**: 2026-09-15
  - **Action**: closed the two wipe-time link-following residuals split out
    from the #9897 plan review. R-1: pre-walk `tls/`, `.configdb`
    (both twins), `.old`/`.restore.partial` (+ snapshots via shared
    `zeroizeDBCopyDir`) with `Lstat`; collect ANY interior symlink into
    `Skipped` while still erasing regulars (mirror `master.key`
    inverse-shape report-and-unlink). R-2: `Lstat`+refuse (leave link,
    report `Skipped`) symlinked `frrConf`/swanctl/kea4/kea6; `SymlinkTarget`
    checks in temp-sweep removes; `StripManagedSectionFile` `Lstat`-refuses
    + re-`Lstat` after `ReadFile` + no-resolve write (no
    `WithResolveSymlinks` on zeroize path).

    Measured at `be73b4d79`, RED on base: 3 R-1 grpcapi cells + twin cell +
    3 R-2 snippet cells + temp-sweep + frr-via-rendered + frr primitive all
    returned nil while targets survived (8/8 RED). FRR victim proof: base
    `Strip` through link returned nil and stripped the victim to
    `hostname r1` (privileged modification). GREEN after fix, named cells
    5x + full `pkg/grpcapi` + `pkg/configstore` + `pkg/frr` suites `count=1`.

    **Plan reviews (2 hostile) adjudicated before implementing.** Adopted:
    census before `master.key` check (combo must report both, exact-path
    exclusion — nested `subdir/master.key` proves it), walk errors abort
    with census-incomplete + partial kept, root-link in `Collect` skips
    whole, Strip re-`Lstat` + TOCTOU upgrade to typed `Skipped` via
    re-`Lstat` on Strip error, writer parameterized (no duplication),
    `.restore.partial`/nested/combo cells + R-2 sibling-erasure +
    byte-identical victims. Rejected (out-of-scope/bounded): rendered
    parent-dir + versionsDir checks (intermediate doctrine, not named),
    hardlink detection, absolutize-Target, Lstat-owner fork, per-entry
    unlink, cross-leg Join change — documented as accepted residuals.

    Shared `configstore.CollectInteriorSymlinks(root, exclude)` keeps the
    #9013 lesson (one predicate, both twins + copy dirs). R-1 `Skipped` =
    link-unlinked-target-survives (like `master.key` precedent) vs R-2
    leave-the-link. `sweepFsatomicTemps` now returns `Skipped` (sole caller).
    `frr` keeps no `configstore` import (layering); grpcapi pre-check gives
    typed `Skipped`, Strip plain error is defense-in-depth. Ordinary wipe
    byte-identical (existing #9013 CONTROL + #4585/#5509 suites green).
- **File(s)**: `pkg/configstore/factory_reset.go`,
  `pkg/grpcapi/server_diag_zeroize.go`,
  `pkg/grpcapi/server_diag_zeroize_snapshots_9236.go`,
  `pkg/frr/manager.go`, `pkg/grpcapi/zeroize_10100_test.go`,
  `pkg/configstore/factory_reset_10100_test.go`,
  `pkg/frr/strip_10100_test.go`, `docs/log/10100.md`

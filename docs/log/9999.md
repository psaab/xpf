# Issue #9999: ChildBase alphabet documentation

- **Timestamp**: 2026-09-17T12:48Z
- **Action**: corrected the `pkg/ipsecname.ChildBase` docstring to match the existing implementation. ASCII letters, digits, `-`, and `_` pass through; every other rune, including `.`, maps to `-`; empty input maps to `traffic-selector`. The runtime switch was not widened.
- **Action**: removed the stale `Disambiguator` wording that called a truncated hash injective. It now documents the hash as a suffix and points to `ChildNames`'s repeated-suffix handling for rendered-name uniqueness.
- **Regression**: `TestChildBaseAlphabet9999` pins the pass-through and replacement mapping table, including `.` -> `-`, along with the empty-input fallback. `TestChildBaseOutputStaysInAlphabet9999` pins that hostile input cannot produce a rune outside `[A-Za-z0-9_-]`.

## RED -> GREEN proof

- **RED**: with the regression cells present, `ChildBase` was temporarily changed to preserve `.`—the behavior asserted by the old docstring's `[A-Za-z0-9._-]` claim. `go test -count=1 -run 'TestChildBaseAlphabet9999|TestChildBaseOutputStaysInAlphabet9999' ./pkg/ipsecname` exited 1. The table reported `a.b`, `...`, `t.s`, and the mixed selector retaining `.`, and the output-alphabet cell reported `.` outside `[A-Za-z0-9_-]`.
- **GREEN**: after restoring the original runtime switch and correcting the documentation, the same targeted command exited 0. The complete `go test -count=1 -v ./pkg/ipsecname` suite passed: 8 tests, 8 passed, 0 failed.

**Files**: `pkg/ipsecname/ipsecname.go`, `pkg/ipsecname/child_base_9999_test.go`.

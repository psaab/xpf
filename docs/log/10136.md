# Issue #10136 — Routing-instance separation wire gate

## Implementation

This branch adds the `wire_routing_separation` gate and adapter, with a hermetic
`--selftest`/`--fixture` path and a live path guarded by the cluster lock. The
live path snapshots the complete normalized configuration on both firewall
nodes, provisions an owned temporary `routing-instances wire-10136` stanza
through the CLI/xpfd manager, waits for the manager-created `vrf-wire-10136`
and pref-2000 l3mdev terminator, runs the same-source/port control and probe
window, then removes the veth/namespace and commits the owned RI deletion.

The Makefile wraps the complete `harness-result.sh run` invocation in
`with-cluster.sh`, including binary attestation. The live row is intentionally
not claimed as measured in this branch: the shared lab is blocked by #10342.

## Hermetic evidence

- `./test/incus/wire-routing-separation.sh --selftest`: 9 passed, 0 failed.
- `bash test/incus/harness-result-selftest.sh`: 187 passed, 0 failed.
- `sh scripts/selftest-census.sh`: 34 discovered/invoked, 58 Python files guarded,
  8 odd self-tests declared.
- `bash test/incus/selftest-census-selftest.sh`: 17 passed, 0 failed.
- `python3 -m py_compile test/incus/wire_routing_probe.py`: passed.

## Live evidence and block

Two live preflight attempts refused the unsafe baseline before any temporary
configuration mutation because the DUT had configured routing instances but no
pref-2000 l3mdev terminator. Their complete config archives are:

- `/var/tmp/xpf-wire-routing-separation-1789729350` (env-VOID)
- `/var/tmp/xpf-wire-routing-separation-1789729374` (env-VOID)

The retained `/var/tmp/xpf-wire-routing-separation-1789729308` archive is
from the earlier broken-fixture/harness attempt, before the env-preflight
diagnostics, and is not counted as an env-VOID.

Each env-VOID and the owned attempt recorded the same normalized fw0 pre/post
SHA:

`f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91`

The owned-config diagnostic attempt used archive
`/var/tmp/xpf-wire-routing-separation-1789729613`. It proved the managed
fixture and fault landing: `vrf-wire-10136` was created with table `210564`,
pref-2000 was present, the clean lookup was unreachable, and the injected
fault lookup fell through `ge-0-0-2.80`. The capture measured
`probe=1000`, `control=1500`, `probe_observed=1000`, and
`control_observed=1500`; this is diagnostic evidence only, not a valid FAIL
row. For the owned archive specifically:
`pre_fw0_sha=f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91`
and
`post_fw0_sha=f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91`.

The finalizer correctly emitted `harness-void` because deleting the temporary
RI could not restore the pre-run kernel rule snapshot. The pre-run config
already declared `sfmix` as a virtual-router, but its manager state lacked
pref-2000. Creating the temporary RI caused xpfd to self-heal the desired
pref-2000 rule; deleting only the temporary RI therefore left the manager-owned
`vrf-sfmix` and pref-2000 present. A locked post-check confirmed:

- `2000: from all lookup [l3mdev-table] unreachable`
- `vrf-sfmix` present
- normalized fw0 pre/post config SHA unchanged as above

No direct `ip rule` repair was performed and no invalid live ledger rows are
retained.

## Post-repair live run

Baseline repair is tracked by #10342: lab-blocked by #10342, with live
PASS/FAIL pending baseline repair. After #10342 repairs the baseline, the
live program will run two locked cases of `make test-wire-routing-separation`
and record a normal PASS for the clean case (probe contained, control
observed) and a fault-injected FAIL for `WIRE_BROKEN_FIXTURE=1` (probe falls
through to the proven main route), each with its config archive and restore
verification. This PR therefore uses `Refs #10136` rather than closing the
issue.
## 2026-09-19 current-master locked attempt — LAB/BLOCKED

The baseline repair was deployed and the live pair was run under one
`with-cluster.sh` cell at `2026-09-19T08:33:49Z` from master
`3e89948fcd0c1376417757cdf580d6d56a8e39a3`.  The raw deploy completed with
`DEPLOY_RC=0`.  Both loss nodes ran the same `xpfd` build
`c875e0539295de859662a1d1a1806c2aed804b0e3393425392c6cb5913cc0d8d` and the
same `xpf-userspace-dp` helper
`bfe6d6a9f83318b30bd67934b4814cfbfa40d744cb04f136acff1ca1ac36792d`; the
ledger attestation is `exe_check=MATCH`, `exe_scope=both` for both rows.
The deploy left node0 primary, and both nodes had IPv4/IPv6 pref-2000
`l3mdev-table unreachable` rules plus only `vrf-sfmix` (488570) and
`vrf-mgmt` (999).

The clean leg (`make test-wire-routing-separation`) emitted
`offered: probe=1000 control=1500 observed: probe=0 control=0 cksum_bad=0`
but its finalizer demoted the result to `WIRE_GATE ... VOID reason=harness-void`
and ledger row `3058f389fc1b55fd` at `2026-09-19T08:35:13Z`.  Its archive is
`/var/tmp/xpf-wire-routing-separation-1789806889`.  The fw0 normalized
pre/post configuration SHA was equal at
`f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91`.  The
fw1 pre SHA was
`45132d9dafd339542871d269b23091b3e87c95c43f38c5446fae8e97e4a4c668` and its
post SHA was `f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91`;
the complete diff is only a reorder of the existing
`set security flow allow-dns-reply` and `set security flow allow-embedded-icmp`
lines.  This ordering drift made the exact restore check VOID; it is not
claimed as a measured PASS.

The fault-injected leg (`WIRE_BROKEN_FIXTURE=1`) emitted
`offered: probe=1000 control=1500 observed: probe=0 control=0 cksum_bad=0`
and ledger row `bee939a02f42eba2` at `2026-09-19T08:35:38Z` recorded
`VOID reason=capture-blind` (not a FAIL).  Its archive is
`/var/tmp/xpf-wire-routing-separation-1789806913`; both nodes' normalized
pre/post SHA was the equal
`f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91`.
The broken-leg finalizer completed its exact rule/link/netns/table/config
restore check.  The post-pair assertions found no `wire-10136` device,
namespace, route-table residue, or configuration path, and the pref-2000
rules and managed `sfmix`/`mgmt` VRFs remained present.  The peer-side capture
still observed zero control frames, so neither leg produced the required
green PASS/expected-FAIL pair.

The lock was released after the broken-leg restore.  These two VOID rows are
retained as measured lab evidence; no coverage declaration is removed and
this blocked follow-up uses `Refs #10136`.

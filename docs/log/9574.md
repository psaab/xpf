# #9574 — `any-ipv4` / `any-ipv6` captured by an object named `0.0.0.0/0` / `::/0`

- **Timestamp**: 2026-09-10
  - **Action**: Re-verified at origin/master `a3a6f5906` with throwaway probes
    (removed). `config.SchemaValidate`, `config.CompileConfig`,
    `config.CompileConfigLenient` and `configstore.CheckText` all accepted
    `address 0.0.0.0/0 10.99.0.0/16` with zero warnings, and the compiled
    token was already `0.0.0.0/0`. Snapshot: SrcBookIDs=[id] SrcLiterals=[].
    `policymatch.Match` returned DefaultUsed (permit-all) for 10.0.1.5. The
    same held for `::/0` + `any-ipv6`, for an address-set named `0.0.0.0/0`,
    and for a zone-local entry (compiled token `zone-local/trust/0.0.0.0/0`).
    Controls: value equal to name is inert; an entry named `any-ipv4` does not
    capture; name-before-literal holds for `10.0.1.0/24`.
  - **File(s)**: none (probes deleted)
- **Timestamp**: 2026-09-10
  - **Action**: Stacked on #9523. Removed the compilePolicy rewrite. Added
    `PolicyAddressWildcardFamilies` (the single keyword source) and
    `PolicyAddressKeywordLiteral` (the wire literal). The classifier and the
    legacy expansion write the CIDR after keyword classification. The
    zone-local rewrite, addrRepresentable, the simulator and CLI detail use
    the shared helpers.
  - **File(s)**: pkg/config/policy_address_keyword_literal_9574.go,
    pkg/config/policy_address_keywords_9523.go,
    pkg/config/compiler_security_policy.go,
    pkg/config/compiler_security_addressbook.go,
    pkg/dataplane/userspace/policies_addrbook.go,
    pkg/dataplane/userspace/capabilities.go,
    pkg/dataplane/userspace/policies_lower.go, pkg/policymatch/policymatch.go,
    pkg/cli/cli_show_security.go
- **Timestamp**: 2026-09-10
  - **Action**: Docs updated (feature-gaps, pkg/config README, pkg/policymatch
    README, the #3958 test comment).
  - **File(s)**: docs/feature-gaps.md, pkg/config/README.md,
    pkg/policymatch/README.md, pkg/config/compiler_addrbook_warn_3958_test.go
- **Timestamp**: 2026-09-10
  - **Action**: Measured what the move breaks before touching tests. Of
    pkg/config, pkg/dataplane/userspace, pkg/policymatch, pkg/cli,
    pkg/grpcapi, pkg/api and pkg/configstore, exactly two cells failed: the
    #2008 H11 compile-time pin (its reason, a parseable CIDR on the wire,
    still holds at the new position) and the stacked #9523 classifier cell
    (the wire literal is now the canonical CIDR). Both were updated with
    their rationale. Added #9574 cells in pkg/config (helpers, zone-local on
    both channels), pkg/dataplane/userspace (byte-identical wire, capture
    rows, typed-literal name control), pkg/policymatch (verdicts, family
    scope, name control) and pkg/cli (detail rendering).
  - **File(s)**: pkg/config/policy_match_excluded_test.go,
    pkg/dataplane/userspace/address_keyword_before_name_9523_test.go,
    pkg/config/policy_address_keyword_literal_9574_test.go,
    pkg/dataplane/userspace/any_ipv4_keyword_literal_9574_test.go,
    pkg/policymatch/any_ipv4_keyword_capture_9574_test.go,
    pkg/cli/policy_detail_family_keyword_9574_test.go
- **Timestamp**: 2026-09-10
  - **Action**: Full `make test` on the tree rebased onto e4544f78d. The Go leg
    passed. The Rust leg failed one cell, `cos_doc_drift_guard`, with
    `scan_dir(/var/tmp/wt-9523/userspace-dp/src/afxdp): No such file or
    directory`. The cause was my own environment, not this change: I had
    moved the userspace-dp target dir from the #9523 worktree into this one
    to keep the build warm, and cargo reused that integration-test binary,
    whose CARGO_MANIFEST_DIR still named the removed worktree. I deleted the
    borrowed target and ran `make test-rust` from a clean build: 5747 passed,
    0 failed, `cos_doc_drift_guard` ok.
  - **File(s)**: none

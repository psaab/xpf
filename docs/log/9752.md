# #9752 — PBR `then routing-instance` session keeps its installing table

Branch `fix/9752-pbr-table-id`, base `origin/master` `9184222dd` (2026-09-14).
Issue: a steered session stores no table, so synced hits, synced import,
owner-RG refresh and unusable-resolution hits re-resolve in `inet.0`.

- **Timestamp**: 2026-09-11 (issue) → 2026-09-14 (PR opened)
  - **Action**: carry the installing-table identity in `SessionDecision` +
    HA sync wire; validate-before-use on every re-resolve; terminal
    `TableUnavailable` for unresolvable stamps; paced retirement walk;
    counters end to end; 17 divergent-fixture cells.
  - **File(s)**: userspace-dp session/entry + routing_domain_wire
    (identity), afxdp/forwarding_build/install_tables (registry),
    forwarding/pbr (miss stamp), session_glue (D4 classifier, import,
    refresh, demote, purge), forwarding/{fib,ha,fabric} (threading),
    poll_descriptor (miss + flow-cache validation), worker loop (purge
    hook), binding_state + disposition + types + worker (counters),
    event_stream + protocol/binding (wire), Go api metrics +
    dataplane binding, `pbr_install_table_9752_tests.rs` (cells).

## STEP-0 (base `9184222dd`)

VERIFIED PRESENT, not ALREADY_FIXED: 2-field Copy `SessionDecision`;
table-less `lookup_forwarding_resolution_for_session_with_cache`; import
(`upsert_synced.rs:75`) and refresh (`refresh_owner_rgs.rs:130`) write-backs
without table input; synced-hit forced re-resolve; miss passing
`route_table_override`. Full record in plan v1 §1.

## Plan evolution (v1 → v4.1 FINAL)

- v1: identity + registry + stamp rule + re-resolve threading.
- r1 both NEEDS-MAJOR (GLM 3+6, Codex 6+coverage) → v2: terminal
  disposition (NoRoute ≠ fail-closed — kernel delegation), purge walk,
  sender memo + keep rule, ProtocolVersion 17, staging fixed.
- r2 GLM READY, Codex NEEDS-MAJOR (9) → v3: H2 owner-check, withhold,
  flag purge, narrowings, v17→C4, honest mixed-version contract.
- r3 GLM READY (+7), Codex NEEDS-MAJOR (6) → v4: H2 corrected
  (2⁻⁶² → 2⁻⁵¹·⁸ accidental-only; "trusted names" model), D4 reorder
  (validate FIRST), one-line publish conjunct, ONE classifier,
  fence + conditional replicate, bilateral note.
- r4 GLM READY (+4 nits folded), Codex NEEDS-MAJOR (3 refinements folded
  as directives) → **v4.1 Final**: companion backlink validation (DNAT
  aliasing breaks uniqueness), store-bulk REFUSE (withhold-inside-window
  dissent conceded — BulkEnd-ack would lie), domain-fanout clear
  recovery. Zero `PLAN-KILL` in 4 rounds; split verdict closed by
  commitment (GLM READY ×3; Codex 6→9→6→3, zero direction challenges
  since r2). Raw outputs uncommitted under `docs/pr/9752/reviews/`;
  plan + verdicts posted as issue comments 2026-09-14T21:04Z.

## Implementation (C1 → C4 + cells)

- C1 (`086174e33` post-rebase): `(install_table_domain, install_table_check)` on
  `SessionDecision` (Copy kept, +8B); FNV-1a/64 identity keyed by
  INSTANCE name (Go vectors both languages); presence registry
  (routes + connected + local); miss stamp rule (tunnel/precedence →
  (0,0)); PBR stamp matrix. NOTE (code-review fold): C1 shipped the
  RECEIVE side only (import reads the fields); the transmit chain
  (binary tails, JSON leg, Go decode/mapping/cluster wire/builders,
  v18) landed in the fold commit below — the log line that once read
  "HA wire tails + JSON + convert + v17" was aspirational and wrong.
- C2–C4 (`a7600d203` post-rebase): D4 classifier (stamp-0 bit-exact; stamped
  validated first; interface-NAT/any-table locals; terminal
  `TableUnavailable` with the #6664 checklist); threading through
  import/refresh/demote/flow-cache/fabric/miss-validation; retirement
  walk (`InstallTablePurge`: flag → paced resumable walk →
  generation-revisit → local pair teardown + fenced shared removal +
  conditional `DeleteSyncedIfTableUnknown` + backlink companions);
  `table_unavailable_{packets,drops}` dataplane → wire → Go;
  required call-site/pin/fixture updates.
- Cells (`2dff7293e` + fold): 22 tests, divergent lan/blue/default fixture.

## Cell table (`pbr_install_table_9752_tests.rs`, 22/22, 5× green)

| # | Cell | Proves |
|---|---|---|
| C1 | synced_hit keeps stamp | synced hit re-resolves blue (egress 12), stamp kept |
| C2 | synced_import | import recomputes in blue, installs stamped |
| C3 | owner_rg_refresh | refresh recomputes blue (HAInactive-gated, egress 12) |
| C4 | real seed + reseed | miss lookup → MissingNeighbor, real stamp fn, seed origin, dynamic learn, hit path → blue |
| C4b | name resolver | stamped → blue.inet.0, unstamped → None |
| C5 | reverse control | reverse unstamped in default (PBR is directional) |
| C6 | demote preserves | failover path keeps the stamp |
| C7/C10b | tunnel bypass | tunnel arm table-independent, never a blue FIB result |
| C8 | unstamped control | zero stamp → default bit-exact (egress 13) |
| C10 | terminal matrix | unknown/mismatch/family-absent → TableUnavailable, ifindex-0 counter unmoved |
| C10 | any-table / iface-NAT | table-independent locals win over terminal |
| C11 | ECMP in X | per-flow spread across blue members, all egress 12 |
| C12 | validation | FabricRedirect invalid iff the X-table candidate appears |
| C13 | retirement | vanished table → walk purges + evicts the key |
| C14 | NAT64 | v6→v4 rewrite reads family off the POST-NAT target |
| C15 | precedence pins | zero-stamp bypass vs stamped-validate-first |
| C16 | refused replicate | full queue → drop counted, prefix intact |
| GPT-2a | re-add skip + flush | latest-blue pre-check skips all; empty flush preserves shared, queues nothing |
| GPT-2b | fence decline + flush | stamp-mismatch declines; no close emitted; flush preserves shared |
| GPT-2c | rejected companion + flush | ambiguous companion survives flagged close; peers get only the conditional |
| GPT-3 | re-arm starvation | 300 stale + re-arm every pass still completes past budget 256 |

Fixture lessons (all real failures, fixed in the cells): identity is
keyed by instance name (`"blue"` → 525590, not `"blue.inet.0"`);
the validation arm enforces before matching (needs active HA);
presence includes connected routes (rotation re-homes the interface);
reverse is a default-table control, not an inherit.

## Wire cells (review fold; all fail pre-fix by mutation/stash check)

- Rust binary tail (`codec_tests`): open+update carry (domain,check) as the
  final 8 bytes; zero-stamp encodes (0,0). Existing end-relative readers
  re-discounted (+8); shared #9412 golden regenerated (length +8, zero tail).
- Leg agreement (`tests_session_delta_json`): JSON↔binary agree on the pair
  (FAIL-ON-REVERT style); e2e import installs the PRODUCER-emitted stamp
  through the real `SessionSyncRequest` wire form (no hand-stamped import).
- Go: binary decode incl. legacy truncations, delta→value mapping V4,
  cluster-wire round-trip V4/V6 + legacy, builders V4/V6 + control,
  golden-tail decode, wire-key lockstep (4 pairs), FNV literal pins.

## Gates (real numbers, this worktree — fold tip)

- `cargo test --manifest-path userspace-dp/Cargo.toml`: full suite
  green (5915 passed main binary + all integration binaries incl.
  canary 31/31; 1 failure found and fixed in the fold: #9412
  update-tail assertion re-based 8 bytes past the new tail).
- `cargo check` (production config): green.
- Go: `pkg/dataplane/userspace`, `pkg/daemon`, `pkg/cluster`,
  `pkg/dataplane`, `pkg/config`, `pkg/api` all green (pins moved
  with the wire: digest cell 17→18 same golden, lockstep literal,
  secure-tunnel pin, scrub counts 39/40→41/42 + classification,
  truncation fixtures +8 with legacy intent preserved).
- New cells 5× consecutive: 22/22 each.
- G3 cluster smoke: (B) control attempted 2026-09-14 — BLOCKED on
  #10046 (FBF-steered traffic match+drops on pin-56, zero wire
  presence); cluster left verified-clean, carrier 42→42. Full cell
  re-queued post-merge per parent; #10046 may block again.

## Code-review fold (parent review: GPT + GLM NEEDS-MAJOR, agreed)

1. **HA never transmitted** (both reviewers): C1 had import-side fields
   only; binary codec, JSON export, Go structs omitted; version stayed
   16. FIXED by completing the chain (this lane chose full fix over
   the offered local-only rescope): Rust binary tail + JSON leg +
   fingerprint (automatic both sides) + Go binary decode + mapping +
   sync-only SessionValue fields (NOT node-local per #7097 pin) +
   cluster-wire tails + SessionSyncRequest + builders + ProtocolVersion
   18 both planes (rebased onto `18f5f9a78` first; #9587 had claimed
   v17 on master, so v18 per the v8 rule). Resync verified through
   the shared producers (open/update encode + delta info). Validated
   by producer-driven e2e cells on both sides.
2. **Purge Close bypassed the fence downstream** (GPT-2): teardown
   emitted ordinary Closes even on decline, and the drain derived
   reverse + replicated repairing deletes unconditionally. FIXED:
   fenced teardowns return removal and gate the close; latest-view
   pre-check skips re-adds with zero churn; replicate + companion
   gated on forward removal; new `SessionDelta.purge_retirement`
   flag skips drain reverse-derivation and sibling replicate (HA,
   RT_FLOW, RPC, recent-deltas still fire). Regressions drive the
   real flush (re-add, fence-decline, rejected-companion).
3. **Re-arm starvation** (GPT-3): `arm()` reset the cursor mid-walk.
   FIXED: retained while running (rotation revisit still owned by
   step); 300-entry + per-pass re-arm cell.
4. **Hand-stamped reseed cell**: C4 called the resolver directly.
   FIXED: C4 now runs miss lookup → stamp fn → seed origin →
   dynamic learn → hit path (name asserts split to C4b).

## Commits (post-rebase onto `18f5f9a78`, clean)

1. `086174e33` userspace-dp: carry installing table identity in
   SessionDecision (#9752) — C1 (receive side + identity + registry).
2. `a7600d203` userspace-dp: re-resolve sessions in their installing
   table (#9752) — C2–C4 behavior + retirement + counters + pins.
3. `2dff7293e` test: installing-table session cells, divergent
   inet.0 (#9752) — 17 cells.
4. `2efecb0b1` docs: full PR history (#9752).
5. `deec065d7` test: stamp new-master revocation fixtures (rebase
   fallout, mechanical).
6. `104cab1ac` userspace-dp+Go: fold code-review findings, complete
   HA transmit, v18 (#9752) — items 1–4 above.
7. `6873a9084` docs: fold history + v18 + final gates (#9752) — this
   file, first edition.
8. `a2627363a` + `63e5aa491`: fold-2 behavior/tests/fixtures, then
   this section's second edition (docs).

## Fold-2 (parent-directed: GW audit + 4(b) sinks + chains; follow-up #10068)

1. **GW audit, all 7** (Go-wire legs): update-frame #9412/#3301 belt,
   close/wire-gap sweep halves, SIGHUP RADV, STATS DNAT, resync-export
   carry — all fixed with RED-capable cells. GW-4 (pre-existing
   close-carry gap on master, independent of #9752) documented, not
   folded; tracked as #10068 cell 1.
2. **4(b) sinks**: drain-fallback TTL/multi-count honored; close-frame
   marker byte (Rust encode + Go decode cells); JSON purge-retirement
   leg at parity; QueueDelete→wire→journal marker; forward-only
   suppress for incapable peers (metric
   `DeletesSuppressedPurgeRetirement`). One seam documented: the
   1-line prod-sink glue neither package can observe (#10068 cell 5).
3. **Chains (no hand-stamped seams)**: golden UPDATE fixture now
   carries a real stamp (525590, 3318534811) — shared bytes regen'd,
   Go asserts the pair; C2 merged producer→import→re-resolution
   (caught a real envelope skew: `addr_family` 4 vs wire 2, fixed);
   cluster composes gen-guard + old-sender resend + keep-rule in one
   cell. v6 mirrors deferred to #10068 cells 2–4 (v4 pins every
   contract).
4. **Gate fallout fixed in-fold**: close-frame end-relative reads
   (#7239/#7188) discount the marker byte; `protocol_wire_v1.json`
   regen'd (diff: exactly `+purge_retirement: false`).

## Gates (fold-2 tip, this worktree)
- `cargo test userspace-dp --bin xpf-userspace-dp`: 5917 passed,
  0 failed, 6 ignored (105.7s). Pre-fix full run found 3 failures
  (close end-relative reads + wire fixture), all fixed in-fold.
- Go: `pkg/cluster` ok 93.4s; `pkg/dataplane` ok 20.9s;
  `pkg/dataplane/userspace` ok 50.2s; `pkg/daemon` ok 83.7s;
  `pkg/conntrack` ok 0.2s (all `-count=1`).
- Sibling-lane notify: broadcast shared touches (golden regen +
  `eventstream_test.go` append); 9872 + 10064 confirmed no overlap.

## Round 3 (parent review delta2: GPT NEEDS-MAJOR + GLM NEEDS-MINOR, adjudicated for GPT)

Methodology mandate honored throughout: every new cell runs on production
shapes (lossy-mirror reads, worker-cached snapshots, real HA receive/apply,
real sweep, full wire envelopes), and every behavior cell is
mutation-verified (the exact revert/stash that must redden it is recorded
per item below).

1. **BPF mirror stamp loss** (Go + Rust). The keep-rule read the installed
   row back from the BPF mirror, which drops sync-only fields — dead in
   production, green only against full-struct doubles. FIXED: receive-side
   in-memory record (`installTableRecvV4/V6` under `recvGenMu`, consulted
   in `installClusterSyncedV4/V6` before the dataplane write, evicted on
   both delete directions); the store keep-rule REMOVED (was dead code);
   sweep resends restore from the receive record (`lookupRecvInstallTableLocked`
   in the stamp path — the e2e below caught this missing handoff); Rust
   `upsert_synced_with_origin` preserves the previous stamp on a (0,0)
   same-incarnation re-import via single-sourced
   `preserved_install_table_for`, and `handle_upsert_synced` resolves with
   the EFFECTIVE stamp (resolving with the incoming (0,0) installed a
   default-table resolution under the preserved stamp — caught by the C2
   resend leg). Cells: cluster lossy-mirror suite (recorded-Set
   observable) + 3 Rust reimport cells. RED: stash the recv wiring → lossy
   test fails; stash the Rust preserve → preserve cell fails; neuter the
   effective resolve → C2 resend leg fails. ALSO FOUND: the fold-2 composed
   test stripped a header from a headerless payload (garbage key, vacuous
   pass) — deleted, subsumed by the lossy cells with key-equality guards.
2. **Mixed-version install fence.** Stamped installs flowed to old peers,
   which install stamp-less and wrong-table after failover. FIXED: new
   `capFlagInstallTableIdentity` (1<<3); `suppressStampedInstallForIncapablePeer`
   withholds STAMPED installs only (unstamped flow; unlearned never gated),
   wired into all four queue fns after the memo stamp; metric
   `InstallsSuppressedNoPeerInstallTable` + incarnation-scoped warning.
   Cells: withhold/capable/unstamped/unlearned quartet on sendCh.
3. **Stale drain truth.** The purge-close guard re-checked against the
   worker-CACHED snapshot (table re-adds invisible), had no incarnation
   fence, and flow-cancel (which DROPS, not requeues) ran before the guard.
   FIXED: `purge_retirement_close_is_stale` (fresh-forwarding predicate +
   primary-shared-map presence — aliases excluded: a non-NAT forward's own
   key doubles as its forward-wire alias, which removal deliberately
   leaves) shared by `flush_session_deltas` (new `shared_runtime` param)
   and `purge_queued_flows_for_closed_deltas` (same fence before cancel);
   pure `purge_close_cancel_keys` pins the gating (a `BindingWorker` needs
   real XSK rings — the documented seam is the 3-line fence→helper wiring,
   reviewed by eye). Cells: production-ordering regression (stale snapshot
   + fresh reader), each leg isolated (table-only, presence-only),
   negative control (live close emits the HA+RT_FLOW pair), cancel-keys
   matrix. RED: each leg + control mutation-verified independently. (The
   pre-flush companion exclusion the finding asked to gate was already
   present; the forward-cancel-before-guard was the real gap.)
4. **HA helper forward-only gap.** The helper request carried only
   `peer_delete`; the helper derived/removed/queued the reverse. FIXED:
   `forward_only` end to end — Go structs + wire key, deleter interface
   (batch + single) + contract + mirror + both managers (batch marks,
   single marks AND skips the companion request), store miss-path
   threading; Rust request field + handler + `delete_*` param threading,
   `reverse_key=None` when set (covers shared removal + worker fan-out).
   Wire fixture regen'd (diff: exactly `+forward_only: false`). Cells: Go
   store-mark test, socket-level request-mark tests, wire-key lockstep
   twin, Rust forward-only/control pair on the #9714 fixture. RED:
   `if forward_only`→`if false` reddens the Rust cell.
5. **True e2e.** Go ONE test: delta→`QueueSessionV4`→sendCh bytes→decode→
   `installClusterSyncedV4` (lossy dp)→real `syncSweep`→bytes→decode→
   fresh-node install, key asserted at every decode. Rust C2: full
   production envelope (every always-sent key; MACs omitted exactly as
   `macString`+omitempty omit them for an unresolved session — a complete
   carried resolution would take the cached fast path and never touch the
   table) → import → blue → (0,0) same-incarnation resend → stamp kept +
   blue kept. Connectors: golden bytes, per-key locksteps, new Go
   builder-shape test (pins the envelope C2 consumes). RED: handoff mutant
   fails at the RESEND leg; envelope skew fails the builder cell.

## Gates (round-3 tip, this worktree)

- `cargo test userspace-dp --bin xpf-userspace-dp`: 5926 passed,
  0 failed, 6 ignored (115.8s).
- Go: `pkg/cluster` ok 93.7s; `pkg/dataplane` ok 20.6s;
  `pkg/dataplane/userspace` ok 50.9s; `pkg/daemon` ok 85.1s;
  `pkg/conntrack` ok 0.2s (all `-count=1`).
- New cells 5x: Rust drainfence/preserve/delete/C2 suites + C2 alone
  5/5; Go cluster/dataplane/daemon/userspace 9752 suites 5/5.

## Round 4 (parent review delta3: GPT NEEDS-MAJOR; delta-3 sub-points (a)-(d))

1. **Fence covers every path + discovery.** Sweep and bulk bypassed the
   fence (own stamp→encode→send loops); undiscovered peers passed through.
   FIXED: sweep skips suppressed sessions (intentional, retried next
   tick); bulk ABORTS mid-window on first refusal (no BulkEnd → no
   reconcile/hold-release/ack; partial upserts idempotent) with a
   per-incarnation latch (`bulkFencedForPeer`, reset on disconnect,
   cleared on capable discovery) so redrives skip quietly instead of
   burning walks; fence default-DENIES while unlearned (installs repeat
   via sweep, so discovery-deny delays, never drops — unlike one-shot
   deletes). Peer VRRP hold stays until bulk completes or the sync-ready
   timer degrades promotion (fail-closed, loud via metric + warn).
   Cells: sweep skip/flow pair, bulk abort (sentinel + no-BulkEnd +
   no-reconcile + latch) / complete pair, discovery withhold-then-flow.
   RED: stash sweep fence → skip test fails; neuter bulk fence → abort
   test fails (completes).
2. **Authoritative shared-install preservation.** A locally-originated
   stamped session never entered the Go receive memo (and the memo
   declines at capacity), so after demotion an old resend overwrote
   shared authority with zeros (worker preserve repairs only
   worker-local). FIXED: `upsert_synced_session` preserves a stored
   stamp on a (0,0) same/unknown-incarnation import, before publish
   (same rule as the worker twin; placed after reverse synthesis so
   companions stay (0,0)). Cells: ha-fixture preserve/control pair. RED:
   neuter → preserve cell fails.
3. **Atomic drain authorization.** Guard released its lock; drain blocked
   on emit, then removed unconditionally — a concurrent republish was
   destroyed. FIXED: table leg (fresh ArcSwap load) up front, then
   `remove_shared_session_if(|_| false)` — any present entry is by
   definition a post-purge republish (purge removes on accept, emits
   nothing on decline) → decline-and-drop, all under one hold. Later
   unconditional shared removal + coordinator release SKIPPED for purge
   closes (already handled; re-removing would reopen the race). The
   pre-flush cancel predicate stays (necessary for republish-before-
   cancel; insufficient alone — documented). Regression restructured to
   publish AFTER the guard's table answer. Sub-point (d): ordinary-close
   bypass cell (full flush + replication despite both stale conditions).
   RED: `|_| true` mutant kills exactly the presence cell (bypass still
   passes). Sub-point (c): guard reads = fresh `shared_runtime.load()`
   (lock-free) + primary shared map under the removal's own hold;
   sub-point (b): cancel honors `purge_close_cancel_keys` (matrix cell).
4. **Genuine single e2e (golden-file chain).** A single in-process test is
   unbuildable (daemon↔cluster import cycle both directions; cluster
   receive/observe surface unexported) — so the ONE chain hands verified
   bytes across the seams, the established golden pattern: R0 Rust
   `session_delta_info` → `pbr_delta_9752.json` → G1 daemon walk →
   `synced_val_9752.json` → G2 cluster queue/wire/install/sweep/
   reinstall → `installed_val_9752.json` → G3 builder →
   `synced_request_9752.json` → R1 Rust import→blue→resend→blue. No manual
   construction at any hop (each input is the prior hop's asserted
   output); resend legs in G2 (sweep) and R1 (programmatic old-sender
   transform). Lossy fixture now drops EXACTLY the 9 (+1 v6) sync-only
   fields, pinned both sides (production round-trip + fixture mirror
   cells). Production losses honored, not papered: RTFlow absent end to
   end (unknown-id rule; pre-existing #5212 sweep gap FILED as #10103),
   Fib scrubbed per #7097 (helper re-derives; pinned). RED: handoff
   mutant fails at RESEND; resolve mutant fails R1. Found along the way:
   G2 handed the companion (fixed: forward row), C2 lost its #[test] in
   round 3 (restored + verified via --list).

## Gates (round-4 tip, this worktree)

- `cargo test userspace-dp --bin xpf-userspace-dp`: 5930 passed,
  0 failed, 6 ignored (107.0s).
- Go: `pkg/cluster` ok 94.0s; `pkg/dataplane` ok 20.7s;
  `pkg/dataplane/userspace` ok 49.6s; `pkg/daemon` ok 83.3s;
  `pkg/conntrack` ok 0.2s (all `-count=1`). One fallout fixed
  in-fold: the round-2 sweep-memo fixture never learned caps, so
  round-4's discovery-deny withheld its announces (fixture now learns
  capable caps; fence behavior itself is pinned by its own cells).
- New cells 5x: C2 5/5; 19-cell Rust batch (drain/preserve/delete/
  producer) 5/5; Go cluster/dataplane/daemon/userspace 9752 suites 5/5.

# #9725: at boot the transit gate opens kernel forwarding before any interface has an XDP program

- **Timestamp**: 2026-09-11 ~12:40 UTC
  - **Action**: Read the thread; the issue body is the spec, and it has no comments. No branch, PR or worktree existed. Created `/var/tmp/wt-9725` on `fix/9725-transit-closed-until-attach` from origin/master `fbc51cc02`. Researched the boot order and the gate, and drafted a design that opened transit once the #7191 arm-coverage proof reported every surface covered.
  - **File(s)**: none

- **Timestamp**: 2026-09-11 ~13:30 UTC
  - **Action**: Rejected the #7191 proof as the open signal after checking it in production.
    - **Not reachable.** A probe at `fbc51cc02` shows that the published runtime, `*userspace.LegacyDataPlaneAdapter`, does not satisfy `armCoverageSource`. So `evaluateArmCoverage` never reads a proof on a production build.
    - **Never complete on a GRE node.** Both nodes of the live loss cluster log `uncovered=1` on every apply: the #8279 raw-L3 refusal of `gr-0-0-0`. A gate keyed on complete coverage would never open there.
    - **Filed.** Both findings are #9804. The issue itself allows the other shape: open at the first successful apply after the arm.
  - **File(s)**: none

- **Timestamp**: 2026-09-11 ~14:00 UTC
  - **Action**: Implemented.
    - **The rule.** Kernel transit is open only while the dataplane is armed AND an `rt.ApplyConfig` has succeeded since that Start (`transitOpen`). The reasoning is stated once, in `pkg/daemon/transit_closed_until_attach_9725.go`.
    - **Bring-up.** `applyBootTransitPolicy`'s default branch applies the host forwarding posture and closes both legs (`closeTransitUntilFirstAttach`). It records nothing about the arm and leaves the #7178 RG arm track alone.
      - `enableForwarding` became `applyHostForwardingPosture`, which no longer writes the transit knobs.
      - Its sysctl map is a package var, so a test can point it at a temp dir.
      - The fsatomic canary allowlist follows the rename.
    - **Arm.** `markDataplaneArmed` records the Start, clears the attach record and writes the gate, which stays closed.
    - **Open.** `markFirstAttachApplied` runs right after a successful `rt.ApplyConfig` in `applyDataplaneAndHACore` and opens both legs.
    - **Close.** `markDataplaneArmFailed` and `markDataplaneNotArmed` clear the attach record along with the arm.
    - **Tail.** `applyKernelTuning` writes `transitOpen` through `writeTransitGate`. Its #7191 comment now notes that the proof does not gate on the published runtime (#9804).
    - **Bootstrap exit.** `runBootstrapExitStartup` applies the host posture only. Transit re-opens at the first apply after the arm.
    - **Image and test VMs.** `scripts/image/bake.py`, `test/incus/setup.sh` and `test/incus/cluster-setup.sh` now write forwarding `0` into `sysctl.d`, so the kernel is closed from boot until xpfd opens it. Neither the image self-tests nor `validate.py` pins that value.
    - **Docs.** `pkg/daemon/README.md` gains a new "Transit waits for the first attach" bullet and a #9804 note on the #7191 bullet. Its "When it re-opens" text and the bootstrap note are updated.
  - **File(s)**: `pkg/daemon/{transit_closed_until_attach_9725.go, daemon.go, daemon_transit_gate.go, daemon_run_bringup.go, daemon_run_naming.go, daemon_apply_dataplane.go, daemon_system.go, README.md}`, `pkg/fsatomic/canary_test.go`, `scripts/image/bake.py`, `test/incus/{setup.sh, cluster-setup.sh}`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 ~14:30 UTC
  - **Action**: Tests.
    - **New file** `pkg/daemon/transit_closed_until_attach_9725_test.go`:
      - `TestBootKeepsTransitClosedUntilTheFirstApplyAttaches9725` is the issue's acceptance cell.
        - It drives the production boot order with the sysctls seeded at 1: the boot policy, the boot arm, then a real `applyConfigLocked` on the #5679 apply harness.
        - Inside `ApplyConfig`, which is the attach, the sysctls read 0/0 with the barrier installed. After the apply both legs are open.
      - `TestAFailedApplyDoesNotOpenTransit9725`:
        - an ordinary `ApplyConfig` failure records no attach;
        - a later tail lowers knobs that something else raised;
        - a later successful apply opens transit.
      - `TestOnlyAnApplyAfterTheCurrentStartOpensTransit9725`: an apply before any Start opens nothing, and an arm failure, not-armed and a re-arm each clear the attach record.
      - `TestTheBootCloseRecordsNothingAboutTheArm9725`: the boot close leaves the arm flag and the RG weight unchanged.
    - **Updated cells.** Every cell whose premise was "open at the arm" now attaches first. Where a cell asserted the arm-only state open, it now asserts it closed.
      - In the #5275 file: `TestApplyKernelTuningHonoursArmGate5275`, `TestBootArmSuccessKeepsTransit5275` (renamed `TestBootArmSuccessOpensTransitAtTheFirstAttach5275`), `TestBootstrapRollbackClosesTransit5275` and `TestRearmAfterArmFailureRestoresTransit5275`.
      - In the #7191 file: the cells that armed and expected the barrier removed or transit open.
    - **Harness.** The first acceptance run panicked on a bare `Daemon`: a nil config store in the apply tail. The cells now use the #5679 apply harness.
    - **Results.** `go test -run '5275|7191|7178|9686|9725|TestDataplaneCell' ./pkg/daemon/` and `go test ./pkg/fsatomic/` both pass, and `go vet` is clean.
  - **File(s)**: `pkg/daemon/{transit_closed_until_attach_9725_test.go, transit_forwarding_failclosed_5275_test.go, transit_barrier_7191_test.go}`

- **Timestamp**: 2026-09-11 15:21 UTC
  - **Action**: Codex round 1 on `7f13e0c6c` returned MERGE NO with four HIGH findings, two MED and one LOW. Each was checked against the code, and the gate is redesigned around an attach milestone from the dataplane.
    - **HIGH 1 and 2: `ApplyConfig`'s result is not the attach signal, in either direction.**
      - **The failures.** `attachUserspaceShimXDP` returns nil with nothing attached when no interface needs the shim (`loader.go`), so a node with only unzoned routed interfaces opened transit. Userspace `ApplyConfig` returns errors from steps after the attach (snapshot publish, HA clear, status), so a node whose programs were adjudicating stayed closed.
      - **Fix.** `attachUserspaceShimXDP` records a milestone, a generation plus the count attached, as its last statement, and only when it attached something (`pkg/dataplane/attach_milestone_9725.go`). `userspace.Manager` and `LegacyDataPlaneAdapter` forward `AttachMilestone`. `markDataplaneArmed` stores the generation it finds at Start. `observeAttachMilestone` opens the gate once the generation is higher with at least one interface attached. It runs right after every `ApplyConfig`, whatever that returned, and at the apply tail.
    - **HIGH 3: a restarting node could preempt a healthy peer while closed.** Bring-up started the cluster before the boot close, and the arm restored the full #7178 weight.
      - **Fix.** `seedDataplaneReadinessDebt` demotes the RG arm track before `cluster.Start`. `markDataplaneArmed` no longer restores it; only the attach milestone does. An arm failure demotes it again, as before.
    - **HIGH 4: in-place upgrades kept `forwarding=1` in `/etc/sysctl.d/99-xpf.conf`.**
      - **Fix.** `debian/xpf.postinst` rewrites exactly those two lines to 0 at `configure`, without applying them live. A failed rewrite warns rather than failing the package operation.
      - **Scenarios** in `test/debian/postinst-test.sh`: an upgrade closes both lines and leaves the rest of the file alone, and a host with no such file gets none. Both pass under `/bin/sh` (dash).
    - **MED 5: gate state and actuation were not serialized.**
      - **Fix.** `transitGateMu` covers every gate state change and both legs: `markDataplaneArmed`, `markDataplaneArmFailed`, `markDataplaneNotArmed`, `closeTransitUntilFirstAttach`, `seedDataplaneReadinessDebt`, `observeAttachMilestone` and `assertTransitGate`. A stale open re-reads the state under the lock. `SetMonitorWeight` takes only the cluster lock and recalculates the weight, so holding the gate lock across it adds no lock-order hazard.
    - **MED 6: false greens.**
      - The acceptance cell records every transit knob write (`transitForwardWriteHook`) and fails on a write of 1 before the attach.
      - `attachingDP9725` attaches inside `ApplyConfig` and can then return an error, which is the attach-then-error case.
      - The image and Incus sysctl values are pinned.
    - **LOW 7: stale descriptions.** The canonical comment distinguishes older images from new ones and names the milestone. The #7191 barrier comment covers the armed window before the attach. The bootstrap-exit comment calls Start shim loading.
    - **Cells** (`pkg/daemon/transit_closed_until_attach_9725_test.go` unless noted):
      - `TestBootKeepsTransitClosedUntilTheFirstAttach9725`, the acceptance cell, with the write history;
      - `TestAnApplyThatFailsAfterItsAttachStillOpensTransit9725`;
      - `TestAnApplyThatAttachesNothingKeepsTransitClosed9725`, tail included;
      - `TestOnlyAnAttachSinceTheCurrentStartOpensTransit9725`;
      - `TestTheBootCloseRecordsNothingAboutTheArm9725`;
      - `TestReadinessDebtHoldsUntilTheFirstAttach9725`, and `TestReadinessDebtIsSeededBeforeTheFirstElection9725`, a structural pin on bring-up;
      - `TestAStaleOpenCannotLandAfterAClose9725`;
      - `TestAnAttachOutsideApplyConfigOpensAtTheApplyTail9725`, because the in-process CLI's apply calls `Compile` directly (`pkg/cli/apply.go`);
      - `TestThePublishedRuntimeReportsTheAttachMilestone9725`;
      - `TestTheImageAndTestVMsStartWithForwardingClosed9725`;
      - in `pkg/dataplane`: `TestAnAttachPassRecordsItsMilestoneOnlyWhenItAttached9725`, and `TestTheAttachStepRecordsItsMilestoneAfterEveryAttach9725`, a structural pin, because the attach itself needs BPF;
      - in `pkg/dataplane/userspace`: `TestTheAdapterForwardsTheShimsAttachMilestone9725`, through `RecordAttachMilestoneForTest`.
    - **Updated cells.** The #5275 and #7191 cells attach through `attachForTest9725`. `fakeCoverageDP` reports a milestone itself, so the wrapper does not hide its coverage summary. `TestSuccessfulArmClearsTheDemotion7178` now requires the arm alone to keep the demotion and the attach to clear it.
  - **File(s)**: `pkg/dataplane/{attach_milestone_9725.go, attach_milestone_9725_test.go, loader.go}`, `pkg/dataplane/userspace/{manager.go, legacy_dataplane.go, attach_milestone_forwarding_9725_test.go}`, `pkg/daemon/{transit_closed_until_attach_9725.go, transit_closed_until_attach_9725_test.go, daemon.go, daemon_transit_gate.go, daemon_apply_dataplane.go, daemon_system.go, daemon_run_bringup.go, daemon_run_naming.go, README.md, transit_forwarding_failclosed_5275_test.go, transit_barrier_7191_test.go, dataplane_arm_track_wiring_7178_test.go}`, `debian/xpf.postinst`, `test/debian/postinst-test.sh`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 16:03 UTC
  - **Action**: Codex round 2 on `3acc48758` returned MERGE NO with four HIGH findings and one MED. Three HIGHs trace to mechanisms this change added, so those are removed rather than patched.
    - **HIGH 1: the milestone counted candidates, not attachments.** It recorded `len(result.pendingXDP)`, but both attach loops skip VLAN children on generic XDP, so a pass could record interfaces it never attached.
    - **HIGH 2: an attach could be undone before the gate saw it, and the gate never looked again.** The #5485 reconcile after the snapshot publish can detach links the pass attached. A later apply that removed the last link left transit latched open.
    - **The replacement for both.**
      - The gate reads the dataplane's live link map (`AttachedXDPLinkCount`, `pkg/dataplane/attached_links_9725.go`), which holds exactly the links a successful attach created and no detach has removed.
      - `transitOpen` is the arm AND a count above zero. It is re-read after every `ApplyConfig`, whatever that returned, and at the apply tail.
      - It is not latched, so an apply that leaves nothing attached closes transit again.
      - The milestone, its baseline and the loader record are gone.
    - **HIGH 3: the readiness debt missed the first election and later RGs.** `cluster.UpdateConfig` creates RGs at weight 255 before the seed, and an RG added later never inherits the debt. Doing that right needs the cluster manager to carry a node-level debt, which is outside this change. So the debt is removed: the #7178 arm track follows the arm again, as on master. The gap is filed as #9842 (MED), which also covers the same inheritance gap after an arm failure.
    - **HIGH 4: the postinst rewrite missed valid sysctl spellings.** ` net.ipv4.ip_forward = 1` or an inline comment did not match, and `sed` still exited 0.
      - The rewrite is removed.
      - Instead, the package ships `/usr/lib/sysctl.d/zz-xpf-transit-closed.conf` (`debian/xpf-transit-closed.sysctl`, installed by `debian/rules`).
      - systemd-sysctl orders files by name across its directories, and a later file wins. So it overrides an older image's `99-xpf.conf`, whatever that file's formatting, without editing the operator's file.
    - **MED 5: the cells admitted broken implementations.** Rewritten:
      - the dataplane cell drives the real link map: recorded links raise the count, and a removed one lowers it;
      - the adapter cell forwards a real shim's count;
      - `TestAnApplyThatLeavesNothingAttachedClosesTransitAgain9725` pins that the gate is not latched;
      - `TestAttachedLinksOpenNothingWithoutTheArm9725` pins the arm half;
      - the stale-open cell no longer sleeps. While the open is blocked inside a knob write, it polls that the stop has neither changed the arm nor returned;
      - `TestThePackageKeepsForwardingClosedFromBoot9725` pins the shipped file's values, its install line and its sort order.
    - **Not proven here.** One attached link opens `ip_forward` for every interface. Per-surface coverage is the #7191 proof's job, and the published runtime does not run it (#9804).
    - **Results.**
      - Pass: `go test -run '5275|7191|7178|9686|9725|TestDataplaneCell|5679' ./pkg/daemon/`, `-run 9725` on `pkg/dataplane` and `pkg/dataplane/userspace`, `go test ./pkg/fsatomic/`, and `test/debian/postinst-test.sh`.
      - The stale-open cell passed 5 of 5 runs.
      - gofmt and vet are clean.
  - **File(s)**: `pkg/dataplane/{attached_links_9725.go, attached_links_9725_test.go, loader.go}` (the milestone files removed), `pkg/dataplane/userspace/{manager.go, legacy_dataplane.go, attached_links_forwarding_9725_test.go}`, `pkg/daemon/{transit_closed_until_attach_9725.go, transit_closed_until_attach_9725_test.go, daemon.go, daemon_transit_gate.go, daemon_apply_dataplane.go, daemon_system.go, daemon_run_bringup.go, README.md, transit_barrier_7191_test.go, dataplane_arm_track_wiring_7178_test.go}`, `debian/{rules, xpf-transit-closed.sysctl, xpf.postinst}`, `test/debian/postinst-test.sh`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 16:21 UTC
  - **Action**: Codex round 3 on `8c8560d20` returned MERGE NO with one HIGH finding, two MED and one LOW.
    - **HIGH: the link count is bookkeeping, not kernel truth.** A pinned link detached or updated from outside keeps its map entry, and a link pinned by an earlier run is not in a new Manager's map.
      - **Checked.** The links are bpf_links (`link.AttachXDP`, pinned under `linkPinPath`). While one is attached, the kernel refuses a netlink XDP detach or replace on that device ("Can't replace active BPF XDP link"). So `ip link set ... xdp off` cannot remove it; only closing the link, or detaching it through its pin, can. That needs a privileged actor, who could as well write `ip_forward`.
      - **Disposition.** Recorded as outside the gate, in the canonical comment ("WHAT THE COUNT IS"). A link pinned by an earlier run errs the safe way: it is not counted, so after a restart the gate stays closed until this process attaches.
    - **MED: the not-latched cells never ran the production detach.**
      - `TestDetachXDPLowersTheAttachedLinkCount9725` calls the real `DetachXDP` on a fake link that can unpin and close.
      - `TestSyncInterfaceAttachmentsLowersTheAttachedLinkCount9725` runs the real #5485 reconcile against a snapshot that keeps one of two links.
    - **MED: the package cell only searched `debian/rules` text.**
      - It now parses the `override_dh_auto_install` recipe, with continuations joined and comment lines dropped, and requires both install commands there.
      - The destination is under `/usr/lib`, where the file is not a conffile.
      - `make deb` is queued as validation, to read the built payload and its conffiles.
    - **LOW: the race cell could miss a lockless stop.** Every gate function now takes the lock through `lockTransitGate`, which calls `transitGateAcquireHook` first. The cell waits until the stop reaches the lock, and only then polls that the stop neither changes the arm nor returns.
    - **Results.** The targeted suites pass with 15 top-level #9725 cells, and the stale-open cell passed 5 of 5 runs. gofmt and vet are clean.
  - **File(s)**: `pkg/daemon/{transit_closed_until_attach_9725.go, transit_closed_until_attach_9725_test.go, daemon_transit_gate.go}`, `pkg/dataplane/{attached_links_9725.go, attached_links_9725_test.go}`, `pkg/dataplane/userspace/attached_links_forwarding_9725_test.go`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 16:47 UTC
  - **Action**: Codex round 4 on `342f15066` returned MERGE NO with two HIGH findings, two MED and one LOW. On the same head, matrix round 3 killed 20 of 21 cells, and `make deb` passed.
    - **HIGH: a link whose device was unregistered stayed counted.** NIC removal, VF re-creation, `ip link del` or a driver reload makes the kernel detach XDP and clear the bpf_link's device, while xpfd's handle and map entry survive.
      - **Fix.** `AttachedXDPLinkCount` counts a link only while the kernel reports it attached (`xdpLinkAttachedSeam`, which reads `link.Info().XDP().Ifindex`). A link whose info cannot be read is not counted, so the gate fails closed.
      - **Cell.** `TestALinkTheKernelDetachedIsNotCounted9725`.
      - **Not changed.** The count is re-read at each apply, not on link events.
    - **HIGH: every compile removes the link pins, and `AttachXDP` never re-pins a registered link, so a restart detaches the shim.**
      - **Checked** in `pkg/dataplane/loader.go`: `removeUserspaceShimXDPLinkPins` runs on each compile; `AttachXDP` returns "already attached" for a registered ifindex; `Manager.Close` closes every link handle.
      - **Disposition.** This predates #9725 and is filed as #9847 (MED).
    - **MED: `systemctl restart systemd-sysctl` would close transit under a running xpfd.** systemd-sysctl re-applies every sysctl.d file whenever it runs, and the package's `zz-` file set both knobs to 0.
      - **Fix.** The file is removed. The package and the image enable `xpf-transit-closed.service`, a boot-only oneshot ordered after `systemd-sysctl` and before networkd, FRR and xpfd.
      - `debian/rules` stages it, enables it with `--no-start` so an install never runs it on a live node, and cleans the staged copy. `bake.py` copies it in and enables it.
    - **MED: the detach cells could not tell which link was removed.** Both cells now assert the exact remaining link. The fake links record `Unpin` and `Close`, so the intended link must be closed and the kept one untouched.
    - **LOW: the recipe parser could pass a target defined twice.** `makeRecipe9725` now fails such a target, because make runs only the last recipe.
    - **Matrix round 3: D04 escaped.** Removing the re-read right after `ApplyConfig` still passed, because an ordinary `ApplyConfig` failure continues to the apply tail, which re-reads the gate. The cell now also runs an abort-class failure (`dpuserspace.ErrPolicySchedulerProtocolIncompatible`), which returns before the tail.
    - **Also.** `debian/.gitignore` now ignores every unit copy `debian/rules` stages; `make deb` had left three untracked.
    - **Results.**
      - 16 top-level #9725 cells and 4 subtests pass, and the stale-open cell passed 5 of 5 runs.
      - The #7253 modularity audit, `go test ./pkg/fsatomic/` and `test/debian/postinst-test.sh` pass.
      - gofmt and vet are clean, and `bake.py` compiles.
  - **File(s)**: `pkg/dataplane/{attached_links_9725.go, attached_links_seam_9725.go, attached_links_9725_test.go}`, `pkg/dataplane/userspace/attached_links_forwarding_9725_test.go`, `pkg/daemon/{transit_closed_until_attach_9725.go, transit_closed_until_attach_9725_test.go}`, `scripts/image/{xpf-transit-closed.service, bake.py}`, `debian/{rules, .gitignore}` (and `debian/xpf-transit-closed.sysctl` removed), `docs/log/9725.md`

- **Timestamp**: 2026-09-11 17:15 UTC
  - **Action**: Codex round 5 on `bec7591c6` returned MERGE NO with one HIGH, one MED and one LOW finding. It found nothing wrong with the oneshot's ordering or with `--no-start`, and it confirmed that the kernel reports ifindex 0 only after it clears an XDP link's device. On the same head, matrix round 5 killed 23 of 23 cells. `make deb` built a package that ships the oneshot, enables it without starting it, and carries no `sysctl.d` file.
    - **HIGH: the count is read only at applies.** If every XDP-bearing device is unregistered between applies, transit stays open until the next apply. Meanwhile networkd and FRR can restore a recreated device's addresses and routes.
      - **Checked.** There is no always-on link subscription to re-read from:
        - `monitorLinkState` runs only while SNMP is configured (`daemon_run.go:592`);
        - `monitorFabricState` runs only for an HA fabric;
        - `pkg/vrrp/track.go` subscribes only for `track-interface`.
      - **Master.** At `fbc51cc02`, master opens transit at `rt.Start()` (`markDataplaneArmed`) and keeps it open for the whole armed run, attached or not.
      - **Disposition.** The gap predates #9725 and needs a new mechanism, so it is filed as #9848 (MED). The canonical comment's NOT CHANGED HERE section and `pkg/daemon/README.md` state it.
    - **MED: the kernel-detach cell replaced the whole decision.** A count that ignored the read error or the ifindex, or counted every link, still passed.
      - **Fix.** The seam, `xdpLinkIfindexSeam`, now only reads the ifindex the kernel reports. `xdpLinkAttached`, which decides over that result, is production code. `CountEveryXDPLinkForTest` fakes ifindex 1.
      - **Cell.** `TestALinkTheKernelDetachedIsNotCounted9725` holds three links: a live one, one the kernel detached, and one whose read fails while reporting a nonzero ifindex. Matrix cells M01 (ignore the error), M04 (ignore the ifindex) and M05 (count every link) target it.
    - **LOW: `pkg/daemon/README.md` was stale.** It said the #7191 barrier is scoped strictly to the unarmed window, and it named the removed `zz-xpf-transit-closed.conf`.
      - Now it scopes the barrier to the closed-transit window, which since #9725 includes an armed node with nothing attached.
      - It also describes the oneshot and why it is not a `sysctl.d` file.
    - **Results.**
      - `go test -run 9725` passes on `pkg/dataplane` and `pkg/dataplane/userspace`.
      - gofmt is clean, and so is `go vet ./pkg/dataplane/`.
      - The daemon source change is a comment only.
  - **File(s)**: `pkg/dataplane/{attached_links_9725.go, attached_links_seam_9725.go, attached_links_9725_test.go}`, `pkg/daemon/{transit_closed_until_attach_9725.go, README.md}`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 17:52 UTC
  - **Action**: Matrix round 6 on `01d736f06` killed 25 of 25 cells. On the same head, Codex round 6 returned MERGE NO with three LOW findings, all prose. It raised nothing on the code, and nothing on the #9848 disposition.
    - **The matrix.** M01 first ran VOID, because its mutant left `err` unused and did not build. The corrected mutant, a decision that ignores the read error, was re-run on the same head and killed by `TestALinkTheKernelDetachedIsNotCounted9725` (count 2, want 1). `9725-matrix.sh` now carries the corrected cell.
    - **LOW: the README's #5275 section was obsolete.** It still said the armed desired value is 1, the gate never lowers the knob while armed, and the arm boundary is Start. It also still described the barrier's other legs as future work.
      - **Fix.** The bullet is rewritten as "What an armed node needs from the kernel", and it points at the #7191 and #9725 bullets.
      - `enableForwarding`, which #9725 renamed, is now `applyHostForwardingPosture` in the README at three sites. The mention in `daemon_transit_gate.go`'s history of the pre-#5275 defect is left as it was.
    - **LOW: the README and `daemon_transit_gate.go` said the barrier "closes nothing that was open".** That holds only for routed transit: bridged frames ignore `ip_forward`, and the bridge-family DROP is what closes them.
      - **Fix.** Both now say the inet leg is a second lock on routed transit, and the bridge leg is what stops bridged frames.
    - **LOW: `daemon_run_naming.go` said transit opens at the first successful apply.** It opens once an apply leaves a shim XDP link attached, whatever that apply returns.
    - **Swept, not flagged.** Six other sites restated the rule in the same way. Each now refers to `transitOpen` instead:
      - `pkg/nftables/transit_barrier.go`: the table comment, the scope paragraph, and "CLOSES NOTHING THAT WAS OPEN";
      - `daemon_transit_gate.go`: THE GATE and WHY THE ARMED CASE IS UNAFFECTED;
      - the apply-tail comment in `daemon_system.go`;
      - a comment in `transit_barrier_7191_test.go`.
    - **Scope.** This round changes comments and docs only.
  - **File(s)**: `pkg/daemon/{README.md, daemon_transit_gate.go, daemon_run_naming.go, daemon_system.go, transit_barrier_7191_test.go}`, `pkg/nftables/transit_barrier.go`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 18:41 UTC
  - **Action**: On `f6eb89871`, matrix round 7 killed 25 of 25 cells in one run. On the same head, Codex round 7 returned MERGE NO with one HIGH, one MED and five LOW findings.
    - **HIGH: bridged transit forwards with no barrier from boot until xpfd installs it.** xpf persists bridge domains as networkd `.netdev` files (`pkg/networkd/networkd.go`), so networkd recreates them before xpfd starts. Bridged frames ignore `ip_forward`, and the boot oneshot writes only the routed knobs.
      - **Disposition.** It predates #9725: master installs no barrier before Start and removes it at Start, so master's window ran until the first attach, and this change narrows it. The fix needs a boot-time barrier that xpfd takes over, so it is filed as #9852 (MED), with a design comment. NOT CHANGED HERE, the README and the PR body state it.
    - **MED: a failed barrier install was logged as a lost defence-in-depth.** For bridged frames the barrier is the only lock.
      - **Fix.** `applyTransitBarrier` logs a failed install at ERROR, names bridged exposure and the apply-tail retry, and takes `open` rather than `armed`. The Install and Remove interface comments no longer scope the barrier to the unarmed state.
    - **LOW 3 to 7: stale restatements of the gate rule.** This is the third round of that class. So instead of fixing the flagged lines, a read-only census swept every comment, doc and script. It found about 65 statements across 25 files that key the gate on the arm alone, treat "the first attach" as a latch, scope the barrier to the unarmed state, call it future work, or claim the #7191 coverage proof gates today.
      - **Fix.** Each now states both conditions or refers to `transitOpen`, across `pkg/daemon`, `pkg/nftables`, `pkg/dataplane`, `pkg/config`, `pkg/routing`, `pkg/fsatomic`, `docs/userspace-dataplane-gaps.md`, `scripts/image`, `debian/rules` and `test/incus`.
      - **Messages.** Two slog messages changed. The bring-up close now reads "kernel transit forwarding closed; it opens when the dataplane is armed and a shim XDP link is attached", and "left to the arm gate" now reads "left to the transit gate". Nothing asserted the old text. Matrix cell D10's anchor and the lab-check script follow the new message.
      - **Left as they are.**
        - `docs/research/`: historical plans, not the shipped rule.
        - `docs/log/`: append-only history.
        - The "the proof gates" prose in files this change does not otherwise touch (`pkg/dataplane/armcoverage_publish.go`, `armproof.go`, `loader.go`, `userspace/manager.go`, `pkg/config/userspace_rx_mtu_advisory_9727.go`, `pkg/daemon/daemon_arm_coverage_7191.go`). It predates this change and belongs to #9804.
    - **Review of the sweep.** Every changed Go line that is not a comment is a log or test message string, the `applyTransitBarrier` rename, or a test description value. No logic changed. An anchor audit of all 25 matrix cells found only D10 stale.
  - **File(s)**: 25 files: `pkg/daemon/...`, `pkg/nftables/{transit_barrier.go, netlink_installer.go}`, `pkg/dataplane/{README.md, armcoverage_abort_7289.go}`, `pkg/config/{README.md, compiler_ipsec_plaintext_warn.go, compiler_wireguard_plaintext_warn.go}`, `pkg/routing/fibimport.go`, `pkg/fsatomic/canary_test.go`, `docs/userspace-dataplane-gaps.md`, `scripts/image/{bake.py, xpf-transit-closed.service}`, `debian/rules`, `test/incus/{setup.sh, cluster-setup.sh}`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 19:04 UTC
  - **Action**: On `73e2c977c`, matrix round 8 killed 25 of 25 cells. On the same head, Codex round 8 returned MERGE NO with one MED and four LOW findings, and it confirmed the #9852 disposition.
    - **MED: the install-failure message claimed more than it knew.**
      - It claimed bridged exposure, but `InstallTransitBarrier` joins per-family errors, so only the inet leg may have failed.
      - It also asserted `ip_forward=0` before the sysctl write had been attempted.
      - **Fix.** The message makes no exposure claim, because the error already names the failing family. The doc comment states what a missing inet or bridge leg leaves open.
    - **LOW: a persistent failure logged ERROR on every apply.**
      - **Fix.** `logTransitBarrierEpisode` logs each direction once per failure episode: ERROR when it starts, Debug while it persists, and Info when it ends. Two episode flags on `Daemon` are guarded by `transitGateMu`.
      - **Cell.** `TestAPersistentBarrierFailureLogsOncePerEpisode9725` drives two failures and then a recovery, for both install and remove.
      - **Matrix.** D18 removes the episode guard, and D19 removes the recovery log.
    - **LOW: the census did not cover Rust files.** Three `userspace-dp` statements still keyed `ip_forward` on the arm: comments in `afxdp/poll_descriptor/mod.rs` and `afxdp/bpf_map/mod.rs`, and a #7480 test message. **Fix.** Each is qualified to the time the transit gate is open.
    - **LOW: the README said a cold reboot carries no transit and forwards no traffic under no policy.** **Fix.** Both statements now say routed transit, and name the #9852 bridged exception.
    - **LOW: the oneshot's `ExecStart` joined its writes with `;`,** so a failed IPv4 write was masked by a successful IPv6 one.
      - **Fix.** It attempts both writes and exits non-zero if either fails.
      - **Cell.** `TestTheBootOneshotFailsWhenEitherWriteFails9725` runs the unit's own script against temp files, one of them unwritable.
      - **Matrix.** D20 makes the script exit 0, and D21 stops the IPv4 failure being recorded.
  - **File(s)**: `pkg/daemon/{daemon_transit_gate.go, daemon.go, transit_closed_until_attach_9725_test.go, README.md}`, `scripts/image/xpf-transit-closed.service`, `userspace-dp/src/afxdp/{poll_descriptor/mod.rs, bpf_map/mod.rs, forwarding/tests_noroute_adjudication_7480.rs}`, `docs/log/9725.md`

- **Timestamp**: 2026-09-11 19:22 UTC
  - **Action**: On `b3c6c32e5`, matrix round 9 killed 29 of 29 cells. On the same head, Codex round 9 returned MERGE NO with one HIGH, two MED and three LOW findings.
    - **HIGH: the deferred-MAC reapply never re-read the gate.** `reapplyAfterDeferredMAC` is the second production `ApplyConfig` caller; the two callers were enumerated.
      - **Consequence.** A reapply that detached the last link left transit open until the next apply tail.
      - **Fix.** The gate is re-read right after that `ApplyConfig`, whatever it returned. The caller holds no `transitGateMu`. The two #5134 cells that call the reapply now use temp sysctl paths, so they do not touch the host's `/proc`.
      - **Cell.** `TestTheDeferredMACReapplyReReadsTheTransitGate9725` covers a reapply that succeeds and one that fails.
      - **Matrix.** D23 removes the re-read.
    - **MED: a barrier failure episode survived a change of direction.** An install failure followed by a successful open left the install episode set, so a later failed close logged only at Debug.
      - **Fix.** Each direction ends the other operation's episode.
      - **Cell.** `TestABarrierFailureEpisodeEndsWhenTheGateChangesDirection9725` drives both operations.
      - **Matrix.** D24 and D25 remove each reset.
    - **MED: the transition logs claimed the kernel's state** ("forwarding enabled" and "closed") even when a leg failed to actuate.
      - **Fix.** They name the gate's state, for example "transit gate open: the dataplane is armed and a shim XDP program is attached". A leg that fails is logged where it fails.
      - The bring-up message is "transit gate closed at bring-up; ...". Matrix cell D10 and the lab-check script follow it.
    - **LOW: the oneshot cell would accept `exit $rc`, which systemd expands as an empty unit variable.**
      - **Fix.** The cell requires `exit $$rc` before it unescapes.
      - **Matrix.** D22 spells it `exit $rc`.
    - **LOW: three passages said `ip_forward` is already 0 when the barrier installs:** `daemon_transit_gate.go`, `pkg/nftables/transit_barrier.go` and the README. **Fix.** Each describes the order: the barrier first, then the sysctl write, and what holds once both have landed.
    - **LOW: a `userspace-dp/tests` diagnostic still said `ip_forward` is force-enabled while armed.** **Fix.** It is qualified to the time kernel transit is open.
  - **File(s)**: `pkg/daemon/{daemon_apply_dataplane.go, deferred_mac_reapply_5134_test.go, daemon_transit_gate.go, transit_closed_until_attach_9725.go, transit_closed_until_attach_9725_test.go, README.md}`, `pkg/nftables/transit_barrier.go`, `userspace-dp/tests/slow_path_admit_single_site_6664.rs`, `docs/log/9725.md`

# #9891 — MonitorInterface gains a subscriber budget and a send deadline

## STEP-0: defect PRESENT on base 9168876e4

Static (file:line proof):

```
$ sed -n '405,570p' pkg/grpcapi/server_diag_monitor.go | grep -n "Acquire|TrySubscribe|ResourceExhausted|Limiter"
NONE FOUND (defect: no admission)
```

- `pkg/grpcapi/server_diag_monitor.go:506` — `ticker := time.NewTicker(time.Second)`
- `:530` — unbounded `for` loop
- `:560` — blocking `stream.Send(resp)` with no deadline
- Sibling `MonitorPacketDrop` at `:177` — `s.eventBuf.TrySubscribe(256)` + `ResourceExhausted`
- Siblings `Ping` (`server_diag_ping.go:69`) and `Traceroute` (`:120`) — `diagLimiter.Acquire()`
- `pkg/grpcapi/server.go:59-74` — `maxConcurrentStreams = 256` caps streams per CONNECTION only; nothing caps connections

Executable repro (temporary `TestReproMonitorInterfaceUnbounded9891`, 70 concurrent streams, since removed):

```
=== RUN   TestReproMonitorInterfaceUnbounded9891
    STEP-0: 70/70 MonitorInterface streams admitted (frames>=1), 0 refused expected on base
--- PASS (0.12s)
```

70/70 admitted proves unbounded. Defect PRESENT, not ALREADY_FIXED.

Sizing correction: the issue text says "sized like MonitorPacketDrop's 256
TrySubscribe" but 256 there is the channel bufSize, not the cap. The real
sibling cap is `pkg/logging/eventbuf.go:170` `defaultMaxSubscribers = 64`
(`maxSubs`). The new budget is 64 to match the sibling streaming budget.

## Fix

- `pkg/diagcmd/limiter.go` — new `MaxConcurrentMonitorInterfaceStreams = 64`
  + `MonitorInterfaceLimiter`, registered in `AllLimiters()` as
  `monitor_interface` (refusals observable via `xpf_admission_refusals_total`).
- `pkg/grpcapi/server_diag_monitor.go` —
  `monitorInterfaceLimiter` package var (same seam shape as
  `sessionWalkLimiter`/`diagLimiter`); fail-fast `Acquire()` after validation
  (cfg nil, NotFound) but before proxy dial and ticker creation;
  `defer release()` covers proxy + local + error paths; over-cap returns
  `codes.ResourceExhausted` with no ticker/goroutine/dial.
  `monitorInterfaceSendTimeout = 30s` (matches `sseWriteDeadline`/
  `bgpStreamWriteDeadline`, per-write not elapsed); local tick loop and
  `proxyMonitorInterface` forwarding loop both send via
  `sendMonitorInterfaceFrame` (Send in a short-lived goroutine +
  `select(ctx.Done, timeout)` with a stopped timer on the fast path; timeout
  severs with `DeadlineExceeded` and the handler return tears the stream down
  so the helper cannot outlive it).

Post-fix repro (same 70-stream probe, before removal):

```
STEP-0: 64/70 MonitorInterface streams admitted — FAIL (expected: cap holds at 64, 6 refused)
```

The repro FAILS post-fix exactly as TDD requires: the cap refuses past 64.

## Tests (`pkg/grpcapi/monitor_interface_budget_9891_test.go`, new)

- `TestMonitorInterfaceRejectsOverCap_9891` — injected cap 3, 4 concurrent:
  4th gets `ResourceExhausted` with 0 frames while the 3 admitted reach 2
  frames across the 1s tick. FAIL-ON-REVERT: removing Acquire admits the 4th.
- `TestMonitorInterfaceTeardownFreesSlot_9891` — cap 1: probe fails while held,
  succeeds after cancel; `InFlight` drains to 0.
- `TestMonitorInterfaceSlowConsumerTimesOut_9891` — blocked Send with 200ms
  compressed budget returns `DeadlineExceeded` in ~207ms, not parked.
  FAIL-ON-REVERT: bare Send deadlocks (test timeout).
- `TestMonitorInterfaceLimiterIsSharedInstance_9891` — alias identity, cap 64,
  `AllLimiters` registration.
- `TestMonitorInterfaceValidationCostsNoSlot_9891` — negative control: NotFound
  with saturated limiter still returns NotFound, not ResourceExhausted.

## Files

- `pkg/diagcmd/limiter.go` — new limiter + registry entry.
- `pkg/grpcapi/server_diag_monitor.go` — admission gate + send helper + both
  Send sites bounded.
- `pkg/grpcapi/monitor_interface_budget_9891_test.go` (new) — 5 cells above.
- `docs/log/9891.md` (this file).

## Validation

- `go test ./pkg/grpcapi/ -run 'TestMonitorInterface.*9891' -count=5` — green, 5/5.
- `go test ./pkg/grpcapi/ -count=1` (full suite) — green, 16.3s.
- `go test ./pkg/diagcmd/ -count=1` — green.
- `go vet ./pkg/grpcapi/ ./pkg/diagcmd/` — clean. `gofmt -l` on touched files — clean.
- Existing `TestMonitorInterfaceSharesStatusAcrossStreams_5707` and
  `TestMonitorInterfaceFramesFollowAConfigCommit9144` — still green (no regression).

# #10486 — session-filter protocol contracts

- **Timestamp**: 2026-09-21
  - **Action**: drove the issue from the source-grounded contract plan through implementation. The five surfaces were REST (`R`), direct gRPC (`G`), local interactive CLI (`L`), remote show CLI (`S`), and remote clear CLI (`C`). The plan fixed the canonical grammar before code: names are case-insensitive, numeric values are `0..255`, name whitespace is trimmed, signed/space-padded numerics are invalid, and absent/empty protocol is a wildcard.
  - **Evidence**: REST previously returned HTTP 200 with an empty result for malformed protocol filters; gRPC already rejected malformed values, but REST and local matching could not resolve valid Lenient names such as `sctp`; and protocol 0 was indistinguishable from an absent local peer-clear filter.
  - **File(s)**: `docs/pr/10486-session-filter-contracts/plan.md`

- **Timestamp**: 2026-09-21 (plan rounds 1–3)
  - **Action**: committed `fc5dc42bb` (plan v1), `46e1a7e6c` (plan v2), and `ea1e0ac7d` (plan v3, after the parent-dispatched adversarial folds). The final plan settled the shared parsed-token/matcher SSOT, A/B split, exact five-surface matrix, callback-driven gRPC endpoint fake, paired C/G semantics, invalid-peer zero-call proof, and first-hand red-on-revert requirement.
  - **Review**: the final parent review accepted the plan for implementation after resolving the canonical numeric grammar, REST validation ordering, gRPC row-fake seam, proto-0 `hasProto` state, and the C serialization boundary.
  - **File(s)**: `docs/pr/10486-session-filter-contracts/plan.md`

- **Timestamp**: 2026-09-21 (implementation)
  - **Action**: committed `f5d688993` (`api: reject invalid REST session protocols (#10486)`). REST now rejects malformed non-empty protocol query values before iteration and before `include_peer` fan-out. The A tests cover `tcpip`, `bogus`, `256`, `-1`, encoded `+6`, encoded space-padded `6`, decoded error text, and zero peer calls.
  - **Action**: committed `52e85dace` (`sessions: share protocol filter contract across surfaces (#10486)`). Added `appid.ParseProtocolFilterToken` and `appid.ProtoFilterMatches`; migrated REST, gRPC, local CLI, and remote show parsing/matching; preserved raw remote clear serialization; made protocol 0 explicit through `hasProto`; and added the REST offset/cursor matrix, gRPC v4/v6 endpoint fake, local SCTP selective-clear proof, paired gRPC clear rejection, and remote C serialization coverage.
  - **Fold**: production callers now parse once and compare numeric protocol values per row. The shared matcher treats `hasProto=false` as wildcard and `hasProto=true, proto=0` as a real HOPOPT filter. No protobuf or wire field changed.
  - **File(s)**: `pkg/appid/catalog.go`, `pkg/api/sessions.go`, `pkg/grpcapi/server_sessions.go`, `pkg/cli/session_filter.go`, `pkg/cli/cli_clear.go`, `cmd/cli/show_flow.go`, the corresponding tests, `pkg/api/README.md`, `docs/junos-cli-reference.md`

- **Validation**:
  - Focused REST, gRPC, local CLI, and remote CLI protocol cells — PASS.
  - Full `go test ./pkg/api -count=1` — PASS.
  - Full `go test ./pkg/grpcapi -count=1` — PASS.
  - Full `go test ./pkg/cli -count=1` — PASS.
  - Full `go test ./cmd/cli -count=1` — blocked by the pre-existing `TestTreeIsGofmtClean` backlog reporting `pkg/api/authz.go` among 39 files; no changed file was listed. No formatter, linter, or project-wide build was run to mask that base failure.
  - Named regressions: REST cursor/GRE matrix; gRPC filtered-total, iterator-error, and bounded-top regressions; local clear-display/parser regressions; and remote selector-scope regressions — PASS.
  - Firsthand red-on-revert: changing `ProtoFilterMatches` to ignore `hasProto` failed `TestProtoFilterMatches` on the wildcard case; disabling the REST protocol validator failed every malformed-protocol row in `TestRESTSessionFilterFailsClosed`. Both guards were restored and the focused tests passed.
  - `git diff --check` and changed-file gofmt — PASS.

- **Outcome**: implementation branch `fix/10486-session-filter-contracts`; PR
  #10541 is open with `Closes #10486`. The known `cmd/cli` gofmt-backlog
  failure remains explicitly recorded for parent review.

- **Timestamp**: 2026-09-22 (code round-1 fold)
  - **Review**: hostile reviewers A and B returned MERGE-NEEDS-MINOR with no
    production correctness or security defects. The fold restores the
    `bogus-token` unknown-keyword guard and numeric-`47` local parser guard,
    adds G-clear zero-work counters, C malformed-port zero-RPC coverage, and
    the L peer-show proto-0 request serializer test.
  - **Docs/process**: plan promoted to READY v4 on base `21df4afd8`, with
    implementation-landed status and G-wiring correction recorded. Q1 is
    closed as the parent/owner-approved single-shot REST 400 rollout. Q4 is
    closed with CLI-owner/parent approval of L widening. Q7 is recorded as a
    split: A is the `#3439` REST residual; B references the #2935-direction
    and standalone #10486 with the Q4 sign-off. These records are in the PR,
    plan, and this log rather than history-rewriting pushed commit trailers.
  - **Accepted residuals**: the staged A-only validated-empty `sctp`/
    `" tcp "` matrix proof and the extra implementation log file were
    accepted in the parent fold; the final tip matrix and log preserve the
    behavior and evidence.
  - **Fixture note**: the G-clear test keeps `clearFaultGRPCDP` rather than
    `viewFaultGRPCDP` because the fault fixture now counts iterator/delete/
    clear-all calls, seeds a matching row, and directly proves invalid input
    performed zero dataplane work.

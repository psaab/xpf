# #9918 — WireGuard hardening residuals (7-member cohort)

- **Timestamp**: 2026-09-15
- **Action**: Harden `userspace-dp/src/afxdp/wg` (+ coordinator) per the cohort: document the crossed-open stall with a recovery test, best-effort transport-key wipe on drop + honest snow-copy residual, Zeroize staging on CryptoFailed, MAC1-first under load with a precomputed key, keep cookie secrets inside the lock, pin deterministic handshake vectors (regression-only — the independent interop proof remains owed, tracked by #10118), document the DNS steering trust boundary.
- **File(s)**:
  - `userspace-dp/src/afxdp/wg/handshake_session.rs` (F-141 docs)
  - `userspace-dp/src/afxdp/wg/session.rs` (F-142 Drop wipe)
  - `userspace-dp/src/afxdp/wg/engine.rs` (F-142 comment, F-143 wipe, F-144 MAC1-first + precompute + seams)
  - `userspace-dp/src/afxdp/wg/handshake.rs` (F-144 precomputed cores)
  - `userspace-dp/src/afxdp/wg/cookie.rs` + `cookie_tests.rs` (F-145 with_secrets)
  - `userspace-dp/src/afxdp/wg/tests.rs` (F-141/F-143/F-146 tests)
  - `userspace-dp/src/afxdp/wg/engine_tests.rs` (F-144 op-order test)
  - `userspace-dp/src/afxdp/wg/handshake_tests.rs` (F-144 KAT, F-146 scope note)
  - `userspace-dp/src/afxdp/coordinator/wg_control/mod.rs` (F-162 trust comment)
  - `docs/config-schema.md` (F-162 trust docs)

Base: `origin/master a17a9c67f` (rebased for the parent-review fold; original base `083c4ca08`), worktree `.claude/worktrees/9918-wireguard`, branch `fix/9918-wireguard`.

## Members (verified via `gh issue view 9918`)

- F-141 `handshake_session.rs:200-211` crossed valid initiations stall (both pending 0, recovery via fresh trigger).
- F-142 `engine.rs:422-427` false "snow internally zeroizes" comment; snow 0.10.0 has no zeroize/Drop.
- F-143 `engine.rs:1569-1572` CryptoFailed leaves unauthenticated bytes in `out`.
- F-144 `engine.rs:628-644` + `cookie.rs:552-577` MAC2-before-MAC1 + per-message MAC1 key (4-6 hashes vs ~1.5).
- F-145 `cookie.rs:494` `secrets()` copies secrets out of `Zeroizing` as bare `[u8;32]`.
- F-146 `handshake_tests.rs:118-126` fixed-pattern KAT + `wg-interop.sh` GATE unreached (no interop oracle).
- F-162 `wg_control/mod.rs:807-844` DNS steering after 90 s silence (blackhole + timing, no decrypt).

All three lanes agree Low (AGY NEG on F-162 only). No protocol violations, no live exposures.

## Plan + 2 hostile reviews (before implementing)

Plan: `/tmp/9918_plan.md` (7 fix directions, tests, risks). Two independent hostile reviewers (`reviewer` agents) verified sites against absolute-path code + snow 0.10.0 registry.

Reviewer A (Eng9918.HostilePlanA): F-141 NEEDS-WORK, F-142 NEEDS-WORK, F-143 APPROVE, F-144 NEEDS-WORK, F-145 NEEDS-WORK, F-146 REJECT (mimic builders), F-162 SPLIT (docs APPROVE, wire REJECT). Must-kill: F-162 wire, F-146 close-claim. Must-fix: exact errors, no-dump claim, delegation, poison policy, production-path vectors.

Reviewer B (Eng9918.HostilePlanB): F-141 NEEDS-WORK, F-142 NEEDS-WORK, F-143 APPROVE, F-144 NEEDS-WORK, F-145 APPROVE, F-146 NEEDS-WORK, F-162 NEEDS-WORK. Must-kill: telemetry-param change, new stamp helper, ring paragraphs, mimic builders, "no wipe API" rationale. Must-fix: exact errors, Drop wipe, comment fix, delegation, mutation check, Go touchpoints.

Full payloads: `agent://Eng9918.HostilePlanA`, `agent://Eng9918.HostilePlanB`.

## Adjudication (what shipped and why)

- F-141: ACCEPT docs (both reviewers: keep-initiator worse — counterpartless sessions with no idle recovery; tie-breaker would not bind kernel/go). FIX test per reviewers: exact `NoPendingHandshake` (never `ReceiverIndexMismatch`), `EncapError::NoSession` both arms (no `Unconfirmed` variant exists; fresh-crossed hits `encap_drops_no_session`), on-demand recovery wording (idle pairs stay stalled until egress demand), re-cross note (no jitter, self-desynchronizing), fresh-vs-rekey distinction.
- F-142: IMPLEMENT `WgSession::drop` rekey-to-zero (both reviewers: `rekey_manually` is public, ~8 lines, best-effort wipe of the 180 s transport keys through vendored `copy_from_slice` — DSE-theoretical, erasure not guaranteed; snow-held static-key/PSK copies remain residual). Correct false comment, DROP unverified no-dump claim (no setrlimit wiring found), document the full residual honestly (verified snow citations below).
- F-143: ACCEPT fix. Drop ring analysis (ring-resolver not compiled — snow default features only), `Zeroize` on `out[..plaintext_len_max]` (tag tail moot; `Zeroize` ends the dead-store debate), fix the "cannot skip the wipe" comment (now: at-or-past `read_message`).
- F-144: ACCEPT MAC1-first + precompute. REQUIRE delegation (`compute_mac1`/`parse_*` delegate to `_with_key` cores via `mac1_key_for`, no mirrored parsers), USE existing `stamp_initiation_mac2` + `cookie_for_test` (zero new helpers), PIN `now_ns` (same window, no rotation skew), DOCUMENT double-verify (classify + consume both precomputed-cheap, independent by design), THREAD precomputed key through consume paths too (initiation + response inbound both key on local pub).
- F-145: ACCEPT `with_secrets`. Make it PRIVATE, return derived cookies/MAC bools only (refs cannot escape; `R: owned`), SEAL outside the lock (build holds across 1 hash only), PRESERVE poison recovery (`into_inner`, #6422), soften "no escapes" to "no caller-frame bare copies" (blake2 stack state unavoidable).
- F-146: REQUIRE production-path seams (no Builder mimic — new `build_*_with_ephemeral` cores + `#[cfg(test)]` fixed-ephemeral wrappers; vectors cover shipped pattern/prologue/PSK). REGRESSION-ONLY (self-generated from believed-good code; `wg-interop.sh` stays GATE/owed in `HARNESSES.unreached`), MANDATORY mutation check (flip prologue → RED), NOTE `fixed_ephemeral_key_for_testing_only` is `#[doc(hidden)]` unstable (pinned knowingly, test-only).
- F-162: KILL wire (Reviewer A decisive for Low + AGY NEG: attack-case adoptions already correspond to `endpoint_changed` ticks + `peer endpoint resolved` log; marginal signal is hold-blocked-vs-applied, not the attack; 5→6-tuple + Go + Prometheus + renderer blast radius disproportionate). DOCS-ONLY: trust boundary in `docs/config-schema.md` + code comment, pointing at existing surfacing (`endpoint_changed`, `endpoint_last_error`, log).

## STEP-0 RED on base (before fixes)

Shell REDs (base `083c4ca08`):
- F-142: `grep "snow internally zeroizes" engine.rs:425` present; registry `grep zeroize|Drop snow-0.10.0/src/` empty; `grep zeroize snow/Cargo.toml` none — false comment + no wipe confirmed.
- F-145: `grep "fn secrets" cookie.rs:494` returns `Option<([u8;32], Option<[u8;32]>)>` — bare copies confirmed.
- F-146: `grep deterministic|fixed_ephemeral wg/` empty; `HARNESSES.unreached:35` lists `wg-interop.sh GATE` — oracle gap confirmed.
- F-162: `grep "trusts DNS" docs/config-schema.md` empty — docs gap confirmed (no wire per adjudication).

Rust REDs (new tests, base code):
- `decap_zeros_staging_on_crypto_failed_9918` FAILED: `CryptoFailed must zero (48 bytes); got [212,173,171,56,...]` — staging retains attacker bytes.
- `classify_skips_mac2_for_bad_mac1_9918` FAILED: `left: 1 right: 0` (`mac2_ok` bumped) — MAC2-first confirmed.
- `crossed_valid_initiations_yield_tunnel_9918_red` FAILED: `Err(NoPendingHandshake)` on `consume_response` — stall confirmed (both pending 0).

## Change (per member)

- F-141: `handshake_session.rs` reserve comment documents the over-fire + accepted stall + recovery + test pointer. New `crossed_valid_initiations_stall_then_recover_9918` pins: both pending 0, no confirmed session, `NoSession` both sides with `encap_drops_no_session`, `NoPendingHandshake` both Responses, fresh trigger recovers + carries traffic BOTH ways (A→B then B→A). No behavior change.
- F-142: `session.rs` adds `impl Drop for WgSession` (rekey both directions to zeros, no RNG, no failure mode; honestly documented as best-effort/DSE-theoretical). `engine.rs` corrects the false comment + records the FULL residual: snow-held static-key copies (`builder.rs:232` → `HandshakeState.s`) and PSK copies (`handshakestate.rs:42,145,457-464`) persist with no wipe API. No new Rust test (keys uninspectable); grep proves Drop present + comment corrected; existing Zeroizing + session tests green.
- F-143: `engine.rs` `try_decap` CryptoFailed arm `Zeroize`s `out[..plaintext_len_max]` before returning; comment fixed to "at or past `read_message`". New `decap_zeros_staging_on_crypto_failed_9918` (tamper ciphertext, 0xa5 prefill, assert zeros).
- F-144: `handshake.rs` adds `mac1_key_for` + `compute_mac1_with_key` + `parse_*_with_key` cores; existing `compute_mac1`/`parse_*` delegate (no drift). `engine.rs` adds `mac1_key` (once in `::new`), reorders `classify_initiation` to MAC1-first (bad-MAC1 → Process without MAC2), documents double-verify; `handshake_session.rs` consume paths use the precomputed key too. New `classify_skips_mac2_for_bad_mac1_9918` (bad-MAC1+valid-MAC2 must NOT bump `mac2_ok`) + `mac1_precomputed_key_matches_kat_9918` (byte-identical to KATs). All existing KATs byte-identical (full wg suite green).
- F-145: `cookie.rs` replaces `secrets()` with private `with_secrets(now, f)` (rotation + closure under one lock, refs never escape; seal outside). `verify_initiation_mac2` / `build_cookie_reply` / `cookie_for_test` recompute inside. `cookie_tests.rs` poison test rewritten to compare derived cookies (same secret → same cookie). `grep "\.secrets("` empty; `grep with_secrets` shows 3 call sites + def.
- F-146: `engine.rs` refactors builders to `*_with_ephemeral` cores + `#[cfg(test)]` fixed-ephemeral wrappers (production pattern/prologue/PSK wiring shared). New `deterministic_handshake_vectors_9918` (fixed privs/ephemerals/TAI64N/indices, production builders + framing + parses, snow round-trip, pinned 148-B msg1 + 92-B msg2 hex). `handshake_tests.rs` scope note updated. Regression-only: `HARNESSES.unreached` untouched (interop still owed).
- F-162: `docs/config-schema.md` + `wg_control/mod.rs` document the DNS trust boundary (90 s, attacker model, mitigations, opt-in, surfacing via `endpoint_changed`/`last_error`/log, IP literals for high-assurance). No wire change per adjudication. Existing 7158 adoption/hold tests green.

## Validation

- Named cells GREEN (after fixes): 5 passed (`crossed_..._stall_then_recover`, `decap_zeros_..._crypto_failed`, `classify_skips_..._bad_mac1`, `mac1_precomputed_...`, `deterministic_handshake_vectors`).
- Named cells 5x: 5/5 runs green (non-flaky).
- Full relevant suites (`--bins -- --test-threads=1`): `afxdp::wg::` 233 passed, 0 failed; `wg_control endpoint_resolver wg_tunnel_status` 45 passed, 0 failed.
- Existing KATs byte-identical: `handshake_tests` + `cookie_tests` + `engine_tests` all green in the 233 (no vector changes except new pins).
- F-146 mutation check (required): flip `WG_PROTOCOL_ID_BYTES` last byte `m→X` → `deterministic_handshake_vectors_9918` FAILED with msg1 drift (left `...8a3c74b0...` vs pinned `...88c2c64d...`); revert → ok. Proves the oracle catches transcript regressions.
- fmt: `rustfmt --check` on touched files shows only pre-existing version-skew diffs (base `HEAD:tests.rs` fails identically at lines 14/196 with rustfmt 1.9.0); our hunks match surrounding file style (multi-line `try_decap`/`try_encap` as in siblings). No `cargo fmt` whole-file run (would churn unrelated lines).
- clippy: tree-wide `cargo clippy` fails on pre-existing `clippy::mut_from_ref` in `umem/mmap.rs:235` (untouched by this lane); `grep wg/|cookie|handshake|session|engine` over clippy output shows zero errors/warnings in our files — our changes are clippy-clean.
- Scrub: `git diff --stat` 11 files, +581/−103; no `TODO`/`FIXME`/`dbg!`/`println!` except the intentional `UPDATE_9918_VECTORS`-gated `eprintln!` re-record path in the F-146 test.

## Parent-review fold (GPT NEEDS-MAJOR 3 / spark NEEDS-MINOR 3, SPLIT)

- (1) Zeroization: staging wipe switched to `Zeroize` (`engine.rs`, import + call — caller-borrowed `out` is observable in practice, `Zeroize` ends the theoretical debate too); session-destruction rekey documented as best-effort/DSE-theoretical (vendored `copy_from_slice`, unchangeable) instead of claiming guaranteed erasure.
- (2) Doc honesty: GPT's snow citations VERIFIED against the locked registry (`builder.rs:232` `(*s_dh).set(k)` static-key copy; `handshakestate.rs:42` owned `psks` array, `:145` `psks: *psks` constructor copy, `:457-464` `set_psk` copy; zero `impl Drop`/`Zeroize` in snow `src/`). `engine.rs` residual corrected: snow-held static-key + PSK copies explicitly included (old "long-lived secrets wiped" was materially false for snow's copies).
- (3) Interop honesty: every interop claim reworded to regression-only; still-owed GATE tracked by #10118 (filed by this lane — no open tracker existed, #1736 is CLOSED). F-146 test doc now forbids citing it as the interop gate.
- (4) SPARK-M1: `cookie.rs` stale `secrets()` doc refs (field doc + `new()`) fixed to `with_secrets()` (the "pre-9918 `secrets()`" history note kept — accurate).
- (5) SPARK-M2: crossed-recovery test now asserts B→A after A→B (responder confirms on first inbound, so the leg is meaningful).
- (6) Rebased onto `origin/master a17a9c67f` (see Base line); gates + 5x re-run green.

## Follow-ups (not in scope)

- `wg-interop.sh` remains GATE/unreached (`HARNESSES.unreached:35`); F-146 vectors are regression-only, not the independent proof. Delivering the interop gate is tracked by #10118 (filed by this lane — narrow scope: Makefile recipe through `harness-result.sh` + first green run). NOTHING in this PR may be cited as the interop gate reaching green.
- F-141 tie-breaker (pubkey compare) intentionally not implemented — would not bind kernel/go peers; stall self-heals on demand.
- F-142 snow-held static-key/PSK copies + DSE-theoretical transport residual have no wipe API — accepted residual, honestly documented in `engine.rs`/`session.rs` (verified snow citations: `builder.rs:232`, `handshakestate.rs:42,145,457-464`, no `Drop`/`Zeroize` in snow `src/`).
- Pre-existing `rustfmt` version skew + `clippy::mut_from_ref` in `umem/mmap.rs` belong to their owners, not this lane.

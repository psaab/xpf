# #9531 — Wire deny gates + ledger coverage + rollback mirror arm

Test-only lane (shell harnesses, Makefile recipes, ledger adapter, census
metadata). Zero production bytes changed on the branch.

- **Timestamp**: 2026-09-09 (issue) → 2026-09-14 (PR #10031 opened)
  - **Action**: closed ZPV-01 (no reached §3-conformant transit deny gate)
    and ZPV-02 (rollback harness never tested revert-time withdrawal) with
    two live gates, ledger wiring for host-inbound, and a rollback mirror
    arm — all pass-on-good + fail-on-bad enveloped.
  - **File(s)**: test/incus/wire-gate-lib.sh,
    test/incus/wire-policy-deny.sh, test/incus/wire-appmatch-twins.sh,
    test/incus/wire_probe_burst.py, test/incus/wire_tcp_accept.py,
    test/incus/cc-rollback-functional.sh, test/incus/harness-result.sh,
    test/incus/harness-result-selftest.sh,
    test/incus/test-host-inbound.sh, test/incus/HARNESSES.unreached,
    scripts/harness-census.sh, scripts/run-selftests.sh, Makefile

## Plan evolution (v1 → v5 PLAN-READY)

- v1 drafted test-only: new `wire-gate` adapter, two gates, Arm B, census.
- Codex r1 PLAN-NEEDS-MAJOR: explicit-DENY fixture ≠ normative
  no-permit-rule row; 1k permit twins violate §2.3 loss floors. Adopted:
  narrow-exclude fixture (default-deny), per-twin floors.
- Codex r2 NEEDS-MAJOR: underspecified adapter + unlabeled regrouping.
  Adopted: closed VOID taxonomy by-reduction, COMPUTE→verify→PRINT,
  required-metrics/headline table, exit-disagreement rules.
- Codex r3 NEEDS-MAJOR (5 blockers): environment deviation stated
  (loss cluster precedent over standalone-DUT default); env-flag
  negative control WITHDRAWN (needs product hooks) → hermetic
  stale-permit transcript + live enforced-permit control;
  full probe_fwd/policy_read/session_list contracts.
- Codex r4 NEEDS-MINOR (3 minors, fixed) + GLM r4 PLAN-READY (GLM r3
  stalled job recorded as NO VERDICT, superseded by r4). GLM verified
  no G1–G9 regressions. Full trail was branch process files
  (docs/pr/9531/, scrubbed per repo policy); substance preserved as
  issue #9531 comments.

## Live numbers (loss-userspace-cluster)

- wire_policy_deny PASS (leak 0/1000, control 1500/1500) + permit-all
  FAIL (leak 1000). wire_appmatch_twins PASS (deny_leaked 0) +
  widened FAIL (deny_leaked 2000). Arm A PASS (1,1,1). Arm B PASS
  (1,1,1,1,1) + enforced-permit FAIL (1,1,0,0,0). Host-inbound
  27/0 + failover 69/0. All rows exe-attributed except the final-HEAD
  Arm B rerun, which passed functionally but rowed VOID on a
  sibling-deploy exe MISMATCH (correct per #2176; accepted —
  revert behavior is tree-independent in a test-only lane).
- Hermetic: deny-lib 8/8, appmatch-lib 10/10, host-inbound-lib 14/14,
  ledger-lib 52 unit + 25/25 mutation kills, census 22/32 green.

## Implementation deviations and live findings

- Permit twins demoted to liveness-grade (offered 1500 / floor 1000):
  measured 0–13% UDP burst loss on an idle DUT; 0%-loss belongs to the
  out-of-lane wire_policy_permit row. Reviewer-sanctioned.
- Arm B debug (all fixed): preamble death spiral (plain-commit
  narrowing + late trap → early idempotent trap + ownership flags);
  close-state Timeout:20 sessions (→ hold-open witness, heartbeats);
  single-conn listener artifact (→ thread-per-accept); baseline
  convergence race (→ poll under listener); holder-silence gate that
  failed good runs by construction (→ lifetime-coverage on the held
  session); invalid `show configuration applications` path (→ store
  `show security applications <name>`).
- Parent-review fold: fixture ownership with captured baseline token;
  provisional verdict + readback-verified postlude + single emit per
  precedence; split timeout taxonomy (CONNECT_TIMEOUT→BLOCKED vs
  124→TIMEOUT→row-timeout) + sink-side readiness + local bounds;
  SID-tied witness + bounded sess_poll.

## Stated limits and follow-ups

- Only 2 of 5 §3 rows land. Deferred: #10028 (wire_zone_matrix),
  #10029 (wire_hostinbound_deny), #10030 (wire_conntrack_lifecycle).
- No live store-only-revert instance without product hooks (the
  enforced control is observation-path evidence). Wrapper
  exe-downgrade prose is ledger-owner follow-up.

# #9824 — Rebase the compiled snapshot's generation under `m.mu`

Worktree `/home/ps/git/pi-xpf/.claude/worktrees/9824-rebase-generation`,
branch `fix/9824-rebase-generation`, base `origin/master b4e78ea28`.
Full plan + raw reviews live on disk at `docs/pr/9824/` (uncommitted);
this file is the shipped history.

- **Timestamp**: 2026-09-14
- **Action**: STEP-0: all four issue premises verified present at base —
  reserve-outside-`m.mu` (`manager_compile.go:258` + `bumpGeneration`
  `:33-38`), verbatim deferral store (`:456`), #6986 guard shape
  (`manager_generation.go:195-212`), tick gate (`process_status.go:15`), no
  rebase in the resample path (`partial_update_outcome_9684.go:127-166`).
  NOT ALREADY_FIXED. Supporting facts read: hash zeroes Generation
  (`builder.go:199`), plan key generation-free (`maps_sync.go:1518+`),
  adopt-after-publish (`apply_snapshot_identity_9520.go:138-143`).
- **File(s)**: none (verification only)

- **Timestamp**: 2026-09-14
- **Action**: Plan v1 (DRAFT, 11 sections): rebase iff
  `snap.Generation <= m.publishedSnapshot` (exact stuck predicate) via inline
  `m.generation++` after `resampleForCompileLocked`, before defer-or-publish.
- **File(s)**: docs/pr/9824/plan.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Round-1 hostile plan review (pi --thinking max).
  GLM-5.3 PLAN-READY (STEP-0 re-verified, predicate/placement/single-producer
  confirmed, 4 advisories). Astra PLAN-KILL: lost-response composition leaves
  helper-installed above publishedSnapshot, so 9 > 7 stores verbatim and the
  helper rollback-refuses with no #9520 retry. KILL conceded after verifying
  every citation (Rust gate `snapshot.rs:97-119`, skipped bookkeeping,
  outcome-unknown defer, conflict-only retry). Author found sub-case 2a during
  verification (reserve → lost republish of unallocated m.generation+1 →
  apply), which kills `< m.generation` too.
- **File(s)**: docs/pr/9824/reviews/round1-{glm53,astra}.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Plan v2: unconditional `m.generation++` + fresh FIBGeneration
  read, based on H ≤ m.generation+1 (equality landing converges via #9520
  conflict retry). Tests gain a helper-model fixture + lost-response cell.
- **File(s)**: docs/pr/9824/plan.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Round-2 hostile plan review. GLM PLAN-READY (H-census,
  applySem audit, all traces, Q4–Q6). Astra PLAN-KILL: the equality landing
  is not loss-tolerant — conflict → retry lands → retry response lost
  strands retained G below H=G+1 (copy-publish + skipped adopt +
  consume-only-first), per-tick fail-closed; plus FIB Q2 (0-on-read-failure
  + u32 wrap break "fresh ≥ held"). KILL conceded; fib leg withdrawn.
  Prescription: strictly-above-H allocation.
- **File(s)**: docs/pr/9824/reviews/round2-{glm53,astra}.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Plan v3: unconditional `m.generation += 2` (offer strictly
  above H; no equality landing, no fib dependence). General
  deferred-retry-recovery hole scoped to a follow-up issue.
- **File(s)**: docs/pr/9824/plan.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Round-3 hostile plan review (budget exhausted 1–1). GLM
  PLAN-READY (r2 KILL re-verified, structural H-census, Q6 conditional on
  filing the follow-up). Astra PLAN-KILL: resample β/land-lost + no-op
  fabric writeback clearing debt and the helper's digest + equal-gen
  conflict + lost retry recreates the strand with no foreign publish —
  "works for any initially chosen G", so v3's loss-closure promise needs
  tick-side recovery; a NARROWER promise (allocation boundary only) would
  be defensible. KILL verified (digest-clear arms, empty-digest conflict,
  debt/writeback asymmetry, Rust pin test) and conceded. Lane reported
  BLOCKED; plan + verdicts posted to #9824 as comments #5671810381 (plan),
  #5671810596 (record).
- **File(s)**: docs/pr/9824/reviews/round3-{glm53,astra}.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Parent ruling: narrowed v4 + ONE r4 round (frozen scope: +=2
  code, allocation-boundary promise, corrected digest rule, drift cell);
  implement only on agreement; 4th KILL with a new verified hole → STOP.
  Plan v4 written to the frozen scope.
- **File(s)**: docs/pr/9824/plan.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Round-4 hostile plan review: AGREEMENT. Astra
  PLAN-NEEDS-MINOR ("narrowed design sound, no NEW verified hole"; 4 exact
  fixes: temporal wording, Cell R baseline mechanics, single ctrl oracle +
  transitions, census wording + #10041 link; follow-up #10041 verified
  OPEN). GLM PLAN-READY (boundary re-derived structurally, fixture + Cell R
  exact, no new hole; 4 advisories). All minors/advisories applied to
  v4-final. Record posted to #9824 as comment #5671893031. Proceeding to
  implementation per the ruling.
- **File(s)**: docs/pr/9824/reviews/round4-{glm53,astra}.md (uncommitted)

- **Timestamp**: 2026-09-14
- **Action**: Implemented in two shippable commits: (1) the `+= 2` rebase
  with proof comment (`manager_compile.go`, +15); (2) helper-model fixture
  + Cells 1–5/R + rollback-prefix liveness pin (new test file, +1211).
  During green-up fixed two test-side faults the cells caught honestly: a
  Cell 1 numbering assertion (rebase offers m.gen-at-apply+2, not
  reserved+2, after an interleaved partial) and the recorder retaining the
  offered snapshot pointer across the in-place #9520 retry mutation.
- **File(s)**: pkg/dataplane/userspace/manager_compile.go,
  pkg/dataplane/userspace/compile_generation_rebase_9824_test.go

- **Timestamp**: 2026-09-14
- **Action**: Gates, all real numbers, no cluster (both review rounds agree
  an ordering defect; plan excludes it): 8/8 new cells green 5x each;
  full `go test ./pkg/dataplane/userspace/` green (51.6s); `go vet` clean;
  RED-verified in an isolated base worktree (Cells 1, 2, 3-deferred,
  3-normal, 4 RED on the exact repair-relevant shapes — gate closed 9≥8,
  rollback (8,0)<(9,0), spurious fail-closed commit; Cell R REDs at its
  rebase-numbering pin while its dual-numbering strand comparison is the
  residual-scope pin for #10041, not repair evidence — the drift strand it
  demonstrates is pre-existing shape the fix shifts but does not repair;
  liveness pin + model pin green as fix-independent). Scratch worktree
  removed; RED output preserved at
  docs/pr/9824/reviews/red-verification.txt (uncommitted).
- **File(s)**: none (verification only)

- **Timestamp**: 2026-09-14
- **Action**: Opened PR (Closes #9824) with the test table; requested
  Copilot review; STOPPED without merging per lane contract (parent owns
  final review and merge).
- **File(s)**: docs/log/9824.md

- **Timestamp**: 2026-09-14
- **Action**: Parent-review micro-fold (GLM READY + GPT NEEDS-MINOR), one
  commit: (1) allocator-exhaustion guard — increments saturate via
  incGenerationSaturatingLocked (reserve, partial, FIB) and the rebase
  refuses fail-closed within two of MaxUint64, so strictly-above holds
  unconditionally; new exhaustion cell pins refusal + saturation on all
  three sites. (2) Cell 2's B setup made baseline-neutral (gate-open +
  zero-offers instead of fixed numbering) so RED runs reach the
  below-helper assertion; Cell R attribution corrected above (scope pin
  for #10041, not repair evidence). (3) Fixture quiescence hardened:
  poller startup prevented via a syncCancel sentinel (no background tick
  can interleave), drop scripting moved under the model lock. Re-ran all
  gates (9/9 cells 5x, full suite, vet, RED worktree); pushed; updated
  the PR body test table.
- **File(s)**: pkg/dataplane/userspace/manager_compile.go,
  pkg/dataplane/userspace/manager_generation.go,
  pkg/dataplane/userspace/compile_generation_rebase_9824_test.go,
  docs/log/9824.md

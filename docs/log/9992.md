# #9992 — tolerant duplicate folding ignored defaulted deny

- **Timestamp**: 2026-09-17
- **Action**: STEP-0 reproduced the issue on `origin/master` at
  `e7cfba758`. A hierarchical zone-pair fixture with an actionless,
  match-bearing `policy p1` followed by a `then permit` duplicate produced
  `action=PolicyDeny`, `ok=false`, and `restricts=false` for the first
  statement. Both tolerant entry points folded the statements into one
  `PolicyPermit` with `LenientContentDropped=false`; the throwaway regression
  therefore failed with the fail-closed-to-fail-open diagnosis. Strict
  `CompileConfig` rejected the fixture. The throwaway file was removed.
- **File(s)**: none (measurement)

- **Timestamp**: 2026-09-17
- **Action**: Widening detection now uses the action returned by `compilePolicy`
  as effective semantics. An empty `terminalActions` list is restrictive when
  that effective action is the #3043 default `PolicyDeny`; explicit permit is
  still required by `statementPermits9571`. The strict compiler path was not
  changed. The warning and type comments now describe explicit deny/reject and
  defaulted-deny forms, and the config contract documents the all-actionless
  non-widening case.
- **File(s)**: `pkg/config/dup_instance_merge_8752.go`,
  `pkg/config/README.md`

- **Timestamp**: 2026-09-17
- **Action**: Added #9992 fail-on-revert cells for zone-pair and global scopes
  through both tolerant entry points. The test retains the permit-fragment
  widening shape, checks #3043's `no terminal action` strict diagnostic for
  full-match fixtures, keeps the missing-match diagnostic pin for the fragment,
  and proves two defaulted-deny statements are not over-poisoned. Existing
  #9571 explicit deny/reject, narrowing, and agreement controls remain green;
  the contradictory actionless control was moved into the widening coverage.
- **File(s)**: `pkg/config/dup_policy_fold_widening_9992_test.go`,
  `pkg/config/dup_policy_fold_widening_9571_test.go`

- **Timestamp**: 2026-09-17
- **Action**: RED-to-GREEN proof. Before the predicate fix, the new
  `TestFoldDefaultedDenyWideningIsPoisoned9992` cases failed in both tolerant
  entry points because the merged permit was unpoisoned. After the final
  restack, the #9992-targeted run reported **25 pass events, 0 failures**. The
  final post-rebase full `pkg/config` suite (after the DHCPv6 master updates)
  reported **10,463 pass events, 4,909 top-level passes, 0 failures**, package
  result `ok` in 195.18s.
- **File(s)**: none (test evidence)

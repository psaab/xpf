# #9656 log

## M40: security-zone groups written without braces

- **Timestamp**: 2026-09-11
- **Action**: Measured at `e09f425dd` through strict `CheckText` and the lenient
  compile. Each of these commits with no error and creates only zone `zga`:
  - `security-zone [ zga zgb ] screen edge;` also drops the screen;
  - `security-zone [ zga zgb ];`;
  - `security-zone [ zga zgb ] { }`;
  - `security-zone [ zga zgb ] apply-groups G;` and `… { apply-groups G; }`,
    where `zga` also lacks the group's statements.

  `bracketedGroupInstances8794` fans out only when `len(Keys) >= 3 &&
  len(Children) > 0`.
- **File(s)**: none (base worktree probe, not committed)

- **Timestamp**: 2026-09-11
- **Action**: Design. Rewrite the group in the #8662 normalizer rather than the
  compiler. The #9620 H7 lesson is that a body compiled after validation bypasses
  the strict gates. Members versus body is decided by schema membership, as the
  #9656 M40 fix direction states. Brackets are not consulted, because
  `Format`/`FormatSet` drop a leaf's bracket mask and a cluster peer compiles
  the rendered text. `bracketedGroupInstances8794` is left unchanged, so its
  interface callers and the #8794 raw-tree discriminator are unaffected.
- **File(s)**: `pkg/config/compact_normalize_zone_group_9656.go` (new),
  `pkg/config/compact_normalize_8662.go` (hook under the `zones` schema)

- **Timestamp**: 2026-09-11
- **Action**: The first cut had no apply-keyword stop. setSchema does not declare
  `apply-groups` inside a zone, so `security-zone trust apply-groups G;` would
  have read as three zones. The member scan now stops at
  `isApplyStatementKeyword`, and `TestZoneGroupStopsAtApplyKeyword9656` pins it.
- **File(s)**: `pkg/config/compact_normalize_zone_group_9656.go`,
  `pkg/config/zone_group_normalize_9656_test.go`

- **Timestamp**: 2026-09-11
- **Action**: Tests written.
  - The tree must equal the longhand in the hierarchical, `groups`,
    compact-head and flat-set shapes.
  - `collectZoneNamesAST` on the normalized tree must agree with the compiled
    zones.
  - The configstore gate must give the longhand's strict verdict and lenient
    compile, and each lenient compile must contain the expected content.

  Positive control: with the tests copied into a base worktree at `e09f425dd`,
  19 of 21 cells failed with #9656 messages. The 2 that passed are the
  single-zone controls, which pass at base by design.
- **File(s)**: `pkg/config/zone_group_normalize_9656_test.go`,
  `pkg/configstore/zone_group_commit_gate_9656_test.go`

- **Timestamp**: 2026-09-11
- **Action**: Branch probe, compared with base:
  - Every group spelling creates both zones with the body.
  - `screen edge tcp-rst` and a shared interface are refused on strict with the
    longhand's messages.
  - `security-zone trust scren edge;`, a typo, now creates empty zones `scren`
    and `edge`; base committed zone `trust` and dropped the statement.
  - `security-zone trust apply-groups G;` with a single zone still drops the
    group, as it did at base. That is not a group spelling, so it is left for a
    separate issue.
- **File(s)**: none (scratch probe, removed)

- **Timestamp**: 2026-09-11
- **Action**: Zone walkers traced; each reads the tree normalized at
  compiler.go:211/522:
  - `compileZones`;
  - `validateZoneIDCollisionAST` (View 1, plus Views 2/3, which clone the
    normalized tree);
  - `runPreWalkGates`, which covers the empty-identity gate and the IPsec and
    WireGuard plaintext advisories;
  - `runUniformGates`, which covers the strict zone gates.

  No non-test code sets `skipCompactNormalize`.
- **File(s)**: none

- **Timestamp**: 2026-09-11
- **Action**: Docs.
  - docs/config-schema.md: the #8794 bracket-remedy row said REFUSE. It now says
    EXPAND. #8794 shipped a fan-out (PRs #8802, #8806; its test asserts every
    zone is created), and #6668 expects the braced group to commit.
  - Added the section "Zone groups written without braces (#9656)".
- **File(s)**: `docs/config-schema.md`, `docs/log/9656.md`

- **Timestamp**: 2026-09-11
- **Action**: Codex round 1 on `4a9ef6590` reported three Medium findings and
  one Low. Each was measured at base `e09f425dd` and on the branch.
  - **F1, Medium, introduced by the first cut.**
    `security-zone [ zga zgb tcp-rst ] { tcp-rst; }` compiled to three zones at
    base and two on the branch, losing zone `tcp-rst`.
    `security-zone [ zga zgb screen ] { screen edge; }` compiled to three zones
    at base. On the branch strict refused it and lenient compiled
    `ScreenProfile=screen`. Hierarchical `Format` renders a braced group
    without brackets (`security-zone zga zgb tcp-rst {`), so a split keyed on
    brackets would not survive rendering.
    **Fix:** a non-empty body decides. The keyword tail is a packed head only
    when `zoneTailHoldsBody9656` finds a complete statement that can hold the
    body. Otherwise every token is a zone, which is #8794's reading. The new
    cells cover a zone named after a flag keyword, after a leaf keyword and
    after an apply keyword, plus a wildcard head and an apply-macro head with
    a body. The configstore gate adds a policy naming zone `tcp-rst`.
  - **F2, Medium, pre-existing for #8794 braced groups.** Measured through
    `configstore.Store` at base and on the branch.
    `deactivate … security-zone zga` deactivates every zone in the group, with
    no error. `show … zga` prints the whole group and `… zgb` prints nothing.
    Codex's `delete` claim did not reproduce: `delete … zga` is refused with the
    #8992 guidance. The leaf form now compiles both zones, so the same
    `deactivate` removes both. Not changed here; filed as #9793.
  - **F3, Medium, introduced.** Cloning the body per member costs members ×
    body nodes, unbounded.
    **Fix:** the cost is charged against `maxParseNodes` with `countNodes`
    before any clone. A group over the budget is left as parsed and compiles
    through `bracketedGroupInstances8794`'s shared body.
    `TestZoneGroupFanOutRespectsNodeBudget9656` pins the boundary at 8 and 9.
  - **F4, Low.** The docs' "ACCUMULATE side" wording was rewritten. The
    bracket-rendering claim was corrected: Format drops brackets on every node,
    FormatSet only on a leaf. "Every zone walker" was narrowed to compile-time
    walkers, with the editing residual named.
- **File(s)**: `pkg/config/compact_normalize_zone_group_9656.go`,
  `pkg/config/zone_group_normalize_9656_test.go`,
  `pkg/configstore/zone_group_commit_gate_9656_test.go`, `docs/config-schema.md`,
  `docs/log/9656.md`

- **Timestamp**: 2026-09-11
- **Action**: Codex round 2 on `9be3a3cf6` reported one High, two Medium and
  one Low.
  - **R2-F1, High, introduced.** The fan-out budget was kept per `zones` node.
    The review's example, 83 stanzas of 100,000 members each (15.84 MiB, 332
    parsed nodes), clones 16.6 million nodes.
    **Fix:** one `zoneGroupPass9656` per normalization of a tree, threaded as
    `normalizeCompactNodesPass9656`. `normalizeCompactNodes` keeps its
    three-argument signature, because tests call it, and runs a pass of its own.
    `TestZoneGroupBudgetSpansTheTree9656` pins a top-level group and a `groups`
    group sharing one budget.
  - **R2-F4, Low, introduced.** A leaf group over the budget compiled only its
    first zone, while the docs claimed a shared-body fallback.
    **Fix:** `validateZoneGroupsExpanded9656`, called from `runPreWalkGates`,
    refuses an unexpanded group on strict and warns on lenient
    (`lenientZoneGroupExpansion9656`). `TestUnexpandedZoneGroupIsRefused9656`
    covers the gate, and `TestOverBudgetZoneGroupIsRefusedAtCommit9656` drives a
    100,001-member group through `CheckText` and the lenient compile.
  - **R2-F2, Medium, introduced.** A keyword that can hold a body was still read
    as the body's head. `security-zone [ zga zgb host-inbound-traffic ]
    { tcp-rst; }` lost zone `host-inbound-traffic`, and `interfaces` and
    `apply-macro M` did the same.
  - **R2-F3, Medium, pre-existing.** `… host-inbound-traffic system-services
    { ping; }` reads as four zones at base and at `9be3a3cf6`. FormatSet replay
    reads it as intended.
  - **Mechanism removed.** Round 1's F1 and round 2's F2 and F3 share one cause:
    reading a keyword before a braced body as the body's head. A non-empty body
    now names zones with every token, which is #8794's reading, and
    `zoneTailHoldsBody9656` is gone, along with the three packed-head-with-body
    cells that asserted it. New cells naming zones `host-inbound-traffic`,
    `interfaces` and `apply-macro M` failed against `9be3a3cf6` (positive
    control: 6 normalize subtests and the configstore cell) and pass on the fix.
    The packed head before a braced body is documented as a residual, unchanged
    from base.
- **Validation**:
  - `-run '9656|8794|6668|8850'`: 114 tests, 0 failures.
  - `go test ./pkg/config/ ./pkg/configstore/`: 10235 tests, 0 failures.
- **File(s)**: `pkg/config/compact_normalize_zone_group_9656.go`,
  `pkg/config/compact_normalize_8662.go`, `pkg/config/compiler_prewalk.go`,
  `pkg/config/compiler_opts.go`, `pkg/config/elided_container_brace_8850_test.go`
  (its comment and message name the renamed recursion),
  `pkg/config/zone_group_normalize_9656_test.go`,
  `pkg/configstore/zone_group_commit_gate_9656_test.go`, `docs/config-schema.md`,
  `docs/log/9656.md`

- **Timestamp**: 2026-09-11
- **Action**: Rebased onto master `6cf4305ab`, because master had changed
  `compiler_opts.go`. The rebase applied cleanly. The targeted run, including
  master's #9623/#9624 cells, passed 117 tests with 0 failures. The matrix
  killed 21 of 21 mutants at `626cf87f9`, with 0 VOID.
- **File(s)**: none beyond the rebase

- **Timestamp**: 2026-09-11
- **Action**: Codex round 3 on `626cf87f9` reported two High findings and one
  Medium. All three came from materialising one statement per zone.
  - **R3-F1, High.** A group of 50,000 members inside `groups node1` charged
    100,000 fan-out nodes, inside the pass budget. It still pushed the node1
    group subtree past `maxGroupExpandWork` (100,000), so node1's tolerant load
    failed while node0's succeeded.
  - **R3-F2, High.** The charge used the body's size before normalization. A
    packed `address-book` body split after it was cloned, and that growth was
    never charged: 10,000 members × 10,000 addresses is about 100 million nodes.
  - **R3-F3, Medium.** `schemaValidateExpandedTreeForNode` normalizes twice,
    through `NormalizeCompactForScan` and then `normalizeCompactForValidation`.
    Each pass gets a fresh budget.
- **Action**: This is the third round tracing to one mechanism, after round 1's
  unbounded clones and round 2's per-`zones` budget. The fan-out is removed
  rather than patched a fourth time.
  - `validateZoneGroupsHaveBody9656` (`compiler_validate_zone_group_9656.go`),
    run from `runPreWalkGates`, refuses a `security-zone` statement that names
    two or more zones without a non-empty braced body. Its message names the
    zones and the braced spelling. Lenient warns (`lenientZoneGroupBody9656`,
    #1960).
  - Braced groups keep #8794's compile-time fan-out.
  - The #9656 acceptance allows a strict refusal that names the spelling.
- **Measured at `6cf4305ab` before building:**
  - **Tree shapes.** On the group-expanded tree, the leaf, packed-tail,
    bracketless and compact-head groups are childless leaves. Empty braces are
    a childless non-leaf, and so is `{ apply-groups G; }` once expansion has
    emptied it. The braced-body and flat-set-with-a-tail groups keep their
    child. A single zone with a packed statement is folded into a child.
  - **Repo spellings.** Every bracketed zone group in the repo's tests and docs
    has a braced body.
- **Restored and removed.** `compact_normalize_8662.go` and the #8850 test are
  restored to `6cf4305ab` content; their diff against it is empty.
  `compact_normalize_zone_group_9656.go` and `zone_group_normalize_9656_test.go`
  are deleted.
- **Validation**:
  - `-run '9656|8794|6668|8850'`: 82 tests, 0 failures.
  - `go test ./pkg/config/ ./pkg/configstore/`: 10204 tests, 0 failures.
  - Positive control: the configstore commit test was copied into a worktree
    at `6cf4305ab`. Its three refusal subtests failed there, because master
    commits each spelling (`got <nil>`), and its five controls passed.
- **File(s)**: `pkg/config/compiler_validate_zone_group_9656.go` (new),
  `pkg/config/zone_group_body_gate_9656_test.go` (new),
  `pkg/configstore/zone_group_commit_gate_9656_test.go` (rewritten),
  `pkg/config/compiler_prewalk.go`, `pkg/config/compiler_opts.go`,
  `pkg/config/compact_normalize_8662.go` and
  `pkg/config/elided_container_brace_8850_test.go` (restored),
  `docs/config-schema.md`, `docs/log/9656.md`

- **Timestamp**: 2026-09-11
- **Action**: Committed the body-less group refusal as `50a280326`. The matrix
  killed 12 of 12 mutants with 0 VOID.
- **File(s)**: as listed in the previous entry

- **Timestamp**: 2026-09-11
- **Action**: Codex round 4 on `50a280326`, capped at Medium and above, reported
  three Medium findings. Each was measured at `6cf4305ab`.
  - **R4-F1.** `security-zone [ zga zga ];` was refused, though master compiles
    the single zone and loses nothing.
  - **R4-F2.** `security-zone [ "tcp-rst" zgb ];` bypassed the check. The member
    scan stopped at the keyword, and `zgb` was lost.
  - **R4-F3.** A `FormatSet` / `load set` replay of `security-zone [ zga zgb ];`
    builds zone `zga` with an unknown child `zgb`, which escapes the check.
- **Action**: Replaced the rule. Measured at `6cf4305ab`: all six zone readers
  go through `bracketedGroupInstances8794`, and none reads a key after the name
  of a childless node. After normalization and group expansion, the childless
  statements that keep extra keys lose them:
  - apply tails;
  - `[ tcp-rst zgb ]`;
  - `[ zga zgb zga ]`.

  The fold moves `description`, `screen`, `tcp-rst`, `interfaces`,
  `host-inbound-traffic` and `address-book` tails into a body first.

  `validateZoneStatementTails9656` now refuses a childless `security-zone`
  statement when any key after the name differs from the name.
  - It resolves R4-F1, since a repeat is allowed, and R4-F2.
  - It also refuses `security-zone trust apply-groups G;`, so #9788's spelling
    is refused until #9788 makes it compile.
  - R4-F3 is a pre-existing loss. FormatSet drops leaf brackets (#9635), and
    the zone schema is open-world. The docs claim is narrowed to the
    hierarchical text, and the replay is recorded as a residual.
  - The lenient flag is renamed `lenientZoneStatementTail9656`.
- **Action**: `TestCompactNormalizeScopePreservesCompiledResult8690` reported
  two things.
  - `security zones security-zone xpfarg description` and `… tcp-rst` were
    refused with the #8662 pass disabled and accepted with it enabled.
  - `… interfaces` and `… screen` were new fixture-limited sites.

  Each site was hand-measured with type-valid values through
  `compileConfigWithOpts`, with `skipCompactNormalize` off and on. The braced
  spelling is accepted both ways. The elided spelling is refused with the pass
  off, naming the dropped statement, and accepted with it on. The gate refuses
  the consequence of a drop that the pass repairs, which is the guard's
  documented benign shape. `description` and `tcp-rst` are registered in its
  `benign` map, and `interfaces` and `screen` in `knownFixtureLimited8690`,
  each with the measurement.
- **Validation**:
  - `-run 'TestCompactNormalizeScopePreservesCompiledResult8690|9656|8794|6668|8850'`:
    90 tests, 0 failures.
  - `go test ./pkg/config/ ./pkg/configstore/`: 10211 tests, 0 failures.
  - Positive control: the configstore commit test copied into a worktree at
    `6cf4305ab`. Its five refusal subtests failed there (`got <nil>`), and its
    six controls passed.
- **File(s)**: `pkg/config/compiler_validate_zone_group_9656.go`,
  `pkg/config/zone_group_body_gate_9656_test.go`,
  `pkg/configstore/zone_group_commit_gate_9656_test.go`,
  `pkg/config/compiler_prewalk.go`, `pkg/config/compiler_opts.go`,
  `pkg/config/compact_normalize_scope_8690_test.go`, `docs/config-schema.md`,
  `docs/log/9656.md`

- **Timestamp**: 2026-09-11
- **Action**: Committed the tail rule as `187dda54e`. The matrix killed 11 of
  11 mutants. T5 was VOID once because the mutant did not build; it was
  re-expressed and killed.
- **File(s)**: as listed in the previous entry

- **Timestamp**: 2026-09-11
- **Action**: Codex round 5 on `187dda54e`, capped at Medium and above, reported
  three Medium findings. Each was measured at master `ed313e4c9`.
  - **R5-F1, introduced.** `security-zone trust apply-macro M;` was refused.
    It compiles exactly like its braced spelling (`trust{}` both ways),
    because no compiler consumes a macro. An `apply-macro` tail is now exempt,
    with an allowed cell and a commit control.
  - **R5-F2, pre-existing.** In a leaf group, a later member spelled like a zone
    keyword is folded into that statement before the check runs.
    - `[ zga tcp-rst ]` commits with no zone named `tcp-rst`, quoted or not.
    - `interfaces`, `host-inbound-traffic` and `address-book` do the same.
    - `description` and `screen` are refused, because they need a value.

    The check cannot see this after the fold, and quotes and brackets do not
    survive rendering (#9635). Actions: a docs residual; the #8690 registry
    text for `tcp-rst` (benign) and `interfaces` (fixture-limited) now states
    the exception; the measured table is posted on #9635.
  - **R5-F3, pre-existing.** Group expansion drops a group's leaf
    `security-zone zga;`, or `[ zga zgb ];`, whenever any inline leaf
    `security-zone …;` exists, because the override in `mergeNodes` matches
    only the keyword. A braced statement on either side merges correctly. Filed
    as #9831, with a docs residual.
- **Action**: Rebased onto master `ed313e4c9`, which had also changed
  `compiler_opts.go`. The rebase applied cleanly. An earlier scratch merge onto
  `443d5a256` had passed the full suites (10234 tests, 0 failures), including
  #9793's tests.
- **Validation**:
  - `-run 'TestCompactNormalizeScopePreservesCompiledResult8690|9656|9793|8794|6668|8850'`:
    101 tests, 0 failures.
  - `go test ./pkg/config/ ./pkg/configstore/`: 10236 tests, 0 failures.
- **File(s)**: `pkg/config/compiler_validate_zone_group_9656.go`,
  `pkg/config/zone_group_body_gate_9656_test.go`,
  `pkg/configstore/zone_group_commit_gate_9656_test.go`,
  `pkg/config/compact_normalize_scope_8690_test.go`, `docs/config-schema.md`,
  `docs/log/9656.md`

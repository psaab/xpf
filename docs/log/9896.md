# #9896 — pool-utilization alarm ignored the tracked-flow cap that refuses flows

- **Timestamp**: 2026-09-15
- **Action**: STEP-0 on base `origin/master 0f69a6164`: defect PRESENT, every
  triage link re-verified rather than trusted:
  - Alarm divides by nominal capacity:
    `pkg/natpoolalarm/natpoolalarm.go:321-325`
    (`capacity = AddressCount*(PortHigh-PortLow+1)`,
    `pct = UsedPorts*100/capacity`).
  - Binding constraint:
    `userspace-dp/src/nat/allocator.rs:105`
    (`MAX_SOURCE_NAT_POOL_TRACKED_FLOWS = 262_144`), per-pool
    `min(nominal, MAX)` at `:1387-1388`, refusal
    `live_by_flow.len() >= max_tracked_flows -> AllocatorExhausted` in
    `allocate_translation_locked` at `:2036-2038`.
  - The sample structs cannot see it: `natpoolalarm.PoolStatus` (`:54-60`)
    and `AppliedNATPoolStatus` (`applied_nat_view.go:32-38`) carry only
    address count/range/UsedPorts; builders drop the flow fields
    (`applied_nat_view.go:139-145`, `daemon_natpoolalarm.go:44-50`).
  - Snapshot already ships both inputs end-to-end: Rust `snapshot()`
    (`allocator.rs:4534+`) -> `status.rs:35,38` -> serde
    `protocol/nat.rs:428-435` -> Go `protocol_counters.go:17,20`, already
    exported by `metrics_userspace.go:368-370` and
    `status_sections.go:544-551`.
  - Repro: synthetic at-cap fixture (`live==max`, ports ratio low) written
    as `TestFlowCapAtCapRaises_9896` — fails on base at COMPILE time
    (`unknown field LiveFlows/MaxTrackedFlows in struct literal of type
    PoolStatus`), which is the structural proof: the monitor cannot
    represent the refusing state. SMALL tier; brief posted to the issue
    before implementing.
- **File(s)**:
  pkg/natpoolalarm/natpoolalarm_tracked_flows_9896_test.go (repro, failing)

- **Timestamp**: 2026-09-15
- **Action**: Fix, pure Go, no Rust/wire change. Plumbed `LiveFlows` +
  `MaxTrackedFlows` through `AppliedNATPoolStatus` -> daemon sampler ->
  `natpoolalarm.PoolStatus`; `evaluate()` now takes
  `max(used_ports/capacity, live_flows/max_tracked_flows)` with the single
  raise/clear state machine unchanged (one transition emits one line even
  when both legs cross — no double-syslog by construction).
  `MaxTrackedFlows == 0` (helper predating the counters) makes the flow leg
  inapplicable: `LiveFlows` ignored, ports-only — every existing
  fixture/golden preserved. Address-only pools stay on the #7361
  inapplicable path (that deliberate decision + its compiler warning are
  untouched). README formula section updated to the max-of-legs.
- **File(s)**: pkg/natpoolalarm/natpoolalarm.go,
  pkg/dataplane/userspace/applied_nat_view.go,
  pkg/daemon/daemon_natpoolalarm.go, pkg/natpoolalarm/README.md,
  pkg/dataplane/userspace/applied_nat_view_test.go (passthrough pin),
  pkg/natpoolalarm/natpoolalarm_tracked_flows_9896_test.go (5 cells)

- **Timestamp**: 2026-09-15
- **Action**: Verification. New cells pass (at-cap raises at 100%, dual-leg
  crossing emits exactly 1 raise at 95%, `Max==0` ignores absurd LiveFlows
  and still raises ports-only, flow-leg clear, band hold refreshes to 75%
  silently); each named test 5x green. Full suites green:
  `pkg/natpoolalarm`, `pkg/dataplane/...` (dataplane, runtime, userspace,
  format), `pkg/daemon/...`. `go vet` and `gofmt` clean on all touched
  packages.
- **File(s)**: none (measurement)

## Fold (parent review: GPT + SPARK NEEDS-MAJOR, converged)

- **Timestamp**: 2026-09-15
- **Action**: The first cut kept address-only pools on the #7361 inapplicable
  path (`continue` before the flow leg) — wrong. Re-verified against the
  refusing code instead of the earlier log claim: address-only tokens LIVE in
  `live_by_flow` and admission refuses at the SAME cap
  (`reserve_address_only_maybe_persistent` at `allocator.rs:3608`,
  roundrobin at `:4076` with the comment "the address-only token lives in
  `live_by_flow`", persistent at `:4223` — all
  `live_by_flow.len() >= max_tracked_flows -> AllocatorExhausted`). Live
  proof: `tests_addr_only_sibling_9131.rs:418-419` reports `used_ports == 0`
  with `live_flows == 2` — refusing while the ports leg reads healthy. The
  #7361 rationale covers only ports/address-count denominators (the
  100%-after-first-flow pathology), NOT live/max, which is monotonic in the
  refusing resource. Restructured `evaluate()`: the flow leg is evaluated
  INDEPENDENTLY of port translation — address-only pools raise/clear on
  live/max alone (ports-sample validation skipped), share the single
  raise/clear switch (still one line per transition), and are recorded
  inapplicable only when NEITHER leg is measurable (MaxTrackedFlows == 0);
  absent samples HOLD without touching the record (marking there would
  delete a raised flow alarm via the unlatch). Reconciled the #7361 compiler
  advisory to the narrowed true claim (ports leg dead + collision mode
  silent; tracked-flow leg is the signal) and updated its two text
  assertions. New cells: address-only at-cap raise (with corrupt ports
  fields, proving independence), no-cap inapplicable, unmeasurable
  silent-unlatch, absent-sample HOLD.
- **File(s)**: pkg/natpoolalarm/natpoolalarm.go,
  pkg/config/natpoolalarm_inapplicable_7361.go,
  pkg/config/natpoolalarm_inapplicable_7361_test.go,
  pkg/natpoolalarm/natpoolalarm_tracked_flows_9896_test.go,
  pkg/natpoolalarm/README.md

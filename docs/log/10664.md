# #10664 — bound allow-dns-reply QR by IP-declared datagram end

- **Timestamp**: 2026-09-24
- **Action**: `dns_reply_qr_set` now requires a parseable IPv4/IPv6 declared L3 end and rejects UDP extents that cross it before reading the DNS QR bit. Regression cells cover an out-of-datagram QR byte, UDP-length overrun with capture slack, an in-datagram reply control, and malformed IPv4; the IPv6 twin pins `payload_len` handling.
- **Validation**: fail-on-revert run `cargo test --manifest-path userspace-dp/Cargo.toml --bins dns_reply_qr_bounded` failed both v4/v6 cells with the old capture bound. Fixed run `cargo test --manifest-path userspace-dp/Cargo.toml --bins afxdp::tests_embedded_poll_filter` passed all 66 tests in 2 suites.
- **File(s)**: `userspace-dp/src/afxdp/poll_descriptor/mod.rs`, `userspace-dp/src/afxdp/tests_embedded_poll_filter.rs`, `docs/log/10664.md`

# Filter defect trio verification

## #10293: interface filter bindings

- RED: the focused strict cases for `input-list`, `output-list`, `rpf-check`, and
  input/output policer bindings compiled cleanly; the tolerant case emitted no
  warning.
- GREEN: `go test ./pkg/config -run '10293|10294|7273|4313|10078|8480|9875|2419|2506' -count=1`
  passed after the interface-family gate was added. The gate covers `lo0` and
  both inet families while preserving single-name input/output bindings.

- P2 RED: bracket and block `filter input/output` with multiple values
  committed cleanly while `nodeVal` retained only the first value.
- P2 GREEN: `go test ./pkg/config -run 'TestInterfaceFilter|10293' -count=1`
  passes with strict #10293 rejection for input/output bracket, block, packed,
  and inet6 cases; single-name bindings remain accepted.
- Round-two RED: `filter { input { f1 f2; } }` and the output twin committed
  cleanly because one child carried multiple Keys.
- Round-two GREEN: the packed-block input/output and inet6 cells reject under
  #10293; single-child blocks retain their binding and remain warning-free.

## #10294: TERM and source-NAT open-world paths

- RED: removing the TERM/source-NAT recording and validation let the unknown
  children commit and produced no tolerant warning.
- GREEN: the focused 10294 tests reject or warn unknown TERM children and
  source-NAT action children, while packed known modes, pool names, and the
  persistent-nat tail remain compiler-owned. The focused regression command
  above passed.

## #10295: referenced prefix-list entries

- RED: removing the referenced-entry gate let malformed IPv4 and IPv6 prefix
  entries commit.
- GREEN: the focused 7273 tests reject malformed referenced entries, accept
  valid referenced entries, and ignore malformed unreferenced lists. The
  focused regression command above passed.

## Suite proof

- `go test ./pkg/config -run '10293|10294|7273|4313|10078|8480|9875|2419|2506' -count=1`: PASS.
- `go test ./pkg/config -run 'Test(SourceNAT.*10294|UnrecognisedContainerDoesNotFabricateAnExemption_7033|NATSourcePoolPackedAddress8800)' -count=1`: PASS.
- `go test ./pkg/config -count=1`: PASS (`ok`, 220.992s; latest round-two run).
- `go test ./pkg/config -list '^Test' | wc -l`: 5025 listed package tests.

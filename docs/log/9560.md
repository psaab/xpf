# #9560: the XDP steering map DELETE was identity-blind

## Re-read at `origin/master` `7c494b5ca`

The issue has no comments. Its body is the spec, and every premise holds at this commit.

- `userspace_sessions` (`userspace-xdp/src/lib.rs:419`, HashMap, `max_entries` 262144) is keyed on
  `UserspaceSessionMapKey` (`userspace-dp/src/afxdp/bpf_map/mod.rs:70`). That 40-byte bare tuple carries neither
  `routing_domain` nor the tunnel `discriminator`.
- Every steering write goes through two primitives, `publish_session_map_key` and `delete_live_session_key`. The
  non-test call counts match the handoff exactly: publish_session_map_key 2, delete_live_session_key 6,
  publish_live_session_entry 8, delete_live_session_entry 5,
  delete_session_map_entry_for_removed_session 4, delete_session_map_redirect_for_session 2. Twenty-five production
  functions take `session_map_fd: c_int`.
- `reverse_canonical_key` (`session/key.rs:299`) sets `routing_domain: 0` (#7160). So two domains'
  reverse-canonical rows are identical `SessionKey`s, and a row's owner must be the entry key.
- On a steering-map miss the shim (`userspace-xdp/src/lib.rs` ~697-760) sends a local destination and ICMP to an
  interface-NAT local address to the kernel. Transit and other interface-NAT traffic falls through to XSK redirect.
  So the DELETE arm changes a packet's fate only for host-bound and interface-NAT-ICMP destinations.
- The steering fd path runs: `ReconcileSnapshotFds` (`coordinator/reconcile/snapshot.rs:283`) ->
  `BindingPlan.session_map_fd` (`bringup.rs:316`) -> `BpfMaps` (`bringup.rs:408`) -> worker `create`
  (`worker/mod.rs:420`) -> `WorkerBpfMaps` -> worker setup (`setup.rs:266`). The coordinator and HA-import sites read
  it from `self.bpf_maps.load()`.
- A bringup replays synced sessions (`bringup.rs:496`, `replay_synced_sessions`). Nothing at worker startup
  republishes worker-local sessions.

## Actions

- **Timestamp**: 2026-09-11
  - **Action**: Worktree created from `origin/master` `7c494b5ca`. The handoff facts were re-verified (only a comment
    changed since `6d930698c`). Plan written and sent to a hostile Codex plan review.
  - **File(s)**: scratchpad `9560-plan.md` (not in the tree), `docs/log/9560.md`.
- **Timestamp**: 2026-09-11
  - **Action**: The first Codex plan review was **VOID**. After about 145K tokens of exploration the configured model
    (`gpt-daybreak-blue-latest`) answered `Selected model is at capacity`, so no verdict file was written and the
    wrapper exited 1. That is an infrastructure failure, not a finding. The review was re-launched on the SAME
    configured model with a bounded retry that fires only on the capacity error. A run counts only if its verdict file
    exists and contains `PLAN YES`/`PLAN NO`. A known trap motivated that check: an unsupported `-m` model once exited
    0 having run no review.
- **Timestamp**: 2026-09-11
  - **Action**: The plan's D4 (registry lifetime) was corrected BEFORE the re-run. The first draft carried the registry
    across a bringup by kernel map id. Two reads showed that was wrong:
    - Worker-local sessions die at a bringup. Each worker builds a fresh `SessionTable::new()` in setup
      (`worker/loop_body/setup.rs:139`), and `tear_down` (`coordinator/reconcile/teardown.rs:54`) preserves only
      synced sessions. A carried registry would keep dead sessions' owners forever, suppressing legitimate deletes
      and leaking REDIRECT rows.
    - `replay_preserved_sessions` runs before `spawn_workers` (`coordinator/reconcile/bringup.rs:157` vs `:172`).
      So a fresh registry per `BpfMaps` is repopulated for the surviving synced sessions before any new worker can
      touch an aliased row.
    The corrected D4 creates the registry alongside `ReconcileSnapshotFds` (`snapshot.rs:280`), clones it into each
    `BindingPlan` and into `WorkerBpfMaps`, and stores it in `BpfMaps` from `publish_runtime`. It needs no map-identity
    FFI. The review prompt says D4 changed and asks for its claims to be verified.
  - **File(s)**: scratchpad `9560-plan.md`, `codex-9560-plan-prompt.txt` (not in the tree), `docs/log/9560.md`.
- **Timestamp**: 2026-09-11
  - **Action**: The second Codex plan review was also lost. It logged three more `at capacity` errors before the host's
    memory reaper killed it (swap 38/46 GiB; other lanes' rustc and rust-analyzer at about 2 GB each). No verdict
    exists, and no orphaned codex process was left (checked by cwd). With both reviewer paths blocked by infrastructure,
    the plan stage was reviewed adversarially here, read-only against the code. The hostile Codex review moves to the
    code stage and still gates landing. Results:
    - **Completeness.** The full production caller inventory of the steering primitives and wrappers matches the
      design. D3's file list was missing `ha/session_import.rs` (`:346` publish, `:760` delete), `worker_loop`
      (`worker/loop_body/mod.rs:984`), `install_helper_local_session_on_miss` (`forwarding/local_delivery.rs:103/140`),
      `install_reverse_session_from_forward_match` (`shared_ops.rs:1129`), the three calls in
      `poll_binding_process_descriptor` (`poll_descriptor/mod.rs:3121/3480/6098`), and `teardown_tcp_rst_flow`
      (`session_glue/mod.rs:1522-1523`). The last one takes the fd from a struct (`current.bpf_maps.session_map_fd`),
      so the handle must be constructible from `WorkerBpfMaps`.
    - **Survivor republishes.** HA prewarm and the owner-RG republish run from `ha/state.rs handle_activated_rgs`; the
      lo0 republish runs from `worker_loop`. All go through the primitives, so each registers owners when it runs.
    - **A Go-side rewrite behind the registry.** `flushStaleBPFStateOnCtrlEnableLocked`
      (`pkg/dataplane/userspace/helper_status_apply.go:326-336`) deletes keys from the pinned `userspace_sessions` map
      on the first ctrl enable after daemon start, and the helper's registry never sees it. The residual is stale
      owners, which fail safe as a leaked REDIRECT row. Its size is still to be measured.
    - **Companion semantics.** `companion_keeps_alive` (`session/expire.rs:512`) reads only the session table, never the
      steering map. Nothing found relies on a reverse row vanishing while the forward half lives.
  - **File(s)**: `docs/log/9560.md`.
- **Timestamp**: 2026-09-11
  - **Action**: Plan-stage verification finished. Two items settled:
    - **Per-packet cost.** The three steering calls in `poll_binding_process_descriptor` are on session-install branches
      only: after the forward-fragment association install (`:3121`), under `if reverse_installed` (`:3480`), and in the
      #2170 missing-neighbour seed (`:6098`). The registry lock is taken once per installed session, beside the
      `bpf_map_update_elem` those calls already make, and never per packet.
    - **The Go startup flush is not a stale-owner leak. The earlier reading was wrong.**
      `flushStaleBPFStateOnCtrlEnableLocked` deletes EVERY key in `userspace_sessions` (a NextKey/Delete loop), but it
      ends no session. The owners the registry holds therefore still belong to live sessions and are removed when those
      sessions end. A later publish rewrites the row, and the last owner's delete of an already-absent row is a harmless
      ENOENT. The flush removing live sessions' rows is existing behaviour, outside #9560's scope.
  - **File(s)**: `docs/log/9560.md`.
- **Timestamp**: 2026-09-11
  - **Action**: Implementation started with the registry itself.
    - `bpf_map/steering_owners.rs` adds `SteeringRowOwners`: 64 cache-padded, poison-tolerant `Mutex<FastMap>` shards
      keyed by the 40-byte row. Owners are stored inline in a `SmallVec<[SessionKey; 1]>`, so a row costs no heap
      allocation. Shard selection uses a per-process seed, following the #7752 rationale.
    - `add` is idempotent. `remove` returns `Remaining`, `Emptied` or `Unregistered`, and a non-owner removing from a
      row that others own gets `Remaining`.
    - Unit cells in the module: single owner, shared row, non-owner, unknown row, idempotent add, shard bounds,
      poison recovery.
    - `session_map_row` in `bpf_map/mod.rs` gives a key's row bytes, with a compile-time size assertion.
    - The first wiring attempt failed its own anchor check and wrote nothing. A filtered read had hidden a comment
      block between the `mod` declarations, and had also hidden the doc comment above `SessionMapWriteRecord`. Had it
      applied, the row function would have taken that struct's doc comment. Re-applied with unfiltered anchors.
  - **File(s)**: `userspace-dp/src/afxdp/bpf_map/steering_owners.rs` (new), `userspace-dp/src/afxdp/bpf_map/mod.rs`,
    `docs/log/9560.md`.
- **Timestamp**: 2026-09-11
  - **Action**: Carrier shape decided from the construction-site reads. Worker `create` already takes every map fd as
    a separate parameter and is well past the >8-parameter cue, so the registry does not become one more parameter.
    An owned `SteeringMapRef { fd, owners: Arc<SteeringRowOwners> }` replaces `session_map_fd: c_int` in `BindingPlan`,
    `WorkerBpfMaps`, `create` and worker setup. Call sites take a borrowed `Copy` handle. `ReconcileSnapshotFds` gains the
    `Arc`, and the exhaustive destructure in `publish_runtime` makes the compiler force it into `BpfMaps`. No Rust source
    was edited while the first `cargo check --tests` ran.
  - **File(s)**: `docs/log/9560.md` (plan in scratchpad).
- **Timestamp**: 2026-09-11
  - **Action**: `cargo check --tests` on `7c494b5ca` plus the registry module and `session_map_row`: exit 0, no
    errors (log `/var/tmp/x9560-check.log`). The only new diagnostic is the expected
    "`session_map_row` is never used", which the primitive rewrite consumes. No Rust source was edited while the
    check ran. The primitive rewrite was staged as a script during that window and is applied only after this result.
  - **File(s)**: `docs/log/9560.md`.

- **Timestamp**: 2026-09-11 09:50 UTC
- **Action**: threaded the owner registry through every steering call site (compiler-driven).
  - Production, 110 edits in 20 files: functions that took `session_map_fd: c_int` for
    steering writes take `SteeringMap<'_>` (fd plus `&SteeringRowOwners`; Copy, no refcount on
    the packet path). Fd-only uses (`count_bpf_session_entries`, `verify_session_key_in_bpf`,
    `dump_bpf_session_entries`, debug prints, the `< 0` early returns) read `.fd`. Workers reach
    it through `WorkerBpfMaps.session_map.handle()`; HA import and activation build it from the
    loaded `BpfMaps`; `bring_up_workers` captures `fds.session_map_owners` before
    `publish_runtime` and replays through it. New owner arguments: the poll_descriptor
    reverse-key publish passes `&reverse_key`; the `handle_delete_synced` no-entry arm passes
    `&key`.
  - Tests, 87 sites: a bare `-1` (86) or `NOT_A_MAP_FD` (1) became
    `SteeringMap::unshared_for_test(fd)`, the same fd with a private leaked registry, so no
    delete is ever suppressed (the pre-#9560 behaviour). The first draft hard-wired fd -1
    (`closed_for_test()`), which silently turned the #9517 cell's `i32::MAX` into `-1` and
    into its `< 0` early return. The compiler's unused-const warning on `NOT_A_MAP_FD` exposed it.
- **Action**: rustfmt measurement was VOID, then redone. Stdin `rustfmt --check` exits 0 while
  printing a diff (control: a mis-formatted snippet gave rc 0), so a first "every file clean"
  table measured nothing. Redone by comparing `--emit stdout` with the input, with a bad and a
  good control. Most touched files are not rustfmt-clean at base, so only hunks overlapping
  this change were applied. That partial-hunk application DROPPED a statement in `bringup.rs`
  (`let replayed_synced_sessions = coord.replay_synced_sessions(`): diff split one reformat
  into a deletion hunk on a changed line and an insertion hunk off it. The compiler caught it
  (E0425). Restored from HEAD with the one intended substitution, and every fmt-touched file
  re-audited with a whitespace-blind token diff against HEAD: only intended tokens differ.
- **Action**: design correction, a check-then-act race. The registry decided ownership under the
  shard lock but issued the BPF write or delete after releasing it: teardown T finds a row
  `Emptied` or `Unregistered` and releases, sibling P registers and writes, then T's delete
  removes P's row. That is the #9560 consequence again, in a narrower window, and my
  adversarial pass on the plan missed it. `add_then`/`remove_then` now run the syscall inside
  the shard lock (one map syscall under a 1-of-64 shard lock, on setup and teardown only).
  Cell C9 binds it through the recorder (`owner_lock_held`).
- **Action**: cells. `RECORDER_ONLY_MAP_FD` (cfg(test)): the primitives record the write and
  report success without a syscall, so an entry publish reaches every derived row. With
  `NOT_A_MAP_FD` the first failed syscall's `?` stops at row one, and no multi-row cell could
  observe owners. C1-C7, C8 (`BpfMaps::default` lifetime) and C9 are in
  `session_glue/steering_row_owners_9560_tests.rs`; the C8 replay cell is in
  `coordinator/tests.rs`. `host_bound_key` in the #9517 module is now `pub(super)`.
  A bash heredoc left unquoted ran two backtick spans of a new doc comment as commands and
  blanked them; repaired.
- **Action**: docs. `forwarding/README.md` residual (d) and the collision-surface bullet;
  `coordinator/README.md` `BpfMaps` row; `afxdp/README.md` measurement note; `session/README.md`.
- **Files**: userspace-dp/src/afxdp/{bpf_map/{mod.rs,steering_owners.rs},
  coordinator/{mod.rs,bpf_maps.rs,tests.rs,README.md,reconcile/{mod.rs,snapshot.rs,bringup.rs}},
  forwarding/{local_delivery.rs,tests.rs,README.md}, ha/{session_import.rs,state.rs}, ha_tests.rs,
  poll_descriptor/mod.rs, session_delta.rs, session_glue/{mod.rs,promote.rs,tests.rs,
  commands/{delete_synced,demote_owner_rgs,refresh_owner_rgs,upsert_synced}.rs,
  deleted_first_policy_purge_9526_tests.rs,routing_domain_publish_9517_tests.rs,
  steering_row_owners_9560_tests.rs}, shared_ops.rs, shared_umem_tests.rs, tests_bind_forward.rs,
  types/runtime.rs, worker/{mod.rs,bpf_maps.rs,loop_body/{mod.rs,setup.rs,debug_report.rs}},
  README.md}; userspace-dp/src/session/README.md

- **Timestamp**: 2026-09-11 09:57 UTC
- **Action**: added a `stop` registry cell (`stop_starts_an_empty_steering_row_owner_registry_9560`,
  coordinator/tests.rs), so a mutant that carries the registry across `stop_inner`'s fresh
  `BpfMaps` can be killed. Two test comments that named `session_map_fd` now say "steering-map fd".
- **Result**: `cargo test --bin xpf-userspace-dp` (dev, pre-rebase, before the stop cell):
  5721 passed, 0 failed, 6 ignored. Filtered `9560` run after the stop cell: 18 passed, 0 failed.
- **Files**: userspace-dp/src/afxdp/coordinator/tests.rs, userspace-dp/src/afxdp/session_glue/tests.rs

# #9989 — unzoned-ingress deny attribution must not alias default-policy

- **Timestamp**: 2026-09-17
- **Assigned base**: `05461194fe1ffc7b42f091eccd73b930b546ed4b`
  (`origin/master` when the lane was created). The detached baseline repro
  used `adabc048e`; the final branch was rebased onto
  `82898ac28f811a2658c190dc920a1135514bbbfb`.
- **Action**: Read the issue and verified the reported sites. The unzoned-ingress
  arm incremented `UNZONED_INGRESS_DENIED` but returned
  `DEFAULT_POLICY_SENTINEL_ID` (`u32::MAX`), while the Go reserved-name
  resolver renders that value as `default-policy`; `UnattributedPolicyID` is
  the existing zero wire id rendered as `unattributed`.
- **File(s)**: `userspace-dp/src/policy.rs`,
  `pkg/dataplane/policy_display.go`, `pkg/logging/ringbuf.go`

- **Timestamp**: 2026-09-17
- **Action**: STEP-0 was reproduced in a detached checkout at the then-current
  `origin/master` (`adabc048e`). A throwaway policy test exercised the real
  #6682 evaluator with `from_id == 0`; the result was `Deny` with
  `policy_id == DEFAULT_POLICY_SENTINEL_ID` (`4294967295`), proving the
  unzoned-ingress deny aliased the default-policy id before any lane changes.
- **Validation**: `CARGO_TARGET_DIR=/dev/shm/cargo-9989-base cargo test
  --bin xpf-userspace-dp repro_9989_on_detached_origin_master` — **1 passed,
  0 failed, 6293 filtered**. The throwaway test was removed and the detached
  worktree was restored cleanly.

- **Timestamp**: 2026-09-17
- **Action**: Added the explicit Rust `UNATTRIBUTED_POLICY_ID` wire-contract
  constant (`0`) and changed only the #6682 unzoned-ingress return to use it.
  The existing `UNZONED_INGRESS_DENIED` increment remains before the return;
  the result still carries `policy_counter_idx == 0`, and the reserved
  `default-policy` hit counter is not touched by this arm. The affected
  forwarding agreement assertion now expects `unattributed` for an ingress
  zone-0 deny; egress-only zone-0 fall-through tests continue to expect the
  genuine default-policy sentinel.
- **File(s)**: `userspace-dp/src/policy.rs`,
  `userspace-dp/src/afxdp/forwarding/tests.rs`

- **Timestamp**: 2026-09-17
- **Action**: Added fail-on-revert coverage. The policy cell pins Deny,
  `UNATTRIBUTED_POLICY_ID`, one `UNZONED_INGRESS_DENIED` increment, no
  default-policy counter increment, and no rule-counter handle. The RT_FLOW
  cell evaluates the same unzoned verdict, passes its `policy_id` through the
  transit emitter, decodes the frame, and requires the result, event
  `policy_id`, and mirrored `rule_id` to agree.
- **File(s)**: `userspace-dp/src/policy_tests.rs`,
  `userspace-dp/src/afxdp/event_emit_tests.rs`

- **Timestamp**: 2026-09-17
- **Action**: Restoring the sentinel in the unzoned arm made both #9989 cells
  RED; restoring `UNATTRIBUTED_POLICY_ID` made both GREEN.
- **Validation**: RED **0 passed, 2 failed**; restored GREEN **2 passed, 0
  failed** (`CARGO_TARGET_DIR=/dev/shm/cargo-9989 cargo test --manifest-path
  /home/ps/git/pi-xpf/.claude/worktrees/9989-policy-id/userspace-dp/Cargo.toml
  9989 -- --nocapture`, all-target run: 13 suites, 6430 filtered). The
  equivalent `--bin xpf-userspace-dp 9989` selector reported 6293 filtered
  on that validation base; the final `82898ac2` rebase reports 6299 filtered.

- **Timestamp**: 2026-09-17
- **Action**: Affected Rust modules are GREEN: policy **190 passed, 0
  failed**; RT_FLOW emitter **22 passed, 0 failed**; forwarding **153
  passed, 0 failed**; unzoned selector **4 passed, 0 failed**.
- **Validation**: `CARGO_TARGET_DIR=/dev/shm/cargo-9989 cargo test --bin
  xpf-userspace-dp ... -- --test-threads=1` for each affected module.

- **Timestamp**: 2026-09-17
- **Action**: Pre-rebase full userspace binary validation reached all tests. The
  only remaining failure is the pre-existing additive-wire guard #7689, which
  is unrelated to policy attribution and reports exhaustive literal ceilings
  for `DestinationNATRuleSnapshot`, `FabricSnapshot`, and `NeighborSnapshot`.
- **Validation**: **6288 passed, 1 failed, 6 ignored**; the #7689 failure was
  unchanged by this diff in that pre-rebase run.

- **Timestamp**: 2026-09-17
- **Action**: Added the Go-side raw `POLICY_DENY` render assertion for
  `UnattributedPolicyID`, with configured runtime id 0 occupied by `allow-web`;
  the reserved zero id must render as `unattributed` in both the decoded record
  and structured RT_FLOW text. Added a source-parsing Rust/Go lockstep pin for
  `UNATTRIBUTED_POLICY_ID`.
- **File(s)**: `pkg/logging/default_policy_sentinel_3057_test.go`
- **Validation**: `go test ./pkg/logging -count=1` — **passed**.

- **Timestamp**: 2026-09-17
- **Action**: Rebased the implementation commit onto current `origin/master`
  (`82898ac28f811a2658c190dc920a1135514bbbfb`) and reran the affected
  Rust modules plus the Go logging package.
- **Validation**: policy **190 passed** (6111 filtered), RT_FLOW emitter
  **22 passed** (6279 filtered), forwarding **153 passed** (6148 filtered),
  #9989 selector **2 passed** (6299 filtered), and
  `go test ./pkg/logging -count=1` **passed**.

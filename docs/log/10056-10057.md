# Issues #10056 and #10057 — group merge residuals

## Context

The complete bodies and all comments for #10056 and #10057 were read before implementation. Both issues had zero comments; neither withdrew, narrowed, marked the defect shipped, nor contradicted the requested scope. `history://EngGrpTrio` and `history://Eng9859` were also read before editing.

#10056 is a process constraint, not a behavior change: the #9859 peer-path admission and #9855 successive-merge suppression must evolve together. A nested-group promotion may also present as container-src versus leaf-dst; that pre-existing braced-group/inline-leaf class coalesces at compile (#10048 syslog; VRRP group-ID last-wins).

#10057's shared `packedBodyChildren` implementation already carried `KeysQuoted` and `KeysBracketed` in this checkout (from the earlier provenance change). This change adds load-bearing cells for both callers and records the shared contract; no compiler reader was changed.

## #10056 change

- Documented the two-path co-evolution contract at `suppressSuccessiveLeaf9855`, including the nested container-src/leaf-dst note.
- Strengthened `TestGroupPackedDifferentInstanceParity10056` with facility and bare-leaf rows that compare both source orders and explicitly exercise the peer-path admission versus successive-suppression boundary.
- Extended `TestPackedGroupLeafVRRP9855` with VRRP IDs 1 and 2 in both source orders, comparing the compiled ID-to-priority maps (`ID1=200`, `ID2=150`).

### RED then GREEN

- **RED — peer guard:** temporarily forced the peer-path identity branch to the legacy keyword fallback. The shipped parity cells failed: the reverse source order lost the second syslog host, and the VRRP different-ID order produced only `map[1:200]`.
- **RED — successive guard:** temporarily disabled `namedLeafCanAdopt9859` admission in `suppressSuccessiveLeaf9855`. The shipped parity cells failed: same-ID-first lost host/VRRP ID 2 while the reverse order passed.
- Both guards were restored; the focused parity/VRRP cells passed.

## #10057 change

- Added `TestPackedTailProvenance10057` for both synthesis callers:
  - #9855 inline-leaf promotion: `match "a b"` retains `KeysQuoted`; the promoted identity masks remain aligned.
  - #7648 group-side container-peer merge: bracketed `virtual-address [ ... ]` retains `KeysBracketed` on the synthesized child and masks remain aligned.
- Added a shared-helper source comment documenting that both callers depend on parallel quote/bracket masks. The existing promotion truncation and synthesis transfers remain the single implementation path.

### RED then GREEN

- **RED — shared helper:** temporarily disabled both mask transfers in `packedBodyChildren`; the quoted-match mask was empty and the bracketed virtual-address mask was empty.
- **RED — #9855 truncation:** temporarily disabled mask truncation alongside the promoted peer's identity keys; the promoted host carried a four-entry quote mask for two identity keys, violating alignment.
- The existing mask transfers were restored; both provenance cells passed.

## Verification

- Focused command: `go test -json ./pkg/config -run 'TestPackedGroupLeafVRRP9855|TestGroupPackedDifferentInstanceParity10056|TestPackedTailProvenance10057' -count=1` — 15 passing test events, zero failures (`0.193s`).
- `gofmt -d` over all changed Go files — no output.
- `git diff --check` — PASS.
- Full `go test -json ./pkg/config -count=1` — 10,656 passing test events, zero failures (`238.198s`).
- No production compiler reader changed behavior; the full suite covers existing syslog/VRRP readers.

## Delivery

- `0f4a7b2c4` — `fix(config): pin successive merge source-order parity (#10056)`
- `4b5892d5e` — `fix(config): pin packed tail provenance (#10057)`
- Docs/log commit follows this log.

No merge was performed; parent owns review and merge.

# #9655: the bulk-start zone snapshot answers an RG-unmapped zone with RG 0

Branch `fix/9655-zone-snapshot-rg0-fallback`, from origin/master `c5a5e5c24`.

- **Timestamp**: 07:26
- **Action**: Read the issue and the code.
  - `snapshotZoneOwnership` listed only zones in `zoneRGMap`.
  - `reconcileStaleSessions` skipped on an empty snapshot and answered `true` (keep) for any zone the snapshot did not list.
  - `ShouldSyncZone` answers an unmapped zone with `IsPrimaryFn`.
  - `ReconcileClusterBulk` deletes forward sessions whose ingress zone answers false and that are absent from the received set. Sessions carry no origin flag.
  - `buildZoneRGMap` maps a zone only through RETH interfaces, and always installs a non-nil map.
- **File(s)**: none

- **Timestamp**: 07:31
- **Action**: Wrote the cells. On the unfixed base `c5a5e5c24`, three cells fail as the issue describes: the RG 0 secondary keeps the stale unmapped-zone session, a cluster with no mapped zones skips the reconcile, and a zone map moved during the bulk still decides by the old snapshot. The RG 0 primary control passes.
- **File(s)**: `pkg/cluster/sync_zone_snapshot_rg0_9655_test.go`

- **Timestamp**: 07:32
- **Action**: Applied `9655-apply.py`:
  - `zoneOwnershipSnapshot` holds the zone answers, `unmapped` (the RG 0 answer) and `mapGen`;
  - the reconcile uses `zoneSnap.shouldSync`, and skips when the map generation changed;
  - `SetZoneRGMap` bumps `zoneRGMapGen` only when `maps.Equal` says the contents changed;
  - `ShouldSyncZone`'s unmapped answer moved into `shouldSyncUnmappedZone`, so both paths share it.

  `go build` and `go vet` clean.
- **File(s)**: `pkg/cluster/sync.go`, `pkg/cluster/sync_conn_sweep.go`

- **Timestamp**: 07:36
- **Action**: The full suite failed one existing test, `TestReconcileSkipsNonEmptyBulkWithoutZoneSnapshot` (470a2b7ba). It deliberately installs NO zone map and expects the reconcile to skip, which guards a bulk that arrives before the daemon wired zone ownership. That is a different fact from an installed empty map, because `buildZoneRGMap` installs a non-nil map even when it names no zone. So the guard is kept: a nil map takes no snapshot, and the reconcile skips. My cell now asserts the same thing for a never-installed map, and asserts a reconcile only for an installed empty one.
- **File(s)**: `pkg/cluster/sync.go`, `pkg/cluster/sync_zone_snapshot_rg0_9655_test.go`

- **Timestamp**: 07:44
- **Action**: The 4 cells, the guard test, the #5085, #6031 and #9174 reconcile tests, and the full `go test ./pkg/cluster` (87.8 s) all pass. `go vet` is clean.
- **File(s)**: none

- **Timestamp**: 07:50
- **Action**: The consequence the issue does not weigh: a zone of node-local (non-RETH) dataplane interfaces is RG-unmapped too. No cluster-mode commit rule rejects one, and sessions carry no origin flag. So on the RG 0 secondary, that node's own flows there are now reconciled away at every bulk; so is every flow it carries when no zone is RG-mapped. Documented this in the README, and raised it with the lead as a decision: ship as the issue says, ship only the map-change and nil-map parts, or hold. Committed locally (not pushed) so race and the matrix can run meanwhile.
- **File(s)**: `pkg/cluster/README.md`, `docs/log/9655.md`

## Option B: ship the safe half, hold the RG 0 answer

- **Timestamp**: 2026-09-11 19:02 PDT
- **Action**: The lead ruled option B on the decision I raised at 07:50: ship the map-change skip and the
  nil-vs-empty distinction, with an empty installed map still SKIPPING, and do not ship the RG 0 answer for
  RG-unmapped zones. Their reason is my own measurement: a zone of node-local, non-RETH interfaces is
  RG-unmapped too, so the RG 0 answer deletes that node's own live flows at every bulk and a TCP flow dies on
  its next non-SYN packet — worse than the stale peer-owned rows the issue is about. Land as `Refs #9655`
  and leave the issue open for the second half.
  - Kept from the first attempt: `zoneRGMapGen` with the `maps.Equal` gate, and the mid-bulk map-change skip.
  - Changed: `zoneOwnershipSnapshot` no longer carries the `unmapped` RG 0 answer; `shouldSync` answers
    from the map and KEEPS a zone the map does not name. `snapshotZoneOwnership` takes no snapshot when the
    map names no zone (`len(m) == 0`), so nil and installed-empty both skip.
  - Reverted: the `shouldSyncUnmappedZone` extraction in the sweep, which existed only to share the RG 0
    answer with the reconcile.
- **File(s)**: pkg/cluster/sync.go, pkg/cluster/sync_conn_sweep.go

- **Timestamp**: 2026-09-11 19:02 PDT
- **Action**: An error my own cells caught, recorded because the shape is easy to repeat. My first option-B
  edit made `shouldSync` return `z.zones[zoneID]` — false for a zone the map does not name. But
  `shouldSync` means KEEP, and master's default for an unnamed zone is true; false deletes unmapped-zone
  sessions on BOTH nodes, which is strictly worse than the RG 0 answer being withheld. Two cells failed
  immediately (`TestUnmappedZoneSessionSurvivesTheReconcile_9655` and
  `TestUnmappedZoneSessionIsKeptOnTheRG0Primary_9655`). The default is now `true`.
  - Cells reworked for option B: the headline cell inverts and is renamed to say the unmapped-zone session
    SURVIVES, with its controls proving the fixture still reconciles the mapped peer-owned zone;
    `TestAZoneMapNamingNoZoneSkipsTheReconcile_9655` covers nil and installed-empty as subtests; the
    map-change cell is unchanged, being the half that ships.
  - `go build`, `go vet` and the filtered `pkg/cluster` run (89 tests, including master's nil-snapshot
    guard) are green.
- **File(s)**: pkg/cluster/sync.go, pkg/cluster/sync_zone_snapshot_rg0_9655_test.go

- **Timestamp**: 2026-09-11 19:02 PDT
- **Action**: Commented on #9655 naming the dependency for the second half, as the lead asked, so the next
  reader does not re-derive it: the origin bit rides lane-dataplane's single `session_value`/HA-wire
  prerequisite covering pair identity (#9604), `route_table_id` (#9752) and the origin bit.
  README rewritten for option B: what ships, why the RG 0 answer is withheld, and what stays open.
- **File(s)**: pkg/cluster/README.md, docs/log/9655.md

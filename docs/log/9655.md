# #9655: the bulk-start zone snapshot answers an RG-unmapped zone with RG 0

Branch `fix/9655-zone-snapshot-rg0-fallback`, from origin/master `c5a5e5c24`.

- **Timestamp**: 07:26
- **Action**: Read the issue and the code.
  - `snapshotZoneOwnership` listed only zones in `zoneRGMap`.
  - `reconcileStaleSessions` skipped on an empty snapshot and answered `true` (keep) for any zone the snapshot did not list.
  - `ShouldSyncZone` answers an unmapped zone with `IsPrimaryFn`.
  - `ReconcileClusterBulk` deletes forward sessions whose ingress zone answers false and that are absent from the received set. Sessions carry no origin flag.
  - `buildZoneRGMap` maps a zone only through RETH interfaces, and always installs a non-nil map.
- **File(s)**: none

- **Timestamp**: 07:31
- **Action**: Wrote the cells. On the unfixed base `c5a5e5c24`, three cells fail as the issue describes: the RG 0 secondary keeps the stale unmapped-zone session, a cluster with no mapped zones skips the reconcile, and a zone map moved during the bulk still decides by the old snapshot. The RG 0 primary control passes.
- **File(s)**: `pkg/cluster/sync_zone_snapshot_rg0_9655_test.go`

- **Timestamp**: 07:32
- **Action**: Applied `9655-apply.py`:
  - `zoneOwnershipSnapshot` holds the zone answers, `unmapped` (the RG 0 answer) and `mapGen`;
  - the reconcile uses `zoneSnap.shouldSync`, and skips when the map generation changed;
  - `SetZoneRGMap` bumps `zoneRGMapGen` only when `maps.Equal` says the contents changed;
  - `ShouldSyncZone`'s unmapped answer moved into `shouldSyncUnmappedZone`, so both paths share it.

  `go build` and `go vet` clean.
- **File(s)**: `pkg/cluster/sync.go`, `pkg/cluster/sync_conn_sweep.go`

- **Timestamp**: 07:36
- **Action**: The full suite failed one existing test, `TestReconcileSkipsNonEmptyBulkWithoutZoneSnapshot` (470a2b7ba). It deliberately installs NO zone map and expects the reconcile to skip, which guards a bulk that arrives before the daemon wired zone ownership. That is a different fact from an installed empty map, because `buildZoneRGMap` installs a non-nil map even when it names no zone. So the guard is kept: a nil map takes no snapshot, and the reconcile skips. My cell now asserts the same thing for a never-installed map, and asserts a reconcile only for an installed empty one.
- **File(s)**: `pkg/cluster/sync.go`, `pkg/cluster/sync_zone_snapshot_rg0_9655_test.go`

- **Timestamp**: 07:44
- **Action**: The 4 cells, the guard test, the #5085, #6031 and #9174 reconcile tests, and the full `go test ./pkg/cluster` (87.8 s) all pass. `go vet` is clean.
- **File(s)**: none

- **Timestamp**: 07:50
- **Action**: The consequence the issue does not weigh: a zone of node-local (non-RETH) dataplane interfaces is RG-unmapped too. No cluster-mode commit rule rejects one, and sessions carry no origin flag. So on the RG 0 secondary, that node's own flows there are now reconciled away at every bulk; so is every flow it carries when no zone is RG-mapped. Documented this in the README, and raised it with the lead as a decision: ship as the issue says, ship only the map-change and nil-map parts, or hold. Committed locally (not pushed) so race and the matrix can run meanwhile.
- **File(s)**: `pkg/cluster/README.md`, `docs/log/9655.md`

## Option B: ship the safe half, hold the RG 0 answer

- **Timestamp**: 2026-09-11 19:02 PDT
- **Action**: The lead ruled option B on the decision I raised at 07:50: ship the map-change skip and the
  nil-vs-empty distinction, with an empty installed map still SKIPPING, and do not ship the RG 0 answer for
  RG-unmapped zones. Their reason is my own measurement: a zone of node-local, non-RETH interfaces is
  RG-unmapped too, so the RG 0 answer deletes that node's own live flows at every bulk and a TCP flow dies on
  its next non-SYN packet — worse than the stale peer-owned rows the issue is about. Land as `Refs #9655`
  and leave the issue open for the second half.
  - Kept from the first attempt: `zoneRGMapGen` with the `maps.Equal` gate, and the mid-bulk map-change skip.
  - Changed: `zoneOwnershipSnapshot` no longer carries the `unmapped` RG 0 answer; `shouldSync` answers
    from the map and KEEPS a zone the map does not name. `snapshotZoneOwnership` takes no snapshot when the
    map names no zone (`len(m) == 0`), so nil and installed-empty both skip.
  - Reverted: the `shouldSyncUnmappedZone` extraction in the sweep, which existed only to share the RG 0
    answer with the reconcile.
- **File(s)**: pkg/cluster/sync.go, pkg/cluster/sync_conn_sweep.go

- **Timestamp**: 2026-09-11 19:02 PDT
- **Action**: An error my own cells caught, recorded because the shape is easy to repeat. My first option-B
  edit made `shouldSync` return `z.zones[zoneID]` — false for a zone the map does not name. But
  `shouldSync` means KEEP, and master's default for an unnamed zone is true; false deletes unmapped-zone
  sessions on BOTH nodes, which is strictly worse than the RG 0 answer being withheld. Two cells failed
  immediately (`TestUnmappedZoneSessionSurvivesTheReconcile_9655` and
  `TestUnmappedZoneSessionIsKeptOnTheRG0Primary_9655`). The default is now `true`.
  - Cells reworked for option B: the headline cell inverts and is renamed to say the unmapped-zone session
    SURVIVES, with its controls proving the fixture still reconciles the mapped peer-owned zone;
    `TestAZoneMapNamingNoZoneSkipsTheReconcile_9655` covers nil and installed-empty as subtests; the
    map-change cell is unchanged, being the half that ships.
  - `go build`, `go vet` and the filtered `pkg/cluster` run (89 tests, including master's nil-snapshot
    guard) are green.
- **File(s)**: pkg/cluster/sync.go, pkg/cluster/sync_zone_snapshot_rg0_9655_test.go

- **Timestamp**: 2026-09-11 19:02 PDT
- **Action**: Commented on #9655 naming the dependency for the second half, as the lead asked, so the next
  reader does not re-derive it: the origin bit rides lane-dataplane's single `session_value`/HA-wire
  prerequisite covering pair identity (#9604), `route_table_id` (#9752) and the origin bit.
  README rewritten for option B: what ships, why the RG 0 answer is withheld, and what stays open.
- **File(s)**: pkg/cluster/README.md, docs/log/9655.md

- **Timestamp**: 2026-09-11 20:03 PDT
- **Action**: The first option-B matrix on `975b1a971` scored 6/8. Z3 and Z4 ESCAPED, and the cell was at
  fault, not the mutants.
  - Both mutants make a zone map that names no zone take a SNAPSHOT anyway (Z3 narrows the guard back to
    `m == nil`, Z4 removes it). The cell asserted only that no session was deleted — and with an empty
    snapshot every zone falls to `shouldSync`'s unnamed-zone default, which KEEPS. So nothing is deleted
    either way and the cell passed for the wrong reason.
  - `TestAZoneMapNamingNoZoneSkipsTheReconcile_9655` now asserts the property itself: a no-zone map
    produces a nil snapshot. The session checks stay as the downstream control.
  - No production code changed. All 5 cells and master's nil-snapshot guard pass.
  - The same chain's `make test` was stopped by the host's low-memory kill (free 5G, page cache 81G,
    available 73G, pressure 0). The matrix and the race run had already completed: race exit 0, no data
    races, and the matrix worktree restored clean at the head.
  - Everything re-runs at the amended head, since the post-gate keys every gate to it.
- **File(s)**: pkg/cluster/sync_zone_snapshot_rg0_9655_test.go, docs/log/9655.md

## Gates on 2afb2dd99

- **Mutation matrix** (`mut9655.py`, a detached copy of 2afb2dd99, `go test -json -race -run '_9655$'
  ./pkg/cluster`; scored by the fail event of the test each mutant is expected to trip). The baseline passed
  exactly the 4 #9655 cells; 8/8 KILLED, and the tree was restored clean:
    - Z1-unnamed-zone-deleted: TestUnmappedZoneSessionSurvivesTheReconcile_9655
    - Z2-map-answer-ignored: TestUnmappedZoneSessionSurvivesTheReconcile_9655
    - Z3-empty-map-snapshots: TestAZoneMapNamingNoZoneSkipsTheReconcile_9655
    - Z4-no-snapshot-guard: TestAZoneMapNamingNoZoneSkipsTheReconcile_9655
    - Z5-map-gen-not-checked: TestReconcileIsSkippedOnlyWhenTheZoneMapChangedDuringTheBulk_9655
    - Z6-identical-map-bumps-gen: TestReconcileIsSkippedOnlyWhenTheZoneMapChangedDuringTheBulk_9655
    - Z7-changed-map-keeps-gen: TestReconcileIsSkippedOnlyWhenTheZoneMapChangedDuringTheBulk_9655
    - Z8-snapshot-records-wrong-gen: TestUnmappedZoneSessionSurvivesTheReconcile_9655
- **`go test -race ./pkg/cluster`** on 2afb2dd99, the whole package with nothing skipped: exit 0, no data
  race. An earlier race run on this branch was taken at the superseded head `975b1a971` (it predates the cell
  that asserts the no-zone snapshot), and was re-run at the landing head rather than carried forward.
- **make test** on 2afb2dd99: exit 0. Go leg 78 packages ok, no FAIL. Rust leg 22
  `test result:` lines, 8373 passed, 0 failed. No disk-full. It ran with `GOFLAGS=-p=4 CARGO_BUILD_JOBS=4`, to keep peak memory down on the shared host.
- **make test-failover** on local merge `b5d88a60a` (origin/master `16df7c8c7` + `2afb2dd99`),
  deployed under the cluster lock with CoS re-applied.
  - The post-deploy reassert verified node0 primary for every RG.
  - 22 passed, 0 failed; iperf3 **17.8000 Gbps**.
  - Ledger row `caa28f5485c7376e.json`: verdict PASS, exe_check MATCH, build_git_sha = the merge.
- **Matrix scoring note.** The first option-B matrix ran on `975b1a971`, the commit before the cell was strengthened, and scored 6/8. Neither miss was a surviving mutant.
  - Z3 and Z4 both make a zone map naming no zone take a SNAPSHOT anyway (Z3 narrows the guard back to `m == nil`, Z4 removes it). The cell asserted only that no session was deleted, and with an empty snapshot every zone falls to `shouldSync`'s unnamed-zone default, which KEEPS — so nothing is deleted either way and the cell passed for the wrong reason.
  - `TestAZoneMapNamingNoZoneSkipsTheReconcile_9655` now asserts the property itself: a no-zone map produces a nil snapshot. The session checks remain as the downstream control. No production code changed.
  - The whole matrix was re-run on the amended head; the result above is that re-run, and the first run is kept as `mut9655-run1-6of8.log`.
- **make test note.** The first `make test` for this head was stopped by the host's low-memory kill (5G free, 81G page cache, 73G available, memory pressure 0), and a second was stopped by the amend that superseded its head. The result above is the run at the final head.

## RG 0 fallback completion

Branch: `fix/9655-zonesnap`; source commit: `0643d2631`.

- **RED — RG 0 primary control.** After correcting the primary-side assertion
  to require the stale RG-unmapped row to remain, the pre-fix
  `go test ./pkg/cluster/ -run '_9655' -count=1 -v` failed
  `TestUnmappedZoneSessionIsKeptOnTheRG0Primary_9655`: the row was deleted.
- **RED — captured fallback.** A revert probe changing
  `return z.fallbackPrimary` to `return false` failed the primary control and
  `TestUnmappedZoneUsesRG0FallbackCapturedAtBulkStart_9655`.
- **RED — valid empty snapshot.** The same fallback revert failed
  `TestInstalledEmptyZoneMapUsesRG0Fallback_9655`: an installed empty map
  deleted the stale row instead of reconciling with the captured RG 0 primary
  answer.
- **RED — no-snapshot distinction.** A probe removing the `m == nil` guard
  failed the `never_installed` subtest with
  `nil zone map produced a snapshot (0 zones, mapGen 0)`.
- **RED — origin control.** A probe removing the unmapped-zone origin guard
  failed `go test ./pkg/dataplane/ -run '10227' -count=1`:
  `TestUnmappedBulkReconcileDeletesOnlySyncedOrigin10227` reported
  `DeletedV4=0, want one synced stale row`.

- **Change.** `zoneOwnershipSnapshot` now captures `IsPrimaryFn()` once when a
  valid map snapshot is taken at BulkStart. `shouldSync` returns that captured
  fallback for zones absent from the map. The session-store V4 and V6
  predicates keep all rows when the captured fallback says primary, and use
  the per-session origin guard only when an absent zone is peer-owned on the
  secondary. A nil map remains no-snapshot/skip; an installed empty map remains
  valid and reconciles.

- **GREEN — per-cell.** The final
  `go test ./pkg/cluster/ -run '_9655' -count=1 -v` passed all 6 top-level
  tests and both empty-map/nil-map subtests. This covers:
  secondary stale-row deletion, primary stale-row retention, fallback
  capture-at-start, valid-empty-map reconciliation, nil-map skip, and
  changed-vs-identical map generations. `go test -race ./pkg/cluster/ -run
  '_9655' -count=1` also passed.
- **GREEN — session controls.** `go test ./pkg/dataplane/ -run '10227'
  -count=1 -v` passed all 5 origin-wire/reconcile tests.
- **GREEN — package suites.** `go test ./pkg/cluster -count=1` passed
  (`ok`, 99.758s); `go test ./pkg/dataplane -count=1` passed (`ok`,
  21.832s).
- **Deployment/provenance.** `make build` and `cargo build --release`
  passed. The rolling deploy installed source commit `0643d2631` on both loss
  peers; local and remote hashes matched for `xpfd`
  (`12b92ff7c7ab92211d7e90b8aa712b75`) and `xpf-userspace-dp`
  (`deb9a63f6e2d8dce1313c6016964f9d2`). Both peers reported
  `working-15719-g0643d2631-dirty`; RG0, RG1, and RG2 were ready with fw0
  primary before the clean gate.
- **Live failover gate — first run.** `make test-failover` ran under the
  cluster lock on `loss-userspace-cluster`; it recorded 19 passed and 3 failed
  and exited 2. The failures were the pre-existing #8280/#7174 M13 fresh-flow
  pool translation failure and an iperf3 incomplete/no-`[SUM] sender`
  measurement. `harness-result` recorded the gate `VOID` because
  `exe_check=UNAVAILABLE`. Core crash failover, rejoin, manual failback, IPv6
  transit, and session-sync checks passed.
- **Live failover gate — clean rerun.** After the failed run left an iperf3
  client behind, a contaminated retry again failed at the iperf3 completion
  checks. I then killed the stale client and restarted xpfd on both peers to
  clear session state, verified every RG ready with fw0 primary, and ran the
  gate from that clean baseline. The clean run recorded 21 passed and 2 failed:
  `iperf3 did not complete` and `iperf3 throughput: no [SUM] sender line`.
  All failover, rejoin, failback, pool translation/data-path, IPv6 transit, and
  session-sync cells passed. A late socket snapshot, collected after the
  diagnostic's five-minute wait, showed the `cluster-userspace-host` client
  holding seven data sockets in `CLOSE-WAIT` and one control socket in `ESTAB`
  to the external server `172.16.80.200:5211`; the client process was killed
  after evidence collection. The late flow-table snapshot was approximately
  590 seconds after the run, beyond the 300-second session timeout, so its
  zero matching fw0 rows is not causal evidence. No passing live-gate verdict
  was obtained.
- **Retention severity.** The userspace standby ceiling is coded as
  `7 * 24 * 60 * 60 * 1_000_000_000` ns, but the issue's concrete stale-row
  survival/reactivation window was not measured in this run; the live gate
  failed before providing that measurement.

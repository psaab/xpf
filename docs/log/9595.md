# #9595 — an application whose unrecognized statement the tolerant path dropped was still installed

- **Timestamp**: 2026-09-10
  - **Action**: Brief from the coordinator: find a distinction that closes the
    widening without overriding #6524's decision (a stray statement must not
    disarm a well-formed application), drawn by STRUCTURE rather than string
    similarity. If no clean line exists, bring it back as a decision.
    - Channel: `config.CompileConfigLenient`, Go plane, 3ae82aa49. Strict
      `CompileConfig` rejects every shape below.
    - What is recorded today: the direct body records only the unrecognized
      KEYWORD (`UnknownDirectLeaves`) and poisons the rest of its run; a term
      records every unrecognized token, value included; a set records only the
      member keyword.
  - **File(s)**: none (probe)
- **Timestamp**: 2026-09-10
  - **Action**: Measured 25 shapes, each labelled by intent: a lost constraint
    that should refuse, or an unrelated stray that should stay armed. Shapes
    covered hierarchical, flat-set chain, flat-set lines, term, flat term and
    application-set.
    - Single lines each erred both ways:

      | Line | Misses | Over-reaches |
      |---|---|---|
      | L1: the compiled term leaves a dimension open | T5, T6, T9 | S2 |
      | L2: the value is typed for the protocol | T8, T9 | S4, S5 |

    - Combined line (the application is otherwise protocol-wide on a dimension
      its protocol has, AND the unrecognized run carries a constraint-shaped
      token), plus a set member whose value resolves as an application:
      **19 ok, 2 over, 4 miss**.
    - Refused (ok): `destination-poort 22` in every shape; udp `source-prot
      53`; icmp `icmp-typ 8`; the real Junos leaves `icmp6-type 128`,
      `rpc-program-number 100003` and `application-protocol ssh`; set member
      `applicaton junos-ssh`.
    - Armed (ok): #6524's flat and hierarchical `bogus value` beside tcp/8080;
      tcp-only `bogus value`; `inactivity-timout 30` and `alg-typo ftp` beside a
      retained port; gre `bogus 5`; term `bogus value` beside tcp/22; icmp-only
      `foo bar`; set `descripton x`.
    - Over, deliberate: tcp-only `bogus 8080` and tcp-only `alg-typo ftp`. On a
      protocol-wide application these are structurally identical to a lost
      port, so they fail closed.
    - Miss, the residual: `destination-port 80; source-poort 1024`, `icmp-type
      8; icmp-cod 0`, `uuid ...` on tcp/135, and set `applicaton nosuchapp`. A
      constraint lost BESIDE a retained one is structurally identical to a
      numeric stray beside a retained constraint; only keyword similarity
      separates them. Filed as a decision: #9603.
  - **File(s)**: none (probe)
- **Timestamp**: 2026-09-10
  - **Action**: Implemented the combined line.
    - The compiler records every token of an unrecognized direct run
      (`Application.UnknownDirectTokens`, carried to term applications) and
      each unknown member's value (`ApplicationSet.UnknownMemberValues`); the
      existing records and strict text are unchanged.
    - `unknownStatementProtocolWide9595` adds a drop to `ApplicationMatchDrops`;
      `unknownMemberReferences9595` adds one in the set walk of
      `ApplicationReferenceMatchDrops`.
    - The #9525 expansion refusal then lowers the policy to the #3261 sentinel.
    - Re-running the probe against the real predicate reproduced the line:
      19 / 2 / 4.
  - **File(s)**: pkg/config/types_security.go, pkg/config/compiler_applications.go,
    pkg/config/application_unknown_statement_9595.go,
    pkg/config/application_match_drops_9525.go
- **Timestamp**: 2026-09-10
  - **Action**: Added cells in pkg/config, pkg/dataplane/userspace and
    pkg/policymatch.
    - pkg/config: refused rows, armed rows (#6524's shape first), the pinned
      deliberate over-reach, token recording, and the shape authority.
    - pkg/dataplane/userspace: the wire sentinel and mirror reason, and the
      #6524 wire unchanged.
    - pkg/policymatch: verdicts.

    Docs updated: README, config-schema, the gaps row, and the
    `lenientApplicationSpecs` comment.
  - **File(s)**: pkg/config/application_unknown_statement_9595_test.go,
    pkg/dataplane/userspace/application_unknown_statement_9595_test.go,
    pkg/policymatch/application_unknown_statement_9595_test.go,
    pkg/config/README.md, docs/config-schema.md, docs/userspace-dataplane-gaps.md,
    pkg/config/compiler_opts.go, docs/log/9595.md
- **Timestamp**: 2026-09-10
  - **Action**: Before the mutation matrix, checked each planned mutant
    against the cells. Five had no row that could kill them, so rows were added
    first:
    - a direct stray on a term-bearing application (the propagation of
      `UnknownDirectTokens` onto terms);
    - a set member statement naming an application-SET (the `isSet` arm);
    - a numeric stray beside a retained `source-port` (the `SourcePort`
      conjunct);
    - `icmp-type 8; icmp-cod 0`, beside a retained icmp-type (the `ICMPType`
      conjunct; a #9603 limit, pinned as the stated boundary);
    - `icmp-typ echo-request` on protocol icmp (the ICMP arm of the shape).

    The last row was RED, and it was a real miss, not a test error. The
    application `icmp-type` leaf takes only 0..255 (`parseICMPTypeCode`), so
    the ICMP arm was subsumed by the decimal arm, and a misspelled keyword
    with a NAMED type installed as icmp-any. The shape now accepts the
    compiler's own Junos type-name tables (`icmpTypeNames`, `icmp6TypeNames`),
    the way named ports come from `validatePortSpec`. All 39 cells (with
    subtests) pass in pkg/config, pkg/dataplane/userspace and pkg/policymatch.
  - **File(s)**: pkg/config/application_unknown_statement_9595.go,
    pkg/config/application_unknown_statement_9595_test.go, docs/log/9595.md
- **Timestamp**: 2026-09-10
  - **Action**: Go mutation matrix at c37bef7ea, 23 cells, scored on
    `go test -json` Action=="fail" against each cell's named targets (a
    mutation not applied, a build failure, a timeout or no target collected
    scores VOID). The unmutated control collected 10/10 and passed. 20 KILLED,
    2 SURVIVED, 0 VOID, and the tree was clean after the run. U04's detail
    reads "old 1->1" only because its replacement text contains the original;
    it was KILLED, so it applied.
    - SURVIVED U02 (the subtree keys of an unrecognized node) and U03 (a
      single-valued leaf's bracket tail). A probe in a separate worktree
      measured the shapes that reach them. Hierarchical `destination-poort
      { 22; }` parks the value in a child node (U02). `protocol tcp 22;`, with
      the destination-port keyword missing, lands "22" in the bracket tail
      (U03). Both are strict rejects that the tolerant path refuses only
      through those arms, and no row had either shape, so rows were added
      for both.
    - Also measured: a protocol-less head-of-chain `destination-poort 22
      protocol tcp` gets no #9595 drop. The poisoned run leaves the
      application protocol-less, which the existing expansion already refuses.
  - **File(s)**: pkg/config/application_unknown_statement_9595_test.go,
    docs/log/9595.md
- **Timestamp**: 2026-09-10
  - **Action**: Follow-up Go mutation matrix at 051a4d69b (control, U02,
    U03). The control collected 10/10 and passed. U02 and U03 were both
    KILLED by the new rows, and the tree was clean after the run. Final
    score over the 22 mutants: 22 KILLED, 0 SURVIVED, 0 VOID.
  - **File(s)**: none (scratch matrix)
- **Timestamp**: 2026-09-10
  - **Action**: Landing rule: master MOVED since the tested base 3ae82aa49, and
    master_delta ∩ mine was NOT empty: docs/userspace-dataplane-gaps.md.
    Master's 6b4aa87a1 repointed an IPsec owner reference in a different
    section of that file, far from this change's row 72, and no mutated file
    changed on master. `git merge-tree` previewed a clean merge. Rebased onto
    origin/master (fa1843126); `git range-diff` shows the patch unchanged.
    The mutation matrix stands, because none of its mutated files changed.
    The full `make test` runs on the rebased tree, which re-runs whatever the
    overlap touches.
  - **File(s)**: docs/log/9595.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` (Go + Rust) at b12435f28, rebased onto fa1843126:
    77 Go packages ok, 0 FAIL; Rust 5801 passed, 0 failed.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: Second rebase. The first guarded landing check refused
    (REFUSE 3): master had moved to 81efb7aad, and #9594's docs change
    (93bc38cac) touched docs/userspace-dataplane-gaps.md again, in its
    WireGuard rows and paragraphs, far from this change's row 72. No mutated
    file changed on master since the matrix base 3ae82aa49, and `git
    merge-tree` previewed a clean merge. Rebased onto 81efb7aad; `git
    range-diff` shows the patch unchanged. Re-ran what the overlap touches, on
    the rebased tree:
    - the suites that read that doc: pkg/dataplane (retirement_boundary_canary)
      and pkg/refactoraudit (closed_owner_8276_9506) both ok, and the Rust snat_contract_doc_guard (1 passed) and forwarding_build tests (179 passed, 0 failed);
    - the #9595 and #6524 cells: 41 results, 0 failures.
  - **File(s)**: docs/log/9595.md
- **Timestamp**: 2026-09-10
  - **Action**: Landing after the second rebase. The branch now sits on
    81efb7aad, and the landing check runs from that base. The mutated-file
    guard is kept strict across both rebases: none of the three mutated files
    (compiler_applications.go, application_unknown_statement_9595.go,
    application_match_drops_9525.go) changed on master since the matrix base
    3ae82aa49, and the landing check refuses if one changes after it. The
    check compiles the merged tree in a throwaway worktree (go build ./...,
    the go vet ./... differential, cargo check --tests --benches). It runs
    immediately before `gh pr merge --merge --match-head-commit`, gated on
    origin/master still being the master it compiled, and the merge commit's
    first parent is verified afterwards. This entry was written before the
    check, so the commit is not amended after it. `make test-failover` does
    not apply: the change is the application compiler and its tolerant-path
    refusal, not session sync, teardown or failover.
  - **File(s)**: docs/log/9595.md

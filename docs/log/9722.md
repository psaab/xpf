# #9722: every config apply re-arms the heartbeat receiver's 30s startup grace

Lane: lane-ha2. Worktree `/var/tmp/wt-9722-heartbeat-restart-grace`, branch
`fix/9722-heartbeat-restart-grace`, from origin/master `304920d71`.

- **Timestamp**: 2026-09-11 05:00 PDT
- **Action**: Read the mechanism at `304920d71`.
  - `RestartHeartbeat` (`heartbeat_manager.go`) stops the heartbeat and calls
    `StartHeartbeat`, which builds a new receiver whose `start()` sets
    `startedAt`. It then seeded `lastSeen` with a CompareAndSwap after the
    receiver had started.
  - `checkTimeout` (`heartbeat.go`): for a peer it has seen,
    `time.Since(startedAt) < heartbeatStartupGrace` returns before
    `heartbeatStale` runs.
  - `TestRestartHeartbeatSeedsLastSeenAndRearmsGrace` pinned the re-arm as
    intended behaviour. The #1792 addendum in
    `docs/bug-heartbeat-vrf-rebind-split-brain.md` says a peer death is
    "detected once the post-restart 30 s startup grace expires".
  - #9751 (open) is the same call site: a restart that fails latches the
    heartbeat dead. It is planned as a second commit on this branch.
- **File(s)**: none

- **Timestamp**: 2026-09-11 05:07 PDT
- **Action**: Fix, applied by a checked patch script. Every anchor had to match
  exactly once, or nothing was written.
  - `heartbeatRestartGrace = 5s`. The restart's own disruption is the socket
    rebind, which `RestartHeartbeat` already retries for up to 5s. A live
    peer's brief heartbeat loss past that stays covered by the sync-recency
    guard.
  - Added `heartbeatReceiver.seenGrace`, `armRestart(lastSeen)` and
    `seenThenLostGrace()`. `checkTimeout`'s seen-then-lost arm now uses
    `seenThenLostGrace()`.
  - `StartHeartbeat` is now a wrapper over `startHeartbeat(..., restartSeed)`,
    which arms the receiver before `start()`. `RestartHeartbeat` passes its
    seed, and the post-start CompareAndSwap is gone.
  - gofmt and `go vet ./pkg/cluster` are clean. The only other
    `heartbeatStartupGrace` reference is a comment in `heartbeat_epoch.go`.
  - File sizes stay under the 2000 LOC floor: `heartbeat.go` 1863 -> 1911,
    `heartbeat_manager.go` 1003 -> 1013.
- **File(s)**: `pkg/cluster/heartbeat.go`, `pkg/cluster/heartbeat_manager.go`,
  `pkg/cluster/heartbeat_liveness_test.go` (comment)

- **Timestamp**: 2026-09-11 05:08 PDT
- **Action**: Cells in `pkg/cluster/heartbeat_restart_grace_9722_test.go`.
  - A restarted receiver with a 10s-stale seed declares the peer lost 5.5s
    after the restart, which is inside the old 30s grace.
  - It holds 1s after the restart. A peer whose heartbeats resume inside the
    grace is not declared lost after it.
  - A cold-start receiver with the same staleness and the same time since
    start holds (the #4386 floor). `armRestart(0)` arms nothing.
  - Through the real `RestartHeartbeat` on 127.0.0.1, the replacement is armed
    only when the replaced receiver had seen the peer, and the seed is intact.
  - `go test -race -run '9722|TestRestartHeartbeat|Neverseen|NeverSeen|5086|6169|7257|4386' ./pkg/cluster`: ok.
- **File(s)**: `pkg/cluster/heartbeat_restart_grace_9722_test.go`

- **Timestamp**: 2026-09-11 05:12 PDT
- **Action**: Docs.
  - `pkg/cluster/README.md`: a new subsection, "A heartbeat RESTART is not a
    cold boot (#9722)", after the cold-boot grace section.
  - `docs/bug-heartbeat-vrf-rebind-split-brain.md`: an addendum superseding
    the #1792 addendum's "30 s post-restart grace" statement.
- **File(s)**: `pkg/cluster/README.md`,
  `docs/bug-heartbeat-vrf-rebind-split-brain.md`

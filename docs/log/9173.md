# #9173 -- the userspace SYN-cookie key derived from the root password verifier

- **Timestamp**: 2026-09-10
  - **Action**: Implemented the coordinator decision of 2026-09-11T01:10:40Z
    (option A plus an epoch). The key is HMAC-SHA256(secret, "xpf-syncookie" |
    v2 | cluster identity | rotation epoch | screened zones). The secret is the
    chassis cluster authentication-key on a keyed cluster, and a
    per-daemon-start random secret (sync.OnceValue, never persisted) on a
    standalone or unkeyed node. The rotation epoch is unix_seconds / 3584,
    which is 56 cookie epochs. Part 2 was refuted by the coordinator; no
    action.
  - **File(s)**: pkg/dataplane/userspace/screens.go, protocol.go, builder.go
- **Timestamp**: 2026-09-10
  - **Action**: Wire contract. The field `syn_cookie_key_ring` was ADDED; nothing
    was redefined. `syn_cookie_master_key` keeps its meaning -- the key to mint
    and validate with -- and now holds the current period's key, so an older
    helper still works. The ring carries primary keys for e-1, e and e+1 plus
    accept-only keys from additional-authentication-key (#6630).

    Deviation from the decision's wording, stated rather than dropped: the
    decision said to extend the accept-only field "to hold the previous key".
    Instead each ring key names its rotation epoch, and the helper selects by
    the COOKIE's own epoch. Trying key(e) and key(e-1) for every ACK would
    double a forger's per-ACK odds against a 24-bit MAC and would switch keys
    at publish time rather than at the period boundary. Every acceptance item
    still holds: key(e-1) is accepted across the boundary and key(e-2) is
    refused.
  - **File(s)**: userspace-dp/src/protocol/snapshot.rs,
    userspace-dp/src/screen/syncookie.rs, userspace-dp/src/screen/mod.rs,
    userspace-dp/src/afxdp/types/forwarding.rs,
    userspace-dp/src/afxdp/forwarding_build/mod.rs,
    userspace-dp/src/afxdp/worker/loop_body/{mod,setup}.rs
- **Timestamp**: 2026-09-10
  - **Action**: Rotation publish. The status loop calls
    refreshSYNCookieKeyRingLocked, which republishes lastSnapshot with only
    the key fields rebuilt once the period changes. It follows the
    UpdatePolicyScheduleState sequence. It stands down on a pending publish or
    an outstanding worker arm, and backs off 30 s after a failed attempt.

    First draft bug, caught by its own test: the draft also stood down on the
    #9520 unknown-apply-outcome flag. A failed rotation publish SETS that flag,
    so rotation would have stopped until the next commit. The flag retires only
    shortcuts that infer helper content, and this publish carries the whole of
    lastSnapshot, so the gate was removed and the test cell inverted.
  - **File(s)**: pkg/dataplane/userspace/syncookie_key_rotation_9173.go,
    process_status.go, manager.go
- **Timestamp**: 2026-09-10
  - **Action**: Retired the root-password dependency everywhere it gated
    behaviour. The capability admission refusal ("require system
    root-authentication encrypted-password material") and
    userspaceSupportsScreenProfiles are removed. The config warning now fires
    only for an unkeyed cluster, whose cookies do not survive a failover; that
    is reachable on the lenient load path only.
  - **File(s)**: pkg/dataplane/userspace/capabilities.go,
    pkg/config/compiler_validate_warn.go, and the tests pinning the old
    behaviour (manager_screens_test.go, manager_capabilities_test.go,
    parser_security_test.go)
- **Timestamp**: 2026-09-10
  - **Action**: Docs. docs/syn-cookie-flood-protection.md gains a "Key
    derivation and rotation" section with the skew assumption and the
    mixed-version upgrade window. docs/userspace-dataplane-architecture.md,
    docs/userspace-dataplane-gaps.md and userspace-dp/src/FEATURES.md are
    corrected, and pkg/cluster/README.md's PSK-rotation procedure gains a note.
  - **File(s)**: the docs named
- **Timestamp**: 2026-09-10
  - **Action**: Gates found after the first commit, each fixed:
    - #8892 wire-shape pin: the digest moved. It is recorded as a v15 STANDS
      entry with measured evidence, not a bump. At origin/master the helper
      source has no syn_cookie_key_ring and the snapshot has no
      deny_unknown_fields; an old helper enforces the current period's
      auth-key-derived legacy key, and a new helper under an old daemon falls
      back to the legacy key.
    - #7343 applied-marker registry: the rotation publish stamps
      publishedSnapshot and publishedPlanKey, so its file is registered with
      statusLoop as the driver.
    - rustfmt: a formatting run followed mod declarations and reformatted 11
      untouched files, and it rewrapped my own hunks. Every Rust file was
      restored and my original edits replayed verbatim from the session
      transcript. The result matched the numstat recorded before the rustfmt
      run exactly, and the tests were re-run on the replayed text.
  - **File(s)**: pkg/dataplane/userspace/snapshot_shape_version_8892_test.go,
    pkg/refactoraudit/applied_marker_registry_7343_test.go, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex hostile review round 1 was MERGE NO on two HIGH findings.
    Both held on inspection, and both are fixed.
    - (1) CORRECTION of my earlier entry. It said not standing down on the
      #9520 unknown-apply-outcome flag was safe because "this publish carries
      the whole of lastSnapshot". That is wrong. UpdatePolicyScheduleState
      sets the desired schedule before it publishes and advances lastSnapshot
      only on success. After a lost response the helper can hold NEWER content
      than lastSnapshot, and the rotation would republish the older content
      (for example reopening a permit the schedule closed). The rotation now
      stands down on an unknown outcome unless its own publish left it:
      recordApplySnapshotOutcomeLocked clears synCookieRotationOwnsUnknown on
      every apply attempt, and the rotation sets it only after its own failed
      apply.
    - (2) A present but stale ring fell back to syn_cookie_master_key, so after
      missed rotations the helper minted and accepted with key(e-2) or older.
      An ABSENT ring (an older control plane) still uses the legacy key. A
      PRESENT ring is authoritative, so a stale or malformed ring fails closed
      (syn-cookie-unavailable). The Rust cell that blessed the fallback is
      replaced by a stale-ring fail-closed cell and an absent-ring legacy cell.
  - **File(s)**: pkg/dataplane/userspace/syncookie_key_rotation_9173.go,
    manager.go, apply_snapshot_identity_9520.go, syncookie_key_9173_test.go,
    protocol.go, userspace-dp/src/screen/{syncookie,mod,tests}.rs,
    userspace-dp/src/afxdp/forwarding_build/{mod,tests}.rs,
    userspace-dp/src/protocol/snapshot.rs, docs/syn-cookie-flood-protection.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex hostile review round 2 was MERGE NO on three HIGH, one
    MED and one LOW finding. All five held on inspection, and all are fixed.
    - (1) HIGH: the #9520 content digest hashed the SYN-cookie keys raw and
      reaches state.json and conflict logs. With the keys now derived from the
      operator-chosen cluster PSK, that is an offline guessing oracle. Each key
      now enters the digest only as an HMAC under a per-daemon-start random
      salt, so the digest still moves on any key change and cannot be
      recomputed outside the process.
    - (2) HIGH: another path's in-band refusal cleared the rotation's ownership
      of its own unknown outcome, so the rotation never retried. Ownership is
      now cleared only by a success or by another path's unknown outcome.
    - (3) HIGH: an omitted ring and an explicit `{}` decoded identically and
      read as absent. The field is now `Option`, and only `None` falls back to
      the legacy key.
    - (4) MED: after a backward wall-clock step larger than the ring spans, the
      helper's non-decreasing cookie epoch lay outside the ring Go built from
      the raw clock. The latch now rebases in exactly that case, and a smaller
      step keeps it.
    - (5) LOW: the cluster README's PSK-rotation guarantee is qualified to
      helpers that read the ring.
  - **File(s)**: pkg/dataplane/userspace/{builder,screens,
    apply_snapshot_identity_9520,manager}.go, syncookie_key_9173_test.go,
    userspace-dp/src/{protocol/snapshot.rs,protocol/tests.rs,
    afxdp/forwarding_build/mod.rs,afxdp/forwarding_build/tests.rs,
    screen/mod.rs,screen/tests.rs}, pkg/cluster/README.md,
    docs/syn-cookie-flood-protection.md
- **Timestamp**: 2026-09-10
  - **Action**: Modularity split. The round-2 changes took
    userspace-dp/src/screen/mod.rs from 1452 to 1510 LOC, across the 1500
    floor (TestTouchedFileCrossedModularityThreshold). The key ring's selection
    policy moved into SynCookieKeyRing in screen/syncookie.rs: mint-or-legacy,
    validate, and the backward-step latch rebase. Each mod.rs call site is now
    one call. Behaviour is unchanged, and the ring owns the policy it enforces.
  - **File(s)**: userspace-dp/src/screen/mod.rs, userspace-dp/src/screen/syncookie.rs,
    docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 3 (MERGE NO: 2 HIGH, 2 LOW) fixes.
    - HIGH: a JSON `null` key ring decoded as absent, which bypassed the
      authoritative-ring fail-closed. It now has a presence-aware
      deserializer: an omitted field is absent, and a present `null` is an
      empty ring that fails closed. Pinned by a `null` cell.
    - HIGH: the rotation republishes lastSnapshot, so an update_neighbors or
      update_fabrics whose response was lost after the helper applied it
      would be rolled back. Per kind, the manager now records the first
      such failure (not an in-band refusal). The same kind's next applied
      send, or any successful apply_snapshot, clears it. The rotation holds
      off while one is set, bounded by a 2-minute grace so a hold nothing
      clears cannot let the ring expire. The two copies of the
      generation-stamped neighbor replace (RegenerateNeighborSnapshot,
      BumpFIBGeneration) are now one helper, replaceHelperNeighborsLocked,
      so one test covers both. Log text is unchanged. The same rollback
      through the pre-existing scheduler, overlay and worker-arm republishes
      is filed separately.
    - LOW: the mixed-version note claimed the client's SYN retransmit
      recovers a refused cookie ACK. It does not: the client has already
      sent its ACK, so the connection fails. Corrected in the SYN-cookie
      doc, the cluster README, and the #8892 STANDS entry.
    - LOW: "reveals nothing about the authentication key" ignored weak PSKs.
      It is now qualified: a derived key is an offline guess check, only as
      strong as the PSK's entropy, and it stays secret.
  - **File(s)**: userspace-dp/src/protocol/{snapshot.rs,tests.rs},
    pkg/dataplane/userspace/{manager,manager_neighbor,manager_generation,
    manager_fabric_sync,apply_snapshot_identity_9520,
    syncookie_key_rotation_9173,screens}.go, syncookie_key_9173_test.go,
    snapshot_shape_version_8892_test.go, pkg/cluster/README.md,
    docs/syn-cookie-flood-protection.md, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 4 (MERGE NO: 1 HIGH, 2 LOW) fixes.
    - HIGH: the 2-minute grace re-opened the rollback. If an applied
      update_fabrics loses its response and later fabric_fwd updates fail,
      SyncFabricState never re-sends, and after the grace the rotation
      republished the stale section with nothing left to correct it.
      Elapsed time proves nothing, so the hold is gone.
      - The Manager keeps the latest unresolved content per kind
        (unresolvedNeighbors, unresolvedFabrics). Neither an in-band
        refusal nor a #6034 fenced replace sets it.
      - The rotation republishes that content in place of lastSnapshot's
        section. It is at least as new as anything the helper can hold from
        that path, so the republish converges and never waits.
      - The same kind's applied send, or any successful apply_snapshot,
        clears it. A failed rotation keeps it.
    - LOW: the apply_snapshot cell called the recorder directly. It now
      publishes through requestApplySnapshotLocked, and every cell checks
      both what the rotation sent and what the Manager retained.
    - LOW: docs/userspace-dataplane-architecture.md still described the
      root-auth-derived key; corrected.
    - Coverage note: BumpFIBGeneration's call into
      replaceHelperNeighborsLocked is not driven by a unit test (it needs
      the BPF shim, as before this change). The helper itself is covered
      through RegenerateNeighborSnapshot.
  - **File(s)**: pkg/dataplane/userspace/{manager,manager_neighbor,
    manager_fabric_sync,apply_snapshot_identity_9520,
    syncookie_key_rotation_9173}.go, syncookie_key_9173_test.go,
    docs/syn-cookie-flood-protection.md,
    docs/userspace-dataplane-architecture.md, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 5 (MERGE NO: 1 HIGH, 3 LOW) fixes.
    - HIGH: carried neighbor content could go stale. Suppose a replace of B
      fails before delivery while the helper and lastSnapshot hold A, and
      the kernel then returns to A. RegenerateNeighborSnapshot's equality
      shortcut skipped the re-send, so the rotation would install B over a
      correct table. Now, while unresolvedNeighbors is set, both neighbor
      paths (RegenerateNeighborSnapshot, BumpFIBGeneration) re-send instead
      of taking the shortcut: an applied send clears the debt, and a lost one
      records the latest sample. SyncFabricState has no such shortcut. The
      kernel sample is indirected through neighborSnapshotBuilder (mirroring
      fabricSnapshotBuilder), so an A -> B (lost) -> A cell drives it.
    - LOW: the round-4 entry above claimed every carry cell checked both the
      sent and the retained snapshot. That was false at that commit and is
      true now: every cell asserts both sections in both snapshots, the
      neighbor fixtures are publishable, and the harness copies the sent
      slices.
    - LOW: syn_cookie_secret_unavailable also counts a present stale or
      malformed ring; its table row now says so.
    - LOW: two passages attributed every key to the cluster
      authentication-key; both now name the standalone/unkeyed random
      secret.
  - **File(s)**: pkg/dataplane/userspace/{manager,manager_neighbor,
    manager_generation,neighbors,syncookie_key_rotation_9173}.go,
    syncookie_key_9173_test.go, docs/syn-cookie-flood-protection.md,
    docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Redesign after Codex round 6 (MERGE NO: 2 HIGH, 1 MED, 1 LOW).
    All four findings were on the rotation republish:
    - the daemon neighbor listener's change filter suppresses the event that
      would refresh the carried content;
    - re-sending while unresolved breaks the control socket's 1/s budget;
    - the #6034 fence ACK cannot be recognised (pre-existing; filed as #9696);
    - the carry cells compared only parts of each section.
    Rounds 3 to 6 all trace to one root cause: rotating keys by republishing
    the whole snapshot drags every partial-update section along. So the
    republish is gone and the helper derives the keys.
    - Go publishes a BASE per keyed cluster (or per daemon start):
      HMAC-SHA256(secret, "xpf-syncookie\0v3-base\0" || identity || zones).
    - A period's key is HMAC-SHA256(base, "xpf-syncookie\0v3-epoch\0" ||
      epoch), truncated to 16 bytes. deriveSYNCookieEpochKey (Go) and
      SynCookieKeyRing::derive_epoch_key (Rust) compute it; one known-answer
      vector, asserted in both languages, pins them together.
    - SynCookieKeyRing keeps its reviewed selection rules:
      - keys are derived for the rotation epochs before, at and after the
        latched cookie epoch's (refresh runs once per period per worker);
      - a key is chosen by the cookie's own epoch;
      - accept-only bases cover the #6630 window;
      - a present ring with no usable base fails closed.
      The latch follows the wall clock back after a step of more than a period.
    - Removed, with each file restored to the merge base: the Go rotation
      republish (syncookie_key_rotation_9173.go, deleted), its #9520 ownership
      bookkeeping, the partial-update carry-forward, the neighbor-replace
      refactor and seam, the status-loop call, and the #7343 registry entry.
    - Wire: syn_cookie_key_ring is now {period_secs, bases: [{base,
      accept_only}]}. The #8892 golden is updated and the v15 STANDS entry
      reworded: an older helper keeps its last full snapshot's period key and
      does not rotate.
    - Trade-offs, stated in the docs:
      - the helper holds the base, so a compromise yields every period's key
        until the base changes;
      - an older helper does not rotate.
      In exchange, cookie protection no longer depends on an hourly
      control-plane publish.
    - The #9684 body was rewritten to record what the carry-forward attempt
      ran into.
  - **File(s)**: pkg/dataplane/userspace/{screens,protocol,manager,
    manager_neighbor,manager_generation,manager_fabric_sync,neighbors,
    process_status,apply_snapshot_identity_9520}.go,
    syncookie_key_rotation_9173.go (deleted), syncookie_key_9173_test.go,
    snapshot_shape_version_8892_test.go,
    pkg/refactoraudit/applied_marker_registry_7343_test.go,
    userspace-dp/src/{protocol/snapshot.rs,protocol/tests.rs,
    afxdp/forwarding_build/mod.rs,afxdp/forwarding_build/tests.rs,
    screen/syncookie.rs,screen/mod.rs,screen/tests.rs},
    docs/syn-cookie-flood-protection.md, pkg/cluster/README.md,
    docs/userspace-dataplane-architecture.md,
    docs/userspace-dataplane-gaps.md, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 7 on the redesign (MERGE NO: 1 MED, 3 LOW), all
    fixed.
    - MED: the latch's backward-step test compared rotation BUCKETS. A
      111-epoch step across two adjacent buckets never rebased, so the node
      kept minting nearly two periods ahead. It now compares elapsed epochs
      (latched - raw > period_epochs). New cells: a step of period + 1
      across adjacent buckets rebases, and a step of exactly one period
      holds.
    - LOW: the base framed the identity and each zone/profile with NUL
      terminators, but a quoted identifier may contain a NUL, so two
      screened-zone sets could derive one base. Every field is now
      length-prefixed, and synCookieScreenedZones returns (zone, profile)
      pairs. Pinned by a NUL-collision cell.
    - LOW: base derivation had only relational cells. A fixed known-answer
      vector (identity plus two zones) now pins its label, order and
      framing.
    - LOW: the digest-secrecy cell could not see the builder dropping ring
      fingerprinting, because the fingerprinted master key already made the
      digests differ. A ring-only snapshot now checks the bases alone.
  - **File(s)**: userspace-dp/src/screen/{syncookie.rs,tests.rs},
    pkg/dataplane/userspace/{screens.go,syncookie_key_9173_test.go},
    docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 8 (MERGE NO: 1 MED, 1 LOW), both fixed.
    - MED: the latch tolerated a backward step of up to one rotation period,
      but a validator accepts a cookie only within one epoch of its own clock
      (candidate_validation_epochs). After a correction of 2 to 56 epochs, the
      node kept minting epochs its peer could not reconstruct, for up to an
      hour. The latch now follows the clock back after any step of more than
      one epoch, with or without a ring. Its non-decreasing rule dates from
      the original wiring (4c26c6a65) and stated no reason that depends on
      it.
      - cells: a long step and a two-epoch step rebase, and a one-epoch step
        holds;
      - a peer on the true clock validates the stepped node's cookie, with a
        ring (steps of 2, 56 and 168 epochs) and on the legacy key (2 and
        56).
    - LOW: the #8892 note claimed a mixed pair fails only after two periods.
      It fails from the FIRST boundary: the older helper signs epoch e+1 with
      key(e), while the newer one picks key(e+1). The note and the SYN-cookie
      doc are corrected. A cell pins the window both ways, with the pair
      agreeing inside period e as its control.
  - **File(s)**: userspace-dp/src/screen/{syncookie.rs,mod.rs,tests.rs},
    pkg/dataplane/userspace/snapshot_shape_version_8892_test.go,
    docs/syn-cookie-flood-protection.md, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 9 (MERGE NO: 1 MED), fixed by reverting round 8's
    rebase.
    - The finding: following a backward clock step re-accepts cookies from
      epochs the node already left. After the clock advances past epoch E and
      steps back to E, a recorded ACK validates again and re-installs its
      64-second whitelist entry, and the active path is not rate-limited.
    - Round 8's finding (a latched node mints epochs a peer on the true clock
      cannot validate) and this one are the two sides of one trade-off.
      Stateless cookies cannot satisfy both.
    - #9173 needed a rebase only because the old finite ring could fail
      closed. Derived keys cover every epoch, so the latch is back to the
      non-decreasing rule it always had (4c26c6a65, still on master).
    - The trade-off and its options are filed as #9712 (LOW, not queued):
      keep the latch, fail closed during a rollback, or add a
      peer-coordinated rollback generation.
    - Cells:
      - the latch holds through backward steps of 1, 2 and 168 epochs;
      - a cookie from an epoch the node already left does not validate again
        after a backward step, with and without a ring. The validation clock
        gate is pinned, so the host's real clock cannot mask the result.
  - **File(s)**: userspace-dp/src/screen/{syncookie.rs,mod.rs,tests.rs},
    docs/syn-cookie-flood-protection.md, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 10 (MERGE NO: 1 LOW), fixed. The doc said a mixed
    old/new pair stays refused from the first boundary until both nodes
    upgrade. A later full snapshot hands the older helper the then-current
    period's key and restores the pair until the next boundary, so the
    window repeats from each boundary until the older helper's next full
    snapshot. The doc now says so, and
    syn_cookie_mixed_version_pair_disagrees_from_the_first_boundary_9173
    continues through a refresh (the pair agrees again) and the next
    boundary (it disagrees again).
  - **File(s)**: docs/syn-cookie-flood-protection.md,
    userspace-dp/src/screen/tests.rs, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 11 (MERGE NO: 1 LOW), fixed. The
    syn_cookie_secret_unavailable table row blamed a "stale" key ring, but a
    base-backed ring derives a key for any epoch and cannot go stale. The row
    now names the real causes:
    - no key was published, or the legacy key was cleared;
    - a present ring has no usable primary base: it is empty, a base is
      malformed, or the period is not a whole number of cookie epochs.
  - **File(s)**: docs/syn-cookie-flood-protection.md, docs/log/9173.md
- **Timestamp**: 2026-09-10
  - **Action**: Rebased onto origin/master 931ea3a65 (10 commits). Master's
    #9512 cell and this change's two cells were both appended at the end of
    userspace-dp/src/afxdp/forwarding_build/tests.rs; the conflict kept all
    three. capabilities.go, manager_capabilities_test.go and
    compiler_validate_warn.go auto-merged.
  - **File(s)**: userspace-dp/src/afxdp/forwarding_build/tests.rs
- **Timestamp**: 2026-09-10
  - **Action**: Codex round 12 (MERGE NO: 2 LOW), fixed. Two code comments
    still described removed designs:
    - SynCookieKeyRing's forwarding-state field called it a
      "per-rotation-period key ring". It now says the ring holds the published
      bases and the helper derives each period's key itself.
    - update_syn_cookie_keys said every snapshot rotation clears the
      validated-client cache. set_hash_keys clears it only when the legacy
      key changes: a snapshot delivered in a later period does, and local
      rotation between snapshots (SynCookieKeyRing::refresh) does not.
  - **File(s)**: userspace-dp/src/afxdp/types/forwarding.rs,
    userspace-dp/src/screen/mod.rs, docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Committed the test-failover ledger row (5e729aa1c2ba672c:
    22 passed, 0 failed, PASS, 21.1 Gbps, built at the pre-rebase commit).
    `make harness-compare GATE=test-failover` reports WITHIN-BAND (band
    18.08-22.52 Gbps over the last 3 green runs), and the ledger lint passes.
  - **File(s)**: test/results/ledger.d/5e729aa1c2ba672c.json
- **Timestamp**: 2026-09-11
  - **Action**: Codex round 13 (MERGE NO: 3 LOW), fixed, plus a sweep of every
    added line for the same two claim classes.
    - update_syn_cookie_keys: only a snapshot newly built in a later period
      clears the validated-client cache. The scheduler, overlay and
      worker-arm republishes copy the last snapshot and keep its key, and
      local rotation does not change it either.
    - Where a comment or doc names the authentication-key as the source, it
      is now scoped to a keyed cluster. Standalone nodes and leniently loaded
      unkeyed clusters use the per-daemon-start random secret, and the
      offline-oracle concern applies only to the keyed case. Places:
      builder.go (the digest note), screens.go (buildSYNCookieKeys),
      FEATURES.md, the digest test's comment, and the #8892 STANDS note.
    - The doc's list of base changes now names a cluster-id change and a
      restart of any node without an authentication-key.
  - **File(s)**: userspace-dp/src/screen/mod.rs, userspace-dp/src/FEATURES.md,
    pkg/dataplane/userspace/{builder.go,screens.go,syncookie_key_9173_test.go,
    snapshot_shape_version_8892_test.go}, docs/syn-cookie-flood-protection.md,
    docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Codex round 14 (MERGE NO: 2 LOW), fixed in the doc, the ring's
    comment, the commit message and the PR body.
    - Node agreement: only a keyed cluster's nodes share a base. They switch
      at the same period boundary to within their clock skew, and the
      adjacent periods' derived keys absorb that skew. An unkeyed cluster's
      nodes never agree.
    - Attempt count: the adjacent periods add no attempts per ACK. A #6630
      PSK window deliberately adds a second key, so "the ring does not
      multiply a forger's odds" was false during the window.
  - **File(s)**: docs/syn-cookie-flood-protection.md,
    userspace-dp/src/screen/syncookie.rs, docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Codex round 15 (MERGE NO: 2 LOW), fixed.
    - Node agreement also needs the same cluster-id and screened (zone,
      profile) set: the base hashes both. Master's legacy key already hashed
      the screened zones, so a node-specific screened zone breaking
      cross-node cookies is pre-existing. The agreement claims in the doc
      (the overlap paragraph, the table, the rotation bullets),
      pkg/cluster/README.md, screens.go, the commit message and the PR body
      now state the full condition.
    - Filed #9740 (LOW, not queued): whether the zone set belongs in the
      base at all.
    - Switch timing is clock skew plus up to one second of sampling lag,
      because each helper refreshes its wall clock at most once per
      monotonic second (#3032). The adjacent periods' keys absorb both.
  - **File(s)**: docs/syn-cookie-flood-protection.md, pkg/cluster/README.md,
    pkg/dataplane/userspace/screens.go, docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Codex round 16 (MERGE NO: 1 LOW), fixed. Two agreement claims
    named the screened (zone, profile) set but not the cluster-id, which the
    base also hashes: the cross-node validation claim in the cluster README
    and the synCookieKeyMaterial comment in screens.go.
  - **File(s)**: pkg/cluster/README.md, pkg/dataplane/userspace/screens.go,
    docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Codex round 17 (MERGE NO: 3 LOW), all in the claim class of
    rounds 15 and 16 (when two nodes agree on a key). Each round patched
    sentences and each round found another, so the conditions now live in one
    place: a "When two nodes agree" paragraph in
    docs/syn-cookie-flood-protection.md. A cookie validates on another node
    only if that node holds the minting node's primary base:
    - the same cluster-id;
    - the same screened (zone, profile) set (#9740);
    - the minter's authentication-key, held as the validator's own key or,
      mid-rotation, as its additional key (the accept-only base).
    The overlap paragraph, the node table, the rotation bullet, the PSK
    paragraph, three screens.go comments, the commit message and the PR body
    now refer to it. screens.go no longer claims heartbeat-authenticated nodes
    share a secret: at #6630 step 3 they sign with different keys and
    cross-accept.
  - **File(s)**: docs/syn-cookie-flood-protection.md,
    pkg/dataplane/userspace/screens.go, docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Codex round 18 (MERGE NO: 1 LOW), fixed. The consolidated
    rule held everywhere except one sentence written in round 17: the
    synCookieKeyMaterial comment said mid-rotation nodes validate each other's
    cookies, which fails when their screened sets differ. The comment now
    states only what the additional key does (validates, never mints) and
    refers to "When two nodes agree" for cross-node validation.
  - **File(s)**: pkg/dataplane/userspace/screens.go, docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: The Rust mutation matrix (rs8) reported an ESCAPE:
    RD3-refresh-widens-to-e-2 (refresh also derives key(e-2)) left
    syn_cookie_ring_refuses_a_key_two_periods_old_9173 green.
    - The cell's refusal holds by epoch selection alone.
      candidate_validation_epochs keeps a cookie's full epoch within one
      cookie epoch of the clock, and selection filters by the cookie's own
      rotation epoch. So no accepted cookie names period e-2, and RD3 changes
      no validation verdict.
    - The doc and refresh's comment still claim key(e-2) is never derived,
      and nothing pinned that. The cell now asserts that refresh derived
      exactly e-1, e and e+1, through a #[cfg(test)] accessor
      (SynCookieKeyRing::derived_rotation_epochs). The change is test-only:
      no production line changed.
  - **File(s)**: userspace-dp/src/screen/{syncookie.rs,tests.rs},
    docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: rs8 scored RD9-refresh-never-re-derives FAILED-WRONG.
    Reproduced by hand: syn_cookie_ring_rotates_without_a_republish_9173
    fails, but inside the shared mint helper ("never minted a cookie
    challenge"), not at the cell's own assertion. The cell mints twice (a
    premise and the post-rotation mint), and the panic did not say which
    one failed.
    - The helper now takes a step label, appended to its unchanged message,
      and this cell labels both mints.
    - The other cells keep the unlabelled helper, so the four rs8 mutants
      that expect the helper text still match it.
    - Test-only change.
  - **File(s)**: userspace-dp/src/screen/tests.rs, docs/log/9173.md
- **Timestamp**: 2026-09-11
  - **Action**: Landing stopped at the file-overlap check.
    - Master (704ae1b9a) had changed operator text in two of this change's
      files, both in a959a2cef: docs/syn-cookie-flood-protection.md (the
      cookie-reply classification paragraph) and
      pkg/dataplane/userspace/screens.go (the unresolved/inert screen-profile
      lines).
    - Rebased onto it with no conflict. This change's added and removed lines
      are byte-identical before and after; the range-diff's only difference is
      one line of trailing context below the doc hunk.
    - make test re-run at the rebased head.
  - **File(s)**: docs/log/9173.md

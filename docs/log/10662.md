# #10662 — IPv6 L4 declared-bound post-check

- **Timestamp**: 2026-09-24
- **Action**: keep the IPv6 `parse_l4` call capture-bounded, then compare its consumed packet-relative L4 end once against `l3_offset + 40 + payload_len`. A parsed span past the declared IPv6 datagram returns the same parse-failure verdict as the IPv4 declared-end gate; no declared bound enters the heavy parser.
- **Files**: `userspace-xdp/src/lib.rs`, `userspace-xdp/src/ipv6_len_gate.rs`, `pkg/dataplane/userspace_xdp_bpfel.o`, `pkg/dataplane/userspace_xdp_manifest.json`, `pkg/dataplane/userspace/fragment_disposition_7494_test.go`, `userspace-dp/src/screen/mod.rs`, `userspace-dp/src/screen/tests_10662_len_gate.rs`, `docs/log/10662.md`.

## Regression coverage

The retained shim regression plants an IPv6 TCP session. A declared 20-byte payload control still reaches that session; with payload length 13, the captured SYN flag lies beyond the datagram and the shim returns `PARSE_FAIL` without `PASS_TO_KERNEL`. Host-side boundary cells cover payload-length arithmetic and the 13/14-byte flags boundary.

## Verification

- `bash pkg/dataplane/build-userspace-xdp.sh` — PASS on the rebased source. The verifier processed **631,263** instructions against the 1,000,000 cap: **36.87% headroom** (368,737 instructions to the cap), with 218,737 instructions of slack before the 15% install floor. The verified object and source manifest were refreshed with nightly-2026-05-23 and bpf-linker 0.10.2.
- `sudo -n make test-shim-run` — PASS on the rebased object. `TestV6TCPFlagsPastDeclaredEndCannotHitSession_10662` reported `ret=1`, `PARSE_FAIL`, and no `PASS_TO_KERNEL`; the 20-byte control hit the planted session. The target's by-name census and fragment, QinQ, and S-tag cells passed.
- `cargo test --manifest-path userspace-dp/Cargo.toml --bin xpf-userspace-dp screen::tests_10662_len_gate` — PASS, 2 tests.
- `go test ./pkg/dataplane/userspace -count=1` — PASS.
- `go test ./pkg/dataplane -run '^TestUserspaceXDPShimObjectMatchesSourceManifest$' -count=1` — PASS.

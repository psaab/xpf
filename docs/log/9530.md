# #9530: alarm a heal sync that discards a partition-era commit

Branch `fix/9530-partition-commit-divergence-alarm`, from origin/master `fd8b4a54e`.

- **Timestamp**: 07:52
- **Action**: Read the issue and the code.
  - The config-sync payload is `[text][magic][gen]`, and an older receiver treats anything without that exact trailer as raw config text. So carrying commit lineage on the wire would need a new capability bit and a mixed-version path.
  - Each single-node digest heuristic fails a routine case. "Active differs from the last sync" alarms on every normal RG0 failover. "A local commit since the last sync" alarms on a node that rebooted and is merely behind. "The push was written" is worthless in the heal window, because the dual-active peer rejects the push (`errConfigSyncRejectedPrimary`).
  - Chose a persisted unshared-commit mark and told the lead, with the lineage design as the alternative.
- **File(s)**: none

- **Timestamp**: 07:58
- **Action**: Store half.
  - `unshared_9530.go`: the `unshared.json` record (durable write, bounded read, durable delete); `SetPeerReachableFn`; `noteLocalActivePromotionLocked`, which is sticky; `NoteActiveSharedWithPeer`; the `SyncApply` classification and recording under the existing mutex; `ConfigSyncDivergence` with the current rollback slot.
  - Hooks at commit, commit confirmed and the commit-confirmed timeout revert.
  - `Load` restores the mark.
  - The #8597 census caught my first `os.ReadFile`, so the read is now `ReadBoundedFile`.
- **File(s)**: `pkg/configstore/unshared_9530.go`, `pkg/configstore/store.go`, `pkg/configstore/store_commit.go`, `pkg/configstore/store_persist.go`

- **Timestamp**: 08:00
- **Action**: Store cells.
  - The issue's two-store acceptance fixture: a commit on node 1 during the partition, then a heal push of node 0's older config. The node ends up on the winner's config, with a divergence that names rollback 1 holding the discarded commit, and a converged re-push stays silent.
  - Routine failover, a push of marked versus other content, restart, stickiness, a sync of the marked content, standalone, and clearing on a different config.
  - All pass, and so does the full `go test ./pkg/configstore`.

  These cells need the new accessors, so they cannot run on the unfixed base. There, the issue's own store-level execution already shows `SyncApply` returning nil with active = C0 and nothing recorded. The fail-on-revert evidence is the mutation matrix.
- **File(s)**: `pkg/configstore/unshared_commit_divergence_9530_test.go`

- **Timestamp**: 08:02
- **Action**: Daemon half and renderers.
  - `configPeerReachable`, wired at cluster bring-up.
  - `noteConfigSharedWithPeer` after the commit push, the reconcile push and its test seam. It counts only when the peer reads RG0 secondary.
  - `reportConfigSyncDivergence` after a successful `handleConfigSync`, raised once as an `EventConfigSync` event.
  - A CRITICAL line in `show system alarms` on the local CLI and over gRPC, both from `ConfigSyncDivergenceAlarm`.
- **File(s)**: `pkg/daemon/config_divergence_9530.go`, `pkg/daemon/daemon.go`, `pkg/daemon/daemon_run_bringup.go`, `pkg/daemon/daemon_ha_sync.go`, `pkg/cli/cli_show_system.go`, `pkg/grpcapi/server_show_system.go`

- **Timestamp**: 08:04
- **Action**: Daemon and gRPC cells pass. The full `go test` of `pkg/daemon` (99.3 s), `pkg/grpcapi` and `pkg/cli` passes with the production edits.
- **File(s)**: `pkg/daemon/config_divergence_9530_test.go`, `pkg/grpcapi/alarms_config_divergence_9530_test.go`

- **Timestamp**: 08:05
- **Action**: Planning the mutation matrix showed mutants no cell would kill, so I added cells:
  - a commit confirmed during the partition;
  - a timeout revert onto a commit made during the partition. The revert hook matters only there: reverting onto a shared config clears a stale mark either way;
  - a stale mark;
  - the marked-content sync asserting no divergence at its first sync as well;
  - the production reachability predicate;
  - a structural pin that cluster bring-up wires the callback;
  - the local CLI renderer.
- **File(s)**: `pkg/configstore/unshared_commit_divergence_9530_test.go`, `pkg/daemon/config_divergence_9530_test.go`, `pkg/cli/alarms_config_divergence_9530_test.go`, `pkg/configstore/README.md`, `pkg/daemon/README.md`

## Gates on c5bb8dd80

- **Mutation matrix** (`mut9530.py`, a detached copy of c5bb8dd80, `go test -json -race`, scored by the
  expected test's fail event). The baseline passed exactly the 19 cells; 24/24 KILLED, and the
  tree was restored clean:
    - U1-plain-commit-not-noted: TestHealSyncThatDiscardsAPartitionCommitRaisesADivergence_9530
    - U2-commit-confirmed-not-noted: TestCommitConfirmedDuringThePartitionIsMarked_9530
    - U3-timeout-revert-not-noted: TestTimeoutRevertOntoAPartitionCommitKeepsItMarked_9530
    - U4-sync-not-classified: TestHealSyncThatDiscardsAPartitionCommitRaisesADivergence_9530
    - U5-divergence-not-recorded: TestHealSyncThatDiscardsAPartitionCommitRaisesADivergence_9530
    - U6-mark-not-sticky: TestALaterCommitCarriesTheUnsharedMark_9530
    - U7-any-push-shares: TestOnlyAPushOfTheMarkedContentClearsTheMark_9530
    - U8-incoming-equal-not-shared: TestSyncOfTheMarkedContentClearsTheMark_9530
    - U9-stale-mark-kept: TestStaleMarkNamesNothing_9530
    - U10-standalone-marks: TestStandaloneStoreNeverMarks_9530
    - U11-mark-not-persisted: TestUnsharedMarkSurvivesARestartBeforeTheHeal_9530
    - U12-mark-not-loaded: TestUnsharedMarkSurvivesARestartBeforeTheHeal_9530
    - U13-slot-off-by-one: TestHealSyncThatDiscardsAPartitionCommitRaisesADivergence_9530
    - U14-alarm-cleared-by-repush: TestHealSyncThatDiscardsAPartitionCommitRaisesADivergence_9530
    - U15-alarm-never-cleared: TestDivergenceClearsWhenADifferentConfigArrives_9530
    - D1-reachable-always: TestConfigPeerReachableNeedsALivePeer_9530
    - D2-share-ignores-peer-role: TestReconcilePushSharesTheCommitOnlyWhenThePeerReadsSecondary_9530
    - D3-event-not-once: TestHealSyncRaisesTheDivergenceEventOnce_9530
    - D4-sync-reports-nothing: TestHealSyncRaisesTheDivergenceEventOnce_9530
    - D5-commit-push-not-noted: TestEveryConfigPushNotesItsContent_9530
    - D6-reconcile-seam-not-noted: TestReconcilePushSharesTheCommitOnlyWhenThePeerReadsSecondary_9530
    - D7-bringup-not-wired: TestClusterBringUpWiresPeerReachability_9530
    - A1-grpc-alarm-dropped: TestShowAlarmsListsAConfigSyncDivergence_9530
    - A2-cli-alarm-dropped: TestShowSystemAlarmsListsAConfigSyncDivergence_9530
- **Matrix scoring note.** The first run on this head scored 22/24, and neither miss was a surviving mutant.
  - `U10-standalone-marks` removed the nil-callback guard. A store with no reachability callback then called a nil function on its first commit. That panic aborted the package before the named cell ran, so the result was OTHER rather than a kill. The mutant was rewritten to the real wrong behaviour: a standalone store marks every commit.
  - `D2-share-ignores-peer-role` dropped `peerSecondary` from the condition, which left the variable unused, so it did not build (VOID). The mutant now keeps the variable in the expression.

  The whole matrix was re-run on the same head. The re-run is the result recorded above, and the first run is kept as `mut9530-run1-22of24.log`.
- **`go test -race` on configstore, daemon, grpcapi and cli (the #9530, config-sync, SyncApply and reconcile cells)** on c5bb8dd80: exit 0, no data race.
- **make test** on c5bb8dd80: exit 0. Go leg 78 packages ok, no FAIL. Rust leg 11
  `test result:` lines, 5879 passed, 0 failed. No disk-full. It ran with `GOFLAGS=-p=4 CARGO_BUILD_JOBS=4`, to keep peak memory down on the shared host.
- **make test-failover** on local merge `6911285c9` (origin/master `ad2ba883a` + #9530 `c5bb8dd80`),
  deployed under the cluster lock with CoS re-applied.
  - The post-deploy reassert verified node0 primary for every RG.
  - 22 passed, 0 failed; iperf3 **20.1000 Gbps**.
  - Ledger row `80686d8c483ac962.json`: verdict PASS, exe_check MATCH, build_git_sha = the merge.

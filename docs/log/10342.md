# Issue #10342 — loss-cluster pref-2000 baseline drift

## Scope and issue evidence

Issue #10342 was opened after the locked #10136 preflight on 2026-09-18.
The reported state was a committed `sfmix` virtual-router with `vrf-sfmix`
present but no IPv4 pref-2000 `l3mdev unreachable` rule.  The issue had no
comments.  The earlier #10136 lane log records the same observation and the
important transition: adding the owned temporary RI caused xpfd to self-heal
pref-2000, while deleting only that RI left manager-owned `vrf-sfmix` and
pref-2000 behind (`docs/log/10136.md`, lines 56–67).

The archived configuration snapshots from that run are:

- `/var/tmp/xpf-wire-routing-separation-1789729350`
- `/var/tmp/xpf-wire-routing-separation-1789729374`
- `/var/tmp/xpf-wire-routing-separation-1789729613`

Each contains the committed `sfmix` stanza, including:

```
set routing-instances sfmix instance-type virtual-router
set routing-instances sfmix interface gr-0/0/0.0
```

The normalized pre/post configuration SHA in the owned attempt was
`f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91` on fw0;
the pre/post configuration was unchanged.

## Diagnosis

The observed behavior is recurrent startup/recovery drift, not a missing
fixture stanza.  The initial absence could have looked like external kernel
drift, but the final incident check invalidated an external-only conclusion.

The managed implementation is explicit:

- `pkg/routing/vrf_miss_terminator_9819.go` defines pref 2000 as the
  `l3mdev unreachable` terminator.
- `pkg/routing/vrf.go` calls `setVRFMissTerminator(..., true)` before creating
  any desired VRF and on every non-empty `Reconcile`; it removes the rule only
  when no VRF is desired or still owned.
- `docs/log/9819.md` records the same install/reassert/remove contract and its
  real-kernel validation.

The locked #10136 evidence showed the rule absent while `sfmix` remained in
committed configuration, then showed the manager restoring it when a temporary
RI commit forced a reconcile.  That proves the managed path can repair the
state eventually, but not that a fresh daemon/recovery path starts with the
required rule.  At 2026-09-19T03:22:43Z, immediately after the recovery lane
started fresh xpfd processes on both nodes, the locked inspection showed the
IPv4 and IPv6 pref-2000 rules absent while `vrf-sfmix` table 488570 and the
committed `sfmix` stanza were present.  Both processes reported build SHA
`20b0ee01cdf69af0bbba4f4f825ae98584917f56340d501f40d430d5dee7200d`.

The decisive readiness-gated probe ran at 2026-09-19T04:46:47Z
(`/var/tmp/xpf-10342-probe-20260919T044647Z`).  It deleted the exact
IPv4/IPv6 pref-2000 rules successfully on both nodes, restarted xpfd without
any configuration commit or temporary RI, waited for `ActiveState=active` and
the committed sfmix stanza to be queryable, then sampled both families on both
nodes once per second for 30 seconds.  Every sample remained empty.  PIDs
changed 24133→24604 on fw0 and 46342→46733 on fw1.  The binary SHA was
`4a196d927c91b5d65d07caaf1a8d6738eb3c56412daaffa330651cba18509748`;
`go version -m` reported vcs revision
`ed4a7c10e04d0a41e95dacad492985f14fce26e1` with `vcs.modified=true`.

This established the then-current/pre-fix startup/recovery defect under that
deployed build.  Dirty-build provenance remains a separate deployment concern.
The managed fallback add/delete immediately restored both families; final
assertions showed `vrf-sfmix`/`vrf-mgmt`, no temporary VRF/device, and no
temporary configuration.  Product issue #10421 records the reproduction.
No product-code change was made in this lane.

## Baseline repair and final-state proof

The 2026-09-19T03:14:46Z clean snapshot is retained as historical evidence
only; the 03:22 fresh-start observation showed that it was not durable.  The
managed restore was then rerun after the recovery lane, with add/delete
evidence in `/tmp/xpf-wire-10030-restore-1789789323` (UTC
2026-09-19T03:42:03Z).  Its `add-cli.txt` and `delete-cli.txt` contain the
annotated `commit complete` marker; the singular
`after-restore-xpf-userspace-fw0.txt` records build SHA
`32c5d7eaaa019fc0c1dc2b6891622c642c12d9b325b3b55b601102de7fabc303`
(both-node proof comes from the later independent locked checks below).

A locked post-restore inspection at 2026-09-19T03:45:54Z
(`/var/tmp/xpf-10342-final-034554`) showed, on **both** nodes:

```
IPv4 pref 2000: 2000: from all lookup [l3mdev-table] unreachable
IPv6 pref 2000: 2000: from all lookup [l3mdev-table] unreachable
vrf-sfmix: ... vrf table 488570 ...
```

The independent locked no-temp check at 2026-09-19T03:48:45Z
(`/var/tmp/xpf-10342-final-no-temp-fw0.txt` and
`/var/tmp/xpf-10342-final-no-temp-fw1.txt`) showed on both nodes:

- IPv4 and IPv6 pref-2000 rules present;
- `Device "vrf-wire-10342" does not exist`;
- `ip vrf show` contains only `vrf-sfmix` (488570) and `vrf-mgmt` (999);
- the CLI reports `configuration path not found:
  routing-instances wire-10342`.

The definitive 2026-09-19T04:46:47Z readiness-gated probe then restored the
same state through managed add/delete.  Its final assertions are in
`/var/tmp/xpf-10342-probe-20260919T044647Z/final-assert.txt`: both nodes have
IPv4 and IPv6 pref-2000, `vrf-sfmix` table 488570 plus `vrf-mgmt` 999, no
`vrf-wire-10342` device, and no `routing-instances wire-10342` configuration.
The lock owner was absent after the cell; no lock file was removed or
force-released.

## Harness repair and #10136 setup verification

The first post-repair run reached the live gate but still emitted
`harness-void`: the CLI actually prints
`commit complete: 1 statement(s) changed (...)`, while
`cli_commit_ri` required an exact bare `commit complete` line.  This was a
harness parser defect, separate from the recurrent startup/recovery drift.

This branch changes `test/incus/wire-routing-separation.sh` to share a
line-anchored `cli_commit_succeeded` parser that accepts both the bare and
summary forms, rejects an embedded error mention, and is used by both RI
add/delete paths.  The full file selftest passed:

```
wire-routing-separation selftest: 12 passed, 0 failed
```

With that fix, that locked live setup reached the traffic oracle and completed
cleanup without the baseline VOID in that run:

```
make test-wire-routing-separation
archive=/var/tmp/xpf-wire-routing-separation-1789786852
probe=1000 control=1500 probe_observed=0 control_observed=0 cksum_bad=0
pre_fw0_sha=f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91
post_fw0_sha=f40e51f2208fa6e70070e58475501bfc504a108ee04dffd7e415b113baf09d91
WIRE_GATE ... VOID reason=capture-blind
```

The finalizer emitted `capture-blind`, not `harness-void`: therefore the
RULE_SNAPSHOT was restorable and the tree-vs-baseline configuration comparison
was completed and equal.  A second locked run against the known WAN target
`172.16.80.200` produced the same independent capture-blind result while
again restoring successfully; its archive was
`/var/tmp/xpf-wire-routing-separation-1789787769`, with equal pre/post SHA
`51e5e059203f9290f7e22de3aeaa789df9f4b4ac6bff1875b7eb7f188af09a08`.
Those runs are setup/restore evidence, not claimed PASS/FAIL ledger rows.
The remaining capture-blind result is an independent #10136 peer-capture
environment issue; it is no longer the #10342 baseline-restore VOID.

## Verdict

**HISTORICAL BLOCKER RESOLVED; CURRENTLY LAB-REPAIRED:** the 04:46
readiness-gated discriminator correctly recorded the pre-fix startup defect
and restored the cluster through managed add/delete.  Product issue #10421
then landed the startup-tail and post-networkd reassertions.  Its current
master evidence (`docs/log/10421.md`, Cell 3) shows the live delete/re-add
ordering on both loss nodes: each IPv4 and IPv6 pref-2000 rule is reinstalled
after the late cleanup, with final rule dumps present on both nodes and
sfmix/temporary-state checks clean.

Therefore #10342's baseline blocker is **LAB-REPAIRED** on current master.
The historical dirty-build caveat remains recorded above; no product change
was made in this lane.  PR #10415 merged the parser correction, PR #10422
merged the readiness-gated evidence, and issue #10421 is CLOSED/COMPLETED.
The #10136 live traffic oracle remains `capture-blind`, so this log makes no
#10136 PASS/FAIL claim.

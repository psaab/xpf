# #7033 — a packed cross-mode `then` contradiction commits under strict

## Timestamp
2026-08-28

## Action
Reject, at strict commit, a NAT rule whose `then` block packs two
mutually-exclusive translation actions onto ONE token run. Packed that way they
lower to a single field, so the #5628 cardinality gate — which counts RESOLVED
fields — sees `n == 1` and the contradiction commits with no diagnostic.

Where `off` is the casualty the operator authored a no-NAT EXEMPTION and the
compiler publishes a TRANSLATION in its place: the inverse of the authored
action, and the outcome #5628's own rejection text says it prevents.

## Files
- `pkg/config/compiler_nat_then_packed_contradiction_7033.go` (new) — the shared
  predicate and the operator-facing detail renderer
- `pkg/config/compiler_nat_then_packed_contradiction_7033_test.go` (new)
- `pkg/config/compiler_nat_then_occurrences_7013.go` — the #7013 authored record
  extended with MODES; `scanKeys` becomes `scanRun` with the stop rule;
  `walkActionChain` replaces the pool-only child loop
- `pkg/config/compiler_validate_strict_nat.go` — the gate, the lenient
  enumerator, and the now-stale #7033 sentence in the `n >= 2` message
- `pkg/config/compiler_nat_conflict_message_7034_test.go` — the pin that
  documented the gap, FLIPPED (not deleted), keeping its resolved-value evidence
- `pkg/config/nat_compact_then_7014_test.go` — the uniformity pin, updated
- `pkg/config/compiler_nat_terminal_action_5628_test.go` — two golden messages
- `pkg/config/nat_source_axis_sweep_6812_test.go` — the new Modes columns
- `docs/config-schema.md`

## Detection, not accumulation — and why that is the whole design

Two rounds of #6820 tried to fix this in the LOWERING and each was reverted for
a regression in the ACCEPTING direction: round 5 made
`then { destination-nat interface pool PD; }` resolve as a pool translation,
round 6 fabricated an exemption out of `then { source-nat { frobnicate { off; } } }`.

Nothing here changes what a rule resolves to. The authored actions are recorded
alongside the resolved `NATThen` and the gate reads that record. A config that
commits today either commits unchanged or is rejected; none resolves differently.
The flipped #7034 test asserts the same resolved values it always did — read off
the lenient path, since the strict path now returns no config — and that is the
evidence, not a claim.

## The three preservation properties

Each is a cell, and each is a shape one of the reverted rounds got wrong:

| must keep working | mechanism |
|---|---|
| `pool off` — a pool NAMED off | `pool` consumes exactly one value token |
| `pool P persistent-nat permit any-remote-host` (#4313 open-world) | the scan stops at the first unrecognised token |
| `frobnicate { off; }` keeps its ZERO-action rejection | the walk does not descend past an unrecognised container |

The open-world cell was strengthened after the matrix: a realistic tail contains
no action keyword, so the original fixture stayed green under a mutation that
removed the stop rule. `TestOpenWorldTailContainingOffStillCommits_7033` puts the
token `off` IN the tail, which is the smallest shape where the stop rule changes
an outcome.

## The hole the matrix found in my own fix

A cell that counted RAW modes instead of distinct ones redded the #7013
same-pool cell but NOT the new `off off` cell. That is only possible if `off off`
never records two modes — and it recorded nothing at all. The per-container
record was ranked by distinct POOLS, so a container naming no pool never beat the
empty starting record and was never stored.

The consequence was a missed class, not a cosmetic one:

    then { source-nat interface off; }   ACCEPTED, resolving {Interface:true}

That is the inversion this issue is about — an authored exemption, an applied
translation — and it was invisible because every fixture in the first draft named
a pool. The table sampled that axis instead of varying it.

Ranking is now distinct MODES first, distinct POOLS second (ties). Pool-less rows
were added in all three shapes and both orders, plus a shape-table row. `N9` (rank
by pools only) now reds them, and `N8` (raw modes) reds the asymmetry cell, which
was VACUOUS before the ranking fix — it had been passing because the record was
never stored, not because distinctness deduped it.

## What the matrix found that review did not

`N5` — moving the check ahead of the resolved-field count — came back GREEN. Not
a coverage hole: two sites enforce the `n == 1` restriction and either alone
suffices, so each masks the other's mutation. The redundancy is deliberate (the
predicate is also called by the lenient enumerator, which has no switch around
it), but the comment claimed the placement carried the property. Corrected, and
`N5b` — the COMPOUND mutation, move the call AND drop the guard — reds
`TestRound5ShapeKeepsItsZeroActionMessage_7033` and the #6820 message golden.

## Pins updated rather than deleted

Three tests documented this defect and each said in its own failure text what to
do when it was fixed. `TestNATTerminalActionPackedContradictionCommits_7034`
became `...Rejected_7034_7033`, `TestCompactThenPackedContradictionMatchesTheChildSpelling_7014`
kept its uniformity property with both spellings now rejecting, and the two
#5628 golden messages no longer say the packed spelling is uncaught — they name
the check that catches it.

## Lenient path
`natTerminalActionCardinalityOffenders` reads the same predicate (#7640). Its own
rows build `NATThen` structs directly, so no packed row can reach them and the
existing agreement test cannot see this class;
`TestLenientNATOffendersMatchThePackedGate_7033` binds it through the lenient
compile instead.

## Validation
- `go test -count=1 ./pkg/config/` — ok
- `go test -count=1 ./...` — 68 packages ok, 0 failures
- 8-cell full-suite mutation matrix with a green control — see PR body

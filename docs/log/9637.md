# #9637: host-inbound admission by destination address, not ingress zone

## Actions

- **Timestamp**: 2026-09-11T14:32Z
- **Action**: Live validation on the loss cluster, in two read-only lock
  cells. It was posted to the issue.
  - From `lan`, which admits ssh, TCP/22 to the `wan` address `172.16.80.8`
    and to the `sfmix` address `10.255.192.42` timed out, while ping answered.
  - The LAN VIP refused port 22, i.e. the SYN was admitted.
- **Timestamp**: 2026-09-11T16:10Z
- **Action**: Refined the posted plan in two ways before implementing.
  - **Per view, not per zone.** The scope is per view (per zone and effective
    token set), because #3362 per-interface overrides split a zone into
    several views.
  - **Ingress rules first, destination rules kept.** The ingress-zone rules
    come first, and the destination-address rules stay unchanged as the judge
    for every netdev the scope leaves out. That makes an incomplete netdev
    enumeration cost coverage and never admission, which is the objection
    that deferred #3718 Option A.
  - **No coverage flag.** The plan's partial-coverage flag is unnecessary:
    each rule is scoped to its own view's netdevs.
- **File(s)**:
  - `pkg/dataplane/userspace/zones_host_inbound.go`:
    `ZoneHostInboundView.IngressNetdevs` and
    `hostInboundViewIngressNetdevs`. A view claims its unit rows' netdevs,
    minus shared claims, lifelines and VRF-enslaved netdevs.
  - `pkg/config/host_inbound_vrf_netdevs_9637.go` (new):
    `HostInboundVRFEnslavedNetdevs`. It shares the ref→netdev walk with the
    junos-host coverage map, via `junosHostNetdevByRef`, used from
    `pkg/config/junos_host_deny.go`.
  - `pkg/daemon/host_inbound_ingress_9637.go` (new) and
    `pkg/daemon/daemon_nft.go`: the text oracle's ingress-zone rules and
    counter pre-pass.
  - `pkg/nftables/netlink_hostinbound_ingress_9637.go` (new),
    `pkg/nftables/netlink_hostinbound.go` and
    `pkg/nftables/netlink_spec.go`: the netlink mirror.
  - `pkg/daemon/daemon_nft_netlink.go`: the converter.
- **File(s)**: tests.
  - `pkg/daemon/host_inbound_ingress_9637_test.go` holds the payload-shape
    cell and the real-kernel verdict cell. The verdict cell runs three client
    namespaces against a port-22 listener through the rendered ruleset, and
    measures three things:
    - the destination-only ruleset reproduces the exposure and the
      over-refusal;
    - the ingress-scoped ruleset inverts both;
    - a netdev no view claims is still judged by destination.
  - `pkg/dataplane/userspace/host_inbound_ingress_9637_test.go` holds the
    resolver cells.
  - `pkg/daemon/daemon_nft_netlink_parity_test.go`: the T1 fixture gives
    every rendering branch an ingress scope.
- **Re-pinned**: `TestHostInboundFilterNoStanzaDefaultDeny`.
  - It asserted that no accept names lan's address, which is the
    destination-only model.
  - The #3405 decision it guards is about traffic arriving on lan. That is
    unchanged, and the cell now asserts it on both of lan's rule sets, adding
    an assertion that lan's ingress drop exists.
  - Another zone's ingress rules, which name lan's address, are exempt.
- **File(s)**: `test/incus/test-host-inbound.sh`.
  - Every probe arrives on `lan`, so ssh is scored ADMIT at every address.
    Before, the smoke asserted the over-refusal at the `wan` VLAN sub-units.
  - A posture check that `lan` owns `reth1.0` keeps that scoring honest.
- **File(s)**: `docs/host-inbound-service-matrix.md`.
  - New "Ingress-zone judgement (#9637)" section.
  - Rewrote the destination-only statements in the #7284, #5699 and #3718
    passages, and marked #3718 Option A as delivered for claimed netdevs.

## Validation

- `go test ./pkg/daemon/ ./pkg/dataplane/userspace/ ./pkg/config/ ./pkg/nftables/`: 14624 passed, 0 failed. `TestHostInboundIngressZoneVerdictsOnRealKernel9637` ran; it did not skip. `TestNftNetlinkParity` passed with the extended fixture.
- gofmt, go vet and the refactoraudit gate are clean.
- **Mutation matrix:** 15/15 killed (below).

## Mutation matrix

Run at 5842cdec7 by `mut9637.py`: `go test -json` over pkg/daemon (`-run '9637|TestNftNetlinkParity|TestHostInboundFilterNoStanzaDefaultDeny'`) and pkg/dataplane/userspace (`-run 9637`); a skipped real-kernel or parity target is VOID, not a kill, scored by `Action=="fail"` on the named top-level test. The no-op run was CLEAN, and each mutant was restored with `git checkout HEAD --`.

| mutant | verdict | target |
|---|---|---|
| M1-oracle-ingress-rules-not-emitted | KILLED | `TestHostInboundIngressZoneVerdictsOnRealKernel9637` |
| M2-oracle-ingress-after-destination | KILLED | `TestHostInboundIngressZoneVerdictsOnRealKernel9637` |
| M3-ingress-scoped-to-own-addresses | KILLED | `TestHostInboundIngressZoneVerdictsOnRealKernel9637` |
| M4-ingress-drop-omitted | KILLED | `TestHostInboundIngressZoneVerdictsOnRealKernel9637` |
| M5-counter-prepass-ignores-ingress | KILLED | `TestHostInboundIngressRulesShape9637` |
| M6-netlink-ingress-rules-not-emitted | KILLED | `TestNftNetlinkParity` |
| M7-netlink-counter-prepass-ignores-ingress | KILLED | `TestNftNetlinkParity` |
| M8-netlink-any-service-emits-matches | KILLED | `TestNftNetlinkParity` |
| M9-converter-drops-ingress-netdevs | KILLED | `TestNftNetlinkParity` |
| M10-shared-claim-kept | KILLED | `TestHostInboundViewIngressNetdevsExcludesSharedClaims9637` |
| M11-lifeline-netdev-kept | KILLED | `TestHostInboundViewIngressNetdevsExcludesSharedClaims9637` |
| M12-vrf-enslaved-netdev-kept | KILLED | `TestZoneHostInboundViewIngressNetdevs9637` |
| M13-physical-row-claims-trunk-parent | KILLED | `TestZoneHostInboundViewIngressNetdevs9637` |
| M14-vrf-helper-returns-nothing | KILLED | `TestZoneHostInboundViewIngressNetdevs9637` |
| M15-oracle-any-service-emits-matches | KILLED | `TestNftNetlinkParity` |

## Lab gate: first run

- **Timestamp**: 2026-09-11T17:50Z
- **Action**: The first lab run deployed `a7d102c5f`. Its probe measured the change on fw0.
  - **WAN leg (exposure) is fixed live.** A client on the WAN VLAN 80 segment, routed through the firewall, sent TCP/22 to the LAN VIP `10.0.61.1`. Before the deploy it was REFUSED (admitted); after it, TIMEOUT. ping answered both times.
  - fw0 rendered 16 ingress-zone rules.
  - **LAN leg (over-refusal) is NOT fixed live.** TCP/22 from the LAN host to the wan addresses `172.16.50.8` and `172.16.80.8` timed out both before and after, although a lan-scoped `iifname "ge-0-0-1" ... tcp dport 22 accept` is rendered.
  - A diagnostic capture of the arrival interface is queued.
- **Action**: Both smokes stopped on the new posture check, before running their matrix. It required a `security-zone lan interfaces reth1.0` line, but the live config spells the member `interfaces reth1 host-inbound-traffic ...`.
- **File(s)**: `test/incus/test-host-inbound.sh`: the check now accepts `reth1` or `reth1.0`, with trailing tokens. It matches the live line and does not match `reth10`.

## Lab gate: diagnosis of the LAN leg

- **Timestamp**: 2026-09-11T18:15Z
- **Action**: Captured on fw0 (tcpdump, in a lock cell) while the LAN host probed.
  - TCP/22 SYNs to `172.16.80.8` arrived on **`xpf-usp0`**, the userspace dataplane's reinject device.
  - A SYN to the LAN VIP `10.0.61.1` arrived on `ge-0-0-1`.
- **Mechanism**:
  - The lab's `lan-to-wan` rule is `source-nat interface`, so the wan addresses are interface-NAT addresses.
  - The XDP shim's `is_local_destination` reports those as non-local (#290 reverse-NAT repair).
  - The userspace dataplane runs its ingress-zone host-inbound check and reinjects through `xpf-usp0`.
  - No view claims `xpf-usp0`, so the kernel's destination rules still judge by the address owner.
- **Result**:
  - The over-refusal remains for interface-NAT-owned addresses.
  - The exposure is closed on the direct path (WAN-leg probe).
  - #9637 stays open for the reinject path; this PR references it.
- **Action**: fw0's `nft` v1.1.6 prints the anonymous ifname sets as `iifname { "", "" }`. The wan drop plainly matched, so the elements are right in the kernel; this is a display quirk of that nft build.
- **File(s)**:
  - `test/incus/test-host-inbound.sh`: the `wan` ssh cells are back to DENY, with the residual stated beside them.
  - `docs/host-inbound-service-matrix.md`: the observable-behaviour list and the #6936 matrix state the residual.
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    db9ffcc36. That tree is origin/master ac03dce96 merged with 1 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9637.
    - Go leg: 78 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 8373 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9637.md

## Reinject path: implementation (parent-authorized, binding gates)

- **Timestamp**: 2026-09-14
- **Action**: Implemented the reinject-path fix on
  `fix/9637-reinject-ingress`: a views-scoped accept (`iifname
  "xpf-usp0"` + view daddr, named counter `xpfhia_reinject`) after the
  global accepts / junos-host jumps and before the ingress-zone rules,
  on both the oracle and netlink surfaces. It renders only when the
  dataplane runs this generation's snapshot
  (`hostInboundDataplaneFresh`, cleared on any ApplyConfig failure
  including cancel closeout, set on success); stale renders are
  byte-identical to pre-#9637.
- **Action**: Guard-first ledger on the accept names exactly which
  deployments lose an effective denial: D1 (closed pre-landing by the
  fresh gate), D2 (default-permit tunnel-NAT), D3 (capped import),
  D4a/D4b (non-owning-table MissingNeighbor / ForwardCandidate
  build-failure), NAT-T (ESP-in-UDP to non-IKE zones). D2/D3/D4/NAT-T
  land only with explicit approver sign-off; follow-up bodies are
  prepared on disk under `docs/pr/9637/followups/` (parent files).
- **Action**: Codex impl-r1/r2 + GLM impl-r1/r2 adversarial reviews
  answered at source (closeout clearing with RED-verified production
  pins, D4 narrowed with Rust citations, builder-disjointness,
  T1 4-way shape matrix, over-claims removed). Reviews stay on disk
  under `docs/pr/9637/reviews/`, out of history per standard.
- **File(s)**:
  - `pkg/daemon/host_inbound_reinject_9637.go`,
    `pkg/nftables/host_inbound_reinject_9637.go`,
    `pkg/nftables/netlink_hostinbound_reinject_9637.go` (new): the
    accept and its netlink mirror.
  - `pkg/daemon/daemon.go`, `daemon_apply_dataplane.go`,
    `daemon_apply_hostauth.go`, `daemon_nft.go`,
    `daemon_nft_netlink.go`: the D1 fresh gate.
  - `pkg/daemon/host_inbound_reinject_9637_test.go` (new, 18-row
    disposition enumeration + gate/RED pins),
    `daemon_nft_netlink_parity_test.go` (T1 fresh/stale x
    programs/no-programs).
  - `docs/host-inbound-service-matrix.md`,
    `test/incus/test-host-inbound.sh`: wan-ssh cells ADMIT with the
    reinject mechanism stated; residual bullet closed.
- **Validation**: `pkg/daemon` full suite ok (88.7s),
  `pkg/nftables` ok, reinject tests green.

## Reinject path: baseline lock cell (residual live, deploy refused)

- **Timestamp**: 2026-09-14T22:00-22:05Z. Lock cell, carrier 42->42
  (no flap). Take: all RGs node0 primary, failover counts 0; builds
  fw0 `3187b9a3`, fw1 `146413d2` (9587 lane's `gbd29a24f3` tree);
  render has the #9860 ingress rules and no reinject accept.
- **Action**: Baseline probes confirm the residual live on the
  current build. From the LAN host: ssh to the LAN VIP REFUSED
  (admitted) v4+v6, telnet TIMEOUT; ssh to `172.16.50.8`,
  `172.16.80.8` and the v6 twins all TIMEOUT; every ping answers
  (2/2). Ten UDP/4500 datagrams to `172.16.80.8` land exactly in
  `xpfhi_ip_3_wan` (+10). The updated smoke scores 23 pass / 4 fail,
  the fails exactly the 4 wan-ssh cells (negative control: the smoke
  detects the residual on the unfixed build).
- **Action**: The WAN exposure leg via `xpf-mouse-target` is
  half-blind (v4 ping via `.254` fails; v6 TCP RSTs without ping) and
  unscored; the re-run must install host routes via the firewall
  (`.8`) and verify ping before scoring.
- **Action**: Deploy of the branch deb refused FAIL-CLOSED by `xpfd
  upgrade --rolling` verify-dataplane: embedded `userspace_ctrl`
  ValueSize 40 vs pinned 56. Provenance verified: origin/master is
  also 40 (identical struct, no touching commits since the base), so
  the 56 pins come from 9587's unmerged PR #10032 — a rebase does not
  unblock. Pins left untouched; no unilateral recovery attempted.
- **Action**: The deploy drain bumped RG1/RG2 failover counts 0->1
  and parked RG2 on node1; RG2 was failed back in-cell. Release
  state: all RGs node0 primary, counts RG0=0/RG1=1/RG2=1, builds and
  xpfd untouched, node1 `secondary-hold`->`secondary` (drain cleared
  the park; plain secondary is the normal backup). Queue notified
  with a pins warning for the next deployers.
- **Action**: Parent ruling: HOLD for the #10032 merge, then rebase
  onto new master (adopting the 56 ABI), re-verify, re-take the cell
  for post-fix probes + smokes + PR. Branch scrubbed locally in the
  meantime (4 standard commits + this log); process files stay on
  disk under `docs/pr/9637/`, out of history.
- **File(s)**: docs/log/9637.md

## Reinject path: rebase + verification lock cell (fix proven both nodes)

- **Timestamp**: 2026-09-15T02:53-03:29Z.
- **Action**: #10032 merged (master `18f5f9a78`, 56-byte ctrl ABI).
  Branch rebased cleanly and has since grown to 10 standard commits
  (operator-narrowing rework included), suites green
  on the rebased tree (`pkg/daemon` 96.4s, `pkg/nftables` ok), fresh
  deb built, force-pushed (parent-authorized).
- **Action**: 9637d's cell deployed the branch (both nodes
  `8e162fd5da32`, banner `working-15135-gf0464d3fd`) and measured
  27/27 + 69/69 (ledgers `05ca1223`, `8b6eeee1`, committed),
  discovering the `xpf_dp_rst` RST-suppression (fixed in-branch via
  the self-managed listener) and reporting fw1 accept-less with a
  data-vacuous failover leg. Both fw1 claims rested on `nft`-CLI
  reads; the CLI is missing on fw1 (see below) so both lanes' fw1
  reads were vacuous — 9637d conceded the method flaw.
- **Action**: Verification cell (this lane), 03:24:39-03:28:37Z,
  carrier 42->42, RGs home node0 at release (counts 3/3 after two
  failover round-trips), builds untouched, listeners cleaned:
  - LAN->wan ssh OPEN v4+v6 x4 with `xpfhia_reinject` delta exactly
    4; telnet TIMEOUT; all pings 2/2. Residual closed on the wire.
  - WAN->VIP leg scored for the first time (host routes via `.8`
    fixed the blind default path): ssh TIMEOUT v4+v6, ping 2/2 —
    exposure stays closed.
  - UDP/4500 x10 to `172.16.80.8`: reinject +10, wan-drop +0 (the
    NAT-T named flip measured live; unzoned untouched).
  - Lifeline addrs absent, RETH VIP judged, fallback `policy
    accept` — preserved on both nodes' renders.
  - `make test-host-inbound` 27/27 PASS;
    `make test-host-inbound-failover` 69/69 PASS (matrix stable
    across failover and failback).
- **Action**: fw1 HAS the accept (proven without trusting any fw1 CLI
  read, all vacuous): post-failover ssh returned OPEN with fw0's
  counters still while the VIP re-resolved to fw1's MAC `:01` — fw0
  is excluded as the server (RG addrs removed on resign in both HA
  modes is defense-in-depth; ARP-to-`:01` plus fw0-reinject-+0
  already suffice), so fw1 served; fw1's install log shows
  `host-inbound filter applied` post-install-success
  (`daemon_nft.go:711-714`, failure returns earlier), proving a
  non-empty install — and OPEN decides accept. (An earlier draft
  cited telnet-DROP for non-emptiness; withdrawn — userspace
  deny-never-reinjects explains identical TIMEOUTs. Verified by the
  9637d lane.) Hence fw1 rendered the fresh shape: fw1 applied
  successfully, and both lanes' failover legs measured
  fw1-with-accept (VALID — GARP refreshes the prober; the vacuity
  inference collapsed with its vacuous premise). No accept-less
  state was ever observed; the boot-race item is reframed as a
  hardiness review (deferred-publish/helper-readiness are real
  paths, unobserved here).
- **Action**: Real lab-health finding: the `nft` CLI is not
  installed on fw1's image (fw0 has it; absolute paths absent too).
  Every fw1 render/counter read via the CLI returns `Error: Command
  not found`, which `grep -c`/masking fallbacks report as
  zeros/absence. Lesson recorded: fw1 reads need a positive-read
  check (fail on empty/error output, never grep-vacuous). Parent to
  file the image-skew follow-up; boot-race is a hardiness review, not
  a confirmed bug.
- **File(s)**: docs/log/9637.md; `test/incus/test-host-inbound.sh`
  (9637d's listener harness, already in-branch).

## Reinject path: clean-tip ledger window (dual-TUN, fails=0)

- **Timestamp**: 2026-09-15T05:47-05:52Z. Lock cell, carrier 42->42.
  Branch rebased onto fixed master `c576960c4` (11 commits; one
  mechanical `apply.go` conflict, both init lines kept); tip
  `feef45f64` suites green (daemon full 88.5s, nftables, dataplane,
  networkd, Rust slowpath/outlet/pin/mtu/dispatch/6664; userspace
  shows only the 2 pre-existing master 9824 fails, proven on clean
  master).
- **Action**: Deployed tip deb `xpf_0.0.17567+gfeef45f64dbd` rolling
  both nodes, exit 0. Identity gate: exe shas MATCH
  (`cbc95ac0…ef209`), banner `working-15159-gfeef45f64-dirty` both
  nodes. Cluster found inverted (all RGs node1); deploy restored
  node0 primacy, verified. RGs home at release (counts 2/2).
- **Action**: Render checks: fw0 usp0 accept v4+v6 + counter present;
  zero `xpf-usp1` references (pinned); lifeline absent, RETH VIP
  judged, fallback `policy accept`; journal applied-lines on both
  nodes (fw1 CLI still missing — journal+behavioral reads).
- **Action**: Probes: LAN->wan ssh OPEN x4, reinject delta exactly 4;
  `:23` tripwire silent (no over-admission); WAN->VIP ssh TIMEOUT +
  ping 2/2 both families (host routes); UDP/4500 re-measured under
  dual-TUN: wan-drop +10 / reinject +0 (INVERTED vs pre-narrowing,
  as the delegated path requires — the old +10/+0 number cannot
  evidence post-narrowing DENY and is retired for that purpose).
- **Action**: `make test-host-inbound` 27/27 PASS (ledger
  `83c05c22`); `make test-host-inbound-failover` 69/69 PASS (ledger
  `52c961bd`), stable across failover+failback; Phase-3 attribution
  fw0-reinject +8 = baseline 4 + failback 4 (0 on fw0 during
  failover). Manual leg: VIP re-resolved to fw1 MAC, ssh OPEN via
  fw1, fw0 still, `:23` silent. Listeners/routes/probers cleaned.
- **File(s)**: docs/log/9637.md;
  `test/results/ledger.d/83c05c221106e9b8.json`,
  `test/results/ledger.d/52c961bd7a7eb0fe.json`.

## 2026-09-14 GPT-1: single-atomic outlet init (handshake read-skew removal)
- **Finding**: constructor handshake sampled `active` + `last_error` in two
  loads; a worker that had published `active` but not yet its io_uring-fallback
  note (or an MTU-degraded error before `active`) could be misread as
  failed-while-healthy. Active-dominates ordering narrowed but could not close it.
- **Change** (`userspace-dp/src/slowpath.rs`): `SharedStatus.init_state:
  AtomicU8` (0=starting, 1=live, 2=failed) + `OutletInit` + one-load
  `init_outcome()` reader. Worker publishes Live after `active`, Failed after
  the cause (cause-before-outcome); constructor loop loads one atomic per
  outlet; `handshake_step(a, b, timed_out)`; `new_without_worker` sets Live.
  Matrix rewritten over `OutletInit` (unrepresentable "live-with-diagnostic"
  row gone by construction).
- **Proof**: matrix 1/1; suites slow_path 41, disposition 34, mtu 75, pin 131,
  reinject 8, delegated/outlet/trusted green; full bin 5897 pass, 1 fail =
  `cos_literal_guard_7689` NeighborSnapshot 29 vs ceiling 26 — PROVEN
  pre-existing (fails identically with this change stashed; from prior
  committed snapshot.rs work, not this lane).
- **File(s)**: `userspace-dp/src/slowpath.rs`, `userspace-dp/src/slowpath_tests.rs`.

## 2026-09-15 fold-3: GPT-2 record path + fixtures + F2-tail + v18 note
- **GPT-2 / F2-MTU (A)**: decision is RECORD, not rewire. `reconcile_preserved_slow_path_mtu` (snapshot.rs:511) short-circuits on the trusted outlet's live MTU; widening the trigger to the lagging delegated outlet without a bounded design re-opens the #6097 ioctl storm the `last_acted` dedup exists to prevent — that design IS the filed follow-up. Verified #10069 OPEN with bounded scope (Gap 1 recovery design, Gap 2 delegated status reporting) + acceptance (bounded retry without regressing the storm pin; delegated live_mtu/degraded/counters on status+metrics with Go decoding). Code + doc now point at the real number: snapshot.rs pin/gap comments cite #10069, `mtu-delegated-retry-reporting.md` titled as filed #10069. `delegated_status()` already cited #10069 (slowpath.rs:735). Production health (`status.rs:770-774` trusted-only) is Gap 2's stated surface.
- **Fixtures (B)**: `ss`-ownership awk (test-host-inbound.sh:181-191) extracted to `hi_wildcard_socket_pids` in host-inbound-lib.sh (pure: stdin -> sorted PIDs, deliberately NO ownership filtering — foreign PIDs are emitted for the caller to reject); caller swapped to the lib helper. 8 hermetic rows in host-inbound-selftest.sh: wildcard v4/v6 + dedup, :220 near-miss vs exact, loopback v4/v6 exclusion, multi-PID, foreign-PID-emitted, empty. `make test-host-inbound-lib` equivalent: 69 passed, 0 failed.
- **F2-tail (C)**: gap test `converged_trusted_lagging_delegated_skips_recovery` extended to first recovery — loops `last_acted` over [9000 (already-acted), 0 (never-acted)]; both skip (the 0 case via the trusted-live short-circuit, proving not even a first attempt issues). Still reds iff the preserved path starts recovering the lagging outlet.
- **Typo (D)**: `52c969bd` — 0 occurrences in the worktree, never committed (`git log -S` empty); stale line number, closes as no-op.
- **v18 note (E)**: protocol.go rewrite grounded in code+test: helper wire refusal is ordinary-class (mixed_version_matrix_6691_test.go) → #5679 deferred path, helper stays armed on previous-good, tail runs with D1 clear → accept-less; daemon-side refusal is an abort sentinel → tail skipped, commit fails, helper disarmed (fail-closed, deliberate), retained kernel table stands. Old "without disarming anything" contradicted daemon_apply.go:602-607 + the test's own gate-sentinel comment.
- **Proof**: Rust day2_mtu / partial_delegated / converged_trusted / apply_snapshot_wires / persistently_degraded 1/1 each; Go `-run '6691|MixedVersion'` ok; bash -n x3 + selftest 69/69.
- **File(s)**: `userspace-dp/src/afxdp/coordinator/reconcile/snapshot.rs`, `pkg/dataplane/userspace/protocol.go`, `test/incus/host-inbound-lib.sh`, `test/incus/test-host-inbound.sh`, `test/incus/host-inbound-selftest.sh`, `docs/pr/9637/followups/mtu-delegated-retry-reporting.md`.

## Reinject path: DELTA window on final tip (fails=0, merge-gating)

- **Timestamp**: 2026-09-15T07:03-07:08Z. Lock cell, carrier 42->42.
  Tip `038738422` (fold-3) verified pre-window (build ok, daemon full
  88.5s-class green, Rust slowpath/outlet/pin/mtu/dispatch/6664 green;
  userspace shows only the 2 pre-existing master 9824 fails).
- **Action**: Deployed tip deb `xpf_0.0.17571+g038738422fa7` rolling
  both nodes, exit 0. Identity gate: exe shas MATCH
  (`569f9def…6114a`), banner `working-15163-g038738422-dirty` both
  nodes. Cluster found inverted (all RGs node1); deploy restored
  node0 primacy, verified. RGs home at release (counts 2/2).
- **Action**: Render: fw0 usp0 accept v4+v6 + counter; zero `xpf-usp1`
  references (pinned); lifeline/RETH/fallback intact; journal
  applied-lines both nodes (fw1 CLI still missing).
- **Action**: Probes: ssh OPEN + reinject exactly +4; `:23` tripwire
  silent both nodes; WAN TIMEOUT + ping 2/2; UDP/4500 dual-TUN
  confirmed (wan-drop +10 / reinject +0).
- **Action**: `make test-host-inbound` 27/27 PASS (ledger
  `9dfa7c34`); `make test-host-inbound-failover` 69/69 PASS (ledger
  `cd06da1d`), stable both directions; manual leg fw1-served OPEN +
  fw0-0 + VIP `:01` + `:23` silent. Listeners/routes/probers cleaned.
- **File(s)**: docs/log/9637.md;
  `test/results/ledger.d/9dfa7c34ddf93269.json`,
  `test/results/ledger.d/cd06da1d11b32315.json`.

# #9751: a heartbeat restart that fails during a config apply latches the heartbeat dead

Lane: lane-ha2. It shares a branch with #9722 (`fix/9722-heartbeat-restart-grace`, worktree
`/var/tmp/wt-9722-heartbeat-restart-grace`): same function (`RestartHeartbeat`), same apply call
site. It is the second commit, on top of #9722's.

- **Timestamp**: 2026-09-11 05:01 PDT
- **Action**: Read the issue and the code at `304920d71`.
  - `RestartHeartbeat` snapshots `running := hbSender != nil || hbReceiver != nil` and returns
    false when it is not running.
  - After `StopHeartbeat`, five `StartHeartbeat` attempts one second apart. On exhaustion both
    handles stay nil.
  - `ApplyCommittedHeartbeatTiming` is gated on a live receiver.
  - The apply (`daemon_apply_dataplane.go`) discards the bool.
  - The daemon's `stopClusterComms` is the only production `StopHeartbeat` outside the start and
    restart paths.
- **File(s)**: none

- **Timestamp**: 2026-09-11 05:19 PDT
- **Action**: Fix, applied by a checked patch script (every anchor exactly once, or nothing
  written).
  - `manager.go`: `hbRestartOwed`, `hbRestartOwedSeed`, `hbDeliberateStops`.
  - `heartbeat_manager.go`:
    - The exported `StopHeartbeat` counts itself, clears the debt, then calls the new internal
      `stopHeartbeat`. `startHeartbeat` and `RestartHeartbeat` tear down through
      `stopHeartbeat`.
    - A publishing start clears the debt.
    - `RestartHeartbeat` proceeds when owed, seeds from `hbRestartOwedSeed` when no receiver
      exists, and on exhaustion records the debt only if `hbDeliberateStops` is unchanged and
      no heartbeat is installed.
    - `HeartbeatRestartOwed()`.
    - `heartbeatRestartRetryDelay` replaces the literal 1s sleep, as a test seam.
  - `pkg/daemon`: `heartbeat_restart_report.go` adds `restartHeartbeatAfterRebind`, with a
    compile-time assertion that `*cluster.Manager` satisfies its interface. The apply joins its
    error into `networkdErr`.
- **File(s)**: `pkg/cluster/manager.go`, `pkg/cluster/heartbeat_manager.go`,
  `pkg/daemon/daemon_apply_dataplane.go`, `pkg/daemon/heartbeat_restart_report.go`

- **Timestamp**: 2026-09-11 05:20 PDT
- **Action**: Cells, all run with `-v` to confirm none skipped.
  - `pkg/cluster/heartbeat_restart_owed_9751_test.go`, on 127.0.0.1 with the remembered address
    pointed at TEST-NET-1 (192.0.2.1) for the failing restart. The cell skips if that address is
    bindable on the host; it was not.
    - A failed restart records a debt, and the next restart starts the heartbeat with the
      pre-failure seed and the #9722 restart grace.
    - `StopHeartbeat` clears the debt, and a later restart does not resurrect the heartbeat.
    - A `StopHeartbeat` landed through `hbStartInWindowHook` inside the retry window records no
      debt.
  - `pkg/daemon/heartbeat_restart_report_9751_test.go`:
    - the helper reports only restarted=false with owed=true;
    - an AST pin: exactly one `restartHeartbeatAfterRebind` call in `daemon_apply_dataplane.go`
      has its error joined into `networkdErr`, and no discarded `RestartHeartbeat()` call remains.
  - `go test -race -run '9751|9722|TestRestartHeartbeat|7257|4033|7164' ./pkg/cluster`: ok.
    `go test -run 9751 ./pkg/daemon`: ok.
- **File(s)**: the two test files above

- **Timestamp**: 2026-09-11 05:24 PDT
- **Action**: Docs.
  - `pkg/cluster/README.md`: a subsection, "A failed heartbeat restart is owed, not latched
    (#9751)".
  - `pkg/daemon/README.md`: the management-VRF rebind paragraph now says the heartbeat restart
    after it is joined into `networkdErr`.
  - `docs/bug-heartbeat-vrf-rebind-split-brain.md`: the #9722 addendum now covers #9751.
- **File(s)**: `pkg/cluster/README.md`, `pkg/daemon/README.md`,
  `docs/bug-heartbeat-vrf-rebind-split-brain.md`

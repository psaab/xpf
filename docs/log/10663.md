# #10663 — bound PPTP control capture by the IP-declared datagram end

- **Timestamp**: 2026-09-24
- **Action**: `capture_pptp_control_segment` now copies only `[TCP payload start, IP-declared datagram end)`, using the frame's IPv4/IPv6 version and shared `declared_l3_end` bound. Malformed/truncated L3 and TCP headers extending beyond the declared datagram fail closed. End-to-end IPv4 and IPv6 cells prove trailing capture slack cannot complete an Outgoing-Call-Reply or teach its call association.
- **Validation**: fail-on-revert `cargo test --manifest-path userspace-dp/Cargo.toml --bins capture_slack_cannot_complete_pptp_call_reply_10663` failed both family cells as intended with the old capture-end slice: each drain learned 1 association instead of 0. Fixed `cargo test --manifest-path userspace-dp/Cargo.toml --bins pptp_dispatch_join_tests_7699` passed (8 tests, 2 suites).
- **File(s)**: `userspace-dp/src/afxdp/poll_stages.rs`, `userspace-dp/src/afxdp/poll_stages_tests.rs`, `docs/log/10663.md`

# #9947 — frr: degraded/hard reload fails the commit (F-007) and closes narrowed-chain permits (F-008/#10129)

- **Timestamp**: 2026-09-15
  - **Cohort**: F-007 (degraded additive reload reports success while a
    stale permit stays installed — bounded by retry convergence for
    TRANSIENT failures, INDEFINITE with frr-pythontools missing) +
    F-008 (narrowed BGP chain renders permit-terminated surviving subset,
    warn-only). Base a126bc209 (rebased from f3e25ae2a; comment-only
    delta), worktree 9947-frr-permit, branch fix/9947-frr-permit.
  - **STEP-0 RED (base, since removed)**: TestF007DegradedReloadReportsSuccess9947
    (daemon) FAILED — injected primary failure → additive fallback → gauge
    set, yet applyRoutingRules returned nil. TestF008NarrowedChainPermitsFallThrough9947
    (frr) FAILED after tightening — narrowed [ACCEPTER,GHOST] attaches
    ACCEPTER permit-terminated (first version passed vacuously: its
    any-deny-in-section assertion matched the unrelated
    ACCEPTER-xpf-redist alias deny; fixed to resolve the neighbor's
    attached map and assert ITS trailing action).
    Raw RED transcript (base f3e25ae2a):
    `--- FAIL: TestF007DegradedReloadReportsSuccess9947 (0.00s)` —
    "degraded reload converged additively (stale removal deferred) yet
    applyRoutingRules returned nil";
    `--- FAIL: TestF008NarrowedChainPermitsFallThrough9947 (0.00s)` —
    "narrowed chain [ACCEPTER, GHOST] attaches ACCEPTER
    permit-terminated".
  - **Plan reviews (2 hostile, pre-implementation)**: HostilePlanA →
    F-007 RESCOPE (separate frrErr slot, hard-vs-degraded + pytools
    liveness, real runbook), F-008 KILL (ships #8369 load-time silent
    outage without measurement; terminating-default/match-all reachability
    unsolved; 4-pair collision matrix unspecified). HostilePlanB →
    F-007 KILL-as-planned (pytools-missing would red every commit; FRR
    debt misrouted through #9693 owner forbidden from FRR; actuator/commit
    divergence), F-008 RESCOPE (warn+gauge only, no deny alias until
    attach-switch/quarantine/VRF/outage closed).
  - **Adjudication (parent review, dual NEEDS-MAJOR folded)**: F-007
    SHIPPED — fail commit closed on HARD (nothing applied) and on EVERY
    degraded reload (stale removal deferred) via a NEW frrErr deferred
    slot threaded to the tail join (never via routingRuleErr/#9693 debt,
    pinned by the extended guard test). NO pytools-missing exemption: the
    fallback cannot remove and the retry re-invokes the missing program
    forever, so nil would stamp MarkActiveApplied certifying a removal
    that is indefinitely unenforced; the message names the persistent
    cause (install frr-pythontools) distinctly. Failing does not brick
    boot/HA-sync (background applies log and continue; sync is
    degrade-not-fail fire-and-forget). Actuator UNCHANGED in contract
    (publishes on degraded — new routes live, FIBs agree; aborts only
    on hard) with the branch explicit after applyFRRConfig started
    returning the sentinel verbatim; alerting contract is gauge + 10m
    for-duration + runbook (pkg/frr/README: plain-commit retry is bare
    `commit`; after FAILED `commit confirmed` bare `commit` is
    confirm-only WITHOUT re-apply — retry is dirty-edit + `commit` or
    nested `commit confirmed`, pinned by the pkg/cli 9947 cells).
    F-008 TRACKED at #10129 (OPEN; this PR ships no F-008 code).
    Settled from source: [EMPTY,SYNTH-DENY] renders lone deny-10,
    reachable by all (TestEmptySurvivorSynthesizedDenyIsReachable9947) —
    #8369's "UNREACHABLE"/over-count rationale is corrected in-tree and
    the empty-survivor shape stays IN the denominator (excluding it
    would hide a permit-all→deny-all outage case). Render-vs-compile,
    stated without conflation: compile-side lenient rejection would
    brick (#1960, stays warn-only); render-side ApplyFull rejection does
    NOT take Store.Load and does NOT brick daemon/mgmt (#1993), but on
    cold boot it leaves FRR EMPTY→permit-all (fail-open, worse than
    narrowed); a rendered deny alias costs the load-time silent routing
    change to a 100%-tolerant-path population. Collision handling is
    SOLVABLE by recipe (reserve + validator + belts + dedupe) and is
    specified in #10129, not a blocker. Remaining unpinned reachability
    (terminating-default, match-all-final-term survivors) is code-read
    and tracked there with the fleet-split + migration decision.
  - **File(s)**: pkg/daemon/daemon_apply_routing.go (applyFRRFull split
    from applyRoutingRules, fail-closed incl. pytools), pkg/daemon/daemon_ipmon.go
    (applyFRRConfig returns sentinel verbatim; actuator branches degraded
    explicitly), pkg/daemon/daemon_apply.go (frrErr capture + thread, never
    latched), pkg/daemon/daemon_apply_tail.go (frrErr operand + join),
    pkg/frr/manager.go (IsFRRReloadPyMissing; hard wrap preserves primary
    cause), pkg/api/metrics_descriptors_controlplane.go (10m alert
    contract), pkg/frr/README.md (runbook: confirmation-vs-retry +
    indefinite-window wording), pkg/frr/policy_chain_narrowing_warn_8363.go
    + policy_chain_empty_survivor_8369_test.go (#8369 rationale correction,
    no behavior change), pkg/daemon/frr_reload_failclosed_9947_test.go
    (7 cells incl. removal-bearing pytools), pkg/frr/policy_chain_empty_survivor_synthesis_9947_test.go
    (settling measurement), pkg/frr/reload_hard_pytools_cause_9947_test.go,
    pkg/cli/cli_commit_retry_after_failed_confirmed_9947_test.go (2 cells),
    pkg/daemon/routing_reconcile_debt_9693_test.go (forbid applyFRRFull),
    15 daemon test files (frrErr nil-operand codemod, no logic change).
  - **Verification**: all 9947 cells green 5x (daemon 7 + frr 2 + cli 2);
    actuator pair green (contract preserved); pkg/frr count=1 green;
    pkg/api count=1 green; pkg/cli count=1 green; pkg/daemon count=1
    green EXCEPT pre-existing base failure
    TestCommittedDigestStableAcrossMTUSync9841 (SyncApply parse error on an
    invalid fixture text; reproduced on clean a126bc209 in a temp worktree
    — sole daemon failure by full `--- FAIL` grep); gofmt/vet clean.
  - **Historical scope note (F-007 commit)**: F-008 was OPEN at #10129
    (surviving-permit behavior, corrected measurement, migration decision,
    collision recipe, acceptance criteria). The follow-up phase below now
    covers that member and closes the cohort.

## F-008 production suffix-safe alias — #10129 covered (2026-09-18)

- **Disposition**: `gh issue view 10129` showed OPEN with no assignee. This
  cohort PR covers the member and records `Closes #10129` alongside
  `Closes #9947`; no separate assignment or parallel implementation exists.
- **RED per member**:
  `go test ./pkg/frr -run 'TestNarrowedProduction' -count=1 -v` failed the
  seven behavior-flip cells before the flip: single, composed,
  empty-survivor, VRF, export, rendered-name collision, and warning-parity.
  The deny-inert control was already green because it must retain its shared
  authored terminal map. F-007's existing RED/GREEN evidence is retained
  above: injected degraded, hard, and missing-pythontools failures fail the
  owning apply boundary, while a clean reload succeeds.
- **GREEN per member**:
  after activating the existing alias machinery at the production owning
  boundaries, all eight production cells pass:
  `TestNarrowedProductionSingleAttachesDenyAlias10129`,
  `TestNarrowedProductionComposedAttachesDenyAlias10129`,
  `TestNarrowedProductionEmptySurvivorAttachesDenyAll10129`,
  `TestNarrowedProductionVRFAttachesDenyAlias10129`,
  `TestNarrowedProductionExportAttachesDenyAlias10129`,
  `TestNarrowedProductionRenderedAliasCollisionFailsApplyClosed10129`,
  `TestNarrowedProductionDenyInertShapesRemainShared10129`, and
  `TestNarrowedProductionWarnParity10129`.
  The compatibility cells also pass for explicit defaults, shared-map
  immutability, global/VRF dedupe, ghost-first/middle survivor preservation,
  deny-inert shapes, nil PolicyOptions, and oversized quarantine.
- **Boundary change**: `ApplyFull` now runs the narrowed-alias collision belt
  before writing; `buildManagedSection` emits deduplicated aliases;
  `generateProtocols` attaches aliases for suffix-narrowed fall-through and
  empty survivors in both import/export and global/VRF paths. The existing
  emptied-chain deny remains emitted outside the PolicyOptions guard.
- **Scope**: F-007 was already shipped in `be73b4d79`; this change closes F-008
  at #10129. Ghost-first/middle remains deliberately non-eligible because a
  synthesized terminating member there would delete later surviving members;
  that discriminator is pinned by the regression suite.
- **Rollout control**: suffix-only is the migration control for this PR:
  deny only suffix fall-through and empty survivors; retain
  non-suffix/ghost-first/middle and deny-inert behavior exactly. Existing
  warnings, shape gauges, and operator guidance to define or remove missing
  names are unchanged. The fleet census/migration gate in the #10129 Phase 1
  log remains owed only for any future non-suffix expansion, outside this PR.
- **Verification**:
  `go test ./pkg/frr -run 'TestNarrowed|TestNarrowing|TestEmptySurvivor|TestTerminating|TestMatchAll' -count=1 -v`
  passed all 44 matching top-level tests; `go test ./pkg/frr/... -count=1` passed.
  The known `BASELINE-RUST` daemon failure from the prior F-007 run remains
  outside this FRR-only lane and is not changed here.

**Runtime probe**: under the shared lock, `incus exec loss:xpf-userspace-fw0 -- vtysh -c 'show version'` reported FRRouting 10.6.0. The same live target's `vtysh -c 'show bgp ipv4 unicast summary'` reported `bgpd is not running`, so no live BGP route-map prefix accept/deny measurement is available; no live route-evaluation claim is made.

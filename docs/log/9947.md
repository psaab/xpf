# #9947 — frr: degraded/hard reload now fails the commit (F-007 shipped); narrowed-chain deny adjudicated DEFERRED (F-008)

- **Timestamp**: 2026-09-15
  - **Cohort**: F-007 (degraded additive reload reports success, stale permit
    alive ≤5m) + F-008 (narrowed BGP chain renders permit-terminated
    surviving subset, warn-only). Base f3e25ae2a, worktree
    9947-frr-permit, branch fix/9947-frr-permit.
  - **STEP-0 RED (base, since removed)**: TestF007DegradedReloadReportsSuccess9947
    (daemon) FAILED — injected primary failure → additive fallback → gauge
    set, yet applyRoutingRules returned nil. TestF008NarrowedChainPermitsFallThrough9947
    (frr) FAILED after tightening — narrowed [ACCEPTER,GHOST] attaches
    ACCEPTER permit-terminated (first version passed vacuously: its
    any-deny-in-section assertion matched the unrelated
    ACCEPTER-xpf-redist alias deny; fixed to resolve the neighbor's
    attached map and assert ITS trailing action).
  - **Plan reviews (2 hostile, pre-implementation)**: HostilePlanA →
    F-007 RESCOPE (separate frrErr slot, hard-vs-degraded + pytools
    liveness, real runbook), F-008 KILL (ships #8369 load-time silent
    outage without measurement; terminating-default/match-all reachability
    unsolved; 4-pair collision matrix unspecified). HostilePlanB →
    F-007 KILL-as-planned (pytools-missing would red every commit; FRR
    debt misrouted through #9693 owner forbidden from FRR; actuator/commit
    divergence), F-008 RESCOPE (warn+gauge only, no deny alias until
    attach-switch/quarantine/VRF/outage closed).
  - **Adjudication**: F-007 SHIPPED rescoped — fail commit closed on HARD
    (nothing applied) and TRANSIENT degraded (stale removal deferred) via
    a NEW frrErr deferred slot threaded to the tail join (never via
    routingRuleErr/#9693 debt); pytools-missing degraded stays tolerated
    (nil) with gauge + warn-once + slow retry, since failing it would red
    every commit until the package is installed; actuator UNCHANGED in
    contract (publishes on degraded — new routes live, FIBs agree; aborts
    only on hard) with the branch made explicit after applyFRRConfig
    started returning the sentinel verbatim; alerting contract is gauge +
    10m for-duration + runbook (pkg/frr/README), not bare descriptor text.
    F-008 NOT SHIPPED — deferred to the #8369 measurement precondition
    (CLOSED-as-DEFERRED via #8591): 100% of narrowed population arrives via
    the tolerant path (strict rejects undefined refs), so a deny is a
    load-time silent routing change guessing at unseen intent; suffix gate
    removes deletion but not outage; empty-survivor + terminating-default
    + match-all-final-term reachability unsolved; alias collision matrix
    (alias-vs-operator/composed/emptied/alias + dedupe) unproven; failing
    the tolerant render would brick (#1960) or leave FRR empty→permit-all
    (worse). No routing change for F-008; warn + narrowed gauges stand.
  - **File(s)**: pkg/daemon/daemon_apply_routing.go (applyFRRFull split
    from applyRoutingRules), pkg/daemon/daemon_ipmon.go (applyFRRConfig
    returns sentinel verbatim; actuator branches degraded explicitly),
    pkg/daemon/daemon_apply.go (frrErr capture + thread, never latched),
    pkg/daemon/daemon_apply_tail.go (frrErr operand + join),
    pkg/frr/manager.go (IsFRRReloadPyMissing), pkg/api/metrics_descriptors_controlplane.go
    (10m alert contract), pkg/frr/README.md (runbook + posture update),
    pkg/daemon/frr_reload_failclosed_9947_test.go (6 cells),
    15 daemon test files (frrErr nil-operand codemod, no logic change).
  - **Verification**: 6 new 9947 cells green 5x; actuator pair
    (TestActuatorPublishesOnDegradedFRR / TestActuatorAbortsPublishOnHardFRRError)
    green (contract preserved); pkg/frr count=1 green; pkg/api count=1
    green; pkg/daemon count=1 green; gofmt/vet clean.
  - **Scope note**: F-008 owes a fleet-measurement follow-up (corrected
    denominator excluding empty-survivor/terminating-default/match-all
    shapes) before any deny ships; this PR intentionally does not close
    that half by code.

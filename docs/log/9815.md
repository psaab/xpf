# #9815 — RI member spelled differently from its stanza (dash vs slash)

Worktree `/home/ps/git/pi-xpf/.claude/worktrees/9815-ri-member`, branch
`fix/9815-ri-member`, restacked onto current `origin/master`
`32ce59ed0` after initial validation at pinned `ba885b0b1`.
Implementation commit `e6afdddeb`; the final history commit adds this log.

Issue `psaab/xpf#9815` (OPEN, 0 comments — `gh ... --json comments --jq
'.comments | length'` → `0`). Two findings, one root cause (members resolved
using raw spelling):
- LEAD-F66 (MATERIAL): kernel VRF bind misses a unit member whose spelling
  differs from its stanza → binds `base.<unit>` (nonexistent) or the parent
  (tagged unit 0, silent).
- SYN-VRF-02 (COHORT): `DefaultInstanceIngressIfaces` misreads the same split
  → claimed units stay in the default next-table scoping set. Ordering hazard:
  bind fix MUST land with scoping fix.

Probe (issue acceptance): vlan-tagging port, stanza `ge-0/0/0` or `ge-0-0-0`,
unit 0 on vlan 10, unit 1 on vlan 50. Matrix: stanza spelling × member
spelling; every row must bind `.50`/`.10`; netns cell asserts VRF master.

## Hygiene gate

Base proven pristine BEFORE implementation: `git status --porcelain` EMPTY,
`git rev-parse HEAD` = `609433efb...`, `git diff --stat` empty, `git stash
list` empty. Prior-attempt residue (2 repro tests + 1 log) archived to
`/tmp/eng9815c-residue/` with matching sha256; the 2 test files restored
byte-identical (sha match) after every line was traced against the issue
acceptance; the residue log was NOT restored (this file replaces it).
Adopted tests are this lane's STEP-0 cells; no other untracked files exist.

## STEP-0 — RED on base (production tree = 609433efb, true exit codes)

- `TestRIMemberBindsVLANDeviceInEitherSpelling9815` (daemon): RED, GO_EXIT=1 —
  4/8 cross-spelling rows fail (`ge-0-0-0.0`→`ge-0-0-0` want `.10`;
  `ge-0-0-0.1`→`ge-0-0-0.1` want `.50`; and reverse). Same-spelling green.
- `TestDefaultInstanceIngressExcludesEitherSpelling9815` (routing): RED,
  GO_EXIT=1 — 3/12 rows fail (dash-member-vs-slash-stanza bare, `.0`, `.1`).
  Reverse rows pass vacuously (verbatim dash coincides with dash stanza).
- `TestDashMemberEnslavesVLANChildInKernel9815` (`unshare -rn`): RED,
  GO_EXIT=1 — `.0` enslaved the PARENT, `.1` WARNed `not found`, children
  master index 0.
- `TestBoundDevicesAreNotDefaultIngressInEitherSpelling9815` (ordering
  invariant): PASS 12/12 on base, vacuously (both sides miss) — must STAY
  green; extended with a bare-aware assertion per adjudication R2-F2 (that
  extension is RED pre-fix — recorded at extension time).

## Plan (post-adjudication; reviewed by 2 hostile reviewers, see below)

ONE shared rule — stanza by linux name, exact-first (same-spelling twin
semantics: a cross-spelling member behaves EXACTLY as its same-spelling
twin):

1. NEW `LookupInterfaceByLinuxName(cfg, base) (key, ifc, ok)` in
   `pkg/config/interfaces_iter.go` (package function per file convention):
   exact `LookupInterface` first (#5886 nil doctrine), else first-sorted
   linux-name scan skipping nil slots. Doc pins HasUnit-only call-site
   placement (for bare refs base==member, a call there WOULD be whole-member
   alias matching), determinism, nil semantics. One-sentence file-header
   touch (commit-path use declared).
2. `logicalUnitDeviceKeyForRef` (`pkg/daemon/daemon_run_routehelpers.go:194`):
   stanza lookup through the helper. Base derivation unchanged
   (`LinuxIfName(s.Base)` equals the matched stanza's linux name). Heals
   singular + plural together (matrix asserts both agree).
3. `riMemberLinuxName` tunnel-alias probe (same function, unit path only):
   after `tunMap[s.Literal]` misses, if the helper matches a DIFFERENTLY
   spelled stanza, probe `tunMap[stanzaKey + TrimPrefix(s.Literal, s.Base)]`
   — the exact key the same-spelling twin probes (Literal suffix is
   canonical per Split docs; keys are `ifName + "." + Itoa`, types.go:572).
   No invented keys (R2F1-safe). Plural inherits via fan-out
   (`InterfaceUnitRefKeys` returns `[Literal]` for unit refs); tunnel rows
   assert singular==plural.
4. `resolveKernelIfNameWith` performs the same declared-spelling tunnel-map
   probe before its numeric/unit fallback, then uses the helper in the unit
   arm (`pkg/config/types.go:389`). Exact/dotted (:318), bare, xfrm, and IRB
   arms remain untouched. This heals scoping tunnel rows through
   `claimed[iif]` (default_instance_ingress.go:99).
5. `DefaultInstanceIngressIfaces` whole-port skip ONLY
   (`pkg/config/default_instance_ingress.go:73`): claim loop records
   `claimedBare` for bare-origin refs (`Split` `!HasUnit` — declared dotted
   names classify bare correctly); skip becomes
   `claimed[ifc.Name] || claimedBare[LinuxIfName(ifc.Name)]`. This
   `claimedBare` partition is the `#9810` resolved-claim delegate's
   conservative companion: it heals cross-spelling bare under-exclusion
   without changing unit-claim rules or over-excluding a reth physical
   member. Lenient slash/dash pairs merge by linux name; strict configs
   remain unaffected.

Named-test coverage in the shipped tests:
- daemon matrix: 8 VLAN stanza/member/unit combinations, plus 8 tunnel
  rows (per-unit and interface-level tunnels in both spelling directions),
  the kernel netns cell, and the 12-row bind-versus-default-ingress
  invariant.
- routing matrix: 12 stanza/member/shape combinations covering bare, `.0`,
  and `.1` claims, plus 8 tunnel rows (per-unit and interface-level tunnels
  in both spelling directions).
Deferred extensions remain out of scope: padded `.01`, malformed-verbatim,
  untagged/collapse, lenient-pair exact-first, determinism, nil-cfg, native
unit, dotted-declaration, and hardening rows.

Explicitly OUT (follow-up requests to Main, §Follow-ups): userspace FIB
alias (HIGH), bare cross-spelling BIND fan-down, dotted-base cross-spelling,
dash-of-dotted pin (#9821 owner), pre-existing dash/dash native-unit-0
scoping over-exclusion, InterfaceUnitRefKeys aliasing (#8829), zones/RA/
DHCP-RG/DDNS, strict warning text, multi-member/dotted hardening pins.

## Adjudication (R1 bind + R2 scope, both WEAKEN → all findings disposed)

R1-F1 (P1 tunnel NEITHER-set): ACCEPTED — alias probe (plan item 3).
Verified vs types.go:391-392 + :562-585; shipping a new divergence is
unacceptable. | R1-F2 (P1 resolver widening): narrowing REJECTED (contradicts
issue root-cause direction; helper is lookup-fallback, Split/parse untouched;
same-spelling byte-identical; cross-spelling fixward-only) — bounded audit
ACCEPTED (full config/daemon/routing/dataplane-userspace suites +
per-consumer before/after in log). Main may overrule. | R1-F3 (P2 dotted
residual): ACCEPTED — named + follow-up. | R1-F4 (P2 dash-of-dotted):
ACCEPTED as residual + follow-up — #9821-D16 precedent (daemon level-2
already reads dash-of-dotted as unit-of-undotted; resolver now matches
daemon; leftover slash-vs-dash asymmetry pre-exists in daemon). |
R1-F5 (P2 helper contract): ACCEPTED (key-returning signature, placement
rule, doc) — direct unit tests REJECTED per bounded rule, covered via named
rows. | R1-F6 (P2 matrix rows): ACCEPTED in bounded form. | R1-F7 (P3
method-vs-fn): ACCEPTED — package function.
R2-F1 (P1 bare over-exclusion): ACCEPTED, conservative variant (plan item 5)
— verified the naive skip would newly over-exclude native unit-0 cross
claims; dash/dash quirk preserved + follow-up. | R2-F2 (P1 invariant
bare-blind): ACCEPTED — bare-aware assertion. | R2-F3 (P2
necessary-not-sufficient): ACCEPTED — documented (partition unachievable
while bare fan-down OUT). | R2-F4 (P2 tunnel f-up): mechanism CORRECTED
(tunnel arm precedes VLAN arm — R1's trace verified; R2's VLAN-arm trace
rejected) — residual follow-up ACCEPTED. | R2-F5 (P1 userspace HIGH):
ACCEPTED — follow-up request; PR scopes claims to kernel+scoping (bind
verified unconditional, daemon_apply_interfaces.go:246 + :333-336). |
R2-F6 (P2 bare-divergence wording): ACCEPTED — wording corrected + follow-up.
| R2-F7 (P2 native cell): ACCEPTED.

## Follow-up requests (for Main to file; this lane ships kernel+scoping only)

1. HIGH: userspace FIB unit-member stanza-alias binding
   (`forEachRoutingInstanceInterfaceKey` + `quarantinedInterfaceKeys`;
   explicit-beats-fanout preserved) — else kernel-VRF vs userspace-FIB
   diverge on cross-spelling unit members (default dataplane is userspace).
2. Bare cross-spelling BIND fan-down (kernel via linux-matched stanza +
   userspace bare alias + scoping partition; netns cells both directions).
3. Dotted-base cross-spelling residual (both sides consistent-broken).
4. Dash-of-dotted reading pin (collision corpus; #9821 owner).
5. Userspace FIB interface-level-tunnel aliasing remains deferred; the
   kernel daemon and shared next-table resolver now handle both spellings.
6. Pre-existing dash/dash native-unit-0 scoping over-exclusion.
7. Hardening pins (multi-member, dotted-declaration, direct helper tests).

## Implementation evidence

The helper and both consumer paths landed in `e6afdddeb` after restacking
onto `32ce59ed0`: `LookupInterfaceByLinuxName` is exact-first, nil-safe, and
first-sorted by declared key for equal linux spellings. The daemon's unit
producer-key lookup and unit tunnel alias probe, plus the shared resolver's
tunnel alias probe, use the matched stanza; `resolveKernelIfNameWith` and
the shared default-ingress set use the same linux-spelling rule. Bare
scoping stores only the raw linux alias because `reth` resolution can map a
bare logical name to a different physical member.

STEP-0 on detached `origin/master` `ba885b0b1` reproduced the defect with true
exit codes: daemon matrix RED (`4/8` cross-spelling rows, `GO_EXIT=1`),
routing matrix RED (`3/12` rows, `GO_EXIT=1`), and the kernel netns cell RED
under `unshare -rn` (parent enslaved, unit 1 missing, VLAN children master
index `0`, `GO_EXIT=1`). The ordering invariant passed `12/12` vacuously.

RED-on-revert was demonstrated after the implementation commit: reverting
`daemon_run_routehelpers.go` alone made the daemon tunnel rows RED (`4/8`,
`GO_EXIT=1`); reverting only the new resolver tunnel probe to the prior PR
head left per-unit rows green but made both cross-spelled interface-level
tunnel rows RED (`2/8`, `GO_EXIT=1`). Restoring the committed files made all
tunnel rows GREEN. The earlier bind and scoping reverts remain independently
reproducible: daemon VLAN matrix `4/8` RED and routing VLAN matrix `3/12`
RED, both with `GO_EXIT=1`.

GREEN evidence: the targeted daemon matrix now has 16 rows, the ordering
invariant has 12 rows, the kernel netns cell passes, and the routing matrix
now has 20 rows. Full `go test ./pkg/config -count=1` passed (1 package;
4,875 top-level tests passed, 5 skipped; 215.056s); full
`go test ./pkg/routing -count=1` passed (1 package; 291 top-level tests
passed, 4 skipped; 0.691s). The daemon suite excluding the unrelated
master-baseline failure passed with 2,056 top-level tests, 8 skipped, and
0 failures in 91.363s. The known baseline test
`go test ./pkg/daemon -run '^TestCommittedDigestStableAcrossMTUSync9841$' -count=1`
fails on this branch and in an isolated `ba885b0b1` worktree with sync
config parse EOF at line 2. The #9815 daemon acceptance set is 29 cells: 16
VLAN/tunnel matrix rows, 12 invariant rows, and 1 netns cell.

## Salvage PR history

- `e6afdddeb` — `config: resolve cross-spelled RI members (#9815)`, restacked
  onto `32ce59ed0`; implementation plus fail-on-revert regression tests.
- Final history commit — this `docs/log/9815.md` evidence and follow-ups.
- PR opened with `Closes #9815` after the final history commit; URL is in the
  delivery report.

The remaining interface-level-tunnel residual is userspace-only:
`forEachRoutingInstanceInterfaceKey` and `quarantinedInterfaceKeys` still
need stanza-alias handling in the userspace FIB binder. The kernel daemon
bind path and shared default-ingress resolver now cover the per-unit and
interface-level tunnel spellings tested above.

## Eng9815d provenance (2026-09-15, adopted after verify)

Prior lane Eng9815c died to a model rate-limit with implementation unstarted
("Implementation evidence: TBD"); this lane adopts its worktree state after
independent verification. Verified this lane, all on base 609433efb:
- Full issue body via `gh api repos/psaab/xpf/issues/9815 --jq .body`
  (9695 chars; tail matches the filed research disposition) + comments
  (`--paginate --jq length` → 0). Named members: bare `ge-0-0-0`, `ge-0/0/0`;
  units `.0`/`.1` in both spellings; control port `ge-0-0-1`. The adopted
  matrix/netns/invariant cells cover exactly these — no named member omitted.
- Adopted test files are byte-identical to `/tmp/eng9815c-residue/` (sha256
  1824f978…/daemon, 89598f32…/routing). Residue log superseded by this file
  (diff reviewed; same RED claims, rewritten hygiene section).
- Behind-1 commit `1b5c976ed` is FRR test-only (#10129 measurement), touches
  none of the cited files — no rebase.
- STEP-0 RED re-derived with true exit codes: daemon matrix 4/8 cross rows
  RED (`.0`→parent, `.1`→`base.1`); routing 3/12 RED (dash-vs-slash bare,
  `.0`, `.1`); netns RED under `unshare -rn` (parent enslaved + WARN not
  found, children master 0); invariant vacuous PASS (must stay green).
- Both hostile reviews re-read in full (`agent://Eng9815c.PlanReviewBind`,
  `agent://Eng9815c.PlanReviewScope`; both WEAKEN, 7 findings each) and every
  adjudication premise re-verified against current sources (tunMap keys
  declared-spelling types.go:572; Literal suffix canonical
  schema_validators.go:287-293; tunnel arm precedes VLAN arm types.go:374-389;
  untagged-0 collapses to kernelBase types.go:398-400; unit refs fan out to
  [Literal] host_inbound_effective_view.go:77-80; bind unconditional
  daemon_apply_interfaces.go:246 + :333-336). Adjudication adopted with three
  bounded-rule deltas (no test-file change of any kind this lane):
  R1-F6 matrix-extension rows DEFERRED, R2-F2 bare-aware assertion DEFERRED,
  R2-F3/R2-F7 doc/native-cell DEFERRED — all as follow-up requests; the F1
  code fixes they would guard are covered by the fixed named cells + full
  suites. New-row-RED plan dropped: existing cells stay fixed, source-only
  diff after RED.
- Eng9810 re-coordinated over hub this lane: owns rules.go (PR #10140 open,
  unmerged); no objection to the minimal flagged hunk in this worktree;
  rebase must re-target to `pkg/config/default_instance_ingress.go`, keeping
  the subsumed-by-9810 flag comment.

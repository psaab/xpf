# #9767: a FullResync is acknowledged even when its export was dropped or withheld

Shipped together with #9766 (`docs/log/9766.md`). Both defects sit in one
transaction: `handleEventStreamFullResync` → `exportUserspaceOwnerRGSessionsWithConfig`
→ the session-sync send queue.

- **Timestamp**: 2026-09-11 08:30 PDT
- **Action**: Checked that the overlap blocker is gone. #9699 merged as PR #9779, and no
  open PR touches `daemon_ha_userspace_stream.go`, `daemon_ha_userspace_export.go` or
  `cluster/sync_conn_write.go`. #9630 is open and unassigned, with no PR.
- **File(s)**: none (read-only)

- **Timestamp**: 2026-09-11 08:40 PDT
- **Action**: Showed that the issue's minimal fix, "return false so the resync is
  retried", is unsafe on its own.
  - A false return queues the frame in `pendingCallbackFrames`.
  - `flushPendingCallbackFrames` then re-invokes `onFullResync` on every later frame's
    enqueue and on the 100 ms `ackLoop` tick. Each call re-runs the synchronous helper
    export.
  - `sendLoop` writes one message per `writeFull`, so an export burst larger than the
    4096-entry queue routinely outruns it.

  On an owned table bigger than the queue every retry would fail again, re-exporting on
  each incoming frame until `pendingCallbackFramesLimit` closes the stream. Reported to
  the lead with the chosen design (paced admission plus a retry backoff) and the heavier
  alternative (an acknowledged bulk re-drive, which overlaps #9630).
- **File(s)**: none (read-only)

- **Timestamp**: 2026-09-11 08:50 PDT
- **Action**: Implemented the fix.
  - `QueueSessionV4Paced`/`V6Paced` (cluster) wait for room instead of dropping. A
    disconnect ends the wait; an expired wait makes a final lossy enqueue, so the drop is
    counted as before.
  - `queueUserspaceSessionDeltasComplete` (daemon) runs the same #7194 schema gate and
    the same walk, through a paced sink. It returns an error unless every install was
    queued. After one install times out, the rest are queued without waiting.
  - The export uses it, so a withheld batch, a missed install or a disconnect declines
    the frame.
  - A failed attempt holds the export off for 1 s, so the frame's retries stop
    re-running the helper export.
  - Deletes keep the journal-on-full behaviour, which the connected sweep flushes
    (#3926).
  - Test seams: `SetConnectedForTesting`, `FillSendQueueForTesting`,
    `TakeQueuedMessageTypeForTesting`.
  - `TestExportUserspaceOwnerRGSessionsWithConfigQueuesForwardWireAlias` used a
    disconnected `SessionSync` and asserted the walk's count of ATTEMPTED sends, which
    is the #9767 defect itself. Its fixture is now connected, and it asserts that both
    sessions are in the queue.
- **File(s)**: pkg/cluster/sync_conn_write.go, pkg/cluster/test_seams.go,
  pkg/cluster/sync_queue_paced_9767_test.go, pkg/daemon/daemon.go,
  pkg/daemon/daemon_ha_userspace_stream.go, pkg/daemon/daemon_ha_userspace_export.go,
  pkg/daemon/full_resync_transaction_9767.go,
  pkg/daemon/full_resync_transaction_9767_test.go, pkg/daemon/userspace_sync_test.go

- **Timestamp**: 2026-09-11 08:54 PDT
- **Action**: Updated the docs.
  - `docs/session-sync-architecture.md`, "Bulk Owner-RG Export":
    - added the #9767 and #9766 paragraphs, including how the backoff interacts with the
      gap and decode-failure triggers, which ignore the handler's result;
    - corrected the section's stale `ExportOwnerRGSessions(rgIDs, 0)` / `max = 0`
      description (#9344 replaced it with the paged export);
    - corrected its "only live caller" claim (the #6031 cold-prime snapshot calls the
      same export).
  - The #7194 paragraph's "single chokepoint", and the same claim in the
    `queueUserspaceSessionDeltas` comment, now name two entry points.
  - `pkg/cluster/README.md`: new section "Paced session installs".
  - `pkg/daemon/README.md`: a sentence on the FullResync transaction.
- **File(s)**: docs/session-sync-architecture.md, pkg/cluster/README.md, pkg/daemon/README.md,
  pkg/daemon/daemon_ha_userspace_stream.go

- **Timestamp**: 2026-09-11 08:59 PDT
- **Action**: Added cells so the mutation matrix reaches the paced sink's v6 arm and its
  one-wait rule. The admission cell now runs a v4 and a v6 subtest; the pacing cell exports
  a v6 session after its two v4 sessions; and a new stalled-link cell checks that an export
  of five sessions into a queue that never drains holds the reader for about one install
  wait, not five.
- **File(s)**: pkg/daemon/full_resync_transaction_9767_test.go

- **Timestamp**: 2026-09-11 09:03 PDT
- **Action**: Ran the full `pkg/cluster/...`, `pkg/daemon/...`, `pkg/dataplane/userspace/...` and
  `pkg/refactoraudit/...` suites on the working tree (`GOFLAGS=-p=4`): exit 0, 6 packages ok. Then
  committed.
- **File(s)**: docs/log/9767.md

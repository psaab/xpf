# #9620: brace-elided multi-statement spellings that fold to their first statement

Entries marked "earlier this session" were recorded at 2026-09-11T06:54Z; their
exact times were not kept.

- **Timestamp**: earlier this session
  - **Action**: Re-ran every #9620 witness at `0e950bd5b` (master after #8921
    and #9657), strict and lenient against a braced control. All members were
    still live and #8921 changed none of them. Posted the table on the issue,
    with one comment on how #9620 differs from #9391: #9391 is the in-brace
    `(container, leaf-run)` axis, and #9620 is the brace-elided fold and
    container-head runs.
  - **File(s)**: none (probe files removed)
- **Timestamp**: earlier this session
  - **Action**: Traced each member to a mechanism by dumping the AST before and
    after `normalizeCompactStanzas`:
    - scope pairs that are not admitted, and containers without
      `packedStatements` (H1, H9, H10, M5, M6);
    - container-head runs that the splitter cannot hand back to an ancestor
      (H2, H11);
    - an instance-name pair that no scope entry can name (H7).
    Split the work into three PRs. Sent the coordinator the file list before
    editing any fold, scope or ratchet file.
  - **File(s)**: none
- **Timestamp**: earlier this session
  - **Action**: PR-B (H7) probe at `0e950bd5b`, both strict:
    - A top-level packed `group G peer-as 65001 neighbor 10.0.0.1` compiles
      correctly, so no `group` opt-in is needed.
    - `ri2 instance-type virtual-router protocols ospf area 0.0.0.0 interface
      ge-0/0/1.0;` compiles to `Interfaces=[ge-0/0/1.0]` and `OSPF=nil`.
    - `ri1 instance-type virtual-router protocols bgp group G description foo;`
      compiles to `Description="foo"` and `BGP=nil`.
    The first probe run printed nothing: `go test` hides a passing test's
    stdout without `-v`. It was scored VOID and re-run with `-v`.
  - **File(s)**: none
- **Timestamp**: 2026-09-11T06:54Z
  - **Action**: PR-B edit:
    - `compileRoutingInstances` now stops its Keys loop at the first
      body-bearing keyword.
    - `routingInstanceElidedBody9620` re-dispatches the tail from that
      keyword. The body is cloned, the #8921 masks are carried, and it is folded
      with the instance schema and `compactNormalizeInScope`.
    - This replaces the #9055 re-dispatch, which handled only a keyword in last
      position.
    - Added the docs section "Brace-elided routing instances".
  - **File(s)**: pkg/config/compiler_routing.go,
    pkg/config/compiler_routing_elided_body_9620.go,
    pkg/config/routing_instance_elided_body_9620_test.go,
    docs/config-schema.md, docs/log/9620.md
- **Timestamp**: 2026-09-11T06:54Z
  - **Action**: Targeted tests, scored by name from `go test -json`: 19 #9620
    names pass, plus the #8787 and #9055 tests. The 19 are the eight spelling
    cells in strict and lenient mode, the node-level equality test, and the
    tree-untouched test.
  - **Note**: no compiler on the routing path reads `KeysQuoted` or
    `KeysBracketed` today, so only the node-level cell can observe the mask
    carry.
- **Timestamp**: 2026-09-11T07:04Z
  - **Action**: Committed PR-B as `e5099cb54`. Created the detached mutation worktree `/var/tmp/wt-9620-mut` at that commit. Started the 11-mutant matrix (baseline: all three targets pass) and a read-only Codex review.
  - **File(s)**: none
- **Timestamp**: 2026-09-11T07:04Z
  - **Action**: Probed the #9323 closed-world gate at `e5099cb54`, strict and lenient.
    - `ri1 instance-type virtual-router bogus-kw foo;` and `ri1 instance-type virtual-router firewall family inet filter f1 term t then discard;` are accepted with no warning. The first-position and braced controls are refused.
    - `ri1 interface ge-0/0/1.0 nat source rule-set r;` compiles the NAT tokens as interface names, with warnings only.
    - Cause: `routingInstanceChildTokensOf9323` reads only `Keys[1]` of an elided instance. This predates PR-B, and the file is outside PR-B's diff.
    - Filed #9736 (Medium) with `model:claude-opus-5-1m` and `validated-by:research`. Added the model label to #9657 and #9685, which had been filed without it.
  - **File(s)**: docs/log/9620.md
- **Timestamp**: 2026-09-11T07:31Z
  - **Action**: Codex round 1 on `e5099cb54`, the compiler-side re-dispatch design (not pushed). It reported four findings. Each was measured through `configstore.CheckText` at the base `0e950bd5b` and at `e5099cb54`:
    - **Value positions:** a quoted `vrf-target`/`route-distinguisher` value was read as a body. Introduced by that design.
    - **`interface [ description protocols ]`:** compiles `Description="protocols"`. Identical at base, so pre-existing.
    - **Group expansion:** `apply-groups` under an elided body compiled a route with no next-hop. Already live at base in the #9055 shape.
    - **Validation bypass:** `hold-time 1` under an elided `protocols` body was accepted with `HoldTime=1`. Already live at base in the #9055 shape; the braced spelling is refused.
    - **#9323 first-keyword gate:** this is #9736.

    Root cause: the re-dispatch ran after group expansion and after SchemaValidate. The design was withdrawn. The measured table is posted on #9620. The local branch `fix/9620-packed-fold-residue` is kept for reference and not pushed.
  - **File(s)**: none
- **Timestamp**: 2026-09-11T07:31Z
  - **Action**: Redesign on `fix/9620-elided-instance-normalize`, branched from origin/master `f1937a261`:
    - The normalizer now rewrites a brace-elided routing instance into its braced shape. It runs from `normalizeCompactNodes` for `routing-instances` children.
    - `compileRoutingInstances` no longer reads the Keys run. The #8787 loop, the #9055 re-dispatch and both predicates were deleted, along with the #9055 predicate test.
    - The #9323 anti-drift test and the schema/gate comments now point at the schema.

    Checked before editing:
    - Live instance grammar: `description`, `instance-type`, `vrf-target` and `route-distinguisher` take one value; `interface` takes one value and is multi-valued; `vrf-table-label` takes none; `routing-options`, `protocols` and `interface-routes` are containers.
    - `groups` reaches the same `schemaRoutingInstances` pointer.
    - The #9323 prewalk runs after normalization (compiler.go:767 against :211).
    - Only three unrelated tests set `skipCompactNormalize`.
  - **File(s)**: pkg/config/compact_normalize_8662.go, pkg/config/compact_normalize_routing_instance_9620.go, pkg/config/compiler_routing.go, pkg/config/compiler_routing_children_9323.go, pkg/config/schema_routing.go, pkg/config/routing_instance_closed_world_9323_test.go, pkg/config/routing_instance_body_keyword_9055_test.go (deleted), pkg/config/routing_instance_elided_normalize_9620_test.go, pkg/configstore/routing_instance_elided_commit_gate_9620_test.go
- **Timestamp**: 2026-09-11T07:31Z
  - **Action**: The first split rule (a value keyword takes exactly its declared args) passed the targeted tests: 357 names, 0 failures. A parity probe then rejected it:
    - The braced `vrf-target export target:65000:1` commits. The elided spelling was refused, naming `target:65000:1` as an undeclared keyword.
    - The braced `ri1 { instance-type virtual-router extra; }` commits, so a value statement already takes trailing tokens.

    Switched to the one-line statement rule: a non-container keyword takes its values and every further token up to the next declared instance keyword. Posted the braced measurements on #9736, since its undeclared-after-value rows are that same trailing-token acceptance.
  - **File(s)**: pkg/config/compact_normalize_routing_instance_9620.go, both #9620 test files
- **Timestamp**: 2026-09-11T07:32Z
  - **Action**: Targeted tests on the one-line rule: 364 test names, 361 pass, 0 fail (3 skipped), 55 of them #9620 names. Scored by name from `go test -json` over pkg/config and pkg/configstore.
  - **File(s)**: none
- **Timestamp**: 2026-09-11T07:32Z
  - **Action**: Docs:
    - Added the section "Brace-elided routing instances" to docs/config-schema.md.
    - In the #9323 section, the compiler reference now names `compileRoutingInstances`.
    - Replaced the claim that `TestRoutingInstanceSchemaAndCompilerAgree9323` holds the schema and the removed predicate to the same set in both directions. The test was already one-directional by its own comment.
  - **File(s)**: docs/config-schema.md
- **Timestamp**: 2026-09-11T08:08Z
  - **Action**: Mutation matrix at `0072bb4a9`: all 11 mutants killed, 0 survivors, 17 of 17 cells matched the expected failure message.
    - One mutant did not build and was VOID: it deleted the extension loop, which left `authored` unused.
    - Re-expressed as `for false && …`, it was killed by both of its targets.
  - **File(s)**: none
- **Timestamp**: 2026-09-11T08:08Z
  - **Action**: Codex round 2 on `0072bb4a9`: six findings, each dispositioned. The `make test` run on `0072bb4a9` was stopped, since the head it tested was going to change.
    - **F1, High, fixed:** an inline apply statement after a value was absorbed into that value. `ri1 instance-type vrf apply-groups MISSING;` committed where the braced spelling refuses the undefined group. Apply keywords now end a statement and stand alone.
    - **F2, High, fixed:** quote and bracket masks moved a boundary, and text renderings drop them (HA sync, `load merge`, rollback files, `show configuration`). So `interface [ ge-0/0/0.0 protocols ]` would bind a different VRF membership on a peer. The split no longer reads provenance, as the scoped fold never did. The masks are still carried; the authored-value question stays with #9635.
    - **F3, Medium, moot for this change:** `show | compare` ignores masks. With a provenance-blind split, a bracket-only difference no longer changes compilation here. The general mask blindness of `nodesEqual` predates this change and sits in the #9635 area.
    - **F4, Medium, residual:** a packed run inside braces (`ri1 { interface ge-0/0/0.0 instance-type forwarding; }`) is not normalized. This is the in-brace leaf-run axis. #9391 already lists `routing-instances <ri> [description -> instance-type]`.
    - **F5, Medium, residual:** `FormatInheritance` expands groups without the normalizer. Probe: for a packed routing instance in a group, `FormatInheritance` omits `description inherited` while the compiled instance has it. With the group body braced, both show it. Filed as #9743 (Low).
    - **F6, Low, fixed:** stale comments in schema_routing.go, compiler_routing_children_9323.go and routing_instance_packed_8787_test.go, plus trailing whitespace in this log. docs/log/9323.md is history and was left as written.
  - **File(s)**: pkg/config/compact_normalize_routing_instance_9620.go, pkg/config/schema_routing.go, pkg/config/compiler_routing_children_9323.go, pkg/config/routing_instance_packed_8787_test.go, pkg/config/routing_instance_elided_normalize_9620_test.go, pkg/configstore/routing_instance_elided_commit_gate_9620_test.go, docs/config-schema.md, docs/log/9620.md
- **Timestamp**: 2026-09-11T08:08Z
  - **Action**: Targeted tests after the round-2 fixes: 373 test names, 370 pass, 0 fail (3 skipped), 64 of them #9620 names. Scored by name from `go test -json` over pkg/config and pkg/configstore.
  - **File(s)**: none

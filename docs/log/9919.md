# #9919: ipsec tolerant-path render cohort (3 members)

F-090 (dangling ESP reference fabricates `aes256-sha256`), F-161 (tolerant-path
dh-group silent drop + empty-keyword render), F-163 (`rand_time = 0s` de-jitter).
One render surface, one stated principle (#4298 never-fabricate), one ingress
(tolerant load / peer-sync). Base origin/master 32b1d0472.

## Plan reviews (2 hostile) + adjudication

Plan (ESP IKE-parity skip via new sentinel + 3-return resolve; DH InvalidSpec
compiler record + strict validator + render skip; rand_time deletion) reviewed
before implementing by HostilePlanA (20 findings) and HostilePlanB (7 findings):
26 adopted, 1 partial (A-M8 rand_time lab-measurement deferral — docs evidence
+ safety argument recorded, cluster measurement an explicit follow-up); no
member PLAN-KILLED.

- Negative/zero DH bypass (B1/A-B1, BLOCKER): parseDHGroup accepts "-5"/"0", so
  record-on-!ok alone misses them and `>0` gates go silent. ADOPTED as Option 1:
  compiler stores ONLY spellable numerics (classifyDHGroup), records everything
  else as InvalidSpec; renderer belt checks `!= 0 && !spellable` for
  direct-constructed structs. Covers negatives, zero, unlisted, unparseable.
- PFS override (A-B2, BLOCKER): proposal DH 99 + PFS 14 renders VALID today (PFS
  overrides); unconditional per-proposal check would newly skip it. ADOPTED:
  judge the EFFECTIVE group only — PFS configured-or-invalid decides alone,
  else per-proposal ANY-semantics; legacy form checks its single proposal.
- Empty IPsecPolicy exemption (B2/A-B3, BLOCKER): the new vpn→policy loop must
  skip "" (intentional default), mirroring the IKE validator. ADOPTED.
- ANY-semantics for DH (A-M5, MAJOR): drop bad-DH entries from multi-proposal
  lists like dangling refs (#3904); sentinel only when nothing remains
  renderable. ADOPTED (consistent with "like a dangling chain").
- Tests (B3/A-M1/A-M2/A-m5, MAJOR): pkg/config strict-reject + lenient-warn
  cells for the VPN loop (both lenient arms), CompileConfig end-to-end DH
  rejects (not just ValidateDHGroup units), ForNode peer-sync cell,
  healthy-sibling cells. ADOPTED.
- IKE-site DH wiring (B6, MAJOR): errDHGroupUnresolved must skip in the IKE
  branch, never abort the whole render. ADOPTED.
- Lenient flag registration + test (A-M3), gate placement adjacent to #9008
  (A-M4/B5), sorted loops (B5/A-m4), VPN-loop placement after policy loop
  (A-m8), NoPolicyStaysDefault 3-return keeper (B4/A-M9), docs/README rewrite
  (B7/A-M6 incl. #8844 subtest rename), present-but-empty "(empty)" marker
  (A-M7), chain-before-DH + DH-before-auth precedence (A-m1), distinct
  undefined-policy vs dangling-refs diagnostics (A-m2), defensive abort arm
  note (A-m3), nil guards (A-m7), builder citation fix (A-m6). ALL ADOPTED.
- rand_time lab measurement (A-M8, MAJOR): PARTIAL. Docs evidence is the
  official strongSwan swanctlConf (rand_time defaults to over_time = 10% of
  rekey_time); lab measurement needs the cluster lock and is out-of-lane.
  Omission of an optional setting is provably safe (worst case no-op), so
  proceed with omission + documented evidence; cluster measurement is a
  follow-up, not a gate.

## Change (per member)

- F-090: errESPChainUnresolved sentinel; resolveESPSettings → 3 returns, skip
  on dangling policy ref and dangling proposal refs (chain-before-DH);
  renderConfig resolves ESP before writing the block and skips on both ESP
  sentinels (stale SA torn down via #5494 rendered-set diff; no orphan
  secret); validateIPsecPolicyProposalReferencesStrict gains the vpn→policy
  link (empty exempt, sorted, appended after the policy loop, same lenient
  flag). The aes256-sha256 fallback block is deleted. 5 fallback-pinning
  tests converted to skip/sentinel pins; ECP Site 4 moved to the resolved
  chain. 11 unrelated fixtures (childname #5122, trafficselector #4098, SA
  index #9511) used dangling chains as stubs — given resolving prop1
  proposals (their subject is names/selectors, not dangling ESP).
- F-161: DHGroupInvalidSpec/PFSGroupInvalidSpec fields (`json:"-"`, #9008
  mirror); classifyDHGroup stores only spellable numerics at all 3 compiler
  sites; validateIPsecDHGroupsStrict (sorted, collect-all) wired after the
  lifetime gate with new lenientIPsecDHGroup flag (registered in
  lenientCompileOpts); errDHGroupUnresolved + dhGroupBad belt in both
  resolvers with effective-group + ANY-semantics + precedence rules above.
  Spelling gate given a {"dhgroup","14","19"} pair (pcp/ospfnet/refbw
  precedent): without an in-domain pair the three leaves fell out of
  `compared` (invalid probes now collapse to 0 + warning); registry
  unchanged after the pair (leaves back to COMPARED, nothing else moved).
- F-163: both `rand_time = 0s` emits deleted (IKE connection + ESP child);
  rekey_time renders alone so strongSwan's default 10% jitter applies.

## STEP-0 RED (base) → GREEN (fixed)

Repro cells (zz_9919_repro_test.go, since replaced by permanent files below)
all RED on base 32b1d0472, all GREEN after the fix:

- F-090 Test9919_F090_DanglingESPSkips (2 subtests): RED — dangling policy
  and dangling proposal refs rendered `esp_proposals = aes256-sha256`.
- F-161 Test9919_F161_UnlistedDHGroupSkips (4 subtests): RED — 99/17/PFS-99/
  IKE-99 rendered trailing-dash `aes256-sha256-`. Config half
  Test9919_F161_TolerantDHWarns (4 subtests): RED — tolerant load recorded no
  COMPILED dh-group warning (cfg.Warnings empty for dh-group). Narrower than
  "warned nothing": the store layer already logs raw schema violations
  (store.go schemaValidateExpandedTree slog.Warn), so the RED is the missing
  validator voice naming the value, not total silence.
- F-163 Test9919_F163_NoRandTimeZero: RED — render emitted `rand_time = 0s`
  beside both rekey_time lines.

Permanent pins: pkg/ipsec/dh_group_render_9919_test.go (6 tests, 17 subtests:
unlisted/negative/InvalidSpec skips, PFS-override both directions, partial
lists good+bad/bad+bad for IKE+ESP, healthy sibling, resolver sentinels,
rand_time absence) + converted pins in ipsec_test.go (3 resolver sentinel,
2 render skip with siblings, NoPolicy keeper, lifetime rand_absence) +
Site 4 resolved-PFS ECP. pkg/config/dh_group_tolerant_9919_test.go (4 tests:
tolerant warns incl. ForNode mixed-version, strict rejects + valid controls,
lenient registration, VPN-chain strict/lenient/empty) + #8844 subtest now
asserts record+warn.

## Fixture regenerations (measured, justified)

- pkg/config/testdata/flat_set_chain_losers_8939.txt: counts only,
  walked 112→111 / unmeasured 81→82, losers unchanged (same 3 rows). The
  mover is `security ipsec vpn arg1 ike`, whose placeholder `ipsec-policy
  xpfval` both arms now strict-reject via the new F-090 VPN loop
  (identical errors — a new gate making the synthetic placeholder
  unmeasurable, same as the pre-existing `manual` row from #4300).
- pkg/config/testdata/spelling_gate_members_7484.txt: UNCHANGED (dhgroup
  pair restores COMPARED; verified no diff after regeneration).

## Gates

- Full suites -count=1: pkg/ipsec GREEN (0.26s), pkg/config GREEN (223s).
- Named cells -count=5: all 9919 tests + converted keepers in both packages
  GREEN. gofmt clean, go vet clean on both packages.
- strongSwan default evidence: docs.strongswan.org swanctlConf + swanctl.opt
  (rand_time defaults to over_time = 10% of rekey_time); pinned fleet
  strongSwan is 6.0.5 (docs/ipsec-swanctl-load-proof.md). Cluster
  load-measurement of the omitted default is a follow-up (see A-M8).
- Scope: pkg/ipsec (ike.go, policy.go, README) + pkg/config (compiler,
  validators, opts, gates, types, 2 tests, 1 fixture, spelling pair). No
  show-surface change (dh=group0 on the proposal display reflects the
  compiled not-configured state; authored value stays visible in warnings +
  `show configuration`) — out of cohort, noted here.

# #9916: logging/eventengine/feeds lifecycle cohort (5 members)

## Brief + plan reviews (2 hostile)

- Brief at docs/pr/9916/brief.md (untracked, not shipped). 2 hostile plan reviews:
  PlanReviewAlpha (F-056 READY; F-131 NEEDS-MINOR metric justified; F-132 NEEDS-MAJOR
  test deadlocks; F-133 NEEDS-MAJOR join KILLED as applySem deadlock; F-135 NEEDS-MINOR
  show guards required), PlanReviewBeta (all 5 NEEDS-MINOR; F-131 kill pkg/api emission
  as never-scraped; F-132 single beforeSend seam; F-133 join+suppress both necessary;
  F-135 show guards required). No PLAN-KILL of any member direction. No second round:
  all deltas are deletions or test redesigns on production shapes both agreed sound.
- No overlap with sibling lanes (Go lifecycle files vs Rust/test lanes; verified file
  ownership — this lane owns the 7 files below plus 5 test files).

## Adjudication (all adopted)

- F-056: retain h.mu as publication mutex for the shared pointer (not dead —
  repurposed for zero-value-safe lazy alloc); document shared SetClients/Close
  semantics (derived affects root; daemon calls root only). Sweep found no
  snapshot-pinning tests (no existing WithAttrs/WithGroup cells).
- F-131: ADOPT Beta — keep DroppedShutdown in Stats/counters + gated INFO log, DROP
  pkg/api emission (shutdown counter increments at exit, never scraped; no descriptor
  churn). ADOPT Alpha — pin the runAction abort increment with a dedicated held-lock
  cell + distinct-policy drain fixture (#5853 collapses same-policy).
- F-132: ADOPT Alpha — single afterDrainFn seam + direct-barrier test; KILL
  beforeSendFn (attacker holding mu while paused deadlocks stop before the worker
  reaches afterDrainFn; Beta's single-beforeSend has the same hole). ADOPT Beta's doc
  reframing (mu closes the enqueue orphan window; re-drain catches the mu-bypassing
  barrier + defense-in-depth). Warn moved outside mu (no logging I/O under retire lock).
- F-133: ADOPT Alpha — KILL the Apply join; suppress-only. Verified: Apply runs under
  applySem (reconcileFeeds inside applyConfigLocked) and onUpdate blocks on applySem
  (onFeedUpdate→applyActiveConfigResult); joining an old loop in the guard-to-onUpdate
  window (passed guard pre-swap, swapping, then publishing) wedges permanently, and the
  suppress guard cannot close that window (onUpdate is outside m.mu by necessity). The
  residual window is content-safe (carried snapshot + idempotent apply) and deadlock-free
  without a join. ADOPT Beta minor — recheck currency before the second publishedHash
  advance. No Apply restructure; join test dropped.
- F-135: guard both show loops (cli_show_routing.go, server_show_events.go — 2 lines;
  fail-closed evaluate + panicking display is incoherent); Apply-before-evaluate in test
  (else vacuous). Fail-closed (not skip-nil): nil in a non-empty list is malformed.
  Re-verified unreachable: compiler always appends non-nil; no JSON/gob decode of
  EventPolicy exists.

## Change (5 commits, fix+test each)

- F-056: syslogClientSet shared by pointer (like forwarding); h.mu guards lazy
  publication; every clients access under shared.mu. Derived loggers observe swaps.
- F-131: DroppedShutdown in Stats/counters (NOT Prometheus) + Close drains under
  enqueueMu after Wait (single drainer; second Close no-ops) + runAction abort counts;
  docstring rewritten (abandon-explicitly, not drain-and-run: 64×60s risks TimeoutStopSec).
- F-132: mu serializes check+send vs retire; stop re-drains (collect under mu, write
  outside — no disk I/O under lock); afterDrainFn seam (nil in prod, retire path only).
- F-133: staleness pointer-guard in installSnapshot + recordFailure (orphan → Debug +
  return); recheck before publishedHash advance. Apply untouched.
- F-135: nil pre-pass fails closed in withinMatches; prune skips nil (60s default);
  show loops skip nil.

## STEP-0 RED (pre-fix) → GREEN (post-fix)

- F-056 TestSyslogDerivedSeesReconfigure9916: RED on base (derived pins [A], want [B]).
- F-131 TestEngineCloseAccountsQueued9916: RED on base (depth 8, want 0); post-fix also
  asserts DroppedShutdown==8 + idempotent re-Close. TestEngineCloseAbortsRetryCounted9916
  (abort half): GREEN post-fix (DroppedShutdown==1); base would silently return (no counter).
- F-132 TestAsyncWriterNoOrphan9916: RED on base+seam (seam is behavior-preserving nil-check;
  pure-base stress is 0/2000 per issue — not deterministic): barrier orphaned, ack never
  closed. GREEN via re-drain.
- F-133 TestStaleFetchDoesNotPublish9916: RED on base (stale install published, calls 2 want 1;
  stale hold-drop likewise). Live-install control GREEN throughout (no over-suppression).
- F-135 TestNilWithinClauseFailsClosed9916 (3 subcases): RED on base (nil panic on every shape).
  Post-fix: no panic, evaluateEvent returns empty (fail closed).

## Gates

- Full suites -count=1: pkg/logging, pkg/eventengine, pkg/feeds, pkg/cli, pkg/grpcapi,
  pkg/api — all GREEN. pkg/daemon GREEN except pre-existing TestCommittedDigestStableAcrossMTUSync9841,
  which fails identically on pure base fcfdafcb5 (verified via detached worktree, since
  removed) — MTU-sync digest, unrelated to this lane; left untouched per bounded scope.
- Named cells -count=5 per package (all 9916 tests): GREEN. -race on logging + eventengine
  9916 cells: GREEN. gofmt/vet clean on all touched packages.

## Parent-review follow-up (dual NEEDS-MINOR, 10 findings, all folded)

Both reviewers: production sound, no new loss/deadlock/ordering violation,
killed-join justified. All findings were test-coverage/doc honesty. Rebased onto
origin/master a66208f48 at scrub (superset of the requested 083c4ca08, which had
advanced by #9902 mid-fold; clean rebase, no conflicts); gates re-run after the
rebase (below).

1. GPT-1 (F-132 serialization unpinned): new TestAsyncWriterEnqueueSerializedAgainstStop9916
   drives the real orphan race through enqueue via a beforeSendFn seam (pause holding mu,
   stop blocks, resume → handled exactly once) plus a TryLock mechanism pin (no sleep).
   Revert check (mu disabled, seams kept): new cell RED (TryLock pin fires), old barrier
   cell GREEN — confirming the parent's point and the pin. Production cost: one nil-check
   on the enqueue path; seam must not call back into enqueue/stop (documented).
2. GPT-2 (F-056 no race coverage): new TestSyslogConcurrentReconfigureRaceFree9916 hammers
   SetClients vs WithAttrs/WithGroup/Handle/Clients (severity-none clients, no network).
   Base + -race: DATA RACE + FAIL. Fixed + -race: GREEN.
3. GPT-3 (F-133 same-name + clock): same-name replacement subcase added (orphan predecessor
   suppressed after its name is re-added with a fresh pointer — pointer-identity, not
   name-existence; GREEN-only, mechanism shared with the removal RED). Hold-drop now uses
   the injected m.now step clock (1s/tick) instead of assuming two real-time calls advance
   >1ns. Full cell RED on base, GREEN on fix.
4. GPT-4 (F-135 latch hiding): rewritten to assert EVERY synchronous evaluation fails closed
   across 5 events including the threshold-crossing (index 1 for TriggerOn:2), plus a
   HandleEvent probe-path smoke (no queue assertion). Still panic-RED on base, GREEN on fix.
5. GPT flake note (abort test): TestEngineCloseAbortsRetryCounted9916 no longer polls
   cumulative Retried under a 10s deadline; the injected retry timer is a barrier (signals
   on park, never fires — stopCh abort is the only exit). fastRetry dropped (60s default).
6. SPARK-1 (caller-slice alias): SetClients now copies (append-nil idiom, nil-preserving)
   with a no-alias contract comment; new TestSyslogSetClientsCopiesSlice9916 pins root +
   derived (FAIL on base, which also stored verbatim — hardening pin).
7. SPARK-2 (re-drain overclaim): comment narrowed — catches landings before THIS mu
   acquisition only; post-release direct sends still orphan (acceptable: production has no
   mu-bypassers; syncForTest's post-stop send is benign, waiter already returned).
8. SPARK-3 (abort RED): revert procedure run live — abort increment removed → abort cell RED
   (DroppedShutdown 0, want 1); restored → GREEN. No base RED possible (field absent =
   compile error, not RED); recorded here as revert-pinned.
9. SPARK-4 (publishedHash recheck): new TestPublishedHashRecheckSkipsStaleAdvance9916 injects
   the swap inside onUpdate (deterministic concurrent-Apply-during-publish); orphan not
   marked published, replacement inherits nothing. RED on base (unconditional advance),
   GREEN on fix.
10. SPARK-5 (show guards): trivial cells in both surfaces (nil clause skipped, valid sibling
    still renders; panic-RED on base, GREEN on fix). Recorded asymmetry (out of F-135 scope,
   fix NOT attempted): a nil POLICY entry still panics both show loops at ep.Name while
   evaluateEvent nil-guards pol — nil-policy display hardening is unfiled.

Follow-up gates (post-rebase onto a66208f48): full suites -count=1 (logging, eventengine,
feeds, cli, grpcapi, api) GREEN; daemon GREEN except the same pre-existing 9841 failure
(re-verified on clean a66208f48 via detached worktree — fails there too, MTU-sync);
named 9916 cells -count=5 per package GREEN; -race on logging + eventengine 9916 cells
GREEN; gofmt/vet clean.

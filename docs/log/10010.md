# Issue #10010 — make deploy day-0 validation messaging truthful

- **Issue:** #10010 — `deploy: dry-run asserts check-config validation that never ran`
- **Branch:** `fix/10010-deploy-dryrun`
- **Worktree:** `/home/ps/git/pi-xpf/.claude/worktrees/10010-deploy-dryrun`
- **Context read:** Read the complete issue body and all comments before implementation. The issue had no comments; no scope withdrawal, narrowing, shipped notice, or contradiction was present.

## Change

`build_config_drive` now says `not validated (dry-run)` in its dry-run plan. The dry-run path intentionally remains side-effect free: it does not invoke `xpfd check-config` or read the config, so it must not claim that validation occurred. On the real path, an absent `xpfd` now reports `day-0 config not validated on build host` while retaining the first-boot validation note. The existing success and rejection behavior when `xpfd` is present is unchanged.

Regression cells in `scripts/deploy/test_xpf_deploy_iso_mode.py` capture the operator output for both paths. Each cell is RED if the old claim or vague missing-tool warning is restored.

## RED then GREEN proof

- **RED (dry-run message cell):** Before the source fix, the new regression failed because output contained `(label xpf-config, check-config validated)` and did not contain `not validated (dry-run)`.
- **RED (missing-xpfd message cell):** Before the source fix, the new regression failed because output contained `skipping build-host validation` and did not contain `day-0 config not validated on build host`.
- **GREEN (full affected module):** `python3 scripts/deploy/test_xpf_deploy_iso_mode.py` — 4 tests passed, including both new validation-message cells and the existing owner-only ISO cells.
- **GREEN (direct behavior repro):** The dry-run output no longer contains `check-config validated`; the missing-`xpfd` output explicitly reports that the day-0 config was not validated on the build host.

## Verification

- All 21 deploy test modules matching `scripts/deploy/test_xpf_deploy_*.py` passed: **293 tests, 0 failures**.
- `git diff --check` passed before the code commit.

- **Code/tests commit:** `8bd97ab4fffad617954e9a0156119fe0a1d57bdc` — `fix: make deploy day-0 validation messaging truthful (#10010)`.

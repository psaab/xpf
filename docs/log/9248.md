# #9248 -- three ungraded findings from the #9075 validation

- **Timestamp**: 2026-09-10
  - **Action**: Graded each by tracing its consumer at master.
    (1) pkg/feeds `0.0.0.0/0`: feed prefixes are merged into the address sets
    userspace policy lowering matches (policies_lower.go dynamic-address
    bindings, policies_addrbook.go feed overlay), so a default-route line in a
    remote feed widens every policy on that dynamic address to "any" -- a
    content-controlled fail-open (permit) or blackhole (deny). Graded real.
    (2) shouldSyncAtBulkStart default `true` for a zone absent from the bulk
    zone snapshot: ReconcileClusterBulk SKIPS (keeps) sessions whose zone
    reports true, so the default is the conservative direction; and absence is
    normal -- the snapshot enumerates only zones in zoneRGMap, and a zone with
    no RG binding holds node-local sessions a peer reconcile must never delete.
    The live ShouldSyncZone answers IsPrimaryFn() for such a zone, so the
    reconcile default is more conservative than the live predicate, never less.
    Worst case: a stale session in an RG-unmapped zone ages out instead of
    being reconciled. Graded not a defect; no change.
    (3) docs/reth-mac.md byte 5 "reserved" and "both nodes present the same
    MAC": config.RethVirtualMAC builds `02:bf:72:CC:RR:NN` with the node id in
    byte 5 and the cluster.RethMAC comment documents the per-node design, so
    CLAUDE.md is right and the doc is stale. Graded doc defect.
  - **File(s)**: none
- **Timestamp**: 2026-09-10
  - **Action**: (1) parseFeed refuses a `/0` prefix, counted and sampled as an
    invalid line so the feed reports degraded; a feed of only the default route
    keeps its last-good set; short real prefixes still install. Tests for all
    three. (3) docs/reth-mac.md corrected to the per-node MAC, the shared
    StableRethLinkLocal router identity, and per-node examples.
  - **File(s)**: pkg/feeds/feeds.go, pkg/feeds/default_route_9248_test.go,
    pkg/feeds/README.md, docs/reth-mac.md, docs/log/9248.md
- **Timestamp**: 2026-09-10
  - **Action**: CORRECTION to the item 2 grading above. It said an RG-unmapped
    zone "holds node-local sessions a peer reconcile must never delete", and
    that was WRONG. The live ShouldSyncZone falls back to IsPrimaryFn for an
    unmapped zone, and the daemon wires IsPrimaryFn to IsLocalPrimary(0): such a
    zone is governed by RG 0. On an RG 0 secondary its sessions are peer-owned,
    and the reconcile's absent-zone default of true keeps a stale one the
    authoritative bulk omitted. An empty zone snapshot also skips reconcile
    entirely. Item 2 is a real HA session-sync defect, filed as #9655 (needs
    make test-failover); no BEHAVIOURAL change to pkg/cluster in this branch --
    its only pkg/cluster edit is the StableRethLinkLocal comment in reth.go.
  - **File(s)**: docs/log/9248.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex hostile review (MERGE NO) findings on items 1 and 3,
    all fixed. (a) HIGH: `::ffff:0:0/96` passed a mask-length check and
    rendered as `0.0.0.0/0` -- the refusal now tests the CANONICAL string that
    would be installed, with a mapped-form cell. (b) LOW: a feed of only
    default routes returned the generic zero-prefix error -- it now names the
    refused entries, and the README states the last-good set follows the
    feed's normal failure handling including hold-interval expiry. The README
    also states the boundary honestly: this guards an accidental whole-space
    line, not a hostile provider listing /1 halves. (c) LOW: the stable
    link-local is `fe80::bf72:CC:RR` (StableRethLinkLocal builds
    fe80::0:0:bf72:CC:RR), not `fe80::bf:72:CC:RR` -- corrected in
    docs/reth-mac.md and in the pkg/cluster/reth.go comment the doc had copied.
    Branch collapsed to one commit before pushing so the wrong item 2 grading
    does not land in master's commit messages; this log keeps it.
  - **File(s)**: pkg/feeds/feeds.go, pkg/feeds/default_route_9248_test.go,
    pkg/feeds/README.md, docs/reth-mac.md, pkg/cluster/reth.go,
    docs/log/9248.md
- **Timestamp**: 2026-09-10
  - **Action**: Codex round-2 review (MERGE NO on three LOW doc-accuracy
    findings; it confirmed all four round-1 dispositions). All fixed. (a)
    docs/reth-mac.md Impact still said neighbour caches stay valid across a
    failover and that both nodes derive one EUI-64 link-local, and the
    Implementation table gave the old two-argument RethMAC; it now says the
    per-node MAC changes on failover, the takeover GARP / unsolicited-NA burst
    refreshes caches, and RethMAC takes (clusterID, rgID, nodeID). (b)
    pkg/feeds/README.md said every refused line shows in InvalidLines,
    InvalidSample and Degraded. A response of ONLY refused entries is a fetch
    failure, so recordFailure keeps the last-good snapshot's parse-quality
    fields and only LastError names it; the README and commit message now say
    so. (c) The item 2 correction above said "no change to pkg/cluster"; the
    reth.go comment is one, now stated.
  - **File(s)**: docs/reth-mac.md, pkg/feeds/README.md, docs/log/9248.md
- **Timestamp**: 2026-09-10
  - **Action**: Mutation matrix on commit 1b7b0cb3b (go test -json, scored by
    test name AND expected failure message; each mutant's anchor confirmed to
    occur exactly once). Baseline: 4 of 4 tests pass, none missing. 5 of 5
    mutants killed by their target test with its expected message: M1 refusal
    branch disabled (TestFeedRefusesTheDefaultRoute9248 and
    TestFeedOfOnlyTheDefaultRouteKeepsLastGood9248), M2 refusal widened to
    short prefixes (TestFeedKeepsShortRealPrefixes9248), M3 refused line not
    counted (TestFeedRefusesTheDefaultRoute9248), M4 mask-length check instead
    of the canonical string (TestFeedRefusesAMappedDefaultRoute9248), M5
    generic zero-prefix error (TestFeedOfOnlyTheDefaultRouteKeepsLastGood9248).
    The mapped-form test also failed under M1-M3, as collateral. The round-2
    edits are docs only, so the matrix still describes the code.
  - **File(s)**: none

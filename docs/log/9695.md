# #9695 (and #9694) — afxdp docs and comments asserting behaviour the source contradicts

- **Timestamp**: 2026-09-10
  - **Action**: #9695, three texts about what happens to an mlx5 zero-copy
    frame on `XDP_PASS` or on fill-ring starvation.
    - `userspace-dp/src/afxdp/umem/README.md`. The #209 mitigation no longer
      claims to "replace every `XDP_PASS` path". It covers IP frames only,
      and only while the cpumap flag is set. Two shim paths still return
      `XDP_PASS`: `pass_non_ip_l2_direct` (every non-IP frame, deliberately,
      because cpumap breaks ARP) and `cpumap_or_pass` (flag unset, or the
      redirect failed). So if the #9043 premise holds, the mitigation does not
      cover them. The #9043 premise itself stays marked UNVERIFIED; this
      change does not decide it.
    - `userspace-dp/src/afxdp/worker/lifecycle.rs`. Fill-ring starvation is
      described as the posting-and-drop behaviour `tx/rings.rs` records
      (`rx_xsk_buff_alloc_err`, packets dropped), not a fallback to the
      regular NAPI path.
    - `docs/afxdp-packet-processing.md`. The incident record stays (RSTs tore
      down the connection). It no longer asserts the NAPI fallback as the
      mechanism, says the path to the kernel was not established, and cites
      upstream v6.18 mlx5e (`en/rx_res.c`, `en/xsk/rx.c`). The vendor-kernel
      caveat is kept.
  - **Action**: #9694. The session-miss policy-tuple comment in
    `userspace-dp/src/afxdp/poll_descriptor/mod.rs` said static DNAT preserves
    the L4 port. It now says a static rule that maps a port carries the
    rewrite: `static_nat.rs` sets `rewrite_dst_port` from `mapped_port`,
    which `policy_dst_port` reads. Only a whole-address static rule and NPTv6
    keep the original port.
  - **Action**: Acceptance. Over tracked files, both `git grep -n 'replaces
    every .XDP_PASS'` and `git grep -n 'non-XSK) NAPI\|non-XSK NAPI'` return
    nothing. The changes are comments and docs only, so there is no runtime
    change.
  - **Action**: rustfmt. `rustfmt --edition 2024 --check` (the crate's
    edition; there is no rustfmt.toml) on `poll_descriptor/mod.rs` and
    `worker/lifecycle.rs` exits 1 with 115 diffs, because it walks the whole
    module tree. It reports the IDENTICAL 115 diffs, with identical bodies, on
    untouched master. None of them falls in the edited comment lines, so the
    formatting drift is pre-existing and this change adds none. An earlier
    draft of this entry said rustfmt "passes"; that was the exit status of an
    `echo`, not of rustfmt.
  - **File(s)**: userspace-dp/src/afxdp/umem/README.md,
    userspace-dp/src/afxdp/worker/lifecycle.rs,
    userspace-dp/src/afxdp/poll_descriptor/mod.rs,
    docs/afxdp-packet-processing.md, docs/log/9695.md
- **Timestamp**: 2026-09-10
  - **Action**: `make test` exited 0 on both legs on integration tree
    84f4c4f1c. That tree is origin/master 2c29dc14f merged with 6 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9603, #9340, #9512, #9695, #9697, #9503.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5831 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9695.md

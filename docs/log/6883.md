# #6883 — fixture pkt_len drift (frame.len() - 14 vs the full frame)

- **Timestamp**: 2026-08-27
  - **Action**: Corrected 78 fixture sites that built `pkt_len` from
    `frame.len() - 14` while the XDP shim stamps the full frame length
    (`packet_len = data_end - data`, `userspace-xdp/src/lib.rs:566`). Added a
    source-scanning guard so the convention cannot be re-copied between sibling
    fixtures.
  - **Deliberately NOT changed**: 4 sites in `tests_mss_inject_inspect.rs` where
    `frame.len() - 14` is the CORRECT expression for an IPv4 total-length or
    IPv6 payload-length wire invariant, which genuinely excludes the Ethernet
    header. The guard matches only the `pkt_len` sinks for this reason.
  - **File(s)**: 18 fixture files under userspace-dp/src/,
    userspace-dp/src/afxdp/pkt_len_fixture_drift_6883_tests.rs (new),
    userspace-dp/src/afxdp/mod.rs

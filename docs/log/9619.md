# #9619 — a shared cluster commit strict-validates only the origin's node view

- **Timestamp**: 2026-09-11
- **Action**: Run the whole strict commit pipeline on the peer node's effective view
- **File(s)**:
  - `pkg/configstore/peer_strict_pipeline_9619.go` (new): `validatePeerStrictPipeline`, `peerStrictError`
  - `pkg/configstore/store.go`: `compileTreeStrict` calls it after the #5876/#4785 registry, on the #6861 F2 rewritten clone
  - `pkg/config/compiler_peer_effective.go`: exported `PeerNodeID`; doc points at the new step
  - `pkg/configstore/check.go`: `CheckText` doc
  - `pkg/configstore/typed_leaf_lenient_test.go`: `TestCommitCheck_AcceptsGroupDefinedForwardingClass` fixture defines `groups node1` too
  - `pkg/configstore/peer_strict_pipeline_9619_test.go` (new): 11 cells
  - `pkg/configstore/README.md`, `docs/config-schema.md`

## Re-verified at origin/master 931ea3a65

The issue's executed probe still holds on current master: `compileTreeStrict`
runs schema validation, the strict compile, `crossCheckNodeID` and
`crossCheckRAIntervals` on the submitting node only, then
`ValidatePeerEffectiveStrict`, which compiles the peer leniently and runs two
registered subjects (SNAT, IPIP), returning nil when the peer view does not
compile.

## Change

`validatePeerStrictPipeline(peerTree, nodeID)` repeats the local strict steps
for the peer id: `schemaValidateExpandedTreeForNode`, `config.CompileConfigForNode`,
`crossCheckRAIntervals`. Errors are `chassis cluster peer node<N>: ... (#9619): <gate error>`.

- The registry still runs first, so the #5876/#4785 cells keep their messages.
- The tree is the existing #6861 F2 clone (`rewriteRetiredDataplaneType(SyncCaller)`).
- `crossCheckNodeID` is NOT run for the peer. Measured first with it included:
  7 of 8 new configstore reds and the one pkg/daemon red
  (`TestBootstrapFromFileAcceptsKeyedCluster_6611`) were all "node identity
  mismatch: the node-id file / -node-id is 1 but 'chassis cluster node' resolves
  to 0" — configs authored for one node with a literal `node 0` leaf, which
  check-config and bootstrap accept on that node by existing decision
  (#4185/#6611/#8444 cells). The check compares against the checking host's
  node-id file, which the origin does not hold for the peer. Two of those reds
  (`ha_cluster_conf_8280_test.go`) came from the test textually replacing
  `${node}` with `node0`, which makes node1 apply node0's group; the real
  shipped file passes for both node ids (new cell).
- The eighth red, `TestCommitCheck_AcceptsGroupDefinedForwardingClass`, was a
  clustered store (node 0) with `apply-groups "${node}"` and only `groups node0`
  defined: node1's view names an undefined group, which node1's `SyncApply`
  cannot load (pinned as a precondition in
  `TestPeerViewThatDoesNotCompileIsRejected_9619`). The fixture now defines
  node1's group as well; the cell's intent (a group-defined forwarding class
  passes the strict gate) is unchanged.

## Findings recorded, not fixed here

- The issue's control value `reth-advertise-interval 40959` is itself rejected
  on both nodes: the schema admits 10..40959 but the #9039 compile gate
  (`MaxRethAdvertiseInterval = 40950`) refuses 40951..40959. The control cell
  uses 40950, and 40955 became the compile-only witness
  (`TestPeerOnlyCompileGateRejected_9619`).

## Validation

- `go test ./pkg/configstore/` at 68d624dfa: 796 passed, 0 failed.
- `TestBootstrapFromFileAcceptsKeyedCluster_6611` (pkg/daemon) passes after the
  identity cross-check was dropped from the peer steps.
- `go test ./pkg/refactoraudit/ -run TestTouchedFileCrossedModularityThreshold`: ok.
- Broad run (`pkg/daemon pkg/grpcapi pkg/cli pkg/api pkg/cluster cmd/...`, taken
  while `crossCheckNodeID` was still included): 7793 passed, 1 failed, the
  identity-class red above.

## Mutation matrix (head 68d624dfa, `go test -json` scored by test name)

NOOP: CLEAN, 45 cells ran (`_9619|_5876|_4785|6611|4185|NodeID|8280|8444|GroupDefinedForwardingClass|4525|RAInterval`).

| Mutant | Change | Target | Verdict |
|---|---|---|---|
| M1 drop-call | `validatePeerStrictPipeline` result ignored in `compileTreeStrict` | `TestPeerOnlyValueRejectedAtOriginCommit_9619` | KILLED |
| M2 raw-tree | pipeline handed `tree`, not the rewritten clone | `TestPeerOnlyRetiredDataplaneTypeStillCommits_9619` | KILLED |
| M3 skip-schema | peer schema error ignored | `TestPeerOnlyValueRejectedAtOriginCommit_9619` | KILLED |
| M4 skip-compile | peer compile error ignored | `TestPeerOnlyCompileGateRejected_9619` | KILLED |
| M5 skip-ra | peer RA ratio error ignored | `TestPeerOnlyRAIntervalRatioRejected_9619` | KILLED |
| M6 local-id | schema + compile run for `nodeID`, not `peerID` | `TestPeerOnlyValueRejectedAtOriginCommit_9619` | KILLED |
| M7 standalone-guard | `!ok` guard disabled | `TestPeerStrictPipelineStandaloneNoOp_9619` | KILLED |
| M8 error-drops-peer | message loses `peer node<N>` | `TestPeerOnlyValueRejectedAtOriginCommit_9619` | KILLED |
| M9 peer-identity-check | `crossCheckNodeID` added for the peer | `TestCheckTextNodeIDMismatchRejected` | KILLED (also 6611, 8280, 8444 cells) |
| M10 lenient-peer-compile | `CompileConfigForNodeLenient` for the peer | `TestPeerOnlyCompileGateRejected_9619` | KILLED |
| F1 fixture-revert | node1 group removed from the forwarding-class fixture | `TestCommitCheck_AcceptsGroupDefinedForwardingClass` | KILLED (first run VOID: anchor matched twice; re-run with a unique anchor) |

Every cell restored the tree (`git status` clean after each).
- **Timestamp**: 2026-09-11
  - **Action**: `make test` exited 0 on both legs on integration tree
    ddb0df0d5. That tree is origin/master 580d959e0 merged with 6 pairwise
    file-disjoint branch heads, this one included, and each head's master
    delta has an empty intersection: #9619, #9415, #9478, #9601, #9365, #9685.
    - Go leg: 77 packages ok, and there are no FAIL or panic lines.
    - Rust leg: 5838 passed, 0 failed.
  - **Action**: Landed by the coordinator. The merged tree was compiled (go
    build and vet) before the merge.
  - **File(s)**: docs/log/9619.md
